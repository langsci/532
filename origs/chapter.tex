\documentclass[output=paper]{langscibook}
\author{Authorname\orcid{}\affiliation{}}
\title{Title}
\abstract{Abstract}
\IfFileExists{../localcommands.tex}{
  \addbibresource{../localbibliography.bib}
  \input{../localpackages}
  \input{../localcommands} 
  \input{../localhyphenation} 
  \togglepaper[1]%%chapternumber
}{}

\begin{document}
\maketitle 
%\shorttitlerunninghead{}%%use this for an abridged title in the page headers
% ATTENTION: Diacritics on the following phonetic characters might have been lost during conversion: {'ə'}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Semantically Vacuous Elements in German: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Adjectival Inflections and the Article \textit{ein}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Dorian Roehrs
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Table of Contents
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Preface                  viii
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Abbreviations                 x
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Chapter 1: Introduction              1
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.  Vacuous Elements – an Imperfection of Language?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  1.1.  The Clause
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  1.2.  The Noun Phrase
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.  German as the Language under Investigation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  2.1.  Some Cross-linguistic Differences
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.1.1. Canonical and Non-canonical Constructions
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.2. Cross-linguistic Differences with Adjectival Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.1.3. Cross-linguistic Differences with Singular Indefinite Articles
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  2.2.  The Indefinite Article in Plural Contexts
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.2.1. Occurrence
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.2.2. Linguistic Properties
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.  Basic Properties and Main Hypotheses
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.1.  Adjectival Inflections and the Article} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.1.1. Basic Properties of Adjectival Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.1.2. Basic Properties of \textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.2.  Main Hypotheses and Relatedness of Adjectival Inflections and} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.2.1. Main Hypotheses
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.2.2. Relatedness of Adjectival Inflections and \textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.  Basic Assumptions 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  4.1.  Basic Assumptions about Structure
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    4.1.1. DPs as a Whole
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    4.1.2. Determiners, Articles, and Determiner-like Elements
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    4.1.3. Adjectives, Numerals, and Quantifiers
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  4.2.  Distributed Morphology and Type Theory
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    4.2.1. Distributed Morphology
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    4.2.2. Type Theory
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.  Overview of the Chapters
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Chapter 2: The Structural Nature of Adjectival Inflections  53\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{1.}  \textbf{Introduction}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.1.  The Strong/Weak Alternation of Adjectives
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.2.  Adjectival Inflections and (In-)definiteness
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{1.3.  Outlook\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

}\textbf{2.}  \textbf{Adjectival} \textbf{Inflections} \textbf{in} \textbf{Canonical} \textbf{DPs\\
\ea} \textbf{
} \textbf{\gll} \textbf{\\
}   \textbf{\\
} \textbf{\glt
\z % you might need an extra \z if this is the last of several subexamples

} \textit{2.1.  Weak Adjectives: Impoverishment\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

}     2.1.1. Basic Alternation and Traditional Generalizations\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.1.2. Inventory of Determiners and Adjectival Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       2.1.3. \citet{Sauerland1996}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.4. Basic Proposal
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.5. The Proposal in More Detail: Vocabulary Insertion Rules
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.6. The Proposal in More Detail: Structural Positions of Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.7. The Proposal in More Detail: Impoverishment\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{2.2.  Strong or Weak Adjectives in Canonical DPs:} Ein\textit{{}-words}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.1. Adjectives after \textit{ein}{}-words: Strong Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.2. Adjectives after \textit{ein}{}-words: Uninflected \textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.3. Adjectives after \textit{ein}{}-words: Some Extensions 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.3.  Strong or Weak Adjectives in Canonical DPs: Null Articles and Saxon Genitives
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.4.  Adjectives in Extended-Adjective Constructions and After Numerals
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{3.}  \textbf{Adjectival} \textbf{Inflections} \textbf{in} \textbf{Non-canonical} \textbf{DPs\\
\ea} \textbf{
} \textbf{\gll} \textbf{\\
}   \textbf{\\
} \textbf{\glt
\z % you might need an extra \z if this is the last of several subexamples

} \textit{3.1.  Regular DP vs. Low Right-Adjunction: Close Appositions\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   3.2.  Regular DP vs. Mid Right-Adjunction: Indefinite Pronoun Constructions\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  3.3.  Regular DP vs. Mid Right-Adjunction: Noun-Adjective Exclamatives}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.4.  Regular DP vs. High Right-Adjunction: Loose Appositions\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   3.5.  Regular DP vs. Complex Specifier inside DP: Dis-agreement in Pronominal DPs\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  3.6.  Regular DP vs. Separate Base-generation: Split Topicalizations}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  3.7.  Regular DP vs. Complex Compound Modifier: Nominal Compounds 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.8.  Regular DP vs. Outside of DP Proper: Predeterminers\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  3.9.  Regular DP vs. No Determiner: Vocatives}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.  Adjectival Inflections on Determiners and Predeterminers
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.  A Brief Critique of some Previous Proposals
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{5.1.  Olsen (1989a, 1989b, 1991a, 1991b)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  5.2.  \citet{Schoorlemmer2009}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  5.3.  \citet{Murphy2018}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

6.  Conclusion\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Chapter 3: Variation and Secondary Mechanisms      124
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.  Introduction
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.  Canonical DPs with Unexpected Weak Adjectives: Phonetic Rule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  2.1.  Two Adjectives without a Determiner
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  2.2.  Indefinite Pronoun Constructions Revisited
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.  Canonical DPs Involving Adjectives with Optional or no Inflections and Definite   Determiners with no Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.   Canonical DPs with Unexpected Weak Determiners: Impoverishment Rule 2
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.  Canonical DPs with Unexpected Strong Adjectives: Pronominal Determiners
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  5.1.  Data
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  5.2.  Three Types of Proposals
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    5.2.1. One Structure: Agreeing vs. Non-agreeing Pronominal Elements
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    5.2.2. One Structure: Pronominal Elements in Different Positions
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    5.2.3. Two Different Structures: Complementation and Adjunction
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    5.2.4. Absence of Weak Adjectives in Certain Instances
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  5.3.  Pronominal Determiners and Complementation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    5.3.1. Structures and Feature Specifications
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    5.3.2. Vocabulary Insertion Rules for Pronominal Elements
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    5.3.3. Preferences for Strong or Weak Adjectives
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  5.4.  Summary of the Discussion
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{6.}  \textbf{Non-canonical} \textbf{Structures} \textbf{with} \textbf{Optional} \textbf{Inflections} \textbf{on} \textbf{Predeterminer} \textbf{\textit{alle}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

7.  Revisiting Two Traditional Generalizations of Adjectival Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

8.  Dialectal Variation: Mannheim German
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

9.  Conclusion
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Chapter 4: Consequences for Other Analyses      179
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.  Introduction
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.  Weak Adjectives in the Context of Spurious Indefinite Articles
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  2.1.  Weak Adjectives in Structures Involving Predicate Inversion
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  2.2.  Weak Adjectives in Structures Involving Null Nouns
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.  Strong Adjectives in Structures Involving Split Topicalization
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.1.  Movement out of the Source}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.2.   Movement but not out of the Source}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.3.  Separate Base-generation}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.4.} Ein \textit{in the Split-off}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.   Weak Adjectives with Non-restrictive Interpretation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.  Adjectival Endings Make Nominal Features Visible
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

6.   Conclusion
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\section{\textmd{Chapter 5:} \textmd{\textit{Ein}}\textmd{{}-words and Adjectival} \textmd{\textit{eine}}\textmd{        201}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{1.}   \textbf{Introduction\\
\ea} \textbf{
} \textbf{\gll} \textbf{\\
}   \textbf{\\
} \textbf{\glt
\z % you might need an extra \z if this is the last of several subexamples

}  \textit{1.1.  Preliminaries and Basic Data\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  1.2.   Initial Taxonomy of} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{1.3.  Outlook}\textbf{\\
\ea} \textbf{
} \textbf{\gll} \textbf{\\
}   \textbf{\\
} \textbf{\glt
\z % you might need an extra \z if this is the last of several subexamples

2.}   \textbf{Similarities\\
\ea} \textbf{
} \textbf{\gll} \textbf{\\
}   \textbf{\\
} \textbf{\glt
\z % you might need an extra \z if this is the last of several subexamples

} \textit{2.1.   Split Topicalization\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  2.2.   Split Topicalization with Fronted Adjective\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  2.3.   Adjective Followed by Elided Noun\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  2.4.   Split Topicalization with Fronted Adjective and Elided Noun\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

}\textbf{3.}   \textbf{Differences\\
\ea} \textbf{
} \textbf{\gll} \textbf{\\
}   \textbf{\\
} \textbf{\glt
\z % you might need an extra \z if this is the last of several subexamples

}   \textit{3.1.   Encliticization\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   3.2.   Stressability\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   3.3.   Semantic Singularity\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

}\textbf{4.}   \textbf{Step} \textbf{1} \textbf{of} \textbf{the} \textbf{Proposal:} \textbf{Morphology} \textbf{and} \textbf{Semantics\\
\ea} \textbf{
} \textbf{\gll} \textbf{\\
}   \textbf{\\
} \textbf{\glt
\z % you might need an extra \z if this is the last of several subexamples

}   \textit{4.1.  Composite Elements: Article} ein \textit{as a Supporting and Flagging Element}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.1.1. Basic Proposal
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.1.2. Derivation of Composite Forms
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.1.3. Some Advantages and Initial Consequences\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{4.2.  Some Evidence for the Composite Analyses\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

} 4.2.1. The Negative Article \textit{kein} \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  4.2.2. Possessive Articles such as \textit{mein}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  4.2.3. The Singularity Numeral \textit{EIN} \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{4.3.  Feature Specifications} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 4.3.1. Cancelling the Presupposition of Adjectival \textit{eine}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    4.3.2. Feature Specifications of the Different Types of \textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{5.}   \textbf{Step} \textbf{2} \textbf{of} \textbf{the} \textbf{Proposal:} \textbf{Syntax\\
\ea} \textbf{
} \textbf{\gll} \textbf{\\
}   \textbf{\\
} \textbf{\glt
\z % you might need an extra \z if this is the last of several subexamples

}  \textit{5.1.   Article vs. Numeral\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

} 5.1.1. Uniform Position(s) of all Numerals\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     5.1.2. Different Scope of \textit{mehr als\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   5.2.   Article / Numeral vs. Adjective \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

} 5.2.1. Different Morphology\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     5.2.2. Co-occurrence with Possessive \textit{ein}{}-words 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  5.2.3. Co-occurrence of \textit{ein} and Determiners Revisited\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    5.2.4. More Evidence for Adjectival \textit{eine}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.2.5. Different Semantics: \textit{EIN} vs. \textit{eine} and \textit{andere} meaning ‘different’ vs.       ‘other’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{6.}  \textbf{Diachronic} \textbf{and} \textbf{Cross-linguistic} \textbf{Evidence} \textbf{for} \textbf{\textit{ein}}\textbf{{}-words as Composites}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

7.  A Brief Critique of a Previous Proposal\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

8.  Conclusion \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleJBHeadingii}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textup{Chapter 6:} Ein \textup{and Emotiveness          258}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleJBHeadingii}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.   Introduction
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  1.1.  General Structural Similarities between the DP and TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  1.2.  General Interpretatory Differences between the DP and TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.  Data
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{2.1.  Preliminary Remarks}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.1.1. Basic Differences Between Role and Kind Nouns in the DP and TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.1.2. Emotiveness and Figurative Extension
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.1.3. Three Different Readings
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  2.2.  Data
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.2.1. Plural
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 2.2.2. Singular
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.2.3. Interim Summary
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.2.4. Ambiguous Role Nouns in the Singular
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.  Proposal
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.1.   Combining Two Previous Proposals}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.1.1. De Swart, \citet{WinterZwarts2007}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.1.2. Lexical Features on Nouns
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.1.3. \citet{Rauh2004}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.2.  Ordinary Reading}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.2.1. Ordinary Reading in the DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.2.2. Ordinary Reading in the TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.3.  Comparative Reading}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.3.1. Preliminaries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.3.2. Comparative Reading in the DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.3.3. Comparative Reading in the TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.4.  Capacity Reading}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.4.1. Preliminaries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.4.2. Capacity Reading in the DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.4.3. Capacity Reading in the TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.  Conclusion
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Chapter 7: \textit{Ein} and Number             292
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.  Introduction
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  1.1.  A Brief Review
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  1.2.  Number in the DP and TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.  Data
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  2.1.  Cases of Agreement
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  2.2.  Cases of Dis-agreement
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{2.3.  More Cases Of Dis-agreement: Instances Involving} als\textit{{}-nominals}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  2.4.  Summary of the Data
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.  Proposal
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.1.  Basic Assumptions}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.2.  Agreement in Constructions without} als
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     3.2.1. Agreement and Dis-agreement
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.2.2. Agreement and NumP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.3. Plural Nouns
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     3.2.4. Non-plural Nouns
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     3.2.5. Singular Nouns
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.3.  Morphological and Semantic Number}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.3.1. Relating Morphological and Semantic Number
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.3.2. Dis-agreement Revisited
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.3.3. Pronominal DPs Involving \textit{Sie} in More Detail
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.4.  Agreement in Constructions Involving} als-\textit{nominals}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.4.1. Structure and Semantics
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    3.4.2. Agreement 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.  Conclusion
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Chapter 8: Discussion and Concluding Remarks      323\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{1.}    \textbf{The} \textbf{Bigger} \textbf{Picture}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{1.1.   Commonalities of Adjectival Inflections and} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    1.1.1. Some Evidence for Hypothesis 1a
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    1.1.2. Some Evidence for Hypothesis 1b: Hypothesis 2a vs. 3a
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{1.2.   Differences between Adjectival Inflections and} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    1.2.1. Some Evidence for Hypothesis 2b
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    1.2.2. Some Evidence for Hypothesis 3b
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  1.3.  Summary of the Main Claims
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.   Some Extensions and Further Consequences 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{2.1.  Extensions and Consequences of Adjectival Inflections: German Dialects}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{2.2.  Extensions and Consequences Involving} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.1. Reviewing Cases of Supporting and Flagging
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.2.2. Nominals with Two Overt Operators
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.2.3. Flagging of Other Operators
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.2.4. Contexts of Vocabulary Insertion for \textit{ein}: Covert Operators
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    2.2.5. Contexts of Vocabulary Insertion for \textit{ein}: Covert and Overt Operators
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.6. Contexts of Vocabulary Insertion for \textit{ein}: Feature Deletions
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.  More General Considerations
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  3.1.  Nominal Structure and Concord
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{3.2.  Expletive Elements more Generally}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Appendix: Examples of Plural \textit{ein} 361
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{1.1.  Googled:} was für (ei)ne \textit{without adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{1.2.  Googled:} was für (ei)ne \textit{with adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{1.3.   Googled:} so (ei)ne \textit{without adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{1.4.   Googled:} so (ei)ne \textit{with adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{2.1.  Twitter:} was für (ei)ne \textit{without adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{2.2.  Twitter:} was für (ei)ne \textit{with adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{2.3.  Twitter:} so (ei)ne \textit{withtout adjective (but with numeral)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{2.4.  Twitter:} so (ei)ne \textit{with adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{3.1.  Facebook:} so (ei)ne \textit{with adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.   Datenbank für Gesprochenes Deutsch
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

References                  374
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Language Index                N/A
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Subject Index                N/A
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Preface
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Anyone teaching German as a foreign language knows that mastering the adjectival endings and \textit{ein}{}-words is particularly challenging for students. I became interested in these grammatical topics over 25 years ago when I started working as a Teaching Assistant in the United States. I have been fascinated by these phenomena ever since. A couple of years ago, this interest was strongly piqued again by the phrase \textit{Eine Störche!} uttered by my dad during a hike. With my family interested in language, a discussion followed where we agreed that this phrase could be loosely rendered as ‘Wow! So many storks!’ However, my family is, to this day, surprised that a singular article like \textit{ein} ‘a’ can combine with a plural noun. Wow, indeed! This book attempts to shed some light on this and other issues. Beside the cases frequently discussed in the literature, here I also engage in the discussion of many non-canonical instances. In particular, I focus on various constructions involving \textit{ein}{}-words and adjectival inflections. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Thinking back, the chapter on \textit{ein}{}-words was the first to be written for my dissertation. However, it has taken the longest to present it in a more polished form. Although the basic idea is still the same, many details, empirical and analytical, have been modified or added. In the meantime, adjectival inflections have become a second major point of interest for me. Again, while the basic system was already laid out in Roehrs (2009a: Chapter 4), more “exotic” cases are discussed in the following pages. I believe it is these new details about \textit{ein}{}-words and adjectival inflections that reveal the true nature of these elements allowing us to formulate some new and interesting hypotheses. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is my hope that due to its richness of data and analyses, many of which are new, this book will be of interest to anyone working on semantically vacuous elements, in general, and on the syntax and semantics of the DP, in particular. With the main focus on German, the book will not be the final word on these topics as the investigation of other languages will surely reveal more interesting facts and lead to other theoretical insights. However, I hope that this book will make a contribution toward the description and explanation of these parts of the grammar, topics I believe have not received due attention.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The material in this book was presented at many linguistic colloquia and conferences, too many to detail here. I thank the respective audiences for questions and comments, especially, Marcel den Dikken, David Fertig, Volker Gast, Hubert Haider, Tracy Hall, Tom Leu, Mark Louden, Joe Salmons, Chris Sapp, Erik Schoorlemmer, and Laura Smith. A very special thanks goes to Björn Köhnlein, who contributed a number of very interesting points. I would also like to express gratitude to Jochen Trommer and Bernd Wiese for helping me find some of their work. Furthermore, I am indebted to many reviewers whose comments over the years have helped me shape the ideas presented here. In particular, I am very grateful to the two anonymous reviewers of this book, who provided many constructive suggestions, data points, and references, and to my editor Mike Putnam for many helpful suggestions. Finally, note that parts of Chapter 5 are based on Roehrs (2006a: Chapter 5, Part II). Chapter 7 was supported by a Faculty Research Grant (G34217) from the University of North Texas, which I hereby gratefully acknowledge. The first version of this book was thoroughly revised during my Faculty Development Leave in \citealt{Fall2020}, and many other changes were made after that.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I dedicate this book to my late dad, who encouraged me to look for commonalities, to my mom, who emphasized I should write clearly, to my brother, who reminded me to stop and smell the roses, and to my partner in crime, who makes life just beautiful. \textit{Danke, Siggi, Rosen, Keule and Xixi!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Denton, \citealt{November2024}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Dorian Roehrs
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Abbreviations
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

ACC   – accusative case
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

CMN  – common gender 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

DAT   – dative case
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

FEM   – feminine gender
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

GEN   – genitive case
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

MASC  – masculine gender
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

NEUT   – neuter gender
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

NOM   – nominative case
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

PL   – plural number
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

PRT   – particle 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

REFL  – reflexive particle
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

SGL   – singular number
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

ST   – strong adjectival ending
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

WK   – weak adjectival ending
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Chapter 1: Introduction
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.  Vacuous Elements – an Imperfection of Language?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In many languages, ordinary sentences contain nominative subjects. Nominative case is often taken to be structural; that is, it is assigned under certain syntactic conditions. This type of case has received a lot of attention in the linguistic literature. In the generative tradition, structural case – often spelled Case – is frequently represented by uninterpretable features. Chomsky (e.g., 2000: 119) pointed out that uninterpretable features may be an imperfection in the design of languages. Specifically, structural case features may receive no interpretation in either PF or LF. In terms of optimally efficient language design, it is surprising that these elements exist at all. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is usually proposed that uninterpretable features have to be removed during the derivation to adhere to the Principle of Full Interpretation \citep{Chomsky1995}. This removal has to do with feature checking (or valuation). Specifically, feature checking (or valuation) motivates movement including that of subjects. As the result of this movement, features including structural case features are removed from the derivation (e.g., \citealt{Chomsky1995}, 2000). In other words, no uninterpretable features are left at the end of the derivation, and the derivation succeeds.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Besides these abstract features, there are also word-level elements that raise similar issues about the design of language – expletives. Although these elements do get an interpretation in PF (i.e., they have a phonetic realization), they seem to have no semantics either. In the next section, I briefly illustrate two such cases that have been intensively discussed in the literature: expletive \textit{there} and the proprial article. In the second section, I discuss two other elements that have received less attention in this regard: German adjectival inflections and the indefinite article \textit{ein} ‘a’. In this book, I propose that the latter two elements also have no semantics at all; that is, they have neither semantics of their own nor do they make semantic features visible. I hypothesize that these elements are not an imperfection of language but provide overt clues about the presence of abstract linguistic elements. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.1.  The Clause
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Sentence pairs such as (1a-b) have received much attention in the literature (\citealt{Milsark1974} and much subsequent work). Clauses like \REF{ex:key:1b} are usually referred to as \textit{there}{}-existentials and are used in presentational contexts:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%1
    \label{ex:key:1}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  A man is in the garden.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  There is a man in the garden.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Both sentences in \REF{ex:key:1} have very similar meanings: they are about a man being in the garden. As \citet[396]{Hazout2004} points out, \textit{there} itself cannot have any semantics; for instance, if \textit{there} were a deictic element, its distal semantics should clash with the proximal meaning of \textit{here} in \REF{ex:key:2a}. Evidently, this is not the case. Second, the unaccusative predicate \textit{arrive} assigns one theta role in \REF{ex:key:2b}, namely to \textit{three men}. With the classical Theta-Criterion \citep[36]{Chomsky1981} satisfied, \textit{there} cannot be an argument or a predicate. If \textit{there} makes no semantic contribution, then we might expect that it can be left out without a (significant) loss in meaning.\footnote{Referencing work by Edwin Williams, Chomsky (1991 [reprinted in \citealt{Chomsky1995}: 157]) points out that scopal elements in constructions with \textit{there} vs. without the expletive may have different readings (also Lasnik \& Uriagereka with \citealt{Boeckx2005}: 157).} This is borne out as can easily be verified in \REF{ex:key:2c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%2
    \label{ex:key:2}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  There are too many people here.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  There arrived three men.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  Three men arrived.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The conclusion is that \textit{there} is indeed an expletive (i.e., pleonastic) element. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There are many proposals that aim to explain \textit{there}{}-existentials (for a brief survey of some of the relevant issues, see, e.g., Lasnik \& Uriagereka with \citealt{Boeckx2005}: 153-169). I briefly illustrate the basic account in Chomsky (1991 [reprinted in \citealt{Chomsky1995}]), one of the first proposals of this construction in the Minimalist tradition. Note though that the purpose of this book is not to discuss this construction in detail but rather to lay out some basic assumptions and to relate existential constructions to other, similar instances.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet[154-57]{Chomsky1995} assumes the Principle of Full Interpretation, according to which all elements in a linguistic expression must have an interpretation in LF. Above, I showed that \textit{there} is semantically vacuous; that is, it has no semantics. As a consequence, \textit{there} can, by itself, not be interpreted in LF. To explain the grammaticality of \textit{there}{}-constructions, Chomsky proposes that \textit{there} is a LF-affix and that the overt noun phrase, the associate, undergoes movement to adjoin to \textit{there} (also Chomsky \& Lasnik (1993 [reprinted in \citealt{Chomsky1995}: 65-66]). As the expletive is licensed by adjunction of another element in LF, Full Interpretation is not violated. I assume that the proposal involving the licensing of the expletive by moving the associative is basically correct. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Clauses and noun phrases are usually held to be parallel in meaning and structure (e.g., \citealt{Abney1987}, \citealt{Chomsky1970}, Iordăchioaia 2020). This can most easily be seen in \REF{ex:key:3}, which juxtaposes a verb and its arguments in \REF{ex:key:3a} with its derived nominal counterpart in \REF{ex:key:3b}. It is important to point out that both the verb in the sentence and the noun in the noun phrase have an agentive subject (\textit{the Romans}) and an affected object (\textit{the city}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%3
    \label{ex:key:3}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  The Romans destroyed the city.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  the Romans’ destruction of the city 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Based on these similarities in meaning, a certain structural parallelism has come to be established. Considering \REF{ex:key:4}, NP is taken to be the nominal counterpart of VP. Furthermore, NumP is similar to AgrP, DP to TP, and PP to CP:\footnote{This alignment based on \citet{Grimshaw1991} is not uncontroversial; for instance, other authors have proposed that DP is parallel to CP (e.g., \citealt{Szabolcsi1994}). Note that constructions such as \REF{ex:key:3} are often discussed in this context. This book will not consider cases involving theta nouns as in \REF{ex:key:3b} in much detail. Rather, investigating many non-canonical constructions (i.e., structures more complex than simple DPs), we will come across cases where more structure is projected above the DP (and below the PP) in \REF{ex:key:4b}. Below, this structural level is labeled Left Periphery Phrase (see \sectref{sec:key:4.1.2}). Furthermore, there is debate in German as to whether subject-initial root clauses involve CPs (e.g., \citealt{SchwartzVikner1996}) or TPs (e.g., \citealt{Zwart1997} for Dutch, which extends to German). Given these complexities and controversies, I do not engage in the debate of the alignment in \REF{ex:key:4} here. Rather, I simply follow the alignment in \REF{ex:key:4} and assume that the DP is parallel to the TP (see also Chapters 6 and 7 below). For clauses, I assume that subject-initial structures are TPs and non-subject initial structures are CPs \citep{Zwart1997}, but nothing important hinges on this choice.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%4
    \label{ex:key:4}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  CP  TP  AgrP       VP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  PP  DP  NumP       NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These and other observations have led to the general hypothesis that noun phrases are similar to clauses. Part of this work has been the establishment of the DP-Hypothesis \citep{Abney1987}. This line of investigation has led to many empirical discoveries and theoretical innovations (for a survey, see Alexiadou \textit{et al}. 2007). The current work espouses the DP-Hypothesis. While noun phrases in argument position are assumed to be at least DPs (e.g., \citealt{Longobardi1994}, \citealt{Stowell1989}), we see in the course of the discussion that noun phrases in non-argument position (e.g., nominal predicates) may be structures of smaller sizes. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Before proceeding, I briefly provide here details about some of the terminology used in this book. The terms noun phrase and nominal are used as general designations for structures involving a noun and possibly some modifiers or other dependents. Terms such as DP, NumP, or NP are used when the structure of the nominal (/noun phrase) is of particular interest. With this in place, I return to the main line of exposition. Given the existence of expletives in clauses and the assumed parallelism between clauses and noun phrases, it may not come as a surprise that expletive elements have also been proposed to occur in the nominal domain.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.2.  The Noun Phrase
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet{Longobardi1994} develops a theory of noun movement in (overt) syntax and LF to account for a number of similarities and differences between the Romance and Germanic languages. \citet[651]{Longobardi1994} suggests that the following alternation can be related to the \textit{there}{}-existentials discussed in the previous section. Similar to the (associative) noun phrases in \REF{ex:key:1}, proper names may appear in different positions. Comparing \REF{ex:key:5a} to \REF{ex:key:5b}, we observe that the proper name in \REF{ex:key:5b} follows a definite article. This determiner is often referred to as proprial article. Longobardi relates the distribution in \REF{ex:key:5a} to \REF{ex:key:5b} by proposing that the proper name in \REF{ex:key:5a} has undergone movement to D within the larger subject DP. This is illustrated in \REF{ex:key:5c} (more on this below):\footnote{\citet[640]{Longobardi1994} actually assumes that N in \REF{ex:key:5c} raises to D by substitution. To keep the discussion parallel to the clause, I assume adjunction here.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%5
    \label{ex:key:5}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Gianni mi  ha  telefonato}.    (Italian)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Gianni me has called
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Gianni called me.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Il}  \textit{\textsubscript{} }\textit{Gianni mi \textsubscript{} }\textit{ha  telefonato}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the Gianni me has called
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Gianni called me.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  [\textsubscript{DP} \textit{Gianni}\textsubscript{i}+D [\textsubscript{NP} t\textsubscript{i} ]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Importantly, both DPs in \REF{ex:key:5a} and \REF{ex:key:5b} are interpreted as referential; that is, the definite article in \REF{ex:key:5b} does not make a semantic contribution (see also \citealt{LekakouSzendrői2012}, \citealt{VergnaudZubizaretta1992}). In other words, the article is added in \REF{ex:key:5b} with no change in the meaning of the noun phrase as a whole. In fact, note that if the definite article in \REF{ex:key:5b} were to make a contribution, it would raise issues with regard to the redundancy of certain semantic components: singular definite articles typically presuppose the existence of a unique entity; however, proper names, in their referential use, already denote unique individuals by themselves. To avoid this issue, Longobardi (1994: 648-50, 655) proposes that the proprial article functions as an expletive element similar to \textit{there} above.\footnote{For different views of the proprial article, see \citet{Matushansky2008} and \citet{Muñoz2019}.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Longobardi proposes that \REF{ex:key:5a} involves movement of the noun to D. Among others, this N-to-D movement lexically licenses the null D position of a syntactic argument, and it obviates a default existential interpretation. With the discussion of \textit{there}{}-existentials in mind, I assume with Longobardi (1994: 651 fn. 48) that this movement is overt in \REF{ex:key:5a} but covert in \REF{ex:key:5b}. In the latter case, the proper name – in Chomsky’s terms, the associate – licenses the expletive proprial article. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There is other distributional and morphological evidence that the proper name undergoes movement and that the corresponding article is different from other types of determiners. Simplifying somewhat, \citet[623]{Longobardi1994} points out that the proper name can precede or follow a possessive adjective. Compare \REF{ex:key:6a} and \REF{ex:key:6b}. If the proper name follows the possessive adjective, the definite article must appear. In this regard, observe the difference in grammaticality between \REF{ex:key:6b} and \REF{ex:key:6c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%6
    \label{ex:key:6}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Gianni mio ha  \textsubscript{} }\textit{finalmente telefonato}.  (Italian)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Gianni my  has finally        called
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘My Gianni finally called.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Il   \textsubscript{} }\textit{mio Gianni ha  \textsubscript{} }\textit{finalmente telefonato}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the my  Gianni has finally        called
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘My Gianni finally called.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  *  \textit{Mio Gianni ha  finalmente telefonato}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my  Gianni has finally       \textsubscript{} called
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Similar to the paradigm in \REF{ex:key:5}, Longobardi proposes that the proper name moves from a lower position within the larger subject DP \REF{ex:key:6b} to the higher position D \REF{ex:key:6a}. The example in \REF{ex:key:6c} is ungrammatical because the empty D is not lexically licensed in syntax, something the Romance languages require. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Second, \citet[656]{Longobardi1994} provides evidence that pleonastic articles can be morphologically different from substantive articles. Compare \REF{ex:key:7a} involving a proper name to \REF{ex:key:7b} containing a common noun, where the article is \textit{en} in the first case but \textit{el} in the second one (for more details, see Longobardi’s work; see also \citealt{Panagiotidis2000} for Northern Greek):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%7
    \label{ex:key:7}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{en  Pere}      (Catalan)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the Peter
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Peter’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{el   gos}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the dog
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the dog’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given the difference in articles in \REF{ex:key:7}, Longobardi presents a strong argument that articles come in several types with different semantics, expletive or contentful.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up, I take it as established that there are expletive elements in the clause as well as in the noun phrase. The assumption that certain lexical elements are semantically vacuous allows us to avoid issues such as contradictions (e.g., as regards deixis in the clause) and redundancies (e.g., as regards uniqueness in the noun phrase). Furthermore, the assumption that such elements are expletives explains why these elements can be left out without a significant loss in meaning. Note that in each case discussed so far, the expletive element is in a position higher than the substantive element. Schematically, this can be illustrated as follows:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%8
    \label{ex:key:8}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

                       α
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  EXPL            β
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 SUBST  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{there}    associate
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  prop. art.  proper name
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In order not to violate Full Interpretation, the pleonastic element must be licensed. In both cases, this licensing process involves movement of a lower substantive element to the higher expletive one.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Two questions arise from the discussion above: (i) are there other semantically vacuous elements and (ii) why do these elements exist at all? The first question is discussed in detail for the German noun phrase in the following pages. I argue that adjectival inflections and the indefinite article \textit{ein} ‘a’ are also semantically vacuous elements. The second question is much harder to answer and is only briefly addressed in this book. I suggest that expletives indicate the presence of abstract linguistic elements. In that sense, adjectival inflections and \textit{ein} are not an imperfection of language.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Note that this book is not intended to contribute to the theory of expletive elements \textit{per se}. Rather, it tries to identify more elements that share some of the properties of the expletives that have been established in the literature. Having said that, I engage in a more detailed discussion of \textit{there} vs. adjectival inflections and \textit{ein} in the final section of this book (Chapter 8, \sectref{sec:key:3.2}). There I point out that both types of elements share a number of traits but that they also seem to exhibit some differences. Although I make some very brief remarks of what might explain some of those differences, I will leave a more detailed exploration for another time. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.  German as the Language under Investigation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I explain why the focus of this book is on one language – German. To this end, I provide some brief cross-linguistic discussion of adjectival inflections and singular indefinite articles in German, Yiddish, and Norwegian. It is shown that these languages exhibit a number of striking differences. This includes evidence for the presence of the indefinite articles in plural contexts. Unfortunately, many of these and other non-canonical data points have not received much attention in the theoretical literature. In this book, I add many data points to the discussion of German, the language I am most familiar with, and analyze these constructions in detail. However, it is beyond the scope of this book to do this kind of empirical work for the other languages, to discuss the relevant constructions in detail, and to provide a thorough cross-linguistic analysis of all these languages. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.  Some Cross-linguistic Differences
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Before I discuss some cross-linguistic data, I start with some brief general remarks.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.1. Canonical and Non-canonical Constructions
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Focusing on German, this book may seem restricted in scope. However, it strives to be more comprehensive than previous accounts by discussing adjectival inflections and the indefinite article \textit{ein} ‘a’ in very diverse structural contexts in that language. On the one hand, reference grammars (e.g., the German Duden) provide a fairly comprehensive survey of the relevant empirical domains. However, their goal is not to lay out a detailed theoretical analysis. On the other hand, theoretically oriented works often make insightful and elegant proposals of the canonical cases. These are simple DPs that involve the schematic structure “determiner + adjective(s) + noun”, where the three elements agree in case, number, and gender \REF{ex:key:9a}.\footnote{Simple DPs involving prenominal Saxon Genitives (e.g., \textit{Maries groß-er Bär} ‘Mary’s big-\textsc{st} bear’) are counted as canonical constructions here despite the fact that the Saxon Genitive itself does not agree in features with the following adjective and/or noun.} This is in contrast to more complex cases like (9b-c), constructions that are frequently relegated to footnotes or even completely left out (for specific references, see Chapters 2 and 5). Specifically, the string in \REF{ex:key:9b} is a complex nominal involving two overt head nouns, and \REF{ex:key:9c} is a complex nominal where the pronoun does not agree in number with the following adjective and noun:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%9
    \label{ex:key:9}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{der} \textit{groß-e  Bär} (German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

the big-\textsc{wk} bear.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the big bear’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. \textit{das Sternbild} [\textit{Groß-er Bär}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

the constellation big-\textsc{st}    bear.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the constellation Great Bear’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.   \textit{ihr} [\textit{jung-es           Gemüse}] e\textsubscript{N}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) young-\textsc{st.sgl} vegetable.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you young folks’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given the characterization of simple DPs above, the latter two examples involve non-canonical structures. This book puts these non-canonical constructions on center stage. In fact, I argue that it is these types of constructions that reveal the true nature of adjectival inflections and \textit{ein}.\footnote{This idea has been echoed by other authors; for instance, Sigurðsson \& \citet[22]{Wood2020} state that the exceptional cases may throw light on the more general instances.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Before moving on, I clarify some more terminology. To distinguish the different nominals in (9b-c), I use the terms matrix nominal and embedded nominal. Embedded nominals are marked in (9b-c) by brackets. They may consist of adjuncts \REF{ex:key:9b}, specifiers \REF{ex:key:9c}, and various complements. The structures without brackets are labeled matrix nominals. Note that each head noun projects its own nominal; that is, both \textit{Sternbild} ‘constellation’ and \textit{Bär} ‘bear’ in \REF{ex:key:9b} involve extended projections (\sectref{sec:key:4.1.1}).\footnote{Notice that if a chapter designation is not provided, the section or footnote is part of the current chapter (i.e., the reference to \sectref{sec:key:4.1.1} in the main text is shorthand for Chapter 1, \sectref{sec:key:4.1.1}).} Head nouns may also include null elements \REF{ex:key:9c}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As illustrated in the next subsection, languages, even closely related ones, show empirical differences in the domains of adjectival inflections and indefinite articles. This becomes clear when we compare German, Yiddish, and Norwegian in canonical as well as non-canonical structures. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.2. Cross-linguistic Differences with Adjectival Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I start with a canonical construction where German, Yiddish, and Norwegian show the same inflectional patterns. In simple DPs, adjectives have weak inflections (WK) when they follow definite articles (the data in this subsection are taken from \citealt{Roehrs2015}; note also that Norwegian involves a second, suffixal determiner; for discussion, see \citealt{Julien2005a} among many others):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%10
    \label{ex:key:10}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{das groß-e Haus}    (German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the big-\textsc{wk} house.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the big house’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{dos groys-e hoyz}    (Yiddish)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the big-\textsc{wk}  house.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the big house’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{det stor-e    hus-et}    (Norwegian)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the big-\textsc{wk} house.\textsc{neut}{}-\textsc{def}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the big house’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Differences emerge when adjectives are preceded by Saxon Genitives. Here, German patterns with Yiddish in exhibiting a strong ending (ST) on the adjective, but Norwegian has a weak inflection (\textit{Ø} indicates an assumed null ending):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%11
    \label{ex:key:11}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Peters  groß-es Haus}    (German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter’s big-\textsc{st}   house.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Peter’s big house’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Berls  groys-Ø hoyz}    (Yiddish)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Berl’s big-\textsc{st}    house.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Berl’s big house’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Pers  stor-e   hus}    (Norwegian)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Per’s big-\textsc{wk} house.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Per’s big house’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Another distinction appears when adjectives follow distal demonstratives. In these cases, German patterns with Norwegian in showing a weak inflection on the adjective, but Yiddish has a strong ending (the stressed demonstrative in Norwegian, which is similar to the definite article in form, is capitalized):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%12
    \label{ex:key:12}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{jenes groß-e  Haus}    (German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    that   big-\textsc{wk} house.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘that big house’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{yents groys-Ø hoyz}    (Yiddish)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    that   big-\textsc{st}    house.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘that big house’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{DET stor-e   hus-et}    (Norwegian)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    that  big-\textsc{wk} house.\textsc{neut}{}-\textsc{def}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘that big house’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As to non-canonical constructions, suffice it to consider one structure. In \REF{ex:key:13}, complex names are juxtaposed with their categorizing nominals. Again, the three languages do not pattern the same. Here, German and Yiddish contrast with Norwegian again:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%13
    \label{ex:key:13}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a. \textit{das Sternbild}  \textit{Groß-er Bär}      (German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

the constellation big-\textsc{st}    bear.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the constellation Great Bear’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{der zhurnal    Sovetish-Ø Heymland}    (Yiddish)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the magazine Soviet-\textsc{st}   homeland.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the magazine Soviet Homeland’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  \textit{indianer-en Stor-e   Bjørn}       (Norwegian)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   Indian-\textsc{def}  big-\textsc{wk} bear.\textsc{cmn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the Indian (called) Big Bear’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up, it is clear that the three languages differ in their distribution of adjectival inflections in canonical and non-canonical constructions.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \citet{RoehrsJulien2014} show that German and Norwegian differ quite generally. Documenting with nine sets of data (possessives involving proper names and pronominals, embedded and unembedded proper names, “dis-agreeing” pronominal DPs, appositives, definite adjectives, vocatives, and discontinuous noun phrases), they show that German consistently has strong endings but that Norwegian has weak endings. They propose that adjectival inflections in German are a function of lexical and structural factors (see Chapter 2) but that Norwegian is regulated by semantic ones (also \citealt{Harbert2007}: 131; \citealt{Lohrmann2011}; \citealt{Schoorlemmer2009}, 2012, and others). \citet{Roehrs2015} points out that Yiddish is similar to German but differs in the lexical elements that bring about the weak inflections on the adjectives.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.3. Cross-linguistic Differences with Singular Indefinite Articles
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Consider the three languages as regards the singular indefinite articles. In German, the indefinite article \textit{ein} ‘a’ is part of the so-called \textit{ein}{}-words (a convenient term often used in textbooks on German, for instance, \citealt{RankinWells2016}). The latter comprise the indefinite article \textit{ein} ‘a’, the (stressed) singularity numeral \textit{EIN} ‘one’, the negative article \textit{kein} ‘no’, and possessive articles like \textit{mein} ‘my’, \textit{dein} ‘your’, etc. Given certain similarities (Chapter 5), these words are taken to be related to one another and are usually discussed together. Comparing the three languages as regards these types of words, we make the initial observation that German, Yiddish, and Norwegian show the same basic patterns – \textit{ein}{}-words precede their related head nouns (stressed singularity numerals that have the same spelling as their corresponding indefinite articles are distinguished by capitalization):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%14
    \label{ex:key:14}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein / EIN / kein / mein Bruder}    (German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a    / one  / no   / my    brother.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a/one/no/my brother’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{a / eyn / keyn / mayn bruder}    (Yiddish)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a / one / no    / my     brother.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a/one/no/my brother’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  \textit{en / EN / ingen / min bror}    (Norwegian)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a   / one / no     / my  brother.\textsc{cmn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a/one/no/my brother’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Considering (14a-c), we note that the singularity numeral, the negative article, and the possessive article are related to the indefinite article in German in that all these forms involve \textit{ein}. This is less straightforward with Yiddish and Norwegian, where the possessive element, for instance, does not seem to relate to the indefinite article in a straightforward way. Besides this first indication that the three languages differ, Yiddish and Norwegian show other, syntactic distinctions.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Yiddish is different from German and Norwegian in at least three aspects. Yiddish allows an inflected possessive pronoun to precede an indefinite article \REF{ex:key:15a}, the negative article can co-occur with the numeral for ‘one’ \REF{ex:key:15b}, and the – what looks like – inflected singularity numeral may precede an indefinite article \REF{ex:key:15c}. The following data are taken from Lockwood (1995: 54, 66) and Weinreich (1999: 195, 205):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%15
    \label{ex:key:15}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{mayn-er    a bruder}       (Yiddish)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    mine-\textsc{infl} a brother.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     ‘a brother of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{keyn eyn land} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    no    one country.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘not a single country’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{eyn-er     a mentsh} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one-\textsc{infl} a person.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     ‘a certain person’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I argue in \citet{Roehrs2022} that the inflected possessive pronoun in \REF{ex:key:15a} involves a second, separate nominal that is in a higher position than the possessive articles in Yiddish \REF{ex:key:14b}, in German \REF{ex:key:14a}, and in Norwegian \REF{ex:key:14c}. This means that \REF{ex:key:15a} presents a non-canonical construction. Turning to \REF{ex:key:15b}, \textit{eyn} is syntactically optional. Semantically, it seems to intensify the negation (if present). As for \REF{ex:key:15c} and similar to \REF{ex:key:15a}, Yiddish most likely involves a structure different from the \textit{ein}{}-words in the three distributions illustrated in \REF{ex:key:14}. Extending the discussion to \textit{epes a khaver} ‘(something a =) some friend’, it is proposed in the aforementioned paper that \textit{eyner} and \textit{epes} are in a higher position similar to the possessive pronoun in \REF{ex:key:15a}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Norwegian is also different from German and Yiddish in that it tolerates the indefinite article between two adjectives and between adjectives and nouns (data are from \citealt{Julien2002}: 269):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%16
    \label{ex:key:16}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          ?  \textit{eit stor-t  eit styg-t    eit hus}      ([Nynorsk] Norwegian)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  a   big-\textsc{st} an ugly-\textsc{st} a   house.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘a big ugly house’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet[270]{Julien2002} argues that the articles in \REF{ex:key:16} cannot be interpreted as adjectival agreement – note that each adjective has a strong inflection. She proposes that the lower instances of the article are in lower head positions of the nominal structure (Julien’s α) and that the adjectival inflections are part of complex specifiers that contain the adjectives (see \sectref{sec:key:4.1.3}).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To conclude, this section has shown that adjectival inflections and indefinite articles are cross-linguistically similar but crucially not the same. Illustrating with German, Yiddish, and Norwegian, this could be observed in both canonical and non-canonical constructions. This conclusion is strengthened when we look at indefinite articles occurring in plural contexts – again, we will see that languages do not pattern the same.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{2.2.  The Indefinite Article in Plural Contexts}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

One of the proposals of this book is that the indefinite article \textit{ein} ‘a’ is not a reflex of singular number. This is probably the most controversial claim of these pages. After some brief cross-linguistic remarks, I provide empirical evidence that shows that \textit{ein} can indeed occur in various plural contexts in many German dialects, both in the north and in the south of central Europe. In my view, this shows that \textit{ein} is not a reflex of singular number. In the second subsection, I discuss some of the linguistic properties of these non-singular instances.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.1. Occurrence
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The indefinite article in plural contexts is perhaps most prominently discussed by Bennis \textit{et al}. (1998) for Dutch, where this element occurs in various contexts, in \textit{wh}{}-exclamatives, non-\textit{wh}{}-exclamatives, and \textit{N-of-a-N} constructions ((17a,c) are taken from Bennis \textit{et al}. 1998: 98, 101; \REF{ex:key:17b} is from van \citealt{Riemsdijk2005}: 165):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%17
    \label{ex:key:17}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Wat *(een) jongens!} (Dutch)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what   a      boys
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘What boys!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{Die auto heeft een deuken!}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    that car   has   a     dents
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘That car has dents!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{idioten van (een) mannen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    idiots   of     a      men
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘idiots of men’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This plural element is often referred to as spurious article. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is perhaps less well known that it also occurs in other Germanic languages, specifically in the three languages discussed above. We see below that all three languages have indefinite articles in plural contexts in \textit{wh-}constructions. While the occurrence of the article is restricted to that context in Yiddish, the other languages have more options but differ from each other; for instance, Swedish\footnote{Due to lack of data for Norwegian, I discuss closely related Swedish here. \citet[33]{Delsing1993} mentions though that the indefinite article is also possible in plural contexts in Norwegian dialects. Unfortunately, the referenced works in Delsing are currently not accessible to me. Note that Icelandic also allows the indefinite article to occur in certain plural contexts \citep[81]{Pétursson1992}.} has this article also in predicative contexts, northern dialects of German also in constructions involving \textit{so} ‘such’, and southern dialects of German have plural \textit{ein} also in argumental expressions without \textit{so} ‘such’. After some brief remarks about Yiddish and Swedish, I engage in a more detailed discussion of German.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Yiddish has indefinite articles in plural contexts in \textit{wh-}constructions ((18a) is taken from \citealt{Jacobs2005}: 188; \REF{ex:key:18b} is from \citealt{Lockwood1995}: 55):\footnote{In Olsvanger’s collection of stories, there is a second example, similar to \REF{ex:key:18c}, that also shows the verb in the singular (his page 172). In German, the verb would have to be in the plural to yield a grammatical example.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%18
    \label{ex:key:18}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{vos    far a bikher} (Yiddish)\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     what for \textsubscript{} a books
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     ‘what kind of books’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{voser      a shprakhn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what.for a languages
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘what kind of languages’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{, vos   iz dos  far a verter}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      what is that for a words
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘what kind of words are these’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(from Olsvanger’s \textit{Röyte pomerantsen}, p. 98)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In addition, this element has also been identified in Swedish. Delsing (1993: 33-35, 143-44) points out that it occurs in \textit{wh-}constructions \REF{ex:key:19a}. Delsing states that in predicative contexts, the article is “nearly obligatory” \REF{ex:key:19b}. Note that it can also surface between an adjective and a noun \REF{ex:key:19c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%19
    \label{ex:key:19}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Vad  \textsubscript{} }\textit{är  ni    för ena filurer?} (colloquial Swedish)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what are you for a     slyboots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘What kind of slyboots are you?’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{Pelle och Lisa är *(ena) idioter} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Pelle and Lisa are   a      idiots 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Pelle and Lisa are idiots.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Dänna      var    he     stor a husa}.    (Northern Swedish)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    over.there were there big  a  houses
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘There were big houses over there.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Delsing points out that these are non-argumental articles.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  It is less clear if this article occurs in German. While its existence is sometimes played down or even denied (e.g., \citealt{Schoorlemmer2009}: 238 fn. 50), I argue that it is beyond doubt that \textit{ein} occurs in plural contexts in this language as well. Having said that, there seems to be a difference between northern and southern dialects of German. As far as I have been able to establish, there are three syntactic contexts in Northern German where plural \textit{ein} occurs with a following overt noun. Similar to Yiddish and Swedish, German has plural \textit{ein} in the \textit{wh-}construction \REF{ex:key:20a}. Furthermore, these dialects seem to have \REF{ex:key:20b} (cf. \REF{ex:key:17b} in Dutch above). The latter type of example might be related to \REF{ex:key:20c}, at least in some dialects:\footnote{There is another context in which \textit{ein} can appear in plural contexts:(i)  \textit{die einen, die anderen}    the ones   the others    ‘these, those’However, this \textit{ein} is of a different type – it is adjectival (see Chapter 5).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%20
    \label{ex:key:20}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Was  für’ne / Was   für eine Idioten!} (Northern German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what for.a    / what for a      idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘What kind of idiots!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Eine Idioten!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a      idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Idiots!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{So’ne / So eine Idioten!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    so.a    / so a     idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Such idiots!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that all these forms involve exclamative contexts. As will become clear though, \REF{ex:key:20c} can also occur in argument positions in Northern German (see also the examples from Southern German below, where these strings occur in argument position without \textit{so} ‘such’). For the most part, I will focus on the exclamative constructions.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There is no doubt that \textit{’ne} in \REF{ex:key:20a} and \REF{ex:key:20c} stands for \textit{eine}. In an online search (for details, see momentarily), I identified 152 examples involving plural \textit{ein}. These examples are collected in the Appendix, Sections 1-3. Of these 152 tokens, 21 involved the full form \textit{eine}, 19 with \textit{was} \emph{für \textup{and} }2 with \textit{so} ‘such’ (both examples below are taken from the Appendix):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleCommentText}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%21
    \label{ex:key:21}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  "\emph{Was   für eine Idioten}" \textit{sagte Liam}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleCommentText}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleCommentText}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      what for  a     idiots      said  Liam
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleCommentText}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleCommentText}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘“Such idiots!” said Liam.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleCommentText}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \emph{Das Personal war sehr unfreundlich. Ich hatte noch nie     so eine Ferien.}~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the  staff         was very unfriendly     I     had    still  never so a      holidays  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘The staff was very unfriendly. I have never had such a (bad) vacation.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Indeed, \textit{’ne} is a typical reduction of the indefinite article as can readily be seen in the nominative feminine: \textit{eine Frau} > \textit{’ne Frau} ‘a woman’. Note that \textit{so’ne} can also be spelled as \textit{so ne} or \textit{sone}.\footnote{While spelling can only be used cautiously as evidence, notice that the speakers of the examples listed in the Appendix, Sections 1-3 spell \textit{so} and \textit{eine}, when the latter is reduced, as \textit{so ne} (61 times) or \textit{so’ne} (26 times). It is perhaps telling that not a single speaker provided the written form \textit{sone}. In my view, this hints at the fact that these speakers analyze the two elements as separate items, similar to \textit{so eine} (2 times).} I refer to \textit{so} and plural \textit{ein} by using the first variant (\textit{so’ne}).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In more detail, I conducted an online seach of the three cases in \REF{ex:key:20} above through Google, on Twitter, and on Facebook. All three cases in \REF{ex:key:20} occur with different frequencies: while \REF{ex:key:20b} is hard to search for, \REF{ex:key:20c} seems to be the most common construction in Northern German. The numeric results are provided in \tabref{tab:key:1}a. Note though that it would be easy to find more examples, especially for columns four and five of \tabref{tab:key:1}a, where I restricted my search to certain nouns, numerals, or adjectives (again, all these hits are listed in the Appendix, Sections 1-3):\footnote{When searching for these types of examples, care must be taken; for instance, many cases involve false positives where a following plural noun actually forms the first part of a singular compound:(i)   \textit{Was  für ne Männer Wg?} what for a   men       living.community.\textsc{fem}‘What kind of a living community for men?’The examples in the Appendix involve genuine plural nouns (which are not part of compounds).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\tabref{tab:key:1}a: Numeric Results of Plural \textit{ein} Identified in Online Media (September 20, 2020)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

String & \textit{was f}\emph{ü}\textit{r (ei)ne} & \textit{was f}\emph{ü}\textit{r (ei)ne} Adj-\textit{en} & {\itshape so (ei)ne} & \textit{so (ei)ne} Adj-\textit{en}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Source &  &  &  & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Google & 19 & 12 & 4 (with the noun \textit{Ferien} ‘holidays’) & 23 (with the adjective \textit{geil} ‘awesome’) and 10 (with s\emph{ü}\textit{ß} ‘cute’)\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Twitter & 28 & 4 & 3 (with the numeral \textit{zwei} ‘two’ following) & 23 (with the adjective \textit{geil} ‘awesome’) and 21 (with \textit{dumm} ‘stupid’)\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Facebook & hard to search & hard to search & hard to search & 5\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Total: 152 & 47 & 16 & 7 & 82\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the Appendix, Sections 1-3, the examples are provided in the order of the rows and columns of \tabref{tab:key:1}a, with the relevant strings in bold print for ease of identification (no glosses or translations are given there). Unless indicated otherwise, all examples below involving \textit{ein} in plural contexts are authentic; that is, these data are taken from the Appendix, Sections 1-3. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In addition to finding data in online media, I conducted a search of plural \textit{ein} in \textit{Datenbank für Gesprochenes Deutsch} (DGD; Database of Spoken German; available online at \url{https://dgd.ids-mannheim.de/}). The DGD involves a convenient platform comprising a number of spoken corpora that can be searched individually or collectively. This search yielded a total of 60 instances of plural \textit{ein}, broken down according to corpus and construction in \tabref{tab:key:1}b.\footnote{It is unlikely that this search was exhaustive, given the size of the corpora and the fact that plural \textit{ein} is spelled in the DGD in different ways. Transcribers of the DGD, who presumably have training in linguistics, spell \textit{so} and \textit{ne} about equally as two separate words like \textit{so ne} (8 times), \textit{so-ne} (18 times), \textit{so\_ne} (1 once; total count: 27) or as one word like \textit{sone} (27 times). However, there may be other variants. Note that when searching for examples, researchers should not enter \textstylew{\textit{für-ne}} or \textit{so-ne} (or \textstylew{\textit{für\_ne}} or \textit{so\_ne}, for that matter) in the search bar: while some data points are orthographically represented like this in some of the texts, they can only be found by replacing the hyphen (or underscore) with a space (i.e., \textit{f}\textstylew{\textit{ü}}\textit{r ne}, \textit{so ne}). Finally, it should also be pointed out that a number of very similar and a few unclear hits were removed from the results.} To allow a direct comparison, \tabref{tab:key:1}b is organized in the same way as \tabref{tab:key:1}a above (further below, I comment on the strong adjectives marked by ST in parentheses in \tabref{tab:key:1}b). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\tabref{tab:key:1}b: Numeric Results of Plural \textit{ein} Identified in DGD (October 6, 2024)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

String & \textit{was f}\emph{ü}\textit{r (ei)ne} & \textit{was f}\emph{ü}\textit{r (ei)ne} Adj-\textit{en} & {\itshape so (ei)ne} & \textit{so (ei)ne} Adj-\textit{en}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Source\textsuperscript{a} &  &  &  & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

AD & 2 &  &  & 1(ST)\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

BW & 1 &  & 18 & 9\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

BR &  &  &  & 1\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

DH &  &  & 2 & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

DNAM & 3 &  & 6 & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

FEGB &  &  & 1 & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

FOLK &  &  & 5 & 3\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

GWSS &  &  & 1 & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

MEND &  &  & 2 & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

RUDI &  &  & 1 & 1(ST) / 1(ST \textit{so})\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

ZW &  &  & 1 & 1\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Total: 60 & 6 &  & 37 & 17\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textsuperscript{a}Corpora:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

AD   = Australiendeutsch (Australian German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

BW   = Berliner Wendekorpus (Berlin Corpus of the Peaceful Revolution)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

BR   = Biographische und Reiseerzählungen (Biographical and Travel Stories)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

DH   = Deutsch heute (German Today)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

DNAM= Deutsch in Namibia (German in Namibia)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

FEGB   = Flucht und Emigration nach Großbritannien (Escape and Emigation to Great Britain)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

FOLK   = Forschungs- und Lehrkorpus Gesprochenes Deutsch (Research and Teaching Corpus of   Spoken German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

GWSS  = Gesprochene Wissenschaftssprache Kontrastiv (Spoken Scientific Language   Contrastively)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

MEND  = Mennonitenplautdietsch in Nord- und Südamerika (Mennonite Low German in North   and South America)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

RUDI   = Russlanddeutsche Dialekte (Russian German Dialects)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

ZW   = Zwirner-Korpus (Corpus named [after] Zwirner)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Similar to the search of online media, almost all relevant speakers in the DGD are from the northern part of Germany (to the extent available, this type of information is provided with all the retrieved examples in the Appendix, \sectref{sec:key:4}).\text{} About 45 percent of the examples involving plural \textit{ein} are contained in the Berlin Corpus alone – all speakers there are from Berlin and its surrounding area. In fact, some speakers from other corpora are also from this region. Note that in general, speakers are of different genders and ages (some born as early as 1879). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The examples in the Appendix, \sectref{sec:key:4} are sorted according to the name of the corpus they appear in, and the corpora are provided in alphabetical order. The examples are given in the order of the columns of \tabref{tab:key:1}b. As above, the relevant strings are provided in bold print, and no glosses or translations are given. Some comments on the numeric results in \tabref{tab:key:1}b are in order.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that there are relatively few instances (six) involving plural \textit{ein} in \textit{was-f}\emph{ü}\textit{r} contexts, and some of these instances are special in other ways. While one example is from a speaker in the Berlin Corpus (no specific information about this speaker is available), all five other examples were recorded in areas outside Germany – one adult speaker was born in Germany but lives now in Australia, one speaker is probably an interviewer from Germany making the recordings in Australia, and three speakers were pupils born in Namibia where at least one parent speaks German. One may wonder now why there are not more examples of this type. This probably has to do with the fact that these constructions are less frequent than those involving \textit{so’ne} in general (see also \tabref{tab:key:1}a) and the fact that plural \textit{ein} in \textit{was-f}\emph{ü}\textit{r} constructions typically involves strongly emotive language (e.g., swear words) used in exclamative contexts. None of the six examples are of this type, and the corpora in the DGD do not seem to involve many such instances in general (perhaps due to the fact that these are recorded conversations).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As to the three strong adjectives, there are two types to distinguish: two instances marked by (ST) vs. one instance indicated by (ST \textit{so}) in \tabref{tab:key:1}b. Starting with the first type, one of the two examples was found in the corpus \textit{Australiendeutsch} uttered by a person born in Australia with ancestors from Silesia; the other was identified in the corpus \textit{Russlanddeutsche Dialekte} said by a person born in the Ukraine. Regarding the second type, this example was retrieved from the corpus \textit{Russlanddeutsche Dialekte} uttered by the same person just mentioned where, crucially, the strong adjective precedes \textit{so’ne}. While the strong adjective in the latter case is expected (there is no preceding determiner), the first two instances are in contrast to what I have generally encountered in my searches. Having said that, these two cases presumably involve speakers where German is not the dominant language, and I will move forward assuming that only weak adjectives occur in these contexts. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These two searches totaled over 210 examples – all listed in the Appendix. Furthermore, there are a few examples that were identified in other ways as indicated further below (they are not part of the example counts in the tables above). Besides that, I also consulted some non-linguist friends from the county of Oberhavel (north of Berlin, Germany). All allowed \REF{ex:key:20c}, one found \REF{ex:key:20b} to be fine, but most stated that \REF{ex:key:20a} was somewhat marked. The individual who allowed \REF{ex:key:20b} took it to be a short form of \REF{ex:key:20c}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Observe now that the occurrence of \textit{ein} in plural contexts has already been established independently. Elmentaler \& \citet[389-394]{Rosenberg2015} document cases of plural \textit{so’ne} ‘(so’a =) such’ in \textit{Norddeutscher Sprachatlas} (Northern German Language Atlas).\footnote{The existence of plural \textit{ein} is particularly clear in the central and eastern parts of this area (in the western part, the plural form \textit{so} as in \textit{so} \textit{Dinge} ‘(so =) such things’ is more prevalent). Elmentaler \& \citet[383]{Rosenberg2015} also point out that the full form (\textit{so eine Dinge}) is not possible. I disagree. As mentioned above, I found a few examples of this type. In fact, if the indefinite element is stressed – as is fairly common in exclamative contexts, unreduced \textit{eine} is certainly possible (and indeed required; see some of the \textit{was-f}\emph{ü}\textit{r} constructions).} Below are two examples from Elmentaler \& \citet[392]{Rosenberg2015}, who identified this element in both interviews and (dinner) table conversations:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%22
    \label{ex:key:22}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{so’ne Kurse} (Northern German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    so a   courses
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘such courses’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{so’ne komischen Löcher}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    so a   weird         holes
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘such weird holes’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These authors agree with \citet[6-7]{Keller2004} that \textit{so’ne} fills a gap by forming an indefinite demonstrative. Note though that this idea cannot extend to the other constructions in (20a-b) (which lack \textit{so}). In my own search, I came across examples with clear geographical indications that these types of cases exist in Berlin, Hamburg, and Lübeck. In addition, linguistic features such as unshifted consonants (e.g., \textit{wat} for \textit{was} ‘what’) indicate the same geographical area.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Besides northern dialects of German, this element also occurs in southern dialects like Swabian and Bavarian \citep[106]{Glaser1993}. While its use and distribution are not the same across all subdialects, it is clear that plural \textit{ein} does occur \REF{ex:key:23a}. The latter is a Bavarian example taken from \citet[109]{Glaser1993}. Hubert Haider (p.c.) points out that these cases also exist in Southern Bavarian, for instance, in Carinthia, Austria \REF{ex:key:23b}. Another southern example was identified during my search of onlike media \REF{ex:key:23c}:\footnote{Note that second-person plural \textit{es} ‘you’ in \REF{ex:key:23b} derives from an old dual pronoun meaning ‘you (two)’ (Paul \textit{et al}. 1989: 222). The -\textit{s} on the verb \textit{brauchen} ‘need’ is an agreement marker sharing features with \textit{es}. I glossed the -\textit{s} on the verb \textit{haben} ‘have’ in \REF{ex:key:23c} as an encliticized \textit{es}. I thank David Fertig and Mark Louden for discussion of these points. Notice also that more cases of Southern Bavarian can be found in \textit{Kärntner Wörterbuch} (Carinthian dictionary) available at~\href{https://nam04.safelinks.protection.outlook.com/?url=http\%3A\%2F\%2Fwww.stelzel.at\%2Fkaernten-woerterbuach\%2Fwort-suachn & data=02\%7C01\%7CDorian.Roehrs@unt.edu\%7C898c9f868c1844be616208d85eba3e2f\%7C70de199207c6480fa318a1afcba03983\%7C0\%7C0\%7C637363501307224768 & sdata=IQdk9TJvjkG\%2BO\%2Fu5wBkFLnUx4ibawqb3ZG35j6UxqKU\%3D & reserved=0}{{http://www.stelzel.at/kaernten-woerterbuach/wort-suachn}}. In fact, these types of examples have even found their way into the media (\url{https://www.diepresse.com/443923/karntnerisch-fur-anfanger}) and advertisements (\href{https://nam04.safelinks.protection.outlook.com/?url=https\%3A\%2F\%2Fpensionria.at\%2Fwoerthersee-kaerntnerisch\%2F & data=02\%7C01\%7CDorian.Roehrs@unt.edu\%7Ca92114d23c0e4d0283d208d85f8406f3\%7C70de199207c6480fa318a1afcba03983\%7C0\%7C0\%7C637364367960730551 & sdata=gKpHkjpnbKev9SXmEq6vjuXAmE8ubb7AZC0YmQz3ThY\%3D & reserved=0}{{https://pensionria.at/woerthersee-kaerntnerisch/}}). I thank Hubert Haider for providing these sources to me.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%23
    \label{ex:key:23}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Dǫ    sàn õa Epfe   drõ}.    (Bavarian German)  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    there are a   apples there.on
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘There are apples on it.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Brauchts  es~  ane Untatatzalan~oda tans de  Schalalan alaan aa?}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

need.\textsc{agr} you a     saucers          or   do.it the cups          alone too
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Do you need saucers or will the cups by themselves do?’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  \textit{Was  für ne Vögel habts       denn da     aufgetrieben?}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what for a   idiots have.you \textsc{prt}   there found
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘What kind of idiots have you got there?’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Unlike many of the instances in Northern German, the cases in (23a-b) involve examples in argument positions (that also lack \textit{so} ‘such’).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Before moving on, it is worth pointing out that the occurrence of this \textit{ein} in Bavarian fits well with the Yiddish facts above. Notice in this respect that the plural indefinite article in Yiddish cannot be an innovation from Slavic (e.g., Polish or Russian) as these languages do not have articles to begin with. However, as is well known, Yiddish is historically related to southern dialects of German, specifically Bavarian (for some discussion, see \citealt{Jacobs2005}: chapter 2).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Indeed, diachronically, plural \textit{ein} is not a recent development as it occurred already in older varieties of German with a specific indefinite interpretation (ENHG stands for Early New High German):\footnote{The indefinite article began to evolve from the singularity numeral and indefinite pronoun \textit{ein} in Old High German (\citealt{BrauneReiffenstein2004}: 234). Consequently, I gloss \textit{ein} as ‘one/a’ in Old High German and Middle High German. Also, singular \textit{ein} in the older varieties is often paraphrased using Modern German \textit{irgendein, ein gewisser} (e.g., \citealt{BrauneReiffenstein2004}: 234). This could be rendered into English as ‘a certain’ in the singular and as ‘certain’ in the plural. Finally, I thank Chris Sapp for providing the example from ENHG. He points out though that this sentence could also have a singular interpretation of \textit{eyne} (i.e., ‘…with respect to the 15 families, one with the assessors and those who have sat on the council.’), but it seems unlikely that all of the assessors and city council members came from one particular family.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%24
    \label{ex:key:24}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{eino   ziti}            (Old High German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one/a times
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘(certain) times’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{in einēn buachon}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    in one/a books
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘in (certain) books’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(both from Otfrid, \citealt{BrauneReiffenstein2004}: 234)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%25
    \label{ex:key:25}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{in einen zîten}            (Middle High German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    in one/a times
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘during (certain) times’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      (Der Nibelunge \citealt{Nôt1143}:1, Paul \textit{et al}. 1989: 388)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{einu   liute}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one/a people
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘(certain) people’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      (Himmelreich Jerusalem 14:12, Paul \textit{et al}. 1989: 388)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%26
    \label{ex:key:26}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Dyt sijnt alle alsulge sachen vnd geschichte, as sich  dese  nyeste. xxxvj. Iair}       (ENHG)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  that are ~ all ~ such ~ ~ things ~and history ~      as \textsc{refl} these recent  36 ~    years\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

her  enbynnen der Stat van Coelne   oeurmitz      die. xv. geslechte eyne mit   den
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

~ ~  here in ~ ~ ~ ~ ~ ~the ~city of   Cologne by.means.of the 15  families   a       with the 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Scheffen  vnd den ghenen, die   mit     yn raide   gesessen haint, ergangen haint}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

~   assessors and the those ~ ~ ~who along in council sat          have  happened have
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Those are all such things and history, as has happened in the last 36 years here in the city of Cologne with respect to the 15 families, some with the assessors and those who have sat on the council.’ (1360 Hauwe Neues Buch Cologne, 33)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If these data are indeed related, then plural \textit{ein} has been in the language for a long time.\footnote{It is not clear if the development of the spurious article in Modern German is a direct development from the plural forms in the older varieties. A reviewer states that \textit{so’ne} may originally not come from \textit{so eine}, but may be a phonetically reduced form of \textit{solche}: In the 19th century, Georg Wenker had 40 Standard German sentences (“Wenkersätze”) translated into local dialects by schoolteachers and their pupils (a total of 40,000 schools; this questionnaire and its replies are available online at: \href{https://apps.dsa.info/wenker/}{{Wenkerbogen-Transliterations-App (dsa.info)}}). Sentence 28 was \textit{Ihr sollt nicht solche}\textbf{ }\textit{Kindereien treiben} ‘You should not engage in such childishness’. The determiner \textit{solche} is regularily translated as \textit{soon}, \textit{sone}, \textit{sonne}, \textit{sunne}, \textit{sön’n}, \textit{soon}, \textit{son’n} etc. in the Low German area and even in some of the Middle German areas. Sentence 36 was \textit{Was sitzen da für Vögelchen …} ‘What kind of little birds are sitting there …’. The replies to the \textit{was}{}-\textit{für} question may already show an enclitic indefinite plural article or an inflected preposition in some of the dialects of the Ruhr valley (town of Benrath: \textit{Wat setten door fün}\textbf{ }\textit{Voskes…}; town of Bredeney: \textit{Wat setten do von}\textbf{ }\textit{Vögelsches…}). Thus, the dialectal forms of \textit{solche} possibly have been reanalyzed as \textit{so} and \textit{eine}, and \textit{eine} – in some regions – has been reanalyzed as a spurious plural article and has spread to \textit{was-für} contexts. Given these two possible paths of development (direct or indirect via reanalysis), it is clear that more work is needed here to make a decision about the diachrony of plural \textit{ein}.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  It seems clear that these cases occur in colloquial contexts, in contemporary speech and writing. This is even acknowledged in Duden, both the dictionary and the grammar. Without indication of the dialect, these works state that \textit{so’ne} ‘such’ occurs in colloquial German ((27a) is taken from \citealt{Duden1989}: 1414 and \REF{ex:key:27b} from \citealt{Duden1995}: 276). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%27
    \label{ex:key:27}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{es gibt   immer  sone und solch-e}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    it  gives always so.a  and such-\textsc{pl}   
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘There are all kinds.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ich kann sone Leute   nicht leiden}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     can   so.a  people not    stand
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I cannot stand such people.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As regards writing, these constructions occur mostly in unedited texts like Twitter messages. However, they can also be found in edited contexts like ebooks such as \textit{Hilfe ich bin’n Vampir} ‘Help, I am a Vampire’ \REF{ex:key:28a} or \textit{Wolfskinder} ‘Wolf Children’ \REF{ex:key:28b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%28
    \label{ex:key:28}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{So ne} \textit{Mädchen gehn mir auf die Nerven.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    so  a   girls        go     me  on  the nerves
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Such girls get on my nerves.’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(\url{https://www.bookrix.de/book.html?bookID=cronalein_1360707129.5978798866#3276,468,12258})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Smilla recherchiert und entdeckt,  das schon   so eine Frauen verschwanden}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Smilla researches    and discovers that already so a     women  disappeared
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Smilla did some research and discovered that such women already disappeared.’        (\href{https://www.vorablesen.de/buecher/wolfskinder/rezensionen/duester-%09%09%09%09a6154566-6616-445c-967e-966d6578811c}{{https://www.vorablesen.de/buecher/wolfskinder/rezensionen/duester-        a6154566-6616-445c-967e-966d6578811c}})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that these examples are not counted in the tables above and that they are not listed in the Appendix. Consider some linguistic properties of these instances involving plural \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.2. Linguistic Properties
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These strings are clearly in the plural: \textit{ein} itself has a plural inflection, the following adjective has the typical weak ending, and the noun has a plural inflection along with the corresponding meaning. Compare \REF{ex:key:29a} to \REF{ex:key:29b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%29
    \label{ex:key:29}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \emph{Was  für ein-e geil-en}~          \textit{Bild-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what for a-\textsc{st}  awesome-\textsc{wk} picture-s
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Such awesome pictures!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{dies-e     geil-en           Bild-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    these-\textsc{st} awesome-\textsc{wk} picture-s
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘these awesome pictures’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Furthermore, verbs and pronouns agree with the plural noun phrase in number:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%30
    \label{ex:key:30}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Was  für eine Idioten wohn-en nur in Sachsen-Anhalt?} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what for a      idiots   live-\textsc{pl}    \textsc{prt} in Sachsen-Anhalt
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘What kind of idiots live in Saxony-Anhalt?’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{was   für eine Idioten, die        so Aufmerksamkeit woll-en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what for a      idiots     who.\textsc{pl} so attention             want-\textsc{pl}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘what kind of idiots that want attention like this’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Observe that these cases do not involve syntactically frozen (or fossilized) templates: they can involve several adjectives in a row \REF{ex:key:31a}, coordinated adjectives \REF{ex:key:31b}, and nominalized adjectives \REF{ex:key:31c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%31
    \label{ex:key:31}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Wieso gibt   es so ne dummen, bescheuerten Leute   die  sowas          machen?? D:}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    why    gives it  so a  stupid      dumb             people who so.something do
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Why are there such stupid, dumb people that do something like that?’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{es gibt  halt so ne dummen und abgehobenen leute}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  it gives \textsc{prt} so a   stupid     and stuck-up         people
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘There are indeed such stupid and stuck-up people.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{ach  so {}'ne dummen gibt   es öfter. xD}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    oh    so a   stupid     gives it  more.often
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Oh, there are often such stupid ones.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note in this regard that plural \textit{ein} is not an adjectival article, an article triggered by the presence of an adjective. This is clear from the fact that the cases without an adjective outnumber those with an adjective at a ratio of about 2.5 to 1 (see \tabref{tab:key:1}b above). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Additionally, these types of cases may include numerals (32a-b) or other preceding elements such as \textit{irgend} ‘any’ \REF{ex:key:32c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%32
    \label{ex:key:32}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{drei}~  \emph{so ne geilen}~     \textit{Mädels}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    three so a   awesome girls
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘three such awesome girls’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{Da    waren heute 2 so'ne dummen Weiber beim  Zumba-Kurs,   die}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  there were   today 2 so.a   stupid     women at.the Zumba-course who
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘Today, there were two such stupid women at the Zumba course who’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  \textit{Alter irgend so ne dummen Hunde haben direkt    neben  mir n Böller   gezündet}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  man  any     so a   stupid     dogs    have   directly next.to me a firecracker lit
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘Man, some such stupid idiots lit a firecracker right next to me.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Indeed, numerals can also follow \textit{so’n}e:\footnote{I have not found any examples involving numerals in \textit{was-f}\emph{ü}\textit{r} constructions. Note though that while (ia) sounds possible to my ears, (ib) is completely out:(i)  a.  \textit{was  f}\emph{ü}\textit{r ne zwei Typen}      what for a  two  guys      ‘what kind of two guys’    b. *  \textit{was   f}\emph{ü}\textit{r zwei (ei)ne Typen}      what for two   a        guys}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%33
    \label{ex:key:33}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{ach man wir sind schon so ne zwei wärmflaschen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    oh  boy   we are   \textsc{prt}     so a   two  warm.bottles
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Oh boy, we are such two hot-water bottles.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{und so ne zwei Kämpfer die   alles umpflügen,}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    and so a   two  fighters   who all     under.plough
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘and such two guys, who create such chaos’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, as we might expect, \textit{was-f}\emph{ü}\textit{r} splits are also possible. Compare \REF{ex:key:34a} to \REF{ex:key:34b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%34
    \label{ex:key:34}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{was~} \emph{für ne krassen}\textit{~Menschen albanische Wurzeln haben}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what for a   weird     people      Albanian    roots      have
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘what kind of weird people have Albanian roots’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{was  meine Unachtsamkeit~}\emph{für ne krassen}\textit{~Folgen            hat}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what my     carelessness     for a   bad        consequences has
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘what kind of bad consequences my carelessness has’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To be clear, these are not frozen (or fossilized) templates but productive constructions in (Northern) German. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up, I take it as established that \textit{ein} occurs in plural contexts in German. Indeed, \textit{ein} exhibits the hallmarks of a plural element: morphologically, \textit{ein} itself has a typical plural ending; syntactically, it may be followed by an adjective with a weak plural ending; and semantically, it occurs within a noun phrase with plural meaning. As to its geographical distribution, cases like \textit{so’ne Äpfel} ‘such apples’ are common in the northern dialects, and instances like \textit{eine Äpfel} ‘(an) apples’ occur in the southern dialects. Plural \textit{ein} also appears in \textit{wh-}constructions, but this seems less common.\footnote{While \citet[341-42]{Duden1995} discusses \textit{was-für} constructions with special forms for the plural in Northern German, this work explicitly states that there is no \textit{ein} in these cases. As is clear, these forms do occur, at least with some speakers.} While a more systematic investigation of the differences between the various dialects and constructions must be left for future research, I proceed on the assumption that most (possibly all) dialects of German have some evidence of plural \textit{ein}. Most of the following discussion focuses on the constructions of the northern dialects. More generally, we may observe that the plural indefinite articles in Dutch, Yiddish, and Swedish are in good company. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To conclude the entire section, what this brief illustration of adjectival inflections and indefinite articles has made clear is that languages, even closely related ones like German, Yiddish, and Norwegian/Swedish, differ in these empirical domains. Consequently, they should not receive the same account. Analyses have been advanced for all these languages (e.g., \citealt{Olsen1991b}, \citealt{Vater2002} for German; \citealt{Roehrs2015}, 2022 for Yiddish; \citealt{Julien2005a}, \citealt{Delsing1993} for Norwegian). However, many languages lack detailed analyses of some or many of the non-canonical constructions (with the notable exception of English, see \citealt{Perlmutter1970}). More empirical and theoretical work is called for. Awaiting more comprehensive analyses of Yiddish and Norwegian, I focus on German, the language I know best, in the rest of this book striving to make headway for this language. Specifically, I focus on (colloquial) Standard German. Having said that, I make a few cross-linguistic remarks throughout the discussion (e.g., when another language exhibits a revealing similarity or difference).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As for empirical grounding of this study, most of the data involving adjectival inflections have been reported in previous work (e.g., in the well-known reference grammar Duden). They appear to be relatively straightforward and accepted. As already seen above, this statement is only partially true of \textit{ein}. Furthermore, a distinction between the reduced, unstressed forms of the indefinite article (e.g., \textit{ein} > \textit{’n} ‘a’) and the unreduced, stressed forms of \textit{ein} (e.g., \textit{ein} > \textit{EIN} ‘one’) is often not made in the literature. However, such a distinction is made here and plays a crucial part in the classification and analysis of these elements below.\footnote{For some phonetic discussion of indefinite (and definite) articles in spontaneous speech, see \citet{Wesener1999}. I think Laura Smith for pointing out this reference to me.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.  Basic Properties and Main Hypotheses
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I discuss some of the characteristics of adjectival inflections and the indefinite article \textit{ein} ‘a’. On the basis of that, I formulate the main hypotheses of this book. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{3.1.  Adjectival Inflections and the Article} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Taking German as the main language under investigation, this monograph focuses on two elements: adjectival inflections and \textit{ein} ‘a’. While these elements have an overt realization and thus receive an interpretation in PF, I argue that they are semantically vacuous. Adjectival inflections are affixes and \textit{ein} is, at least in some cases, a free morpheme. I suggest later that adjectival inflections are licensed by movement of the adjective stem and \textit{ein} by that of certain operators. Some of these operators have an overt manifestation of their own and some do not. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

First, I briefly illustrate why these elements are of interest. Similar to the cases in \sectref{sec:key:1}, the assumption that adjectival inflections and \textit{ein} make semantic contributions raises some issues. In general terms, if adjectival inflections and \textit{ein} had semantic import, that would lead to some contradictory conclusions. These issues disappear if we assume that these elements are semantically vacuous. This assumption is strengthened by the fact that while these elements have to be generally present for independent reasons, they can be left out in certain, well-defined contexts without a loss of meaning. Indeed, these elements can also co-occur without a change in interpretation. I illustrate these points in more detail below.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.1.1. Basic Properties of Adjectival Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Jacob \citet[718-756]{Grimm1870} pointed out that adjectives can alternate as regards their inflections: in his (now widely-accepted) terminology, adjectives can exhibit strong or weak endings.\footnote{In the literature, there are other terms for these inflections: pronominal (i.e., diachronically related to third-person pronouns and pronominals) vs. nominal, determining vs. determined, and primary vs. secondary. Here, I adopt the neutral terms strong and weak.}  The distribution of these endings is often referred to as the strong/weak alternation. Before turning to the distribution of these inflections, I start with some brief diachronic remarks.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  It is generally assumed (for detailed discussion, see, e.g., \citealt{Evans2019}, \citealt{Lockwood1968}: 37-46, \citealt{Petrova2024}: 187-92, \citealt{Pfaff2020}, \citealt{Rehn2019}: chapters 2 and 3, and references cited therein) that Proto-Indo-European did not have a distinct category of adjectives but rather seemed to have a more general category of nominal elements (comprising adjectives and nouns). The strong inflections on adjectives are a continuation of the old nominal \textit{o}{}-/\textit{a}{}-stem inflections into Germanic, but these inflections also recruited several endings from the pronominal paradigms (e.g., demonstratives). Besides a set of strong adjective inflections, this yielded a difference between the nominal categories of noun and adjective. By contrast, weak inflections appear to be an innovation in Germanic. Specifically, a nominalizing suffix -\textit{n}{}- was added that produced \textit{n}{}-stem nominals and eventually adjectives with weak inflections (for a dissenting view, see \citealt{Evans2019}: chapter 2). Finally and returning to the strong inflections, \citet[152-53]{Evans2019} points out that regular sound changes in West Germanic led to a number of zero endings in the strong paradigm that, one the one hand, resulted in uninflected adjectives in predicative contexts but, on the other hand, were later replenished by the inflections of pronominals yielding uniformly inflected adjectives in attributive contexts in (Standard) German (but see also \citealt{Rehn2019}: 135-39).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Returning to the main focus of the discussion, Modern German, note first that noun phrases inflect for case, number, and gender.\footnote{For the most part, I do not consider person in this book.} While specifications for number and, to some degree, case can be inspected on suffixes to nouns, gender is usually expressed on other elements – by the inflections on determiners including articles and on adjectives. As mentioned above, adjectives can alternate between exhibiting strong or weak inflections. It is also worth pointing out already here that strong inflections have more different forms than weak ones thus expressing more distinctions in case, number and gender (Chapter 2). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As for the distribution of these inflections, consider the canonical cases in \REF{ex:key:35}. We can observe that the strong/weak alternation involves an adjective with a weak inflection if a preceding determiner has a strong inflection \REF{ex:key:35a}, but it shows an adjective with a strong inflection if not \REF{ex:key:35b}. This distribution is concisely described in the generalization in \REF{ex:key:36} (for statements along these lines, see \citealt{Bierwisch1967}: 257, \citealt{Eisenberg1998}: 171-73, \citealt{Gallmann1998}: 144, \citealt{Giusti2015}: 207, G. \citealt{Müller2002a}: 129, \citealt{Petrova2024}: 184-85, \citealt{Pfaff2017}: 286, \citealt{Rehn2019}: 58, \citealt{Sauerland1996}: 34, \citealt{Schoorlemmer2009}: 53, and many others):\footnote{Katzir \& \citet[268]{Siloni2014} claim that this type of generalization holds for all Germanic languages. For the discussion of the Principle of Monoinflection, see Chapter 2.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%35
    \label{ex:key:35}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{d-er    gut-e        Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{st} good-\textsc{wk} coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the good coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{ein gut-er    Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a    good-\textsc{st} coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a good coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%36
    \label{ex:key:36}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Weak After Strong}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

An adjective with a weak inflection is preceded by a determiner with a strong inflection. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Notice that adjectives with strong inflections are not explicitly mentioned in the generalization. However, since adjectives in prenominal position only alternate between showing strong or weak inflections, the generalization above implies that the strong inflections occur in all instances not mentioned in \REF{ex:key:36}. Given this, the generalization in \REF{ex:key:36} captures the cases in \REF{ex:key:35}. For brevity, it is often referred to as Weak After Strong. Notice that when the term Weak After Strong is used, the reader should keep in mind that weak and strong refer to inflections on elements of different lexical categories (adjective vs. determiner). This is important as we will see later that two co-occuring adjectives have the same ending (and the same applies to two determiners to the extent these elements can co-occur). Note also that although it is the inflection of the adjective that alternates (i.e., the adjective \textit{gut}{}- ‘good’ in \REF{ex:key:35} above has either a weak or a strong inflection), I often refer to these cases simply as strong or weak adjectives. This is in contrast to verbs and nouns, which are lexically specified as strong or weak.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Observe that the generalization in \REF{ex:key:36} is surface-oriented making reference to the notion of precedence. It works quite well for canonical cases such as \REF{ex:key:35} above. However, it fares less well with some other instances. There are two types of exceptions (see Chapter 3, \sectref{sec:key:7} for fuller discussion). First, strong adjectives can be preceded by determiners with a strong inflection. This holds when an inflected demonstrative precedes an uninflected possessive article \REF{ex:key:37a}. Second, weak adjectives can be preceded by determiners without strong inflections. This is the case with pronominal determiners (e.g., \citealt{DéchaineWiltschko2002}: 421-22, \citealt{Höhn2020}, \citealt{Pesetsky1978}, \citealt{Postal1966}, \citealt{Roehrs2005}), which do not have an inflection in the first and second person \REF{ex:key:37b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%37
    \label{ex:key:37}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.    \textit{dies-es mein groß-es   Glück}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this-\textsc{st} my    great-\textsc{st}  happiness.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this great happiness of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{ihr  \textsubscript{} }\textit{nett-en   Studenten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you nice-\textsc{wk} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It seems clear that a simple notion of precedence referring to inflections cannot capture all the distributions. Rather, it is argued in Chapters 2 and 3 that structural and lexical factors need to be taken into account. Note though that other explanations have also been put forward.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is sometimes suggested that this alternation involves a semantic distribution; that is, it correlates with the (in-)definiteness of the larger noun phrase. This kind of statement is usally made in the context of the Scandinavian languages (see again \sectref{sec:key:2.1.2} above) and the older varieties of German (see Chapter 2, \sectref{sec:key:1}). However, it can, occasionally, also be found in the older literature on Modern German (e.g., \citealt{Curme1910}, \citealt{Lockwood1968}, \citealt{Prokosch1939}; see \citealt{Esau1973} for critical discussion of these works) and even in the more recent literature (e.g., \citealt{KarnowskiPafel2004}: 177-78, \citealt{Pafel1994}: 257-64). Having said that, a consensus seems to have emerged that the strong/weak alternation is not regulated by the semantics in Modern German but rather by the morpho-syntax (see \citealt{Harbert2007}: 131 and the many references cited below). As I show in detail throughout this book, this characterization is indeed correct, with the qualification that there is also a lexical component to the explanation. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To provide some brief illustration here, there are indeed many cases where a strong inflection occurs in an indefinite context \REF{ex:key:38a} and a weak ending in a definite environment \REF{ex:key:38b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%38
    \label{ex:key:38}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{lauter dumm-e  \textsubscript{} }\textit{Idioten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    many \textsubscript{} stupid-\textsc{st} idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘many stupid idiots’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{wir dumm-en  \textsubscript{} }\textit{Idioten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    we \textsubscript{} stupid-\textsc{wk} Idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘us stupid idiots’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

However, a weak inflection can also occur in an indefinite context, and a strong ending is possible in a definite environment as well. Consider \REF{ex:key:39a} and \REF{ex:key:39b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%39
    \label{ex:key:39}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{mancher klug-e       Freund}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    some      \textsubscript{} smart-\textsc{wk} friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘many a smart friend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Peters  klug-er   Freund}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter’s smart-\textsc{st} friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Peter’s smart friend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is clear from the two paradigms in \REF{ex:key:38} and \REF{ex:key:39} that strong endings do not correlate with indefiniteness or weak endings with definiteness. Below, I demonstrate that this holds more generally. Faced with these contradictory patterns, I propose that adjectival inflections have neither semantics of their own nor do they make such features visible – they are not associated with (in-)definiteness. This is what I mean by stating that these elements are semantically vacuous.\footnote{In the context of Icelandic, \citet[293]{Pfaff2017} states that the “weak inflection – rather than expressing definiteness by itself – is a semantically vacuous reflection of definiteness marked elsewhere.” Crucially, unlike in German, the weak inflection in Icelandic is dependent on the presence of definiteness in the DP. \citet[316]{Pfaff2017} makes this dependency more formal by postulating an agreement relation between a definite D and an uninterpretable definiteness feature [uDEF) on the adjectival inflection. Adjectival inflections in German do not have such a feature – they do not reflect definiteness.} If so, the expectation is that adjectival inflections can be left out without a change in meaning.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Adjectival inflections do not appear under certain, well-defined structural and lexical conditions in German. Structurally, adjectives must be inflected before the noun (40a-b), but they remain uninflected when they follow it (40c-d):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%40
    \label{ex:key:40}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein auf seine Frau stolz-er  \textsubscript{} }\textit{Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    an  of  \textsubscript{} his    wife  proud\textsc{{}-st} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a man proud of his wife’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{der auf seine Frau stolz-e      Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the \textsubscript{} of  \textsubscript{} his    wife  proud-\textsc{wk} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the man proud of his wife’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{ein Mann,        stolz   auf seine Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a    man.\textsc{masc} proud of   his    wife
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a man, proud of his wife’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{der Mann,        stolz   auf seine Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the \textsubscript{} man.\textsc{masc} proud of   his    wife
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the man, proud of his wife’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Comparing \REF{ex:key:40a} to \REF{ex:key:40c} and \REF{ex:key:40b} to \REF{ex:key:40d}, there is no change in the (in-)definiteness of the noun phrase, and yet adjectival inflections are present in (40a-b) but absent in (40c-d). This could be evidence that inflections make no semantic contribution. One objection might be that the adjectives in (40c-d) are not part of the noun phrase proper given that they belong to reduced relative clauses. While this may be a valid point, there is other evidence that adjectival inflections can be left out without a loss in meaning. Before I turn to these cases, note that the paradigm in \REF{ex:key:40} does indicate that structural conditions play a role in the occurrence of adjectival inflections – only prenominal adjectives have inflections.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Besides being sensitive to structure, adjectival inflections can or must be left out in certain lexical contexts; that is, adjectival inflections are also sensitive to lexical conditions. Among others, this can be seen with specific sets of adjectives that can occur prenominally and as such, they are clearly part of the noun phrase. As just mentioned, the inflections on these adjectives can or must be absent. It will become clear though that the presence or absence of these inflections does not lead to a change in the semantics. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

First, there is a special set of color adjectives involving \textit{lila} ‘purple’ and \textit{rosa} ‘pink’ as well as \textit{orange} ‘orange’ and \textit{beige} ‘beige’ that show that adjectival endings can be optional. Note that the first two adjectives (\textit{lila}, \textit{rosa}) require -\textit{n}{}- to be inserted (41a-b) but that the second two adjectives (\textit{orange}, \textit{beige}) do not \REF{ex:key:41c}:\footnote{Notice that the pronunciation of \textit{orange(-s)} ‘orange’ in \REF{ex:key:41c} has some special properties. Without the inflection -\textit{s}, -\textit{ge} is pronounced as [ʃ] – the -\textit{e} is silent triggering Final Devoicing of the consonant. However, when the inflection is added yielding -\textit{ges}, this sound sequence is rendered as [ʒəs]. Observe that besides \REF{ex:key:41c}, \textit{das orange Kleid} ‘the orange dress’ is also possible. Here, however, the ending on \textit{orange} (i.e., -\textit{e}) is pronounced as schwa and can be interpreted as the regular weak ending. This interpretation is not possible with \textit{lila} and \textit{rosa}. The latter two end in -\textit{a}, which is not pronounced as schwa.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%41
    \label{ex:key:41}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein lila(-n-es)   Kleid} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a    purple-n-\textsc{st} dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a purple dress’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{das lila(-n-e)      Kleid}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the purple-n-\textsc{wk} dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the purple dress’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{ein orange(-s) Kleid}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    an  orange-\textsc{st}  dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘an orange dress’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that the presence or absence of the inflection makes no semantic difference.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Second, with a few types of adjectives, the inflection must be absent. This holds for adjectives derived from geographical names, so-called toponymic formations (-\textit{er} is an adjective-forming suffix):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%42
    \label{ex:key:42}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein Berlin-er(*-es)    Bier}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a    Berlin-\textsc{adj}(-*\textsc{st)} beer.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘a Berlin beer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{das Berlin-er(*-e)      Bier}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

the Berlin-\textsc{adj}(-*\textsc{wk)} beer.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the Berlin beer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The same applies to adjectives like \textit{prima} ‘great’, \textit{sexy} ‘sexy’, \textit{super} ‘super’, etc. \citep[256-57]{Duden1995}.\footnote{A reviewer points out that the latter adjectives are loan words that may not be fully intergrated into the language yet. This also applies to the color adjectives discussed in the previous paragraph.} Note that since adjectival inflections are absent, they cannot indicate or contribute to (in-)definiteness.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To be clear, while inflections on prenominal adjectives are usually obligatory, there are some special cases where they can or must be absent. Importantly, this presence or absence of inflection does not correlate with a change in the (in-)definite interpretation of the noun phrase as a whole. Besides the semantic property of (in-)definiteness, I demonstrate in Chapter 4 that adjectival inflections are also not a reflex of other semantic concepts such as (non-)restrictiveness of the interpretation of modifiers or referentiality. In view of that, the account of the strong/weak alternation must involve something else. Consider the following.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The following noun phrases in the (a)-examples are in the nominative and the ones in the (b)-examples are in the dative. In keeping with the facts above, the strong and weak endings occur both in indefinite and definite contexts:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%43
    \label{ex:key:43}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein rot-es \textsubscript{} }\textit{Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a    red-\textsc{st} car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a red car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mit\textsubscript{} } \textit{einem rot-en   Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with a    \textsubscript{} red-\textsc{wk} car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with a red car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%44
    \label{ex:key:44}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{sein rot-es \textsubscript{} }\textit{Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    his  \textsubscript{} red-\textsc{st} car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘his red car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mit\textsubscript{} } \textit{seinem  rot-en   Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with his       \textsubscript{} red-\textsc{wk} car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with his red car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Again, it is unlikely that semantic concepts like (in-)definiteness can explain the strong/weak alternation. Rather, it seems more promising to find an explanation in the morpho-syntax. Considering the similarities between \REF{ex:key:43} and \REF{ex:key:44}, I assume that possessive articles consist of a possessive component (\textit{s}{}-) and \textit{ein} (Chapter 5). If so, the types of adjectival endings seem to be a function of preceding \textit{ein}, independently of the presence or absence of the possessive component. In other words, \REF{ex:key:43} and \REF{ex:key:44} are directly relatable. Given that, we might claim that the strong/weak alternation is a reflex of the semantics of \textit{ein} behaving differently in the nominative vs. dative case. However, like adjectival inflections, \textit{ein} exhibits not only contradictory properties with regard to the semantics but also shows characteristics of having no meaning at all. These points can be made most straightforwardly as regards semantic number.\footnote{Making the following points with regard to the (in-)definiteness of \textit{ein} would take me too far afield here. I discuss the relation between adjectival inflections and \textit{ein} as regards (in-)definiteness in detail in Chapter 5. I propose there that \textit{ein} has nothing to do with (in-)definiteness, neither in the nominative nor in the dative. Thus, this semantic dimension cannot explain the distribution of adjectival inflections either.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.1.2. Basic Properties of \textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As briefly mentioned in \sectref{sec:key:2.2.1}, the indefinite article \textit{ein} emerged on the basis of the singularity numeral and indefinite pronoun \textit{ein} in Old High German (e.g., \citealt{BrauneReiffenstein2004}: 234, \citealt{Presslich2000}, \citealt{Rehn2019}: 88-90). It is often claimed that \textit{ein} in Modern German is semantically singular in meaning (e.g., \citealt{Vater2002}). Specifically, \REF{ex:key:45a} makes mention of the fact that a girl was seen, and \REF{ex:key:45b} emphasizes the point that only one girl has a certain property, namely having been at a party (parentheses around \textit{ei} yielding \textit{n} indicate the reduced form of \textit{ein}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%45
    \label{ex:key:45}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Ich habe gestern   (ei)n Mädchen getroffen}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I    have  yesterday a     girl.\textsc{neut} met
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I met a girl yesterday.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Nur}  \textit{EIN Mädchen war auf der Party!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    only one \textsubscript{} girl.\textsc{neut}\textsubscript{} was at   the party
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Only one girl was at the party.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Crucially, \textit{ein} can also imply the existence of a second entity \REF{ex:key:46a}. In these cases, the first clause often, but not always, occurs with the second one put in parentheses below. Independently of the second clause, \textit{ein} preceded by a definite determiner yields a duality partitive; that is, it is presupposed that there is a second girl present (NB: this is different in the English counterpart).\footnote{In English, \textit{one} following a definite article often has an intensifying meaning close to ‘only’:(i)  She was the (one) girl I did not recognize.} In fact, \textit{ein} can be part of a noun phrase denoting a more numerous plurality of entities. As already discussed in \sectref{sec:key:2.2}, \textit{ein} can clearly occur in semantically plural contexts. To repeat, \REF{ex:key:46b} is a googled example from the Appendix, and \REF{ex:key:46c} is an example from the ebook \textit{Wolfskinder} ‘Wolf Children’:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%46
    \label{ex:key:46}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Das ein-e     Mädchen blieb        stehen (, das andere ging  weg)}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the  one-\textsc{wk} girl.\textsc{neut} remained standing, the \textsubscript{} other   \textsubscript{} went away
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘One of the two girls stopped walking, the other went away.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \emph{Was  für ein-e geil-en}\textit{~          Bilder    wer hat  die    bloß gemacht}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

what for a-\textsc{pl}  awesome-\textsc{wk} pictures who has those just  made
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Such awesome pictures! Just who took those?’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Smilla recherchiert und entdeckt,  das schon   so ein-e Frauen verschwanden}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Smilla researches    and discovers that already so a-\textsc{pl} women  disappeared
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Smilla did some research and discovered that such women already disappeared.’        (\href{https://www.vorablesen.de/buecher/wolfskinder/rezensionen/duester-%09%09%09%09a6154566-6616-445c-967e-966d6578811c}{{https://www.vorablesen.de/buecher/wolfskinder/rezensionen/duester-        a6154566-6616-445c-967e-966d6578811c}})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As with adjectival endings, we are faced with a contradictory state of affairs: in this case, \textit{ein} seems to imply not only singularity but also different pluralities. Given this, we might also expect that similar to adjectival inflections, \textit{ein} can sometimes be left out without a change in meaning.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To illustrate this point, consider nominals in predicate contexts. Note that \textit{ein} is obligatory in \REF{ex:key:47a} but that it is optional for many speakers in \REF{ex:key:47b}:\footnote{I comment on the optionality of \textit{ein} with [-figurative] roles nouns as in \REF{ex:key:47b} further below.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%47
    \label{ex:key:47}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Er ist ein Mann}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is  a    man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘He is a man.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Er ist (ein) Lehrer}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is   \textsubscript{} a     teacher.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘He is a teacher.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is clear that both of these predicate nominals denote a property; that is, \textit{ein} does not make a semantic contribution with regard to singular number (or indefiniteness, for that matter). Following de Swart \textit{et al}. (2007), I argue in Chapter 6 that the presence of \textit{ein} is a function of the different types of nouns, a kind noun in \REF{ex:key:47a} vs. a role noun in \REF{ex:key:47b}. Thus, similar to adjectival endings, I conclude that the presence or absence of \textit{ein} makes no difference with regard to the semantics, here illustrated with semantic number. In fact, I show below that this type of element is not a reflex of other semantic concepts such as indefiniteness or a certain type of emotiveness either. I propose that \textit{ein} neither has semantics nor does it make such features visible – it is semantically vacuous. This also means that the strong/weak alternation of adjectives briefly illustrated in \sectref{sec:key:3.1.1} cannot be explained by the semantics of \textit{ein} either.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  If it is true that adjectival inflections and \textit{ein} are semantically vacuous, then we may wonder what their function is; that is, why they exist at all. This question is particulary interesting in view of the fact that similar to the expletives in \sectref{sec:key:1}, these elements can also co-occur with elements that seem similar or related in some way.\footnote{Recall that \textit{there} can occur with a locative adverbial and that the (formally definite) proprial article occurs with a proper name.} In fact, here adjectival inflections and \textit{ein} can each co-occur yielding multiple instances of the same elements; consider \REF{ex:key:48a} and \REF{ex:key:48b}. I assume for now (and argue in Chapter 5) that similar to possessives like \textit{sein} ‘his’, the negative article \textit{kein} ‘no’ consists of a negative element, NEG, and \textit{ein} (where NEG is later spelled out as \textit{k}{}-; \REF{ex:key:48b} is adapted from \citealt{GallmannLindauer1994}: 24):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%48
    \label{ex:key:48}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{frisch-er heiß-er Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    fresh-\textsc{st}  hot-\textsc{st}  coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘fresh hot coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{k-ein  \textsubscript{} }\textit{so’n Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsc{neg}{}-a so a man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘no such man’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Importantly, given the proposed pleonastic status of the adjectival inflections and \textit{ein}, note that not only one but both of these elements seem to be redundant. Observe though that neither of the two adjectives in \REF{ex:key:48a} can occur with no ending at all. Similarly, neither \textit{ein} nor \textit{’n} in \REF{ex:key:48b} can be left out. If adjectival inflections and \textit{ein} were to make specific semantic contributions, then it would remain unclear why several instances of the same elements can, and in these cases must, co-occur. Again, this curious fact finds a natural explanation if we assume that adjectival endings and \textit{ein} are semantically vacuous and their obligatory presence in \REF{ex:key:48a} and \REF{ex:key:48b} is due to a different, independent reason or reasons. Below, I propose that adjectival inflections make abstract morpho-syntactic features visible and that \textit{ein} supports certain semantic operators.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To recapitulate, I have illustrated some cases where adjectival inflections and \textit{ein} seem to lead to contradictory conclusions about their relation to the semantics: adjectival inflections, strong or weak, occur in both definite and indefinite contexts, and \textit{ein} appears in singular and plural environments. At the same time, we have seen that these elements seem to have no semantics at all as they are absent in certain, well-defined contexts without loss of meaning: adjectival inflections do not have to or cannot occur with special sets of adjectives, and \textit{ein} does not have to occur with a specific set of nouns in predicative contexts. Furthermore, I provided evidence that these elements can co-occur without any semantic import. I proposed that adjectival inflections and \textit{ein} are semantically vacuous. If so and returning to the more general issues, what is interesting then about adjectival endings and \textit{ein} is that they are overt elements that receive no interpretation in LF – like \textit{there} and the proprial article discussed above.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{3.2.  Main Hypotheses and Relatedness of Adjectival Inflections and} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I briefly summarize the general characteristics of adjectival inflections and \textit{ein}, and I formulate the main hypotheses about these elements. On the basis of that, I provide further motivation why adjectival inflections and \textit{ein} can be fruitfully discussed in tandem.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.1. Main Hypotheses
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Some of the main general properties of adjectival inflections and \textit{ein} have already been touched upon; others will be introduced later on in the book. They can be summarized as follows. Specifically, while these elements (usually) have to occur for independent reasons, adjectival inflections and \textit{ein} can each:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumxviiileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

co-occur
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

appear in contradictory semantic contexts
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

be left out in certain, well-defined contexts without loss of meaning
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumxviiileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Furthermore, adjectival inflections are \textit{not} a reflex of:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumxviileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(in-)definiteness
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(non-)restrictiveness of interpretation of modifiers
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

referentiality
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumxviileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, \textit{ein} is \textit{not} a reflex of:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumxixleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

indefiniteness
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

emotiveness
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

singular number/countability
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumxixleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given this, I formulate the two main claims that adjectival endings and \textit{ein} have in common. While \REF{ex:key:49a} has already been mentioned above, I add \REF{ex:key:49b} now:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%49
    \label{ex:key:49}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           \textit{Hypothesis 1}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Adjectival inflections and \textit{ein}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      a.     are expletive elements and
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      b.     indicate abstract structure in the noun phrase.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Hypothesis 1b is slightly different for adjectives and \textit{ein}. This can be fleshed out in the two (a)-statements below. In addition, each of these two types of elements have another difference summarized in the (b)-statements:\footnote{Note though that the (b)-statements are also partially relatable: both adjectival inflections and \textit{ein} make elements visible, morpho-syntactic features vs. operators (the latter in the context of flagging, cf. \REF{ex:key:51b}). On a different note, I argue in Chapter 5 that there are two basic types of \textit{ein}: the so-called indefinite article characterized in Hypothesis 3 and adjectival \textit{eine}, which is not related to the article: adjectival \textit{eine} is different in that it has semantics and is argued to be in a different position.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%50
    \label{ex:key:50}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Hypothesis 2}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Adjectival inflections:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   a.  indicate abstract structure in the higher layers of the noun phrase (DP vs. LPP);       they provide clues about structures involving various degrees of embedding of       adjectives (simple vs. complex DPs), and
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.  they make nominal features like case, number, and gender visible.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%51
    \label{ex:key:51}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Hypothesis 3}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{Ein}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   a.  indicates abstract structure in the lower layers of the noun phrase (NP vs. ArtP),       and
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.  it supports overt semantic operators (e.g., NEG \textit{k}{}-) and flags the presence of       covert semantic operators (e.g., REL).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Some remarks are in order here. The structural levels LPP and ArtP stand for Left Periphery Phrase and Article Phrase. The first is above the DP-layer, and the second is below the position of adjectives (AgrP; for detailed discussion of my structural assumptions, see \sectref{sec:key:4}). Furthermore, supporting as mentioned in \REF{ex:key:51b} has been discussed in the context of the well-known phenomenon of \textit{do}{}-support in English (\citealt{Chomsky1957}, \citealt{Lasnik2000}). In the current discussion, supporting involves overt operators. More precicely, operators like negation (NEG) have a detectible manifestation when spelled out, and that element is supported by \textit{ein} (i.e., \textit{k-ein} ‘no’). As to flagging, \textit{ein} indicates the presence of operators like REL, which is a realization operator in cases like \textit{ein} \textit{Mann} ‘a man’ (de Swart \textit{et al}. 2007). However, here the operator itself has no detectable manifestation and remains invisible.\footnote{In the typological literature, the term flagging has been proposed by \citet{Haspelmath2019} for marking nominals with case markers and/or adpositions. Unlike in the main text, here flags (i.e., case markers, adpositions) involve role-identifiers that appear with \textit{overt} nominals.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Some of the claims in \REF{ex:key:49} to \REF{ex:key:51} are not entirely new; for instance, Hypothesis 1b as instantiated by adjectives in Hypothesis 2a has been established by \citet{Pfaff2017} for Icelandic adjectives. Hypothesis 2b is related to \citet[40]{Olsen1991b} and \citet[122-26]{Rehn2019}, who state that morphological features need to be made visible (cf. also \citealt{Esau1973}, who takes the strong endings as case markers). Hypothesis 3b has, in part, been argued for by de Swart \textit{et al}. (2007). In the course of the following discussion, I discuss this and other related work. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, I followed Chomsky in \sectref{sec:key:1} in assuming that an expletive needs to be licensed by a substantive element (the associate) to avoid a violation of the Principle of Full Interpretation. This licensing can be seen as a semantic licensing condition and was instantiated above by moving the substantive element to the expletive. In contrast, the hypotheses above state that expletives indicate the presence of linguistic elements. This, in turn, can be interpreted as a syntactic licensing condition. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If this is on the right track, then there is a division of labor: syntactically, we may state that the expletive indicates the substantive element; semantically, we may say that the substantive element identifies the expletive:\footnote{These two conditions are reminiscent of the licensing of \textit{pro} proposed by \citet{Rizzi1986}: \textit{pro} is formally licensed by government, and the content of \textit{pro} is recovered through rich agreement specification (for ellipsis involving \textit{pro}, see also \citealt{Lobeck1995}).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             Indicate (syntax)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%52
    \label{ex:key:52}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          EXPL          SUBST
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           Identify (semantics)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Considering \REF{ex:key:52}, there is an interplay between the two elements in that each element licenses the other. I return to this in Chapter 8.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.2. \textit{Relatedness of Adjectival Inflections and} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is reasonable to ask why adjectival inflections and the article \textit{ein} ‘a’ should be discussed together in one volume. Adjectival inflections are bound inflectional morphemes, and the indefinite article is, at least in some cases, a free morpheme. Given their different status, it is expected that they exhibit differences. However, as seen in the previous section, they also show many similarities. To the extent that the hypotheses above prove tenable, both of these elements are expletives, they indicate structure, and they make linguistic elements visible. In addition, both elements involve functional items inside the nominal spine of the DP. What is special about them is that both adjectival inflections and \textit{ein} can each appear in multiple syntactic positions inside the DP, and they interact morphologically.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the absence of evidence to the contrary, I assume that the strong and weak inflections appearing on different elements are the same (e.g., \citealt{MilnerMilner1972}, also Chapter 2). Specifically, strong endings can appear on adjectives \REF{ex:key:53a}. In addition, the same (strong) endings surface on predeterminers (e.g., \textit{alle} ‘all’) and on determiners \REF{ex:key:53b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%53
    \label{ex:key:53}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{gut-e      Studenten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    good-\textsc{st} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘good students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{all-e   dies-e    gut-en      Studenten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all-\textsc{st} these-\textsc{st} good-\textsc{wk} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all these good students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is argued in later parts of the book that adjectives, determiners, and predeterminers are in different syntactic positions (i.e., AgrP, DP, and LPP). Indeed, weak endings can also occur on different elements. Not only do they occur on adjectives as seen in \REF{ex:key:53b} and \REF{ex:key:54a}, but they can also (optionally) occur on certain determiners in the genitive masculine/neuter (for more detailed discussion, see Chapter 3, \sectref{sec:key:4}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%54
    \label{ex:key:54}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{der Verkauf heiß-en Kaffee-s}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the  sale      hot-\textsc{wk}  coffee.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the sale of hot coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{im      Sommer dies-en  Jahr-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    in.the summer this-\textsc{wk} year.\textsc{neut}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘in the summer of this year’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This means that while more restricted, weak endings may also occur on elements in different positions (i.e., AgrP and DP). Given these distributions, I refer to these endings, including the ones on determiners and predeterminers, as adjectival inflections (rather than adjective endings).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Similar to adjectival inflections, \textit{ein} can appear in different syntactic positions. This can be shown with certain (complex) \textit{ein}{}-words. As already briefly mentioned in \sectref{sec:key:3.1.2}, the negative article \textit{kein} ‘no’ consists of \textit{ein} and the negative element NEG \REF{ex:key:55a}. Similarly, I argue in Chapter 5 that the singularity numeral \textit{EIN} ‘one’ is made up of \textit{ein} and the null element \textit{Ø}\textsubscript{[-}\textsc{\textsubscript{pl}}\textsubscript{]} bringing about singularity \REF{ex:key:55b}. Both \textit{kein} and \textit{EIN} can co-occur with a lower instance of \textit{ein} provided \textit{so} ‘such’ intervenes:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%55
    \label{ex:key:55}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{k-ein-e     so’n-e  Frauen}    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsc{neg}{}-a-\textsc{st} so a-\textsc{st} women
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘no such women’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{EIN-E        so’n-e   Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{Ø}\textsubscript{[-}\textsc{\textsubscript{pl}}\textsubscript{]}+a-\textsc{st} so \textsubscript{} a-\textsc{st} woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one such woman’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recalling the string \textit{kein so’n Mann} ‘no such man’ from above, note that all instances of \textit{ein} have the same inflection or lack thereof. I argue in later chapters that \textit{ein} along with its inflection may surface in the head positions of (at least) ArtP, CardP, and DP.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To repeat, adjectival inflections and \textit{ein} can each appear in various syntactic positions: strong inflections occur in AgrP, DP, and LPP; weak inflections surface in AgrP and DP; and \textit{ein} occurs in ArtP, CardP, and DP. This is different from inflections on nouns, which only surface on the nouns themselves, and different from other determiners, which only surface in the DP-layer.\footnote{It could be claimed that the dative plural ending -\textit{n} on nouns (ia) can appear on other elements when the noun itself is elided (ib) (see Corver \& van \citealt{Koppen2010}, 2011b for discussion):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumiiileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a.  \textit{mit   den zwei Männer-n}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumiiileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

with the  two  men-\textsc{dat}‘with the two men’b.  \textit{mit   den zwei-n}with the two-\textsc{infl}‘with those two’However, this is not possible with the genitive masculine/neuter ending -\textit{s} or other plural endings.} It is clear then that adjectival inflections and \textit{ein} share similar syntactic properties.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Besides these similarities, adjectival inflections and \textit{ein} interact directly as regards inflectional morphology. In the typical cases, the determiner has a strong ending, and the following adjective has a weak one \REF{ex:key:56a}. However, as is well known, there are certain instances where \textit{ein} has no ending, and the adjective exhibits the strong ending \REF{ex:key:56b}. Importantly, when the adjective (and noun) is absent, \textit{ein} has the strong ending \REF{ex:key:56c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%56
    \label{ex:key:56}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{d-er    gut-e       Student} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{st} good-\textsc{wk} student.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the good student’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{ein gut-er    Student}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a    good-\textsc{st} student.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a good student’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{ein-er} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one-\textsc{st} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As discussed in detail in Chapter 2, there are three instances where \textit{ein} does not have an inflection in \REF{ex:key:56b} – in the remaining cases, \textit{ein} works like the definite article in \REF{ex:key:56a}. These three instances have received an enormous amount of attention. Specifically, note that the presence of uninflected \textit{ein} has a special impact on the ending of the adjective (the latter can be strong as in \REF{ex:key:56b}) but that \textit{ein} itself can take the ending of the adjective (if \textit{ein} occurs by itself as in \REF{ex:key:56c}). In other words, inflections and \textit{ein} may intersect directly: \textit{ein} may regulate the distribution of the adjectival inflections, and adjectival inflections occur on \textit{ein} if the adjective (and noun) is absent. Crucially, this alternation in (56b-c) is not possible with other determiners.\footnote{Definite articles are different from \textit{ein} – they always have an inflection when they are followed by an adjective and/or a noun (ia). However, they may get an additional ending when they are used pronominally (ib) (again, see Corver \& van \citealt{Koppen2010}, 2011b for discussion, also \citealt{Roehrs2013a}: 398-402):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumvileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a.  \textit{mit   d-en    gut-en  Männer-n}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumvileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

with the-\textsc{st} gut-\textsc{wk} men-\textsc{dat}‘with the good men’b.  \textit{mit   d-en-en}  with the-\textsc{st}{}-\textsc{infl}  ‘with those’This additional ending -\textit{en} is not a regular adjectival inflection. Furthermore, it is restricted to dative plural contexts as in (ib) and to genitive instances more generally (e.g., \textit{d-er-en} ‘the-\textsc{st}{}-\textsc{infl}’ in the feminine and plural). Note also that \textit{manch} ‘some’, \textit{solch} ‘such’, and \textit{welch} ‘which’ are also different from \textit{ein}: besides similar distributions as in (56b-c) (e.g., \textit{solch gut-er Student} ‘such good student’; \textit{solch-er} ‘such a one’), these three elements also allow the inflection to occur on the first element like definite articles (e.g., \textit{solch-er gut-e Student} ‘such good student’). Furthermore, uninflected \textit{ein} is restricted to three instances, but the other three elements are not.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up, discussing adjectival inflections and \textit{ein} in one volume allows us to recognize and appreciate the above-mentioned similarities, the related differences, and the interactions of the two elements. The analysis of adjectival inflections in Chapter 2 will feature prominently in the discussion of the different types of \textit{ein} in Chapter 5. In fact, the analysis of this interaction narrows down the options of plausible analyses of certain other phenomena and raises issues for specific types of accounts of those phenomena. In addition, the following detailed discussion of both of these elements side-by-side goes beyond the canonical cases providing a more comprehensive discussion needed for both of these elements. These points make it advantageous to discuss adjectival inflections and \textit{ein} in tandem.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.  Basic Assumptions
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I briefly lay out my assumptions of the syntactic structure of canonical noun phrases (non-canonical constructions are discussed in later chapters in detail). Then I detail my assumptions about determiners, adjectives, and numerals/quantifiers. Finally, I summarize the main points of Distributed Morphology and Type Theory, approaches to the morphology and semantics that play an important part in the analysis to follow. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.1.  Basic Assumptions about Structure
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I start by discussing noun phrases as a whole. This is followed by specifying my assumptions about determiners, adjectives, and numerals/quantifiers.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.1.1. DPs as a Whole
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This book is written in the general generative tradition (e.g., \citealt{Chomsky1995}, 2000). As mentioned above, I assume the DP-Hypothesis such that nominals in argument position are (at least) DPs but nominals in non-argument position may be of smaller size. Taking a cartographic approach, I assume that DPs like \REF{ex:key:57a} have the structure shown in \REF{ex:key:57b}. Proceeding bottom-up, I assume that nouns project NPs and that morphological number is specified in NumP \citep{Ritter1991}.\footnote{In this work, I take a (traditional) projectionist view. In contrast, neo-constructionist models postulate that certain properties of lexical elements are determined by higher functional ones. For instance, \citet{Marantz1997} proposed that lexical items such as nouns and adjectives are merged as acategorial or category-neutral roots that receive their lexical category during the derivation; that is, after they have merged with a category-defining head (for similar ideas, see \citealt{Borer2005}, 2013). Below, I assume that ellipsis involves a null noun. Since I am not entirely sure how a null noun (or \textit{pro} as in \citealt{Olsen1987} and related work) are compatible with acategorial/category-neutral roots, I do not adopt this type of (neo-constructionist) view here, with the qualification that the mass/count distinction of nouns is determined by the higher Num head (Chapter 7).} I claim here (and argue momentarily) that determiners originate in a lower Article Phrase (ArtP). Following Cinque (1994, 2010), I take adjectives to reside in the specifiers of a recursive AgrP. I follow \citet{Zamparelli2000} in arguing that (weak) quantifiers are housed in Spec,CardP. Finally, I assume that determiners move from ArtP to the DP-level, with definite and indefinite articles moving to D but demonstratives moving to Spec,DP (for more detailed background discussion, see among many others \citealt{Abney1987}; Alexiadou \textit{et al.} 2007; \citealt{Bernstein2001a}; \citealt{Julien2005a}; \citealt{Longobardi2001}; \citealt{Roehrs2020b}; \citealt{Salzmann2020}, 2022):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%57
    \label{ex:key:57}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{die zehn kleinen Autos}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the ten   small      cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the ten small cars’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.   DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       D     CardP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{die}\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      QP      Card’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{zehn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        Card    AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           AP    Agr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{kleinen} %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            Agr     ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                           
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                       Art’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 Art    NumP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                  t\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                      Num’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                    Num      NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                    [+\textsc{pl}]    \textit{Autos}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I assume with \citet{Julien2005b} that nouns undergo partial N-raising to Num (not shown). Note also that with the noun at the bottom of the tree, the higher projections can be interpreted as the extended projection of the noun (\citealt{Grimshaw1991}, van \citealt{Riemsdijk1998b}). We see below that there are other types of extended projection; for instance, QP and AP in \REF{ex:key:57b} are more complex (and are fleshed out further below). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The structure in \REF{ex:key:57b} involves a simple DP – there are no other nominals embedded as specifiers, as adjuncts, or as complements. Furthermore, as mentioned above, case, number, and gender are concord features in the German noun phrase. I assume that gender comes from the noun and that (morphological) number originates in NumP. Case is determined DP-externally. Note that all elements in simple DPs like \REF{ex:key:57b} exhibit concord in agreement features.\footnote{It is not clear what the mechanism is that yields concord in agreement features (see, e.g., \citealt{Sigurðsson1989}: 112-113 for an analysis based on feature percolation/spreading, \citealt{Schoorlemmer2009} for an Agree-based account, \citealt{Norris2014} for a proposal in terms of feature spreading and local feature copying, and \citealt{Carnie2021}: 293 and \citealt{Olsen1991b}: 38 for an idea involving selection; for insightful discussion of agreement in the clause, see \citealt{PesetskyTorrego2007}). Note in this regard that the successive-cyclic movement of the determiner, as discussed below, could do double duty and mitigate concord. I leave this interesting option open here.} There is one exception. I assume that Saxon Genitives are also part of simple DPs despite the fact that they do not agree in features with the rest of the nominal. To be clear then, I take simple DPs to involve canonical constructions. Since the low position of determiners plays an important role in the analysis to be developed, I review some previous work on it.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Cases of Double Definiteness have received much attention in the literature on the Scandinavian languages; for instance, Norwegian \textit{den gamle mannen} ‘the old man’ involves a free-standing determiner (\textit{den}) and a suffixal determiner on the noun (-\textit{en}).\footnote{For cases of double articulation of (in-)definiteness in other languages and other constructions, see \citet{Plank2003} and \citet{Alexiadou2014}.} \citet[428]{Taraldsen1990} was one of the first to propose that the suffixal determiner originates in a D-type position below prenominal adjectives and that the free-standing determiner appears in a second D-type position above adjectives. Julien (2002, 2005a) elaborated on that providing arguments that the low determiner position is tied to the feature [\textsc{specificity}] in certain Scandinavian languages (see also \citealt{Schoorlemmer2012}; cf. \citealt{IhsanePuskás2001}). Roehrs (2002, 2009a) proposed that both of these determiner positions are related by movement accounting for the different interpretations of adjectives as regards (non-)restrictiveness (see also Chapter 4, \sectref{sec:key:4}). In order to explain the Definiteness Cycle in the history of English, \citet{Nykiel2015} employed determiner movement from a low position as well (see also the discussion of Old Norse in van \citealt{Gelderen2007}: 293-94). Borer (2005: chapter 4) derives the mass/count distinction of nouns by assuming the absence or presence of a Classifier Phrase (cf. NumP below) and movement of determiners from this low position to higher positions in the nominal structure (see also \citealt{Rehn2019}: 172-73 and \citealt{HeycockZamparelli2005}).\footnote{Unlike the current account, which merges determiners, singular or plural, in ArtP, \citet[123]{Borer2005} has to assume that given her assumption about plural morphology, singular and plural determiners are merged in different positions (singular determiners in ClP, but plural ones in the higher \#P; cf. CardP below).} \citet{Schoorlemmer2009} utilized the two determiner locations in his analysis of concord of the noun phrase (see Chapter 2, \sectref{sec:key:4.2}). Heck \textit{et al.} (2008) proposed that a definiteness feature moves from N to D when a prenominal adjective is present. Finally, separating the definiteness feature into different components, Roehrs (2015, 2019) provided evidence that the low position of some of these definiteness components explains adjective endings in the Scandinavian languages and certain interactions between demonstratives/possessives and suffixal determiners.\footnote{There are other proposals that assume that determiners originate in lower positions. In her analysis of constructions involving DP-internal degree Quantifier Raising (e.g., \textit{an idiot of a doctor}, \textit{too big (of) a house}), \citet[272-73]{Matushanksy2002} locates the indefinite and definite articles in a head position below the DP-level (presumably Card in the current analysis). Similarly, Wood \& Vikner (2011, 2013) argue that there are two positions in which the indefinite article can occur in doubling cases of the type \textit{a such a hotel} and \textit{a so bad a hotel}: D and Num (which is Card here). Furthermore, in order to account for the referential defectiveness of French articles (\citealt{VergnaudZubizaretta1992}), Déchaine \& \citet[428-31]{Wiltschko2002} propose that the definite article in French is introduced below the DP-level.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Adopting a similar position here, I assume that the determiner moves from ArtP to the DP-level in a successive-cyclic fashion. Leaving out the numeral in \REF{ex:key:57a} above and adding the adjective \textit{rot} ‘red’, the derivation can be updated as follows:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%58
    \label{ex:key:58}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     D    AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   \textit{die}\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               AP     Agr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{kleinen}%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Agr     AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      die\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   AP     Agr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{roten}%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          Agr    ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          die\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Art’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                          Art                 NumP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               die\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                    Num’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                          Num      NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                         t\textsubscript{k}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                      \textit{Autos}\textsubscript{k}              Num\textsubscript{[+}\textsc{\textsubscript{pl}}\textsubscript{]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I assume that definite articles move to D by head adjunction as shown in \REF{ex:key:58} but that (phrasal) demonstrative move to Spec,DP by adjunction to phrasal projections.\footnote{Head movement of the definite article in \REF{ex:key:58} is illustrated in a simplified way. Note that there are well-known problems with assuming that head movement is syntactic and proceeds by head adjunction. Among others, head adjunction constitutes a violation of the Extension Condition \citep{Chomsky1995}. Solutions to these issues have been proposed; for instance, \citet[45-46]{Borer2005} argues for head-pairs where one element of this pair is merged above the other (but crucially not by head adjunction). The higher element of that pair can move up to the next phrasal level merging above the head of this phrase forming another head-pair – there is no violation of the Extension Condition (note that there are other, related analyses: \citealt{Matushansky2006}: 91-93 proposes that head movement consists of displacement of a lower head to the specifier of a higher phrase followed by m-merger of this head with the head of the higher phrase; “excorporation” is possible if m-merger does not take place; \citealt{Giusti2015}: 84, 116-26 argues that articles are realizations of remerging N-heads).  In what follows, I simply assume that heads raise by adjunction to the next higher head. The head noun raises to adjoin to Num, and both elements are spelled out as the appropriate singular or plural form of the noun. As for determiners, I assume that they also raise by adjunction to the next head. However, I assume that after adjunction, they excorporate and move to the next higher head (keeping in mind that there are other solutions, as just mentioned). Note that unlike the movement of the noun, the movement of determiners is not structure-building – it involves excorporation.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Unlike case, number, and gender, definiteness is not a concord feature. Rather, I assume that definite determiners like \textit{der} ‘the’ have the feature [+DEF] as part of their structure (see \sectref{sec:key:4.1.2}) and move to the DP-level to specify the definitenss feature on D such that [uDEF] becomes [+DEF]. The article \textit{ein} ‘a’ is different. Proposing that it is a semantically vacuous element, it does not have a feature for definiteness. This means that when \textit{ein} is present, D has no definiteness feature.\footnote{This implies that there are two types of D in argument DPs, one with a feature for definiteness and one without it. Alternatively, we could postulate just one type of D and assume that its definiteness feature can but does not have to be checked/valued. The question of whether there is one or two types of D extends to indefinite nominals more generally. Predicate DPs are indefinite by form (e.g., \textit{Er ist ein netter Mann.} ‘He is a nice man.’), but not by meaning (they denote properties). This is different for indefinite argument DPs, which may assert the existence of an entity (e.g., \textit{Ein netter Mann war da}. ‘A nice man was there.’). Considering that \textit{ein} occurs in both types of nominals, it is clear that \textit{ein} itself does not bring about semantic indefiniteness, as claimed in the main text. Note in this regard that the current assumptions are in line with \citegen{Pfaff2017} analysis of the strong/weak alternation of adjectives in Icelandic, where definite DPs have a definiteness feature, but indefinite DPs, predicative and argumental, do not. Given that the question of the different types of D is a more general issue, I leave it for future research and continue assuming that \textit{ein} has no definiteness feature.} Given the lack of a definiteness feature on \textit{ein}, this element can surface in lower positions (cf. also \citealt{Borer2005}: 144-59, \citealt{Rehn2019}). Among others, this allows me to postulate structures smaller than DP, for instance, when nominals involving \textit{ein} occur in predicative contexts without a modifier (e.g., \textit{Er ist ein Mann}. ‘He is a man.’; see Chapter 6).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  There is overt distributional evidence that determiners originate in low positions in German (for detailed cross-linguistic discussion, see \citealt{Roehrs2009a} and references cited therein). \citet[255]{Sternefeld2008} reports work by Horst Simon, who observes that the negative article \textit{kein} ‘no’ can be repeated in dialectal German \REF{ex:key:59a}.\footnote{This dialect is not further specified by Sternefeld. An example where the negative article appears to be in a low position similar to \REF{ex:key:59a} is also provided by Haegeman \& \citet[187]{Lohndal2010} from West Flemish.} Sternefeld concludes that another position is needed for the second (low) instance. Above, I identified this position as Art. Furthermore, I noted above that colloquial German also tolerates two indefinite articles (59b-c):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%59
    \label{ex:key:59}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{Ich hab   k-ein  blau-es \textsubscript{} }\textit{k-ein   Kleid         nicht}.  (dialectal German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     have \textsc{neg}{}-a blue\textsc{{}-st} \textsc{neg}{}-a dress.\textsc{neut} not
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I don’t have a blue dress.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{k-ein   so’n Kleid}    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsc{neg}{}-a so a  dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘no such dress’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{EIN       so’n Kleid}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{Ø}\textsubscript{[-}\textsc{\textsubscript{pl}}\textsubscript{]}+a so a dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one such dress’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Observe that unlike the recurring indefinite articles in Norwegian (\sectref{sec:key:2.1.3}), in German \textit{ein} can only be repeated if both instances of \textit{ein} are part of another element, for instance, the negative article \textit{kein} ‘(not a =) no’ and the type particle \textit{so’n} ‘such a/of that kind’ (see also Chapter 8). Given the works cited above and the empirical evidence just provided, I take it as established that determiners originate low in the structure.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With the structure of canonical DPs in place, I should point out that the current proposal is based on morpho-syntactic subanalyses where words are separated into stems and inflections, each involving a position in the syntactic representation as explicated in greater detail in the next subsections:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%60
    \label{ex:key:60}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{gut-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    good-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘good’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{d-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that these subanalyses are part of a more general approach to morpho-syntax involving the segmentation of word and morpheme forms into smaller units (e.g., \citealt{Anderson1992}; \citealt{DéchaineWiltschko2002}: 422; \citealt{Fischer2006}; Gunkel \textit{et al}. 2017: 1297-98; \citealt{Leu2015}; G. \citealt{Müller2002a}: 125; \citealt{Olsen1991b}: 38; \citealt{Pike1963}, 1965; \citealt{Rehn2019}; \citealt{Roehrs2013a}; \citealt{Stump2001}; B. \citealt{Wiese1999}; \citealt{Wiltschko1998}, \citealt{Wunderlich1997}; but see also \citealt{JandaJoseph1992}). Furthermore, similar to Distributed Morphology, which is utilized here (see \sectref{sec:key:4.2.1}), the recent approach of nanosyntax also involves analyses at the submorphemic level (see \citealt{Hachem2015} and \citealt{Starke2009}). However, the latter approach explicitly rejects Impoverishment rules (\citealt{ArregiNevins2012}: 341), a type of operation essential to the account to be developed below. I return to some of these works in later parts of the book. With this in mind, I discuss determiners and related elements in the next subsection, and adjectives and similar items in the subsection after that.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.1.2. Determiners, Articles, and Determiner-like Elements
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In German, determiners are categorized into three groups: \textit{der}{}-words, \textit{ein}{}-words, and null articles (e.g., \citealt{RankinWells2016}).\footnote{For complex determiners like \textit{ein jeder} ‘(an every =) each’, see, e.g., \citealt{KarnowskiPafel2004}: 172-76, \citealt{Pafel1994}, and below.} The first set includes \textit{der} ‘the’, (stressed) \textit{DER} ‘that’, \textit{dieser} ‘this’, \textit{jener} ‘that’, \textit{jeder} ‘every’, \textit{mancher} ‘some’, \textit{solcher} ‘such’, \textit{welcher} ‘which’, and \textit{alle} ‘all’ in the plural (and occasionally with mass and abstract nouns; for more details on the use of these elements, see Zifonun \textit{et al.} 1997: 1930-51). Second, as already stated above, \textit{ein}{}-words are comprised of the indefinite article \textit{ein} ‘a’ including its reduced form \textit{’n}, the (stressed) singularity numeral \textit{EIN} ‘one’, the negative article \textit{kein} ‘no’, and possessive articles like \textit{mein} ‘my’, \textit{dein} ‘you’, etc. The third category involves null articles that occur with mass and plural nouns in indefinite contexts. They are indicated here by \textit{Ø\textsubscript{D}}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Articles are a specific subgroup of determiners. They comprise the definite article \textit{der} ‘the’, all \textit{ein}{}-words, and the null articles (I argue in detail in Chapter 5 that complex \textit{ein}{}-words are composites consisting of the vacuous article \textit{ein} and another, semantic component). Finally, possessives (including the possessive components of the possessive articles) and predeterminers are labeled determiner-like elements here. I assume that all determiners originate in ArtP. This is different for determiner-like elements as explained further below.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I argue in Chapter 2 that articles are heads consisting of a categorial feature [+D]. The indefinite null articles are illustrated in \REF{ex:key:61a}, which includes a definiteness feature with a negative value. Unlike the null articles, the overt articles in (61b-c) have a separate feature bundle for case, number, and gender ([CNG]), later to be spelled out as the inflection.\footnote{I assume that agreement features are part of syntax (see also \citealt{Murphy2018}; for the advantages and disadvantages of such an assumption in the framework of Distributed Morphology, see \citealt{EmbickNoyer2007}: 305-10).} Above, I proposed that \textit{ein} is a semantically vacuous element. As such, it does not have a feature for definiteness \REF{ex:key:61b}. By contrast, the definite article \textit{der} ‘the’ has a feature for definiteness. However, unlike the null indefinite articles, the value of the definiteness feature is positive \REF{ex:key:61c}:\footnote{I finalize the discussion of Vocabulary Insertion of the determiners in Chapter 8, Sections 2.2.5 and 2.2.6. Also, note that the existence of null articles is a fairly standard assumption (e.g., \citealt{Carnie2021}: 271, \citealt{Schoorlemmer2009}). Among others, it allows us to claim that all argument DPs have determiners. In the context of the current analysis, their existence also helps explain the distribution of the article \textit{ein} vs. adjectival \textit{eine} (Chapter 5, \sectref{sec:key:5.2.3}). Furthermore, assuming that the null articles have the feature [-DEF] has a number of advantages: (i) it makes \textit{Ø\textsubscript{D}} parallel to \textit{der} ‘the’ in terms of (in-)definiteness, (ii) it provides a straightforward solution to the distribution of articles with certain operators (Chapter 8, Sections 2.2.5 and 2.2.6), and (iii) it makes \textit{ein} the least specified determiner (stem).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%61
    \label{ex:key:61}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Indefinite Articles} Ø\textsubscript{D}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               Art
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              [+D; -DEF]    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Indefinite Article} ein  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Art
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [+D]    [CNG]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Definite Article} der
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Art
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      [+D; +DEF]    [CNG]  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Abstracting away from the adjectival inflection, note already here that \textit{ein} is the least specified article in German – it only has the categorial feature [+D].
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Demonstrative such as \textit{dieser} ‘this’ are phrasal in structure (e.g., \citealt{Bernstein1997}, 2001b; \citealt{Brugè1996}, 2002; \citealt{Giusti1997}, 2002; \citealt{RauthSpeyer2021}). For simplicity’s sake, I assume that there are two terminal heads \REF{ex:key:62}. Dem involves the features [+D; +DEF, +DEIX] and builds its own extended projection with an Inflectional Phrase (InflP) at the top (see \citealt{Leu2007}, 2015; \citealt{Roehrs2010}, 2013a).\footnote{The head Dem involving the features [+D; +DEF, +DEIX] could be analyzed as two heads, Art with the features [+D; +DEF] and a deictic head Deic with the feature [+DEIX] (for discussion, see the above-mentioned works). Since these finer distinctions do not play a role here, I continue utilizing the simpler analysis in the main text.} The features for case, number, and gender are in Infl. Dem moves to adjoin to Infl (not shown):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%62
    \label{ex:key:62}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Demonstrative} dieser
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 InflP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         [\textsc{cng}]    DemP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            {\textbar}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        Dem
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         [+D; +DEF, +DEIX]  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As will become clear in Chapter 3, \sectref{sec:key:4}, phrasal determiners have some special properties; for instance, they can optionally have a weak ending in genitive masculine/neuter contexts. Besides \textit{dieser} ‘this’, this includes \textit{jener} ‘that’, \textit{jeder} ‘every’, \textit{mancher} ‘some’, \textit{solcher} ‘such’, \textit{welcher} ‘which’, and \textit{alle} ‘all’. I assume that all these elements have a structure similar to \REF{ex:key:62}.\footnote{The demonstrative \textit{DER} ‘that’ seems to have an intermediate status. On the one hand, this element is expected to project a phrasal structure (similar to the demonstrative \textit{dieser} ‘this’); on the other hand, it behaves like the definite article \textit{der} ‘the’ in not exhibiting the optionality of strong or weak inflection in the genitive masculine/neuter.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Possessives will not feature prominently in the following discussion. They involve complex structures that deserve more discussion than I can provide here (but see \citealt{Roehrs2013b}, 2020a). However, they need to be briefly addressed as they are mentioned in various contexts below. Focusing on prenominal possessives, there are different types: possessive articles involving the possessive component \textit{s}{}- and \textit{ein} \REF{ex:key:63a}, Saxon Genitives consisting of a possessor and the possessive marker -\textit{s} \REF{ex:key:63b}, and \textit{von}{}-possessives comprising a possessor and the possessive preposition \textit{von} ‘of’ \REF{ex:key:63c} (see also \citealt{Bhatt1990}: 223-24, \citealt{Fortmann1996}, \citealt{Haider1992}: 315): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%63
    \label{ex:key:63}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{sein Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    his  car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘his car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Peters  Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter’s car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Peter’s car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{von Peter das Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    of   Peter  the car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the car of Peter’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Following Roehrs (2013b, 2020a), I assume that possessives involve multi-component constituents consisting of a possessive functor and a possessor \REF{ex:key:64}. Possessors may involve overt or null elements (more on \textit{pro} below). The possessive functor forms the head of a Possessive Phrase (PossP) taking the possessor as its complement. A functional phrase (XP) is at the top. This yields the following underlying structure of the possessives in \REF{ex:key:63}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%64
    \label{ex:key:64}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

             XP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        X’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 X             PossP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            Poss’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        Poss      DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 \textit{s-}    \textit{pro}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        {}-s    Peter
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        von    Peter
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that similar to demonstratives, possessives involve extended projections. I assume that all Poss heads, that is, the possessive component \textit{s}{}-, the possessive marker -\textit{s}, and the possessive preposition \textit{von} ‘of’, move to X in \REF{ex:key:64}. If \textit{s}{}- or -\textit{s} move, this brings about a nominal extension of Poss; if \textit{von} moves, it results in a prepositional extension of Poss. Furthermore, the possessor DP undergoes movement to Spec,XP in the first two cases but stays in situ with \textit{von}{}-possessives (for more details, see \citealt{Roehrs2020a}: 123). This yields the correct surface strings for Saxon Genitives (\textit{Peters}) and \textit{von}{}-possessives (\textit{von Peter}). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As to the possessive articles, consider briefly the periphrastic possessive construction \textit{Peter sein Auto} ‘(Peter his =) Peter’s car’ \citep{Fiva1985}, where a possessor is added to the left. This distribution is sometimes referred to as Possessor Doubling. If we assume with \citet[315]{Haider1992}, Grohmann \& \citet[53]{Haegeman2003}, and Alexiadou \textit{et al}. (2007: 611) that \textit{Peter} can be replaced by a null argument (\textit{pro}), then the periphrastic possessive construction in \REF{ex:key:65a} can be directly related to the simple possessive article in \REF{ex:key:65b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%65
    \label{ex:key:65}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [\textsubscript{XP} \textit{Peter s}{}-]\textit{ein Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [\textsubscript{XP} \textit{pro s}{}-]\textit{ein Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The possessive structure XP in \REF{ex:key:64} can surface in different positions inside the larger DP. I assume that XP is base-generated in Spec,NP, where it may receive a theta role (depending on the type of head noun). Recall that the head noun moves to Num. If XP stays in situ, postnominal possessives are derived; if XP moves higher, prenominal possessives come about. Focusing on the latter again and abstracting away from the movement of the article, the possessive component \textit{s}{}- and its possessor (\textit{Peter} or \textit{pro}) move to Spec,DP \REF{ex:key:66a}. The Saxon Genitive consisting of the possessive marker -\textit{s} and its possessor also moves to Spec,DP \REF{ex:key:66b}.\footnote{Note again that possessive -\textit{s} is part of the specifier (and not in D). This is Abney’s (1987: 81-85) “preferred” analysis (see also \citealt{Borer2005}: 40-41; \citealt{Lowe2016}: 172, 174, and others).} Unlike those two cases, the \textit{von}{}-possessive moves to a higher specifier. \citet{GiustiIovino2016} propose that there is a Left Periphery Phrase (LPP) above the DP-layer. I assume that the \textit{von}{}-possessive moves to the specifier of that phrase.\footnote{Rather than LPP, we could postulate K(ase)P (see \citealt{Pfaff2017}: 294 for discussion and references). However, I prefer the functionally neutral name Left Periphery Phrase here.} This derives the data in (63a-c) above as follows:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%66
    \label{ex:key:66}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [\textsubscript{DP} [\textsubscript{XP} \textit{Peter}/\textit{pro} \textit{s}{}-]\textsubscript{k} \textit{ein} [\textsubscript{NumP} \textit{Auto}\textsubscript{i} [\textsubscript{NP} t\textsubscript{k} t\textsubscript{i} ]]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [\textsubscript{DP} [\textsubscript{XP} \textit{Peter}s]\textsubscript{k} \textit{Ø\textsubscript{D}} [\textsubscript{NumP} \textit{Auto}\textsubscript{i} [\textsubscript{NP} t\textsubscript{k} t\textsubscript{i} ]]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  [\textsubscript{LPP} [\textsubscript{XP} \textit{von} \textit{Peter}]\textsubscript{k} LPP [\textsubscript{DP} \textit{das} [\textsubscript{NumP} \textit{Auto}\textsubscript{i} [\textsubscript{NP} t\textsubscript{k} t\textsubscript{i} ]]]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Comparing \REF{ex:key:66a} to \REF{ex:key:66b}, I follow \citet{Krause1999} and Roehrs (2013b, 2020a) in that the periphrastic possessives (including the possessive articles involving \textit{pro}) and the Saxon Genitives have essentially the same structure. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

While I cannot argue for these structures and derivations in more detail here (see references mentioned above), one immediate advantage is that all prenominal possessives in German have the same basic internal structure (XP) and that they all have the same basic derivation undergoing movement from a low position to a high position within the larger noun phrase. Indeed, each noun phrase involves a determiner in D: \textit{ein} in \REF{ex:key:66a}, \textit{Ø\textsubscript{D}} in \REF{ex:key:66b}, and the definite article in \REF{ex:key:66c}.\footnote{At this point, an issue arises. As is well known, possessors in prenominal position may spread their (in-)definiteness to the larger noun phrase (e.g., \citealt{Alexiadou2005}, \citealt{Roehrs2022}). If this definiteness spread is instantiated by Spec-head agreement between Spec,DP and D, then definite Saxon Genitives and null articles specified as [-DEF] are incompatible (cf. \REF{ex:key:66b}). In Footnote Error: Reference source not found, I pointed out some advantages of assuming that null articles exist and that they have a negative feature for definiteness. Given these points, I proceed with the assumption of a negative feature for definiteness, but I offer two solutions to this issue in Chapter 8, \sectref{sec:key:2.2.6} (once further discussion is in place).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, predeterminers are intensifiers like \textit{alle} ‘all’, \textit{diese} ‘these’, and \textit{ein} ‘a’ emphasizing exhaustiveness, deixis, and distributivity, respectively: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%67
    \label{ex:key:67}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{alle meine Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all   my     friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all my friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{diese meine Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    these my     friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘these friends of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{ein jeder meiner Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    an  every of.my  friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘each of my friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that predeterminers form a subset of determiners. In other words, all predeterminers also do double duty as determiners (but not vice versa). I propose below that all these elements have the categorial feature [+D]. As determiners, they are base-generated in ArtP and move to the DP-level. However, if ArtP already contains a determiner, a second determiner element can only be merged as a predeterminer provided it can supply an intensifying meaning compatible with the rest of the nominal. Given that there is only one determiner in the DP-layer, I assume that predeterminers are located in LPP illustrating this with \textit{alle} here:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%68
    \label{ex:key:68}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          [\textsubscript{LPP} \textit{alle} [\textsubscript{DP} \textit{meine Freunde}]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As such, I assume that LPP can be occupied by prenominal \textit{von}{}-possessives and predeterminers. To distinguish predeterminers and possessives from determiners, I often refer to the former set of elements as determiner-like elements.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.1.3. Adjectives, Numerals, and Quantifiers
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Elements in specifiers project their own phrases. Above, I illustrated this with demonstratives and possessives. Each of these elements project their own complex structure. In this subsection, I turn to adjectives and numerals/quantifiers. We see that AP and QP in \REF{ex:key:57b} above are also more complex.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Starting with adjectives, I assume that they also involve extended projections (e.g., \citealt{Corver1991}, 1997; Neeleman \textit{et al}. 2004; \citealt{Zamparelli2000}: chapter 7). Minimally, the adjective projects an AP, and InflP is at the top (e.g., \citealt{Corver2006}: 68, \citealt{Leu2015}, \citealt{SappRoehrs2016}). The adjective stem is located in A and the inflection in Infl. Both combine by movement of the adjective to Spec,InflP (not shown, but see Chapter 2, \sectref{sec:key:2.4}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%69
    \label{ex:key:69}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

             InflP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       Infl’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             [CNG]      AP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               A’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          A    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 \textit{klein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, numerals (e.g., \textit{zwei} ‘two’) and quantifiers (e.g., \textit{viele} ‘many’) are assumed to have the same representation as \REF{ex:key:69}, where Q is at the bottom projecting more structure. The internal makeup of the singularity numeral \textit{EIN} ‘one’ is discussed in detail in Chapter 5.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These are the basic structural assumptions of the noun phrase that I take to be fairly uncontroversial. Against this backdrop, a number of other, non-canonical constructions involving adjectival inflections and/or \textit{ein} are discussed in later sections. It will become clear that not all nominal constructions can have the same structure. More importantly, as already mentioned above, I argue that it is these types of different constructions that reveal the true nature of adjectival inflections and \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.2.  Distributed Morphology and Type Theory
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Besides the general syntactic assumptions in the previous section, I make use of Distributed Morphology and semantic Type Theory to account for adjectival inflections and \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.2.1. Distributed Morphology
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I usually provide syntactic representations of the form in \REF{ex:key:57b} and \REF{ex:key:58}, where vocabulary items are directly merged in the syntactic tree, that is, during the syntactic derivation. This is a convenient shortcut. While not relevant in many cases, I assume that such syntactic representations are actually built on terminal nodes containing morpho-syntactic features (rather than lexical elements). This is a basic tenet of Distributed Morphology (DM) (see, e.g., \citealt{ArregiNevins2012}: 3-11; \citealt{EmbickNoyer2007}; \citealt{Halle1997}; \citealt{HalleMarantz1993}, 1994; \citealt{HarleyNoyer1999}; \citealt{Sauerland1996}), a realizational approach to morphology where the derivation of complex words is separate from the spelling out (or realization) of those words. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In DM, terminal nodes of a syntactic tree involve abstract features. These features can be manipulated in certain ways; for instance, features can be rearranged (Lowering) or deleted (Impoverishment). After Linearization, the resultant features are spelled out by overt vocabulary items. In other words, the insertion of these items occurs late in the derivation (after syntax) and depends on the specific morpho-syntactic features present on the terminal nodes. As just stated, these features may have undergone some changes.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Vocabulary Insertion is regulated by the Subset Principle. Provided that all features of the vocabulary item match those on the terminal node, the vocabulary item with the most specifications, that is, the most matching features, is inserted over less specified items. When a more specific vocabulary item cannot be inserted (i.e., at least one of its features does not match those on the terminal head), the vocabulary item with fewer or no features is inserted as the elsewhere case. In other words, it is assumed in DM that vocabulary items may be underspecified as regards their morpho-synactic features. This allows competition between related items in certain contexts. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

After insertion, vocabulary items can undergo Local Dislocation combining with other vocabulary items. Unlike Lowering (which operates on hierarchical structures), Local Dislocation involves linear adjacency. Note that while not all these operations are equally important in this book, they are assumed to occur in the general order below (see also \citealt{Murphy2018}: 359). The operations whose orderings are not relevant here are enclosed in curly brackets:\footnote{(Late) Vocabulary Insertion usually concerns functional elements but not lexical ones. The latter are merged as terminal nodes and involve phonological features (\citealt{EmbickNoyer2007}: 295).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%70
    \label{ex:key:70}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \{Lowering, Impoverishment\} >> Linearization >> Vocabulary Insertion >> Local   Dislocation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is clear that Impoverishment precedes Vocabulary Insertion, and we observe in Chapter 2 that this operation seems to have access to structural information. As such, Impoverishment has been ordered with Lowering. This is compatible with Arregi \& \citet[342]{Nevins2012}, who order Impoverishment before Linearization and the latter before Vocabulary Insertion. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Impoverishment plays an important part in the analysis to be developed. To illustrate briefly, inflections are taken to be the overt spell-out of abstract feature bundles on terminal nodes in syntax.\footnote{I assume that terminal nodes may contain feature bundles (rather than just individual features).}  These feature bundles can be manipulated in certain ways. \citet{Sauerland1996} proposes for Norwegian (see Chapter 2 for German) that the weak inflections on adjectives are the result of the deletion of a feature in contexts where strong inflections occur otherwise. To see this, consider the inflectional paradigms of the strong and weak endings in Tables 2 and 3:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Strong Adjective Inflections in Norwegian}
\label{tab:key:2}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXX}
 & {}-neuter & +neuter\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{}-plural & {}-Ø & {}-t\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

+plural & {}-e & {}-e\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Weak Adjective Inflections in Norwegian}
\label{tab:key:3}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXX}
 & {}-neuter & +neuter\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{}-plural & {}-e & {}-e\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

+plural & {}-e & {}-e\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Sauerland establishes the generalization that the weak inflections (-\textit{e}) form a proper subset of the strong inflections (-\textit{e}, -\textit{Ø}, -\textit{t}). He observes that the weak endings occur in the same featural contexts as their homophonous strong endings (i.e., in [+plural] contexts in Tables 2 and 3) and, additionally, in other environments (i.e., [-plural] in \tabref{tab:key:3}). Sauerland proposes that the weak inflections are the least marked endings from the set of strong endings. In other words, there is no difference between strong and weak inflections other than the degree of their specificity. He argues that the weak endings appear when the feature bundles of the strong inflections undergo the deletion of a feature. This feature deletion is instantiated by Impoverishment.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Sauerland formulates the following vocabulary insertion rules:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%71
    \label{ex:key:71}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [-plural, +neuter]  →   {}-\textit{t}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [-plural, -neuter]  →   {}-\textit{Ø}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  []      →   {}-\textit{e}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

He proposes for Norwegian that Impoverishment deletes the gender feature in the syntactic representation. If the feature [neuter] is deleted from the relevant terminal node, then \REF{ex:key:71a} and \REF{ex:key:71b} can no longer be inserted in that terminal node. As a consequence, only \REF{ex:key:71c} can be inserted resulting in weak inflections on adjectives. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Note that the exact conditions as to when Impoverishment is triggered in Norwegian are not provided (but Sauerland states that those conditions are morpho-syntactic, at least in German). For some discussion of the contexts and conditions where Norwegian weak endings appear, see \citet{Julien2005a}; \citet{KatzirSiloni2014}; \citet{RoehrsJulien2014}; \citealt{Schoorlemmer2009}, 2012, and many others (for the discussion of related Icelandic, see \citealt{Pfaff2017}). The morpho-syntactic conditions postulated by Sauerland for German are reviewed and discussed in more detail in Chapter 2. With this brief illustration of DM in place, I return to the syntax for a moment. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I assume with \citet{Nunes2001} that Move, more recently termed Internal Merge, is not a primitive operation but rather the output of Copy, Merge, Form Chain, and Copy Reduction. I assume that the first three operations apply to feature bundles which are later spelled out by the corresponding overt vocabulary items. We see later that the fourth operation, Copy Reduction, is sensitive to the overt form of vocabulary items (free vs. bound). As such, I assume that this operation applies after Vocabulary Insertion. This is compatible with \citet{Schoorlemmer2012}, who explains cases of Double Definiteness in Swedish DPs by ordering Copy Reduction after Local Dislocation.\footnote{\citet{Nunes2001} argues that Copy Reduction takes place before Linearization. For discussion in favor of the ordering Copy Reduction after Linearization, see \citet[135-37]{Schoorlemmer2012}.} For convenience and expository simplicity, especially when the underlying details of the derivation are not relevant, I indicate movement by indexed traces and provide the more familiar syntactic representations given in \sectref{sec:key:4.1.1}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.2.2. Type Theory
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This book is not about the semantics of the DP \textit{per se} (or the semantics of the TP, for that matter). Nevertheless, I make some statements about the semantics involved. Rather than providing detailed denotations (see \citealt{Roehrs2009a}), I use Type Theory to show that the relevant elements are semantically compatible with one another. In other words, the goal of employing Type Theory is to find plausible structures by narrowing down the analytical options. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To illustrate the main assumptions of Type Theory, consider the following three sentences:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%72
    \label{ex:key:72}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Peter ist blond}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter is  blond
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Peter is blond.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Er ist blond}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is  blond
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘He is blond.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Der Mann ist blond}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     the  man    is  blond
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘The man is blond.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

What all these sentences have in common is that somebody is blond. In fact, assuming that \textit{er} ‘he’ and \textit{der Mann} ‘the man’ are the same person as \textit{Peter}, all these sentences are true if a certain man called Peter has the property of being blond.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I assume that the copular verb \textit{sein} ‘to be’ is semantically vacuous (\citealt{CoppockBeaver2015}: 399, \citealt{HeimKratzer1998}: 61-62). A sentence has a truth value of 1 if its proposition is true in a given context or 0 if it is false. Illustrating with \REF{ex:key:72a}, if a certain Peter is indeed blond in the given context, then this statement has a truth value of 1; that is, it is true. Truth values are of type <t> \REF{ex:key:73a}. It is usually assumed that individuals like \textit{Peter} are entities. They are of type <e> \REF{ex:key:73b}. Like \textit{Peter}, \textit{er} ‘he’ and \textit{der Mann} ‘the man’ are definite expressions and can replace \textit{Peter}. Thus, I assume that they are also of type <e>. Turning to (intersective) adjectives like \textit{blond} ‘blond’, they involve predicates. Put simply, predication involves membership of an element in a certain set of ordinary/individual entities. Specifically, the adjective \textit{blond} denotes a set of entities that are all blond. Predicates are of type <e,t> \REF{ex:key:73c} – they are functors combining with entities and return truth values. Finally, definite determiners like \textit{der} ‘the’ are functors as well presupposing uniqueness. They combine with predicates yielding entities \REF{ex:key:73d}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%73
    \label{ex:key:73}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  truth values (type <t>):   \textit{Peter ist} \textit{blond}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  entities (type <e>):     \textit{Peter}, \textit{er}, \textit{der Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  predicates (type <e,t>):   \textit{blond}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  determiners (type <<e,t>e>):   \textit{der}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Before commenting on \textit{der Mann} ‘the man’ in \REF{ex:key:73b}, note that Type Theory involves a combinatorial system whereby elements are related to one another, two at a time. There are two main operations. First, Functional Application combines a functor and an argument by plugging the latter into the former \REF{ex:key:74a}. For instance, the predicate \textit{blond} can be a functor, and it takes an entity, say \textit{Peter}, as its argument yielding a truth value, schematically: X\textsubscript{<e,t>}(Y\textsubscript{<e>}) = Z\textsubscript{<t>}. This yields the sentence in \REF{ex:key:72a}. Second, Predicate Modification combines two predicates; that is, it forms an intersection of two sets of certain entities \REF{ex:key:74b}, schematically: X\textsubscript{<e,t>}, Y\textsubscript{<e,t>} = X\textsubscript{<e,t>} \& Y\textsubscript{<e,t>}. For example, predicates like \textit{unbekannt}  ‘unknown’ and \textit{blond} ‘blond’ can be combined as in \textit{der unbekannte blonde Mann} ‘the unknown blond man’ such that a certain man is a member of the set of unknown entities and a member of the set of blond entities; that is, he has the properties of being both unknown and blond:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%74
    \label{ex:key:74}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  Functional Application:  functor(argument)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  Predicate Modification:   predicate \& predicate
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Returning to \textit{der Mann} in \REF{ex:key:73b}, I assumed above that this string is of type <e> and that the determiner \textit{der} itself is of type <<e,t>e>. Given Functional Application, the noun \textit{Mann} is often taken to be a predicate, just like the adjective \textit{blond}. Note that here the predicate \textit{Mann} itself is the argument, and the determiner is the functor. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To illustrate the workings of Type Theory in a syntactic representation, consider the more complex nominal in \REF{ex:key:75a}, derived as in \REF{ex:key:75b}. Proceeding bottom-up and leaving out NumP and ArtP, \textit{Mann} passes up its semantic type to NP. The predicate NP and the intersective adjective \textit{blond} are both of the same type (<e,t>). They combine by Predicate Modification resulting in a complex element of the same type. Finally, the determiner \textit{der} is a functor that takes the complex predicate in AgrP as its argument returning an entity (<e>):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%75
    \label{ex:key:75}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{der blonde Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the blond   man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the blond man’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.   DP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            (Functional Application)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{der}\textsubscript{<<e,t>e>}         AgrP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 (Predicate Modification)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             \textit{blonde}\textsubscript{<e,t>}    NP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{Mann}\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These are the basic assumptions (as discussed in \citealt{HeimKratzer1998}), and they are sufficient to account for most cases. In Chapter 6 and 7, I refine my assumptions about nouns and extend the discussion to pronominal determiners like \textit{du} ‘you’.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up, this section gave an overview of my basic assumptions as regards the syntax, morphology, and semantics. More details are provided as they become relevant.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.  Overview of the Chapters
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This book pursues two goals. First, it seeks to provide an overview of certain synchronic data in the nominal domain in German. In particular, Chapters 2-7 discuss in detail adjectival inflections, \textit{ein} along with its related words, and consequences of the proposed analyses. The second goal is to find commonalities between these different phenomena; that is, to tie these analyses together to offer some remarks about the larger issues involved. While touched on throughout the book, Chapter 8, the final chapter, focuses on these more general points. On the basis of the similarities and differences of adjectival inflections and \textit{ein}, I engage there in a summary discussion of the main hypotheses stated in \REF{ex:key:49} through \REF{ex:key:51}, point out further consequences, and indicate avenues for future research. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is important to point out that both of these goals go in partially different directions. Specifically, an overview aims to be fairly exhaustive, but discussing the larger issues involved is an attempt to see what different sub-domains, empirical or theoretic, have in common. As such, discussing issues to a comprehensive degree tends to move the focus away from traits shared by all the different domains. However, as already briefly illustrated above, the discussion of one phenomenon often forms the background for the analysis of another. It is on the basis of the shared properties and the interwoven argumentation that I pointed out above that adjectival inflections and \textit{ein} can and should be discussed in tandem. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The book is organized into two primary chapters (2, 5), four supporting chapters (3, 4, 6, 7), and a conclusion \REF{ex:key:8}. Here is a brief overview:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textbf{Chapter} \textbf{2} (\textit{The Structural Nature of Adjectival Inflections}) This chapter investigates adjectival inflections in a wide range of nominal constructions. It is observed that weak inflections only occur in canonical constructions. These involve simple DPs of the form “determiner + adjective(s) + noun”, where all these elements involve concord in agreement features. It is proposed that weak inflections are underlyingly fully specified feature bundles that undergo Impoverishment \citep{Sauerland1996}. This feature deletion is triggered by determiners (Impoverishment Rule 1) or by a certain featural context (Impoverishment Rule 2). In contrast, feature bundles surface as strong endings if Impoverishment does not occur, either in canonical or non-canonical structures. As such, the strong inflections present the elsewhere case. The final section discusses three previous proposals in detail pointing out that none of these accounts can account for all the data.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Chapter} \textbf{3} (\textit{Variation and} \textit{Secondary Mechanisms}) This chapter focuses on variation. Given the analysis in Chapter 2, a number of unexpected inflections are discussed. It is pointed out that these cases are very restricted in that they occur only in specific, well-defined contexts. It is argued that unexpected strong adjectives follow from the assumption that pronominal determiners do not trigger Impoverishment. In contrast, unexpected weak adjectives are accounted for by a phonetic rule, which is a reflex of markedness reduction. Unexpected weak inflections on determiners follow from the extension of Impoverishment Rule 2. Overall, it is proposed that adjectival inflections are due to several mechanisms. Finally, inflectional variation on certain sets of adjectives and predeterminers is addressed, issues with the traditional generalizations Weak After Strong and Principle of Monoinflection are discussed, and the strong/weak alternation of the dialect of Mannheim is analyzed.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textbf{Chapter} \textbf{4} (\textit{Consequences for Other Analyses}) This chapter discusses some other consequences of the first part of the book. Specifically, it is shown that weak adjectives in the context of plural \textit{ein} raise issues for analyses involving Predicate Inversion (Bennis \textit{et al}. 1998) and for accounts postulating the presence of certain null nouns (van \citealt{Riemsdijk2005}). Furthermore, it is argued that the present analysis is not compatible with split topicalization constructions if simply analyzed as movement (van \citealt{Riemskijk1989}) but only if analyzed as involving the separate base-generation of two nominals where one or both of these nominals undergo movement later \citep{Fanselow1988}. Finally, it is suggested that non-restrictive adjectives must have the same basic structure as restrictive ones and that strong inflections are not “referential” in nature but serve to make nominal features like case, number, and gender visible.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textbf{Chapter} \textbf{5} (Ein\textit{{}-words and Adjectival} eine) This chapter investigates the different kinds of \textit{ein}. It is argued that there are basically two main types: semantically vacuous \textit{ein} and adjectival \textit{eine}. The former can occur by itself as the indefinite article but also as part of the singularity numeral \textit{EIN} ‘one’, possessive articles like \textit{mein} ‘my’, etc., and the negative article \textit{kein} ‘no’. These four types of elements are labeled \textit{ein}{}-words. It is proposed that \textit{EIN}, \textit{mein}, and \textit{kein} are composite forms consisting of vacuous \textit{ein} and another component. This derives the morpho-syntactic similarities of these elements. The distinctions follow from the second component, which has different feature specifications from \textit{ein} and occupies different positions in the syntactic representation. The second main type involves adjectival \textit{eine}. This element is a lexically different element: it is an adjective that only appears in definite contexts and induces a duality presupposition. This adjectival element is proposed to be located in a high AgrP.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textbf{Chapter} \textbf{6} (Ein \textit{and Emotiveness}) This chapter presents the first consequence of the second major component of this book. Contrasting pronominal DPs and copular TPs, it is shown that singular contexts exhibit the most restrictions. Three different interpretations are distinguished. First, the ordinary reading is emotive in pronominal DPs but both neutral and emotive in copular TPs where TPs show \textit{ein} in both neutral and emotive contexts. Second, the comparative reading is emotive in both DPs and TPs, where the latter involve \textit{ein}. Finally, the capacity reading is neutral in DPs involving \textit{als} ‘as’ and emotive in TPs involving \textit{ein}. It is proposed that \textit{ein} is not responsible for the emotiveness in the various interpretations. Rather, following \citet{Rauh2004} and de Swart \textit{et al}. (2007), I propose that the emotive readings are due to certain pragmatic restrictions and the realization operator REL. The presence of the latter is flagged by \textit{ein} accounting for the presence of \textit{ein} in these contexts. More generally, this and the next chapter discuss the relatedness of noun phrases and clauses.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Chapter} \textbf{7} (Ein \textit{and Number}) This chapter presents a second consequence of Chapter 5. Comparing again the nominal and clausal domains, I discuss four different constructions: pronominal DPs, copular TPs, pronominals followed by \textit{als}{}-nominals (\textit{als} ‘as’), and non-copular TPs involving \textit{als}{}-nominals. I show that pronominal DPs are very restricted with regard to morphological and semantic number; that is, singular DPs are singular in interpretation, and plural DPs are plural in interpretation. This is different for the other three constructions. While nouns preceded by \textit{ein} can only be singular in interpretation and nouns in the plural can only be plural in interpretation, bare nouns can be both singular and plural in interpretation and combine with singular or plural pronominals. I propose that pronominal DPs always project NumP but that predicate nominals in the other constructions may lack NumP, provided NumP is the highest phrase of the predicate nominal. It is suggested that number derives from an interaction between the noun and the Number head and that \textit{ein} flags the presence of an operator.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textbf{Chapter} \textbf{8} (\textit{Concluding Remarks}) This chapter ties the previous parts together by discussing in more detail the hypotheses proposed for adjectival inflections and \textit{ein}. Reviewing important empirical and theoretical points, it strengthens the conclusions that both adjectival inflections and \textit{ein} are semantically vacuous elements and that both elements provide clues about the structure of the noun phrase. In addition, this chapter tentatively extends the discussion of adjectival inflections to another dialectal variant – Alemannic German, and it considers \textit{ein} in a number of other semantic contexts. Suggestions are made as to how these extensions can be accommodated in the current system. Finally, this chapter addresses some further consequenes as regards concord in agreement features in non-canonical noun phrases, and it considers the question of what all semantically vacuous elements may have in common.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\setcounter{page}{53}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Chapter 2: The Structural Nature of Adjectival Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.  Introduction
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This chapter provides detailed evidence in support of Hypothesis 1, namely that adjectival inflections are semantically vacuous and indicate abstract structure. As regards the latter, the discussion supports Hypothesis 2a: adjectival inflections indicate structure in the higher layers of the noun phrase, and they provide clues about various degrees of embeddings of adjectives.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.1.  The Strong/Weak Alternation of Adjectives
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As briefly discussed in Chapter 1, adjectives can take strong or weak endings. Compare the following typical inflectional alternation on adjectives where the ending is strong when no determiner is present, and it is weak when there is a determiner. Below, I show these patterns in the nominative case using a singular masculine mass noun and a plural count noun (for a complete inventory of all the combinations of case, number, and gender, see \sectref{sec:key:2.1.2}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%1
    \label{ex:key:1}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{frisch-er Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    fresh-\textsc{st}  coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘fresh coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{d-er     frisch-e   Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{st} fresh-\textsc{wk} coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the fresh coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%2
    \label{ex:key:2}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{nett-e   Frauen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     nice-\textsc{st} women
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘nice women’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{di-e     nett-en   Frauen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{st} nice-\textsc{wk} women
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the nice women’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These are the basic patterns. Recall that the phenomenon involving the two related forms of the adjective is labeled the strong/weak alternation. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These inflectional patterns have received a good amount of attention in the literature (e.g., \citealt{Murphy2018}, \citealt{Olsen1991b}, \citealt{Schoorlemmer2009}; for a discussion of these works, see \sectref{sec:key:5}).\footnote{To anticipate the discussion in \sectref{sec:key:5}, previous accounts of adjectival inflections in German have focused on the canonical cases, that is, the alternation of the adjective in simple noun phrases of the schematic type “determiner + adjective(s) + noun”, where the three elements agree in case, number, and gender. As argued in that section, it is not clear how these proposals can explain some of the non-canonical instances. In my view, considerations of elegance cannot be the sole or decisive measure to evaluate a proposal – the empirical coverage must be taken into account to evaluate the plausibility of an analysis.} As pointed out in Chapter 1, it could be claimed that the strong and the weak endings correlate with an indefinite or definite interpretation of the noun phrase. In the (a)-examples above, the nominals with the strong adjectives are indefinite, but in the (b)-examples, the nominals with the weak adjectives are definite. This is often referred to as the semantic distribution of adjectival inflections. At first glance then, these different inflections seem to be a reflex of the varying semantics of the noun phrase. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet{Demske2001}, \citet[60-62]{Lohrmann2010}, and \citet{Rehn2019} show that this is indeed the case in the older Germanic languages including older varieties of German.\footnote{Focusing on the development from Proto-Indo-European to Old Norse, \citet{Pfaff2020} notes that weak adjectives occur indeed in definite contexts but that strong adjectives have nothing to do with indefiniteness. \citet[119-47]{Evans2019} makes an even stronger claim for the older Germanic languages such that weak adjectives only occur in the context of determiners (for the lack of a clear semantic distribution in OHG, see \citealt{Petrova2024}).}  Interestingly, the older literature on Modern German (e.g., \citealt{Curme1910}, \citealt{Lockwood1968}, \citealt{Prokosch1939}) also claims that adjective inflections are regulated by the semantics. Indeed, this claim can occasionally be encountered in the more recent literature as well (e.g., \citealt{KarnowskiPafel2004}: 177-78, \citealt{Pafel1994}: 257-64).\footnote{To explain certain exceptions, these authors claim that \textit{ein} ‘a’, \textit{kein} ‘no’, and possessives involve idiosyncracies.} To briefly illustrate one such proposal, \citet[259]{Abraham2013} seems to claim that different adjectival inflections mark anaphoric/thematic vs. non-anaphoric/rhematic relations, especially in dialectal German. It should be pointed out though that not many details are provided.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet[130-137]{Harbert2007} points out that the strong/weak alternation is different in some of the contemporary languages (also \citealt{Evans2019}, \citealt{Gallmann1996}: 302-03, \citealt{RoehrsJulien2014}, \citealt{Schoorlemmer2009}: 53 fn. 55). In fact, he claims that diachronically, the two sets of endings developed from a semi-regular distribution toward regularization and functionalization \citep[131]{Harbert2007}. This development occurred in different ways: in the Scandinavian languages, the alternation signals (in-)definiteness (e.g., \citealt{Julien2005a}, \citealt{Lohrmann2011}); in German, it developed into an economy principle such that the strong ending is limited to one per noun phrase (e.g., \citealt{Esau1973}). Looking at the literature, it is probably fair to state that a consensus seems to have emerged that the strong/weak alternation in Modern German is regulated by the morpho-syntax (and not the semantics).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

While I basically concur with Harbert and others, I argue that the German facts are more complicated than often presented (I briefly discuss earlier proposals and the related traditional generalizations in \sectref{sec:key:2.1.1}). Before I turn to a detailed discussion of the strong/weak alternation in German, I provide more evidence that adjectival inflections in German do not correlate with (in-)definiteness. This lack of correlation can be shown on the basis of diachronic and synchronic considerations.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.2.  Adjectival Inflections and (In-)definiteness
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Prenominal adjectives in the Germanic languages vary as regards a correlation between the different types of inflections and (in-)definiteness. As briefly discussed in Chapter 1, \sectref{sec:key:2.1.2}, German, Yiddish, and Norwegian exhibit a number of distinctions in this regard. Specifically, adjectival inflections in German clearly lack a correlation regarding (in-)definiteness, and this point can be strengthened by a side-by-side diachronic comparison of Modern German and Old High German (OHG). I provide two types of examples (the OHG instances are taken from \citealt{Demske2001}: 67). First, adjectives behave differently in vocatives: in Modern German, adjectives are strong; in OHG, adjectives are weak:\footnote{As pointed out by Petrova (2024: 203-04, 211-12), vocatives could also involve strong adjectives in OHG, and the same goes for possessive pronominals (see below). Note that this variation is not possible in Modern German.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%3
    \label{ex:key:3}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a. \textit{Dumm-er Idiot!} (Modern German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    stupid-\textsc{st}  idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘Stupid idiot!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{líob-o     man}        (OHG)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    dear-\textsc{wk} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘dear man’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Second, possessive elements also occur with adjectives showing different inflections. As above, Modern German exhibits strong adjectives but OHG weak ones:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%4
    \label{ex:key:4}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{mein lieb-er  Sohn} (Modern German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my   \textsubscript{} dear-\textsc{st} son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘my dear son’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mîn liob-o     sun}      (OHG)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my \textsubscript{} dear-\textsc{wk} son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘my dear son’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I know of no evidence that indicates that the definiteness involved in these vocatives and possessives is different in the two language varieties. As such, Modern German and OHG show different endings on adjectives in the same definite contexts. Recalling the cross-linguistic discussion from Chapter 1, \sectref{sec:key:2.1.2}, I conclude that adjectival endings do not uniformly indicate the definiteness of noun phrases across the different Germanic languages, synchronically (German vs. Norwegian) and diachronically (Modern German vs. OHG).\footnote{There is no agreement in the literature on when adjectival inflections changed from a semantic to a morpho-syntactic distribution. \citet{Demske2001} claims that this change occurred during ENHG. In contrast, \citet[79-88]{Rehn2019} argues that this happened between OHG and MHG (for discussion, see also \citealt{Evans2019}: 147-57, \citealt{Klein2007}, \citealt{Petrova2024}). Observing an increase in the occurrence of the indefinite article \textit{ein}, \citet[88-90]{Rehn2019} proposes that the marking of indefiniteness by the article triggered the change in the way adjectival inflections are regulated in German.}  This lack of correlation in Modern German between inflection and interpretation can be made even more forcefully when we consider certain synchronic data of that language.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I also briefly showed in Chapter 1, \sectref{sec:key:3.1.1} that noun phrases involving indefinite articles or possessive articles take either a strong adjective or a weak adjective in (Modern) German. This depends on the morphological case the noun phrase appears in. Compare the nominals in the nominative in the (a)-examples to those in the dative in the (b)-examples, where the former have strong adjectives but the latter weak ones:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%5
    \label{ex:key:5}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{ein}  \textit{groß-es Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a     big\textsc{{}-st}    car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a big car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{mit} \textsubscript{} \textit{ein-em}  \textit{groß-en Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with a-\textsc{st}      big-\textsc{wk}  car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with a big car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%6
    \label{ex:key:6}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{mein groß-es Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my    big-\textsc{st}   car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘my big car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{mit} \textsubscript{} \textit{mein-em groß-en Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with my-\textsc{st}     big-\textsc{wk}  car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with my big car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As shown in \REF{ex:key:5} and \REF{ex:key:6}, adjectives pattern the same after indefinite articles and possessive articles in German; that is, adjectives can take a strong or weak ending in both indefinite and definite contexts. I argue in detail in Chapter 5 that possessive articles in Modern German consist of a possessive component and vacuous \textit{ein} (e.g., \textit{m-ein} ‘my’). This composite analysis of possessive articles directly relates the latter to \textit{ein}. If so, it is \textit{ein} that seems to be responsible for the regulation of the inflectional endings in \REF{ex:key:5} and \REF{ex:key:6}. In fact, the distinguishing factor appears to be case. However, it is undisputed that the different morphological cases in \REF{ex:key:5} and \REF{ex:key:6} have no impact on the (in-)definiteness of those instances. Indeed, Saxon Genitives also involve strong and weak adjectives, similar to \REF{ex:key:6} above, but in different morphological cases (for detailed discussion, see \sectref{sec:key:2.3}). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  There is more evidence that morphological case alone cannot explain the strong/weak alternation in this language. This can be exemplified with the same lexical item where the morphological case remains unchanged. For instance, the quantifying element \textit{manch}{}- ‘some’ is indefinite. If it is inflectionless, the adjective is strong \REF{ex:key:7a}; if it has a strong ending, the adjective is weak \REF{ex:key:7b}. Similarly, the pronominal determiner \textit{wir} ‘we’ is definite but can occur with both types of endings (8a-b). Note that \% below marks a less preferred but certainly possible form (see \citealt{Bhatt1990}: 154-55; \citealt{Darski1979}: 198; \citealt{Duden1995}: 280, 2007: 39):\footnote{The strong ending in \REF{ex:key:8b} becomes more acceptable if the overt noun is missing (\citealt{Bhatt1990}: 179, \citealt{Duden2007}: 39). Presumably, a strong ending is better able to license a null noun.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%7
    \label{ex:key:7}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{manch nett-e   Studenten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    some   nice-\textsc{st} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘some nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{manch-e nett-en   Studenten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    some-\textsc{st} nice-\textsc{wk} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘some nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%8
    \label{ex:key:8}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{wir nett-en    Studenten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

we  nice-\textsc{wk} \textsubscript{} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘we nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \%  \textit{wir nett-e    Studenten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

we  nice-\textsc{st} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘we nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As will become clear below, these are not isolated cases: they also apply to \textit{solch}{}- ‘such’, \textit{welch}{}- ‘which’, \textit{mir} ‘me(\textsc{dat})’, \textit{dir} ‘you(\textsc{dat.sgl})’, and \textit{ihr} ‘you(\textsc{nom.pl})’. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To summarize, it is, in my view, beyond doubt that at least in (Modern) German, different adjectival endings do not correlate with differences in definiteness. Again, the majority of the recent literature on German concludes the same. However, despite this emerged consensus, the fact that the strong/weak alternation is regulated by the semantics in the related Scandinavian languages and given the occuasional claims that this is also the case in German, I will continue arguing in favor of a morpho-syntactic distribution making this point in different ways throughout this book. This fits well with my overall hypothesis that adjectival inflections are semantically vacuous: they neither have semantics of their own nor do they make semantic features visible.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Returning briefly to the diachronic data, it was pointed out by \citet[82]{Demske2001} that adjectives in OHG are weak after the possessive element but strong after the indefinite article. As illustrated above, this is different in Modern German, where adjectives pattern the same after the possessive element and the indefinite article. If so, this implies that the possessive element and the indefinite article were not related to each other in the older varieties of German as compared to Modern German. It seems clear that German underwent a number of changes in this regard during its diachronic development (for the discussion of some diachronic issues, see Chapter 5; also \citealt{Alexiadou2004}, \citealt{Demske2001}, \citealt{Evans2019}, \citealt{Rehn2019}, and \citealt{Wood2007}).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.3.  Outlook
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The following discussion focuses on (Modern) German with a few cross-linguistic remarks if the phenomenon under discussion shows an interesting parallel or contrast between German and another language. Restricting the discussion to prenominal adjectives, I reach the conclusion that, at least in German, adjectival inflections are semantically vacuous (Hypothesis 1a) and that the strong/weak alternation is not simply a surface phenomenon. Rather, it is, in part, a reflex of different structures of the noun phrase (Hypothesis 2a). In fact, this alternation is argued to provide a good diagnostic of the structure, but not the semantics, of the nominal domain. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To anticipate the discussion, I propose that the derivation of the weak endings on adjectives must take a certain structure into account. I argue that adjectives are only weak if they occur in canonical DPs and undergo a certain reduction process. This process is argued to be feature deletion (Impoverishment), and it is triggered by certain determiners (Impoverishment Rule 1) or in a specific feature context (Impoverishment Rule 2). The diverse distribution of the strong adjectives in other canonical DPs and non-canonical DPs more generally is explained by the fact that Impoverishment does not occur, either because there is no determiner, no relevant determiner, or because the determiner or the adjective is in a position that is different from that in canonical DPs. In a sense, the strong endings form the elsewhere case. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If this is on the right track, then a number of other cases involving canonical DPs appear to be exceptional. I argue in Chapter 3 that the inflectional patterns of adjectives (and determiners) in German are due to several mechanisms. It is important to point out though that the other mechanisms only occur in specific, well-defined contexts; that is, unlike Impoverishment Rule 1, they are restricted to certain combinations of case and gender, or case and number. Chapter 4 discusses some consequences of the current proposal for other analyses. I utilize the system developed for adjectival inflections as a diagnostic of the plausibility of the structures put forth in those works.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  This chapter is organized as follows. In \sectref{sec:key:2}, I provide the basic proposal deriving the infections on adjectives in the canonical cases. I propose that the operation of Impoverishment explains these cases. \sectref{sec:key:3} turns to the discussion of adjectives in non-canonical DPs, and it is proposed that these cases involve different structures where Impoverishment does not apply. In \sectref{sec:key:4}, I address the inflections on determiners and determiner-like elements arguing that Impoverishment does not apply to these elements either. \sectref{sec:key:5} discusses three previous proposals of the strong/weak alternation, and \sectref{sec:key:6} concludes the chapter.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.  Adjectival Inflections in Canonical DPs
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The basic proposal is as follows. Adopting the framework of Distributed Morphology (Chapter 1, \sectref{sec:key:4.2.1}), I assume that adjectival inflections are the result of the spell-out of the features for case, number, and gender on the terminal heads in the syntactic representation. I propose that the weak endings follow from the operation of Impoverishment. Arguing that this mechanism only occurs in canonical DPs, it is taken to reduce the fully specified feature bundles in the tree representation. These reduced feature bundles are then spelled out as the weak endings. Given the diverse distribution of the strong endings, I propose that the occurrence of the latter instances is not subject to any specific conditions. Rather, the strong endings surface in the absence of Impoverishment as the fully specified (underlying) feature bundles. In the current account, the strong inflections involve the elsewhere case. Below, I develop this proposal in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  This section is organized as follows. First I discuss adjectives in the context of \textit{der}{}-words. This is followed by noun phrases involving \textit{ein}{}-words, null articles, and Saxon Genitives. At the end of this section, I turn to adjectives occurring in more complex canonical DPs, canonical DPs that contain extended-adjective constructions and numerals.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.  Weak Adjectives: Impoverishment
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I start by repeating the basic alternation of adjectival inflections involving \textit{der}{}-words, briefly reviewing some traditional generalizations that have been put forth to describe these patterns. This is followed by providing an inventory of the adjectival inflections and discussing a previous proposal. After these preliminaries, I lay the foundation of my own analysis of weak adjectives and spell out some of the details of my account.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.1. Basic Alternation and Traditional Generalizations
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall the basic alternation as regards adjectival inflections: if a determiner is not present, then adjectives surface with strong endings \REF{ex:key:9a}. If a determiner is present, then the determiner has a strong ending, and the adjectives appear with weak inflections \REF{ex:key:9b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%9
    \label{ex:key:9}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a. \textit{frisch-e*(r)    schwarz-e*(r) Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    fresh-\textsc{st}/*\textsc{wk} black-\textsc{st}/*\textsc{wk} coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘fresh black coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{d-er    frisch-e(*r)\textsubscript{} }\textit{schwarz-e(*r) Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{st} hot-\textsc{wk}/*\textsc{st} black-\textsc{wk}/*\textsc{st}  coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the fresh black coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note again that the definite article in \REF{ex:key:9b} has the same ending as the adjectives in \REF{ex:key:9a}. In fact, following \citet{MilnerMilner1972}, \citet{Leu2015} and many others have pointed out that the endings on determiners (D) are the same as the strong endings on adjectives (for detailed discussion, see the next subsection). Assuming a null article for \REF{ex:key:9a}, I summarize this determiner-adjective interaction in the following schematic patterns (> stands for precede; * means multiple occurrences):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%10
    \label{ex:key:10}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{Ø\textsubscript{D}} > STRONG\textsubscript{ADJ*}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  STRONG\textsubscript{D} > WEAK\textsubscript{ADJ*}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Observe that this abstract distribution is instantiated by the canonical cases, simple noun phrases of the schematic type “determiner + adjective(s) + noun”, where the three elements agree in case, number, and gender.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Different generalizations have been offered to describe the distribution in \REF{ex:key:10}. They basically fall into two types. One type of generalization focuses on the inflectional behavior of the adjectives. I provide one such statement in \REF{ex:key:11}, which is often conveniently referred to as Weak After Strong (for similar statements, see \citealt{Bierwisch1967}: 257, \citealt{Eisenberg1998}: 171-73, \citealt{Gallmann1998}: 144, \citealt{Giusti2015}: 207, G. \citealt{Müller2002a}: 129, \citealt{Petrova2024}: 184-85, \citealt{Pfaff2017}: 286, \citealt{Rehn2019}: 58, \citealt{Sauerland1996}: 34, \citealt{Schoorlemmer2009}: 53, and many others):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%11
    \label{ex:key:11}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Weak After Strong}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

An adjective with a weak inflection is preceded by a determiner with a strong inflection. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that this particular formulation does not explicitly mention adjectives that are not preceded by (inflected) determiners. However, since prenominal adjectives are only subject to a two-way alternation, the distribution of strong adjectives can be inferred from \REF{ex:key:11}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The other type of generalization is broader in that it focuses on inflections more generally, that is, on the inflections of both the adjective and the preceding determiner. It highlights the facts that the strong inflection occurs only once in the noun phrase and that it appears in first position. \citet[135]{Roehrs2009a} provides the following formulation of the so-called Principle of Monoinflection (see also, e.g., \citealt{Darski1979}, \citealt{Esau1973}: 140, \citealt{HelbigBuscha2001}: 274-76, \citealt{Murphy2018}: 344, \citealt{Wegener1995}: 105, 153, cf. \citealt{Nübling2011}: 178):\footnote{There is a variant of this generalization that includes the inflection of the head noun. \citet[58]{Evans2019} provides the following statement of the so-called “Once-per-DP” Principle: In the maximal projection of a noun (i.e., in a DP), the strong ending occurs on at most one lexical type (determiner, adjective, or noun). Evans (2019: chapter 3) proposes that the order of strong endings occurring before weak endings is derived from c-command. However, this leads to some issues. One problem is the relevance of the head noun in the calculation of the adjectival inflections (see \citealt{Roehrs2021}).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%12
    \label{ex:key:12}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Principle of Monoinflection}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The first inflected element within a noun phrase carries the strong and the second one the   weak ending.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These are the two main types of traditional generalizations for adjectival inflections in German. Unlike the semantic distribution, they describe a morpho-syntactic pattern. Both types seek to capture the fact that some combinations of the strong and weak endings do not occur; for instance, both exclude patterns where a weak determiner in -\textit{e} or \textit{-en} is followed by a strong adjective in -\textit{er}, \textit{-es,} or \textit{-em} (e.g., *\textit{d-en gut-em Noun} ‘the-\textsc{wk} good-\textsc{st} Noun’). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

While the more general statement in \REF{ex:key:12} should, in my view, be preferred, both generalizations face two types of exceptions. As briefly discussed in Chapter 1, \sectref{sec:key:3.1.1}, these issues have to do (i) with weak elements occurring without the presence of strong elements, and (ii) with strong elements surfacing despite the presence of other strong elements. To my mind, these issues are the result of stating the generalizations in terms of surface precedence and the inflections involved (rather than in terms of the structure and the lexical and featural triggers involved, as made more precise below). The relevant problematic cases are addressed in detail in the course of the following discussion and are summarized in Chapter 3, \sectref{sec:key:7}. Having said that, the generalizations above are good approximations of the canonical facts in German and provide a useful heuristic of the investigation. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The generalization that I will work towards is the following:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%13
    \label{ex:key:13}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Strong/Weak Alternation in German}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Disregarding a few (mostly) lexical exceptions, both adjectives and determiners have strong or weak inflections. Weak inflections only occur in canonical DPs, either 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumviiileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

in the context of certain lexical items (e.g., definite articles), or
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

in certain featural contexts (e.g., genitive masculine/neuter), or
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

in a combination of (a) and (b) (e.g., null articles and stacked adjectives in dative masculine/neuter).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumviiileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Strong inflections occur in all other environments.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

While this generalization is, admittedly, more complex than its predecessors, it will be shown that it is more in line with the empirical facts.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Returning to the data in \REF{ex:key:9}, it is important to point out that the grammaticality judgments of these instances are very sharp. I propose that the morphological mechanism of Impoverishment explains these data. I closely follow the discussion of \citet{RoehrsJulien2014}, who base their proposal on \citet{Sauerland1996}. Before I turn to my analysis, I provide an inventory of the relevant elements, and I briefly review \citet{Sauerland1996}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.2. Inventory of Determiners and Adjectival Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this subsection, I discuss the inventory of determiners and adjectival inflections. This allows me to formulate specific vocabulary insertion rules. Starting with determiners, they can be categorized into three groups: \textit{der}{}-words, \textit{ein}{}-words, and null articles:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%14
    \label{ex:key:14}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.   \textit{der}{}-words: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{der} ‘the’, (stressed) \textit{DER} ‘that’, \textit{dieser} ‘this’, \textit{jener} ‘that’, \textit{jeder} ‘every’, \textit{mancher} ‘some’, \textit{solcher} ‘such’, \textit{welcher} ‘which’, and \textit{alle} ‘all’\footnote{These are the most frequent \textit{der}{}-words (usually provided in reference works).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{ein}{}-words:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{ein} ‘a’, (stressed) \textit{EIN} ‘one’, \textit{kein} ‘no’, and possessive articles like \textit{mein} ‘my’,   \textit{dein} ‘you’, etc.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.   null articles: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ø\textsubscript{D}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The stem forms of the definite article \textit{der} ‘the’ may vary between \textit{de}{}-, \textit{da}{}-, and \textit{di}{}- depending on case and gender. Furthermore, the form \textit{die} is pronounced [di] as the article or [di:] as the distal demonstrative; that is, this form has no inflection. I dedicate a separate section to these and related points in Chapter 3, \sectref{sec:key:3}. Given that, I illustrate the \textit{der}{}-words with the proximal demonstrative \textit{dieser} ‘this’ setting off its endings by a hyphen (I discuss \textit{ein}{}-words and null articles in Sections 2.2 and 2.3). The arrangement of case, number, and gender features in \tabref{tab:key:1} follows \citet{Bierwisch1967}. This alignment allows me to state syncretisms based on natural classes (more on this below). I also follow \citet[250]{Bierwisch1967} and others in assuming that the schwa before a consonantal inflection is due to schwa-epenthesis (R. \citealt{Wiese1996a}: 109-10, 243). This is not indicated separately below:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Determiner Inflections}
\label{tab:key:1}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXX}
 & Masculine & Neuter & Feminine & Plural\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Nominative & dies-er & dies-es & dies-e & dies-e\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Accusative & dies-en & dies-es & dies-e & dies-e\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Dative & dies-em & dies-em & dies-er & dies-en\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Genitive & dies-es & dies-es & dies-er & dies-er\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As already noted above, the endings on the determiners are the same as the strong endings on the adjectives. This is exemplified with the adjective \textit{gut}{}- ‘good’ in \tabref{tab:key:2}. Comparing Tables 1 and 2, there are only two instances (in the genitive masculine/neuter) where the endings on the determiners are different from the endings on the adjectives (-\textit{es} vs. -\textit{en}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Strong Adjective Inflections}
\label{tab:key:2}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXX}
 & Masculine & Neuter & Feminine & Plural\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Nominative & gut-er & gut-es & gut-e & gut-e\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Accusative & gut-en & gut-es & gut-e & gut-e\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Dative & gut-em & gut-em & gut-er & gut-en\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Genitive & gut-en & gut-en & gut-er & gut-er\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Studying Tables 1 and 2 further, we also notice that in many instances, masculine and neuter pattern together (e.g., in the dative and genitive) and feminine and plural do too (e.g., in the nominative, accusative, and genitive). The weak inflections are provided in \tabref{tab:key:3}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Weak Adjective Inflections}
\label{tab:key:3}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXX}
 & Masculine & Neuter & Feminine & Plural\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Nominative & gut-e & gut-e & gut-e & gut-en\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Accusative & gut-en & gut-e & gut-e & gut-en\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Dative & gut-en & gut-en & gut-en & gut-en\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Genitive & gut-en & gut-en & gut-en & gut-en\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In view of \tabref{tab:key:2} and \tabref{tab:key:3}, German is traditionally taken to have an inventory of five strong and two weak endings \citep{Duden1995}. The latter (-\textit{e}, -\textit{en}) form a proper subset of the former (-\textit{e}, -\textit{en}, -\textit{er}, -\textit{es}, -\textit{em}). Considering all three tables, we notice that the nominative is the same as the accusative within each gender and plural, with one well-known exception (e.g., \citealt{Blevins1995}: 145-46, \citealt{Eisenberg1998}: 173-74): the masculine accusative has a different form (-\textit{en}) from the masculine nominative (-\textit{er}, -\textit{e}), something I return to below. Given the overall similarity of the inflections on determiners and adjectives, I treat them the same and refer to them as adjectival endings. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To reiterate, strong endings have more different forms than weak ones. In fact, comparing the tables above, they express more distinctions in case, number and gender. One of the main insights of \citet{Sauerland1996} is that strong endings involve more features than weak ones. As such, there is no real difference between the traditional strong and weak endings (although I retain this distinction in name for expository purposes). The vocabulary insertion rules formulated in \sectref{sec:key:2.1.5} reflect this. Indeed, taking the number of features as the relevant metrics, we will see that the (less specified) weak endings form a proper subset of the (more specified) strong endings.\footnote{Rather than an account based on feature specificity as developed below, G. \citet{Müller2002a} provides an optimality-theoretic proposal (also \citealt{Gallmann2004}). In order to formulate simpler constraints, Müller analyzes noun phrases as NPs although DPs (e.g., \textit{dieser} ‘this’) exist in the account. Furthermore, and also unlike the current account, Müller’s proposal is that inflections have no independent status as morphemes in the lexicon.} \citet{Sauerland1996} proposes to derive the weak inflections from the strong endings by a deletion process. Consider his proposal in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.3. \citet{Sauerland1996}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Adopting the general framework of Distributed Morphology (see Chapter 1, \sectref{sec:key:4.2.1}), \citet{Sauerland1996} proposes to derive the basic alternation illustrated in \sectref{sec:key:2.1.1} by a feature deletion process called Impoverishment (focusing here on German only, I make some minor adjustments to the presentation of his account). He assumes that inflections involve fully specified, abstract feature bundles underlyingly. The feature bundles make up the terminal nodes of the syntactic representation. If these feature bundles are reduced by Impoverishment, they are spelled out as weak endings; if not, they surface as strong endings. Impoverishment is triggered morpho-syntactically: the inflectional feature bundle of the adjective undergoes feature deletion if the preceding determiner has a strong inflection. Sauerland combines Impoverishment and Feature Hierarchy in his account. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In more detail, the weak endings occur, unsurprisingly, in all featural contexts. Sauerland observes though that like in Norwegian, in German they surface in the same featural contexts as their homophonous strong endings; for instance, -\textit{e} is not only the corresponding weak inflection of strong -\textit{er} in the nominative masculine but also of strong -\textit{e} in the nominative feminine (see Tables 2 and 3 above). Similar considerations hold for the other weak inflection; for instance, -\textit{en} is not only the corresponding weak inflection of strong -\textit{em} in the dative masculine but also of strong -\textit{en} in the accusative masculine. In other words, the two weak endings -\textit{e} and -\textit{en} also occur as strong endings. Now, since they occur in more contexts (weak and strong), -\textit{e} and -\textit{en} are taken to be the least marked strong endings. This means that there are actually no weak endings \textit{per se} – only (strong) inflections that differ in their specificity.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As regards -\textit{e} and -\textit{en} themselves, they are also clearly distinguished as regards their indiviudal specificity. Considering the relevant feature combinations, strong -\textit{e} can appear as weak -\textit{e} (e.g., in the nominative feminine), and strong -\textit{en} can surface as weak -\textit{en} (e.g., in the dative plural). However, while strong -\textit{e} is changed to weak -\textit{en} (e.g., in the nominative plural), strong -\textit{en} is never altered to weak -\textit{e}. Sauerland concludes that this indicates that -\textit{e} is more specified than -\textit{en}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In order to account for the strong/weak alternation, Sauerland adopts the Hierarchy of Case features from \citet{Harris1994}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%15
    \label{ex:key:15}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

             Case
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  direct    oblique
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  accusative  genitive
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Evidence in German that the direct cases (i.e., nominative and accusative) pattern together in opposition to the oblique cases (i.e., dative and genitive) comes from the weak inflections in the neuter and feminine where -\textit{e} occurs in the direct cases but -\textit{en} in the oblique ones (see \tabref{tab:key:3}).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Sauerland formulates the following seven vocabulary insertion rules:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%16
    \label{ex:key:16}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [+plural, +oblique, -genitive]    →  {}-\textit{en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [+masc, +direct, -accusative]     →  {}-\textit{er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  [-fem, +oblique, -genitive]     →  {}-\textit{em}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  [-fem]           →  {}-\textit{es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  e.  [+oblique]         →  {}-\textit{er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  f.  [+direct]         →   {}-\textit{e}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  g.  []           →   {}-\textit{en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

He proposes that feature deletion widens the distribution of -\textit{e} in \REF{ex:key:16f} and -\textit{en} in \REF{ex:key:16g}. In his account, there are three types of deletion rules yielding a total of six:\footnote{I added the feature [+genitive] to \REF{ex:key:17b} to allow -\textit{em} to surface on strong adjectives in dative contexts.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%17
    \label{ex:key:17}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a. Impoverishment rule applying to inflections on determiners and adjectives:  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(i) Delete Agr with [+masc, +direct, +accusative]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. Impoverishment rule applying to inflections on adjectives:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    (ii) Delete Agr with [-fem, +oblique, +genitive]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. Impoverishment rules applying to adjectives in weak positions:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    (iii) Delete [Gender]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    (iv) Delete [accusative]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    (v)  Delete [oblique]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    (vi) Delete [Case] in the environment of [+plural]  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

After briefly commenting on the role of the various deletion rules, I discuss some shortcomings and point out how the analysis to be developed below deals with these issues.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As mentioned above, accusative masculine -\textit{en} is special with both determiners and adjectives: it is the only instance where strong and weak inflections differ in the nominative and accusative cases. This exception is captured by formulating the deletion rule in \REF{ex:key:17a}. As a consequence, only \REF{ex:key:16g} can apply.\footnote{\citet[235]{Schoorlemmer2009} formulates an Impoverishment rule to account for the weak ending -\textit{en} (on adjectives) in the accusative masculine.}  Similarly, adjectives in the genitive masculine/neuter are exceptional in that their ending is -\textit{en} (rather than -\textit{es} as compared to the determiners). This follows from the deletion rule in \REF{ex:key:17b} such that only \REF{ex:key:16g} can apply. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Finally, the set of rules in \REF{ex:key:17c} accounts for the (traditional) weak inflections. The weak positions of adjectives are defined by adjectives occurring after determiners with strong inflections (no further details are provided). Adopting additional Feature Hierarchies, Sauerland assumes that features with a capital letter subsume all instances of that category. This means that the deletion of Gender removes masculine, neuter, and feminine features and that the deletion of Case removes direct (which includes nominative and accusative) and oblique (which consists of dative and genitive) cases. After the deletion of the features in \REF{ex:key:17c} from the syntactic representation, the vocabulary items in (16a-e) can no longer be inserted. Rather, only the rule in \REF{ex:key:16f}, restricted now to singular contexts (due to (vi) in \REF{ex:key:17c}), and the rule in \REF{ex:key:16g} can apply. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given certain interpretations (e.g., which exact features are present on terminal nodes), the system works, and I think it is basically on the right track. However, there are also some issues. Pointing out these shortcomings is not meant to be a criticism of Sauerland’s proposal. Rather, the following points are intended to be understood as issues that any proposal that works on a similar set of assumptions needs to address. There are three general and three technical issues.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with the general points, there are three types of deletion rules (feature deletion for both determiners and adjectives \REF{ex:key:17a}, feature deletion for adjectives only \REF{ex:key:17b}, and feature deletion to bring about weak inflections \REF{ex:key:17c}). In the system to be developed below, the effect of \REF{ex:key:17a} does not follow from an Impoverishment rule but rather from the way the vocabulary insertion rule for accusative masculine -\textit{en} is stated: Impoverishment deletes a feature, but that feature is not part of the vocabulary insertion rule for this -\textit{en}. The effects of \REF{ex:key:17b} and \REF{ex:key:17c} do involve Impoverishment rules in the system to be developed (but in modified form): the effect of \REF{ex:key:17b} is capured by Impoverishment Rule 2 and that of \REF{ex:key:17c} by Impoverishment Rule 1.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Second, there are six deletion rules in total. Below, only two Impoverishment rules are proposed. In fact, it is suggested that the application of \REF{ex:key:17b} is more general: Impoverishment Rule 2 also accounts for the weak inflections on certain determiners in genitive masculine/neuter contexts (Chapter 3, \sectref{sec:key:4}). Third, since the weak inflections on adjectives depend on the strong inflections of determiners, the latter must be inserted first. It is left unspecified by Sauerland why this must be so (and cannot be the other way around). Below, no such ordering is crucial. It is proposed that it is not the inflection of the determiner but rather its stem (specifically, its categorial feature [+D] present in the syntax) that triggers Impoverishment.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Turning to some technical questions, it is not clear (except by stipulation) why \REF{ex:key:16d} must be ordered before both \REF{ex:key:16e} and \REF{ex:key:16f} – all these vocabulary insertion rules are equally specified (note that if \REF{ex:key:16e} were ordered before \REF{ex:key:16d}, this would yield the incorrect forms \textit{der} ‘the’ or \textit{guter} ‘good’ in genitive masculine/neuter contexts; if \REF{ex:key:16f} were ordered before \REF{ex:key:16d}, this would result in the incorrect forms \textit{die} ‘the’ or \textit{gute} ‘good’ in nominative/accusative neuter contexts).\footnote{Halle \& \citet[281]{Marantz1994} assume “a reasonable feature hierarchy that treats Case features as more specific than gender features”. If so, then both \REF{ex:key:16e} and \REF{ex:key:16f} should indeed be ordered before \REF{ex:key:16d} yielding incorrect forms as discussed in the main text. This means that Sauerland must stipulate the ordering of the vocabulary items (see \citealt{EmbickNoyer2007}: 298 fn. 14).} Second, as far as I can tell, the deletion rule (iv) in \REF{ex:key:17c} is not needed. Finally, if Agr, presumably the head in the syntactic representation that involves the inflection, is deleted by \REF{ex:key:17a} or \REF{ex:key:17b}, it is not clear where or how the default ending -\textit{en} will be attached.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The following proposal draws heavily on the insights of \citet{Sauerland1996} but tries to avoid the general and technical issues raised above. In addition, it aims at spelling out in detail the morpho-syntactic (and lexical) conditions the strong/weak alternation is subject to.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.4. Basic Proposal
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I propose that determiners, \textit{der}{}-words and \textit{ein}{}-words, form triggers for Impovishment. I assume that Impoverishment operates in a local fashion. More precisely, I propose that the feature bundles that are to be realized as the overt inflections on the adjective undergo Impoverishment inside the phrase that hosts them (i.e., AgrP). This means that the trigger for Impoverishment, the determiner, and its target, the inflection on the adjective, must be in a local relation at some point in the derivation. With the determiners surfacing in the DP-level, this implies that determiners must have originated in a lower position. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In Chapter 1, \sectref{sec:key:4.1.1}, I argued that there are two determiner positions in the noun phrase, ArtP and DP (e.g., \citealt{Julien2002}, 2005a; \citealt{Taraldsen1990}), and that both are connected by movement (e.g., see again \citealt{Borer2005}; \citealt{Nykiel2015}; \citealt{Rehn2019}; \citealt{Roehrs2009a}, 2015, 2019; \citealt{Schoorlemmer2009}, 2012; van \citealt{Gelderen2007}). I provided some distributional evidence for a lower position of determiners in that section. Adopting the following structure and derivation, I reiterate the proposal that the determiner moves from ArtP to DP in a successive-cyclic fashion. This is illustrated below with an article (for the DP structure with a demonstrative undergoing movement, see \sectref{sec:key:2.4}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%18
    \label{ex:key:18}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{d-er    frisch-e\textsubscript{} }\textit{schwarz-e Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{st} hot-\textsc{wk} black-\textsc{wk}  coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the fresh black coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.   DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       D    AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     \textit{der}\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                InflP     Agr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{frische}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        Agr             AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        der\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                  InflP    Agr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{schwarze}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            Agr             ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            der\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Art’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                           Art                NumP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                der\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                              Num’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                           Num    NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                         t\textsubscript{k}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                       \textit{Kaffee}\textsubscript{k} Num    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall also that definite determiners have the feature [+DEF] and move to the DP-level to specify the definiteness feature on D. In contrast, \textit{ein} is semantically vacuous and does not have a feature for definiteness. Consequently, it can surface in different positions (i.e., Art, Card, or D). This, in turn, makes it possible to claim that \textit{ein} supports overt operators or flags the presence of covert operators (Chapter 5). To be clear, I assume that \textit{ein} remains in ArtP unless it has to move up for a reason. As shown below, weak inflections only occur in regular, simple DPs like \REF{ex:key:18b}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I propose that determiners move via adjunction to the DP-level. Articles (Art) are heads, and they move by head adjunction to D; demonstratives (Dem) are phrases, and they move by phrasal adjunction to Spec,DP.\footnote{Recall from Chapter 1, \sectref{sec:key:4.1.2} that demonstratives involve complex structures with InflP at the top. To avoid confusion, I use Dem (for demonstrative) in the tree representation in \REF{ex:key:19b} below.} I assume that all elements including structural elements are fully specified for case, number, and gender yielding concord. Impoverishment involves the deletion of a certain feature (to be made more precise below). Taking determiners to be triggers for Impovishment, I illustrate this feature reduction by [+I(mpoverishment)] for now. I propose that adjunction by the determiner reduces the feature bundle of the element adjoined to. Assuming Percolation within the same phrase, this feature reduction spreads from the head to the phrase in case of an article \REF{ex:key:19a}, or from the phrase to the head in case of a demonstrative \REF{ex:key:19b}.\footnote{\citet{Olsen1991b} assumes Percolation from the phrase to the head; \citet[136-37]{Norris2014} points out that Percolation from the head to the phrase immediately follows from Bare Phrase Structure \citep{Chomsky1995}.} Finally, assuming Spec-head agreement and Percolation, [+I] spreads to InflP in Spec,AgrP and to its head Infl. The latter contains the feature bundle for case, number, and gender ([CNG]) and will be spelled out as the ending on the adjective stem in AP in (19a-b). The relevant portions of the trees, which include the adjoined determiner, are given for these two scenarios below:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%19
    \label{ex:key:19}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Adjunction of Article}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            AgrP\textsubscript{[+I]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    InflP\textsubscript{[+I]}    Agr’\textsubscript{[+I]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         [CNG]\textsubscript{[+I]}    AP  Agr\textsubscript{[+I]}    … t\textsubscript{k}…
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Art\textsubscript{k}    Agr\textsubscript{[+I]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Adjunction of Demonstrative}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         AgrP\textsubscript{[+I]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Dem\textsubscript{k}          AgrP\textsubscript{[+I]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    InflP\textsubscript{[+I]}    Agr’\textsubscript{[+I]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  [CNG]\textsubscript{[+I]}    AP  Agr\textsubscript{[+I]}    … t\textsubscript{k}…  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that the adjunction to heads and phrases, feature Percolation within the same phrase, and Spec-head agreement are all local relations; that is, Impoverishment occurs in a locally restricted domain. Given a recursive AgrP in \REF{ex:key:18b} above and successive-cyclic movement of the determiner from ArtP to DP, this analysis provides an immediate explanation of why either all adjectives are strong or all are weak. Next, I flesh out this basic proposal. The following discussion is based on Roehrs \& \citegen{Julien2014} analysis of German with one modification (Footnote Error: Reference source not found). In the course of the discussion, I add many details to that proposal. As mentioned above, \citet{RoehrsJulien2014} was heavily influenced by \citet{Sauerland1996}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.5. The Proposal in More Detail: Vocabulary Insertion Rules
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Following \citet{Bierwisch1967} and others, I argue that case, number, and gender are not primitive features. Rather, I assume that case involves a two-category system and can be represented by the features [O(blique)] and [S(tructural)].\footnote{The feature [S(tructural)] was chosen over [D(irect)] to avoid confusion. Below, I propose that the feature [S] is deleted by Impoverishment, which is triggered by the presence of the categorial feature of determiners [+D]. This choice allows me to clearly distinguish between the feature that undergoes deletion and the one that involves the trigger of that deletion.} Each of these features can have a negative or a positive value (i.e., [-, +]) yielding the following decomposition:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%20
    \label{ex:key:20}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  nominative:  [ -O,  {}-S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  accusative:  [ -O, +S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  dative:    [+O,  {}-S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  genitive:  [+O, +S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Similarly, gender consists of the features [F(eminine)] and [N(euter)] and can be broken down as in (21a-c). Plural is the neutralization of gender \REF{ex:key:21d}, something \citet[80]{Sternefeld2008} refers to as the “fourth gender”:\footnote{\citet[163-65]{Krifka2009} points out that there are advantages in assuming that plural involves a separate gender (for the relatedness of feminine and plural, see also \citealt{Leiss1994}: 291-94).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%21
    \label{ex:key:21}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  masculine:  [ -F,  {}-N]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  neuter:   [ -F, +N]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  feminine:   [+F,  {}-N]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  plural:    [+F, +N]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Unlike plural, all singular forms involve at least one negative value for [F] or [N].
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Roehrs \& Julien (2014: 250 fn. 5) provide motivation for the system above. Most importantly, following Harbert’s (2007: 468) Case Hierarchy, they assume that nominative is the least and genitive the most oblique case. Based on \citet{Steinmetz2001}, they assume that masculine is the default gender in German, and following \citet[80]{Sternefeld2008}, they take plural to be the neutralization of gender. Note that taking the masculine gender and the nominative case as the least marked categories – they each have two negative values in their feature decompositions – is consonant with Bierwisch’s (1967: 253) statement that there is more syncretism within marked categories (observe that with the strong inflections, masculine has the most different forms in \tabref{tab:key:4}; conversely, genitive has the fewest different forms).\footnote{There are many different systems to decompose case, number, and gender – too many to review here in detail. For other, partially similar decompositions, see \citet[246-50]{Bierwisch1967}, \citet[51]{Evans2019}, \citet[124-25]{Gallmann2004}, G. \citet[119]{Müller2002a}, \citet{Trommer2005}, B. Wiese (1996, 1999), \citet[48-49]{Wunderlich1997}, and many others. \citet{Blevins1995} captures syncretism as part of Feature Hierarchy, with primary (e.g., feminine vs. non-feminine) and subsidiary (e.g., masculine vs. neuter) features. This hierarchy as well as Feature Geometry more generally will not play a role here (for some brief remarks on hierarchies as regards DM, see \citealt{ArregiNevins2012}: 204-05, \citealt{HarleyNoyer1999}: 6).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Before moving on, I point out that the decomposition of gender and number in \REF{ex:key:21} allows us to state certain well-known syncretisms that we find in the masculine/neuter vs. in the feminine/plural, that is, in the opposition [-F] vs. [+F]: (i) strong adjectival inflections in the nominative/accusative (C = consonant): -\textit{eC} vs. -\textit{e}, (ii) strong adjectival inflections in the dative: -\textit{em} vs. -\textit{er/-en}, (iii) strong adjectival inflections in the genitive: -\textit{es} vs. -\textit{er}, (iv) third-person pronouns in the nominative/accusative (abstracting away from \textit{ihn} ‘him’): \textit{er}/\textit{es} ‘he/it’ vs. \textit{sie} ‘she, her; they, them’, (v) \textit{ein}{}-words in the nominative/accusative (abstracting away from \textit{einen} ‘a’): uninflected \textit{ein} vs. inflected \textit{eine}, (vi) definite article stems in the nominative/accusative: \textit{de}{}-/\textit{da}{}- ‘the’ vs. \textit{di}{}- ‘the’, (vii) possessive articles consisting of bound and free morphemes: \textit{sein} ‘(POSS+\textit{ein} =) his, its’ vs. \textit{ihr} ‘her, their’ (Chapter 5, \sectref{sec:key:4.1.2}), and perhaps less widely known (viii) pronominal DPs in the dative masculine/neuter prefer strong adjectives vs. pronominal DPs in the dative feminine and nominative plural prefer weak adjectives (Chapter 3, \sectref{sec:key:5}).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As to case, syncretism is much less prevalent here. As should be clear from the discussion of syncretism in gender and number provided just above, there is syncretism in the nominative/accusative vs. the oblique cases. Specifically, while nominative and accusative have the same inflections in the neuter, feminine, and plural (e.g., plural -\textit{e}), the oblique cases dative and genitive have the same inflections only in the feminine (i.e., -\textit{er}). It is also worth pointing out that there are very few instances of syncretism between the structural and oblique cases; for instance, the forms of the first and second person plural of the personal pronoun are the same in the accusative and dative cases (i.e., \textit{uns} ‘us’, \textit{euch} ‘you’). Note though that the latter instances are the exception, rather than the rule. This may have to do with the fact that these items are non-third person elements.\footnote{This may also be related to the fact that the structural cases behave differently from the oblique cases in syntactic terms (see Bayer \textit{et al}. 2001). To account for these differences, these authors propose that the oblique cases project a K(ase) P on top of the DP, an idea that I will not follow here.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In order to state the syncretisms above and to reduce the number of vocabulary insertion rules, I utilize value variables (α, β), whereby α or β in a vocabulary insertion rule is compatible with a positive or negative value in the syntactic representation (note that the variable does not get specified for such values – it is a compatibility feature). To give an example, the specification [αO] in a vocabulary insertion rule is compatible with a positive or a negative value in the syntactic representation (but not both at the same time). Among others, this captures the syncretism of nominative and accusative case (cf. (20a-b) above). Similarly, I employ a category variable (γ), which ranges over [F] or [N]; that is, over one of these two features (but not both at the same time). To illustrate, [-γ] in a vocabulary insertion rule is compatible with either [-F] or [-N] in the syntactic represenation. This means that [-γ] corresponds to singular (as opposed to plural, which has a positive value for both [F] and [N]; for the discussion of the explicit use of disjunctions, see G. \citealt{Müller2002a}: 120 fn. 14).\footnote{The category variable [γ], with a negative value, will find more application with first and second-person pronouns, which lack gender (Chapter 3, \sectref{sec:key:5}).} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Following \citet{Sauerland1996}, I propose that the two weak endings have the same feature specifications as certain strong endings (cf. also \citealt{Gallmann2004}: 140). I assume that these instances are related and have the same vocabulary insertion rules (see below). For clarity, these related instances are respectively marked by round and curly brackets in \tabref{tab:key:4}. Using the feature decomposition above, all strong and weak inflections can be analyzed as follows (for convenience, I added the traditional labels as subscripts after the featural decomposition):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{ Strong and Weak Inflections in German}
\label{tab:key:4}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXXXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\bfseries STRONG} & [-F, -N]\textsubscript{M} & [-F, +N]\textsubscript{N} & [+F, -N]\textsubscript{F} & [+F, +N]\textsubscript{PL} & {\bfseries WEAK} & [-γ]\textsubscript{SGL} & [+F, +N]\textsubscript{PL}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

[ -O,  {}-S]\textsubscript{NOM} & {}-er & {}-es & (-e) & {}-e & [-O] & (-e) & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

[ -O, +S]\textsubscript{ACC} & {}-en & {}-es & (-e) & {}-e &  &  & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%\hhline%%replace by cmidrule{-------~}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

[+O,  {}-S]\textsubscript{DAT} & {}-em & {}-em & {}-er & \{-en\} & [+O] & \multicolumn{2}{c}{ \{-en\}}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

[+O, +S]\textsubscript{GEN} & {}-es & {}-es & {}-er & {}-er &  &  & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%\hhline%%replace by cmidrule{-------~}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Comparing \tabref{tab:key:4} to \tabref{tab:key:3} from above, we notice that the accusative masculine ending -\textit{en} is missing from the weak endings here. The special status of this ending follows from the way the strong ending for accusative masculine is stated in \REF{ex:key:22c}.\footnote{In \citet{RoehrsJulien2014}, the exceptional accusative masculine form -\textit{en} contained the specification [+S]. Removing this feature from the vocabulary entry as in \REF{ex:key:22c} still allows me to specify this ending disambiguously (as nominative masculine -\textit{er} is more specific). In fact, this is now the only strong ending without a specification for [S]. This will become significant during the discussion of Impoverishment.} Assuming that features can be underspecified, the rules for vocabulary insertion of the endings can be stated as follows:\footnote{In his critique of some previous – what he calls – constructive analyses (\citealt{Bierwisch1967}, \citealt{Blevins1995}, B. \citealt{Wiese1999}, and \citealt{Wunderlich1997}), G. \citet{Müller2002b} points out that all these types of accounts have rules that show different contexts for the insertion of (some of) the same strong inflections, for instance, -\textit{er} in \REF{ex:key:22a} and \REF{ex:key:22b}. This leads to systematic as well as accidental syncretism. According to G. Müller (2002a, 2002b), destructive type of analyses involving rules that ban the insertion of certain inflections fare better.  This criticism seems valid and can also be leveled against the current proposal. However, I think such multiple vocabulary insertion rules for one and the same inflectional form(s) cannot completely be avoided (also \citealt{Sauerland1996}: 24). Considering that certain endings occur in other, unrelated domains as well (e.g., plural inflections on nouns: -\textit{e}, -\textit{en}, -\textit{er}, -\textit{s}; personal endings on verbs: -\textit{e}, -\textit{en}), it is not clear how these inflections can be related yielding just one rule of vocabulary insertion (or ban) for each inflection. Note in this regard that unspecified -\textit{en} in \REF{ex:key:23b} could potentially do multiple duty as a plural inflection for nouns and verbs. An advantage of the current proposal is that unlike some of the previous analyses, here the weak inflections are explicitly discussed; in fact, they are integrated with the strong inflections, and there is no inherent difference between these types of inflections (for the discussion of quite different types of analyses of weak inflections, see the end of \sectref{sec:key:5.3}).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%22
    \label{ex:key:22}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          Strong (except for feminine \textit{{}-e} and plural \textit{{}-en}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  a.  [+F, -N, +O, αS]  →  \textit{{}-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [+F, +N, -O, αS]  →  \textit{{}-e}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [αF, αN, αO, αS]  →  \textit{{}-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  [-F, -N, -O]    →  \textit{{}-en} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [-F, +O, -S]    →  \textit{{}-em}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  [-F, αO, βS]    →  \textit{{}-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%23
    \label{ex:key:23}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

             Weak (including strong feminine \textit{{}-e} and plural \textit{{}-en}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  a.  [-γ, -O]      →  \textit{{}-e}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  []      →  \textit{{}-en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Comparing \REF{ex:key:22} to \REF{ex:key:23}, note first that the (unambiguously) strong endings are stated in more specific terms than the weak endings. In other words, weak endings are underspecified to a greater degree. This means that there is no real difference between \REF{ex:key:22} and \REF{ex:key:23}. This is confirmed by the fact that some of the strong endings (i.e., certain instances of -\textit{e} and -\textit{en}) share the same vocabulary insertion rules as the weak inflections -\textit{e} and -\textit{en} \REF{ex:key:23}. This was one of \citegen{Sauerland1996} main insights. Second, I assume that a vocabulary item involving the same number of features with a variable is less specific than a corresponding vocabu\-lary item without such a variable. Accordingly, I have listed the vocabulary items with descending degrees of specificity in \REF{ex:key:22} and \REF{ex:key:23}. Note that equally specified items differ from one another (in both \REF{ex:key:22a} and \REF{ex:key:22c}, the two respective items vary in the feature [O]). Finally, as is often stated, note that the two paradigms, strong and weak, in \tabref{tab:key:4} are simply generalizations. They have been replaced by the vocabulary insertion rules above; that is, paradigms are an epiphenomenon without independent status. Before moving on, I comment on the relationship between the vocabulary insertion rules and the terminal nodes in the syntactic representation.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with the weak inflections, note that the weak ending -\textit{e} never occurs in the plural. Thus, it distinguishes number as it only occurs in the singular. However, it does not distinguish individual cases or genders. This is particularly clear in the current account where -\textit{e} is specified [-γ, -O]. The weak ending -\textit{en} is not specified for any CNG features. In the current account, it is the elsewhere case. What both of these inflections have in common is that they do not distinguish case and gender. In contrast, the strong endings are specified for more features. Note first that strong endings are more specific in their occurrence (e.g., -\textit{em} only occurs in the dative masculine/neuter; -\textit{s} only in the masculine/neuter). This is reflected by the contexts in which their vocabulary insertion rules apply; for instance, the inflection -\textit{em} is specified as [-F, +O, -S]. Compared to weak inflections, strong inflections distinguish case, number, and gender better.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The feature specifications on the terminal nodes in the syntactic representation are different. If present, they always involve specifications for the four features [F, N, O, S]. In other words, all features are present in the abstract syntactic representation, but only a subset of those features are specified in the vocabulary insertion rules – the latter involve underspecification allowing competition between some of the vocabulary insertion rules. Note again that these are basic tenets of DM.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.6. The Proposal in More Detail: Structural Position of Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In DM, vocabulary items are inserted late. Matching the maximum number of features on the terminal head, the availability of a more specific vocabulary item precludes the insertion of a less specific one. With the system laid out above in mind, the abstract feature bundle in Infl ([CNG]) of the larger adjective structure can now be restated (for convenience, I continue referring to the feature bundle as CNG if the decomposition is not relevant). Taking dative masculine as an example, the terminal head Infl is specified as in \REF{ex:key:24}. Turning to Vocabulary Insertion, note that [-F, +O, -S] in \REF{ex:key:22c} is the most specific, matching vocabulary item. It is inserted and spelled out as the strong ending -\textit{em} on \textit{klein}{}- ‘small’ resulting in the form after the arrow:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%24
    \label{ex:key:24}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

             InflP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       Infl’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  [-F, -N, +O, -S]    AP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               A’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           A    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                  \textit{klein} →  \textit{klein}{}-\textit{em}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The structure above is a convenient shorthand for a more detailed derivation. More specifically, after movement of the adjective stem \textit{klein} to Spec,InflP, Linearization, and Vocabulary Insertion, we obtain the string in \REF{ex:key:25}. Following \citet{Murphy2018}, I assume that the adjectival suffix undergoes Local Dislocation (instantiated as a type of leaning) combining with the adjective stem to yield the spell-out form after the arrow (see also \sectref{sec:key:5.3}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%25
    \label{ex:key:25}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{klein    {}-em} →  \textit{kleinem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Unless these details are of signifance, I usually illustrate the analysis by a syntactic representation as in \REF{ex:key:24}. As briefly discussed in Chapter 1, \sectref{sec:key:4.1.2}, determiners also have internal structure. Since they are proposed to be the trigger of Impoverishment, I discuss their inner makeup in more detail now.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I focus here on four common types of determiners: the indefinite null articles (in the contexts of mass and plural nouns), the indefinite article \textit{ein}, the definite article, and the demonstrative. Again illustrating with the dative masculine, these elements have the following internal structure. The terminal head Art where the indefinite null articles later surface has a categorial feature [+D(eterminer)] and a negative feature for definiteness. As null elements, they have no features for case, number, and gender that are later spelled out as inflections \REF{ex:key:26a}. The indefinite article \textit{ein} only has the categorial feature [+D] and features for case, number, and gender. Given these different types of features (category vs. CNG), I assume that these make up two individual feature bundles. These two separate bundles undergo feature union yielding Art as in \REF{ex:key:26b}.\footnote{There is evidence that inflections are generated with their determiner heads. As mentioned in Chapter 1 and discussed in more detail in Chapter 8, \sectref{sec:key:2.2.2}, there are cases where \textit{ein} occurs in two positions. Crucically, \textit{ein} in the lower position has the same, varying inflection as related \textit{kein} ‘no’. Compare (ia) to (ib):(i)  a.  \textit{k-ein-e     so’n-e  Leute}      \textsc{neg}{}-a-\textsc{st} so.a-\textsc{st} people      ‘no such people’  b.  \textit{mit}  \textit{k-ein-en \textsubscript{} } \textit{so’n-en Leuten}      with \textsc{neg}{}-a-\textsc{st} so.a-\textsc{st} people      ‘with no such people’This clearly shows that inflected \textit{ein} has a bipartite structure. As indefinite articles are usually assumed to be heads, this is captured in the main text by two separate feature bundles under Art.} Like the indefinite overt article, the definite article has a feature bundle for case, number, and gender; like the indefinite null articles, the definite article has a definiteness feature, but it is specified as positive \REF{ex:key:26c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%26
    \label{ex:key:26}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Indefinite Null Articles} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               Art\textsubscript{[+D; -DEF]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              [+D; -DEF]        →  \textit{Ø\textsubscript{D}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Indefinite Overt Article}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Art\textsubscript{[+D][-F, -N, +O, -S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [+D]    [-F, -N, +O, -S]  →  \textit{ein-em}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Definite Article}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Art\textsubscript{[+D; +DEF][-F, -N, +O, -S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      [+D; +DEF]    [-F, -N, +O, -S]  →  \textit{d-em}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The spell-out forms are as follows: [+D; -DEF] is realized as \textit{Ø\textsubscript{D}}, [+D] as \textit{ein}{}-, and [+D; +DEF] as \textit{d}{}-. As for the inflection, only [-F, +O, -S] matches the CNG bundle yielding -\textit{em}. Taken together, this spells out the forms of the determiners provided after the arrow.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Unlike the first three elements, the demonstrative is phrasal. I assume there are two terminal heads \REF{ex:key:27}. Dem involves the features [+D; +DEF, +DEIX] and projects its own extended projection with InflP at the top (see \citealt{Leu2007}, 2015; \citealt{Roehrs2010}, 2013a). The features for case, number, and gender are in Infl. Dem moves to adjoin to Infl (not shown). Given feature union and Percolation, all features spread to InflP:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%27
    \label{ex:key:27}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Demonstrative}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 InflP\textsubscript{[+D; +DEF, +DEIX][-F, -N, +O, -S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     [-F, -N, +O, -S]    DemP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            {\textbar}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        Dem
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         [+D; +DEF, +DEIX]  →  \textit{dies-em}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The features [+D; +DEF, +DEIX] and [-F, -N, +O, -S] are spelled out as \textit{dies}{}- and -\textit{em}, respectively, yielding the form after the arrow.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Besides the demonstrative \textit{dieser} ‘this’, other phrasal \textit{der}{}-words have this general structure: (stressed) \textit{DER} ‘that’, \textit{jener} ‘that’, \textit{jeder} ‘every’, \textit{mancher} ‘some’, \textit{solcher} ‘such’, \textit{welcher} ‘which’, and \textit{alle} ‘all’ (see also Chapter 3, \sectref{sec:key:4}). While I cannot specify (and motivate) all the relevant features of these individual words, I assume that all these elements have the categorial feature [+D]. Furthermore, like \textit{dieser} ‘this’, the demonstratives \textit{DER} ‘that’ and \textit{jener} ‘that’ involve the features [+DEF, +DEIX]. The quantifiers \textit{jeder} ‘every’ and \textit{alle} ‘all’ and the (presuppositional) interrogative \textit{welcher} ‘which’ have the feature [+DEF] but lack the deixis feature. In contrast, \textit{mancher} ‘some’ and \textit{solcher} ‘such’ lack the definiteness feature but have the feature [+DEIX] in their feature makeup.\footnote{The presence of the deixis feature is most straightforward with \textit{solch} ‘such’. For simplicity, I also assume this for \textit{manch} ‘some’ (although this element may turn out to have a different positively valued (relevant) feature in its makeup). Notice that these positively valued features play a role later in the discussion (\sectref{sec:key:2.2.1}). On a different note, recall that \textit{mancher} ‘some’, \textit{solcher} ‘such’, and \textit{welcher} ‘which’ can also occur without an inflection (i.e., \textit{manch}, \textit{solch}, \textit{welch}). Given the lack of inflection, I assume that they have a different structure and analysis than in \REF{ex:key:27}. Observe in this regard that they cannot occur directly before nouns (in non-formal contexts) but rather require \textit{ein} and/or an adjective to be present: \textit{manch *(ein/guter) Student} ‘some (good) student’. Given this dependency, I assume that they are not determiners but modificational elements, which do not involve the categorial feature [+D].} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that all determiners have the categorial feature [+D]. I propose that this is the trigger for Impoverishment. In addition, some determiners have features for definiteness and/or deixis, which also play a role in the account below. The indefinite article \textit{ein} is different – it has no features for definiteness and deixis. Note already here that considering the features of the various determiner stems, \textit{ein} is the least specified element. In Chapter 5, I propose in more detail that \textit{ein} is a semantically vacuous element.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.7. The Proposal in More Detail: Impoverishment
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Impoverishment involves a rule deleting a specific feature. Note that in the current context, Impoverishment cannot be stated in terms of the features [F] or [N] as these features are still relevant for the weak ending -\textit{e} given the category variable γ in \REF{ex:key:23a}. Furthermore, it cannot be stated in terms of the feature [O] as that feature is also mentioned in \REF{ex:key:23a}. I propose that Impoverishment deletes the feature [S] if a determiner adjoins to a head or a phrase in the nominal structure:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%28
    \label{ex:key:28}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Impoverishment Rule 1:}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  [\textsubscript{δ} Determiner [\textsubscript{δ} [S] ]], where δ = X, XP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that this is a language-specific rule that applies to German only (for the discussion of Impoverishment in Yiddish, see \citealt{Roehrs2015}). Furthermore, recall the partial ordering of \{Lowering, Impoverishment\} >> Vocabulary Insertion from Chapter 1, \sectref{sec:key:4.2.1}. First, as observed by \citet{Sauerland1996}, Impoverishment widens the distribution of the least specified (i.e., weak) inflections. This is consistent with the ordering of Impoverishment preceding Vocabulary Insertion. Second, Impoverishment Rule 1 involves the structural relation of adjunction. Bearing in mind that Lowering also has access to structural relations and precedes Vocabulary Insertion, this also fits with the claim that Impoverishment precedes Vocabulary Insertion. In other words, current assumptions are in agreement with the general layout of DM provided earlier.\footnote{Notice that Impoverishment Rule 1 and Rule 2 (see below) bear some resemblance to Arregi \& \citeapo{Nevins2012} syntagmatic neutralization rules, Impoverishment rules that involve two distinct nodes where a rule affecting one node makes reference to the (external) morpho-syntactic environment of a second node. Note though that both nodes in their system are inside the same M-word (defined as a X\textsuperscript{0} that is not immediately dominated by another X\textsuperscript{0}; note that M-words are typically complex heads assembled by head movement). While the Impoverishment rules in the main text also apply in a local context, they do not occur inside M-words. If it turns out that the context of application of Impoverishment only involves M-words, then there are several options to update the proposal above; for instance, we could assume that all determiners and adjectives are heads and that determiners move by head adjunction and excorporation to D.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To illustrate the workings of Impoverishment Rule 1, I begin by discussing the determiners, the triggers of Impoverishment, and how they relate to the rest of the nominal structure. With \REF{ex:key:28} in place, the property [+I(mpoverishment)] from \sectref{sec:key:2.1.4} can now be restated. I propose that the categorial feature [+D] triggers Impoverishment. Recall that the definite article moves to adjoin to Agr. Leaving out the positive/negative values of the CNG features, the general constellation is as in \REF{ex:key:29}. Impoverishment Rule 1 deletes [S], marked by strikethrough, from Agr, the adjunction site. In fact, due to Percolation and Spec-head agreement, this feature is deleted from the feature bundles on all elements except the terminal head Art of the determiner itself. The relevant portion of the nominal structure is as follows (for the indefinite articles \textit{ein} and \textit{Ø\textsubscript{D}}, see Sections 2.2 and 2.3): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%29
    \label{ex:key:29}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Impoverishment by Definite Article}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            AgrP\textsubscript{[F, N, O, S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    InflP\textsubscript{[F, N, O, S]}    Agr’\textsubscript{[F, N, O, S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       [F, N, O, S]    AP  Agr\textsubscript{[F, N, O, S]}  … t\textsubscript{k}…
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         Art\textsubscript{[+D; +DEF][ F, N, O, S]k} Agr\textsubscript{[F, N, O, S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         [+D; +DEF]        [F, N, O, S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Operations in DM occur after syntax. Given the successive-cyclic movement of the determiner from ArtP to DP, there are several copies of this element in the nominal structure. Each of these copies triggers Impoverishment deleting the feature [S]. This explains why multiple adjectives in a noun phrase all undergo Impoverishment (provided there is a determiner). Note also that after Impoverishment and Copy Reduction, there is still (at least) one fully specified feature bundle in the structure – it is on the terminal head Art later to be spelled out as the appropriate form of the determiner. Surfacing in the DP-level, this element is accessible to DP-external operations (e.g., case checking/valuing).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The updated analysis of the demonstrative is similar, but this element undergoes adjunction to AgrP triggering Impoverishment (the demonstrative structure in \REF{ex:key:27} above is simplified here for reasons of space):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%30
    \label{ex:key:30}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Impoverishment by Demonstrative}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           AgrP\textsubscript{[F, N, O, S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Dem\textsubscript{[+D; +DEIX][F, N, O, S]k}        AgrP\textsubscript{[F, N, O, S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

[+D; +DEIX]  [F, N, O, S]  InflP\textsubscript{[F, N, O, S]}    Agr’\textsubscript{[F, N, O, S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      [F, N, O, S]    AP  Agr\textsubscript{[F, N, O, S]}  … t\textsubscript{k}…  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, I turn to the discussion of the inflection on the adjective, the target of Impoverishment.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  If Impoverishment applies, and the feature [S] is deleted in the nominal structure, then this impacts the inflection on the adjective with visible effect – InflP in \REF{ex:key:29} and \REF{ex:key:30} undergoes feature deletion. For instance, the removal of the feature [S] yields \REF{ex:key:31} in the dative masculine. With [S] absent, only [] in \REF{ex:key:23b} above is a match here spelling out the weak ending -\textit{en}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%31
    \label{ex:key:31}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

             InflP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       Infl’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       [-F, -N, +O]    AP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               A’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           A    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                  \textit{klein} →  \textit{klein}{}-\textit{en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I discuss the inflections on the determiners themselves in \sectref{sec:key:4}. Before moving on, note again that all strong endings in \REF{ex:key:22} have the feature [S], except for one: -\textit{en}. In other words, the accusative masculine ending is left untouched by Impoverishment and surfaces as the (so-called) exceptional case in strong and weak contexts alike. In the current account, this inflection is not exceptional: like the vocabulary entries in \REF{ex:key:23}, -\textit{en} in \REF{ex:key:22c} lacks the feature [S]; unlike the entries in \REF{ex:key:23}, -\textit{en} in \REF{ex:key:22c} is (simply) more specified as regards other features. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The above discussion explains the weak inflections in canonical constructions. Importantly, note that the trigger of Impoverishment, the categorial feature [+D], is on the stem of the determiner (rather than on the inflection). In other words, the current account of the strong/weak alternation is independent of the presence of adjectival inflections on determiners. With this in place, I turn to the discussion of strong adjectives in canonical structures. In the next section, I address adjectives in the context of \textit{ein}{}-words. In \sectref{sec:key:2.3}, I discuss adjectives following null articles and Saxon Genitives. \sectref{sec:key:2.4} is dedicated to the discussion of more complex canonical DPs – those involving extended-adjective constructions and numerals. It is shown that these cases also follow from the system developed above. In the course of the discussion, I refine the current proposal further.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{2.2.  Strong or Weak Adjectives in Canonical DPs:} Ein\textit{{}-words}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I provide my analysis of strong adjectives after certain \textit{ein}{}-words, and I discuss the inflectional behavior of the \textit{ein}{}-words themselves.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.1. Adjectives after \textit{ein}{}-words: Strong Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this subsection, I consider canonical DPs that involve adjectives preceded by \textit{ein}{}-words. Recall that \textit{ein}{}-words consist of the indefinite article \textit{ein} ‘a’, the singularity numeral \textit{EIN} ‘one’, the negative article \textit{kein} ‘no’, and possessive articles like \textit{mein} ‘my’, \textit{dein} ‘your’, etc. As is well known, these elements occur with strong adjectives in three instances: in the nominative masculine and in the nominative/accusative neuter. This is exemplified in the neuter with an indefinite article and a possessive article in \REF{ex:key:32a}. In all the other instances, the adjective must be weak as shown with the dative in \REF{ex:key:32b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%32
    \label{ex:key:32}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{(m-)ein groß-es Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    (my) a  big-\textsc{st}   car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a / my big car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{mit}   \textit{(m-)einem groß-en Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with (my) a       big-\textsc{wk} car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with a / my big car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This inflectional distribution is often referred to as mixed pattern. Considering \REF{ex:key:32}, it is clear that the presence of possessive \textit{m}{}- does not make a difference for the inflection on the adjective. Furthermore, given this pattern, I need to say something about the determiners that in some instances, do not trigger Impoverishment \REF{ex:key:32a} but in others, do \REF{ex:key:32b}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note first that it is not possible to propose that \REF{ex:key:32a} involves no concord in features, and consequently, there is a strong ending on the adjective. This is so as uninflected \textit{ein} is restricted to specific featural combinations, and the following adjective has a regular strong ending. In other words, the agreement features need to be present underlyingly so that the correct forms of the article and the adjectival inflection can be inserted.\footnote{Note in this regard that the generalization Weak After Strong makes crucial reference to the agreement morpheme of a determiner. In other words, determiners are not unanalyzed word forms that involve some abstract features independently of the overt inflection. As discussed above, I also assume that determiners consist of morphemes. However, I argue that the underlying features are crucial, not the overt inflection.} More generally, this means that concord is not a sufficient condition for the occurrence of weak endings (see also \sectref{sec:key:3}). In order to explain the distribution in \REF{ex:key:32}, I do not modify the general system laid out above but derive these patterns by a certain property of the relevant \textit{ein}{}-words themselves. I focus on \textit{ein} and later briefly comment on possessive articles, the negative article \textit{kein} ‘no’, and the singularity numeral \textit{EIN} ‘one’.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Some scholars claim that uninflected \textit{ein} is, in a sense, invariable: for some reason, it does not have an ending in the three above-mentioned cases (\citealt{Demske2001}: 33, \citealt{Eisenberg1999}: 233, \citealt{Olsen1991b}: 47 fn. 14). Consequently, the following adjective must be strong to spell out the relevant features for case, number, and gender. These authors seem to suggest that there are two types of lexical items: a determiner involving uninflected \textit{ein}, and a pronoun involving inflected \textit{einer} (more on this in the next subsection).\footnote{Note that it is not possible to simply claim that the adjectival inflection on \textit{ein} emerges to make case, number, and gender features visible. This is so because there are cases where the adjectival inflection is optional (e.g., \textit{ein lila(nes) Kleid} ‘a purple dress’) and instances where no CNG features are visible (e.g., \textit{zehn (Kleider)} ‘ten (dresses)’).} Other authors propose a timing mechanism to explain the three exceptional instances of \textit{ein}. For instance, Roehrs (2009a: chapter 4) suggests that certain cases of \textit{ein} move to the DP-level later in the derivation. As a consequence, they do not trigger Impoverishment on the adjective, and the ending on \textit{ein} itself is not licensed. In order to avoid such late syntactic movement, I make a different proposal here.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I break down the behavior of \textit{ein} into two issues: (i) \textit{ein} itself has no inflection, and (ii) the following adjective is strong (i.e., \textit{ein} does not trigger Impoverishment). At first glance, these points seem to be related (e.g., \citealt{Murphy2018} and references cited therein). Indeed, they have given rise to the traditional generalizations discussed in \sectref{sec:key:2.1.1}: Weak After Strong and the Principle of Monoinflection. Note though that we have already indicated that these generalizations do not cover all the facts (e.g., \textit{wir nett-en Studenten} ‘we nice-\textsc{wk} students’; for a more detailed discussion, see Chapter 3, \sectref{sec:key:7}). Second, generalizations are descriptions of the facts, not explanations.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Starting with the second issue (i.e., the strong inflection on the adjective after \textit{ein}), I proposed in the previous section that the categorial feature [+D] of the determiner is the trigger for Impoverishment. To explain the special properties of \textit{ein}, I refine this proposal by claiming that the cateroial feature [+D] of determiners may be a trigger for Impoverishment but only under certain (featural) conditions. In order to establish a natural group, I propose that four (not three) instances of \textit{ein} are special. Besides the three traditional cases (i.e., nominative masculine and nominative/accusative neuter), I add accusative masculine to the group.\footnote{There are two points that deserve mentioning here. First, grouping the accusative masculine of \textit{ein} with the (traditional) three exceptions means that -\textit{en} on the adjective is a strong (rather than weak) ending, an interpretation that is possible given that -\textit{en} is both a strong and weak ending in this feature combination. Indeed, I formulated a vocabulary insertion rule in \REF{ex:key:22c} above that captures this identity directly. Second, Paul \textit{et al.} (1989: 234) provide the following forms of \textit{ein} in the nominative/accusative singular in MHG: (i)    MASC  NEUT  FEM    NOM  ein  ein  ein      (MHG)    ACC  ein-en  ein  ein(e)It appears as if over time, the feminine form of \textit{ein} has taken on -\textit{e}, possibly to mark feminine gender more consistently (cf. \textit{dies-e Lamp-e} ‘this lamp’; for discussion involving markedness and syncretism formation, see \citealt{Bittner2006} and \citealt{Krifka2009}). This change in the feminine may have led to the establishment of a natural group containing the four other instances of \textit{ein} in (i), as suggested in the main text.} In the system laid out above, nominative/accusative masculine and nominative/accusative neuter are the least marked items in terms of the features [O] and [F]: both have a negative value in these four instances (recall from \sectref{sec:key:2.1.5} that I take negative values to represent less marked instances). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I propose that [+D] is only a trigger for Impoverishment if it occurs in the context of positively valued [O], [F], [DEF], or [DEIX]. Note now that the terminal heads that are later to be spelled out as the four relevant instances of \textit{ein} have [+D] but lack [DEF], [DEIX]. Furthermore, they have no positive value on [O] and [F]. Consequently, these cases of \textit{ein} will not trigger Impoverishment. This is illustrated with \textit{ein} in the nominative masculine:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%33
    \label{ex:key:33}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{No} \textit{Impoverishment by} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            AgrP\textsubscript{[-F, -N, -O, -S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    InflP\textsubscript{[-F, -N, -O, -S]}  Agr’\textsubscript{[-F, -N, -O, -S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      [-F, -N, -O, -S]    AP  Agr\textsubscript{[-F, -N, -O, -S]}  … t\textsubscript{k}…
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         Art\textsubscript{[+D][-F, -N, -O, -S]k} Agr\textsubscript{[-F, -N, -O, -S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      [+D]          [-F, -N, -O, -S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To be clear, like the other determiners, the four instances of \textit{ein} undergo movement up the syntactic representation by way of adjunction. Unlike the other determiners, they do not trigger Impoverishment given the feature specification under Art. In keeping with the tradition, I usually refer to the special cases as the three (not four) instances of \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.2. Adjectives After \textit{ein}{}-words: Uninflected \textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Turning to the first issue (i.e., \textit{ein} may have no inflection), note that the endings on \textit{ein} do appear in elliptical contexts when no noun or adjective is present. Compare (34a-b) to \REF{ex:key:34c}:\footnote{Observe that ellipsis is irrelevant for Impoverishment; that is, the ellipsis of a noun in a regular DP does not impact the strong/weak alternation on the adjective itself:(i)  \textit{der gut-e}    the good-\textsc{wk}    ‘the good one’Also, recall from Chapter 1, Footnote Error: Reference source not found that \textit{der}{}-words used pronominally are different from \textit{ein} in \REF{ex:key:34c} in that they have two inflections in certain feature combinations, the usual strong inflection and an additional -\textit{en} (e.g., \textit{d-en-en} ‘the-\textsc{st}{}-\textsc{infl} in the dative plural). This means that the additional strong inflection on \textit{ein} in \REF{ex:key:34c} has its own explanation.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%34
    \label{ex:key:34}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Das ist ein-(*er) Wagen}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this is  a-*\textsc{st}       car.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘This is a car.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Das ist ein-(*er) gut-er}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this is  a-*\textsc{st}       good-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘This is a good one.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Das ist ein-*(er)}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this is   one-\textsc{st}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘This is one.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With Panagiotidis (2002, 2003) and others, I assume that (34b-c) involve null nouns (cf. also \textit{pro} as in \citealt{Kester1996a},b; \citealt{Lobeck1995}; \citealt{Olsen1987}; \citealt{Rehn2019}: 209-12). This makes these two nominals structurally parallel to \REF{ex:key:34a}, which involves an overt noun.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Inflected \textit{einer} as in \REF{ex:key:34c} can be followed by overt elements provided these elements are outside the noun phrase that contains \textit{ein} and the (null) matrix noun. Such elements include genitive DPs, PPs, and relative clauses:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%35
    \label{ex:key:35}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein-er  mein-er  Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one-\textsc{st} my-\textsc{gen} friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one of my friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{ein-er  von mein-en Freunden}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one-\textsc{st} of   my-\textsc{dat} friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one of my friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{ein-er, den ich gesehen habe}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one-\textsc{st} that I    seen       have
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one that I have seen’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I summarize the complete set of \textit{ein}{}-words in \tabref{tab:key:5}, where the inflections in brackets indicate the forms occurring in the absence of the relevant overt material (e.g., adjectives and nouns in the matrix nominals). Given the restrictions on the occurrence of \textit{ein} in the plural (Chapter 1, \sectref{sec:key:2.2}), I illustrate this with the possessive article \textit{mein}{}- ‘my’:\footnote{The two neuter forms of \textit{meines} are often realized without a schwa as \textit{meins} (the same goes for the other \textit{ein}{}-word forms in these instances: \textit{eins} ‘a’, \textit{EINS} ‘one’, and \textit{keins} ‘no’).} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Endings on \textit{ein}{}-words}
\label{tab:key:5}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXX}
 & Masculine & Neuter & Feminine & Plural\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Nominative & mein-[er] & mein-[es] & mein-e & mein-e\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Accusative & mein-en & mein-[es] & mein-e & mein-e\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Dative & mein-em & mein-em & mein-er & mein-en\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Genitive & mein-es & mein-es & mein-er & mein-er\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Abstracting away from the brackets for a moment, the inflections in \tabref{tab:key:5} are the same as those on the \textit{der}{}-words discussed earlier. Having said that, recall that the inflection on \textit{ein} in the three special instances is a function of the presence or absence of following overt material such as an adjective and/or a noun. Given the large overlap of forms, it is, in my view, undesirable to state two types of vocabulary items, one involving \textit{ein} as a determiner and one with \textit{einer} as a pronominal form. Rather, since the presence of the inflection depends on overt material, I propose that the alternation between uninflected and inflected \textit{ein} is brought about by certain vocabulary insertion rules. These are late operations that occur after lexical elements such as adjectives and nouns have been inserted.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Inspired by Höhn’s (2020: 13-16) analysis of the third-person gap in pronominal DPs (e.g., *\textit{they linguists}), I provide a – what he calls – PF-analysis, an account of uninflected \textit{ein} that occurs after syntax. I propose that the different forms of \textit{ein} present another case of contextually conditioned allomorphy. Höhn states that the relevant overt material must be linearly adjacent and in the same spell-out domain. As to the latter, I assume that the DP as a whole involves an independent domain – a phase in Chomsky (2000 and subsequent work). Rather than formulating three separate, exceptional instances of \textit{ein} in the nominative masculine and nominative/accusative neuter, I use the feature system from \sectref{sec:key:2.1.5} and propose the following vocabulary insertion rules.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The insertion rules are provided in \REF{ex:key:36}: (36a-c) state the vocabulary entries for \textit{ein} and (36d-e) for the adjectival inflections on \textit{ein} (note that \REF{ex:key:36d} shows the entry for the nominative masculine inflection -\textit{er}, and \REF{ex:key:36e} stands for the remaining vocabulary entries of the endings from \sectref{sec:key:2.1.5}). As mentioned several times before, instances in the accusative masculine are exceptional in the nominal system in German. As such, I single out this feature combination as the most specific one \REF{ex:key:36a}. In order to make sure that this element is inserted in the right feature combination, the context of the application of the rule is specified by the relevant features after the forward slash sign. Note also that \REF{ex:key:36a} only spells out the stem feature – the CNG features are spelled out by the vocabulary insertion rules for the adjectival inflections. In contrast, \REF{ex:key:36b} spells out the stem feature and the CNG features by one element. This explains the lack of inflection on \textit{ein} in these cases. Furthermore, \REF{ex:key:36b} is sensitive to the presence of overt material, the following adjective and/or noun, at the right edge of the same phase. This adheres to Höhn’s condition of linear adjacency. Given the relevance of word order, this is consistent with the assumption from Chapter 1 that Vocabulary Insertion follows Linearization. Notice that taken together, \REF{ex:key:36a} and \REF{ex:key:36b} form the four special cases of \textit{ein}. Finally, the vocabulary entry in \REF{ex:key:36c} presents the elsewhere case for \textit{ein} (the closing square bracket followed by φ in \REF{ex:key:36b} indicates the right edge of the DP phase, more on this momentarily):\footnote{For the use of a forward slash sign to define the contextual condition of vocabulary items, see Embick \& \citet[299]{Noyer2007}. Also, the setup in (36a-c) is inspired by Blevins’ (1995: 146) treatment of weak adjectives in German where the form \textit{kleinen} ‘small’ is both the most and least specific, and \textit{kleine} is inbetween (also \citealt{Evans2019}: 56). Note that in an earlier version of this work, I formulated \REF{ex:key:36a} as [+D][-F, -N, -O, +S] → \textit{einen}, where the latter spells out both the categorial feature and the CNG features by one element. Observe though that \textit{einen} would now be an unanalyzed form; that is, the ending -\textit{en} would no longer be a regular adjectival inflection. It is worth pointing out that the specification of a feature context after the forward slash sign as in \REF{ex:key:36a} will find wider application with inflected third-person pronouns (see next subsection), inflected definite articles (see Chapter 3, \sectref{sec:key:3}), and first and second-person pronouns, which do not have CNG features as part of their structure (Chapter 3, \sectref{sec:key:5}). Finally, note again that \REF{ex:key:36b} involves two separate feature bundles spelled out by one morpheme. This has to do with the earlier observation that articles, despite being heads, have internal structure consisting of stems and inflections. In some cases, these individual bundles are spelled out by one morpheme \REF{ex:key:36b}; in others, they are realized by two morphemes where (36a,c) and (36d-e) are later combined by Local Dislocation (for sample derivations, see the main text below). That two feature bundles are spelled out by one morpheme is independently needed for (uninflected) third-person pronouns in the genitive (see next subsection). More generally, the different types of insertion rules in \REF{ex:key:36} will be used for other cases as well.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%36
    \label{ex:key:36}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [+D]      →   \textit{ein-} / [-F, -N, -O, +S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [+D][-F, -O]    →   \textit{ein} / \_\_ word ]\textsubscript{φ}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  [+D]      →   \textit{ein}{}-
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

d.  [+F, -N, +O, αS]  →  \textit{{}-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  e.  etc.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Some more comments are in order.  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I assume that a rule with specifications for both the categorial feature [+D] and the CNG features is more specific than a rule with either the categorial feature [+D] or the CNG features alone; that is, (36a-b) are more specific than (36c-e). Note again that \REF{ex:key:36a} is more specific in its feature specification than \REF{ex:key:36b}: [-F, -N, -O, +S] vs. [-F, -O]. This means that \REF{ex:key:36a} applies first, provided its feature context matches that of the noun phrase. In conjunction with \REF{ex:key:36e}, this brings about the inflected form \textit{einen}. If \REF{ex:key:36a} does not apply, \REF{ex:key:36b} may, again provided its features and context match the syntactic representation. Specifically, \REF{ex:key:36b} spells out uninflected \textit{ein} in the context of a following overt word inside the phase φ. However, if both \REF{ex:key:36a} and \REF{ex:key:36b} do not apply, \REF{ex:key:36c} does. In conjunction with \REF{ex:key:36d} or \REF{ex:key:36e}, this brings about the remaining inflected forms of \textit{ein} including the ones in elliptical contexts. Note that the latter cases also concern the three exceptional instances where \textit{ein} surfaces with a strong inflection in elliptical contexts. Finally, observe that the insertion rules in (36a-c) involve three related vocabulary items and that there is no inherent difference as regards determiner vs. pronominal forms. Next, I provide more details as regards the morpho-syntax involved.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  After movement of \textit{ein} to the DP-level (provided the nominal is an argument), the simplified structure of the noun phrase can be represented as below:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%37
    \label{ex:key:37}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

                  DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      D                ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             Art\textsubscript{k}    D              Art’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          [+D]       [F, N, O, S]         t\textsubscript{k}          NumP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                         Num’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                      Num    NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                      t\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                      N\textsubscript{i}          Num    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Like \citet{Höhn2020}, I assume that lexical elements like adjective and nouns are inserted before functional elements like determiners and inflections. This is consistent with Embick \& Noyer’s (2007: 295) assumption that lexical elements are merged as terminal nodes that involve phonological features.\footnote{This early presence of lexical elements in the derivation is presumably needed independently as nouns in German have inherent grammatical gender. Mediated by a syntactic mechanism that brings about concord in agreement features, this gender determines the form of the inflected determiner (e.g., \textit{der}\textsubscript{MASC} vs. \textit{das}\textsubscript{NEUT} vs. \textit{die}\textsubscript{FEM}).} Now, after Linearization, functional elements and lexical elements like, for instance, \textit{Wagen} ‘car’ or a null noun, yield strings such as in \REF{ex:key:38} below. To finalize the derivation, I highlight the interaction of the rules in \REF{ex:key:36} with the presence of overt material, specifically, the noun. I use examples in the nominative and dative masculine for illustration (I comment on \REF{ex:key:36a}, which involves accusative masculine, at the end).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

First, when the features later to be spelled out as \textit{ein} are in the nominative masculine and appear in the context of an overt noun, insertion rule \REF{ex:key:36b} applies yielding the spell-out form after the arrow in \REF{ex:key:38a}. Specifically, \REF{ex:key:36b} spells out the adjacent feature bundles for category and case, number, and gender by one element. Second, when the same features occur with a null noun, insertion rules \REF{ex:key:36c} and \REF{ex:key:36d} apply bringing about the spell-out in \REF{ex:key:38b}. In contrast to the first instance, here both feature bundles of the article are realized separately, and the inflection undergoes Local Dislocation onto the determiner stem. Third, similar assumptions hold for the dative masculine. Here, \REF{ex:key:36c} and \REF{ex:key:36e} apply. The presence of a relevant overt element makes no difference in this feature combination \REF{ex:key:38c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%38
    \label{ex:key:38}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [+D]      [-F, -N, -O, -S]  \textit{Wagen}    →   \textit{ein Wagen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [+D]      [-F, -N, -O, -S]      e\textsubscript{N}    →   \textit{einer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [+D]      [-F, -N, +O, -S]     \textit{Wagen}/e\textsubscript{N}  →   \textit{einem} (\textit{Wagen})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Briefly commenting on \REF{ex:key:36a}, it works as in \REF{ex:key:38c}, only in a different featural context.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To be clear, the vocabulary insertion rules only supply the phonetic form of \textit{ein} and that of the inflection – the abstract features have been present during the entire syntactic derivation. Furthermore, unlike adjectives and nouns as in \REF{ex:key:36b}, elements outside the DP (genitive DPs, PPs, or relative clauses) are not part of the DP phase, delinated above by φ, and do not have an impact on the calculation of the right edge. This means that \REF{ex:key:36b} does not apply. Hence, genitive DPs, PPs, and relative clauses occur with inflected \textit{ein} if an adjective and/or noun is absent in the matrix DP. Before turning to the other \textit{ein}{}-words, I briefly discuss two extensions of the insertion rules in (36a-b).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.3. Adjectives after \textit{ein}{}-words: Some Extensions
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

First, note that these insertion rules may also be relevant to another, very recent development in German. \citet{Vogel2006} observes that the reduced form of the indefinite article \textit{‘n} is replaced in the nominative masculine and nominative/accusative neuter – the three exceptional cass – by \textit{nen}. This is particularly clear in chat contexts (notice that the example below makes it clear that \textit{nen} is not an accusative form):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%39
    \label{ex:key:39}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{das  waere     nen gut-er    preis}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  that would.be a    good-\textsc{st} price.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘That would be a good price.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Vogel proposes that speakers might take \textit{‘n} to be too short filling it with more phonetic material. With an added initial \textit{n} and schwa, this makes \textit{nen} more similar to other reduced forms of the article (e.g., accusative masculine \textit{nen}).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I interpret \textit{nen} as the reduced form of \textit{einen} (see Chapter 5). With this in mind, notice that this form \textit{nen} occurs in the featural context of \REF{ex:key:36b}, that is, in the masculine/neuter in the nominative and accusative cases. Thus, as an alternative to Vogel’s explanation, we could suggest that the featural context of the insertion rule \REF{ex:key:36a} has become less specific yielding the context in \REF{ex:key:36b}: [+D] → \textit{(ei)n-} / [-F, -O]. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Vogel also states that unreduced \textit{einen} is sometimes replaced by reduced \textit{ein} (rather than \textit{nen}) in the accusative masculine. This makes the accusative identical to the nominative here. This holds for Mannheim German, more generally. I argue in detail in Chapter 3, \sectref{sec:key:8} that in this dialect, \REF{ex:key:36a} has been deleted. What seems to be clear then is that the featural contexts of (36a-b) – masculine/neuter in the nominative/accusative – are special. I take this as confirmation that these four instances of \textit{ein} form a natural group, in Standard German and in other varieties. We see in the next paragraphs that the forms of third person pronouns are also delineated by this feature combination.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The second extension of the analysis of \textit{ein} involves pronominal determiners (for fuller discussion, see Chapter 3, \sectref{sec:key:5}). As is well known, third-person personal pronouns have the same inflections as definite determiners. Consider \tabref{tab:key:6}. As mentioned in \sectref{sec:key:2.1.5} above, there is syncretism in the nominative and accusative such that masculine and neuter pattern together, and feminine and plural do too: \textit{e-/ih}{}- ‘he, him; it’ vs. \textit{sie} ‘she, her; they, them’. Note that like with determiners (and adjectives), the accusative masculine form (\textit{ihn}) is exceptional. Also, all dative forms start in \textit{ih}{}-, and there is an additional ending (-\textit{en}) on the dative plural pronoun \textit{ihnen} (which I abstract away from here; for the historical development of this -\textit{en}, see \citealt{Lühr1991}). Finally, notice that unlike the other forms, the genitive pronouns do not have regular inflections:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Third-Person Personal Pronouns in German}
\label{tab:key:6}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Gen/num & Masc & Neut & Fem & PL\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Nom & e-r & e-s & sie & sie\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Acc & ih-n & e-s & sie & sie\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Dat & ih-m & ih-m & ih-r & ih-n-en\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Gen & seiner & seiner & ihrer & ihrer\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There are some interesting parallels to the distribution of \textit{ein}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that the three exceptional cases of \textit{ein} involve nominative masculine and nominative/accusative neuter, where \textit{ein} does not have an inflection if an adjective and/or noun follows. The corresponding third-person personal pronouns are also special: these three cases are the only ones that start in \textit{e}{}-, and as is well known, none of the third-person personal pronouns can be followed by an adjective and/or noun (e.g., \textit{er} \textit{(*Idiot)} ‘he (*idiot)’; \citealt{Höhn2020}). In other words, both uninflected \textit{ein} and the stem \textit{e}{}- occur in exactly the same contexts, and both are sensitive to the overt material that follows them (although in opposite ways).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   Similar to the vocabulary insertion rules of \textit{ein}, I propose that the exceptional accusative masculine form \textit{ihn} ‘him’ is singled out as the most specific element \REF{ex:key:40a}.\footnote{In Chapter 3, \sectref{sec:key:5}, I propose in detail that personal pronouns do not involve features for definiteness and deixis but rather for author (AUTH) and participant (PART) (for a full list of the vocabulary insertion rules for personal pronouns, see Chapter 3, \sectref{sec:key:5.3.2}).} Note that it only spells out the stem form (but not the CNG features; see also Gunkel \textit{et al}. 2017: 1297-98). In order to make sure that this form (and others like it) is inserted in the right feature combinations, an indication to that effect is provided after the forward slash sign. The genitive pronouns are the second most specified elements (40b-c). Unlike \textit{ihn} ‘him’ (and the other forms), they spell out the stem features and the CNG features by one element. This explains their irregular endings. The pronouns starting in \textit{e}{}- are the third most specified forms \REF{ex:key:40d}. Note now that [-F, -O] is the same feature combination as that of uninflected \textit{ein}. As to the remaining rules, \REF{ex:key:40e} provides the insertion rule for the feminine/plural form in the nominative/accusative, and \REF{ex:key:40f} involves the elsewhere case. Observe that all vocabulary insertion rules specify that there cannot be an overt word in the right edge of the DP adopting an idea from \citet[16]{Höhn2020}. Finally, \REF{ex:key:40g} and \REF{ex:key:40h} specify the vocabulary insertion rules for the adjectival inflections (see \REF{ex:key:22} and \REF{ex:key:23} above; recall that the schwa in the insertion rules in (40g-h) is due to epenthesis, not applying here):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%40
    \label{ex:key:40}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [+D; -AUTH, -PART]     →   \textit{ih-}\textsubscript{}         / \_\_]\textsubscript{φ} [-F, -N, -O, +S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  [+D; -AUTH, -PART]  [ -F, +O, +S]  →   \textit{seiner  \textsubscript{} } / \_\_]\textsubscript{φ} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  [+D; -AUTH, -PART][+O, +S]  →   \textit{ihrer  \textsubscript{} } / \_\_]\textsubscript{φ} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

d.  [+D; -AUTH, -PART]      →   \textit{e-  \textsubscript{} } / \_\_]\textsubscript{φ} [ -F,  {}-O]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

e.  [+D; -AUTH, -PART]      →   \textit{sie-}        / \_\_]\textsubscript{φ} [-O]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

f.  [+D; -AUTH, -PART]      →   \textit{ih-}\textsubscript{}  / \_\_]\textsubscript{φ}   
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

g.  [+F, -N, +O, αS]       →  \textit{{}-r}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  h.  etc.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that the extension of the discussion of \textit{ein} to third-person pronouns highlights several points. First, one morpheme can spell out two feature bundles at the same time (40b-c).\footnote{For another advantage of \textit{ein} spelling out two feature bundles, see Chapter 8, \sectref{sec:key:2.1}, where I briefly discuss \citegen{Rehn2019} analysis of adjectival inflections in Alemannic German.} Second, the exceptional accusative masculine form is part of the four special cases (masculine/neuter in the nominative/accusative). In order to capture this, the set-up of the vocabulary insertion rules in \REF{ex:key:36} and \REF{ex:key:40} is basically the same: \REF{ex:key:40a} corresponds to \REF{ex:key:36a}, \REF{ex:key:40d} relates to \REF{ex:key:36b}, and \REF{ex:key:40f} is the elsewhere case just like \REF{ex:key:36c}. Finally, I comment on the other types of \textit{ein}{}-words.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that all \textit{ein}{}-words behave the same as regards their inflection (also Chapter 5). It would be undesirable though to formulate the same type of vocabulary insertion rules in (36a-c) for possessive articles, the negative article, and the singularity numeral, respectively. Rather, the composite analysis of these \textit{ein}{}-words (consisting of \textit{ein} and another component) allows us to use the vocabulary items in (36a-c) here as well. While I provide a more detailed discussion in Chapter 5, I point out briefly here that possessive elements undergo some late interaction with \textit{ein} (i.e., after the insertion of \textit{ein}). Note in this regard that possessive articles fall into two groups: elements that are phonetically similar to \textit{ein}, and those that are not. Decomposing \textit{mein} ‘my’, \textit{dein} ‘your(\textsc{sgl})’, and \textit{sein} ‘his’ into \textit{ein} and possessive components, the latter parts surface as in \REF{ex:key:41a}. The second type of the possessive articles involves free forms as in \REF{ex:key:41b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%41
    \label{ex:key:41}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{m}{}-,  \textit{d}{}-,              \textit{s}{}-
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my, your(\textsc{sgl}), his
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{ihr},                                  \textit{unser}, \textit{euer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    her/their/your(\textsc{formal}), our,     your(\textsc{pl})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall also that \textit{ein} is proposed to be a semantically vacuous element. After Vocabulary Insertion, the elements in \REF{ex:key:41a} combine with \textit{ein}; that is, the possessive components are supported by \textit{ein}. The same holds for the negative article \textit{kein} ‘(NEG+a =) no’ and the singularity numeral \textit{EIN} ‘(\textit{Ø}\textsubscript{[-}\textsc{\textsubscript{pl}}\textsubscript{]}+a =) one’. As for \REF{ex:key:41b}, I assume that these forms suppress the pronunciation of \textit{ein} (for more details, see Chapter 5). In the next section, I turn to another set of cases where strong or weak adjectives are possible. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{2.3.   Strong or Weak Adjectives in Canonical DPs: Null Articles and Saxon Genitives}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I briefly argued in Chapter 1, \sectref{sec:key:4.1.2} that prenominal possessives are in Spec,DP. This means that possessives can structurally occur with articles. In the previous section, this is what I tacitly assumed for possessive articles where the possessive component (e.g., \textit{m}{}-) is in Spec,DP and \textit{ein} is in D yielding \textit{mein} ‘my’ (more on this in Chapter 5). Similarly, I suggested in Chapter 1 that Saxon Genitives are also in Spec,DP. Unlike possessive articles, they do not occur with an overt determiner, and I assumed that Saxon Genitives are followed by a null article. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There are several advantages of this idea: on the one hand, all possessives occur with articles; on the other hand, we can capture the fact that adjectives that occur after Saxon Genitives behave in the same way as adjectives that are not preceded by an (overt) determiner or determiner-like element. This is illustrated with the nominative in \REF{ex:key:42a} and with the dative in \REF{ex:key:42b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%42
    \label{ex:key:42}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{(Marias) kalt-es  Bier}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsubscript{} Mary’s   cold-\textsc{st} beer.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘(Mary’s) cold beer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mit}  \textit{(Marias) kalt-em Bier}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with Mary’s   cold-\textsc{st} beer.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with (Mary’s) cold beer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

On my assumptions then, both \REF{ex:key:42a} and \REF{ex:key:42b} involve null articles. We can observe that like in the previous section, the presence of the possessive does not have an impact on the inflection of the adjective. However, unlike above, the adjective here is also strong in the dative. As such, Saxon Genitives are in stark contrast to possessive articles such as \textit{mein} ‘my’ (and the other \textit{ein}{}-words). Before proceeding, note also that \textit{wessen} ‘whose’ and \textit{dessen} ‘his’ behave like Saxon Genitives as seen in the dative:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%43
    \label{ex:key:43}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{mit}  \textit{(wessen) kalt-em Bier}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with whose    cold-\textsc{st} beer.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with (whose) cold beer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mit  (dessen) kalt-em Bier}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with his        cold-\textsc{st} beer.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with (his) cold beer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This means that \textit{wessen} ‘whose’ is morpho-syntactically related to Saxon Genitives and \textit{dessen} ‘his’ (but not to possessive articles). Note that this might be relatable to the proposal that unlike possessive articles, Saxon Genitives and \textit{dessen} do not involve composite forms (which consist of a possessive component and the article \textit{ein}). Be that as it may, given the different inflections on the following adjectives, Saxon Genitives and possessive articles must have a different analysis. Interestingly, the inflectional behavior of adjectives in the genitive is revealing here.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As mentioned in \sectref{sec:key:2.1.2}, (overtly) unpreceded adjectives have strong inflections, with the exception of two instances: in genitive masculine/neuter contexts, these adjectives are weak. Note that the noun has an inflection in these very contexts. Compare \REF{ex:key:44a} to \REF{ex:key:44b}. Once again, the presence of a Saxon Genitive does not make a difference:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%44
    \label{ex:key:44}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  *  \textit{statt       \textsubscript{} } \textit{\textsubscript{} }\textit{kalt-es  Bier-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    instead.of  cold-\textsc{st} bier.\textsc{neut}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘instead of cold beer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 b.  \textit{statt       \textsubscript{} } \textit{(Marias) kalt-en   Bier-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    instead.of Mary’s  cold-\textsc{wk} bier.\textsc{neut}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘instead of Mary’s cold beer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that the inflections on the determiners themselves do not undergo Impoverishment (i.e., they are strong). This means that the ending -\textit{es} on, for instance, the determiners \textit{des} ‘the’ or \textit{eines} ‘a’ in the genitive masculine/neuter, is directly related to the underlying features. Now, if the endings on the determiners and those on the adjectives are indeed the same, then -\textit{en} on the adjectives in the genitive masculine/neuter must be the result of Impoverishment.\footnote{Historically, adjectives in this context had the strong ending -\textit{es}. According to \citet[84]{Demske2001}, the weak ending -\textit{en} started to spread in the 15\textsuperscript{th} century, and according to \citet[29]{Sahel2021}, this change was basically completed during the 18\textsuperscript{th} century.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  On the current analysis, a weak adjective in these contexts could imply the presence of a determiner triggering Impoverishment. One could suggest that this determiner is an(other) null article \textit{Ø\textsubscript{D}} with the feature specification [+D][-F, +O, +S] yielding \textit{Ø-es}. Following \citet{Roehrs2009a}, another timing mechanism could be suggested. It could be claimed that the null article in the genitive masculine/neuter moves to the DP-level in the regular fashion triggering Impoverishment (the remaining instances of the null article would move later in the derivation). Since null articles cannot support overt suffixes, a repair mechanism could be formulated. It could be suggested that the lower copy of the null article combines with the head noun by partial N-raising, and the higher copy of the null article is deleted under Recoverability of Deletion: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%45
    \label{ex:key:45}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          [\textsubscript{DP} \textit{Ø}{}-\textit{es} [\textsubscript{AgrP} \textit{kalten} [\textsubscript{ArtP} \textit{Bier}\textsubscript{k}\textit{{}-Ø}{}-\textit{es} [\textsubscript{NP} t\textsubscript{k} ]]]  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Considering that nouns are usually not inflected for case in German, this derivation would explain the weak ending of the adjective and the presence of a suffix on the noun. However, I would like to avoid a timing mechanism involving late syntactic movement (cf. the discussion of \textit{ein} in the previous section). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that null articles cannot be triggers of Impoverishment in the current analysis. Like the other determiners, null articles have the categorial feature [+D] and, additionally, they have the feature [-DEF]. Unlike the other determiners, they do not have feature bundles for CNG. While the absence of CNG features avoids the issue of stranded affixes – after all, null elements cannot provide overt hosts for affixes, note that there are also no positively valued features for definiteness or deixis on the null article that make [+D] a trigger for Impoverishment. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I propose that Impoverishment in the current cases occurs independently of the presence of determiners. I formulate a second Impoverishment rule that has to do with the features on the terminal head Agr and its projections; that is, Impoverishment operates in a certain featural context. This rule says: When InflP is a daughter of AgrP, delete [S] on InflP in genitive masculine/neuter contexts:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%46
    \label{ex:key:46}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Impoverishment Rule 2:}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            InflP    Agr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       [-F, αN, +O, +S]       [-F, αN, +O, +S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Notice that the feature specification in \REF{ex:key:46} excludes [+D]; that is, this rule applies independently of the presence of a determiner. In fact, it is the specific featural contexts and the structural relation of motherhood between AgrP and InflP that trigger the rule. Observe also that the same feature (i.e., [S]) is deleted here as in Impoverishment Rule 1.\footnote{In Chapter 3, \sectref{sec:key:4}, we see that this rule is more general applying to elements not only in AgrP but also to elements in other phrases. Note also that this rule has the looks of a dissimilation operation where one of two identical features (here [S]) is deleted (cf. Arregi \& Nevins’ 2012: 213 Participant Dissimilation). Interestingly, Impoverishment Rule 1 also deletes [S]. Assuming that the two Impoverishment rules are on the right track, it is currently not clear to me why [S] has this special status in German.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To illustrate the application of this rule, note first that the null article moves to adjoin to Agr but does not trigger Impoverishment. Rather, the features on Agr percolate to AgrP and induce Impoverishment on InflP by the rule in \REF{ex:key:46}. This reduced feature set on InflP percolates to Infl:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%47
    \label{ex:key:47}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

                    AgrP\textsubscript{[-F, αN, +O, +S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    InflP\textsubscript{[-F, αN,+O, +S]}  Agr’\textsubscript{[-F, αN, +O, +S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  [-F, αN, +O, +S]    AP  Agr\textsubscript{[-F, αN, +O, +S]} … t\textsubscript{k}…
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Art\textsubscript{k}    Agr\textsubscript{[-F, αN, +O, +S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                  [+D; -DEF]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There are only three vocabulary items in \REF{ex:key:22} and \REF{ex:key:23} that do not involve [S]: two have a negative specification for [O], and one is the elsewhere case in \REF{ex:key:23b}. The latter is the only vocabulary item that matches [-F, αN, +O] in Infl yielding the desired ending -\textit{en} on the adjective. Some other remarks are in order.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Null articles are in complementary distribution with other determiners. If a null article is present, then there is no overt determiner. Consequently, there is no (additional) Impoverishment (triggered by another determiner). Conversely, if an overt determiner is present, then there is no null article. However, in this scenario, there is an overlap in the application of the two Impoverishment rules. Given that feature deletion occurs postsyntactically, if Rule 1 applies first, Rule 2 does not (as [S] has already been removed in the entire structure); if Rule 2 applies first, Rule 1 still applies to the structural elements in AgrP (without visible effect), but not to the adjective in Spec,AgrP (where the feature [S] has already been removed by Rule 2). In either scenario, only the least specified element -\textit{en} can be inserted under Infl.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Impoverishment Rule 2 is stated independently of the suffix on the noun. Two more remarks are in order here. First, there are nouns that do not take the genitive suffix -\textit{es}; for instance, \textit{des Faschismus(*-es)} ‘of fascism’ or \textit{des Jazz(*es)} ‘of Jazz’ (see also \citealt{Olsen1991b}: 41). Furthermore, there is another type of genitive suffix on singular nouns: -\textit{en}. It is sometimes claimed (e.g., \citealt{Rehn2019}: 116) that weak adjectival inflections also occur on nouns, specifically on so-called weak masculine nouns. Note that -\textit{en} occurs in all instances on these nouns except in the nominative singular – just like with weak adjectives in the masculine singular. However, there are some important differences. While the endings on these nouns are obligatory in the genitive singular, they are optional in the accusative/dative singular, at least for some speakers (note that \citealt{Eisenberg1998}: 153 claims that this optionality holds more generally; \citealt{Gallmann1996}: 289, 1998: 143 points out some interesting restrictions on this optionality).\footnote{\citet{Krischke2012} notes that besides -\textit{en}, the endings -\textit{s}, -\textit{ens}, and quite rarely no ending can appear on weak masculine nouns in the genitive in non-standard contexts. He concludes that these forms are not established rival variants yet and are far from becoming part of the standard language (his page 66). Rather, they are due to analogy formations with similar forms in local contexts (\textit{locale Analogien}, his page 72). The relation of these genitives to accusative and dative forms is only very briefly discussed.} In contrast, weak endings on adjectives are not optional at all (and there is no variation). I conclude that these two cases should not be collapsed (although they are diachronically related, Chapter 1, \sectref{sec:key:3.1.1}). In other words, adjectival endings appear on determiners and adjectives (but not on nouns). Note that the Impoverishment rule in \REF{ex:key:46} is independent of these types of considerations.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As to the second remark, note that although the genitive suffix -\textit{es} does not play a role in the current account of the strong/weak alternation, this does not mean that this suffix is synchronically not relevant. The so-called Genitive Rule states that a noun phrase in the genitive must contain at least one element that is sufficiently specific as regards case (i.e., it must contain -\textit{es} in the masculine/neuter or -\textit{er} in the feminine/plural). In conjunction with the analysis above, this explains the ungrammaticality of the following data where both weak and strong adjectives are ungrammatical with nouns that have -\textit{en} as their genitive suffix (48a-b) (data from Gunkel \textit{et al}. 2017: 1308). Indeed, this also accounts for the ungrammaticality of nouns that do not have a genitive ending at all (48c-d): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%48
    \label{ex:key:48}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

             a.   *   \textit{der Geruch gebraten-en Ochse-n}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the smell     fried-\textsc{wk}      ox.meat.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the smell of roast ox’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.   *   \textit{der Geruch gebraten-es Ochse-n}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the smell     fried-\textsc{st}       ox.meat.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the smell of roast ox’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. ?(?)  \textit{eine Stunde toll-en     Jazz}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    an    hour    great-\textsc{wk} jazz
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘an hour of great jazz’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.   *  \textit{eine Stunde toll-es    Jazz}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    an    hour    great-\textsc{st} jazz
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘an hour of great jazz’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Specifically, the weak adjectives in (48a,c) are ungrammatical as the Genitive Rule is violated (for more discussion of the Genitive Rule, see Chapter 3, \sectref{sec:key:4});\footnote{Note that \REF{ex:key:48c} has a fairly good status. This presumably has to do with the fact that the second nominal in pseudo-partitive constructions can, for many speakers, be in all four morphological cases in German \citep{Löbel1989}. Note in this regard that the second nominal in \REF{ex:key:48c} is morphologically ambiguous between genitive and accusative.}  the strong adjectives in (48b,d) are ungrammatical as Impoverishment Rule 2 should have applied in this context.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Before moving on, note that there are some special, rare cases where adjectives appear to have a strong inflection in genitive masculine/neuter contexts ((49a) is from Gunkel \textit{et al}. 2017: 1308, \REF{ex:key:49b} is from \citealt{Durrell2002}: 126):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%49
    \label{ex:key:49}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{der Inhalt   folgend-es     Paragraph-en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the content following-\textsc{st} paragraph.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the content of the following paragraph’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{das Gesuch obig-es    Adressant-en} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the  request above-\textsc{st} sender.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the request of the above sender’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is sometimes claimed that the weak ending -\textit{en} on the noun licenses the strong ending -\textit{es} on the adjective \citep[126]{Durrell2002}. However, there are several points worth making here. Gunkel \textit{et al}. (2017: 1307) observe that such cases often involve adjectives that are similar to determiners (in their words “Pronomina”). Notice now that both head nouns in \REF{ex:key:49} are singular count nouns that seem to occur without articles. Interestingly, both \textit{folgend-} ‘following’ and \textit{obig-} ‘above(-mentionted)’ are definite in interpretation. As such, these elements might be special types of adjectives, sometimes called definite adjectives or adjectival determiners (see \citealt{Roehrs2009a}: 167-68, van de \citealt{Velde2011}; also Chapter 5, \sectref{sec:key:5.2.4}). Additionally, Gunkel \textit{et al}. (2017: 1308) point out that cases like \REF{ex:key:49b} often sound archaic. This might imply that such instances are not regulated by the contemporary grammar. If these remarks are on the right track, then \REF{ex:key:49} does not fall under the purview of Impoverishment Rule 2.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To summarize the last two sections, I have discussed three (or rather four) instances where adjectives preceded by \textit{ein} have a strong ending (the remaining instances are weak) and two instances where adjectives without (overt) determiners are weak (the remaining instances are strong). The first set of cases was explained by exempting four instances of \textit{ein} from triggering Impoverishment; the second was accounted for by formulating another Impoverishment rule triggered by a certain featural context. Furthermore, I have documented that the presence of possessives, be they determiner-like elements like \textit{m(ein)} or Saxon Genitives like \textit{Marias}, does not have an impact on the inflection of a following adjective.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Overall, I conclude that adjectival inflections are regulated by \textit{der}{}-words, \textit{ein}, and a certain featural context. If so, then I also have an account of why the possessives themselves cannot have an influence on the adjectival inflections – possessive elements such as \textit{m(ein)} ‘my’ and \textit{Marias} ‘Mary’s’ are not determiners themselves, but they co-occur with such elements, \textit{ein} in the case of \textit{m}{}- and \textit{Ø\textsubscript{D}} in the case of \textit{Marias}. As stated in Chapter 1, possessives are determiner-like elements: they are like definite determiners in that they occur before adjectives and bring about definiteness; they are unlike determiners in that some of them (i.e., Saxon Genitives) may also occur after the head noun (e.g., \textit{die Werke Goethes} ‘the works of Goethe’). This explains why possessives in German do not regulate adjectival inflections (neither in prenominal nor postnominal position). In the next section, I discuss more complex canonical DPs.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.4.   Adjectives in Extended-Adjective Constructions and After Numerals
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If Impoverishment occurs in a local domain, then the question arises how adjectival inflections that appear to be deeply embedded can undergo this type of feature deletion. Consider the extended-adjective constructions in \REF{ex:key:50}, where an adjective takes an argument. Interestingly, this argument is separated from the adjective by the degree word \textit{sehr} ‘very’. Despite the presence of these elements, the adjective alternates between exhibiting a strong or a weak ending: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%50
    \label{ex:key:50}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{ein} [ \textit{auf seinen Sohn sehr stolz-er} ] \textit{Vater}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    an    \textsubscript{} of  \textsubscript{} his       son   very proud-\textsc{st} father.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a father very proud of his son’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 b.  \textit{der} [ \textit{auf seinen Sohn sehr stolz-e} ] \textit{Vater}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the    of  his       son   very proud-\textsc{wk} father.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the father very proud of his son’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As discussed in Chapter 1, \sectref{sec:key:4.1.3}, I follow, among many others, Corver (1991, 1997) in assuming that adjectives involve an extended projection, similar to verbs and nouns (\citealt{Grimshaw1991}, van \citealt{Riemsdijk1998b}). In particular, I assume that the extended projection of the adjective includes a Degree Phrase (DegP). Furthermore, assuming that theta-role assignment occurs in a local fashion (i.e., within AP), the intervening degree word implies that the argument must have moved to the left, stranding the adjective. This in turn implies that the extended projection of the adjective has more structure on top of DegP. Given that the adjective appears to be so deeply embedded in the structure, it is important to make explicit how the analysis above deals with the strong/weak alternation in this type of data.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I proposed in Chapter 1, \sectref{sec:key:4.1.3} that adjectives and their inflections are base-generated in separate positions (e.g., \citealt{Corver2006}: 68, \citealt{Leu2015}): while the adjective stem forms the bottom part of the extended projection, the inflection closes this structure off. Moving top-down, I reiterate the claim here that the inflection projects InflP. The head Infl involves the abstract feature bundle eventually spelled out as a strong or weak ending. Furthermore, I assume that there is a functional phrase (FP), which can form the landing site for the argument of the adjective. Finally, DegP is on top of AP. The basic structure is provided in \REF{ex:key:51a}. Now, with the PP-argument moved to Spec,FP, I propose that FP moves to Spec,InflP. This is illustrated for the part enclosed in square brackets in \REF{ex:key:50b} in a simplified fashion as in \REF{ex:key:51b}: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%51
    \label{ex:key:51}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  [\textsubscript{InflP} Infl [\textsubscript{FP} F [\textsubscript{DegP} Deg [\textsubscript{AP} Adj ]]]] 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.            InflP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      FP\textsubscript{k}        Infl’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    PP       F’     {}-\textit{e}     ... t\textsubscript{k} ...
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     [\textit{auf seinen Sohn}]\textsubscript{i}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         F    DegP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        \textit{sehr}    AP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          \textit{stolz}      t\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

After Linearization (and Vocabulary Insertion – recall that the tree representation in \REF{ex:key:51b} contains all the vocabulary items for expository convenience), we obtain the (abbreviated) string in \REF{ex:key:52}. The inflection undergoes Local Dislocation onto the adjective yielding \textit{stolze} ‘proud’:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%52
    \label{ex:key:52}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  …  \textit{stolz}       {}-\textit{e} →  \textit{stolze}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This derivation has a number of advantages. First, Impoverishment can occur in a local fashion – the inflection is in Infl and InflP itself is in Spec,AgrP just like in the cases discussed above. Second, the argument of the adjective has moved to the left. As such, the inflected adjective is, on the surface, adjacent to the head noun, a restriction that has been widely noted and is traditionally called Head-Final Filter (e.g., \citealt{Williams1982}). If this is on the right track, then I can also account for some other cases.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   Based on work by van \citet[673]{Riemsdijk1998a}, \citet[222]{Roehrs2006a} discusses some instances where the adjectival inflection is not on the adjective itself but on an element that is in the right periphery of the extended projection of the adjective. Like above, the inflection alternates:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%53
    \label{ex:key:53}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{ein} [ \textit{so schnell wie möglich-es} ] \textit{Aufräumen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  a      so quick    as   possible-\textsc{st}   cleaning.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a cleaning as quick as possible’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{das} [ \textit{so schnell wie möglich-e} ] \textit{Aufräumen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  the    so quick    as   possible-\textsc{wk} cleaning.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the cleaning as quick as possible’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As in the examples above, the question arises how the deeply embedded inflection can undergo Impoverishment on current assumptions. Furthermore, it is not clear how the inflection can occur on an element that is not the head of the AP in the first place.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I propose that the discussion above can shed some light on these issues. Consider in more detail the portion in \REF{ex:key:53b} that is delineated by square brackets. The structure of this portion is illustrated in \REF{ex:key:54} below. I assume that the adjectival head cannot move out of the comparative structure, call it CompP. Abstracting away from the internal structure of CompP, the latter moves to Spec,InflP: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%54
    \label{ex:key:54}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

                    InflP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          CompP\textsubscript{k}        Infl’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       \textit{so schnell wie möglich}%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           {}-\textit{e}     ... t\textsubscript{k} ...
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

After Linearization (and Vocabulary Insertion), we obtain \REF{ex:key:55}. With \textit{möglich} ‘possible’ an adjective (at least by form), this provides an appropriate overt host for the inflection, and the latter can undergo Local Dislocation onto the adjective :
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%55
    \label{ex:key:55}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  …  \textit{möglich}    {}-\textit{e} →  \textit{mögliche}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This, then, allows Impoverishment not only to occur in a local fashion, just like above, but also explains the unexpected position of the adjectival inflection.\footnote{Another advantage of separating the inflection from the adjective stem is that it can account for the difference in German between attributive adjectives, which have an inflection, and predicative ones, which do not:(i)  \textit{Das Haus ist klein}.    the  house is small    ‘The house is small.’This can be captured by assuming that InflP is present in the former cases but not in the latter.} I return to adjectives as extended projections in the context of non-restrictive adjectives in Chapter 4, \sectref{sec:key:4}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Above, I discussed adjectives that appear to be deeply embedded inside the specifier of AgrP. Next I turn to adjectives that are separated from the determiner by a numeral. In other words, the determiner and the adjective do not seem to be in a local relation either. Nevertheless, the adjective shows a weak ending:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%56
    \label{ex:key:56}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{dies-e     zehn klein-en   Autos}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  these-\textsc{st} ten   small-\textsc{wk} cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘these ten small cars’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Assuming successive-cyclic movement of determiners from ArtP to DP, I suggested above that (phrasal) demonstratives adjoin to AgrP on their way to Spec,DP. This is now illustrated in more detail with \REF{ex:key:57} below:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%57
    \label{ex:key:57}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{diese}     D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     D    CardP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            diese\textsubscript{i}     CardP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      InflP\textsubscript{Q}     Card‘
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      \textit{zehn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                Card     AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   diese\textsubscript{i}    AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

InflP     Agr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{kleinen}%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                          Agr                 ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                         diese\textsubscript{i}    Art’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                  Art             NumP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                      \textit{Autos} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This adjunction of the demonstrative to AgrP provides the local context required for the application of Impoverishment (see again \sectref{sec:key:2.1.6}). Consequently, the adjective surfaces with a weak ending. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up, this section discussed adjectives in the context of canonical noun phrases. It was proposed that determiners, or more precisely their categorial features [+D], trigger Impoverishment Rule 1 such that fully specified feature bundles on terminal heads undergo feature deletion and are realized as less specified (weak) inflections. Impoverishment rule 2 works in a similar way except that it is triggered by genitive masculine/neuter contexts in AgrP. Making certain assumptions, all Impoverishment rules apply in local constellations. Strong adjectives occur when Impoverishment does not occur (e.g., features for definiteness or deixis are absent). In other words, these inflections spell out underlyingly fully specified features and present the elsewhere case. Next, I turn to the discussion of non-canonical structures to see how adjectival inflections fare in these contexts.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.  Adjectival Inflections in Non-canonical DPs
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the previous section, I focused on the simple (canonical) cases of the schematic form “determiner + adjective(s) + noun”, where all these types of elements agree in case, number, and gender. In addition, I also discussed Saxon Genitive constructions, extended-adjective constructions, and adjectives after numerals. I proposed that all these cases involve regular DPs, with adjectives in Spec,AgrP and the determiner in the DP-level. These are the contexts where weak adjectives occur. In this section, I contrast those noun phrases with non-canonical constructions. The latter are argued to involve structures different from regular DPs and different from one another. Some independent evidence is provided for the individual structures (for more detailed discussion, see the original sources referenced below). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If weak adjectives occur in regular, simple DPs only, as proposed in the previous section, then there are two distinct options for analyzing DPs containing strong adjectives and relevant determiners: it could be claimed that either the determiner is in a position where it cannot have an impact on the adjective or that the adjective itself is outside the determiner’s regular domain of influence. Note that in each of these scenarios, the structural relation between the determiner and the adjective differs from that in regular DPs, and Impoverishment Rule 1 does not apply. The following discussion features both options and provides evidence for Hypothesis 2a (i.e., inflections indicate abstract structure).\footnote{There are two more options. As a third option, we could suggest that both the determiner and the adjective are in non-canonical positions. To keep things simple, I do not consider this possibility. Fourth, as seen in the following discussion, some non-canonical DPs involve no relevant determiner to begin with. Consequently, Impoverishment cannot occur in these cases (independent of the structure).} For expository clarity, the non-canonical position of either the determiner or the adjective is marked by square brackets around the relevant (complex) element in the tree diagrams. Finally, note that with the exception of indefinite pronoun constructions (\sectref{sec:key:3.2}), all cases involve definite contexts. This is clear given that many of these instances contain definite determiners. As such, weak endings on the adjectives are expected. As we will see though, this is not borne out. Again, adjectival inflections in German cannot be a reflex of the definiteness of the containing noun phrase (cf. Hypothesis 1a).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.1.  Regular DP vs. Low Right-Adjunction: Close Appositions
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I begin with a canonical noun phrase \REF{ex:key:58a} and compare it to a noun phrase where a name-like nominal such as \textit{Großer Bär} ‘Great Bear’ occurs to the right of another nominal \REF{ex:key:58b}. The second construction is an instance of close apposition, and it typically involves one intonation unit (e.g., \citealt{LekakouSzendrői2007}, \citealt{Löbel1991}). It is clear that the first case involves a weak inflection on the adjective but that the second case must have a strong ending on the adjective:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%58
    \label{ex:key:58}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{der} \textit{groß-e(*r)  Bär}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the big-\textsc{wk/}*\textsc{st} bear.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the big bear’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. \textit{das Sternbild      Groß-e*(r)  Bär}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

the constellation big-\textsc{st}/*\textsc{wk} bear.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the constellation Great Bear’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To repeat, regular, simple DPs as in \REF{ex:key:58a} involve one nominal; that is, they have one head noun. This is different for close appositions as in \REF{ex:key:58b}. Here, this structure contains two nominals where the nominal on the left – often called anchor – contains a categorizing count noun, and the nominal on the right is a proper name or a uniquely referring noun phrase. Given these inflectional and syntactic differences, the cases in \REF{ex:key:58} are argued to involve different structures.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Turning first to the canonical case in \REF{ex:key:58a}, the relevant part of the structure is repeated in simplified form below: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%59
    \label{ex:key:59}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Regular DP-Structure (Simplified)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     D    AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   \textit{der}\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              InflP     Agr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{große}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Agr     ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      der\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                       Art’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          Art      NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          der\textsubscript{i}   \textit{Bär}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As discussed in \sectref{sec:key:2}, I take this to be the structural constellation where weak adjectives come about. With the adjective strong in \REF{ex:key:58b}, something else must be assumed to explain this example. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In certain northern dialects of German, proper names can take an optional proprial article as in \textit{(die) Anna} ‘Anna’ (Nübling \textit{et al} 2015: 123-28). As noted by \citet{Löbel1991}, this optional article is not possible in close appositions \REF{ex:key:60a}, and the name has to follow the noun it delimits. Compare \REF{ex:key:60a} to \REF{ex:key:60b}:\footnote{As a reviewer points out, (southern) dialects with an obligatory proprial article allow this type of apposition in \REF{ex:key:60a} (although the following genitive sounds somewhat formal). Note also that \REF{ex:key:60b} improves significantly if there is a long pause after \textit{Bruder} ‘brother’ (see the discussion of loose appositions in \sectref{sec:key:3.4}).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%60
    \label{ex:key:60}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{die Tochter (*die) Anna meines Bruders}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

the daughter   the  Anna of.my  brother
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Anna, my brother’s daughter’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  *  \textit{die Tochter  meines Bruders (die) Anna}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

the daughter of.my  brother   \textsubscript{} the  Anna 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Relating the close appositive in \REF{ex:key:60a} to the one in \REF{ex:key:58b} above, I propose for these cases a structure different from that of canonical DPs, namely the name-like element is adjoined to the NP of the larger DP. Abstracting away from movements internal to the matrix DP, this can schematically be represented as follows (for a symmetric structure, see also \citealt{LekakouSzendrői2012}; cf. \citealt{Löbel1991}: 13):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%61
    \label{ex:key:61}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Close Appositions}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  D    NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           \textit{das} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    NP         [\textit{Großer Bär}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    N      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        \textit{Sternbild} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As to the inflection on the adjective, it is clear that the determiner did not adjoin to the phrase containing the adjective. Consequently, Impoverishment Rule 1 does not apply, and this accounts for the strong adjective.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.  Regular DP vs. Mid Right-Adjunction: Indefinite Pronoun Constructions
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet{Roehrs2008} argues that indefinite pronoun constructions involve several different types. With current purposes in mind, there are two types relevant here: one type exhibits a weak adjective \REF{ex:key:62a}, and the other shows a strong adjective (62b-c):\footnote{Note that both \textit{wer} ‘someone’ and \textit{jemand} ‘someone’ can also be followed by an adjective ending in -\textit{es} (e.g., \textit{jemand anderes} ‘someone different’). As discussed above, -\textit{es} cannot be a regular genitive inflection on the adjective – such an ending would be -\textit{en} (for discussion and analysis of these cases, see \citealt{Roehrs2008}). Also, \textit{wer anderer} in \REF{ex:key:62b} sounds rather marked in isolation (and there may also be some speaker variation). However, this string gets better in context (e.g., \textit{das macht wer anderer} ‘someone different will do that’, \textit{wer anderer als du} ‘someone/who else than you’; both examples were retrieved from the internet).} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%62
    \label{ex:key:62}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{jed-er                   ander-e(\textsuperscript{\%}}\textit{r)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    every(one)-\textsc{masc} different-\textsc{wk}/\textit{\textsuperscript{\%}}\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘everyone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \%  \textit{w-er                  ander-e*(r)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    someone-\textsc{masc} different-\textsc{st}/*\textsc{wk}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘someone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{jemand}  \textit{ander-e*(r)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    someone.\textsc{masc} different-\textsc{st}/*\textsc{wk}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘someone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is argued in that paper that although both types in \REF{ex:key:62} involve concord between the pronominal element and the adjective, they involve different structures. As also briefly mentioned in the context of uninflected \textit{ein} (\sectref{sec:key:2.2.1}), this means that concord can only be a necessary, but not sufficient, condition on the occurrence of weak adjectives. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Turning to the individual structures, I point out first that there are other important differences between these two types. Among others, the first type allows an overt noun, but the second crucially does not:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%63
    \label{ex:key:63}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{jeder          ander-e         Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    every(one) different-\textsc{wk} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘every different man’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. *  \textit{wer         ander-e(r)          Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    someone different-\textsc{st}/\textsc{wk} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. *  \textit{jemand   ander-e(r)          Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    someone different-\textsc{st}/\textsc{wk} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I follow the aforementioned paper in that the first case involves a regular DP-structure with the option of overtly realizing the head noun but that the second and third example involve a completely different structure. \citet{Roehrs2008} argues that the adjectives here are part of a Modifier Phrase (ModP, see \citealt{Rubin1996}). The latter phrase is proposed to be right-adjoined to an Indefinite Pronoun Restrictor Phrase (IPRP). The structure can be illustrated as follows (e stands for a null element; see also \citealt{Leu2005}):\footnote{Given the indefiniteness of the construction, one objection might be that a strong ending is expected here. Importantly though, this construction also exhibits a strong ending in the dative (for detailed discussion, see Chapter 3, \sectref{sec:key:2.2}):(i) \textit{mit  \{jemand / \textsuperscript{\%}}\textit{wem\} ander-em}                with someone.\textsc{masc}   different-\textsc{st}    ‘with someone different’In other words, this indefinite pronoun construction is different from the noun phrases involving \textit{ein} ‘a’ (e.g., \textit{mit} \textit{einem ander-en Mann} ‘with a different-\textsc{wk} man’). As the latter are usually used to exemplify the inflectional alternation in indefinite environments, indefinite pronoun constructions deserve being mentioned in the current context – they show that not all indefinite constructions behave the same as regards inflections on adjectives. Again, it is clear that structure must be taken into account.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%64
    \label{ex:key:64}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Indefinite Pronoun Constructions}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    D      IPRP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      IPRP             ModP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    IPR    NP  Mod        AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      {\textbar}      {\textbar}             {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textit{je}      \textit{{}-mand}     \textit{e\textsubscript{N}}\textsubscript{} [\textit{anderer} \textit{e\textsubscript{N}}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           \textit{wer}       \textit{e\textsubscript{R}}     \textit{e\textsubscript{N}}\textsubscript{} [\textit{anderer} \textit{e\textsubscript{N}}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Some brief remarks about the structure are in order here. Note that I put the quantifying, determiner-like element under D. In conjunction with the DP-layer, IPRP accounts for the complex internal structure of indefinite pronouns (e.g., \textit{je-mand} ‘someone’). Finally, the presence of ModP is motivated by the corresponding constructions in some of the Romance languages. For instance, French makes use of \textit{de} ‘of’ in indefinite pronoun constructions (\textit{quelque chose}\textsubscript{MASC} \textit{de grand}\textsubscript{MASC} ‘something big’), and this element seems to mediate concord between the two nominals (cf. \textit{une bonne chose}\textsubscript{FEM} \textit{de faite}\textsubscript{FEM} ‘(a good thing of done =) at least, that is done’; for details, see \citealt{Roehrs2008}: 21-23). Returning to the discussion of the strong adjectives, note that there is no relevant determiner that can trigger Impoverishment, and a strong ending appears on the adjective. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.3.  Regular DP vs. Mid Right-Adjunction: Noun-Adjective Exclamatives
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Certain exclamatives involve a noun and a following definite article and inflected adjective \REF{ex:key:65a}. As can be seen in this example, these constructions license the absence of a determiner before the noun. In fact, the definite article can also be missing before the inflected adjective \REF{ex:key:65b}. When this article is missing, the adjective is strong. Compare \REF{ex:key:65a} to \REF{ex:key:65b}:\footnote{\citet[70]{Dürscheid2002} provides the following, related examples: \textit{Mist, verdammt-er!} ‘darn-\textsc{st} rubbish’; \textit{Biest, elend-es!} ‘miserable-\textsc{st} beast’; \textit{Idiot, verdammt-er!} ‘darn-\textsc{st} idiot’; \textit{Gott, allmächtig-er} ‘God almighty-\textsc{st}’.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%65
    \label{ex:key:65}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Schwein, das schwarz-e!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    pig.\textsc{neut} the  black-\textsc{wk}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Stupid bastard!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 b.  \textit{Schwein, schwarz-e*(s)!} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    pig.\textsc{neut} black-\textsc{st/*wk}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Stupid bastard!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

While there are other versions of this type of exclamative, I limit myself here to pointing out that a pronominal determiner can also be present before the adjective \REF{ex:key:66a}. Crucially, though, an indefinite article cannot precede the adjective \REF{ex:key:66b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%66
    \label{ex:key:66}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Schwein, du   schwarzes!} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

pig.\textsc{neut} you black-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     ‘Bastard, you stupid one!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{Schwein, ein schwarz-es!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    pig.\textsc{neut} a    black-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Like the indefinite pronoun construction in the last section, these cases also involve concord in agreement features. Unlike the indefinite pronoun construction, the inflected adjective follows a noun here. Furthermore, it is clear that the exclamatives in (65a-b) and \REF{ex:key:66a} have similar, but not identical, referential properties. Specifically, while the examples in (65a-b) single out some individual in a specific context, \REF{ex:key:66a} addresses such an individual. I hypothesize that all these cases involve definite expressions. This fits well with the common assumption that vocatives of the type \textit{dummer Idiot} ‘stupid idiot’ involve definite contexts too (\sectref{sec:key:3.9}).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Turning to the structure, we can observe that the adjective in \REF{ex:key:65b} is strong in a definite context. Importantly, considering the ungrammaticality of \REF{ex:key:66b} and the definite reference of \REF{ex:key:65b}, it seems implausible that an indefinite determiner, including a null article, is or was present in \REF{ex:key:65b}. Furthermore, given that the inflected adjective occurs in postnominal position, I suggest that \REF{ex:key:65b} consists of two nominals. The matrix nominal involves (at least) NumP, where the noun \textit{Schwein} ‘pig’ has undergone raising to Num. It is possible that there is more structure on top of NumP (cf. the structure of vocatives in \sectref{sec:key:3.9}). As to the following adjective \textit{schwarz} ‘black’, I suggest that it is part of a second nominal. Given concord in agreement features between the overt noun and the inflected adjective, I argue that the adjective is followed by a null noun, which is coreferential with the overt noun in the matrix nominal. The adjective and the null noun make up their own nominal, and I assume that the latter is adjoined to the NP of the matrix nominal. Like in the indefinite pronoun construction, the adjunction is mediated by ModP. This derives \REF{ex:key:65b} as follows:\footnote{I discuss the strong adjective in cases like \REF{ex:key:66a} in detail in Chapter 3, \sectref{sec:key:5}.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%67
    \label{ex:key:67}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Noun-Adjective} \textit{Exclamatives}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         NumP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           Num’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     Num        NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{Schwein}\textsubscript{i}  Num    NP    ModP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               t\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            Mod        AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                       {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                      [\textit{schwarzes} \textit{e\textsubscript{N}}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that there is no determiner present in these cases. Consequently, Impoverishment is not triggered, and a strong ending surfaces on the adjective. Next, I turn to a third type of adjunction.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.4.  Regular DP vs. High Right-Adjunction: Loose Appositions
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

A typical case of a – what is sometimes called – loose apposition is given in \REF{ex:key:68a} below. Like in the two types of adjunction cases from Sections 3.1 and 3.2/3.3, the adjective can only be strong. This is in contrast to the loose apposition in \REF{ex:key:68b}, which involves a definite determier:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%68
    \label{ex:key:68}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{Wir, begeistert-e(*n)       Linguisten, fordern  mehr Unterstützung}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

we   enthusiastic-\textsc{st}/*\textsc{wk} linguists     demand more support
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘We, enthusiastic linguists, demand more support.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Wir, die begeistert-e*(n)       Linguisten, wollten mehr Unterstützung}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

we   the enthusiastic-\textsc{wk}/*\textsc{st} linguists     wanted more support
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘We, the enthusiastic linguists, wanted more support.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There are other differences as regards the two types of cases in the previous sections. First, the nominal following \textit{wir} ‘we’ in \REF{ex:key:68a} is not a name-like element (unlike the close apposition in \REF{ex:key:58b}), and second, this nominal involves an overt head noun after the adjective (unlike the indefinite pronoun construction in \REF{ex:key:62c} and unlike the exclamative in \REF{ex:key:65b}). Third, these constructions have a distinctive intonation contour, which includes pauses before and after the appositive. Fourth, a genitive complement to the first head noun can only precede this type of apposition \REF{ex:key:69a} but cannot follow it \REF{ex:key:69b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%69
    \label{ex:key:69}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a. \textit{die Professoren unserer Uni,  \textsubscript{} }\textit{sehr nett-e    Leute}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the professors   \textsubscript{} of.our    univ, very nice-\textsc{st} people
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the professors of our university, very nice people’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  *    \textit{die Professoren, sehr nett-e   Leute,   unserer Uni}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  the professors, \textsubscript{} very nice-\textsc{st} people, of.our    univ
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In order to capture the difference in (69a-b), I propose that this is another case of right-adjunction. Unlike the two other types of instances, I suggest that this adjunction is higher in the structure, namely to the DP-level (cf. \citealt{DelormeDougherty1972}). \REF{ex:key:68a} and \REF{ex:key:69a} are derived in simplified form as follows:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%70
    \label{ex:key:70}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Loose Appositions}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    DP             AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

D    NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   {\textbar}      {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           \textit{wir}     \textit{e\textsubscript{N} }[\textit{begeisterte Linguisten}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           \textit{die    Professoren unserer Uni} [\textit{sehr nette Leute}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As in the cases above, a determiner did not adjoin to the phrase containing the adjective. With Impoverishment not occurring, a strong adjective surfaces. Note that the pronominal determiner \textit{wir} ‘we’ in \REF{ex:key:70} is taken to have a null noun as part of its complement. In the next section, I consider cases where this type of determiner occurs with an overt noun.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.5.  Regular DP vs. Complex Specifier inside DP: Dis-agreement in Pronominal DPs
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet{Postal1966} argues that pronouns are determiners in that they can combine with adjectives and nouns (also \citealt{Pesetsky1978}; for more recent discussion, see \citealt{DéchaineWiltschko2002}: 421-22, \citealt{Roehrs2005}, and the extensive work by Höhn, e.g., \citealt{Höhn2020}). A typical case of such a pronominal DP is given in \REF{ex:key:71a}. What is interesting about this case is that the adjective can, with some differences in preference, have a weak or a strong inflection. Compare \REF{ex:key:71a} to \REF{ex:key:71b}. Furthermore, \citet{Roehrs2006b} discusses some other pronominal DPs where plural pronouns combine with singular head nouns despite the fact that these two types of elements do not agree in morphological number \REF{ex:key:71c}. Importantly though, here only a strong adjective is possible (e.g., \citealt{Bhatt1990}: 154-55, \citealt{Darski1979}: 202, Gunkel \textit{et al}. 2017: 1308):\footnote{\citet{Roehrs2006b} provides some other, related examples: \textit{ihr verdammt-es Pack} ‘[intended:] you(PL) darn-\textsc{st} gang’, \textit{ihr blōd-e Bande} ‘[intended:] you(PL) stupid-\textsc{st} gang’, \textit{ihr dumm-es Gesindel} ‘[intended:] you(PL) dumb-\textsc{st} riff-raff’ (see also Footnote Error: Reference source not found).} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%71
    \label{ex:key:71}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{ihr        dumm-en   Idioten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) stupid-\textsc{wk} idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you stupid idiots’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 b. \%  \textit{ihr        dumm-e    Idioten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) stupid-\textsc{st} idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you stupid idiots’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.   \textit{ihr         jung-e*(s)               Gemüse}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) young-\textsc{st.sgl/*wk} vegetable.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you young folks’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Unlike the loose appositives in the last section, none of the cases above involve comma intonation. In addition, pronominal DPs and loose appositives involve a difference in interpretation. While it is not impossible that the adjective in the pronominal DP may receive a restrictive interpretation (e.g., \textit{nur} \textit{ihr Großen} ‘only you tall ones’), this is not true of appositional structures. In the latter case, the adjective and noun simply provide additional information, which does not delimit the set denoted by \textit{wir} ‘we’ (e.g., \textit{ihr, begeisterte Studenten} ‘you, enthusiastic students’). Before moving on, note also that the unmarked example in \REF{ex:key:71a} runs counter to the traditional generalizations Weak After Strong and the Principle of Monoinflection.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is pointed out in \citet{Roehrs2006b} that semantic agreement in number must hold between all the elements in (71a-c). For instance, like the pronominal determiner \textit{ihr} ‘you(\textsc{pl})’, both the count noun \textit{Idioten} ‘idiots’ and the mass noun \textit{Gemüse} ‘(vegetable =) folks’ imply several individuals. This is similar to regular determiners such as \textit{diese} ‘these’ – they also require semantic agreement with the head noun. However, unlike pronominal determiners, regular determiners are more restrictive: they do not tolerate morphological dis-agreement at all (e.g., \textit{ihr blödes Pack} ‘[intended:] you(PL) stupid gang’ vs. *\textit{diese Pack} ‘[intended:] these gang’). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Furthermore, without a special intonation contour, agreeing nominals (e.g., \textit{dumme(n) Idioten} ‘stupid idiots’ as in (71a-b)) or dis-agreeing nominals (e.g., \textit{junges Gemüse} ‘young [vegetable =] folks’ as in \REF{ex:key:71c}) cannot be iterated in pronominal DPs, and both of these nominals cannot interact with one another. In other words, there can be only one overt noun and related adjective in the pronominal DP:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%72
    \label{ex:key:72}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ihr         Idioten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you idiots’    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. *  \textit{ihr         Idioten jung-es}         (\textit{Gemüse})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) idiots   young-\textsc{st.sgl} vegetable.\textsc{neut} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. *  \textit{ihr         jung-es}         (\textit{Gemüse})            \textit{Idioten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) young-\textsc{st.sgl} vegetable.\textsc{neut} idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%73
    \label{ex:key:73}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ihr        Gemüse}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) vegetable.\textsc{neut} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (young) folks’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{ihr        Gemüse             dumm-en}      (\textit{Idioten})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) vegetable.\textsc{neut} stupid-\textsc{wk.pl} idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  *  \textit{ihr         dumm-en}      (\textit{Idioten}) \textit{Gemüse}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) stupid-\textsc{wk.pl} idiots     vegetable.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In order to capture these facts, \citet{Roehrs2006b} proposes that the pronominal determiner can optionally select either an AgrP resulting in the (regular) agreeing cases or a – what he calls for lack of a better name – Dis-agreement Phrase (DisP) bringing about the dis-agreeing cases.\footnote{With adjunction less constrained (e.g., \citealt{Pysz2006}: 66-67), above-mentioned restrictions are best stated in terms of selection.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I assume that prononomal DPs with an agreeing adjective and noun as in (71a-b) involve a regular, canonical DP where the pronominal determiner surfaces in the DP-layer, the adjective is in Spec,AgrP, and the noun is in NP. This derives \REF{ex:key:71a} as follows (recall that the AP in Spec,AgrP involves InflP at the top; I abstract away from NumP here): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%74
    \label{ex:key:74}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Transitive Pronouns (Regular DP - simplified)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     D    AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   \textit{ihr}\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              InflP     Agr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{dummen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Agr     ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      ihr\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                       Art’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          Art      NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          ihr\textsubscript{i}   \textit{Idioten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In Chapter 3, \sectref{sec:key:5}, I discuss the strong/weak alternation in pronominal DPs, including the strong inflection on the adjective in \REF{ex:key:71b}, in more detail. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As for cases like \REF{ex:key:71c}, pronominal DPs with a morphologically dis-agreeing nominal have a different structure. They involve non-canonical DPs where the entire dis-agreeing element is assumed to be in Spec,DisP, and the matrix nominal involves a null noun. Compared to \REF{ex:key:74}, this construction features an intransitive pronoun:\footnote{Note that both pronouns in \REF{ex:key:74} and \REF{ex:key:75} take complements, overt material in \REF{ex:key:74} but a null noun in \REF{ex:key:75}. As such, I use the terms transitive and intransitive as regards the overtness and covertness of the following material. Also, the structure in \REF{ex:key:75} has wider application. In Chapter 6, it is pointed out that a (number-neutral) bare noun like \textit{Bauer} ‘peasant’ must be singular in interpretation in dis-agreement cases such as \textit{Sie Bauer} ‘you peasant’ (where \textit{Sie} ‘you’ is morphological plural and could, at least in principle, be plural in interpretation). I argue in more detail there that the nominal \textit{Bauer} is in Spec,DisP.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%75
    \label{ex:key:75}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Intransitive Pronouns (Dis-agreement)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     D    DisP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   \textit{ihr}\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        [\textsubscript{AgrP} \textit{junges Gemüse}]   Dis’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Dis     ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      ihr\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                       Art’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          Art      NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          ihr\textsubscript{i}   \textit{e\textsubscript{N}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note now that there is a clear difference between the agreeing DP in \REF{ex:key:71a} and the dis-agreeing case in \REF{ex:key:71c}. In the former, the plural adjective and its projected InflP reside in Spec,AgrP \REF{ex:key:74}; in the latter, the singular adjective also projects InflP, but there is more linguistic material that relates to this adjective – the singular noun. Since both agree in concord features, they make up their own nominal, presumably AgrP. Given the lack of morphological agreement with the plural pronoun, this AgrP has been located in Spec,DisP \REF{ex:key:75}. To be clear, unlike \REF{ex:key:74}, the adjective in \REF{ex:key:75} is more deeply embedded (cf. InflP vs. AgrP, which contains InflP). To repeat, the adjective in the latter establishes its own agreement relation with the overt noun inside the embedded AgrP and is morphologically independent of the larger DP (for the discussion of lack of concord here, see Chapter 8, \sectref{sec:key:3.1}). Given this complex specifier, it is clear that Impoverishment cannot occur resulting in a strong adjective. More generally, this section has shown that adjectival inflections provide clues about various degrees of embeddings of adjectives (Hypothesis 2a).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.6.  Regular DP vs. Separate Base-generation: Split Topicalizations
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet{Fanselow1988} and van \citet{Riemsdijk1989} discuss discontinuous noun phrases, often referred to as split topicalizations (also \citealt{Ott2011a}). Putting it in simple terms, the lower part of a noun phrase occurs in a higher position of the clause, and the higher part of the same noun phrase is in a lower position. Compare the continuous nominal in \REF{ex:key:76a} to its discontinuous counterpart in \REF{ex:key:76b}: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%76
    \label{ex:key:76}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Er hat zehn Hemden getragen}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he has ten   shirts     worn
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘He wore ten shirts.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Hemden hat er zehn getragen}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    shirts     has he ten   worn
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for shirts, he wore ten.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This type of construction has a number of interesting properties. As pointed out by \citet[249-50]{Bhatt1990} and \citet{FanselowĆavar2002}, some speakers allow the split of a definite noun phrase. Importantly, while an adjective in a non-split noun phrase has a weak ending \REF{ex:key:77a}, this adjective has a strong inflection when it is topicalized stranding the determiner \REF{ex:key:77b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%77
    \label{ex:key:77}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{Ich habe immer nur  diese bunt-e*(n)          Hemden da    getragen}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I    have always only these colored-\textsc{wk}/*\textsc{st} shirts     there worn
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I have always worn only these colored shirts there.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Bunt-e(*n)         Hemden habe ich immer nur   diese da     getragen}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    colored-\textsc{st}/*\textsc{wk} shirts      have I    always only these there worn
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for colored shirts, I have always worn only these there.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This split results in two nominals. \citet{Fanselow1988} proposes that these two nominals are base-generated separately in the VP and that one of them undergoes movement. This can be illustrated for \REF{ex:key:77b} in \REF{ex:key:78}. For concreteness, I put adverbial elements as adjuncts to a Perfective Phrase (PerfP), where the auxiliary \textit{haben} ‘to have’ originates (for more detailed discussion of split topicalization, see Chapter 4, \sectref{sec:key:3}): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%78
    \label{ex:key:78}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Split Topicalizations}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          CP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    AgrP\textsubscript{k}       C’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 [\textit{bunte Hemden}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         C    TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                \textit{habe}\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          \textit{ich}     T’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                    PerfP     T
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                  t\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 \textit{immer}            PerfP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                    \textit{nur}            PerfP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                       VP            Perf
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                            t\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                      t\textsubscript{k}             V’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                      DP             V
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{diese e\textsubscript{N}} \textit{da        getragen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Some independent evidence for this analysis comes from the fact that the discontinuous noun phrase can have two mismatching determiners, for instance, \textit{‘n} ‘a’ and \textit{dieses} ‘this’ below (recall that \textit{‘n} is the reduced form of the indefinite article \textit{ein}): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%79
    \label{ex:key:79}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         ‘\textit{N Hemd        habe ich immer  nur  dieses getragen.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 a  shirt.\textsc{neut}\textsubscript{} have I     always only this     worn
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for a shirt, I only wore this one.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With the two nominals assembled separately from one another, it should be clear that the demonstrative in \REF{ex:key:77b} did not adjoin to the AgrP that contains the adjective. Impoverishment does not occur yielding a strong ending on the adjective.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.7.  Regular DP vs. Complex Compound Modifier: Nominal Compounds
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As illustrated before, adjectives in canonical DPs undergo the strong/weak alternation \REF{ex:key:80a}. This is also possible with phrasal proper names like \textit{die Deutsche Bank} ‘the German Bank’ \REF{ex:key:80b}. Interestingly, these proper names can form complex modifiers of nominal compounds (\citealt{Lieber1988}, R. \citealt{Wiese1996b}). Crucially, here the adjectives have to appear with strong endings \REF{ex:key:80c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%80
    \label{ex:key:80}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{mit   dem deutsch-en   Chef}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with the   German-\textsc{wk} boss.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with the German boss’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mit   der Deutsch-en  Bank}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

with the German-\textsc{wk} Bank.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘with the German Bank’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{mit   dem} [\textit{Deutsch-e(*n)    Bank}] \textit{Chef}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with the    German-\textsc{st/*wk} Bank \textsubscript{} boss.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with the German Bank-boss’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The example in \REF{ex:key:80c} involves a complex nominal, and it can be represented as in \REF{ex:key:81}, where the complex modifier is adjoined to the left of the head noun:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%81
    \label{ex:key:81}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Nominal Compounds}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  D    NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          \textit{dem}     {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       N
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  [\textit{Deutsche Bank}]   N
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 \textit{Chef}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As in the cases above, the determiner did not adjoined to the structure containing the adjective. With Impoverishment not occurring, a strong adjective appears.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

So far, I have discussed a number of cases, where the adjective is outside the determiner’s regular domain of influence. Next, I turn to a case where the determiner itself is in a position where it cannot bring about a weak adjective.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.8.  Regular DP vs. Outside of DP Proper: Predeterminers
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Roehrs (2009a: Chapter 4) discusses some cases where determiners and determiner-like elements may co-occur (also \citealt{Wood2007}; Chapter 1, \sectref{sec:key:4.1.2}). To set the stage, consider first \REF{ex:key:82a}, where a demonstrative occurs with a weak adjective. In contrast, a possessive article occurs with a strong adjective \REF{ex:key:82b}. Now, the demonstrative and the possessive article can also be combined as in \REF{ex:key:82c}. Here, the adjective must be strong (\citealt{Duden1995}: 286, Gunkel \textit{et al}. 2017: 1308):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%82
    \label{ex:key:82}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{dieses groß-e(*s)     Glück}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this     great-\textsc{wk}/*\textsc{st} happiness.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘this great happiness’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mein groß-e*(s)      Glück}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my   \textsubscript{} great-\textsc{st}/*\textsc{wk} happiness.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘my great happiness’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{dieses mein groß-e*(s)      Glück}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this     my   \textsubscript{} great-\textsc{st}/*\textsc{wk} happiness.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘this my great happiness’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that, morphologically, the presence of the demonstrative in \REF{ex:key:82c} does not play a role for the inflection on the adjective in this example. This is surprising given that it does have an impact in \REF{ex:key:82a}. Before commenting on the syntax and semantics, note also that the strong adjective in \REF{ex:key:82c} is preceded by the strongly inflected determiner element \textit{dieses} ‘this’ in violation of the traditional generalizations about adjectival inflections. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Syntactically, it is well known that the demonstrative can only occur to the left of the possessive (cf. *\textit{mein dieses} \textit{große(s) Glück} ‘[intended:] my this great happiness’) – it is a predeterminer in such combinations. Semantically, this element seems to function as an intensifier for deixis \citep{Wood2007}. In order to account for this different behavior in the morphology, syntax, and semantics, it is proposed in \citet{Roehrs2009a} that the demonstrative is merged in a phrase above the DP-level. Here, I label this projection Left Periphery Phrase (LPP) (see \citealt{GiustiIovino2016}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%83
    \label{ex:key:83}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Predeterminers}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         LPP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   [\textit{dieses}]        DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 \textit{mein}       AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      \textit{großes}        NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textit{Glück}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As the intensifying demonstrative is base-generated above the DP-level, it did not adjoin to the phrase containing the adjective. Consequently, Impoverishment does not occur, and as a result, the adjective exhibits a strong ending.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.9.  Regular DP vs. No Determiner: Vocatives
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Vocatives are structures that occur in non-argument position. As illustrated below, these cases exhibit strong adjectives as well:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%84
    \label{ex:key:84}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Blöd-e*(s)       Schwein!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    stupid-\textsc{st/*wk} pig.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Stupid idiot!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{Dumm-e*(r)    Idiot!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    stupid-\textsc{st/*wk} idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Stupid idiot!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It can be pointed out that vocatives involve the presupposition that the individual addressed is present in the situational context. As such, vocatives are usually taken to be definite constructions (see also the exclamatives in \sectref{sec:key:3.3}). Following other work, \citet[86]{Julien2016} proposes that this definiteness follows from the presence of a second-person feature on a Vocative head. This Voc head selects a DP with an empty D head (except for an agreeing second-person feature). Given the presence of VocP, this means that vocatives as in \REF{ex:key:84} involve non-canonical structures: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%85
    \label{ex:key:85}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Vocatives}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       VocP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     Voc                DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   D             AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      \textit{blödes}         NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          \textit{Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As a determiner is absent, this accounts for the strong adjectives. Finally, note that these data confirm once again that strong adjectives are not tied to indefiniteness.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up the entire section, I discussed one context in \sectref{sec:key:2} where weak adjectives occur (regular, simple DP); in this section, I examined nine contexts where strong adjectives surface: the adjective may be part of different types of adjunction, it may be deeply embedded inside a specifier, the determiner and adjective are in their typical positions but both occur separately in a discontinuous noun phrase, the adjective may be part of a complex nominal compound, the determiner may be outside of the DP proper, and a determiner may be absent altogether. In all relevant cases, the adjective or the determiner is in a position different from that of a regular (continuous) DP. Some independent evidence was provided for the assumption of different structures. What all these nine types of constructions have in common is that Impoverishment Rule 1 cannot apply as a determiner does not adjoin to the phrase (immediately) containing the adjective. As a consequence, the inflectional feature bundle of the adjective does not undergo Impoverishment, remains unaltered, and is spelled out as a strong inflection.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Thus far, I have focused on the discussion of the inflections on adjectives. As we have seen though, determiners also have strong inflections, and the same goes for (non-possessive) determiner-like elements – intensifying predeterminers like \textit{diese} ‘this’ and \textit{alle} ‘all’. In the next section, I turn to the explanation of the strong inflections on these elements.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.  Adjectival Inflections on Determiners and Predeterminers
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

First, consider \REF{ex:key:86a} and \REF{ex:key:86b}, where both \textit{diese} ‘these’ and \textit{alle} ‘all’ are followed by a weak adjective: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%86
    \label{ex:key:86}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{dies-e     klein-en   Autos}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    these-\textsc{st} small-\textsc{wk} cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘these small cars’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 b.  \textit{all-e   klein-en   Autos}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all-\textsc{st} small-\textsc{wk} cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all small cars’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I proposed in \sectref{sec:key:2} that determiners move to the DP-level triggering Impoverishment on adjectives. As to the determiners themselves, recall that the strong endings on the determiners are identical to those on the adjectives, and as such they should receive a similar account. Now, if we make the plausible assumption that similar to adjectives, the inflections on determiners can undergo Impoverishment (for such cases involving Impoverishment Rule 2, see Chapter 3, \sectref{sec:key:4}), then the latter mechanism cannot have occurred on either \textit{diese} ‘these’ or \textit{alle} ‘all’ in \REF{ex:key:86}. I propose that Impoverishment does not apply to the inflections on determiners as the determiners themselves are not adjoined to by a(nother) determiner – after all, there is only one determiner in each of the nominal structures in \REF{ex:key:86}. As the feature bundles for case, number, and gender are not reduced, this yields strong endings on the determiners (for the significance of this discussion in the context of the Scandinavian languages, see \citealt{KatzirSiloni2014}: 278).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In \sectref{sec:key:3.8}, we saw that determiners can co-occur with intensifying predeterminers \REF{ex:key:87a}. Importantly, \textit{dieses} ‘this’ itself has a strong ending, but the possessive article is uninflected (for independent reasons, see again \sectref{sec:key:2.2.2}). However, there are also distributions where both determiner(-like) elements exhibit a strong ending at the same time \REF{ex:key:87b}. Comparing \REF{ex:key:87a} to \REF{ex:key:87b}, note also that the adjective is strong in the former but weak in the latter case:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%87
    \label{ex:key:87}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{dies-es mein groß-es  Glück}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this-\textsc{st} my    great-\textsc{st} happiness.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘this my great happiness’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{all-e   dies-e(*n)      klein-en   Autos}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all-\textsc{st} these-\textsc{st/*wk} small-\textsc{wk} cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all these small cars’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Importantly, the inflections on the predeterminers are not frozen. As can be seen in instances involving the dative case, they change just like those on the determiners themselves:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%88
    \label{ex:key:88}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{in} \textit{dies-em mein-em groß-en  Glück}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    in this-\textsc{st}  my-\textsc{st}     great-\textsc{wk} happiness.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘in this my great happiness’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mit}  \textit{all-en dies-en  klein-en   Autos}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with all-\textsc{st} these-\textsc{st} small-\textsc{wk} cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with all these small cars’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given this alternation, it is rather implausible to suggest that predeterminers and determiners form compounds. Note in this regard that \citet[217-218]{Bhatt1990} claims that \textit{alle} and \textit{diese} are heads that form a complex adjunction structure (see also Chapter 3, \sectref{sec:key:6}). In my view, a different analysis is called for. The structural account of the strong/weak alternation offered in this chapter extends to these cases without a problem. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that I proposed in \sectref{sec:key:3.8} that \textit{dieses} in \REF{ex:key:87a} is predeterminer and is taken to be base-generated in LPP; that is, it is higher up in the structure. This means that this element was neither adjoined to by another determiner, nor did it adjoin to another element – again, it originated in LPP. Consequently, it neither underwent nor triggered Impoverishment. I assume that \textit{alle} in \REF{ex:key:87b} is also base-generated in LPP. With Impoverishment not occurring, this yields strong inflections on the predeterminers and on the determiners. Briefly returning to the adjectives in \REF{ex:key:87}, \textit{mein} ‘my’ in \REF{ex:key:87a} does not trigger Impoverishment on the adjective as discussed in \sectref{sec:key:2.2.1}. This results in a strong ending. As to \REF{ex:key:87b}, \textit{diese} ‘these’ does trigger Impoverishment bringing about a weak ending on the adjective. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is worth pointing out that these are not isolated cases. An example similar to \REF{ex:key:87a} can be provided in the plural \REF{ex:key:89a}, and both \textit{alle} and \textit{diese} can precede a possessive article in the same nominal \REF{ex:key:89b} (\citealt{Bhatt1990}: 217-18, \citealt{Vater1991}: 28-29). Again, in each case, the determiner and predeterminer(s) have a strong ending:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%89
    \label{ex:key:89}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{dies-e     mein-e(*n)  nett-en   Freunde} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  these-\textsc{st} my-\textsc{st/*wk} nice-\textsc{wk} friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘these my nice friends.’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{all-e   dies-e(*n)      \textsubscript{} }\textit{mein-e(*n)  Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all-\textsc{st} these-\textsc{st/*wk} my-\textsc{st/*wk} friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all these friends of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I suggest that \textit{diese} in \REF{ex:key:89a} is also base-generated in LPP and that there is a second LPP in \REF{ex:key:89b} accommodating \textit{alle}. Note again that none of the determiners or predeterminers were adjoined to by another determiner. This explains why these elements do not have weak endings. Again, this is in line with the assumption that one determiner originates in ArtP and moves to the DP-level but that the second (or third) element that has the appearance of a determiner is in effect a predeterminer, a determiner-like element base-generated higher in the structure (LPP). As stated in Chapter 1, \sectref{sec:key:4.1.2}, determiner(-like) elements can be merged in the LPP provided they can function as intensifiers and are semantically compatible with the following determiner in the DP. More generally, this means that a sequence of two or three determiner(-like) elements with strong endings indicates more structure on top of the DP proper; that is, adjectival inflections are indicators of abstract structure (Hypothesis 2a).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up the current proposal, I provided diachronic and synchronic evidence that adjectival inflections in (Modern) German cannot be a reflex of (in-)definiteness. I proposed that these inflections are semantically vacuous (Hypothesis 1a). Furthermore, having shown that concord is only a necessary (but not sufficient) condition for the appearance of weak adjectives, I pointed out that these adjectives are only possible in regular, simple DPs, where the determiners adjoin to the phrases containing the adjectives. By contrast, strong inflections occur in very diverse contexts and do not seem to be subject to any special conditions. As such, they involve the elsewhere case. Indeed, we have seen distributions where the highest element or elements exhibit a strong inflection at the top of the structure although adjectives with weak inflections surface in the lower part of the structure. I argued that Impoverishment occurs locally and in a bottom-up fashion – it affects all adjectives but none of the determiners or predeterminers.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  More generally, if this is correct, then the strong/weak alternation of adjectival inflection appears to be a reflex of different structures. In fact, the different types of inflections may indicate the degree of embedding of adjectives (Spec,AgrP vs. Spec,DisP containing Spec,AgrP) and a certain amount of structure (DP vs. LPP on top of DP). The strong adjectives in the latter two types of contexts (Spec,DisP and LPP) provide evidence for Hypothesis 2a – more abstract structure is involved. Another result of the discussion is that the traditional terms weak and strong inflections have no significance other than different degrees of feature specificity. The weak endings form a subset of the strong endings in the sense of having the fewest features in their specifications. Finally, to highlight some of the points of the previous sections and to compare the current analysis to other work, I discuss three earlier proposals.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.  A Brief Critique of some Previous Proposals\\\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet{Abney1987} stimulated much important research on the noun phrase. However, many of the discussions of German have concentrated on the structure of the DP (e.g. \citealt{Felix1990}; \citealt{Haider1988}; \citealt{Löbel1990a},b; \citealt{Vater1991}) and relatively fewer contributions have been devoted to the explanation of morpho-syntactic phenomena such as the distribution of adjectival inflections. It is perhaps telling that three monographs on the DP, \citet{Bhatt1990}, \citet{Demske2001}, and \citet{Wegener1995}, discuss only three different types of analyses at length (they all discuss \citealt{Olsen1991b}). In this section, I review three proposals in more detail. I start off by examining Olsen’s seminal work (1989a, 1989b, 1991a, 1991b), and then I turn to two more recent accounts: \citet{Schoorlemmer2009} and \citet{Murphy2018}.\footnote{For the discussion of other proposals, see some earlier remarks in this chapter but also \citet{KatzirSiloni2014} and \citet[175-191]{Roehrs2006a}.} Note that all these analyses discuss only the canonical cases but not the non-canonical structures, and it is often not clear how the latter can be accommodated in these accounts. As laid out above, I argue, however, that it is the non-canonical structures that reveal the true nature of adjectival inflections in German.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.1.  Olsen (1989a, 1989b, 1991a, 1991b)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In a series of papers (1989a, 1989b, 1991a, 1991b), Olsen discusses inflection within the German DP (I refer only to her last paper as that contains the main relevant insights). Assuming \citegen{Abney1987} DP-Hypothesis, Olsen proposes that phi-features are located under D. These features involve person, case, number, and gender. Abbreviated as AGR, they need to be made visible. Olsen assumes that agreement within the DP is brought about by D selecting NP as its complement and by Percolation of superscripts from NP down the tree. Here are the relevant definitions (my translations):\footnote{These are the original definitions (\citealt{Olsen1991b}: 40, 38):(i)  a.  \textit{Prinzip der morphologischen Realisierung}    Grammatische Merkmale werden phonologisch sichtbar gemacht.  b.  \textit{Kongruenzkette}:Eine Kongruenzkette besteht aus einer ununterbrochenen Folge identischer Indizes, die auf der Basis der funktionalen Selektion entsteht, die zwischen einer AGR-Kategorie und ihrem Komplement erfolgt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%90
    \label{ex:key:90}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Principle of Morphological Realization}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Grammatical features are rendered phonologically visible.      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Agreement Chain}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    An agreement chain consists of an uninterrupted sequence of identical 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

indices which are brought about by functional selection, which holds 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

between an AGR-category and its complement.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As an illustration, the example in \REF{ex:key:91a} is derived as in \REF{ex:key:91b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%91
    \label{ex:key:91}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{da-s    kalt-e     Wetter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  the\textsc{{}-st} cold-\textsc{wk} weather.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘the cold weather’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.             DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    D\textsuperscript{i}             NP\textsuperscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             \textit{das}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               AP\textsuperscript{i}    N\textsuperscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                \textit{Wetter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      A\textsuperscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             \textit{kalte} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

AGR is made visible under D by the definite determiner \textit{das} ‘the’, which has a strong inflection. An identical superscript is on NP (by functional selection) and on N and A (by Percolation). To ensure unique realization of grammatical features, Olsen follows Emonds’ (1987: 615) Invisible Category Principle:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%92
    \label{ex:key:92}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Invisible Category Principle}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  A closed category B with positively specified features C\textsubscript{i} may remain empty 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

throughout a syntactic derivation if the features C\textsubscript{i} ... are all alternatively realized 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

in a phrasal sister of B.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To briefly illustrate with a construction in English, the Invisible Category Principle is taken to restrict the realization of the comparative to just one overt maker:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%93
    \label{ex:key:93}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [\textsubscript{DEG} more ] brightØ
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [\textsubscript{DEG} Ø ] bright-er
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As regards adjectival inflections, this principle allows the realization of AGR on a sister node, the adjective in \REF{ex:key:94b}, and it rules out two strong endings in cases like \REF{ex:key:94c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%94
    \label{ex:key:94}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{da-s    kalt-e}Ø \textit{Wetter}    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{st} cold-\textsc{wk} weather.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the cold weather’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [\textsubscript{D} Ø ] \textit{kalt-es  Wetter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              cold-\textsc{st} weather.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘cold weather’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. *  \textit{da-s    kalt-es  Wetter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{st} cold-\textsc{st} weather.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the cold weather’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The weak ending in \REF{ex:key:94a} is assumed to be the “unmarked” inflection of the adjective \citep[44]{Olsen1991b}. Crucially, the strong ending can only be realized on the sister node, if D is empty \REF{ex:key:94b}; that is, the strong inflection can appear on the adjective (but not the noun, which cannot realize AGR). In order to rule out cases such as *\textit{ein-es kalte Wetter}, where \textit{ein} would have a strong ending in the three exceptional cases, Olsen (1991b: 47 fn. 14, 53) points out that \textit{ein} ‘a’ is part of a group of simple determiner stems (“schlichte Determinans-Stämme”) that in the case of \textit{ein}, have no inflection in the nominative masculine and in the nominative/accusative neuter. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As far as I am aware, this is the first detailed proposal of the strong/weak alternation in German within the framework of the DP-Hypothesis. While this is an elegant account, it is not without problems. Besides cases where AGR is present but does not have to be overtly realized (\textit{ein Auto} ‘a car’; \textit{Karls Auto} ‘Karl’s car’), unmodified mass nouns do not involve a DP-level in Olsen’s account, and thus no AGR is present. \citet[191]{Bhatt1990} and others have pointed out that this leads to the problem that these noun phrases have to enter DP-external agreement relations without AGR. Furthermore, while \citet[44]{Olsen1991b} states that one strong inflection in \REF{ex:key:94} exemplifies a tendency for economical realization of features or an avoidance of redundancy, it raises questions about strong endings that appear on stacked adjectives: \textit{frisch-es kalt-es Wasser} ‘fresh-\textsc{st} cold-\textsc{st} water’. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, note that instances such as \textit{dies-en Jahr-es} ‘of this-\textsc{wk} year-\textsc{gen}’ and non-canonical structures such as \textit{dies-es mein groß-es Glück} ‘this-\textsc{st} my great-\textsc{st} happiness’ raise issues. The first case involves a determiner with a weak inflection, which is unexpected in Olsen’s system. The second instance consists of two determiner elements where the leftmost element exhibits a strong ending, but the lower adjective also shows a strong inflection. Again, this is surprising under Olsen’s assumptions (for other problems, see \citealt{Wegener1995}: 159-63, \citealt{Bhatt1990}: 44). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.2.  \citet{Schoorlemmer2009}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Schoorlemmer proposes to account for the strong/weak alternation by employing certain aspects of the Agree relation \citep{Chomsky2000}. His novel claim is that the agreement relation between the adjective and the noun is not direct but rather indirect; that is, it is mediated by another element. In general terms, if this element is present and mediates the agreement between the adjective and the noun, the adjectival inflection is spelled out as strong. In contrast, if this agreement relation does not hold, then the features on the adjective are not valued/specified, and the inflection is spelled out as weak. The latter instance is interpreted as a default option. This account of agreement is labeled Indirect Agree.\footnote{Given the complexity of the system, I cannot do full justice to all the details of Schoorlemmer’s proposal. In what follows, I limit myself to the illustration of the basic account leaving many interesting features unexplored. However, it will become clear that there are also certain shortcomings in the basic parts of the proposal.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In Chomsky’s Agree system, probes have uninterpretable features and appropriate goals have the corresponding interpretable features. If the probe c-commands the goal, the former can value/specify its features on the basis of the latter. In Schoorlemmer’s terms, the probe and the goal share their features. Given that adjectival inflections are dependent on the phi-features of other elements, Schoorlemmer proposes that they are probes for nominal phi-features.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Specifically, adjectival inflections vary for gender and number. In addition, they may also exhibit properties of definiteness and case. The former (definiteness) is particularly clear in the Scandinavian languages and the latter (case) in German. Given that probes must c-command their goals, he suggests that adjectives are higher than nouns, which are specified for gender and number, but that adjectives are also higher than determiners, which are specified for definiteness (note that case is a DP-external feature). In other words, adjectives c-command not only nouns but also determiners. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Adjectives can also function as restrictive modifiers; that is, they participate in delimiting the reference of the noun phrase. As such, adjectives on their restrictive interpretation are often assumed to be in the scope of the determiner where scope is interpreted as the relevant c-command domain (for more discussion, see Chapter 4, \sectref{sec:key:4}). This leads Schoorlemmer to the following C-command Paradox:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%95
    \label{ex:key:95}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          Attributive adjectives with weak adjectival inflection must be c-commanded by a definite   D for their interpretation, but they must c-command a definite D in order to license their   inflection \citep[12]{Schoorlemmer2009}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It appears then as if there must be two positions for determiners, one above adjectives for interpretatory purposes and one below for agreement reasons. Schoorlemmer proposes to resolve this apparent paradox by assuming that the determiner moves from the lower position to the higher position.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The movement of the determiner is triggered by the presence of an adjective. Adopting the general framework of \citet{HeimKratzer1998} (see Chapter 1, \sectref{sec:key:4.2.2}), consider the simplified structure in \REF{ex:key:96}. Note now that the adjective (here assumed to be of type <<e,t>,<e,t>>) cannot combine with the lower DP (type <e>). The location of this type mismatch is indicated by underlining the incompatible elements. To resolve this mismatch, the determiner is moved to the left periphery:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textmd{(96)    DP}\textsubscript{<}\textmd{\textsubscript{e}}\textsubscript{>}\textmd{} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           det\textsubscript{<<e,t>,e>k}  DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             \textsubscript{} AP\textsubscript{<<e,t>,<e,t>>}    DP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     A       det\textsubscript{<<e,t>,e>k}         NP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           N
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

At PF, the lower copy of the determiner is deleted; at LF, the lower copy of the determiner and its projection are deleted \citep[27]{Schoorlemmer2009}. This is indicated by crossing out the relevant elements in \REF{ex:key:97}: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textmd{(97)    DP}\textmd{\textsubscript{<e>}}\textmd{} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           det\textsubscript{<<e,t>,e>k}  DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             \textsubscript{} AP\textsubscript{<<e,t>,<e,t>>}    DP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     A       det\textsubscript{<<e,t>,e>k}         NP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           N
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Observe that this leaves only one, the higher copy of the determiner at PF. This gives the right surface order. As for LF, with the lower copy of the determiner and its projection deleted, the adjective can now combine with NP semantically. This yields an element of type <e,t>, which in turn can combine with the (higher) determiner. This brings about the right semantics of the noun phrase. Basically, all noun phrases with an attributive adjective have this much in common.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Turning to the strong/weak alternation, recall that Schoorlemmer claims that adjectives and nouns do not enter into an agreement relation directly. Rather, this relation is mediated by another element; that is, the agreement relation is indirect. Importantly, for the adjective not to c-command the noun and thus enter into a direct agreement relation with it, the agreement relation is taken to be based on dominance. Specifically, considering the structure in \REF{ex:key:97}, the adjective does not dominate any of the nodes of the noun. I first discuss the strong inflections and then the weak endings.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Note again that Indirect Agree is a function of a mediating head. Mediating heads are proposed to be case-assigners such as little \textit{v}, T and presumably other elements. These heads have uninterpretable phi-features and thus function as probes too. For concreteness, suppose that \textit{guter Wein} ‘good wine’ is the subject of a clause and that the DP-external case-assigner is T. Assuming a null article (\textit{Ø\textsubscript{D}}), the DP in \REF{ex:key:98a} is assembled as in \REF{ex:key:98b}. After merging the DP in the clause, the mediating head T probes down the tree and finds the noun phrase \textit{guter Wein}. With the adjective the closest potential goal, T inspects the features on the adjective. However, the adjective does not have interpretable features, here marked as [-phi] (I comment on the strikethrough of [-phi] in \REF{ex:key:98} further below):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%98
    \label{ex:key:98}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{gut-er    Wein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    good-\textsc{st} wine.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘good wine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  DP  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textsubscript{} \textit{Ø\textsubscript{D}}\textsubscript{k}    DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             \textsubscript{} AP              DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textit{gut\textsubscript{[- phi]}}  \textit{Ø\textsubscript{D}}\textsubscript{k}                   NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 \textit{Wein\textsubscript{[+phi]}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As a consequence, the probe looks further down the structure and finds the noun. Since the noun does have the relevant interpretable features, both T and the noun enter into an Agree relation, and they share their features. Now, since T had entered into an agreement relation with the adjective before, T also shares its features with the adjective. With all its uninterpretable features licensed, the adjective is spelled out with a strong inflection. This is marked by crossing out the uninterpretable phi-features in \REF{ex:key:98}. To be clear, indirect agreement and feature sharing explain the strong endings (for more details, see \citealt{Schoorlemmer2009}: 145-46).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Turning to the weak endings, Schoorlemmer assumes that unlike null articles \citep[156]{Schoorlemmer2009}, overt articles are probes – just like adjectives and case-assigners; that is, overt articles have uninterpretable features. This has consequences for the DP-internal agreement relations. Specifically, when the determiner probe is merged with the noun, it inspects the interpretable features of the noun. Both enter into an Agree relation and share their features. As all the features on the determiner have been specified, the determiner becomes a deactivated probe. Next, the adjective is merged and due to type mismatch, the determiner moves to the left periphery:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%99
    \label{ex:key:99}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{der gut-e     Wein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the good-\textsc{st} wine.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the good wine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  DP  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          \textsubscript{} \textit{der\textsubscript{[- phi]}}\textsubscript{k}  DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             \textsubscript{} AP              DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textit{gut\textsubscript{[- phi]}} \textit{der\textsubscript{[- phi]}}\textsubscript{k}           NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 \textit{Wein\textsubscript{[+phi]}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

At this point, the DP is merged in the clause and T probes down the tree to find a goal. The closest goal is the determiner of the noun phrase. With its features already specified by the noun, the determiner deactivates T as probe. As both the determiner and T are deactivated, the features on the adjective remain unspecified. As a consequence, they are spelled out as a weak inflection by default. In a way, the determiner blocks the Agree relation between T and the adjective.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  This is, in a nutshell, Schoorlemmer’s basic proposal. It presents a new and interesting view on the strong/weak alternation. To account for all the basic data in German, Schoorlemmer (2009: chapter 5) has to make some other assumptions with regard to certain features. I do not review these finer points here. Rather, I point out again that this proposal makes some claims about the structure of the DP that are similar to the ones put forth in this book. Specifically, in Schoorlemmer’s account, the determiner is also merged in a low position and undergoes subsequent movement to the left periphery. Note though that there are some very general differences between Schoorlemmer’s and the current proposal. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

For instance, while it is the strong inflections in Schoorlemmer’s analysis that are due to a specific operation (Indirect Agree), it is the weak endings in the current account that are brought about by a certain mechanism (Impoverishment). Now, as illustrated in Sections 3 and 4, the strong endings occur in more diverse contexts than the weak ones. I interpreted the strong inflections as the elsewhere case. For Schoorlemmer, the presence of a mediating head is an essential ingredient to explain the strong adjectives. This raises the question as to what the mediating head in the non-canonical constructions, say vocatives in German, is.\footnote{If the Voc head discussed in \sectref{sec:key:3.9} is taken to be the relevant mediating head here, then this head is presumably absent in the Scandinavian languages, which have weak adjectives in vocatives.} In fact, some of the complex noun phrases, discussed in \sectref{sec:key:3}, involve more feature sharing relations than there seem to be mediating heads (e.g., \textit{das Sternbild Groß-er Bär} ‘the constellation Great-\textsc{st} Bear’). Again, it is not clear how strong adjectives in the different sub-structures of these nominals can be explained. In my view, these cases can be straightforwardly handled if we assume that the strong inflections involve the elsewhere case. After these general remarks, I point out some more specific issues with Schoorlemmer’s account.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To motivate the movement of the determiner, semantic types are interpreted as part of the syntactic feature bundles. It appears then as if semantic elements (types) and semantic operations (Functional Application) are either relocated into syntax or duplicated there (but see also \citealt{Schoorlemmer2012}). Moreover, it is not entirely obvious that little \textit{v}, T and other mediating heads have the full range of uninterpretable phi-features (e.g., gender). Finally, while I cannot discuss this in detail here, it appears that Schoorlemmer’s system works best for the Scandinavian languages but is less straightforward for German. For instance, the mixed pattern involving \textit{ein}{}-words in German is left for future research, and the cross-linguistic differences observed in canonical noun phrases involving possessives and demonstratives (Chapter 1, \sectref{sec:key:2.1.2}) do not follow from this uniform account.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.3.  \citet{Murphy2018}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet{Murphy2018} discusses the emergence of the strong ending on \textit{ein}{}-words in elliptical contexts; compare \REF{ex:key:100a} to \REF{ex:key:100b} (strikethrough marks ellipsis here):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%100
    \label{ex:key:100}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein (gut-es)  Buch}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a     good-\textsc{st} book.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a good book’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{ein-es Buch}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a-\textsc{st}    book
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

He relates this change to the strong/weak alternation of adjectives. Rather than claiming that a strong ending licenses NP-ellipsis, here the strong ending is proposed to be a by-product of NP-ellipsis. Specifically, the latter creates a stranded affix that undergoes Local Dislocation onto a non-canonical element, the \textit{ein}{}-word. Murphy assumes the structure in \REF{ex:key:101}.\footnote{Besides \textit{$\varphi $} residing in a higher position than the adjective, the adjective can also be above \textit{$\varphi $}. These two options are important for the derivation of all the cases involving ellipsis. In this regard, Murphy assumes that \textit{$\varphi $} may induce ellipsis of its complement. Now, if AP is deleted (when \textit{$\varphi $} is above A as in \REF{ex:key:101} in the main text), \textit{eines gut Buch} ‘one (good book)’ comes about; if \textit{n}P is deleted (when A is above \textit{$\varphi $} – the alternative ordering of \REF{ex:key:101}), \textit{ein gutes Buch} ‘a good one’ is brought about (for \textit{eines Buch} ‘one (book)’, see the main text below). Furthermore, note that the option of the adjective residing above \textit{$\varphi $} also accounts for some special inflectional features of adjectives like \textit{lila} ‘purple’ (see also Chapter 3, \sectref{sec:key:3}).} In his account, inflections and their hosts do not form constituents; that is, inflections are generated separately from their hosts: determiners are assumed to be in Spec,DP and adjectives in A, but strong endings are taken to be located in D and weak inflections in \textit{$\varphi $}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%101
    \label{ex:key:101}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          [ [ DET ] D [ \textit{$\varphi $} [ A [ \textit{n} [ N ]]]]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         {\textbar}     {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                      \textsc{st}  \textsc{wk}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In other words, inflections are part of the extended projection line of nouns (for similar ideas, see \citealt{Evans2019}: 76, \citealt{Rehn2019}: 194, \citealt{Wiltschko1998}). Murphy formulates a requirement that (basically) all adjectives must have an inflection in prenominal position. In order to combine the ending with the adjective or determiner, he follows assumptions from Distributed Morphology (see Chapter 1, \sectref{sec:key:4.2.1}), where Lowering precedes Local Dislocation. Specifically, he makes the suggestion that if (downward) Lowering is not possible, (leftward) Local Dislocation takes place (note that Local Dislocation is conceived of here as “leaning” to the left, \citealt{Murphy2018}: 346). I illustrate the proposal by way of discussing four derivations involving \textit{ein}{}-words.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I begin by discussing the most common case, where a determiner with a strong inflection precedes an adjective with a weak ending. This is exemplified by an instance in the dative neuter in \REF{ex:key:102a}. The weak ending under \textit{$\varphi $} combines with the adjective by Lowering, marked by a downward pointing arrow in \REF{ex:key:102b}. With the weak ending originating under \textit{$\varphi $}, the strong ending in D cannot undergo Lowering. Consequently, it combines with \textit{ein} by Local Dislocation. This is indicated by an upward pointing arrow (despite the fact that this mechanism operates on lineary adjacency). This derives \REF{ex:key:102a} as in \REF{ex:key:102b} below:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%102
    \label{ex:key:102}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein-em gut-en     Buch}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a-\textsc{st}     good-\textsc{wk} book.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a good book’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.   DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{ein}         D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       D      \textit{$\varphi $}P
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{}-em %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        \textit{$\varphi $}        AP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{{}-en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          A      \textit{n}P
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        \textit{gut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             \textit{n}      NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{Buch}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To account for the idiosyncracy of the three special cases of \textit{ein}{}-words, where the adjective is strong \REF{ex:key:103a}, it is stipulated that the strong ending is in \textit{$\varphi $} (103b), rather than in D \citep[356]{Murphy2018}. Recalling that Lowering precedes Local Dislocation, the strong ending is displaced onto the adjective deriving \REF{ex:key:103a} as in \REF{ex:key:103b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%103
    \label{ex:key:103}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein gut-es    Buch}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a    good-\textsc{st} book.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a good book’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.   DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{ein}         D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       D      \textit{$\varphi $}P
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        \textit{$\varphi $}       AP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{{}-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          A      \textit{n}P
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        \textit{gut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             \textit{n}      NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{Buch}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Removing the adjective from the noun phrase yields \REF{ex:key:104a}. As just seen, the strong ending undergoes Lowering but this time onto \textit{n} (104b). The result is spelled out as null (marked by parentheses below):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%104
    \label{ex:key:104}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein Buch}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a    book.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a book’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.   DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{ein}         D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       D      \textit{$\varphi $}P
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        \textit{$\varphi $}       \textit{n}P
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 (\textit{{}-es})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          \textit{n}      NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{Buch}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally and most importantly, I turn to the cases where the noun is elided \REF{ex:key:105a} (for the ellipsis involving adjectives, see Footnote Error: Reference source not found). With \textit{n}P undergoing ellipsis, the strong ending cannot undergo Lowering. Rather, Local Dislocation applies as a kind of repair mechanism, and the strong ending surfaces on \textit{ein} \REF{ex:key:105b} (the arch below sets off the elided material):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%105
    \label{ex:key:105}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

one-\textsc{st}     
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.   DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{ein}         D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       D      \textit{$\varphi $}P
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        \textit{$\varphi $}       \textit{n}P
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{{}-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          \textit{n}      NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textit{Buch}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This is a novel proposal accounting for the emergence of the strong ending on \textit{ein}. As such, it is a welcome contribution.\footnote{There are other formal proposals seeking to explain the emergence of the strong inflection on \textit{ein} (for the discussion of \citealt{Evans2019}: chapter 3, see \citealt{Roehrs2021}).} Note though that not all aspects of the proposal are entirely straightforward.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To obtain the correct surface forms, a number of adjustments have to take place. In particular, there are three instances where an inflectional suffix is spelled out as null: (i) as seen above, if a strong ending combines with \textit{n} by Lowering, it is spelled out as null yielding \textit{ein Buch} ‘a book’, (ii) if both D and \textit{$\varphi $} have an inflection but there is no adjective present in elliptical contexts, the weak ending in \textit{$\varphi $} is spelled out as null bringing about \textit{mein-em} ‘mine’ (Murphy’s page 358), and (iii) if Spec,DP is empty, the strong ending in D appears on the adjective, and the weak ending in \textit{$\varphi $} is spelled out as null resulting in \textit{heißer Kaffee} ‘hot coffee’ (his page 362).\footnote{The difference between the weak inflection being spelled out as null in (ii) and (iii) is that in (ii), the weak ending undergoes – what Murphy calls – Morphological Ellipsis, and that in (iii), the weak ending is contextually spelled out as null (as Spec,DP is empty). In my view, allowing these adjustments opens the door for other issues. Assuming the structural option with A above \textit{$\varphi $} (Footnote Error: Reference source not found), it is not clear how to rule out the following ungrammatical example in the dative (where the strong ending is in D):(i)  *  \textit{ein gut-em   Buch}    a    good-\textsc{st} book.\textsc{neut}With Lowering taking precedence, the strong ending (in D) combines with the adjective. Given that the weak ending in \textit{$\varphi $} can, at least in principle, be spelled out as null, it is not clear to me how to rule this case out without stipulation. In my view, these issues are avoided if adjectives and inflections do not form individual heads in the extended projection line of the noun (but rather inflections are part of the structures of the determiners and adjectives themselves, as assumed here).} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that Murphy’s proposal is not compatible with the current analysis as all his determiners including \textit{ein}{}-words are in Spec,DP, and adjectives are in head positions. More importantly for current purposes, there are not many details provided as to what regulates the strong/weak alternation. While the Principle of Monoinflection is mentioned, it is not clear what derives (rather than stipulates) the fact that the strong ending must precede the weak one(s). From Murphy’s mechanism of partial copying, it is clear though that weak endings are taken as impoverished features that involve a subset of the features that spell out the strong endings. To be fair, while Murphy’s goal was not to provide those types of details, such information along with vocabulary insertion rules would be desirable. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is also worth pointing out that there are two ways for adjectival endings to come about in Murphy’s account: besides originating in D or \textit{$\varphi $}, strong and weak endings can also be the result of copying (accounting for multiple inflected adjectives). Furthermore, note that the stipulation to capture the idiosyncracy of the three special \textit{ein}{}-words (i.e., the strong ending is exceptionally located in \textit{$\varphi $}) may involve a non-local relation when the adjective intervenes (i.e., when A is above \textit{$\varphi $}, which is the required ordering to generate \textit{ein gutes Buch} ‘a good one’). This means that selection cannot be invoked here. Moreover, the three cases of \textit{ein} do not seem to have anything in common (unlike in the current account where they share features with a fourth instance of \textit{ein}).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Finally, I turn to two empirical issues that arise with non-canonical structures. First, split topicalizations are analyzed as either NP-ellipsis or NP-movement. However, it is not clear how to account for the two strong endings in \REF{ex:key:106a}. As plural forms, these types of \textit{ein}{}-words do not involve a strong ending in \textit{$\varphi $} (but only in D). It is clear that a second D must be present in the higher nominal. However, this is unexpected on NP-ellipsis or NP-movement. Second, pronominal DPs like \REF{ex:key:106b} present another challenge: although D is present here, the strong inflection is absent. It appears as if the latter is, for some reason, not spelled out. Note that this strong ending did not combine with \textit{n} by Lowering as the adjective and \textit{$\varphi $} are present:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%106
    \label{ex:key:106}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Nett-e   Leute   waren kein-e da}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    nice-\textsc{st} people were   no-\textsc{st}  there
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for nice people, there were none there.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.    \textit{wir nett-en  Studenten} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

we nice-\textsc{wk} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘we nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up, the three proposals reviewed in this section discuss the canonical patterns with different degrees of success and explanatory force. All accounts have an explanation of the strong inflections, but the status of the weak endings is less clear: this type of ending is taken to be the “unmarked” inflection (Olsen), the default inflection (Schoorlemmer), or it is base-generated or due to a certain type of partial copying (Murphy).\footnote{G. \citet[129]{Müller2002a} makes an explicit distinction between markers of case (strong inflection) and agreement (weak inflection).} Recall in this regard that there are actually two weak endings, -\textit{e} and -\textit{en}, and that their distributions do not relate to natural groups. In my view, it is important to account for the distribution of the weak inflections as well. In the current account, the strong and weak inflections have the same status, the difference being that the (traditional) weak inflections are less specified than the (unambiguous) strong endings. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

More importantly, none of these proposals address the non-canonical cases, and as pointed out above, it is not clear how some of these instances can be accounted for by those analyses. As such, while these proposals are quite elegant, their empirical coverage is somewhat limited. As mentioned above, in my view, empirical coverage should be taken into account when evaluating the plausibility of a proposal. Indeed, one of the main claims of this book is that it is these non-canonical cases that reveal the true nature of adjectival endings in German. While this may turn out to be wrong, all analyses should eventually discuss these non-canonical structures to explore the consequences of those analyses for these more complex structures.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

6.  Conclusion
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

One goal of this book is to provide a detailed survey of the strong/weak alternation of adjectives in German and to determine the exact conditions for the emergence of the weak and strong endings. In the course of the discussion, I isolated one structure, the simple (canonical) DP, where weak endings occur. Illustrating that concord is a necessary (but not a sufficient) condition, it was proposed that the weak endings are underlyingly fully specified feature bundles that get reduced by Impoverishment. Impoverishment is triggered by determiners, which move across the specifier positions containing the adjectives. \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I also investigated nine contexts where strong endings surface. In each case, some independent evidence was provided that suggests that different structures are indeed involved. Importantly, arguing that Impoverishment proceeds locally and in a bottom-up fashion, I suggested that in these different structures, the relevant feature bundles remain unreduced and are spelled out as the strong endings. In that sense, the strong endings are the elsewhere case. This explains their diverse occurrences. The previous proposals discussed in the final section of this chapter do not discuss the whole range of data and as a consequence, reach very different conclusions. Another goal of this book is to draw some more general conclusions. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In this chapter, I showed that adjectival inflections in German do not correlate with (in-)definiteness. I proposed that these inflections are semantically vacuous (Hypothesis 1a). I argued that the strong/weak alternation is a reflex of different structures (Hypothesis 2a): on the one hand, weak endings can only occur in Spec,AgrP; on the other hand, strong inflections can be in Spec,AgrP, in DP, and in other, non-canonical positions (e.g., LPP, Spec,DisP, etc.). If this is on the right track, then we can utilize the strong/weak alternation as a diagnostic for the structures of other nominals. With this in mind, I turn to some consequences of the analysis above for the current account in Chapter 3 and to some consequences for other proposals in Chapter 4. The discussion of these consequences will reveal other properties of adjectival inflections.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\setcounter{page}{124}\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Chapter 3: Variation and Secondary Mechanisms
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.   Introduction
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This chapter and the next continue the discussion of adjectival inflections begun in the previous chapter. While the current chapter focuses on consequences for and extensions of the present proposal, the next chapter discusses implications for other analyses. In more detail, this chapter addresses data involving variation and how this variation can be accounted for in the current system. I examine two types: (i) variation within colloquial Standard German and (ii) variation as regards different regional dialects with special focus on Mannheim German. I provide a brief overview of the relevant cases before I engage in a more detailed discussion.\footnote{It is not an easy task to explain variation, especially optionality, in a formal system. Thus, while certain parts of the discussion below are admittedly somewhat tentative, I discuss new data and offer novel (and I hope, interesting) generalizations and proposals.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The first type of variation involves both canonical and non-canonical constructions. In canonical DPs, strong, weak, or no inflections are possible on adjectives and determiners. This variation can only occur in certain structures. Strong or weak inflections are possible on the second adjective of two stacked modifiers when a determiner is missing, on adjectives in pronominal DPs, and on certain \textit{der}{}-words. Strong or no inflections occur on certain other \textit{der}{}-words (e.g., \textit{d-er} vs. \textit{die} both meaning ‘the/that’). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Importantly, this type of variation only surfaces in specific, well-defined featural contexts in these structures; that is, this variation within colloquial Standard German is restricted to certain combinations of “case + gender” or “case + number”. I argue that there is another mechanism besides Impoverishment: a phonetic rule. Furthermore, the application of Impoverishment Rule 2 is extended, a phonological constraint (avoidance of a hiatus) is held to explain certain unexpected determiner forms, and Impoverishment is proposed not to be triggered by pronominal determiners. In addition, I address the optional inflections on certain adjectives (e.g., \textit{lila} ‘purple’), and I discuss the absence of inflections on some other adjectives (e.g., \textit{prima} ‘great’).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  There is also variation within colloquial Standard German in non-canonical structures. In this respect, I discuss the inflectional options on the predeterminer \textit{all-} ‘all’, where in the plural, the ending can be present or absent in all four morphological cases. All these constructions, their restricted variation, and their analyses are briefly summarized in \tabref{tab:key:1} (\textsc{infl} indicates where the variation occurs):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Restricted Variation in Colloquial Standard German}
\label{tab:key:1}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 Construction & Ending & Restrictions & Analysis \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ adj + adj-\textsc{infl} + noun} & weak (unexpected) & only in dative masc/neut (in sgl) & phonetic rule\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ det-\textsc{infl} + noun} & weak (unexpected) & { only in genitive masc/neut (in sgl); certain \textit{der}{}-words} & Impoverishment Rule 2\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ det-\textsc{infl} + noun} & absent & { only in nom/acc in fem/pl; \textit{die} ‘the/that/those’}  & deletion of inflection
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(avoid hiatus)\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ det + adj-\textsc{infl} + noun}  & (optional/) absent & { lexically restricted to a few adjectives (e.g., \textit{lila} ‘purlple’)} & (optional) deletion of inflection\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ pron + adj-\textsc{infl} + noun} & strong (unexpected) & (expected) weak endings only occur in dat sgl and nom pl & ST: pron. det.; WK: phonetic rule, analogy \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ predet-\textsc{infl} noun} & optional & { \textit{all}{}- ‘all’} & Deletion of inflection in LPP\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As part of the discussion of the absence of inflections on \textit{die} ‘the/that/those’, I also address the different stem forms of the definite article and the almost homophonous distal demonstrative (i.e., \textit{de}{}-\textit{r}, \textit{da}{}-\textit{s}, \textit{di}{}-), and I relate the distal and proximal demonstratives to the definite article. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Studying \tabref{tab:key:1}, we note that some variation is found in the oblique cases in masculine and neuter contexts and that other variation occurs in the structural cases in the feminine and plural. Indeed, the workings of the phonetic rule and Impoverishment Rule 2 are restricted to certain constructions and specific featural combinations making them different from Impoverishment Rule 1. Consequently, I conclude below that Impoverishment Rule 1 is the primary mechanism and that the other two are secondary ones. Based on the discussion of this variation, I review the traditional generalizations of the strong/weak alternation.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The second type of variation concerns regional varieties, here focusing on one dialect – Mannheim German (for some brief remarks on Alemannic German, see Chapter 8, \sectref{sec:key:2.1}). This variation can be captured in the current system: while it involves Impoverishment Rule 1 as formulated in Chapter 2, some of the vocabulary insertion rules provided there are deleted here or have a different featural context of application and, as a consequence, are reordered. These adjustments to the vocabulary insertion rules account for the inflectional distribution in this dialect. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  More generally, I propose in this chapter that adjectival inflections in German do not receive a homogenous account but rather are the result of (at least) three different mechanisms, one primary (Impoverishment Rule 1) and two secondary ones (phonetic rule, Impoverishment Rule 2). Furthermore, this chapter provides more evidence that adjectival inflections do not mark (in-)definiteness; that is, they are semantically vacuous elements (Hypothesis 1a). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The chapter is organized as follows. First, I turn to instances involving different inflections on two unpreceded adjectives. \sectref{sec:key:3} is dedicated to the inflectional properties of certain adjectives and certain \textit{der}{}-words, and to the stem forms of the definite article and the demonstratives. In \sectref{sec:key:4}, I take up the variation of inflections on certain other \textit{der}{}-words. \sectref{sec:key:5} discusses the variation in pronominal DPs. In \sectref{sec:key:6}, I address the presence or absence of the strong inflection on the predeterminer \textit{all-} ‘all’. \sectref{sec:key:7} returns to the discussion of the traditional generalizations Weak After Strong and the Principle of Monoinflection. In \sectref{sec:key:8}, I provide a comparison between Standard German and the German dialect of Mannheim, and \sectref{sec:key:9} closes the chapter with a summary of the main inflectional patterns in colloquial Standard German and their respective analyses. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.  Canonical DPs with Unexpected Weak Adjectives: Phonetic Rule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I start the investigation with the variation that has seen the most interest in the theoretical literature. As such, I begin by discussing noun phrases that lack an overt determiner but involve two adjectives in a row where the first adjective exhibits a strong ending, but the second shows a weak one. This inflectional alternation concerns nasal sounds, and I provide a phonetic rule suggesting that this involves phonetic markedness reduction. In the second subsection, this discussion is extended to indefinite pronoun constructions.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.  Two Adjectives Without a Determiner
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the previous chapter, we saw examples where two co-occurring adjectives have the same ending, with either both endings being strong \REF{ex:key:1a} or both endings being weak \REF{ex:key:1b}: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%1
    \label{ex:key:1}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a. \textit{frisch-er schwarz-er Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    fresh-\textsc{st}  black-\textsc{st}     coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘fresh black coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{d-er     frisch-e schwarz-e Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{st} hot-\textsc{wk}  black-\textsc{wk}  coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the fresh black coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that \textit{der} ‘the’ in \REF{ex:key:1b} involves [+DEF], a context where [+D] triggers Impoverishment. With the determiner moving from ArtP to DP in a successive-cyclic fashion, the CNG features of the two adjectives undergo Impoverishment and are spelled out as weak. As for \REF{ex:key:1a}, Impoverishment does not occur, and the underlying features are spelled out as the strong endings.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  There is one much-discussed exception to the pattern in \REF{ex:key:1a} above: in the dative masculine (and neuter), there is the option of the second adjective exhibiting a weak ending. Comparing (2a-b), we observe that \REF{ex:key:2a} exhibits the expected pattern but that \REF{ex:key:2b} shows two adjectives in a row that have different endings. The latter is often referred to as non-parallel distribution. This is in stark contrast to all the other patterns I discussed above. As is clear from (2c-d), patterns with a weak ending on the first adjective are not possible (also \citealt{Nübling2011}, \citealt{Sahel2021}), independently of whether the second adjective is strong or weak. I call the inflectional option of -\textit{em} vs. -\textit{en} nasal alternation. Note also that contrasting the examples involving the different nasal inflections yields less sharp grammaticality judgments (\% indicates variation with speakers; see \tabref{tab:key:4} below):\footnote{\citet[182]{Nübling2011} states that the coexistence of the parallel and non-parallel adjective strings has a long history, possibly several hundred years. Note that this co-occurrence seems to have existed already in ENHG (see Ebert \textit{et al}. 1993: 201-02). However, it is hard to tell when this phenomenon started as there is some independent variation with adjectives in the earlier stages of German (e.g., \citealt{Peter2013}, \citealt{Sahel2021}). \citet{Sahel2021} observes that \REF{ex:key:2b} is the only non-parallel string that continues occurring after the 18\textsuperscript{th} century.  Also, \citet[183]{Nübling2011} finds in her corpus study that the parallel string occurs slightly more often (57\%) and that all classes of adjectives have this variation (albeit adjectives in coordinations much less so; more on this in Footnote Error: Reference source not found). Interestingly, there is a slight correlation such that adjective classes that occur closer to the noun (in the nominal extended projection) tend to be inflected weak more often than those further away (her page 187), provided they are preceded by a (strong) adjective. The corpus studies by \citet{Peter2013} and \citet{Sahel2021} offer partially different results, but both Peter and Sahel point out that non-parallel strings seem to be on the increase.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \REF{ex:key:2} a.  \%  \textit{mit   frisch-em schwarz-em Kaffee}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with fresh-\textsc{st}   black-\textsc{st}      coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with fresh black coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \%  \textit{mit   frisch-em schwarz-en Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with fresh-\textsc{st}   black-\textsc{wk}    coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with fresh black coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  *?  \textit{mit   frisch-en schwarz-em Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with fresh-\textsc{wk} black-\textsc{st}      coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  *?  \textit{mit   frisch-en schwarz-en Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with fresh-\textsc{wk} black-\textsc{wk}    coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet[290]{Sternefeld2004} points out that the alternation in (2a-b) is also possible with Saxon Genitives:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%3
    \label{ex:key:3}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Johanns  gut-em   alt-em Wein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Johann’s good-\textsc{st} old-\textsc{st} wine.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Johann’s good old wine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{Johanns  gut-em   alt-en   Wein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Johann’s good-\textsc{st} old-\textsc{wk} wine.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Johann’s good old wine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As in the canonical cases (e.g., \textit{d-em frisch-en Kaffee} ‘the-\textsc{st} fresh-\textsc{wk} coffee’), the strong ending precedes the weak one yielding a left-to-right asymmetry in \REF{ex:key:2b} and \REF{ex:key:3b}. Notice also that the weak ending in \REF{ex:key:2b} and similar instances below cannot mark definiteness.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet[38]{Duden2007} claims that the weak adjective occurs most frequently when the second adjective and noun form a whole concept (“Gesamtbegriff”). \citet{Nübling2011} suggests that the weak adjective strengthens the tendency to mark the left bracket in the noun phrase (cf. the sentence bracket in German). However, \citet[9-14]{Sahel2021} argues convincingly that there is no semantic or functional difference between the two variants. Having said that, these patterns do have a number of peculiar properties. First, as just mentioned, the grammaticality judgments are less sharp than in the cases discussed in the previous chapter. Second, these inflectional distributions are restricted to the two instances above (i.e., dative masculine/neuter). For instance, a weak ending on a second adjective is not possible in the feminine gender:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%4
    \label{ex:key:4}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{mit   gut-er    rot-er  Sauce}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with good-\textsc{st} red-\textsc{st} sauce.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘with good red sauce’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. *  \textit{mit   gut-er    rot-en   Sauce}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with good-\textsc{st} red-\textsc{wk} sauce.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Third, as noted in \citet{Roehrs2009b}, different authors have reported different possibilities for (2a-b). I refer to this inter-speaker variation as dialects. Considering the two inflectional options in \tabref{tab:key:2} (column 1), Gallmann (1996: 296, 2004: 156), \citet[119]{Schlenker1999}, and \citet[53]{Demske2001} describe dialect 1 where a strong or a weak ending is equally possible on the second adjective. G. \citet[139]{Müller2002a} discusses dialect 2 where the strong ending on the second adjective is preferred. Finally, \citet{Schlenker1999} reports that some speakers have dialect 3 where the weak ending on the second adjective is strongly preferred. Dialect 4 does not exist. In other words, all speakers have at least one of the two inflectional distributions in their language:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Different Dialects for the Dative Masculine/Neuter}
\label{tab:key:2}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

String & Dialect 1 & Dialect 2 & Dialect 3 & Dialect 4\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Adj+\textit{em} Adj+\textit{em} & ${\surd}$ & ${\surd}$ & ?? & {}-\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Adj+\textit{em} Adj+\textit{en} & ${\surd}$ & ? & ${\surd}$ & {}-\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The weak ending -\textit{en} cannot be the result of Impoverishment. Recall that Impoverishment is a local process that occurs within a phrase (AgrP). Non-local effects are brought about by determiner movement (Impoverishment Rule 1) or a specific featural context (Impoverishment Rule 2). Both options affect all adjectives in the same way. In the cases discussed here, the weak ending occurs only on the second adjective and seems to be parasitic on the presence of the first adjective. I return to this statement in \sectref{sec:key:2.2}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  An account of this alternation should answer the questions as to why this is only possible with adjectives ending in -\textit{em} but not, for instance, with adjectives in -\textit{es} instead or why this alternation is not possible with adjectives in -\textit{em} and -\textit{es}. In this regard, \citet[19-20]{Sahel2021} points out that -\textit{m} and -\textit{n} have the most similarities as compared to all other strong/weak inflectional pairs (e.g., -\textit{er} vs. -\textit{en} as in (4a-b)). In order to explain the above properties, I proposed in \citet{Roehrs2009b} that the alternation in (2a-b) and (3a-b) follows from a phonetic rule, according to which the inflection on the second adjective is changed from -\textit{m} to -\textit{n} (+ indicates a morpheme boundary; \# signifies a word boundary):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%5
    \label{ex:key:5}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Rule (preliminary version):}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

m  →  n  / […]\textsubscript{A} […]\textsubscript{A} +\_\_\_\_\#  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This rule turns a labial sound into its coronal (/alveolar) counterpart; that is, the place of articulation of this inflection is altered. Note in this regard that R. \citet[165]{Wiese1996a} states that “alveolar is taken to be the default place of articulation for consonants”. Utilizing this, R. Wiese formulates rules where coronal is the default value (his page 219). In a similar vein, de \citet[35-42]{Lacy2006} argues that coronal is a less marked place of articularion than labial or dorsal. Specifically, -\textit{n} is considered the unmarked nasal sound as compared to -\textit{m}. I suggest that the rule above is a reflex of markedness reduction, where a more marked sound is changed into a less marked sound (e.g., de \citealt{Lacy2006}: 23, 75-76). For similar explanations of the data in (2a-b), see also \citet[78-79]{Evans2019} and \citet{Sahel2021}.\footnote{In an earlier version of this work, I took this rule to involve phonetic simplification, presumably a reflex of ease of articulation. Joe Salmons (p.c.) pointed out to me that there are issues with such an idea (see \citealt{Salmons2021}: 39-41, also references cited therein). He and Tracy Hall suggested to me that reduction in markedness might be more appropriate. In this vein, note that \citet[156]{Gallmann2004} formulates a phonological markedness constraint in his optimality-theoretic proposal that bans German words that end in schwa + /m/ (cf. also Gunkel \textit{et al}. 2017: 1308).  Note that -\textit{m} and -\textit{n} are diachronically and dialectally related. Braune \& \citet[120-22]{Reiffenstein2004} point out for OHG that diachronically -\textit{m} changes to -\textit{n} in suffixal endings (if \textit{n} changes, it tends to change to \textit{l}). Paul \textit{et al}. (1989: 146) show for MHG that final -\textit{m} also changes to -\textit{n}, except with strong adjectival endings. In certain dialects, though, specifically in Middle Franconian, adjectival -\textit{em(e)} is changed to -\textit{en} in dative masculine/neuter contexts (Paul \textit{et al}. 1989: 211-12). ENHG also shows the relatedness of these two elements with certain nouns changing their final sound (e.g., \textit{besem} > \textit{besen} ‘broom’), but there is also general variation with articles, pronominal elements, and adjectives (Ebert \textit{et al}. 1993: 140, 192-93). With this in mind, we might expect that these two sounds are also dialectically related. This is borne out in that contemporary Southern Hessian dialects retain \textit{Besem} for \textit{Besen} ‘broom’ \citep[433]{Schirmunski2010}. In my view, this diachronic and dialectal relatedness of -\textit{m} and -\textit{n} strengthens the proposal that the nasal alternation is a phonetic/phonological phenomenon (also \citealt{Peter2013}, \citealt{Sahel2021}: foontoe 6). I thank Tracy Hall, Joe Salmons, and Laura Smith for help with this section including providing references.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Returning to \tabref{tab:key:2}, the application of the rule is optional in dialect 1, “costly” in dialect 2, and obligatory in dialect 3.\footnote{The grammaticality judgments in \tabref{tab:key:2} are taken from the original sources. It is not clear to me if the degree of the markedness/ungrammaticality of the respective bad patterns in dialect 2 or 3 is the same. If the relevant case in dialect 2 were marked more than “?”, we could suggest that the rule in \REF{ex:key:5} simply does not apply in that dialect at all.} Thus, this rule accounts for the unexpected weak adjectives in canonical DPs of dialects 1 and 3. Given this rule, non-nasal inflections are immediately excluded explaining why this pattern is so restricted in occurrence. The left-to-right asymmetry follows from the way the rule above is stated – only the second adjectives may undergo the phonetic change.\footnote{Besides \citet{Nübling2011}, \citet[194]{Peter2013} points out that parallel inflections are much more frequent in coordinations, which includes two adjectives separated by a comma. Specifically, parallel inflections occur 39\% in non-coordinations vs. 79\% in coordinations. Björn Köhnlein (p.c.) makes an interesting observation in this regard. In non-coordinated noun phrases, main phrasal stress usually goes to the head noun, but there is a secondary stress on the initial element (this is probably determined rhythmically). In the current cases, the initial element is the first adjective. This means that the second adjective~has the lowest degree of prominence. This low degree of prominence may facilitate the reduction from -\textit{m} to -\textit{n}. In contrast, if there is a pause between the two adjectives as in coordinations, then the second adjective is not in a weak prosodic position but tends to have its own stress. This may make the retention of -\textit{m} more likely.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There are two possible alternatives: First, the left-to-right asymmetry might also have to do with easier parsing because a disambiguating, strong ending occurs first in the nominal string. Matthias Schlesewsky (p.c.) points out to me that this claim about more effective processing is in agreement with work by John Hawkins. In particular, Hawkins (1994: 404-05; 2004: 49, 92-93) explains the left-to-right asymmetry seen with the examples above by general processing advantages (for an early intuition in this regard, see \citealt{Esau1973}: 139). As a second alternative, we could follow G. \citet[140]{Müller2002a} claiming that this asymmetry has to do with analogy with the canonical pattern \textit{d-em nett-en Mann} ‘the-\textsc{st} nice-\textsc{wk} man’. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The cases in the next subsection present a challenge to both of these alternatives. Indeed, what often goes unmentioned is that this nasal alternation is more general; for instance, it can also be found with indefinite pronoun constructions (and pronominal DPs, see \sectref{sec:key:5}). In my view, these are related phenomena that should be discussed in the same context and receive the same account (note again that all other authors only discuss the contrast in \REF{ex:key:2}, with the qualification that \citealt{Sternefeld2004} also discusses \REF{ex:key:3}). Unlike the two alternatives, the phonetic rule above is able to capture the above-mentioned cases too once it is slightly modified. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.  Indefinite Pronoun Constructions Revisited
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In Chapter 2, \sectref{sec:key:3.2}, I discussed indefinite pronoun constructions. To extend this discussion, consider cases involving the pronoun \textit{etwas} ‘something’ and a following adjective. In either the nominative/accusative (6a-b) or the dative (6c-d), there is no variation – only a strong adjective is possible:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%6
    \label{ex:key:6}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{etwas                  ander-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    something.\textsc{neut} different-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘something different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. *  \textit{etwas                  ander-e}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    something.\textsc{neut} different-\textsc{wk}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{mit   etwas                  ander-em}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with something.\textsc{neut} different-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘with something different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  ?*  \textit{mit   etwas                  ander-en}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with something.\textsc{neut} different-\textsc{wk}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It was proposed in the previous chapter that the nominative string \textit{jemand anderer} ‘someone different’ is analyzed as in \REF{ex:key:7}. I now add \REF{ex:key:6a} to the analysis (recall that IPRP stands for Indefinite Pronoun Restrictor Phrase; for the internal makeup of indefinite pronouns, see also Footnote Error: Reference source not found):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%7
    \label{ex:key:7}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Indefinite Pronoun Construction}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    D      IPRP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      IPRP             ModP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    IPR    NP  Mod        AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      {\textbar}      {\textbar}             {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textit{je}      \textit{{}-mand}     \textit{e\textsubscript{N}}\textsubscript{} [\textit{anderer} \textit{e\textsubscript{N}}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textit{et}      \textit{{}-was}     \textit{e\textsubscript{N}}\textsubscript{} [\textit{anderes} \textit{e\textsubscript{N}}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given right adjunction to the pronoun, the adjective is only expected to have a strong ending. However, \citet[491]{Duden2007} points out that there is variation of the adjective in the dative when the adjectives follows \textit{jemand} ‘someone’ and \textit{niemand} ‘no one’, and the same goes for the two pronouns themselves.\footnote{There is also variation with indefinite pronouns in the accusative (e.g., \textit{jemanden} vs. \textit{jemand}), but the following adjective always ends in the expected inflection -\textit{en}. Furthermore, as far as I know, there is no variation with the dative pronoun \textit{ihm} ‘him’; in other words, this pronoun cannot appear as \textit{ihn} in dative contexts. In addition, this element is not part of the discussion of inflections on adjectives, as third-person pronominal elements cannot be followed by adjectives (\sectref{sec:key:5.1}). Given these remarks, the cases mentioned in this footnote are not relevant to the discussion of the nasal alternation.} I start by discussing the pronoun \textit{jemand} in more detail. This is followed by addressing the inflections on the adjective.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Although \textit{jemand} ‘someone’ is the first element in the noun phrase, this pronoun shows the nasal alternation (8a-b). In addition, it can also occur without an ending \REF{ex:key:8c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%8
    \label{ex:key:8}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{mit}   \textit{jemand-em}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

with someone.\textsc{masc}{}-\textsc{st}    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘with someone’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mit}   \textit{jemand-en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

with someone.\textsc{masc}{}-\textsc{wk} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘with someone’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{mit}   \textit{jemand}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

with someone.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘with someone’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The same options can be found with \textit{niemand} ‘no one’. It is clear that this inflectional alternation is not due to the phonetic rule proposed above. On the one hand, this element occurs in first position; on the other hand, it is not an adjective: syntactically, \textit{jemand} can appear in argument position; morphologically, it can have a strong, a weak, or no ending at all. Rather, \citet{Roehrs2009b} argues that \textit{jemand} belongs to three different morphological paradigms. Besides the older declensions that are similar to those of strong nouns and determiners, there seems to be a third paradigm in the progress of developing. In the latter case, \textit{jemand} has features of a weak noun where the ending -\textit{en} is generalized throughout the non-nominative cases. The three paradigms are summarized in \tabref{tab:key:3}:\footnote{Note that \citet[491]{Duden2007} claims that dative pronouns in -\textit{en} are not part of the standard language. It is clear though that such pronouns occur in the colloquial standard language. Notice also that the different inflections in \tabref{tab:key:3} presumably indicate different internal makeups of the pronoun. For instance, \textit{jemand} may involve a determiner component (ia), but it may also involve a determiner component with an indefinite pronoun restrictor where the latter has features of a weak noun (ib) or strong noun (ic): (i)  a.  [\textsubscript{D} \textit{jemand}]-\textit{em}    b.  [\textsubscript{D} \textit{je} ] + [\textsubscript{IPR} \textit{mand} ]-\textit{en}    c.  [\textsubscript{D} \textit{je} ] + [\textsubscript{IPR} \textit{mand} ]The analyses in (ib-c) indicate how I interpret the terms weak noun and strong noun in \tabref{tab:key:3} – the latter are grammaticalized nouns under IPR in \REF{ex:key:7}.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Different Paradigms of the Indefinite Pronoun \textit{jemand}}
\label{tab:key:3}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXX}
 & Strong Noun & Determiner & Weak Noun\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Nominative & jemand & jemand (/w-er) & jemand\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Accusative & jemand & jemand-en & jemand-en\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Dative & jemand & jemand-em & jemand-en\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Genitive & jemand-s & jemand-(e)s & jemand-en\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Adding an adjective after the pronoun retains the variation on the pronoun itself. In addition, the adjective also alternates between a strong and weak ending yielding six different options. Below, I provide my own grammaticality judgments:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%9
    \label{ex:key:9}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a. (?)  \textit{mit   jemand-em             ander-em}                  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with someone.\textsc{masc}{}-\textsc{st} different-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘with someone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mit   jemand-em             ander-en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with someone.\textsc{masc}{}-\textsc{st} different-\textsc{wk}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘with someone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%10
    \label{ex:key:10}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a. (?)  \textit{mit   jemand-en               ander-em}              
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with someone.\textsc{masc}{}-\textsc{wk} different-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘with someone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. (?)  \textit{mit   jemand-en               ander-en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with someone.\textsc{masc}{}-\textsc{wk} different-\textsc{wk}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘with someone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%11
    \label{ex:key:11}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.   \textit{mit   jemand              ander-em}              
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with someone.\textsc{masc} different-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘with someone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. (?)  \textit{mit   jemand              ander-en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with someone.\textsc{masc} different-\textsc{wk}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘with someone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

All these patterns occur naturally but with different frequencies. \tabref{tab:key:4}a presents the numeric results of an informal \textit{google}{}-search involving three dative prepositions: \textit{mit} ‘with’, \textit{von} ‘from’, and \textit{bei} ‘at’ (note that while I checked quite a few examples for relevance, the totals are too large to examine all the individual instances). While these numbers are, admittedly, not precise, the tendencies are clear. Whereas all patterns above occur, a weak adjective is more frequent with a pronoun involving an inflection, be it -\textit{em} or -\textit{en} (cf. \REF{ex:key:9b}, \REF{ex:key:10b}). In contrast, a strong adjective is more frequent with a pronoun involving no inflection (cf. \REF{ex:key:11a}). Notice also that the latter is the most frequent pattern overall (more on these points below). Finally, it is worth pointing out that the positive pronoun, separated by a forward slash sign in \tabref{tab:key:4}a, is more frequent than its negative counterpart.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\tabref{tab:key:4}a: Numeric Results of IPC in the Dative Identified by \textit{Google} (October 7, 2020)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Preposition and Pronoun & {\itshape anderem} & {\itshape anderen}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape mit jemandem/niemandem} & 35K/425 & 95K/5K\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape von jemandem/niemandem} & 8K/711 & 63K/50K\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape bei jemandem/niemandem} & 3K/9 & 10K/347\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape mit jemanden/niemanden} & 9K/238 & 75K/3K\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape von jemanden/niemanden} & 7K/350 & 32K/16K\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape bei jemanden/niemanden} & 1K/109 & 14K/503\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape mit jemand/niemand} & 755K/129K & 408K/69K\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape von jemand/niemand} & 762K/195K & 362K/19K\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape bei jemand/niemand} & 141K/19K & 108K/816\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In addition to the \textit{google}{}-search, I examined these constructions in \textit{Datenbank für Gesprochenes Deutsch} (DGD; Database of Spoken German). The results are provided in \tabref{tab:key:4}b. Note that \tabref{tab:key:4}b is similar to \tabref{tab:key:4}a above. However, the prepositions were removed from the first column and added to the relevant cells. The number of occurrences of these prepositions in these constructions is provided behind the preposition (the one instance of R after the preposition \textit{von} ‘of’ indicates a speaker in the corpus \textit{Australiendeutsch}, who is probably the interviewer, but no information is available here; IO stands for indirect object). Since the number of hits involving the two indefinite pronouns and the adjective \textit{ander}{}- ‘different’ was fairly low, I extended the search to other adjectives. These cases are provided in the second row of each form of the two pronouns (as above, the forward slash sign separates the results of the positive pronoun from those of the negative pronoun). Consider the results in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

While all three different forms of the positive and negative indefinite pronouns (strong, weak, uninflected) can be found in dative contexts when the adjective is absent, this differs when the adjective is present. For instance, in this search, I did not find any examples of a pronoun with a weak ending followed by an adjective with a strong ending. With that in mind, note though that the numeric distributions in \tabref{tab:key:4}b are similar to those in \tabref{tab:key:4}a: inflected pronouns occur, with one exception, with weak adjectives, but uninflected pronouns occur most frequently with strong adjectives (18 strong vs. 5 weak). Also similar to above, examples involving uninflected pronouns are more frequent than those with inflected pronouns. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\tabref{tab:key:4}b: Numeric Results of IPC in the Dative Identified in DGD (October 10, 2024)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Pronoun & {\itshape anderem} & {\itshape anderen}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape jemandem/niemandem} &  & { 2 / 1}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 (\textit{bei} 2) / (\textit{aus})\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

& {\itshape mit jemandem Deutschem} & {\itshape vor jemandem Fremden}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape jemanden/niemanden} &  & { 1 /-}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 (\textit{von} R) /-\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

&  & {\itshape mit jemanden Fremden}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape jemand/niemand} & { 12 /-}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 (\textit{bei} 2; \textit{mit} 4; \textit{von} 4; \textit{zu}; IO) /- & { 1 /-}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 (\textit{mit}) /-\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

& \textit{mit jemand neuem/ Deutschem/ Fremdem} \REF{ex:key:2};
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape von jemand Fremdem/ fremdem} & \textit{mit jemand andern} \REF{ex:key:3};
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape von jemand fremden}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that all constructions involving weak adjectives were checked.\footnote{There were two additional examples in the dative with a weak adjective (\textit{bei jemand anderen}, \textit{jemand anderen}). These were uttered by speakers whose first language is not German, and they were not counted in \tabref{tab:key:4}b above.} There is one example where the pronoun and the adjective both have a strong inflection. This instance was uttered by a speaker of Swiss German from Zurich. Finally, the weak form \textit{andern} ‘different’ (where schwa is missing in the inflection) was also found in another weak context (with at least one speaker). Now, while the numbers are not large, there are two types of instances where the distribution of the strong and weak adjectives can be directly compared. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

First, the weak adjective \textit{fremd-en} ‘strange-\textsc{wk}’ occurs one time each with strong \textit{jemandem}, weak \textit{jemanden}, and uninflected \textit{jemand}. In other words, the weak adjective occurs independently of the inflection on \textit{jemand}. In contrast, the strong adjective \textit{fremd-em} ‘strange-\textsc{st}’ occurs four times with uninflected \textit{jemand}. In fact, there is a minimal pair where \textit{jemand} occurs either with weak \textit{fremd}{}-\textit{en} or strong \textit{fremd}{}-\textit{em}. Note that the latter scenario is more general with the second type of instances, which involve \textit{jemand} and \textit{ander}{}- ‘different’. In these minimal pairs, (uninflected) \textit{jemand} occurs several times each with weak \textit{ander-(e)n} (4 times) and with strong \textit{ander}{}-\textit{em} (12 times). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Analyzing the results of Tables 4a and 4b, it is clear that the adjectives exhibit the nasal alternation basically independent of whether the pronoun has a strong, a weak, or no inflection. For the cases discussed in \sectref{sec:key:2.1}, this means that the second adjective in -\textit{en} should not be taken to be parasitic on a preceding adjective ending in -\textit{em} (for such an idea, see Murphy’s 2018 proposal of partial copying). Rather, the weak adjective depends on a preceding element more generally. Furthermore, indefinite pronoun constructions have a structure different from canonical DPs. This suggests that the nasal alternation is independent of a specific structure. If these cases are indeed related by the nasal alternation, then no strong morpho-syntactic claims should be made on the basis of the data in \sectref{sec:key:2.1}. Note in this regard that \citet{Schlenker1999} argues for top-down derivations and \citet[288-90]{Sternefeld2004} for recursive DP-levels. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As seen above, the word preceding the (lower) adjective may not only be an adjective but also a pronoun. What these elements have in common is that they are nominal in nature. I modify the phonetic rule above slightly (the feature [+N] stands for nominal): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%12
    \label{ex:key:12}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Rule (final version):}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

m  →  n  / […]\textsubscript{[+N]} […]\textsubscript{A} +\_\_\_\_\#  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In words, an adjective preceded by another nominal element (in the same DP) may occur with the inflection -\textit{en}. Notice that the rule does not apply to adjectives that have already undergone Impoverishment due to a preceding determiner. The latter has changed the ending on the adjective from -\textit{em} to -\textit{en} independently. The varying inflections on the pronouns are due to the three paradigms above.\footnote{As my focus is on adjectives, I do not reformulate the three paradigms of the indefinite pronouns as vocabulary insertion rules.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  At first glance, the different frequencies in Tables 4a and 4b seem to confirm the left-to-right asymmetry noted in \sectref{sec:key:2.1} (i.e., a strong inflection precedes a weak one). While \REF{ex:key:13a} is generally preferred over \REF{ex:key:13b}, it is worth repeating that the strong ending on the adjective is preferred over the weak one in \REF{ex:key:13a} but vice versa in \REF{ex:key:13b}. In other words, recalling the three paradigms of \textit{jemand} ‘someone’ in \tabref{tab:key:3}, a strong inflection is preferred on the first element that can take an inflection (the percentage sign indicates a less frequent option, which I interpret as a less preferred string):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%13
    \label{ex:key:13}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{jemand}              \textit{ander}{}-\textit{em}/\textsuperscript{\%}{}-\textit{en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    someone.\textsc{masc} different-\textsc{st}/-\textsc{wk}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘someone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \%  \textit{jemand-em/-en}              \textit{ander}{}-\textit{en}/\textsuperscript{\%}{}-\textit{em}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    someone.\textsc{masc}{}-\textsc{st}/-\textsc{wk} different-\textsc{wk}/-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘someone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

However, unlike in \sectref{sec:key:2.1}, the different frequencies seen with the indefinite pronoun construction are only tendencies, rather than clearcut left-to-right asymmetries. In fact, an element with a weak ending can precede one with a strong ending (e.g., \textit{jemand-en ander-em} ‘someone-\textsc{wk} different-\textsc{st}’), and a weak adjective can occur despite the fact that there is no element with a strong inflection preceding it (e.g., \textit{jemand(-en) ander-en} ‘someone(-\textsc{wk}) different-\textsc{wk}’). Recall that these inflectional distributions are not possible with two adjectives in a row. This means that explanations involving parsing or analogy as mentioned in \sectref{sec:key:2.1} cannot be the whole story. I submit that the rule in \REF{ex:key:12} is better able to capture these inflectional distributions. Indeed, this rule is extended in \sectref{sec:key:5} to pronominal DPs like \textit{mir nett-em/\textsuperscript{\%}}\textit{{}-en Studenten} ‘me (nice-\textsc{st/}\textsc{\textsuperscript{\%}}\textsc{{}-wk} student)’.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up, we arrive then at a first secondary mechanism – a phonetic rule – to explain inflectional variation, here accounting for unexpected weak adjectives in canonical DPs. Note that the weak endings here occur in a combination of certain featural and lexical contexts (i.e., an adjective in the genitive masculine/neuter is preceded by another nominal element). Next, I turn to some special cases that share a phonological constraint but are restricted to certain lexical items.  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.  Canonical DPs Involving Adjectives with Optional or no Inflections and Definite   Determiners with no Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I discuss two types of variation. I address the special inflectional behavior of certain adjectives, and I analyze some uninflected forms of the definite article and its related distal demonstrative. On the basis of the second discussion, I relate the distal and proximal demonstratives to the definite article in terms of their decompositions. More generally, I propose that all these cases have something in common: they exhibit the avoidance of a hiatus – the occurrence of two adjacent vowels in a prosodic word.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In Chapter 1, \sectref{sec:key:3.1.1}, I briefly illustrated some adjectives exhibiting special inflectional behaviors. Recall that there are three types of cases. First, the adjectives \textit{lila} ‘purple’ and \textit{rosa} ‘pink’ participate in the strong/weak alternation, but the inflection is optional: it can be absent, and it can be present (14a-b). In order to account for the presence of -\textit{n}{}-, I assume for convenience that the latter consonant has been epenthesized.\footnote{Björn Köhnlein (p.c.) points out that -\textit{n}{}- is not a typical epenthetic consonant (the glottal stop being the usual element).} Inflected forms without \textit{n}{}-epenthesis are not possible \REF{ex:key:14c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%14
    \label{ex:key:14}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{das lila(-n-e)      Kleid}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the purple-n-\textsc{wk} dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the purple dress’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{ein lila(-n-es)   Kleid} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a    purple-n-\textsc{st} dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a purple dress’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  *  \textit{ein} \textit{lila-es      Kleid}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a    purple-\textsc{st} dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There are two ways to analyze this optionality. On the one hand, we could assume that InflP of the extended projection of the adjective is built optionally: if InflP is not projected, we obtain the uninflected forms; if InflP is projected, we get the inflected forms, provided \textit{n}{}-epenthesis occurs. On the other hand, we could suggest that InflP is built in every instance. In this scenario, the inflection is deleted yielding the uninflect forms, or the inflection is not deleted, provided \textit{n}{}-epenthesis occurs.\footnote{This deletion could be made more formal by assuming that Impoverishment deletes either all the features of the Infl head, or it deletes a feature that all inflections have in common (perhaps a nominal feature like [+N]).}  Note that either option involves \textit{n}{}-epenthesis and results in the avoidance of a hiatus. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Turning to the second case, certain adjectives never take inflections. This holds for \textit{prima} ‘great’, \textit{sexy} ‘sexy’, \textit{super} ‘super’, and others \citep[256-57]{Duden1995}. Note that \textit{n}{}-epenthesis cannot rescue these cases (e.g., *\textit{prima-n-es}, *\textit{sexy-n-es}). Observe that many of these cases seem to be borrowings from other languages. Following the idea of a reviewer, I assume that these cases are not fully intergrated into the language yet. To make this idea more formal, I suggest that InflP is not projected in these instances (note in this regard that the emergence of InflP on top of an adjective takes time; see \citealt{SappRoehrs2016} for the discussion of \textit{viele} ‘many’ changing from the uninflected noun \textit{vil} in OHG to the inflected quantificational adjective emerging in the 16\textsuperscript{th} century).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Third, toponymic formations where adjectives are derived from place names by adding -\textit{er} never take inflections either, for instance \textit{Berlin-er-(*es)} ‘(from) Berlin’. Again, there are two options to analyze this. We could suggest that either this derivational suffix blocks the projection of InflP, or alternatively, we could assume that the derivational ending -\textit{er} does double duty as an invariant inflectional element under Infl. Note in this regard that this -\textit{er} can license a genitive noun phrase, for instance, \textit{der Verkauf Berlin-er Bier-es} ‘the sale of Berlin-\textsc{infl} beer-\textsc{gen}’ \citep{Fuhrhop2003} (for more discussion of the Genitive Rule, see \sectref{sec:key:4}).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Notice that these three sets of cases have other intriguing properties that may help us decide between the different analytical options.\footnote{I have not investigated adjectives in ellipsis contexts in detail. As far as I am aware, all ordinary adjectives have the same inflections in ellipsis and non-ellipsis contexts. However, the three types of adjectives discussed in the main text are also special in ellipsis contexts. First, \citet[101]{Fanselow1988} observes that the inflectional ending with \textit{lila} ‘purple’ and \textit{rosa} ‘pink’ is obligatory in ellipsis contexts:(i)  \textit{ein lila*(-n-es)}     a    purple-n-\textsc{st}     ‘a purple one’Second, uninflected adjectives like \textit{prima} ‘great’, \textit{sexy} ‘sexy’, and \textit{super} ‘super’ cannot appear with an elided noun. Third, derived adjectives like \textit{Berliner} ‘(from) Berlin’ can occur in ellipsis contexts (as also noted by \citealt{Rehn2019}: 221-24 for Alemannic German). Given that ordinary adjectives have the same forms in ellipsis contexts and that only the special cases behave differently, I leave the analysis of ellipsis for future research. Note though that a detailed analysis of these facts may help narrow down the analytical options mentioned in the main text, which were offered to account for the optionality or absence of the inflections in non-ellipsis contexts.} While I cannot investigate these three special cases of adjectives in more detail here, it seems clear that the avoidance of a hiatus plays a role here and in the cases I turn to next.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Definite articles also have some special forms. Consider \tabref{tab:key:5}, where the inflections are set apart by hyphens. The parsing follows the one proposed in R. \citet[33]{Wiese1988} closely:\footnote{Note that the presentation of the definite article might be a simplification as reduced forms like \textit{s} as in \textit{s Haus} ‘the house’ can be found in speech. For Swiss German, \citet{Studler2011} shows that there are two paradigms of definite articles – the reduced and the full forms. Besides their different morphological manifestations, they also involve different semantics. Since the current investigation focuses on adjectival inflections and \textit{ein}, I will not investigate this for (colloquial) Standard German.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Definite Articles}
\label{tab:key:5}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXX}
 & Masculine & Neuter & Feminine & Plural\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Nominative & de-r & da-s & die & die\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Accusative & de-n & da-s & die & die\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Dative & de-m & de-m & de-r & de-n\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Genitive & de-s & de-s & de-r & de-r\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As can be observed in \tabref{tab:key:5}, there are two points of interest (see also Gunkel \textit{et al}. 2017: 1297-98). First, there is no inflection on \textit{die} ‘the (/that)’. In other words, this form is pronounced [di] as an article and [di:] as its related distal demonstrative (where stress and the slightly different vowel quality of the demonstrative are often indicated by capitalization as in \textit{DIE}).\footnote{A reviewer asks why \textit{die} cannot be analyzed as \textit{d-i}. One advantage would be that now all articles would have an inflection. Note though that one of the determiner stems would have no vowel and that this new inflection would be the only full-vowel inflection in Standard German. Furthermore, it would increase the inflectional inventory considering that *\textit{dies-i} ‘this/these’ or *\textit{gut-i} ‘good’ are not possible in Standard German. Having said that, there may be a phonetic explanation of the absence of a full vowel in second (unstressed) syllables. Be that as it may, I proceed on the basis of the traditional assumptions regarding the inflectional inventory. Notice that assuming no inflection on \textit{die} allows me to relate \textit{wir} ‘we’ and \textit{ihr} ‘you(PL)’ to this element in the discussion of adjectival inflections in pronominal DPs (\sectref{sec:key:5.3.1}).} Second, the stem forms vary between \textit{de}{}- in most instances vs. \textit{da}{}- in the nominative/accusative neuter vs. \textit{di}{}- in the nominative/accusative in feminine/plural contexts.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Starting with the first issue, it is clear that schwa cannot occur with the form \textit{die} ‘the (/that)’. On the one hand, it cannot be added to the stem directly; compare \REF{ex:key:15a} to \REF{ex:key:15b}. On the other hand, it cannot be added, mitigated by \textit{n}{}-epenthesis \REF{ex:key:15c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%15
    \label{ex:key:15}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{die}   [di(:)]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the (/that)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  *  \textit{di-e}   [di(:)ə]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  the-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  *  \textit{di-n-e}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-n-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

We can suggest that \REF{ex:key:15b} follows from the avoidance of a hiatus, and that \REF{ex:key:15c} indicates that \textit{n}{}-epenthesis is not possible. As seen with the proximal demonstrative below, the avoidance of a hiatus and the non-availability of \textit{n}{}-epenthesis also play a role in the account of other determiners. Indeed, this type of analysis is even more general accounting also for the third-person pronoun \textit{sie} ‘she, her; they, them’, which is pronounced as [zi:] (for the discussion of pronouns, see \sectref{sec:key:5}). If so, then the final -\textit{e} on \textit{die} and \textit{sie} can be taken as an orthographic element marking the quality of the stem vowel (cf. also \textit{nie} ‘never’, \textit{wie} ‘how’, \textit{Knie} ‘knee’, \textit{Chemie} ‘chemistry’, etc.).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As to the second issue, the different stem forms, I assumed in Chapter 2, \sectref{sec:key:2.1.2} that the schwas in adjectival inflections are due to epenthesis. This is presumably different for -\textit{e}{}- in the relevant stem forms of the definite article: \textit{der}, \textit{den}, \textit{dem}, \textit{des}. R. \citet[34]{Wiese1988} observes that the stem vowels are short and lax if an obstruent follows (\textit{des}, also \textit{das}) but long and tense otherwise (\textit{der}, \textit{den}, \textit{dem}, also \textit{die}). Again, this is more general and applies to the different vowel qualities in \textit{es} ‘it’ vs. \textit{er} ‘he’, \textit{ihn} ‘him(\textsc{acc})’, \textit{ihm} ‘him(\textsc{dat})’, \textit{sie} ‘she, her; they, them’, \textit{ihr} ‘her(\textsc{dat})’, and \textit{ihnen} ‘them(\textsc{dat})’.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Given this regularity and to make the vocabulary entries of the different definite article forms parallel, I include the stem vowel -\textit{e}{}- in the vocabulary insertion rule. I analyze the different stem forms as another instance of contextually conditioned allomorphy. The vocabulary insertion rule for the nominative/accusative form in the neuter is given in \REF{ex:key:16a}, the one for the nominative/accusative in the feminine and plural is in \REF{ex:key:16b}, and the elsewhere case is provided in \REF{ex:key:16c} (adapted here from \citealt{Roehrs2009a}: 132):\footnote{Referencing unpublished work by B. Wiese, G. \citet[125]{Müller2002a} derives both issues (absence of schwa on \textit{die}, different stem forms) by assuming one stem form (\textit{de}{}-), where the stem vowel /e/ is realized as [a] in nominative/accusative neuter contexts and as [i:] when the inflection schwa is added to \textit{de}{}- yielding a sequence of /e/-/e/. As far as I know, these are not regular phonetic realizations of an underlying /e/.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%16
    \label{ex:key:16}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [+D; +DEF]   →   \textit{da}{}- / \_\_ [-O, -F, +N]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [+D; +DEF]   →   \textit{di}{}- / \_\_ [-O, +F]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  [+D; +DEF]   →   \textit{de}{}-
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that the stem form spells out the features on the left of the arrow; the features on the right specify the context of application of the vocabulary insertion rules. As just discussed, I assume that the phonological environment (i.e., elements such as obstruents) will determine the specific properties of the stem vowels in \REF{ex:key:16}. To bring about the inflected forms of the definite article, recall that overt articles involve complex heads consisting of two separate feature bundles. As such, inflections are added to the stems in \REF{ex:key:16} by the vocabulary inflection rules, discussed in Chapter 2, \sectref{sec:key:2.1.5}. These rules spell out the CNG features of the bipartite article structure. With these insertion rules in mind, consider demonstratives.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is well known that demonstratives and definite articles are diachronically related in that the latter has evolved from the former (e.g., \citealt{Greenberg1978}, van \citealt{Gelderen2007}). As briefly mentioned in Chapter 1, \sectref{sec:key:4.1.2}, it is also often argued that they are synchronically related such that the demonstratives consist of the definite article and an additional deictic component (e.g., \citealt{Leu2007}; \citealt{Roehrs2010}, 2013a; and references cited therein). Starting with the proximal demonstrative, its decomposition is exemplified in \REF{ex:key:17a}. This set of examples illustrates the masculine, neuter, and feminine forms in the nominative, where \textit{d}{}- spells out [+D; +DEF], -\textit{ies}{}- spells out [+DEIX], and the varying inflections spell out the different CNG feature bundles. Note though that the vocabulary insertion rules proposed in \REF{ex:key:16} should actually yield the strings in \REF{ex:key:17b} or \REF{ex:key:17c}, contrary to fact:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%17
    \label{ex:key:17}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{d-ies-er},        \textit{d-ies-es}, \textit{d-ies-e} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{deix}{}-\textsc{st}, the-\textsc{deix}{}-\textsc{st}, the-\textsc{deix}{}-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{de-ies-er}, \textit{da-ies-es},  \textit{di-ies-e} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

the-\textsc{deix}{}-\textsc{st}, the-\textsc{deix}{}-\textsc{st}, the-\textsc{deix}{}-\textsc{st} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  *  \textit{de-n-ies-er}, \textit{da-n-ies-es}, \textit{di-n-ies-e} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

the-n-\textsc{deix}{}-\textsc{st}, the-n-\textsc{deix}{}-\textsc{st}, the-n-\textsc{deix}{}-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If demonstratives and definite articles are indeed related, then certain adjustments have to occur. To generate the correct forms in \REF{ex:key:17a}, I follow \citet{Roehrs2013a} in suggesting that \REF{ex:key:17b} shows that a hiatus is avoided. Furthermore, \REF{ex:key:17c} indicates that similar to definite articles, \textit{n}{}-epenthesis cannot rescue the cases in \REF{ex:key:17b}. As to the distal demonstrative \textit{DER} ‘that’, I follow \citet{Roehrs2013a} in assuming that the deitic component involves a null element here (i.e. \textit{de-Ø-r}). Given this null element, no adjustments have to be made. Moving forward, I abstract away from these finer points continuing with \textit{d}{}- as the stem for the definite article and its related distal demonstrative and with \textit{dies}{}- as the stem for the proximal demonstrative.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up, this section accounted for some unexpected forms of certain adjectives, definite articles and their related demonstratives. All these cases show that hiatuses do not occur. While it seems clear that a hiatus is generally avoided in the determiner system, the exact conditions that induce the avoidance of a hiatus with adjectives are less clear: a hiatus is avoided with certain disyllabic adjectives ending in -\textit{a}, but other instances tolerate a hiatus (e.g., \textit{blau-e} ‘blue’, \textit{roh-e} ‘raw’). I leave the detailed investigation of these differences for future research. Next, I discuss a type of variation that has received very little attention in the theoretical literature.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.  Canonical DPs with Unexpected Weak Determiners: Impoverishment Rule 2
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I continue discussing variation that concerns the inflections on the determiners themselves. Like in the previous section, this variation is restricted, but here to genitive masculine/neuter environments. Furthermore, like in the previous section, this variation involves inflections on \textit{der}{}-words, but here on \textit{der}{}-words other than the definite article and its related distal demonstrative. Unlike in the previous section, the inflection on these determiners is present, but it alternates between strong and weak. Now, given the discussion of Chapter 2, it is surprising that determiners may have a weak ending at all. I propose that this variation is the result of Impoverishment; in particular, it is due to a more general application of Impoverishment Rule 2. First, I present the data, and then I provide my account along with an additional restriction.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To begin, certain determiners show inflectional variation in genitive masculine/neuter contexts. Considering the data below, the (a)-examples exhibit the (expected) strong endings on the determiners \textit{dieser} ‘this’, \textit{jeder} ‘every’, and \textit{aller} ‘all’; the (b)-examples show these elements with the corresponding weak endings:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%18
    \label{ex:key:18}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{im      Sommer dies-es Jahr-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    in.the summer this-\textsc{st} year.\textsc{neut}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘in the summer of this year’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{im      Sommer dies-en Jahr-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    in.the summer this-\textsc{wk} year.\textsc{neut}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘in the summer of this year’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%19
    \label{ex:key:19}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{die Verarbeitung jed-es      Holz-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the processing     every-\textsc{st} wood.\textsc{neut}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the processing of every (type of) wood’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{die Verarbeitung jed-en      Holz-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the processing     every-\textsc{wk} wood.\textsc{neut}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the processing of every (type of) wood’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%20
    \label{ex:key:20}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{der Beginn     all-es Schön-en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the beginning all-\textsc{st} beautiful.\textsc{neut}{}-\textsc{wk}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the beginning of everything beautiful’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{die Wege all-en Übel-s} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the ways all-\textsc{wk} evil.\textsc{neut}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the ways of all evil’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As noted by other scholars, this variation is possible with the following \textit{der}{}-words: \textit{dieser} ‘this’ (G. \citealt{Müller2002a}: 137 fn. 38), \textit{jener} ‘that’ \citep[154]{Gallmann2004}, \textit{jeder} ‘every’ (also \textit{jedweder} ‘every’, \textit{jeglicher} ‘every’), \textit{aller} ‘all’, \textit{mancher} ‘some’, \textit{solcher} ‘such’, \textit{welcher} ‘which’ (the latter are all mentioned in Zifonun \textit{et al}. 1997: 1936-48). In contrast, there is no inflectional variation with the definite article \textit{der} ‘the’, its related demonstrative \textit{DER} ‘that’, and \textit{ein}{}-words in general – the latter must all have strong inflections:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%21
    \label{ex:key:21}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{der Verkauf d-es   Wagen-s}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the sale       the-\textsc{st} car.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the sale of the car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{der Verkauf d-en    Wagen-s}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the sale       the-\textsc{wk} car.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{der Verkauf ein-es Wagen-s}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the sale        a-\textsc{st}   car.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the sale of a car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  *  \textit{der Verkauf ein-en Wagen-s}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the sale        a-\textsc{wk}  car.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

B. \citet[184-85]{Wiese2009} states that there is no variation in this context with regular adjectives either. However, unlike the determiners in \REF{ex:key:21}, adjectives are always weak in this context. Rather than explaining the variation on the relevant \textit{der}{}-words by analogy with adjectives, I make the stronger and more interesting claim that the weak endings on these determiners are also due to Impoverishment.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I argued in Chapter 2, \sectref{sec:key:2.3} that adjectives in genitive masculine/neuter contexts have weak inflections due to Impoverishment Rule 2. Impoverishment affects elements that have their InflP in the specifier of AgrP:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%22
    \label{ex:key:22}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Impoverishment Rule 2:}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            InflP    Agr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       [-F, αN, +O, +S]       [-F, αN, +O, +S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Observe again that genitive masculine/neuter contexts are the exact same featural environments where the relevant \textit{der}{}-words from above can have weak inflections too. By contrast, note again that \textit{der} ‘the’, \textit{DER} ‘that’, and \textit{ein}{}-words do not exhibit this variation. The same goes for the head noun itself – its ending is not changed from -\textit{(e)s} to -\textit{(e)n}. The difference between these two types of elements seems to involve structural size: the first group (\textit{der}{}-words other than \textit{der} and \textit{DER}) involves phrasal elements but the second group (\textit{der}, \textit{DER}, \textit{ein}{}-words, nouns) consists of heads. I propose that Impoverishment only applies to the inflections on the phrasal elements.\footnote{The demonstrative \textit{DER} ‘that’ presumably patterns with the article \textit{der} ‘the’ due to their obvious relatedness.} Consider this in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I proposed in Chapter 2, \sectref{sec:key:2.1.6} that demonstratives have a more complex structure than articles in that they have InflP at the top of their structure:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%23
    \label{ex:key:23}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Demonstrative}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 InflP\textsubscript{[+D; +DEF, +DEIX][F, N, O, S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          [F, N, O, S]    DemP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            {\textbar}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         [+D; +DEF, +DEIX]  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This yields a phrasal determiner, which occurs in phrasal (e.g., specifier) positions. Extending this discussion, I propose that besides \textit{dieser} ‘this’, \textit{jener} ‘that’, \textit{jeder} ‘every’ (also \textit{jedweder} ‘every’, \textit{jeglicher} ‘every’), \textit{aller} ‘all’, \textit{mancher} ‘some’, \textit{solcher} ‘such’, and \textit{welcher} ‘which’ also have structures similar to \REF{ex:key:23}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  With that in mind, I argue that Impoverishment Rule 2 has a more general context of application, updated as Impoverishment Rule 2’. Rather than just applying in AgrP, this rule operates in all phrases indicated by XP below: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%24
    \label{ex:key:24}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Impoverishment Rule 2’:}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                XP  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             InflP    X’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       [-F, αN, +O, +S]       [-F, αN, +O, +S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given this, the more general Impoverishment rule in \REF{ex:key:24} now applies to all determiners in Spec,ArtP, adjectives in Spec,AgrP, quantifiers/numerals in Spec,CardP, and determiners in Spec,DP.\footnote{Note that Impoverishment Rule 2’ applies to CardP with some restrictions. While it is applicable to inflected singular quantifiers (e.g., \textit{vieler} ‘much’), it is not relevant to the singularity numeral \textit{EIN} ‘one’, which consists of a null element in Spec,CardP and \textit{ein} in Card (Chapter 5).} More generally, if Impoverishment applies in this featural context, then only the least specified ending -\textit{en} can be inserted as discussed with adjectives in Chapter 2. This accounts for the weak ending on the relevant determiners. Importantly, note that this Impoverishment rule presents a second, secondary mechanism that accounts for the strong/weak alternation.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  It is not entirely clear to me how to derive the optionality of the strong/weak alternation on these determiners. Currently, I see two analytical options. First, it would be possible to stipulate that there are actually two independent Impoverishment rules, Rule 2 and Rule 2’, where the former applies obligatorily, but the latter does not. Note though that both rules have the same basic context of application. Assuming the existence of two separate rules applying in the same context runs the risk of losing an important generalization. Alternatively, it would be possible to suggest that – what I have proposed to be – phrasal determiners can actually vacillate between involving phrases or heads (cf. \citealt{Borer2005}: 169-74). If the relevant determiners merged as heads, Impoverishment Rule 2’ would not apply to them (but only to their phrasal counterparts and adjectives more generally). Whichever is the right solution to pursue, note that determiners, be they phrases or heads, still trigger Impoverishment on the adjective (by Impoverishment Rule 1).\footnote{This makes the predication that a determiner with a weak inflection can be followed by an adjective with a weak inflection. While I have not found any attested examples yet, such cases seem possible to me:(i)  a.   \textit{im      Verlauf dies-en schön-en        Sommer-s}    in.the course  this-\textsc{wk} beautiful-\textsc{wk} summer.\textsc{masc}{}-\textsc{gen}      ‘in the course of this beautiful summer’    b.  \textit{im      Sommer jed-en      heiß-en Jahr-es}    in.the summer every-\textsc{wk} hot-\textsc{wk} year.\textsc{neut}{}-\textsc{gen}      ‘in the summer of every hot year’}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I close this section by discussing a final restriction. The occurrence of weak endings on the determiners is constrained by the ending on the following noun: if the ending on the noun is -\textit{(e)s}, both strong or weak endings on the determiners are usually possible (25a-b); if the ending on the noun is -\textit{(e)n}, only strong endings on the determiners are possible; compare \REF{ex:key:25c} to \REF{ex:key:25d} (data are from \citealt{Gallmann1996}: 293, also \citealt{Gallmann1990}: 268-75):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%25
    \label{ex:key:25}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{der Traum manch-es Schüler-s}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the dream  some-\textsc{st}   pupil.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the dream of some pupil’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{der Traum manch-en Schüler-s}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the dream  some-\textsc{wk}  pupil.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the dream of some pupil’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{der Traum manch-es Student-en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the dream  some-\textsc{st}   student.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the dream of some student’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d. *  \textit{der Traum manch-en Student-en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the dream  some-\textsc{wk}  student.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that this restriction has nothing to do with the strong/weak alternation per se. Rather, it involves a dependency between the inflection on the determiner (or adjective, not discussed here separately; for some remarks, see Chapter 2, \sectref{sec:key:2.3}) and the inflection on the noun.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In Chapter 2, I provided a specificity-based account of the inventory and distribution of adjectival inflections. Gallmann (1996, 1998, 2018) utilizes specificity of inflections to account for the cases above. Given the interplay between the inflection on the determiner element and that on the noun, \citet{Gallmann1996} formulates the Genitive Rule: a noun phrase in the genitive must contain at least one element that is sufficiently specific as regards case. Gallmann states that -\textit{es} and -\textit{er} are more specific than -\textit{en}: the first two are [+Genitive], which consists of [+Oblique] and other features, but the third is only [+Oblique] (see \citealt{Gallmann1998}: 146, 153). This is consistent with the feature specifications in Chapter 2, where the strong inflections -\textit{es} and -\textit{er} are also more specific than the weak inflection -\textit{en} (the latter is interpreted as the elsewhere case and has no feature specification at all).\footnote{Although case suffixes on nouns and adjectival inflections on determiners and adjectives are not the same elements (Chapter 2, \sectref{sec:key:2.3}), the case suffix -\textit{es} presumably has a similar specification as the adjectival inflection -\textit{es}.} Since there is no sufficiently specific element in \REF{ex:key:25d}, this example violates the Genitive Rule, and its ungrammaticality is accounted for.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up, this section illustrated some inflectional variation on certain \textit{der}{}-words in genitive masculine/neuter contexts. I suggested that Impoverishment Rule 2 from Chapter 2 appears to have a more general context of application. I made two tentative suggestions to account for the (restricted) optionality of this variation. Given its restricted application, Impoverishment Rule 2 is interpreted as a second, secondary mechanism. More generally, besides Impoverishment Rule 1, adjectival inflections in German are, in certain contexts, regulated by a phonetic rule and Impoverishment Rule 2.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.   Canonical DPs with Unexpected Strong Adjectives: Pronominal Determiners
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I return to the discussion of pronominal DPs. Recalling that these are definite constructions, I show that there is variation as regards the inflections on adjectives: in some cases, both (expected) weak and (unexpected) strong adjectives are possible; in others, only (unexpected) strong adjectives occur. I propose that pronominal DPs involve regular structures. However, pronominal determiners lack features that trigger Impoverishment. This accounts for the general option of strong adjectives. Weak adjectives are due to the phonetic rule discussed in \sectref{sec:key:2.2} and to (phonetically constrained) analogy with ordinary definite DPs. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I also discuss briefly preferences for strong adjectives over weak adjectives and vice versa. I suggest that, if possible, strong adjectives are preferred as they are more specified as regards CNG features than their corresponding pronominal determiners. Note that I often refer to pronominal determiners (or pronouns) as pronominal elements or short as pronominals. Given the complexity of the data and the fact that this variation has not received much attention in the theoretical literature, I discuss pronominal DPs in quite some detail. At the end of this section, I summarize the data and analysis.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.1.  Data
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

First, I present the data under discussion (see also \citealt{Bhatt1990}: 154-55; \citealt{Darski1979}: 198; \citealt{Duden1995}: 280, 2007: 39; Gunkel \textit{et al}. 2017: 1308). I focus on the first-person pronominal elements. Second-person pronominals show the same distribution and are only briefly discussed in what follows. Starting with the singular, there is no variation in the nominative and accusative in that only strong adjectives are possible:\footnote{Recall that adjectival inflections in the nominative/accusative feminine involve -\textit{e} (which is ambiguous between a strong and a weak inflection). Such a case is not provided in \REF{ex:key:26}. Also, strings like (26a-b) are ungrammatical in English. Given that there are no straightforward translations of such German examples, I put the adjective and noun in parentheses in the English translations. For simplicity, I translate the figurative meaning of animal names as ‘idiot’. Note though that different animal names have different implications; for instance, \textit{Schwein} ‘pig’ implies that the person is, in some sense, dirty whereas \textit{Esel} ‘donkey’ implies that the person is not very bright.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%26
    \label{ex:key:26}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ich      dumm-er Idiot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I.\textsc{nom} stupid-\textsc{st} idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I (stupid idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mich      dumm-es Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

me.\textsc{acc} stupid-\textsc{st} pig.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘me (stupid idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The second-person pronominal element \textit{du} ‘you(\textsc{nom.sgl})’ shows the pattern in \REF{ex:key:26a}, and \textit{dich} ‘you(\textsc{acc.sgl})’ has the same distribution as in \REF{ex:key:26b}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  There is variation in the dative in all three genders. Strong adjectives are preferred over weak ones in the masculine and neuter but vice versa in the feminine (the less preferred option is indicated by \%):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%27
    \label{ex:key:27}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \%  \textit{mir        groß-en   Esel}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    me.\textsc{dat} great-\textsc{wk} donkey.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘me (stupid idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mir        groß-em Esel}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    me.\textsc{dat} great-\textsc{st} donkey.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘me (stupid idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%28
    \label{ex:key:28}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \%  \textit{mir        groß-en   Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    me.\textsc{dat} great-\textsc{wk} pig.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘me (stupid idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mir        groß-em Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    me.\textsc{dat} great-\textsc{st} pig.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘me (stupid idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%29
    \label{ex:key:29}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{mir        groß-en   Gans}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    me.\textsc{dat} great-\textsc{wk} goose.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘me (stupid idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \%  \textit{mir        groß-er  Gans}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    me.\textsc{dat} great-\textsc{st} goose.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘me (stupid idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The same patterns hold for the second-person pronominal element \textit{dir} ‘you(\textsc{dat.sgl})’. The cases in the singular are summarized in \tabref{tab:key:6}, where, if important, [-F] indicates pronominal DPs in the masculine/neuter and [+F] does so as regards feminine/plural:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Summary of Adjectival Inflections in Singular Pronominal DPs}
\label{tab:key:6}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Pronominal Element & Gender of the noun & Weak & Strong\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ \textit{ich} ‘I(\textsc{nom})’/\textit{du} ‘you(\textsc{nom})’} &  & {}- & ${\surd}$\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ \textit{mich} ‘me(\textsc{acc})’/\textit{dich} ‘you(\textsc{acc})’} &  & {}- & ${\surd}$\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ \textit{mir} ‘me(\textsc{dat})’/\textit{dir} ‘you(\textsc{dat})’} & { [ -F]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 [+F] & { \%}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 ${\surd}$ & { ${\surd}$}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \%\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the plural, there is variation in the nominative where, like in the dative feminine singular, weak adjectives are preferred over strong ones (30a-b). There is no variation in the accusative and dative (30c-d), with the qualification that the inflections on the adjectives in the dative are ambiguous between weak and strong \REF{ex:key:30d}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%30
    \label{ex:key:30}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a. \textit{wir         nett-en   Studenten} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

we.\textsc{nom} nice-\textsc{wk} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘we nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 b.  \% \textit{wir         nett-e    Studenten} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

we.\textsc{nom} nice-\textsc{st} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘we nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 c.  \textit{für uns       nett-e    Studenten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    for us.\textsc{acc} nice-\textsc{st} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘for us nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 d. \textit{von   uns       nett-en        Studenten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    from us.\textsc{dat} nice-\textsc{wk/st} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘from us nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The nominative plural pronominal element \textit{ihr} ‘you(\textsc{nom.pl})’ has the same distribution as in (30a-b), and the dative/accusative pronominal element \textit{euch} ‘you(\textsc{acc/dat.pl})’ has the patterns as in (30c-d). The plural cases are summarized in \tabref{tab:key:7}, where the specified features for [O] and [S] indicate the different morphological cases of the pronominal DPs involved: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Summary of Adjectival Inflections in Plural Pronominal DPs}
\label{tab:key:7}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Pronominal element & Case of the DP & Weak & Strong\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ \textit{wir} ‘we(\textsc{nom})’/\textit{ihr} ‘you(\textsc{nom})’} & [ -O,  {}-S] & ${\surd}$ & \%\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ \textit{uns} ‘us(\textsc{acc})’/\textit{euch} ‘you(\textsc{acc})’} & [ -O, +S] & {}- & ${\surd}$\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ \textit{uns} ‘us(\textsc{dat})’/\textit{euch} ‘you(\textsc{dat})’} & [+O,  {}-S] & ambiguous & ambiguous\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, as is well known, third-person pronominals do not take overt complements in the singular or plural (for recent discussion, see \citealt{Höhn2020}):\footnote{A brief note on older varieties of German is of interest here. MHG appears to have tolerated forms of the third person, for instance, \textit{er süezer man vil guoter} ‘he (nice man), very good’ (see Paul \textit{et al}. 1989: 359). However, these strings may actually involve appositives (as suggested by the continuation with \textit{vil guoter}). Be that as it may, it is clear that MHG is different in that pronominal elements in the nominative singular could be followed by a weak adjective, for instance, \textit{ich arme tore} ‘I (poor-\textsc{wk} fool)’. For similar facts in ENHG, see Ebert \textit{et al}. (1993: 197).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%31
    \label{ex:key:31}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{er Idiot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{sie    Linguisten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    they linguists
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note also that pronominal elements in the genitive are not commonly used in German and do not do double duty as (transitive) pronominal determiners – similar to third-person pronominals.\footnote{Recall that transitive determiners have overt complements but that intransitive determiners have covert complements.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Taking stock thus far, pronominal determiners are definite elements. Given their similarity to ordinary definite determiners, we would expect all adjectives to have weak inflections. However, this is not the case. In fact, all instances allow strong adjectives. Crucially, weak adjectives only occur in the dative singular and in the nominative plural (but not in the nominative/accusative singular and not in the accusative plural). Adjectives in the dative plural are inflectionally ambiguous. Finally, third-person and genitive pronominals do not take adjectives and nouns.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Observe again that the strong endings cannot mark indefiniteness. Furthermore, similar to the distribution of strong adjectival inflections where, for instance, -\textit{em} only occurs in the masculine/neuter, and -\textit{e} only surfaces in the feminine/plural, here masculine also patterns with neuter and feminine also with plural. In the current contexts though, the similarity concerns the preference of the relevant endings: strong endings are preferred in the dative masculine/neuter but weak endings in the dative feminine and in the nominative plural. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Before moving on, consider the complete set of pronominal elements in \tabref{tab:key:8} (for detailed discussion of the internal structure, see \citealt{Fischer2006}). Starting with the first and second-person pronominals, notice that these elements do not involve inflections. Furthermore, they do not distinguish gender in the singular, and they do not have different forms in the accusative and dative case in the plural (\textit{uns}, \textit{euch}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Pronominal Elements in German}
\label{tab:key:8}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXXXXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Number & \multicolumn{5}{c}{ Singular} & \multicolumn{3}{c}{ Plural}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Person & 1\textsuperscript{st} Pers. & 2\textsuperscript{nd} Pers. & 3\textsuperscript{rd} Masc & 3\textsuperscript{rd} Neut & 3\textsuperscript{rd} Fem & 1\textsuperscript{st} Pers. & 2\textsuperscript{nd} Pers. & 3\textsuperscript{rd} Pers.\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Nom & ich & du & e-r & e-s & sie & wir & ihr & sie\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Acc & mich & dich & ih-n & e-s & sie & uns & euch & sie\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Dat & mir & dir & ih-m & ih-m & ih-r & uns & euch & ih-n-en\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Gen & meiner & deiner & seiner & seiner & ihrer & unserer & eurer & ihrer\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Third-person pronominal elements are different in a number of ways. First, they have the typical determiner endings, and the singular instances of these pronominals also distinguish gender. Second, note that the form \textit{sie} is special: on the one hand, it is pronounced as [zi:]; that is, it does not have an inflection (see \sectref{sec:key:3}); on the other hand, it is the only pronominal element unspecified for number: it translates as ‘she/her’ and ‘they/them’. Third, as mentioned above, third-person elements, like genitive pronominals more generally, do not take overt complements.\footnote{There is another type of \textit{sie}: when capitalized in writing, \textit{Sie} ‘you(\textsc{sgl/pl.formal})’ is morphologically third-person plural, but semantically, it is second-person singular/plural. Also, unlike lower case \textit{sie}, capitalized \textit{Sie} can take overt complements (for detailed discussion, see Chapter 7).} Finally, note that dative plural \textit{ihnen} has the additional inflection -\textit{en} (similar to the dative plural pronominal \textit{d-en-en} ‘those (ones)’) and that genitive pronominal elements do not involve (regular) adjectival inflections. This is particularly clear in masculine/neuter contexts where the strong inflection would be -\textit{es} or the weak one would be -\textit{en}, both contrasting with -\textit{er}. I provide the vocabulary insertion rules for all these elements in \sectref{sec:key:5.3.2}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The lack of gender specification in the first and second person singular and the fact that case is not uniquely differentiated by all these pronominal elements in the plural can be linked to the general preference of strong adjectives (provided there is variation). I propose in \sectref{sec:key:5.3.3} that \textit{wir} ‘we’ and \textit{ihr} ‘you(\textsc{nom.pl})’ are the only first and second-person pronominals that are specified for all CNG features. Consequently, they are preferably followed by weak adjectives. By contrast, all other relevant pronominal determiners are not fully specified for CNG features (note again that one pronominal form spells out multiple feature combinations), and the following adjective is recruited to indicate those features with a strong inflection. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In the next section, I briefly discuss three types of proposals that might be offered to account for the inflectional variation on the adjectives inside the pronominal DPs. Given certain assumptions, I point out some shortcomings for all of them (if the reader is not interested in the detailed discussion of other proposals, they are invited to proceed to \sectref{sec:key:5.2.4}). In \sectref{sec:key:5.3}, I propose a different analysis that avoids these issues. \sectref{sec:key:5.4} summarizes the discussion.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.2.  Three Types of Proposals
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As discussed in Chapter 2, \sectref{sec:key:3.5}, personal pronouns are determiners in that they take the adjective and noun as their complement (e.g., \citealt{DéchaineWiltschko2002}: 421-22, \citealt{Höhn2020}, \citealt{Pesetsky1978}, \citealt{Postal1966}, \citealt{Roehrs2005}); consider \REF{ex:key:30a} repeated below as \REF{ex:key:32a}. This pronominal DP can be represented in the following, simplified structure in \REF{ex:key:32b} (AgrP abbreviates the rest of the canonical structure discussed at length in Chapters 1 and 2): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%32
    \label{ex:key:32}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{wir nett-en  Studenten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    we nice-\textsc{wk} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘us nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 b.  \textit{(Pro)nominal DP – Complementation} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   (LPP)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              \textit{wir}    CardP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              \textit{netten} \textit{Studenten} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If personal pronouns are indeed determiners, then they should trigger Impoverishment. From this perspective, the weak ending on the following adjective in \REF{ex:key:32a} is expected (in \sectref{sec:key:5.2.4}, I discuss the absence of weak adjectives in certain instances; this will lead to the ultimate proposal that pronominal determiners do not trigger Impoverishment). In order to account for the variation, that is, the unexpected strong ending on the following adjective as in \textit{wir nette Studenten} (cf. \REF{ex:key:30b}), I entertain three basic types of proposals. All these proposals involve the structural analysis in \REF{ex:key:32b} above and some additional assumptions.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.2.1. One Structure: Agreeing vs. Non-agreeing Pronominal Elements
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The first type of proposal involves one structure where the pronominal element is in the same position in \REF{ex:key:32b} above. This position could be Spec,DP or D.\footnote{Note that in the present account, determiners in either of these positions trigger Impoverishment. Having said that, I point out in \sectref{sec:key:5.3.1} that pronominals of the first and second person seem to be associated with a certain kind of deixis (in fact, this kind of deixis will be crucial for the discussion of Impoverishment). Given the deixis, these pronominals are similar to demonstratives and are assumed to project phrasal structures. If so, these pronominal determiners are presumably in Spec,DP.} This is what I assumed in Chapter 2, \sectref{sec:key:3.5} to account for the weak ending on the following adjective. To explain the strong ending, \citet[259]{Roehrs2005} makes a distinction between “agreeing” and “non-agreeing” determiners. Note first that these pronominal elements share concord features with the following adjective and noun; that is, a pronominal in the nominative combines with an adjective and noun in the nominative (and not, say, in the dative). This means that non-agreeing cannot mean non-participation of these pronominal elements as regards concord in agreement features. Rather, we could assume that (non-)agreeing has to do with the presence or absence of agreement morphology on the pronominal element itself. This idea could be made more concrete by assuming a null inflection for the agreeing type \REF{ex:key:33a} and the lack of such an inflection for the non-agreeing type \REF{ex:key:33b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%33
    \label{ex:key:33}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{wir-Ø} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  we-\textsc{infl}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘we’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{wir}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    we
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘we’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Roehrs proposes that an agreeing determiner takes a weak adjective; a non-agreeing determiner takes a strong adjective (see again (30a-b)). Elements like this exist elsewhere in German, with the qualification that they show an overt inflection in the agreeing cases; compare \REF{ex:key:34a} to \REF{ex:key:34b} (the same distribution holds with \textit{solch(e)} ‘such’ and \textit{welch(e)} ‘which):\footnote{Recall from Chapter 2, Footnote Error: Reference source not found that I analyze uninflected \textit{manch} in \REF{ex:key:34b} as a modificational element.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%34
    \label{ex:key:34}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{manch-e gut-en     Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    some-\textsc{st} good-\textsc{wk} friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘some good friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{manch gut-e      Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    some   good-\textsc{st} friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘some good friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note though that the notion of (non-)agreeing determiner is not entirely straightforeward. Unlike with \textit{manch}{}- ‘some’, \textit{solch}{}- ‘such’, and \textit{welch}{}- ‘which’, agreeing pronominals involve an (assumed) null inflection. However, given that first and second-person pronominal elements never have an overt inflection in German, this raises the question if the postulation of a null inflection is indeed warranted. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up, given the issue of a true distinction between agreeing vs. non-agreeing pronominal elements, this proposal seems to lack proper motivation. Furthermore, this distinction would not work in the current system at all as inflections on adjectives are a function of the determiner stem – its categorial feature [+D] – but not its inflection.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.2.2. One Structure: Pronominal Elements in Different Positions
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Similar to the previous subsection, this type of proposal also involves one structure – the one in \REF{ex:key:32b}. However, the pronominal element could be proposed to be in different phrasal levels in that structure. Like in the previous subsection, the pronominal could be in the DP-level. In this case, the pronominal would be a determiner trigging Impoverishment and, consequently, it would occur with a weak adjective. In addition, the pronominal could be in a different structural level. There are two options: either it could be a quantifier in CardP, or it could be a predeterminer in LPP. As explicated in more detail below, Impoverishment would not be triggered in either case, and the following adjective would surface with a strong ending.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As a first option to explain a strong adjective, the pronominal element could do double duty as a quantifier in CardP. This option was entertained in a previous version of this work. As a quantifier, the pronominal element would not have the feature [+D], and as such, it would not trigger Impoverishment (for details, see \citealt{Roehrs2021}, also \citealt{Roehrs2009a}: 166-67). However, there does not seem to be evidence that pronominal elements can also appear as quantifiers. In fact, they can co-occur with numerals, a type of quantifier located in CardP:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%35
    \label{ex:key:35}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{wir drei  Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  we three friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘us three friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As such, this possibility seems unlikely to be on the right track.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The second option to explain a strong adjective involves the claim that pronominal elements can do double duty as predeterminers in LPP. Unlike quantifiers, predeterminers are determiner-like elements with the feature [+D]. Unlike determiners, these elements are base-generated in LPP and neither trigger nor undergo Impoverishment (Chapter 2, \sectref{sec:key:4}). Ambiguous elements like this exist elsewhere in German. For instance, \textit{all-} ‘all’ and \textit{dies-} ‘this’ can be in two positions. When in the DP-level, these elements are determiners triggering Impoverishment \REF{ex:key:36}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%36
    \label{ex:key:36}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{alle gut-en}      \textit{Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all   good-\textsc{wk} friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all good friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{diese gut-en      Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    these good-\textsc{wk} friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘these good friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

When in the LPP-level, they are predeterminers, which do not bring about Impoverishment, neither on a following determiner \REF{ex:key:37} nor on a lower adjective \REF{ex:key:38}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%37
    \label{ex:key:37}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{alle mein-e} \textit{gut-en}      \textit{Freunde} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all   my-\textsc{st}  good-\textsc{wk} friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all my good friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.  \textit{diese mein-e gut-en     Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    these my-\textsc{st} good-\textsc{wk} friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘these good friends of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%38
    \label{ex:key:38}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{all mein} \textit{groß-es  Glück}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all my    great-\textsc{st} happiness.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘all my great happiness’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{dieses mein groß-es  Glück}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this     my    great-\textsc{st} happiness.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘this my great happiness’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Before moving on, note that this conjecture is more promising as pronominals of the first and second person seem to be associated with deixis; that is, these pronominals are similar to the demonstrative \textit{dies-} ‘this’ above, which can appear as either a determiner or a predeterminer.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Again, it is hard to provide direct independent evidence for the claim that pronominal elements can be in two different positions – in this case, the DP-layer for pronominals as determiners and the LPP-layer for pronominals as predeterminers. On the one hand, these pronominal elements are distributionally restricted (they must occur before numerals, adjectives, and nouns); on the other hand, first and second-person pronominals are inflectionally restricted (they exhibit no endings). Thus far, I have come across only one distributional restriction: these pronominals cannot co-occur with ordinary predeterminers in German (39a-b):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%39
    \label{ex:key:39}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{all(e)   wir Studenten}    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  all(\textsc{st}) we  students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘all us students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{diese wir Studenten} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  these we  students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘(these) us students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Let us consider how the data in \REF{ex:key:39} bear on the question of whether pronominals can be determiners or predeterminers. As we see momentarily, the explanation of the ungrammaticality of \REF{ex:key:39} under the option of pronominals as determiners has an independent (and indeed language-specific) explanation. In contrast, the explanation of the ungrammaticality of \REF{ex:key:39} under the option of pronominals as predeterminers receives a more general explanation. We will wind up concluding that pronominals are indeed determiners but do not do double duty as predeterminers.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with the option of pronominal elements as determiners, \citet[53]{Haider1988} discusses the contrast between the possessive article in \REF{ex:key:40a} and the Saxon Genitive in \REF{ex:key:40b} (see also \citealt{Haider1992}: 314-15, \citealt{Bayer2015}). Both of these elements are usually assumed to be in the DP-level:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%40
    \label{ex:key:40}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{all(e)   ihr-e    Autos}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all(\textsc{st}) her-\textsc{st} cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all her cars’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{all(e)   Marias Autos}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all(\textsc{st}) Mary’s cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all Mary’s cars’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To the constrast in \REF{ex:key:40}, we can add a similar distinction involving the predeterminer \textit{dies-} ‘this’ \REF{ex:key:41}. While the (a)-example may sound slightly marked, there is a clear contrast between \REF{ex:key:41a} and \REF{ex:key:41b}, just as in \REF{ex:key:40} above: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%41
    \label{ex:key:41}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  ?  \textit{dies-e     ihr-e   Autos}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    these-\textsc{st} her-\textsc{st} cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘these cars of hers’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  *  \textit{dies-e     Marias Autos}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    these-\textsc{st} Mary’s cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘(these) Mary’s cars’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The difference between \textit{ihre} ‘her’ in \REF{ex:key:40a} and \REF{ex:key:41a}, on the one hand, and \textit{wir} ‘we’ in (39a-b) and \textit{Marias} ‘Mary’s’ in \REF{ex:key:40b} and \REF{ex:key:41b}, on the other hand, is that the former has an overt inflection but latter do not. Indeed, \textit{wir} and \textit{Marias} never take an overt adjectival inflection (unlike \textit{mein} in \REF{ex:key:38}, which, for instance, may appear as \textit{mein-em} in dative masculine/neuter contexts). The failure to (ever) take an inflection may account for this constrast (for fuller discussion, see \citealt{Roehrs2021}). If this is on the right track, then we can account for the ungrammaticality of (39a-b) under the option of pronominals as determiners on par with the ungrammaticality of the determiner-like element \textit{Marias} in \REF{ex:key:40b} and \REF{ex:key:41b}. Given that the ungrammaticality receives an independent explanation, we can continue maintaining that pronominals are determiners.\footnote{This is confirmed by the fact that this is a language-specific restriction. Nominals similar to \REF{ex:key:39a} above are possible in English, as pointed out by \citet[353]{Pesetsky1978}  (i)  all us linguists}  Indeed, this explanation is based on the very assumption that pronominal elements are in fact determiners in the DP-level, just like possessive articles and Saxon Genitives. To repeat, what is crucial here is that determiner(-like) elements in German have to be inflected if they follow a predeterminer. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As to the option of pronominal elements as predeterminers, it could be suggested that pronominals can also be in LPP. As seen in \REF{ex:key:42}, ordinary predeterminers can be stacked: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%42
    \label{ex:key:42}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{all-e   dies-e    mein-e Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  all-\textsc{st} these-\textsc{st} my-\textsc{st} friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘all these friends of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As such, there is no structural competition between predeterminers – each can be housed in its own LPP. In other words, the occurrence of multiple predeterminers should be fine. However, as we see momentarily, while analyzing the strings in (39a-b) in such a way provides an explanation of the ungrammaticality of these cases, it does not for the simpler, grammatical cases as in \REF{ex:key:30b}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that pronominal DPs are definite in interpretation. If pronominal elements as predeterminer are in LPP, then the DP-level does not involve an overt determiner (e.g., a definite article) or determiner-like element (e.g., a Saxon Genitive). This, however, is something that is required for definite noun phrases. Note in this regard that \textit{Studenten} ‘students’ by itself cannot have a definite interpretation – the DP-level must be filled with a relevant overt element (see \citealt{Alexiadou2005}, \citealt{Longobardi1994}). Thus, the option of pronominals as predeterminers can also explain the ungrammaticality of \REF{ex:key:39}, but, unlike the option of pronominals as determiners, this option receives a more general explanation. To repeat, the DP-layer must have an overt determiner(-like) element when the nominal is definite in interpretation.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With this in mind, I return to the simple cases like \textit{wir nette Studenten} ‘we nice-\textsc{st} students’ as in \REF{ex:key:30b}. If the discussion above is on the right track, then this means that such grammatical strings cannot be analyzed as \textit{wir} involving a predeterminer, as the DP-layer would not contain an overt element although the nominal is definite in interpretation. Such a structure would lead to ungrammaticality. However, since this simple pronominal DP is fine, we can argue that the pronominal must be a determiner in the DP-level here. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To conclude, both options of pronominal as quantifier and pronominal as predeterminer are unlikely to be correct. As such, the variation of the inflection on the adjective remains unclear on those assumptions.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.2.3. Two Different Structures: Complementation and Adjunction  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Besides the structure in \REF{ex:key:32b}, a second structure could be proposed to account for the possibility of a strong ending on the adjective. Beside being the complement of the pronominal determiner, we could assume that the adjective and noun is right-adjoined to the pronominal determiner. There are two basic options: the adjective and noun could be adjoined either high or in the middle of the structure. High right-adjunction was utilized in the discussion of appositional DPs (Chapter 2, \sectref{sec:key:3.4}). The structure of the pronominal DP would look as follows:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%43
    \label{ex:key:43}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{wir nett-e   Studenten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    we nice-\textsc{st} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘us nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 b.  \textit{Pronominal DP – High Right-Adjunction}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  DP    AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  wir      nette Studenten
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Importantly, unlike appositional DPs, pronominal DPs do not involve comma intonation, and their adjectives may, given the right conditions, receive a restrictive interpretation (in the plural cases). Alternatively, the adjunction could be located in the middle \REF{ex:key:44}, where XP might range over CardP, AgrP, ArtP, NumP, or NP (recall that e\textsubscript{N} is the null head noun of a pronominal DP with an intransitive determiner):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%44
    \label{ex:key:44}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Pronominal DP – Mid Right-Adjunction}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{wir}     XP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     XP    AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{nette Studenten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     \textit{e\textsubscript{N}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Mid right-adjunction was employed in the discussion of indefinite pronoun constructions. The latter construction does not involve comma intonation and its adjective may have a restrictive interpretation. These properties fit better with pronominal DPs. Thus, unlike high adjunction, this structure presents a more promising candidate. Note now that this is a non-canonical structure. As such, Impoverishment would not occur, and a strong ending would surface on the adjective. Observe that assuming two structures (complementation, adjunction) would essentially result in the more general claim that pronominals are equally transitive and intransitive (cf. \citealt{Bhatt1990}: 155), where complementation of the adjective and noun would instantiate a transitive pronominal element, but adjunction of the adjective and noun would manifest an intransitive pronominal element. This claim is interesting and requires further investigation, something I will not pursue here.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To take stock thus far, note that despite the issues raised for the first two types of accounts above, all three proposals can potentially account for the strong adjectives, which are possible in all contexts. However, given these proposals, it is not clear why weak adjectives can occur in only some contexts. I make this issue of the three proposals more explicit in the next subsection.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.2.4. Absence of Weak Adjectives in Certain Instances
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that the different structural options above were entertained to account for the variation of the inflections on the adjective following the pronominal determiner. As mentioned above, pronominal determiners are definite elements. As such, we expect weak adjectives to occur with them. However, as documented above, all featural combinations pronominal DPs appear in exhibit strong adjectives but only some combinations allow weak adjectives. Weak adjectives are missing in three instances: nominative and accusative singular (which is very clear in the masculine/neuter) and accusative plural. All three types of proposals discussed above have to come to terms with the variation in some contexts but the lack of it in others – the three instances just mentioned. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As I show below, all three proposals can account for the absence of weak adjectives in the nominative/accusative singular. Given certain assumptions, they all can account for the lack of variation here. It will turn out though that including the dative singular in the discussion will bring out issues for all three proposals. The absence of the weak adjectives in the accusative plural is special. As far as I can see, none of the three proposals can rule them out without ad-hoc stipulations. At the end of this subsection, I provide a functional explanation for the absence of weak adjectives here.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In order to account for the absence of weak adjectives in the nominative/accusative singular, we could follow \citet{Roehrs2005} in observing that the strong adjectives in these contexts are mirrored by certain strings involving \textit{ein}{}-words (also \citealt{Bhatt1990}: 155). The first proposal could suggest that the pronominal elements involve non-agreeing determiners. Like \textit{ein}, these instances of the pronominal determiner would have no inflections at all, and consequently the adjectives would be strong. The second proposal could also assume that these instances of the pronominal determiner are like \textit{ein}{}-words. However, unlike the first proposal, this account could follow the assumptions in Chapter 2 claiming that these pronominal determiners do not induce Impoverishment. Consequently, the adjectives would be strong. The third proposal could take a different route. It could claim that all instances in the singular, including the dative, only involve adjunction (and not complementation) of the adjective and noun. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If so, the question arises why the dative singular does involve variation. The first two proposals have a straightforward answer: \textit{ein}{}-words are only special in the nominative and accusative singular. The third proposal has to make additional assumptions to allow weak adjectives in the dative. Now, while this may indicate an advantage of the first two proposals, an account involving \textit{ein}{}-words is not straightforward. As discussed in Chapter 2, \sectref{sec:key:2.2.3} and Chapter 5, \textit{ein}{}-words are composite forms consisting of semantically vacuous \textit{ein} and another component. As such, it is \textit{ein} itself that has special properties. Crucially, \textit{ein} contrasts with pronominal elements as regards definiteness. This makes these two types of elements rather different from each other, and an extension of the properties of \textit{ein} to these pronominal elements does not seem straightforward. To be clear, given these assumptions, all three proposals face some challenges, either the parallel to \textit{ein}{}-words is not straightforward or additional assumptions have to be made to explain all the data. It appears then as if a different solution needs to be found (see next sextion).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Turning to the instances in the accusative plural, recall that a weak adjective cannot occur in this context \REF{ex:key:45a}. Furthermore, observe again that the dative counterpart in \REF{ex:key:45b} exhibits the ending -\textit{en} on the adjective, which is ambiguous between strong and weak:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%45
    \label{ex:key:45}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{für uns nett-e(*n)     Schüler}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    for us   nice-\textsc{st}/*\textsc{wk} pupils
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘for us nice pupils’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 b. \textit{von   uns nett-en        Schüler-n}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    from us   nice-\textsc{st/wk} pupils-\textsc{dat}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘from us nice pupils’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note now that case is not overtly manifested by a unique form of the pronominal element – it is \textit{uns} ‘us’ (or \textit{euch} ‘you(\textsc{pl})’) in both the accusative and dative cases. In addition, notice also that the weak ending in the accusative plural would be -\textit{en}, the same as the (ambiguous) ending in the dative plural. As such, it is the different inflections on the adjectives in (45a-b) that distinguish the accusative from the dative.\footnote{Note that the head noun has the case inflection -\textit{n} in the dative in \REF{ex:key:45b}, but not in the accusative in \REF{ex:key:45a} This inflection may potentially indicate case in the dative. There are two points that need to be made here. First, case in this instance would be manifested on the noun (rather than on the pronominal element). Second, \citet[154-63]{Wegener1995} and \citet[289]{Gallmann1996} point out that this case inflection on the noun has become somewhat unstable. Furthermore, this case marking cannot occur at all if the plural of the noun is formed in -\textit{n} or -\textit{s}, for instance, \textit{Junge-n-(*en)} ‘boy-s’ or \textit{Jung-s-(*en)} ‘(coll.) guy-s’. Given these points, I put the case marking on the noun aside.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  It is not clear how the three proposals can account for the lack of variation in the accusative plural: \textit{ein}{}-words are only special in the singular instances, and complementation (which allows weak inflections) should not be restricted to specific morphological cases like nominative and dative. As such, the accusative plural instances seem to be special. Given this state-of-affairs, I follow \citet[39]{Duden2007} in providing a functional explanation of the absence of the weak inflection here. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I assume that morphological case needs to be distinguished (if possible). Since the cases at hand involve dative and accusative, this may be related to their typical grammatical functions, indirect object and direct object, respectively. Now, as is well known, German has relatively free word order due to the availability of scrambling its objects. \citet[261-62]{Roehrs2005} provides the following examples where the pronominal DPs are in the order of indirect object > direct object in \REF{ex:key:46a} but in the sequence of direct object > indirect object in \REF{ex:key:46b}:  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%46
    \label{ex:key:46}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Sie   haben uns nett-en                Jungen euch klug-e            Mädchen vorgestellt}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    they have   us   nice-\textsc{dat.st/wk} boys      you  smart-\textsc{acc.st} girls         introduced
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘They introduced you smart girls to us nice boys.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Sie   haben uns nett-e           Jungen euch klug-en                 Mädchen vorgestellt}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    they have   us   nice-\textsc{acc.st} boys     you   smart-\textsc{dat.st/wk} girls         introduced
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘They introduced us nice boys to you smart girls.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that despite the two orderings of the objects in \REF{ex:key:46a} vs. \REF{ex:key:46b}, the different inflections on the adjectives inside the objects make it clear who was introduced to whom in each case. This functional explanation of the strong inflection in the accusative plural – the strong adjective clearly distinguishes accusative objects from dative objects – can apply to all three (and other) proposals. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Returning to the first part of this section, given the issues with the postulation of (non-)agreeing pronominals in the first proposal, with the suggestion that pronominals can do double duty as determiners and quantifiers or predeterminers in the second account, and with the claim of two co-existing types of structures in the third analysis, I make a different suggestion. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.3.  Pronominal Determiners and Complementation 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I offer my proposal of the structure of pronominal DPs. I also discuss the structure and feature specifications of the pronominal determiners themselves, I provide the vocabulary insertion rules for pronominal elements, and I account for the preferences of strong or weak adjectives in certain pronominal DPs. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.3.1. Structures and Feature Specifications
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I propose that pronominal DPs involve complementation and that pronominal elements are determiners that lack features that make [+D] a trigger of Impoverishment. In other words, this proposal postulates one structure and one type of pronominal element. The fact that Impoverishment does not occur accounts for the general option of strong adjectives. The restricted occurrence of weak adjectives follows from certain phonetic considerations – the phonetic rule discussed in \sectref{sec:key:2.2} and phonetically conditioned analogy with ordinary definite DPs. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As far as I am aware, the proposal sketched just above is the simplest account with the fewest additional assumptions. Furthermore, proposing that pronominal DPs involve complemenation is compatible with the discussion in later chapters. Specifically, Chapters 6 and 7 investigate similarities and differences between noun phrases (DPs) and clauses (TPs) of the form “pronoun + (copular verb) + noun”. TPs involve complementation and if DPs also constitute cases of complementation, where the string “pronoun + noun” is analyzed as the determiner taking the noun as its complement, then this allows a straightforward comparison between the nominal and clausal domains.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The proposal that pronominal elements are determiners is compabitle with work by \citet{Postal1966} and others, briefly discussed in Chapter 2, \sectref{sec:key:3.5}. As determiners, pronominals have the categorial feature [+D]. They are merged in ArtP and take overt complements. I propose that the difference in inflections on adjectives in ordinary vs. pronominal DPs lies in the different structure and feature specifications of the pronominal determiners themselves. At first glance, there seem to be two options. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Pronominal elements could involve structures of demonstratives as in \REF{ex:key:47a}. Recalling that the relevant pronominal determiners are of the first and second person, they are associated with deixis, similar (but not identical) to that of third-person demonstratives. I follow \citet[429]{Halle1997} and \citet[288]{Nevins2007} in that grammatical person can be broken down into the features [±AUTH(or)] and [±PART(icipant)]: first person consists of [+AUTH, +PART], second person involves [-AUTH, +PART], and third person has [-AUTH, -PART].\footnote{It is possible that third person only includes [-PART].} As an alternative to \REF{ex:key:47a}, pronominal determiners could project reduced structures where InflP is missing at the top \REF{ex:key:47b}. This yields the following feature specifications for the two structural options of, say, the first-person pronominal \textit{wir} ‘we’:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%47
    \label{ex:key:47}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Pronominal Determiner (Variant 1)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 InflP\textsubscript{[+D; +AUTH, +PART][F, N, O, S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          [F, N, O, S]    DemP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            {\textbar}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        Dem
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              [+D; +AUTH, +PART]    →  \textit{wir}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Pronominal Determiner (Variant 2)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                DemP\textsubscript{[+D; +AUTH, +PART]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          {\textbar}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Dem
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           [+D; +AUTH, +PART]    →  \textit{wir}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The immediate advantage of the variant in \REF{ex:key:47a} is that all phrasal determiners have the same basic structure. As for \REF{ex:key:47b}, this reduced demonstrative structure provides a straightforward account of the absence of inflections on first and second-person elements. Note that neither of the two structures involves features for definiteness or deixis. However, first and second person (i.e., [+PART]) presumably entail definiteness, and the category person is associated and indeed relatable to the category deixis (see, e.g., \citealt{Lyons1999}: 18-19, \citealt{Roehrs2019}: 378-79). Crucially, with [+DEF] and [+DEIX] absent, the presence of the CNG feature bundle in \REF{ex:key:47a} but its absence in \REF{ex:key:47b} have different consequences for the occurrence of Impoverishment. Thus, depending on which of the two structural variants in \REF{ex:key:47} is adopted, we need to find different ways to account for the general variation and the varying preferences as regards the inflectional distribution on the adjectives. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  If we adopt \REF{ex:key:47a}, we expect the adjectives after the pronominal determiners to pattern with those after \textit{ein}{}-words – the featural specification in \REF{ex:key:47a} is similar to the article \textit{ein} in that Impoverishment is not triggered in [-F, -O] contexts. In other words, the masculine/neuter instances in the nominative/accusative are expected to surface with strong adjectives. By contrast, Impoverishment is triggered in all other featural contexts yielding weak adjectives in the remaining cases (including the nominative/accusative feminine, where both the strong and the weak inflections are -\textit{e}). However, it is not clear how to account for the strong inflections in the oblique and/or plural instances. By assumption, there is no other structural option available as regards the larger DP (as all these pronominal DPs involve canonical structures). Furthermore, note that an explanation involving analogy with other, related DPs is not straightforward as definite DPs of the form “determiner + adjective + noun” do not have strong inflections in these instances either. This means that the strong inflections in the dative singular and in the plural instances remain unaccounted for. The variant in \REF{ex:key:47b} fares better.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If \REF{ex:key:47b} is adopted, all adjectives should be strong. With no feature present that makes [+D] a trigger of Impoverishment, feature deletion does not occur at all. This accounts for the general option of strong adjectives. The restricted occurrence of weak adjectives requires a different account. There are three cases to consider. Note that all three can be related by certain phonetic considerations. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

First, recall that instances in the dative masculine/neuter involve strong adjectives ending in -\textit{em} or weak adjectives ending in -\textit{en}. Notice that the weak inflection has a phonetic resemblance to the strong inflection – both involve nasal sounds. Extending the discussion in \sectref{sec:key:2.2}, I assume that this presents another case of nasal alternation such that -\textit{em} can be changed into -\textit{en}, provided a nominal element (here: the pronominal) precedes the adjective.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Second, the dative feminine instances involve phonetic resemblance between – what sounds like – strong inflections on the pronominal determiners \textit{mi-r} ‘me(\textsc{dat})’ and \textit{di-r} ‘you(\textsc{dat.sgl})’ and the strong inflection -\textit{er} on following adjectives. This brings about a special distribution such that two strong inflections occur on two different lexical categories at the same time (i.e., the pronominal determiner and adjective). Sequences like \textit{mi-r} \textit{groß-er Gans} ‘me (stupid idiot)’ do not exist elsewhere in the system. Following Duden (1995: 281, 2007: 39), I assume that speakers tend to avoid such strings. Note again that the final sound on \textit{mir} and \textit{dir} is the same as the inflection on the dative feminine article \textit{der} ‘the’. I suggest that the weak adjectives are the result of analogy with other definite DPs (e.g., \textit{d-er nett-en Frau} ‘the-\textsc{st} nice-\textsc{wk} woman’). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Before moving on to the third case, the plural instances, notice that there is no phonetic rememblance in the nominative/accusative singular, neither as regards the nasal alternation on adjectives nor as regards the inflections on adjectives and the final sound on the pronominal determiners: the strong inflections -\textit{er}, -\textit{en}, -\textit{es}, -\textit{e} are very different from (or identical to) the weak inflection -\textit{e}, and all these inflections are different from the final sound on the pronominal determiners \textit{ich} ‘I’, \textit{mich} ‘me(\textsc{acc})’, \textit{du} ‘you(\textsc{nom.sgl})’, \textit{dich} ‘you(\textsc{acc.sgl})’. This means that the phonetic rule and analogy do not play a role in the nominative/accusative singular intances – as mentioned above, there is no variation here at all.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The third case involves the instances in the plural. Starting with the dative, recall that the inflection is -\textit{en}, which is ambiguous between strong and weak. Given my analysis, I interpret this inflection as strong. Continuing with the nominative, note that we cannot easily state that the weak endings here are also due to the phonetic considerations mentioned above: the strong inflection -\textit{e} is different from the weak inflection -\textit{en}, and both of these inflections are different from the final sound on the pronominals \textit{wir} ‘we’ and \textit{ihr} ‘you(\textsc{nom.pl})’. However, like in the dative feminine, the forms of the ordinary and pronominal determiners are related. Unlike in the dative feminine, here the relatedness concerns the fact that \textit{die} ‘the/those’ has no inflection, just like \textit{wir} and \textit{ihr}. I suggest that the weak adjectives in the nominative plural are also due to analogy with other definite DPs (e.g., \textit{die nett-en Frauen} ‘the nice-\textsc{wk} women’). Finally, observe that uninflected \textit{die} also occurs in the accusative plural. However, as discussed in \sectref{sec:key:5.2.4}, pronominal DPs in the accusative plural are special. I assume that the analogy is blocked here – the absence of the weak adjectives has a functional explanation.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Above, I made use of analogy with ordinary definite DPs in two instances, in the dative feminine and in the nominative plural. In order to constrain the application of analogy, I have assumed that analogy is only possible if there are special phonetic circumstances as regards the determiners: related endings on \textit{mi-r/di-r} and \textit{d-er}, and no inflections on \textit{wir/ihr} and \textit{die}. Note in passing that the presence or absence of endings on determiners seems to play a role here for the inflections on adjectives. This is reminiscent of the traditional generalization of Weak After Strong. However, both cases bring about weak adjectives by analogy, making these cases different from \textit{ein}{}-words (which have strong endings in the exceptional cases).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up, all pronominal DPs may involve strong adjectives. Assuming complementation, this is explained by the reduced demonstrative structure in \REF{ex:key:47b} and the feature specifications of the first and second-person pronominals. As a consequence, Impoverishment does not occur resulting in strong adjectives. Weak adjectives in the dative masculine/neuter are due to the fairly general phonetic rule that accounts for the nasal alternation on adjectives. Weak adjectives in the dative feminine and nominative plural are due to analogy (constrained by the similarity of the inflections of the ordinary determiners and the final sounds of the pronominal determiners). In other words, weak adjectives in all these cases only occur under specific phonetic conditions. Also, note again that masculine/neuter pattern in opposition to feminine/plural, which, as seen before, is a more general property of the nominal system in German. Before turning to the discussion of the different preferences as regards strong or weak adjectives, I provide the vocabulary insertion rules for pronominal elements.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.3.2. Vocabulary Insertion Rules for Pronominal Elements
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I illustrate the vocabulary insertion rules with first-person elements (second-person items are like the first-person ones but have a negative value for [AUTH]). Third-person pronominals are provided for completeness’ sake (for some discussion, see also \citealt{Höhn2020}). Starting with the first-person pronominals, I take genitive pronouns to be the most specific elements. Recall though that they do not take overt complements. This is indicated in (48a-b) such that no overt element can be in the right edge of the noun phrase. Note also that the CNG features after the forward slash sign in \REF{ex:key:48} specify the featural context of insertion. As to the pronominals that do take overt complements, I pointed out in \sectref{sec:key:5.1} that \textit{wir} ‘we’ is the only (transitive) pronominal that has a unique form for the relevant CNG features. Indeed, singular pronominals do not distinguish different genders, and \textit{uns} ‘us(\textsc{acc/dat})’ is the only form that does not distinguish (accusative/dative) case. Importantly, note again that accusative and dative case do not form a natural class in German (i.e., they are not syncretic except here). Given these points, I take \textit{wir} as the most specific transitive element \REF{ex:key:48c}, and \textit{uns} as the least specific transitive item \REF{ex:key:48g}. The latter presents the elsewhere case. The remaining, singular pronouns are provided in (48d-f). The vocabulary insertion rules below are ordered by decreasing specificity and are grouped according to case. Similar to the weak adjectival ending -\textit{e} (Chapter 2, \sectref{sec:key:2.1.5}), I utilize the category variable [γ], where a negative value indicates singular (48e-f):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%48
    \label{ex:key:48}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [+D; +AUTH, +PART]  →   \textit{unserer} / \_\_]\textsubscript{φ} [+F, +N, +O, +S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [+D; +AUTH, +PART]  →  \textit{meiner}  / \_\_]\textsubscript{φ} [+O, +S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  [+D; +AUTH, +PART]  →  \textit{wir}   \textsubscript{} /         \textsubscript{} [+F, +N, -O,\textsubscript{}  {}-S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  [+D; +AUTH, +PART]  →  \textit{ich}        /         \textsubscript{} [ -O,  {}-S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  e.  [+D; +AUTH, +PART]  →  \textit{mich}     /          [-γ,  {}-O]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  f.  [+D; +AUTH, +PART]  →  \textit{mir}   /          [-γ]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  g.  [+D; +AUTH, +PART]  →  \textit{uns} \textsubscript{}            \textsubscript{}   
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To round off the discussion, I also provide the vocabulary insertion rules for the third-person pronominals. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Without going into much detail here, it is clear that pronominals like \textit{er} ‘he’ have to be distinguished from ordinary determiners like \textit{der-}words\textit{, ein-}words, or the null articles. Note that pronominals are inherently definite but that ordinary determiners are specified for definiteness (see again the different determiner structures in Chapter 2, \sectref{sec:key:2.1.6}). As such, I assume that the features [-AUTH] and/or [-PART] also entail definiteness. This allows me to leave out the feature for definiteness in the vocabulary insertion rules, provided in \REF{ex:key:49}.\footnote{If this turns out to be untenable, then the feature [+DEF] could be added to these pronominals. Note that this does not affect the discussion of Impoverishment as third-person pronominals do not take adjectives (and nouns) as their complement.} As above, the rules are ordered with decreasing specificity and are grouped according to case (if possible). Note that unlike first and second-person pronominals, their third-person counterparts involve adjectival inflections. Consequently, they have separate CNG feature bundles. As briefly discussed in Chapter 2, \sectref{sec:key:2.2.3}, I single out exceptional \textit{ihn} ‘him’ as the most specific case \REF{ex:key:49a}. Recall also that I pointed out above that the genitive pronominals do not have regular inflections. As such, I assume that like uninflected \textit{ein}, they spell out the stem features and the CNG features by one morpheme (49b-c). Furthermore, \REF{ex:key:49c} and \REF{ex:key:49d} are equally specified but differ in the value of [O]. The remaining pronominals are given in (49e-f), with \REF{ex:key:49f} forming the elsewhere case. The inflections for \REF{ex:key:49a} and (49d-f) are given in (49g-h) (for some discussion, see also Gunkel \textit{et al}. 2017: 1298):\footnote{The elsewhere case in \REF{ex:key:49f} involves an additional -\textit{en} in the dative plural. This makes \textit{ih-n-en} ‘them’ similar to the dative plural pronominal \textit{d-en-en} ‘those (ones)’ (for the historical development of this additional -\textit{en}, see \citealt{Lühr1991}). Interestingly, this additional -\textit{en} is not possible with \textit{ein}{}-words (e.g., *\textit{mein-en-en} ‘mine’, *\textit{kein-en-en} ‘none’). Again, what the latter elements have in common is that they consist of vacuous \textit{ein} and another component. The obvious difference between the first type of elements and \textit{ein} is that the first involves definiteness. There are different ways to account for this additional -\textit{en} on the definite pronominals. We could follow, for instance, work by Corver \& van Koppen (2010, 2011b), who propose that -\textit{en} in Dutch is generated in a lower position, or we could formulate a late insertion rule. I leave the decision between these options for future research.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%49
    \label{ex:key:49}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [+D; -AUTH, -PART]     →   \textit{ih-}\textsubscript{}         / \_\_]\textsubscript{φ} [-F, -N, -O, +S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  [+D; -AUTH, -PART]  [ -F, +O, +S]  →   \textit{seiner  \textsubscript{} } / \_\_]\textsubscript{φ} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  [+D; -AUTH, -PART][+O, +S]  →   \textit{ihrer  \textsubscript{} } / \_\_]\textsubscript{φ} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

d.  [+D; -AUTH, -PART]      →   \textit{e-  \textsubscript{} } / \_\_]\textsubscript{φ} [ -F,  {}-O]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

e.  [+D; -AUTH, -PART]      →   \textit{sie-}        / \_\_]\textsubscript{φ} [-O]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

f.  [+D; -AUTH, -PART]      →   \textit{ih-}\textsubscript{}  / \_\_]\textsubscript{φ}   
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

g.  [+F, -N, +O, αS]       →  \textit{{}-r}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  h.  etc.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall from \sectref{sec:key:3} that the inflection on \textit{sie} ‘she, her; they, them’ is later deleted to avoid a hiatus.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.3.3. Preferences for Strong or Weak Adjectives
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As discussed above, there is no variation as regards the inflections on the adjectives after \textit{ich} ‘I’, \textit{mich} ‘me(\textsc{acc})’, \textit{du} ‘you(\textsc{nom.sgl})’, \textit{dich} ‘you(\textsc{acc.sgl})’, \textit{uns} ‘us(\textsc{acc/dat})’, \textit{euch} ‘you(\textsc{acc/dat.pl})’. All these adjectives are strong – there are no specific phonetic conditions that induce the phonetic rule from \sectref{sec:key:2.2} or facilitate analogy with other definite DPs. In contrast, dative singular \textit{mir} ‘me(\textsc{dat})’ and \textit{dir} ‘you(\textsc{dat.sgl})’, and nominative plural \textit{wir} ‘we’ and \textit{ihr} ‘you(\textsc{nom.pl})’ do involve variation where both strong and weak adjectives are possible. The account of this variation was based on certain phonological conditions. Interestingly, the preference for either strong or weak adjectives is not the same. I propose that this has to do with the underlying features of the larger noun phrase and the specificity of the vocabulary insertion rules of the pronominal determiners.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that elements in DPs including pronominal DPs share concord features in case, number, and gender. As is well known, ordinary DPs involving count nouns require a determiner. In fact, as has been observed for German, it is the determiner that typically spells out the features for case, number, and gender on its inflection.\footnote{This is particularly clear with gender, which is inherently specified on the noun but has no overt manifestation on it.} The inflection on the determiner is typically strong. This is different for the cases under discussion here. While first and second-person pronominals also combine with count nouns, they do not have inflections. In fact, they do not have spell-out forms specified for the different genders in the singular, and with the exception of \textit{wir} ‘we’ and \textit{ihr} ‘you(\textsc{nom.pl})’, they do not have spell-out forms specified for the different cases in the plural (\sectref{sec:key:5.3.1}). I propose that if an adjective is present, it is the latter that preferably surfaces with a strong ending thereby providing a more specified spell-out form of the underlying features of the noun phrase. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \tabref{tab:key:9} lists the four pronouns that involve varying inflections on their following adjectives. It shows which case, number, or gender the pronominal forms distinguish, and it contains the featural contexts in which their corresponding vocabulary insertion rules apply (\sectref{sec:key:5.3.2}). Finally, the table repeats the general variation and the varying preferences, pointing out again that strong adjectives are preferred in dative masculine/neuter contexts and weak adjectives in dative feminine and nominative pural environments (the varying specification of [F] in the last column distinguishes the cases in the masculine/neuter and feminine):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Specifications of Pronominal Elements and their Relation to the Inflection on the Adjective}
\label{tab:key:9}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Pronom.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

element & \multicolumn{2}{c}{ Unique form for} & Featural contexts of vocabulary insert. rules & Ending on the following adjective\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

& Case & Number/gender &  & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{mir}, \textit{dir} & dative & unspecified (gender) & [{}-γ] & [ -F]:       strong/\textsuperscript{\%}weak\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

&  &  &  & [+F]:       weak/\textsuperscript{\%}strong\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{wir}, \textit{ihr} & nominative & plural & [+F, +N, +O, +S] & \textsubscript{} weak/\textsuperscript{\%}strong\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is important to state that, while adjectival inflections spell out CNG features (Chapter 2, \sectref{sec:key:2.1.5}), pronominals of the first and second person do not. As seen in the previous subsection, these pronominals involve a reduced demonstrative structure and are inserted in the context of CNG features. Note though that the vocabulary insertion rules of adjectival inflections and pronominal determiners do have something in common: they are ordered as regards the specificity of insertion such that strong inflections take precedence over weak inflections, and \textit{wir} ‘we’ and \textit{ihr} ‘you(\textsc{nom.pl})’ take precedence over the other relevant pronominal determiners.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Abstracting away from the dative feminine instances for a moment, \tabref{tab:key:9} suggests a certain correlation provided in \REF{ex:key:50a} and \REF{ex:key:50b}. This correlation can be collapsed into the statement in \REF{ex:key:50c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%50
    \label{ex:key:50}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  less specified pronominal {\textasciitilde} preference for more specified (/strong) adjective
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  more specified pronominal {\textasciitilde} preference for less specified (/weak) adjective
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  DPs prefer one element to be more specified (where the element is of the lexical       category determiner or adjective).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Some comments are in order here. In both \REF{ex:key:50a} and \REF{ex:key:50b}, a more specified and a less specified element combine (cf. again the last two columns of \tabref{tab:key:9}). Put differently, it appears as if more specified pronominals and strong adjectives have something in common, and less specified pronominals and weak adjectives have something in common – the two groups differ in their degree of specificity and stand in opposition to each other. Note again that this correlation is not about the spell-out of features, but rather it is about the specificity of the vocabulary insertion rules involved. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Furthermore, the unified, more general correlation in \REF{ex:key:50c} states a preference, not a requirement. Since the form of the pronominal is determined by the morpho-syntactic and semantic context, the actual choice has to do with the vocabulary insertion rule that yields the preferred inflection on the adjective (NB: the use of the term preference is also compatible with the fact that adjectives are generally optional). Observe that this is different from, for instance, the generalization Weak After Strong, which describes an either/or distribution of the different inflectional forms. Finally, note also that \REF{ex:key:50c} is not in conflict with Weak After Strong as \REF{ex:key:50c} involves a weaker statement. With these preliminary remarks in place, consider the three individual cases distinguished in the last column of \tabref{tab:key:9}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I start with \textit{wir} ‘we’ and \textit{ihr} ‘you(\textsc{nom.pl})’. These elements are the only relevant first and second-person pronominals whose vocabulary insertion rules involve a fully specified context as regards CNG features (i.e., [+F, +N, +O, +S]). I propose that since all the underlying features of the larger noun phrase are part of the vocabulary insertion rules for \textit{wir} and \textit{ihr}, the less specified weak inflection is preferred. The latter involves -\textit{en}, the vocabulary insertion rule of which is the elsewhere case.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Singular pronominals of the first and second person are not specified for gender. The vocabulary insertion rules of the dative singular pronominals \textit{mir} ‘me(\textsc{dat})’ and \textit{dir} ‘you(\textsc{dat.sgl})’ are specified as [-γ]. Note that this involves a low degree of specificity. Starting with the dative masculine/neuter instances, I propose that the inflectable adjective is recruited to indicate the gender of the larger noun phrase. Specifically, since gender is not specified in the vocabulary insertion rules of the relevant pronominals, a more specified inflection on the adjective is preferred. Note in this regard that the vocabulary insertion rule for the strong inflection -\textit{em} is specified as [-F, +O, -S] (see Chapter 2, \sectref{sec:key:2.1.5}). This explains the preference for the strong adjective in the dative masculine/neuter instances. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Finally, I turn to the feminine singular cases. Given that the dative pronominal in feminine contexts is also specified as [-γ], the question arises why these instances prefer weak adjectives. I suggest that this involves the same type of explanation that was offered in \sectref{sec:key:5.3.1} to account for the very existence of weak adjectives in this context. Again, I follow Duden (1995: 281, 2007: 39) in observing that the feminine instances lead to – what sounds like – the sequence of two strong endings (e.g., \textit{mi-r} \textit{groß-er Gans}). It seems as if speakers tend to avoid similar endings on co-occurring but lexically different elements (i.e., the pronominal element and adjective). Note though, if -\textit{r} on the pronominal element were to be interpreted as a strong inflection, then it would be possible to claim that this element provides the more specified form (observe in this regard that the vocabulary insertion rule of -\textit{er} is specified as [+F, -N, +O, αS]). Unlike in the dative masculine/neuter instances, here the “inflected” pronominal element itself would provide the more specified form. This would make these instances similar to \textit{wir} ‘we’ and \textit{ihr} ‘you(\textsc{nom.pl})’ as regards a higher degree of specificity. Indeed, this parallelism would be in line with the general observation that feminine and plural often behave alike.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Note that the preference of strong adjectives discussed above is consistent with the instances that do not involve variation. Recall that the pronominal determiners \textit{ich} ‘I’, \textit{mich} ‘me(\textsc{acc})’, \textit{du} ‘you(\textsc{nom.sgl})’, \textit{dich} ‘you(\textsc{acc.sgl})’, \textit{uns} ‘us(\textsc{acc/dat})’, \textit{euch} ‘you(\textsc{acc/dat.pl})’ only take strong adjectives. Importantly, none of these elements involve forms specified for gender in the singular or case in the plural. With that in place, I briefly comment on the different degrees of specificity of the vocabulary insertion rules for adjectival inflections and pronominal elements in the last part of this subsection. This includes a brief discussion of how these vocabulary insertion rules relate to the terminals nodes in the syntactic representation.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The different adjectival inflections and pronominal elements are not specified for all the features present in the underlying syntactic structure. Having adopted DM, vocabulary insertion rules involve underspecification, which regulates the order of the application of the rules. In other words, the absence of features in the vocabulary insertion rules has to do with the very assumptions of how Vocabulary Insertion works in DM. Indeed, the varying numbers of features in rules lead to different degrees of specificity of those rules. For instance, the strong inflection -\textit{er} has more features and is thus more specified than the strong inflection -\textit{em}, and both of these are more specified than the weak inflection -\textit{en}, the elsewhere case. Similar considerations hold for the contexts of vocabulary insertion of pronominal determiners. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Returning to the correlations in \REF{ex:key:50}, note that the more specified elements involve three features (e.g., -\textit{em}: [-F, +O, -S]) or four features (e.g., \textit{wir}: [+F, +N, +O, +S]). By contrast, the less specified elements contain no feature (e.g., -\textit{en}: []) or one feature (e.g., \textit{mir}: [-γ]). Comparing adjectival inflections to each other and pronominal forms to each other, they each involve a difference of three CNG features. This constitutes different degrees of specificity that find overt manifestation in the varying preferences for the different inflections on the adjectives. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Finally, note that the specification of the vocabulary insertion rules differs from that of the terminal nodes in the syntactic representation. Provided that the relevant terminal nodes are present, they involve all CNG features. In other words, all CNG features are present in the abstract representation, but only a subset of those features are specified in the vocabulary insertion rules to be applied. Given the discussion above, it appears then as if German prefers one more and one less specified element to co-occur, provided such a choice is available to begin with (i.e., Impoverishment does not occur). I summarize the entire section.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.4.  Summary of the Discussion
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

All pronominal DPs involve canonical DPs; that is, pronominal determiners take adjectives and nouns as part of their complement structure. If modifiers are present, all pronominal DPs may exhibit strong adjectives. This is explained by the reduced demonstrative structure and the feature specifications involving [±AUTH] and [±PART]. With CNG, definiteness, and deixis features absent on the pronominal determiners, Impoverishment cannot occur resulting in the strong adjectives. Besides strong adjectives, some instances may also involve weak adjectives. The weak adjectives in the dative masculine/neuter are part of the nasal alternation and can be accounted for by the phonetic rule from \sectref{sec:key:2.2}. The weak adjectives in the dative feminine and nominative plural follow from analogy with ordinary definite DPs (constrained by the similarity of the inflections on the ordinary determiners and the final sounds of the pronominal determiners). In other words, all the weak adjectives surface under specific phonetic conditions only. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The variation in the dative singular and in the nominative plural shows varying preferences as regards strong or weak inflections on adjectives. This preference is a reflex of the different degrees of specification of two co-occurring elements, the pronominal determiner and the inflection on the following adjective. The preference for strong adjectives in the dative masculine/neuter is due to the preference for more specified forms provided by the adjectival inflection. In contrast, the preference for weak adjectives in the dative feminine and in the nominative plural is also due to the preference for more specified forms, but here provided by the pronominal determiners themselves. As the above discussion has made clear again, masculine and neuter pattern in opposition to feminine and plural. In the next section, I turn to variation in a non-canonical structure. Like in Sections 3 and 4, it is lexically restricted. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{6.}   \textbf{Non-canonical} \textbf{Structures} \textbf{with} \textbf{Optional} \textbf{Inflections} \textbf{on} \textbf{Predeterminer} \textbf{\textit{alle}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I discuss the presence and absence of the strong endings on the predeterminer \textit{alle} ‘all’ in the plural. Recall from Chapter 1, \sectref{sec:key:4.1.2} that a predeterminer is a type of determiner-like element that intensifies the meaning of the following DP in a certain way. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As is well known, both uninflected \textit{all} and inflected \textit{alle} can appear before elements such as possessive determiners \REF{ex:key:51} or demonstratives \REF{ex:key:52}. Interestingly, there is a restriction such that the co-occurrence of inflected \textit{alle} and a definite article results in slight degradedness \REF{ex:key:53b}. Note that in this section, I gloss determiners like \textit{die} ‘the/those’ differently. Recall that they are pronounced as [di(:)]; that is, they are monosyllabic. This will become important below.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%51
    \label{ex:key:51}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{all mein-e Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all my-\textsc{st}  friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all my friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{all-e   mein-e Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all-\textsc{st} my-\textsc{st}  friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all my friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%52
    \label{ex:key:52}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{all dies-e    Leute}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all these-\textsc{st} people
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all these people’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{all-e   dies-e     Leute}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all-\textsc{st} these-\textsc{st} people
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all these people’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%53
    \label{ex:key:53}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{all die         Leute}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all the.\textsc{nom} people
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all the people’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  ?  \textit{all-e   die          Leute}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all-\textsc{st} the.\textsc{nom} people
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all the people’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Before proceeding, a remark on these data is necessary.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet[197-200]{Cirillo2016} states that inflected \textit{alle} in the (b)-examples above is marked or even ungrammatical for some speakers; that is, uninflected \textit{all} is the dominant or correct form. While I agree that \REF{ex:key:53b} is indeed marked, \REF{ex:key:51b} and \REF{ex:key:52b} are completely fine for me. An informal \textit{google}{}-search has revealed that uninflected \textit{all} is actually less frequent than inflected \textit{alle}. The results are provided in \tabref{tab:key:10}a (M stands for million).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\tabref{tab:key:10}a: Numeric Results of \textit{all(e)} Identified by \textit{Google} (ca. 2020)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXX}
 & {\itshape all} & {\itshape alle}\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape meine} & 1.4M & 8.8M\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape diese} & 16.5M & 21.2M\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

While I checked many instances for relevance, these numbers are too large to check every single example. Note though that while there are some false positives, it is clear that inflected \textit{alle} is fine for many speakers. A reviewer points out that the inflected forms may be more frequent in written German. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I also conducted a search in the DGD, specifically, the corpus of spoken German called \textit{Forschungs- und Lehrkorpus Gesprochenes Deutsch} (FOLK) (Research and Teaching Corpus of Spoken German), and found similar results. Consider \tabref{tab:key:10}b.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\tabref{tab:key:10}b: Numeric Results of \textit{all(e)} Identified in FOLK (October 31, 2024)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXX}
 & {\itshape all} & {\itshape alle}\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape meine} & 1 & 9\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\itshape diese} & 9 & 12\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These numbers are much smaller. Note that every example was checked for relevance, and a few examples were removed – either they were not relevant or their status was somewhat unclear. Observe though that the relative frequencies of the uninflected and inflected forms of \textit{all}{}- here are the same as in \tabref{tab:key:10}a above. In other words, inflected \textit{alle} seems to be more frequent than uninflected \textit{all}, in both written and spoken contexts. As such, I point out that \citegen{Cirillo2016} description of the data is not entirely accurate. Given the data above, there are two questions that arise here. First, why is the inflection on \textit{all}{}- optional in \REF{ex:key:51} and \REF{ex:key:52}? Second, why is the inflection in \REF{ex:key:53b} dispreferred?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Starting with the first question, I proposed in Chapter 2, \sectref{sec:key:4} that \textit{alle} ‘all’ in the cases above is located in LPP. Not being part of the DP proper at any point, the predeterminer \textit{alle} can neither trigger nor undergo Impoverishment explaining the strong ending on \textit{alle} itself and on the following determiner:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%54
    \label{ex:key:54}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

            LPP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{alle}    DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    D    NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          meine            Leute
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet[264-66]{Pafel1994} makes a similar syntactic proposal for inflected \textit{alle} such that this predeterminer selects a DP complement.\footnote{\citet[217-18]{Bhatt1990} claims that \textit{alle}, \textit{diese}, and \textit{meine} in \textit{alle diese meine Freunde} ‘all these friends of mine’ are heads. She proposes that \textit{alle} is adjoined to \textit{diese} and the resulting complex is adjoined to \textit{meine}. The entire complex is inside Spec,DP. Note that it is not a standard assumption that heads reside in phrasal positions.} Several options have been proposed to account for the absence of the inflection on \textit{all}{}-. There are two basic types. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  First, a number of authors have proposed that uninflected \textit{all} is in a different position. For instance, \citet{Pafel1994} proposes that \textit{all} is adjoined to the determiner in D. \citet[701]{Bošković2004} argues that while inflected \textit{alle} is either left-adjoined to DP or in a higher position, uninflected \textit{all} is in that higher position (left unspecified in Bošković, see his footnote 23). Given the discussion above, this higher position can be related to the LPP-layer. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The second type of proposal involves the claim that inflected and uninflected \textit{all}{}- start out in the same position but that uninflected \textit{all} undergoes some further operation. To name just two ideas, we could build on Pafel suggesting that uninflected \textit{all} is a deficient element that starts out in LPP but undergoes adjunction to D late in the derivation. Alternatively, we could formulate an Impoverishment rule that deletes the inflection optionally (cf. Footnote Error: Reference source not found). Since the account of the inflection on \textit{all}{}- has repurcussions for the analysis of floating quantifiers (where \textit{all}{}- must have an inflection), I will not pursue this question further here and turn to the second query.\footnote{Notice also that it is sometimes claimed that uninflected \textit{all} and inflected \textit{alle} involve different semantics (see \citealt{Merchant1996}: 183 and \citealt{KobeleZimmermann2012}: 249). For some brief discussion, see Chapter 8.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As far as I am aware, there is no explanation of the contrast in \REF{ex:key:53}, neither in Pafel’s nor in any other work. Following an idea from Björn Köhnlein (p.c.), I make a tentative suggestion at the end of this section. To begin, note that the contrast above is found in the other morphological cases as well, here illustrated with the dative:\footnote{Like \textit{all}{}-, the demonstrative \textit{dies}{}- ‘this/these’ can also do double duty as a determiner and as a predeterminer. Unlike \textit{all}{}-, the inflection of \textit{dies}{}- can only be left out in the nominative/accusative neuter yielding \textit{dieses} vs. \textit{dies} (see Chapter 4, \sectref{sec:key:5}).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%55
    \label{ex:key:55}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{mit   all den       Leuten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with all the.\textsc{dat} people
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with all the people’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  ?  \textit{mit   all-en den        Leuten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with all-\textsc{st} the.\textsc{dat} people
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with all the people’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If the (lower) DP is replaced by a disyllabic pronoun, the counterparts of \REF{ex:key:55a} and \REF{ex:key:55b} are both fine:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%56
    \label{ex:key:56}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{mit   all denen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with all those
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with all those’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mit   all-en denen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with all-\textsc{st} those
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with all those’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The obvious difference between these plural elements is that possessive determiners and demonstratives are disyllabic but that definite articles are monosyllabic. Assuming that LPP constitutes the left periphery of the noun phrase, I formulate a preliminary version of the relevant generalization:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%57
    \label{ex:key:57}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Generalization (preliminary version):}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

A disyllabic determiner-like element in the left periphery cannot be followed by a monosyllabic determiner.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that the generalization is silent about sequences starting with a monosyllabic element; that is, it says nothing about the grammatical (a)-examples above. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

At first glance, it might be suggested that German prefers trochaic feet, which yield a certain phonotactic rhythm in the pronunciation. This suggestion seems to get confirmation when three determiner elements are present \REF{ex:key:58a}. However, we have already seen above that two monosyllabic elements do co-occur (\textit{all die} ‘all the’). Furthermore, as pointed out to me by David Fertig (p.c.), the string in \REF{ex:key:53b} above becomes better with stress on \textit{die} (58b); that is, when \textit{die} functions as a demonstrative. Note that cases as in \REF{ex:key:58b} are often followed by a relative clause (also \citealt{Pafel1994}: 238):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%58
    \label{ex:key:58}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{all-e   dies-e     mein-e Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all-\textsc{st} these-\textsc{st} my-\textsc{st}  friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all these friends of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{all-e   DIE           Leute (, die   in einer Stadt wohnen)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all-\textsc{st} those.\textsc{nom} people  who in a        city   live
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘all those people (who live in a city)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

While \textit{die} is stressed now, it is still a monosyllabic element. I interpret multiple syllabicity and stress as related measures of heaviness. In other words, I assume that multiple syllables in an element, on the one hand, or stress on an element, on the other hand, leads to heaviness of a word. This groups disyllabic and stressed monosyllabic words together in opposition to unstressed monosyllabic words. With this in mind, I finalize the generalization as follows:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%59
    \label{ex:key:59}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Generalization (final version):}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

A multisyllabic determiner-like element in the left periphery cannot be followed by a lighter determiner.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Put differently, the relevant elements must be of the same weight or the relevant elements must increase (but not decrease) in weight from left to right. The question arises as to how to deal with this generalization.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Björn Köhnlein (p.c.) makes the interesting suggestion that the generalization above actually involves an epiphenomenon of the fact that articles are clitics. In more detail, phonology tends to avoid lapses, that is, sequences of two adjacent unstressed syllables in the same phonological word. Assuming that definite articles are clitics, the phonological word [\textit{all-die}] consists of a sequence of a stressed and one unstressed syllable. This is fine in the phonology. In contrast, the phonological word [\textit{alle-die}] consists of a sequence of a stressed and two adjacent unstressed syllables, something that is marked in the phonology (yielding the slight degree of grammatical markedness). Unlike articles, demonstratives and possessive determiners are not clitics, and they do not form a phonological word with \textit{all(e)}. Consequently, those sequences are fine, with or without the inflection on \textit{all}{}-. Similarly, when \textit{die} is stressed, it is no longer a clitic and forms a different phonological word.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In the last couple of sections, I discussed variation of adjectival inflections in noun phrases in Standard German. With this in mind, I return to the traditional generalizations Weak After Strong and the Principle of Monoinflection.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

7.  Revisiting Two Traditional Generalizations of Adjectival Inflections
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I return to the two traditional generalizations that describe the strong/weak alternation in German. To recapitulate, compare the canonical cases in \REF{ex:key:60}, where the adjective is weak provided it is preceded by a determiner with a strong inflection \REF{ex:key:60a}, but the adjective is strong in all other cases (60b-c): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%60
    \label{ex:key:60}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{d-er    gut-e        Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{st} good-\textsc{wk} coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the good coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{ein gut-er    Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a    good-\textsc{st} coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a good coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{gut-er    Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    good-\textsc{st} coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘good coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This inflectional distribution can be summarized in the generalization in \REF{ex:key:61}, repeated from Chapter 2, \sectref{sec:key:2.1.1} (again, for similar statements, see \citealt{Bierwisch1967}: 257, \citealt{Eisenberg1998}: 171-73, \citealt{Gallmann1998}: 144, \citealt{Giusti2015}: 207, G. \citealt{Müller2002a}: 129, \citealt{Petrova2024}: 184-85, \citealt{Pfaff2017}: 286, \citealt{Rehn2019}: 58, \citealt{Sauerland1996}: 34, \citealt{Schoorlemmer2009}: 53, and many others):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%61
    \label{ex:key:61}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Weak After Strong}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

An adjective with a weak inflection is preceded by a determiner with a strong inflection. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note again that this formulation does not explicitly mention adjectives that are not preceded by (inflected) determiners. However, since prenominal adjectives alternate only between weak and strong, the distribution of strong adjectives can be inferred from \REF{ex:key:61}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  It is clear that reference to lexical categories like adjective vs. determiner or determiner-like element is essential to the proper workings of this generalization. As documented above, inflections on elements of the same lexical category are the same. Specifically, independently of the presence or absence of \textit{ein}, an adjective that follows a strong adjective must also be strong \REF{ex:key:62a}. As for determiners and determiner-like elements, they typically exhibit strong inflections. In fact, as long as the determiner(-like) elements appear in the order in \REF{ex:key:62b}, any combination of them yields a grammatical string:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%62
    \label{ex:key:62}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{(ein) gut-er    frisch-er Kaffee}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     a     good-\textsc{st} fresh-\textsc{st}  coffee.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘(a) good fresh coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{(all-e) (dies-e)  (mein-e) Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     all-\textsc{st}  these-\textsc{st} my-\textsc{st}   friends.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all these friends of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To be clear, the generalization Weak After Strong must take the different lexical categories of the elements involved into consideration. Recall that determiners and determiner-like elements have the categorial feature [+D], but adjectives do not. This lexical distinction prevents us from expecting the second adjective in \REF{ex:key:62a} and the non-initial determiner(-like) elements in \REF{ex:key:62b} to exhibit weak inflections.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Note that this generalization is surface-oriented making reference to the notion of precedence. While I already pointed out in Chapter 2, \sectref{sec:key:2.1.1} that this generalization is, in and of itself, not an explanation, there are also two basic types of exceptions: (i) strong adjectives can be preceded by determiners with strong inflections; (ii) weak adjectives can occur without determiners with strong inflections preceding them.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The first type of exception manifests itself when complex proper names are embedded in larger nominals. In these cases, strong adjectives that are part of proper names are preceded by the determiners of the larger nominals, where the determiners also have strong inflections:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%63
    \label{ex:key:63}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{d-em} [ \textit{Deutsch-e  Bahn} ]           \textit{Personal}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{st} German-\textsc{st} Railroad.\textsc{fem} personnel.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘(to) the German-Railroad personnel’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{d-es}  [ \textit{Deutsch-e   Bank} ]      \textit{Chef-s}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{st} German-\textsc{st} Bank.\textsc{fem} boss.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘(of) the German-Bank boss’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is clear that any generalization (or proposal) of the strong/weak alternation must take structure into account. I argued above that the determiners are in nominals different from those of the complex proper names in \REF{ex:key:63}. As the determiners do not adjoin to any parts of the AgrPs containing the adjectives, Impoverishment does not occur. This explains the strong adjectives in \REF{ex:key:63}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As to the second type of exception, weak adjectives routinely occur in the genitive masculine/neuter without any determiners being present \REF{ex:key:64a}. Furthermore, they also occur in the presence of Saxon Genitives, items often taken to be determiner(-like) elements \REF{ex:key:64b}. Observe that – what looks like – strong inflections (i.e., -\textit{s}) are present in both \REF{ex:key:64a} and \REF{ex:key:64b}. Crucially though, these inflections are on elements that follow (but not precede) the relevant adjectives – they are on the head nouns. Third, weak adjectives can be preceded by pronominal determiners, which do not have inflections in the first and second person \REF{ex:key:64c}. Finally, weak adjectives can also be preceded by uninflected \textit{dies} ‘this’ \REF{ex:key:64d}:\footnote{I argue in detail in Chapter 4, \sectref{sec:key:5} that uninflected \textit{dies} is not based on inflected \textit{dieses} such that the ending -\textit{es} has been deleted. This is different for \textit{die} ‘the/that/those’. Recall from \sectref{sec:key:3} that schwa has been deleted here. Note that, if the generalization above only applies to surface strings, then nominals involving \textit{die} present another exception to it.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%64
    \label{ex:key:64}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{trotz     heiß-en Kaffee-s}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    despite hot-\textsc{wk}  coffee.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘despite hot coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{trotz     Peters  heiß-en Kaffee-s}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    despite Peter’s hot-\textsc{wk}  coffee.\textsc{masc}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘despite Peter’s hot coffee’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{wir nett-en  Studenten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    we nice-\textsc{wk} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘we nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{dies schön-e    Kleid}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this pretty-\textsc{wk} dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this pretty dress’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The cases in \REF{ex:key:64} involve canonical structures. Above, I argued that Impoverishment Rule 2 is at work in (64a-b). Turning to \REF{ex:key:64c}, the weak adjective is due to analogy with related definite DPs. As for \REF{ex:key:64d}, I argued that it is the feature [+D] on the determiner stem that triggers Impoverishment on the adjective, not the inflection on the determiner – as is clear from \REF{ex:key:64d}, such an inflection can be absent and yet, the adjective is weak. There are other issues with this generalization.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Strictly speaking, Weak After Strong is a generalization about inflections on adjectives. More precisely, it describes the dependency of the inflections on the adjectives with regard to the inflections on determiners. The inflectional behavior of determiners or determiner-like elements themselves is not covered. Since inflections on determiners, determiner-like elements, and adjectives are identical, it is desirable to put forth a generalization that captures the distribution of the inflections on all these elements.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  A more general statement than the first generalization has come to be known as the Principle of Monoinflection, also repeated here from Chapter 2, \sectref{sec:key:2.1.1} (see also, e.g., \citealt{Darski1979}, \citealt{Esau1973}, \citealt{HelbigBuscha2001}: 274-76, \citealt{Murphy2018}: 344, \citealt{Roehrs2009a}: 135, \citealt{Wegener1995}: 105, 153, cf. \citealt{Nübling2011}: 178):\footnote{Note that a similar principle is also mentioned in the Duden reference grammar (\textit{Monoflexion}), but with a slightly different formulation, where the strong ending does not necessarily precede the weak one (e.g., \citealt{Duden2016}: 954).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%65
    \label{ex:key:65}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Principle of Monoinflection}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The first inflected element within a noun phrase carries the strong and the second one the   weak ending.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As with the first generalization, reference to lexical categories must be made. In other words, the first element of a nominal string is taken to be of a different lexical category than the second element. Given that determiner(-like) elements and adjectives can each be stacked, the notion of element (of a certain lexical category) must be understood as group of elements (of the same category). To briefly illustrate, given that \REF{ex:key:66} involves determiner(-like) elements, they are all considered to be one type of element as regards the relevant lexical category. As such, they are all taken to be first in the relevant sense, and the generalization correctly describes the fact that all of them have a strong ending:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%66
    \label{ex:key:66}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{(all-e) (dies-e)  (mein-e) Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   all-\textsc{st}  these-\textsc{st} my-\textsc{st}   friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘all these friends of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

By contrast, if determiner(-like) elements are absent, then adjectives are the first inflected element. As expected under \REF{ex:key:65}, they appear with strong inflections (cf. \REF{ex:key:60c} above).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

While this generalization covers more empirical ground, it faces the same general challenges as Weak After Strong. Besides the issues already mentioned above, a determiner-like element with a strong ending may precede an adjective with a strong ending \REF{ex:key:67a}.\footnote{Note that this case cannot simply be discounted by stating that the adjective is not immediately preceded by \textit{dieses} ‘this’. This is so because weak adjectives can be preceded by determiners with a strong inflection even if an uninflected element intervenes:  (i)  \textit{dies-e     zehn klein-en    Autos}    these-\textsc{st} ten    small-\textsc{wk} cars    ‘these ten small cars’Again, a surface-oriented generalization involving precedence cannot capture cases like \REF{ex:key:67a} and others to be discussed momentarily.} Similarly, taking indefinite pronouns to be determiner-like elements (\sectref{sec:key:2.2}), indefinite pronoun constructions present a similar problem \REF{ex:key:67b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%67
    \label{ex:key:67}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.    \textit{dies-es mein groß-es   Glück}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this-\textsc{st} my    great-\textsc{st}  happiness.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this great happiness of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \%  \textit{jemand-em}             \textit{ander}{}-\textit{em}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    someone.\textsc{masc}{}-\textsc{st} different-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘someone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In Chapter 2, I argued that the demonstrative in \REF{ex:key:67a} is base-generated in LPP. As such, it can neither trigger nor undergo Impoverishment. As to \REF{ex:key:67b}, I formulated in \sectref{sec:key:2.2} three different paradigms for \textit{jemand} ‘someone’ and an optional phonetic rule, which has not applied to the inflection on the adjective in \REF{ex:key:67b}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The second type of exception concerns a weak ending on the first inflected element. This may hold with determiners \REF{ex:key:68a}, indefinite pronouns \REF{ex:key:68b}, and adjectives \REF{ex:key:68c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%68
    \label{ex:key:68}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{im      Sommer dies-en Jahr-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    in.the summer this-\textsc{wk} year.\textsc{neut}{}-\textsc{gen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘in the summer of this year’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \%  \textit{jemand-en}               \textit{ander}{}-\textit{en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    someone.\textsc{masc}{}-\textsc{wk} different-\textsc{wk}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘someone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  \%  \textit{jemand}              \textit{ander}{}-\textit{en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    someone.\textsc{masc} different-\textsc{wk}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘someone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

While the occurrence of these strings is admittedly restricted, these distributions do exist. In \sectref{sec:key:4}, I argued that determiners can optionally undergo Impoverishment in genitive masculine/neuter contexts \REF{ex:key:68a}. In \sectref{sec:key:2.2}, I provided three paradigms for \textit{jemand} ‘someone’ (68b-c) and an optional phonetic rule, which has applied to the inflections on the adjectives in (68b-c). Finally, notice that both generalizations are silent about prenominal adjectives that can appear without inflections (e.g., \textit{das lila/prima/Berliner Auto} ‘the purple/great/Berlin car’). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given these issues, I propose the following generalization. Note that the generalization below is not based on precedence but on structure, where canonical DPs play a crucial role:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%69
    \label{ex:key:69}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Strong/Weak Alternation in German}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Disregarding a few (mostly) lexical exceptions, both adjectives and determiners have   strong or weak inflections. Weak inflections only occur in canonical DPs, either 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(a)  in the context of certain lexical items, or
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(b)  in certain featural contexts, or 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(c)  in a combination of (a) and (b).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Strong inflections occur in all other environments.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To briefly illustrate, \REF{ex:key:69a} refers to adjectives following \textit{der}{}-words, \REF{ex:key:69b} involves adjectives and determiners in genitive masculine/neuter contexts, and \REF{ex:key:69c} is instantiated by adjectives following \textit{ein}{}-words that occur in the feminine, plural, and in the two oblique cases. Furthermore, \REF{ex:key:69c} is also manifested by adjectives in dative masculine/neuter contexts that are preceded by nominal elements, and by adjectives in the dative singular and in the nominative plural preceded by pronominal determiners. These are the main contexts where weak inflections occur.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up, the generalization Weak After Strong is a good initial description of the canonical facts, but it does not capture the whole spectrum. The Principle of Monoinflection is more general but faces the same general issues. Many (but not all) of the issues with these generalizations reveal themselves when we consider non-canonical constructions. An analysis that seeks to be more comprehensive has to include the latter cases as well. If so, the different structures involved must be part of the account of the strong/weak alternation. As mentioned before, I argue that the non-canonical constructions reveal the true nature of adjectival inflections.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Thus far, I have discussed variation (and generalization) in the same dialect – colloquial Standard German. Next, I turn to variation of a different kind by discussing a regional variety. I chose Mannheim German for two reasons. First, the account developed above can, without adding too many details, be extended to this dialect showcasing its more general applicability. Second and more importantly, the discussion of this regional dialect will reiterate the special status of \textit{ein}{}-words in the four exceptional cases, delineated in Chapter 2, \sectref{sec:key:2.2.2} as masculine/neuter in the nominative and accusative.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

8.  Dialectal Variation: Mannheim German
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In his critique of \citet{Bierwisch1967}, \citet{Blevins1995}, \citet{Wunderlich1997}, and B. \citet{Wiese1999}, G. \citet{Müller2002b} discusses a regional variety of German spoken in and around Mannheim, \textit{Mannheimer Regionalsprache} ‘Mannheim regional dialect’, a Standard German dialect that is influenced by the local palatinate variety. For convenience, I refer to this dialect as Mannheim German. G. Müller observes that regular nominative masculine forms are used in accusative masculine contexts (data from G. \citealt{Müller2002b}: 354):\footnote{G. \citet[353-55]{Müller2002b} provides more details about this locally colored Standard German variety. There are three points of interest here: the accusative masculine personal pronoun is \textit{en} ‘him’ (not \textit{er}), endings including weak adjectival endings seem to be systematically reduced from -\textit{en} to -\textit{e} – a general feature of the dialect, and the weak ending -\textit{e} in the nominative masculine is often left out. This means that personal pronouns have their own insertion rules (at least in this dialect), that phonological rules may obscure the underlying inflectional paradigms, and that some endings may be optional (possibly analyzed as instances of Impoverishment). I abstract away from these finer points here. Also, as \citet[210-14]{Russ1989} points out, this Mannheim German dialect should not be equated with regional varieties that are spoken in Mannheim or near this geographical area (see, for instance, \citealt{Russ1989}: 231, 251 on the German spoken in Michelstadt in southern Hesse and in Kaulbach in western Palaninate). I thank a reviewer for making me aware of this issue.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%70
    \label{ex:key:70}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Ich kenn} [ \textit{d-er   ander-e    Mann} ].    (Mannheim German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I    know  the-\textsc{st} other-\textsc{wk} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I know the other man.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Der hat} [ \textit{ein groß-er Hubser} ]    \textit{gemacht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he   has   a    big-\textsc{st}   jump.\textsc{masc} done
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘He did a big jump.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Ich mag} [ \textit{gut-er    Wein} ].
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I    like     good-\textsc{st} wine.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I like good wine.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{Ich seh} [ \textit{ein-er} ].
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     see   one-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I see one.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Müller concludes that all analyses including his own can straightforwardly account for this dialect by deleting a rule or changing the featural specification of a rule. We see below that the current proposal can also explain this variety. Unlike G. Müller’s discussion, here I also address the weak inflections and \textit{ein}{}-words.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I start by updating the two inflectional paradigms. Considering \REF{ex:key:70a}, note that the strong and weak ending -\textit{en} in the accusative masculine is replaced by the strong ending -\textit{er} and the weak ending -\textit{e}, respectively ((70b-d) provide additional evidence that the nominative masculine form occurs in accusative contexts). The inflectional paradigm of Mannheim German is summarized in \tabref{tab:key:11}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{ Strong and Weak Endings in Mannheim German}
\label{tab:key:11}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXXXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{\bfseries STRONG} & [-F, -N] & [-F, +N] & [+F, -N] & [+F, +N] & {\bfseries WEAK} & [-γ] & [+F, +N]\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

[ -O,  {}-S] & {}-\textit{er} & {}-\textit{es} & (-\textit{e}) & {\itshape {}-e} & [-O] & (\textit{{}-e}) & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

[ -O, +S] & {}-\textit{er} & {}-\textit{es} & (-\textit{e}) & {\itshape {}-e} &  &  & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%\hhline%%replace by cmidrule{-------~}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

[+O,  {}-S] & {}-\textit{em} & {}-\textit{em} & {}-\textit{er} & \{-\textit{en}\} & [+O] & \multicolumn{2}{c}{ \{-\textit{en}\}}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

[+O, +S] & {}-\textit{es} & {}-\textit{es} & {}-\textit{er} & {\itshape {}-er} &  &  & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%\hhline%%replace by cmidrule{-------~}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The rules for vocabulary insertion are provided below. There are three differences to Standard German as regards adjectival inflections: (i) the rule inserting -\textit{en} in the accusative masculine has been deleted, (ii) both rules inserting -\textit{er} have been changed in their feature specifications, and (iii) due to the latter change, these two rules have been reordered using the same specificity metrics as above (in \tabref{tab:key:12} below, I juxtapose the rule systems of Standard and Mannheim German):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%71
    \label{ex:key:71}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          Strong (except for feminine \textit{{}-e} and plural \textit{{}-en}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  a.  [+F, +N, +O, +S]  →  \textit{{}-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [+F, +N, -O, αS]  →  \textit{{}-e}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  [αF, -N, αO, βS]  →  \textit{{}-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

d.  [-F, +O, -S]    →  \textit{{}-em}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  e.  [-F, αO, βS]    →  \textit{{}-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%72
    \label{ex:key:72}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

             Weak (including strong feminine \textit{{}-e} and plural \textit{{}-en}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  a.  [-γ, -O]      →  \textit{{}-e}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  []      →  \textit{{}-en}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As regards \textit{ein}{}-words, the rule that inserts accusative masculine \textit{einen} ‘a’ has been deleted yielding the following insertion rules. Recall that if \REF{ex:key:73a} does not apply, \REF{ex:key:73b} will in conjunction with the inflectional rules in \REF{ex:key:73c} or \REF{ex:key:73d}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%73
    \label{ex:key:73}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [+D][-F, -O]    →   \textit{ein} / \_\_ word ]\textsubscript{φ}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [+D]      →   \textit{ein}{}-
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  [+F, +N, +O, +S]  →  \textit{{}-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  etc.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Besides the three differences in adjectival inflections, this adds a fourth difference to Standard German.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  More generally, deleting the vocabulary insertion rules for accusative masculine -\textit{en} and for accusative masculine \textit{einen} ‘a’ highlights the fact that the feature combination of “nominative/accusative + masculine/neuter” has a special status. This is directly stated by the features [-F, -O] in \REF{ex:key:73a}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The vocabulary insertion rules for the two varieties are provided in \tabref{tab:key:12} juxtaposing the strong endings in \REF{ex:key:1}, the weak endings in \REF{ex:key:2}, and \textit{ein} in \REF{ex:key:3} in Standard German and their counterparts in (1’), (2’), and (3’) in Mannheim German: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Comparison between Standard and Mannheim German}
\label{tab:key:12}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 Standard German & Mannheim German\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1a.  [+F, -N, +O, αS]       →  \textit{{}-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       [+F, +N, -O, αS]       →  \textit{{}-e}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [αF, αN, αO, αS]       →  \textit{{}-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  [-F, -N, -O]       →  \textit{{}-en} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       [-F, +O, -S]       →  \textit{{}-em}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  [-F, αO, βS]       →  \textit{{}-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2a.  [-γ, -O]         →  \textit{{}-e}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  []           →  \textit{{}-en} & 1a’.  [+F, +N, +O, +S] →  \textit{{}-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b’.  [+F, +N, -O, αS]   →  \textit{{}-e}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c’.  [αF, -N, αO, βS]   →  \textit{{}-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d’.  [-F, +O, -S]   →  \textit{{}-em}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  e’.  [-F, αO, βS]   →  \textit{{}-es}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2a’.  [-γ, -O]     →  \textit{{}-e}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b’.  []       →  \textit{{}-en}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ 3a.  [+D]                   →   \textit{ein-} / [-F, -N, -O, +S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{   b.  [+D][-F, -O]       →   \textit{ein} / \_\_ word ]\textsubscript{φ}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{   c.  [+D]         →   \textit{ein}{}-} & { 3a’.  [+D][-F, -O]   →   \textit{ein} / \_\_ word ]\textsubscript{φ}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{   b’.  [+D]     →   \textit{ein}{}-}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note again that the rule inserting -\textit{en} in \REF{ex:key:1c} in \tabref{tab:key:12} has no counterpart in (1’). Given that, all vocabulary insertion rules for adjectival inflections in Mannheim German in (1a’-e’) have the feature [S]. Impoverishment applies as in Chapter 2 but now to all (unambiguously) strong inflections.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up, like the five other analyses, the current system can also account for the regional variety of Mannheim German, including the variation seen with weak inflections and \textit{ein}{}-words. Notice that I focused here on one regional dialect to show that the current system can capture dialectal variation as well. For the description of other dialects, see \citet{Schirmunski2010}; for some tentative discussion of Alemannic German, see Chapter 8, \sectref{sec:key:2.1}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

9.   Conclusion
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This chapter continued the discussion of adjectival inflections begun in Chapter 2. Here I focused on variation in (mostly) canonical DPs where unexpected strong or unexpected weak adjectives occur. The unexpected strong adjectives are due to pronominal determiners not triggering Impoverishment. The unexpected weak adjectives follow from a phonetic rule, presumably a reflex of markedness reduction. The unexpected weak inflections on determiners are accounted for by a more general application of Impoverishment Rule 2. We arrive then at three different mechanisms that explain the weak inflections: one primary mechanism (Impoverishment Rule 1) and two secondary mechanisms (phonetic rule, Impoverishment Rule 2). Unlike the primary mechanism, the secondary ones apply in restricted contexts, that is, in certain constructions and/or specific featural contexts. Note that the secondary mechanisms are brought to light when the primary one does not occur. The main patterns of Chapters 2 and 3 along with their analyses are summarized in \tabref{tab:key:13} (the inflection under consideration is set apart by a hyphen):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Summary Chart}
\label{tab:key:13}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 Construction: Example & Inflection: Analysis\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Regular DP: \textit{der heiß-e schwarz-e Kaffee} & Weak: \textit{Impoverishment Rule 1} \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Regular DP: (i) \textit{heiß-en Kaffees}; (ii) \textit{dies-en Jahres} & Weak: \textit{Impoverishment Rule 2}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Regular DP: \textsuperscript{\%}\textit{mit frischem schwarz-en Kaffee} & Weak: \textit{Phonetic Rule} \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Regular DP: \textit{ein heiß-er schwarz-er Kaffee} & Strong: ein\textit{{}-words}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Close Appositions: \textit{der Indianer Groß-er Bär} & Strong: \textit{Low Right-Adjunction} \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Indefinite Pronoun Constructions: \textit{wer ander-er} & Strong: \textit{Mid Right-Adjunction} \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Noun-Adjective Exclamatives: \textit{Schwein, schwarz-es!} & Strong: \textit{Mid Right-Adjunction}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Loose Appositions: \textit{wir, begeistert-e Linguisten} & Strong: \textit{High Right-Adjunction} \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Agreement in Pronominal DPs: \textit{ich dumm-er Idiot} & Strong: \textit{Pronominal Determiner}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Dis-agreement in Pronominal DPs: \textit{ihr jung-es Gemüse} & Strong: \textit{Complex Specifier inside DP} \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Split Topicalizations: \textit{bunt-e Hemden ... diese da} & Strong: \textit{Separate Base-generation} \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Nominal Compounds: \textit{dem Deutsch-e Bank Chef} & Strong: \textit{Complex Compound Modifier}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Vocatives: \textit{Dumm-er Idiot!} & Strong: \textit{Voc Head and Absence of Det}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Predeterminers: \textit{dieses mein groß-es Glück} & Strong: \textit{Determiner Outside DP Proper} \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Besides these cases, I also discussed inflectional variation on certain adjectives (e.g., \textit{lila} ‘purple’, \textit{prima} ‘great’, \textit{Berliner} ‘(from) Berlin’), and I addressed the uninflected forms of the definite article and its related distal demonstrative. Furthermore, I discussed the optional inflection on the predeterminer \textit{all-} ‘all’. On the basis of these cases, I demonstrated that the generalization Weak After Strong and the more general Principle of Monoinflection face a number of empirical challenges. Finally, I showed that the current system can be extended to Mannheim German, one of the dialects in Germany. The overall conclusion is that a purely surface-oriented account does not work – the consideration of structure appears to be an essential part of any proposal. The next chapter discusses consequences of the current proposal for other accounts. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\setcounter{page}{179}\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Chapter 4: Consequences for Other Analyses
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.  Introduction
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this chapter, I turn to some other consequences of the analysis developed in Chapter 2. Unlike the previous chapter, I now consider implications for other proposals. In so doing, I provide more evidence for the hypotheses proposed in Chapter 1. I consider three types of accounts where adjectival inflections are revealing as regards the proposed structure (Hypothesis 2a). Starting with analyses that seek to account for the presence of spurious indefinite articles, I show that weak adjectives present problems for structures involving Predicate Inversion and for analyses postulating null nouns. Second, discussing discontinuous noun phrases, I argue that strong endings on topicalized adjectives are only compatible with split topicalizations being analyzed as the base-generation of two separate nominals but not as movement involving one nominal. Third, returning to weak inflections, I show that adjectives involving a restrictive or a non-restrictive interpretation have the same basic structure. The discussion of these consequences documents some other properties of adjectival inflections. For instance, I demonstrate that strong endings are not “referential” but simply serve to make nominal features such as case, number, and gender visible (Hypothesis 2b). I begin by discussing the analysis involving Predicate Inversion and take up the discussion of the other points in the order mentioned above.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.  Weak Adjectives in the Context of Spurious Indefinite Articles
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I discuss two types of analysis that seek to account for the occurrence of spurious indefinite articles in Dutch. These proposals involve Predicate Inversion, on the one hand, and null nouns, on the other hand. Extending the discussion to German, it is shown that weak adjectives follow the spurious articles. Given that both proposals involve structures different from canonical DPs, the weak adjectives indicate that these types of analysis are not compatible with the system laid out in Chapter 2.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.  Weak Adjectives in Structures Involving Predicate Inversion
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Taking Dutch as their empirical basis, Bennis \textit{et al}. (1998) discuss – what they call – \textit{wat}{}-exclamative constructions \REF{ex:key:1a}. Like some other constructions they investigate \REF{ex:key:1b}, a singular indefinite article occurs with a plural noun. They call this element spurious article. This article is syntactically obligatory in the first construction but not in the second, where its presence or absence has semantic consequences (data are taken from Bennis \textit{et al}. 1998: 98, 101, see also den \citealt{Dikken2006}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%1
    \label{ex:key:1}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{Wat *(een) jongens!} (Dutch)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what   a      boys
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘What boys!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{idioten van (een) mannen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    idiots   of     a      men
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘idiots of men’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I focus on the exclamative construction in \REF{ex:key:1a} as that has a counterpart in German.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Adopting the general framework of den \citet{Dikken1995}, Bennis \textit{et al}. (1998) propose a small clause structure with some further functional positions on top. Considering \REF{ex:key:2} below, the small clause is represented by XP and the functional structure by DP. The nominal \textit{jongens} ‘boys’ is assumed to be the subject, \textit{wat} ‘what’ is the predicate, and \textit{een} ‘a’ is the head of the small clause. Bennis \textit{et al}. propose that D is an [+EXCL] operator, and this element needs to be lexicalized. As a consequence, \textit{een} raises from X to D. The predicate \textit{wat} undergoes Predicate Fronting to Spec,DP, a type of A’-movement. The authors interpret this as something similar to the Verb Second Constraint in the clause (their tree diagram on page 106 is slightly adapted here):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%2
    \label{ex:key:2}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

             DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   \textit{Wat}\textsubscript{k}       D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        \textit{een}\textsubscript{i}+[+\textsc{excl}]   \textsubscript{} XP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             \textit{jongens}      X’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           t\textsubscript{i}       t\textsubscript{k} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The counterpart of the \textit{wat}{}-exclamative in German involves similar (but not identical) properties. First, unlike in Dutch, the prepositional element \textit{für} ‘for’ must be present \REF{ex:key:3a}. Second, the spurious article is not obligatory in this construction. In fact, as pointed out in Chapter 1, \sectref{sec:key:2.2}, if it occurs, it typically appears in colloquial northern dialects (\% indicates dialectal variation). Note again that this \textit{ein} has a typical plural ending as seen in the attested example in \REF{ex:key:3b}, taken from the Appendix:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%3
    \label{ex:key:3}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{Was *(für) Idioten!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what   for   idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘What idiots!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. \%  \textit{"}\emph{Was   für eine Idioten}\textit{" sagte Liam}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  what for \textsubscript{} a-\textsc{pl} idiots     said   Liam
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘“What idiots!”, said Liam’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

After these preliminary observations, I return to the main line of investigation. To make these cases relevant, it is important to determine how adjectives, especially their inflections, fare when they are added to the subject of the small clause.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Inserting an adjective yields an interesting asymmetry. With no determiner present, the adjective has a strong ending \REF{ex:key:4a}. This is expected and follows from the system developed in Chapter 2. When the spurious article is present, the adjective is weak. In fact, all the examples I have found involve weak adjectives only. Consequently, I mark the strong inflection in the attested example in \REF{ex:key:4b} as ungrammatical:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%4
    \label{ex:key:4}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{Was  für geil-e(*n)             Bilder!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what for awesome-\textsc{st}/*\textsc{wk} pictures
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘What awesome pictures!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \%  \textit{Was  für ein-e geil-e*(n)             Bilder    wer  hat die    bloß gemacht}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what for a-\textsc{pl}  awesome-\textsc{wk}/*\textsc{st} pictures who has those \textsc{prt}  made
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘What awesome pictures! Who in the world took those?’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With Bennis \textit{et al}.’s (1998) discussion in mind, the structure of \REF{ex:key:4b} would presumably look as follows:\footnote{In their discussion of \textit{N of a N}{}-constructions, Bennis \textit{et al}. (1998) allow a preposition and a spurious article to occupy the same head position. In other words, the structure in the main text should, in principle, be fine.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%5
    \label{ex:key:5}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         \textit{Predicate Inversion}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   \textit{Was}\textsubscript{k}       D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         \textit{für}+\textit{eine}\textsubscript{i}     \textsubscript{} XP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       [\textit{geilen Bilder}]    X’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           t\textsubscript{i}       t\textsubscript{k} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The question that arises now is how to account for the weak ending in \REF{ex:key:4b}, given this structure.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Note that the Predicate Inversion structure above is configurationally identical to pronominal DPs involving dis-agreement. This type of case was discussed in Chapter 2, \sectref{sec:key:3.5} and is repeated below as \REF{ex:key:6}. Notice first that both the spurious article in \REF{ex:key:5} and the pronoun in \REF{ex:key:6b} move from a lower position to the DP-level. Second and more importantly, in both cases, the adjective and noun form a complex specifier. In other words, unlike canonical DPs, the two cases in \REF{ex:key:5} and \REF{ex:key:6b} have an additional layer of embedding. However, unlike \REF{ex:key:4b}, the nominal in \REF{ex:key:6a} involves a strong adjective:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%6
    \label{ex:key:6}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \textit{ihr          dumm-e*(s)    Pack}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) stupid-\textsc{st}/*\textsc{wk} gang.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you stupid gang’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 b.    DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   \textit{ihr}\textsubscript{i}    DisP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           [\textsubscript{AgrP} \textit{dummes Pack}]      Dis’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           Dis    ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                t\textsubscript{t}        XP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                        \textit{e\textsubscript{N}} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Before addressing the different inflections on the adjectives, it is instructive to consider a contrast between German and English.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As mentioned above, German \textit{ein} exhibits a typical plural ending in these cases. It appears then as if the spurious article morphologically agrees with the plural noun. Now, Bennis \textit{et al}. (1998: 94, 97) explicitly claim that the indefinite article does not form a constituent with the following noun. In their base positions, though, the noun and the indefinite article are in a Spec-head relation. This, for instance, accounts for the absence of spurious articles in English, as witnessed in the ungrammatical string *\textit{What} \textit{a men!}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In order to explain the contrast in the plural between English and German, we could assume that English \textit{a} is specified for singular morphological number but that German \textit{ein} is unspecified as regards morphological number: [αPL morph] (see Chapter 5). Given the assumed Spec-head relation, English \textit{a} is only compatible with a singular noun but German \textit{ein} is with both a singular and plural noun. So far, so good. Note, however, that this Spec-head relation in and of itself does not explain the weak ending on the adjective.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall from Chapter 2 that concord is not a sufficient condition for weak adjectives. What is required is an appropriate structure. As is clear from the above discussion, the Predicate Inversion structure is not a regular DP – the adjective is more deeply embedded. This leaves the occurrence of the weak adjective unexplained. However, it is possible that the structure in \REF{ex:key:5} is not correct. There are several options. For instance, one option might be to assume that a null article is present as part of the nominal in Spec,XP. As documented in Chapter 2, \sectref{sec:key:2.3} though, null articles occur with strong adjectives (except in the genitive masculine/neuter). In other words, a null article cannot account for the weak adjective either. To keep Bennis \textit{et al}.’s structure, we would need to change the system of Chapter 2 to account for the weak adjectives under the assumption of a null article.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As a second option, we could suggest that the adjective itself is in a position different from that assumed in \REF{ex:key:5}. Interestingly, for ordinary DPs such as \REF{ex:key:7a}, den \citet[49]{Dikken2006} proposes the structure in \REF{ex:key:7b}, where the adjective is in the specifier position of a Relator Phrase (RP), and the noun is in the complement position of the Relator head (non-pronunciation is marked by capital letters):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%7
    \label{ex:key:7}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  a big butterfly  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.   [\textsubscript{DP/NumP} \textit{a} [\textsubscript{RP} [\textsubscript{AP} \textit{big} ] [ RELATOR [\textsubscript{NP} \textit{butterfly} ]]]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Importantly, the adjective is also in the specifier below the determiner, just as in the canonical cases in Chapter 2 (note that this holds independently of whether or not the adjective is base-generated or moved there). Updating the structure in \REF{ex:key:5}, notice though that the entire RP, which includes the noun in \REF{ex:key:7b}, would form the subject located in Spec,XP. This would essentially yield the same configuration as in \REF{ex:key:5}, and the weak adjectives remain unexplained. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up this subsection, at best, we can observe that the indefinite article in these constructions is not entirely spurious in German. Accepting Bennis \textit{et al}.’s (1998) structure (or adopting a similar structure), we would need to modify the analysis in Chapter 2 to account for the weak adjectives. It is currently not clear to me how to do that without losing the account of the canonical and other non-canonical constructions. If a solution can be found, this modification should preferably not be a (construction-specific) mechanism that simply serves to “save” the Predicate Inversion analysis. At worst, the weak endings on the adjectives hint at the fact that Bennis \textit{et al}.’s structure is not on the right track (for other issues, see \citealt{Matushansky2002}). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that Chapters 2 and 3 documented a number of points that speak in favor of keeping the current analysis – it accounts not only for the canonical but also for many non-canonical cases. Indeed, if we accept the system laid out in Chapter 2, then this discussion reiterates the fact that the inflections of the adjectives are closely tied to the structure of the noun phrase as a whole (Hypothesis 2b). If this is on the right track, then we have a means to narrow down the choices of possible analyses of other constructions. In the next subsection, I consider another type of analysis that seeks to account for the occurrence of spurious articles. We will see that given current assumptions, this type of proposal is also ill-equipped to deal with weak adjectives.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.  Weak Adjectives in Structures Involving Null Nouns
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Extending earlier work by Leu, later published as Leu (2008a,b), van \citet{Riemsdijk2005} also discusses spurious articles in Dutch. Discussing various constructions, van \citet[165]{Riemsdijk2005} provides the example in \REF{ex:key:8} involving a non-\textit{wh}{}-exclamative (see also Bennis \textit{et al}. 1998: 92 fn. 7):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%8
    \label{ex:key:8}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Die auto heeft een deuken!}      (Dutch)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  that car   has   a     dents
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘That car has dents!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Unlike the Predicate Inversion analysis, van Riemsdijk argues for the presence of a semi-lexical null noun. He labels this element TYPE (again, capitalization here indicates non-pronunciation). Based on his structure on page 173, the datum in \REF{ex:key:8} is analyzed as in \REF{ex:key:9}, where !!! stands for an exclamative operator:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%9
    \label{ex:key:9}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          !!!.......[\textsubscript{DP} [ [\textsubscript{D} (\textit{een})] [ DEG [ [\textsubscript{n} TYPE] \textit{deuken} ]]]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With this in place, I return to the discussion of the spurious article and weak adjectives in German. Since I have not come across any examples like \REF{ex:key:8} in German that involve an adjective, I use a slightly different type of case as the basis for discussion – examples where \textit{so} ‘so’ precedes the indefinite article. As far as I can see, this additional element does not affect the point to be made.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The presence of a null noun allows a different analysis of the spurious article. Consider \REF{ex:key:10a}, an example taken from the Appendix. Unlike Predicate Inversion, here a null noun is present by assumption, and the ending on \textit{ein} could be interpreted as feminine agreeing with that null noun (recall that the strong inflection -\textit{e} is ambiguous between feminine and plural in the nominative/accusative). As for the postulated null noun with feminine gender, an overt counterpart in German would be \textit{Art} ‘kind’ \REF{ex:key:10b}. In certain “affective” contexts, this noun would remain unpronounced \REF{ex:key:10c}: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%10
    \label{ex:key:10}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \%   \textit{ich hab  mal~}[\emph{so'n{}-e            ferien]}\textit{~   \textsubscript{} }\textit{in den alpen mit   ner jugendgruppe gemacht~}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I    have \textsc{prt}  so.a-\textsc{pl/fem} holidays in the  Alps  with a     youth.group   made
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Once, I had such a vacation with a youth group in the Alps.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{so} \textit{eine Art   Ferien}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    so a     \textsubscript{} kind holidays
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘such a kind of holidays’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   c.   \textit{so} \textit{eine ART    Ferien} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     so a     \textsubscript{} TYPE holidays
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In other words, the bracketed string in \REF{ex:key:10a} is analyzed as in \REF{ex:key:10c}. Note that the presence of a null noun leads to a split of these structures into two subparts, each involving a noun (i.e., \textit{ART}, \textit{Ferien}). To be clear, then, the postulation of a null noun involves an analysis different from the Predicate Inversion structure. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Notice, however, that this type of account also faces problems as regards weak adjectives. Considering an attested example with an adjective \REF{ex:key:11a}, there are two ways to analyze this datum as regards the modifier. The adjective could precede the null noun \REF{ex:key:11b}, or it could follow the null noun \REF{ex:key:11c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%11
    \label{ex:key:11}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

         a.  \%  \textit{ihr  macht~}\emph{so ne geilen}\textit{~      lieder~}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

you make so a    awesome songs
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘You compose such awesome songs.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. (*)  \textit{so} \textit{eine} \emph{geilen} \textit{ART Lieder}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. (*)  \textit{so} \textit{eine ART} \emph{geilen} \textit{Lieder}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There are strong indications that both analyses in \REF{ex:key:11b} and \REF{ex:key:11c} are not correct. Starting with \REF{ex:key:11b}, it is clear that the feminine form \textit{eine} can only occur in the nominative/accusative. I point out that -\textit{en} is not a possible adjective ending, strong or weak, in those instances (it would be -\textit{e}). As for \REF{ex:key:11c}, the adjective is located in the second subpart together with the plural noun. This type of structure is familiar from pseudo-partitive constructions. This seems an avenue worth taking. \citet{Löbel1989} observes in this regard that the second nominal in pseudo-partitives can be in all four morphological cases in German. In light of this, I entertain the possibility that the adjective inflection -\textit{en} could be interpreted as weak or strong. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with the strong endings, I observe that -\textit{en} can only occur in the dative plural (the strong endings in the other cases are -\textit{e} or -\textit{er}). In the dative plural, the head noun usually takes -\textit{n} (e.g., \textit{mit geilen Lieder-n} ‘with awesome songs’). Crucially though, \textit{Liedern} is not at all possible in \REF{ex:key:11a}. This means that \textit{geilen} cannot be in the dative plural and thus cannot be strong. If so, then \textit{geilen} should be weak in these instances. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note first that the weak ending -\textit{en} occurs in all morphological cases in the plural. As argued in Chapter 2, this implies the presence of a determiner triggering Impoverishment. However, \textit{geilen} in \REF{ex:key:11c} does not have its own relevant determiner (recall again that a null determiner, arguably present in the second nominal in \REF{ex:key:11c}, does not trigger Impoverishment). This leaves the option of the weak inflection unexplained. I conclude then that the presence of a null feminine noun cannot explain \REF{ex:key:11a}. In other words, weak adjectives also present a problem for this type of analysis (for other issues, see Corver \& van \citealt{Koppen2011a}: 61 fn. 6, 69 fn. 24).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   In order to explain the weak adjective in the current system, it is clear that the indefinite article and the adjective must form a regular DP. While I cannot discuss all the above constructions in detail here, note that they are, in some sense, “affective” or “emotive”. As such, I would like to suggest that such contexts license the occurrence of the plural indefinite article in these constructions. In Chapter 8, \sectref{sec:key:2.2.3}, I propose that these contexts license the presence of certain null operators (cf. the operator !!! in \REF{ex:key:9}). These null operators are flagged by \textit{ein} yielding the instances of the plural article. The latter triggers Impoverishment on the adjectives as discussed in Chapter 2.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Summarizing the last two subsections, I documented that overt plural indefinite articles are followed by weak adjectives in German. Given the current analysis, I pointed out that these weak adjectives pose a challenge for accounts involving Predicate Inversion or null nouns. There are two ways to proceed: either the current analysis of adjectival inflections needs to be modified, or the above accounts of the spurious articles need to be changed. In other words, proponents of analyses involving Predicate Inversion or null nouns need to find a solution to explain the weak adjectives in these non-canonical constructions (NB: this proposal should also explain the canonical and other non-canonical cases). Since I am not aware of any such attempts, the alterntive is to suggest that constructions with spurious articles involve regular structures licensed in certain contexts. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

More generally, given that the current analysis has broad empirical coverage as regards adjectival inflections, it shows good promise for a (more) comprehensive account. Indeed, the current system has revealed a number of interesting consequences. Most importantly, adjectival inflections in German are related to abstract structure (Hypothesis 2b). To reiterate, if this turns out to be correct, then this account narrows down the options of possible structural analyses of nominals. In the next section, I discuss strong adjectives that raise issues for certain structural analyses of split topicalization.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.  Strong Adjectives in Structures Involving Split Topicalization
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Discontinuous phrases have received much attention in the literature. German is interesting in that it allows the lower part of a noun phrase to be left dislocated. Compare \REF{ex:key:12a} to \REF{ex:key:12b}. The topicalized element in \REF{ex:key:12b} functions as a contrastive topic and the lower nominal forms a focus. These two parts are related by a bridge intonation contour indicated by slash signs below:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%12
    \label{ex:key:12}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.    \textit{Ich habe keine Bücher gelesen}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      I     have no     books   read
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      ‘I have read no books.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    b.  /\textbf{\textit{BÜcher}} \textit{habe ich} \textbf{\textit{KEI}}{\textbackslash}\textbf{\textit{ne}} \textit{gelesen}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     books    have  I    none     read.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for books, I have read none.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To establish some terminology, I refer to this construction as split topicalization, to the left nominal as split-off, and to the right one as source. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As is well known, discontinuous DPs exhibit a number of paradoxical properties indicating both movement and separate base-generation. With current purposes in mind, I focus on the intriguing behavior of adjectives (for paradoxical properties not related to adjectives, see, for instance, \citealt{Ott2011a} and other references mentioned below). To account for these properties, different proposals have been made. In what follows, I discuss three types of analysis (see also van \citealt{Hoof2006} and references cited therein). I show that movement out of one noun phrase faces problems in the account of strong adjectives. Similarly, a copy-and-delete approach to split topicalization cannot account for strong adjectives either. However, the base-generation of two independent noun phrases is completely compatible with the discussion in Chapter 2.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.1.  Movement out of the Source
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Van \citet[122]{Riemsdijk1989} observes that the linear order of the adjectives in split topicalizations corresponds to the one without a split. Compare the sequences of adjectives in the unsplit examples in \REF{ex:key:13} to those in the split ones in \REF{ex:key:14}:\footnote{The judgments in \REF{ex:key:13b} and \REF{ex:key:14b} are not uncontroversial and probably too strong (see \citealt{FanselowĆavar2002}: 79-80, \citealt{Ott2011a}: 30). However, I continue providing the original judgments.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%13
    \label{ex:key:13}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein neues amerikanisches Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a    new   American          car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a new American car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. *  \textit{ein amerikanisches neues Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a    American          new   car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%14
    \label{ex:key:14}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textbf{\textit{Ein} \textbf{amerikanisches} \textbf{Auto}} \textit{kann ich mir} \textbf{\textit{kein} \textbf{neues}} \textit{leisten.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    an   American           car\textsc{.neut} can   I     \textsc{refl} no    new     afford
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for an American car, I cannot afford a new one.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. *  \textbf{\textit{Ein} \textbf{neues} \textbf{Auto}} \textit{kann ich mir} \textbf{\textit{kein} \textbf{amerikanisches}} \textit{leisten.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    an   new    car\textsc{.neut} can   I     \textsc{refl} no    American           afford
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This can straightforwardly be explained under a movement analysis. In more detail, van \citet{Riemsdijk1989} and \citet[249-50]{Bhatt1990} argue for movement of the split-off out of the source. Adopting the DP-hypothesis, the above data can be analyzed as follows (see also \citealt{Pafel1995}, \citealt{Murphy2018}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%15
    \label{ex:key:15}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textbf{\textit{Ein} \textbf{amerikanisches} \textbf{Auto}} \textit{kann ich mir} \textbf{\textit{kein} \textbf{neues}} \textit{leisten.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    an   American           car\textsc{.neut} can   I     \textsc{refl} no    new     afford
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for an American car, I cannot afford a new one.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 b. [ \textbf{\textit{Ein} \textbf{amerikanisches} \textbf{Auto} }]\textsubscript{i} \textit{kann ich mir} [\textsubscript{DP} \textbf{\textit{kein} \textbf{neues}} [ t\textsubscript{i} ]] \textit{leisten..}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that there are two determiners in \REF{ex:key:15}, \textit{ein} ‘a’ and \textit{kein} ‘no’, where \textit{ein} in the split-off is apparently doubling the determiner element in the source. To explain the presence of \textit{ein} in the split-off, it is assumed that this element is inserted later in the derivation (in \sectref{sec:key:3.4}, I show that late insertion of \textit{ein} is unlikely to be correct).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Indefinite articles are usually absent in the split-off. Crucially, the distribution of adjectival inflections in such cases indicates an analysis different from simple movement. Note in this regard that adjectival inflections differ in regular and discontinuous noun phrases: the adjective is weak in a regular, unsplit DP \REF{ex:key:16a}; it is strong, if the adjective is topicalized \REF{ex:key:16b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%16
    \label{ex:key:16}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Ich habe kein-e bunt-en                 Blumen gekauft}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     have no-\textsc{st}  multi.colored-\textsc{wk} flowers bought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I have bought no multi-colored flowers.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [ \textbf{\textit{Bunt-e}                \textbf{Blumen}} ]\textsubscript{i} \textit{habe ich} [\textsubscript{DP} \textbf{\textit{kein-e}} [ t\textsubscript{i} ]] \textit{gekauft}..
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      multi.colored-\textsc{st} flowers     have I         none-\textsc{st}       bought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for multi-colored flowers, I have bought none.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With Chapter 2 in mind, I point out that the weak ending in \REF{ex:key:16a} shows that Impoverishment has taken place. The strong ending in \REF{ex:key:16b} indicates that Impoverishment has not occurred. This implies that either \REF{ex:key:16b} involves a non-canonical structure or that no relevant determiner is present in the split-off (or both). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Given a movement type of analysis, note that the entire noun phrase is assembled first. This is followed by movement of the split-off. Observe though that late separation, that is, building a regular DP first and then moving the lower part out would bring about a (wrong) weak ending in \REF{ex:key:16b}. In other words, assuming movement out of the DP, the change of the adjective ending from weak in \REF{ex:key:16a} to strong in \REF{ex:key:16b} becomes mysterious. As documented in Chapters 2 and 3, a simple surface-oriented account of the strong/weak alternation does not suffice to explain these data.\footnote{Note that proponents of this and other structures of split topicalization do usually not provide (m)any details of their account of the strong/weak alternation of adjectives.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  However, a strong ending on an unpreceded adjective is exactly what we expect if the two noun phrases in \REF{ex:key:16b} are base-generated independently of each other; that is, these elements do not seem to be related by movement. In the latter case though, the structure is different – there are two separate nominals, and the determiner that could trigger Impoverishment on the adjective in the split-off is in the other noun phrase, the source. As a consequence, Impoverishment does not occur, and the adjective in the split-off surfaces with a strong inflection as in \REF{ex:key:16b}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To drive this point home, note that two related adjectives may show different inflections when split up such that the adjective in the source is weak, but the adjective in the split-off is strong (cf. \citealt{Haider1993}: 215 for similar data). Compare \REF{ex:key:17a} to \REF{ex:key:17b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%17
    \label{ex:key:17}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Ich habe kein-e groß-en bunt-en                 Blumen gekauft}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     have no-\textsc{st}  big-\textsc{wk}  multi.colored-\textsc{wk} flowers  bought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I have bought no big multi-colored flowers.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textbf{\textit{Bunt-e}                 \textbf{Blumen}} \textit{habe ich} \textbf{\textit{kein-e} }\textbf{\textit{\textsubscript{} }}\textbf{\textit{groß-en}} \textit{gekauft}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    multi.colored-\textsc{st} flowers  have I     no-\textsc{st}   big-\textsc{wk}  bought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for multi-colored flowers, I have not bought any big ones.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Again, a movement analysis cannot account for the different endings on the two adjectives. This is particularly clear here since the underlyingly higher adjective (\textit{groß} ‘big’) is weak, but the underlyingly lower adjective (\textit{bunt} ‘multi-colored’) is strong. This issue is brought out very clearly on current assumptions: with the determiner having moved to the DP-level in the source, it is the lower element(s) that should appear with a weak ending (usually adjectives) and the higher one(s) with a strong ending (usually determiner(-like) elements). Indeed, all adjectives should have the same inflection in a nominal, later split up by movement.\footnote{There is another type of analysis involving movement out of the source. \citet{Tappe1989} argues for a combination of different base-generations and movement. In particular, the split-off is base-generated in Spec,CP, and the source is base-generated in situ. The lower part of the source is proposed to move into the complement position of the split-off. The example in (ia) is derived as in (ib):(i)  a.  \textit{So*(}\textbf{\textit{’nen}}) \textbf{\textit{Wagen}} \textit{kann ich mir} \textbf{\textit{keinen}} \textit{leisten}      such  a      car.\textsc{masc} can   I    \textsc{refl} none    afford      ‘As for such a car, I cannot afford one.’    b.  [\textsubscript{DP} \textit{so’nen} [\textsubscript{NP} \textit{Wagen} ]\textsubscript{i} ] \textit{kann ich mir} [\textsubscript{DP} \textit{keinen} t\textsubscript{i} ] \textit{leisten}This proposal leads Tappe to revise standard assumptions about chains, which raises other issues.}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.  Movement but not out of the Source
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet{FanselowĆavar2002} hypothesize that split topicalizations involve movement but crucially \textit{not} out of the DP that will surface in two different parts. As a technical implementation, they argue for a different type of account adopting the copy-and-delete approach to movement \citep{Chomsky1995}. Moving the entire DP, they propose that deletion may affect \textit{both} copies. Glossing over some of the details here, they suggest that the determiner is deleted in the higher copy and the head noun in the lower one. This derives \REF{ex:key:18a} as in \REF{ex:key:18b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%18
    \label{ex:key:18}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

            a.     \textbf{\textit{Wagen}} \textit{hat  er sich  noch k-}\textbf{\textit{einen}} \textit{leisten können}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    car.\textsc{masc} has he \textsc{refl} yet    \textsc{neg}{}-one afford  could
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for a car, he has not been able to afford one.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    b.  \{einen Wagen\} hat er sich noch k- \{einen Wagen\} leisten können 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that these authors treat \textit{keinen} ‘no/none’ as a composite form consisting of negative \textit{k}{}- and the indefinite article \textit{einen} (Chapter 5).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  At first glance, this analysis of distributed deletion seems to receive strong confirmation from the fact that the deletion of the higher copy of the determiner may be suspended yielding two instances of \textit{ein}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%19
    \label{ex:key:19}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

            a.     \textbf{\textit{Einen} \textbf{Wagen}} \textit{hat  er sich  noch k-}\textbf{\textit{einen}} \textit{leisten können}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a         car.\textsc{masc} has he \textsc{refl} yet    \textsc{neg}{}-one afford  could
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for a car, he has not been able to afford one.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    b.  \{einen Wagen\} hat er sich noch k- \{einen Wagen\} leisten können 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

However, upon closer inspection, it turns out that both determiners do not have to be the same \REF{ex:key:20a}. In fact, when the determiner in the source is definite, the one in the split-off cannot be definite \REF{ex:key:20b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%20
    \label{ex:key:20}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

            a.  \textbf{\textit{Einen} \textbf{Wagen}} \textit{hat er  sich  nur  \textsubscript{} }\textbf{\textit{diesen}} \textit{leisten können}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a         car.\textsc{masc} has he \textsc{refl} only this     afford  could 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for a car, he has only been able to afford this one.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    b.  *  \textit{\{}\textbf{\textit{Diesen}} \textit{/} \textbf{\textit{Den}}\textit{\}} \textbf{\textit{Wagen}} \textit{hat \textsubscript{} }\textit{er sich  nur} \textbf{\textit{diesen}} \textit{leisten können}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        this      / the     car.\textsc{masc} has he \textsc{refl} only this     afford  could 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Now, if a copy-and-delete type of analysis were correct, we would expect the grammaticality judgments in (20a-b) to be the reverse. It could be suggested though that this type of contrast is handled by repair rules. However, we see in \sectref{sec:key:3.4} that late insertion of \textit{ein} in \REF{ex:key:20a} is unlikely to be correct. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Returning to the main line of argument, we can observe that this type of proposal is not compatible with the analysis of adjectival inflections in Chapter 2 either. Specifically, constructing a regular DP first (\textit{keine bunt-en Blumen} ‘no multi-colored{}-\textsc{wk} flowers’), we would expect the weak ending -\textit{en} on the adjective in the split-off, contrary to fact. Note that this would hold independently of whether \textit{keine} is treated as a composite \REF{ex:key:21b} or not \REF{ex:key:21c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%21
    \label{ex:key:21}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textbf{\textit{Bunt-e}                 \textbf{Blumen}} \textit{habe ich} \textbf{\textit{kein-e}} \textit{gekauft}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    multi.colored-\textsc{st} flowers  have I     none-\textsc{st} bought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for multi-colored flowers, I have bought none.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  (*)  \{eine bunt-en Blumen\} habe ich k- \{ein-e bunten Blumen\} gekauft.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  (*)  \{keine bunt-en Blumen\} habe ich \{kein-e bunten Blumen\} gekauft.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As mentioned already above, a surface-oriented explanation of the strong ending is not sufficient to explain the whole range of facts. In view of these and some other issues (e.g., the licensing of Negative Polarity Items, see \citealt{Bosse2009}: 278), a different technical implementation of split topicalizations is called for.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.3.  Separate Base-generation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Some alternative analyses take as their point of departure Fanselow and \citegen{Ćavar2002} generalization that split topicalizations involve movement but crucially \textit{not} out of the DP that surfaces in two separate parts. Specifically, these proposals involve the base-generation of two separate nominals in the VP and moving one of them (or both) to the left. I agree with this assessment, and as far as I am aware, a consensus seems to be emerging that this is indeed the correct characterization of the facts. There have been several proposals to make this idea concrete.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Specifically, \citet{Bosse2009} proposes an analysis where she links split topicalizations to Restrictive Elliptical Appositives (van \citealt{Riemsdijk1998b}). \citet{Ott2011a} argues for an account that involves breaking up a symmetric bare-predication structure. Third, \citet{Roehrs2011} also argues for the base-generation of two separate nominals. All three analyses are compatible with the discussion in Chapter 2. Given that the third analysis involves fewer new assumptions, I illustrate this proposal here in some detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Basing the following account on \citet{Fanselow1988}, \citet{Roehrs2011} proposes that there is a division of labor between the syntax and the semantics. In particular, it is suggested in that paper that split topicalizations involve the separate base-generation of an argumental DP and a related predicative nominal in the same local domain, the VP. The argumental part contains an empty noun (e\-\textsubscript{N}). The initial stage of the derivation of \REF{ex:key:22a} is illustrated in \REF{ex:key:22b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%22
    \label{ex:key:22}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textbf{\textit{Bunt-e}                 \textbf{Blumen}} \textit{habe ich} \textbf{\textit{kein-e}} \textit{gekauft}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    multi.colored-\textsc{st} flowers  have I    \textsubscript{} none-\textsc{st} bought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for multi-colored flowers, I have bought none.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.          VP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    AgrP       V’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{bunte Blumen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        DP     V
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{keine e\textsubscript{N}} \textit{gekauft}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Both the predicate split-off and the argumental source undergo movement to the left: AgrP undergoes topicalization to Spec,CP; DP moves for case. This yields the correct word order. Assuming that the overt nominal in the split-off and e\textsubscript{N} in the source are of the same semantic type, the “unsaturated” predicate in Spec,CP is closed off by interpreting it in e\textsubscript{N} of the argumental DP filling e\textsubscript{N} with semantic content at the same time. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Returning to the issue of inflection, both nominals involve regular structures where the adjective in the split-off is in Spec,AgrP. It is clear though that the adjective is in a nominal different from that of the determiner, and as such Impoverishment does not occur. As a consequence, the CNG features on the adjective are not reduced, and the adjective surfaces with a strong ending. This gives the desired result (see also Chapter 2, \sectref{sec:key:3.6}). More generally, proposals involving separate base-generation are fully compatible with the analysis in Chapter 2. I briefly return to the discussion of \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{3.4.} Ein \textit{in the Split-off}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

We saw in \sectref{sec:key:2} that \textit{ein} is not entirely spurious in that it triggers a weak ending on a following adjective. This means that \textit{ein}, or more precisely its feature bundles, cannot be inserted late in a high position. There is independent evidence for this conclusion. It derives from indefinite pronoun constructions occurring as split topicalizations.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As illustrated in Chapters 2 and 3, adjectives can follow an indefinite pronoun \REF{ex:key:23a}. Interestingly, an adjective preceded by an indefinite article cannot \REF{ex:key:23b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%23
    \label{ex:key:23}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{etwas                  Amerikanisches}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    something.\textsc{neut} American
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘something American’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\setcounter{listWWviiiNumxleveli}{1}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumxleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

*  \textit{etwas                  ein Amerikanisches}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumxleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    something.\textsc{neut} an  American
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

At first glance, we might claim that this is a phonetic restriction such that the pronoun and \textit{ein} cannot be adjacent. However, consistent with \REF{ex:key:23b}, split topicalizations formed on the indefinite pronoun construction cannot involve \textit{ein} in the split-off either. Compare \REF{ex:key:24a} to \REF{ex:key:24b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%24
    \label{ex:key:24}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  (\textbf{\textit{Ein}}) \textbf{\textit{amerikanisches}} \textit{hat er sich   nur} \textbf{\textit{eins}} \textit{leisten können}.        
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  an    American          \textsubscript{} has he \textsc{refl} only one  afford \textsubscript{} could
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for American ones, he has been able to afford only one.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{(*}\textbf{\textit{Ein}}) \textbf{\textit{Amerikanisches}} \textit{hat er \textsubscript{} }\textit{sich  \textsubscript{} }\textbf{\textit{etwas}} \textit{leisten können}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       an     American           \textsubscript{} has he \textsc{refl} something.\textsc{neut} afford could
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for American stuff, he has been able to afford something.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Considering \textit{nichts (*ein) Amerikanisches} ‘nothing American’, the same facts hold if the source involves the negative counterpart of \textit{etwas} ‘something’:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%25
    \label{ex:key:25}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  (\textbf{\textit{Ein}}) \textbf{\textit{amerikanisches}} \textit{hat er sich   noch} \textbf{\textit{keins}} \textit{leisten können}.        
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  an    American          \textsubscript{} has he \textsc{refl} yet    none  afford could
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for American ones, he has not been able to afford one yet.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{(*}\textbf{\textit{Ein}}) \textbf{\textit{Amerikanisches}} \textit{hat er \textsubscript{} }\textit{sich  \textsubscript{} }\textit{noch} \textbf{\textit{nichts}} \textit{leisten können}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       an     American           \textsubscript{} has he \textsc{refl} yet   \textsubscript{} nothing.\textsc{neut} afford  could
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for American stuff, he has not been able to afford anyething.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In view of the ungrammaticality induced by \textit{ein} in \REF{ex:key:24b} and \REF{ex:key:25b}, a phonetic restriction cannot explain all the ungrammatical cases: unlike in \REF{ex:key:23b}, the pronoun and \textit{ein} are neither adjacent in \REF{ex:key:24b} nor in \REF{ex:key:25b}. Note in this respect that late insertion of \textit{ein} does not help explain the ungrammaticality in a surface-oriented account: if \textit{ein} were inserted late (i.e., in PF), the local context of \textit{ein} inside the split-off in the grammatical \REF{ex:key:24a} and \REF{ex:key:25a} and in the ungrammatical \REF{ex:key:24b} and \REF{ex:key:25b} would be the same; that is, \textit{ein} would precede the adjectives (and be separated from the pronouns) in both the grammatical and ungrammatical cases of \REF{ex:key:24} and \REF{ex:key:25}. Consequently, this cannot explain the difference between the (a)-examples and the (b)-examples above. Rather, the difference between these cases seems to be located in the source: while \textit{eins} ‘one’ and \textit{keins} ‘none’ are pronominal forms related to \textit{ein} ‘a’ and \textit{kein} ‘no’, \textit{etwas} ‘something’ and \textit{nichts} ‘nothing’ are inherent pronouns.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Above, I suggested that the split-off is related to the source by interpreting the former inside the latter. Note that interpretation occurs at LF (and not PF). Now, the pronominal forms (\textit{eins}, \textit{keins}) presumably involve an internal makeup different from that of the inherent pronouns (\textit{etwas}, \textit{nichts}). While there are several options to implement this idea, it seems clear that interpreting the split-off containing \textit{ein} inside the source is compatible with the makeup of the pronominal forms but that this is not compatible with the makeup of the inherent pronouns.\footnote{One idea to make this more concrete is to assume that DPs and smaller elements can be interpreted in e\textsubscript{N} of the pronominal forms but that DPs are too large to be interpreted inside the inherent pronouns – only smaller elements, the ones lacking \textit{ein}, can do so. Alternatively, in the second part of the book, I argue that \textit{ein} flags the presence of a null operator. If so, it might be claimed that it is actually this null operator that is incompatible with the inherent pronouns.} If this is on the right track, then it seems clear that the presence of \textit{ein} cannot be a late phenomenon in the ungrammatical \REF{ex:key:24b} and \REF{ex:key:25b} – its presence at LF helps explain the ungrammaticality. Note in this regard that the presence of an element at LF implies the presence of that element earlier in the syntactic derivation. If so, then the occurrence of \textit{ein} in the grammatical \REF{ex:key:24a} and \REF{ex:key:25a} cannot involve a PF phenomenon on parity of assumption. Returning briefly to \REF{ex:key:23b}, recall from Chapter 2, \sectref{sec:key:2.3} that indefinite pronoun constructions involve adjunction mediated by ModP. With this in mind, the ungrammaticality of this example could be explained by assuming that the head Mod selects AgrP, which excludes preceding \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up, I have demonstrated that split topicalizations are only compatible with the analysis of Chapter 2 if they involve two separate base-generations (but not if they involve movement out of the DP or a copy-and-delete approach). Next, I briefly discuss adjectival inflections as regards semantic concepts. Specifically, I discuss inflections on adjectives as regards the restrictive and non-restrictive interpretation of adjectives, and I examine inflections on determiners as regards “referentiality”. In keeping with the current discussion, we will see that independently of their (non-)restrictive interpretation, inflected adjectives are in their regular positions (i.e., Spec,AgrP) and that the inflections themselves make no semantic contribution in German.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.   Weak Adjectives with Non-restrictive Interpretation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As documented in detail in Chapter 2, adjectives are usually weak if they follow a determiner. Note that the adjective in these types of strings can actually have two interpretations, restrictive or non-restrictive (the latter is often called appositive). Considering the noun phrases in (26a-b), the restrictive interpretation of the adjective is given in the translation line in \REF{ex:key:26a}, and the non-restrictive reading is provided in that of \REF{ex:key:26b}. For clarity, I use a relative clause to translate the adjective. The non-restrictive interpretation is clearly brought out by the addition of \textit{übrigens} ‘by the way’ and is sometimes called a by-the-way remark. To repeat, the adjectives are weak in both contexts:\footnote{In other languages, the strong/weak alternation of the adjective does indicate a difference in (non-)restrictive interpretation. In Icelandic, adjectives with a weak ending are restrictive in interpretation (ia), but adjectives with a strong ending are not restrictive (ib) (see \citealt{Delsing1993}: 132 fn. 25; \citealt{Thráinsson1994}: 166, 2007: 3, 89; \citealt{Sigurðsson2006}: 200 fn. 3): (i)  a.  \textit{gul-i            bill-inn}        (Icelandic)      wellow\textsc{{}-wk} car-\textsc{def}      ‘the car that is yellow’    b.  \textit{gul-ur       bill-inn}      yellow-\textsc{st} car-\textsc{def}      ‘the car, which is yellow’For recent discussion, see also \citet{Pfaff2017}. As regards inflection and position of prenominal adjectives, note also \citet{Evans2021}, who discusses a correlation of inflected and uninflected adjectives with different interpretations in Dutch. He proposes that inflected and uninflected adjectives are in different positions yielding the different interpretations.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%26
    \label{ex:key:26}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{der alt-e     \textsubscript{} }\textit{Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the \textsubscript{} old\textsc{{}-wk} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the man that is old’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{der (übrigens)    \textsubscript{} }\textit{alt-e     Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the  \textsubscript{} incidentally old-\textsc{wk} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the man, who is (by the way) old’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With the above analysis of weak endings in mind, this implies that both restrictive and non-restrictive interpretations involve the same basic (regular) structure in which Impoverishment has occurred. In other words, inflected non-restrictive adjectives cannot be inserted late, that is, after Impoverishment has occurred. Rather, I suggest that the structure of non-restrictive adjectives involves an additional lexical element.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In \citet[104]{Roehrs2009a}, I propose that the main difference between the restrictive and the non-restrictive interpretation of adjectives involves the absence or presence of a null \textit{pro} co-indexed with the hosting DP. This can be schematically represented as follows:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%27
    \label{ex:key:27}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          [ \textit{der} [ (pro\textsubscript{i}) \textit{alte} ] \textsubscript{} \textit{Mann} ]\textsubscript{(i)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsubscript{} the             old     man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In order to account for the uniform inflectional behavior of the adjectives, it is important to be more precise about the relevant structures and derivations. With the focus on the morpho-syntax, I only briefly discuss the semantics (for the denotations, see \citealt{Roehrs2009a}: Chapter 3). I start with the case involving a restrictive interpretation. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall from Chapter 2 that the determiner moves from below the adjective to the DP-level \REF{ex:key:28}. Given this structure and the presence of a determiner, Impoverishment occurs bringing about the weak ending on the adjective. As to the semantics, I assume that kind nouns are of type <e>. Following de Swart \textit{et al}. (2007), I argue in Chapter 6 that a kind noun combines with the realization operator REL (type <e<e,t>>) to yield a predicate (type <e,t>). This predicate combines with the adjective by Predicate Modification. The resulting conjunction of the two predicates is combined with the determiner (type <<e,t>e>) by Functional Application yielding an individual (type <e>):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%28
    \label{ex:key:28}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Restrictive Interpretation}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    DP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          (Functional Application)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{der}\textsubscript{<<e,t>e>i}        AgrP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            (Predicate Modification)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{alte}\textsubscript{<e,t>}            ArtP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      \textit{der}\textsubscript{i}            NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              (Application of REL)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             Num\textsubscript{REL<e<e,t>>}  NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                     \textit{Mann}\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The structure of the non-restrictive reading is basically the same. Unlike above, however, the determiner is interpreted in its base-position; that is, the adjective is outside the scope of the determiner \REF{ex:key:29} (see also \citealt{Partee1973}: 54, \citealt{Schoorlemmer2009}). Proceeding bottom-up and much like above, the kind noun combines with the realization operator REL to yield a predicate. Unlike above, the low copy of the determiner combines with the predicate noun yielding an element of type <e>. In order for this resultant nominal to combine with a clausal predicate (type <e,t>), I adopt a model of multiple semantic spell-out, whereby the higher by-the-way remark is sent off for interpretation separately (for details, see \citealt{Roehrs2009a}: 102-06). Structurally, this remark consists of the adjective and \textit{pro}, indicated by square brackets below. These two elements combine by Functional Application yielding a truth value (I continue the discussion of this subcomponent further below). After this element is sent off to Spell-out, the remaining nominal can combine with a clausal predicate (not shown here): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%29
    \label{ex:key:29}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Non-restrictive Interpretation}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    DP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{der}\textsubscript{i}                 AgrP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       [pro\textsubscript{<e>} \textit{alte}\textsubscript{<e,t>}]\textsubscript{<t>}           ArtP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              (Functional Application)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        sem. spell-out    \textit{der}\textsubscript{<<e,t>e>i}       NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              (Application of REL)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             Num\textsubscript{REL<e<e,t>>}  NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                     \textit{Mann}\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recalling that \textit{pro} and \textit{der Mann} ‘the man’ are coindexed, the expression involving \textit{pro} and the adjective is interpreted as an additional, non-restrictive remark about \textit{der Mann}, something along the lines of ‘the man – he is old’. To be clear then, the determiner in both interpretations has moved to the DP-level in syntax triggering Impoverishment. However, the determiner is interpreted in different positions at LF. Consider the internal structure of the non-restrictive modifier in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In Chapter 2, \sectref{sec:key:2.4}, I argued that adjectives involve extended projections. In particular, I suggested there that they consist of an adjective stem at the bottom and an inflectional head at the top. Note that adjectives can take dependents, for instance, an assumed \textit{pro}. In order for the stem to combine with its inflection, the stem along with its dependents moves to Spec,InflP. The minimal structure is as follows: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%30
    \label{ex:key:30}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

                   InflP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    AP\textsubscript{<t>k}      Infl’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 (Functional Application)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

pro\textsubscript{<e>}           \textit{alt}\textsubscript{<e,t>}   {}-\textit{e}      t\textsubscript{k}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Semantically, the pronominal element \textit{pro} (type <e>) combines with the adjective (type <e,t>) by Functional Application yielding a truth value (type <t>). This is the desired result for the by-the-way remark ‘he is old’.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Observe that independently of the presence of \textit{pro}, Impoverishment can occur in a local fashion, namely on the highest head of the extended projection of the adjective (Infl). In other words, given this structure, Impoverishment is compatible with different interpretations of the adjective. If this is accepted, then I can continue claiming that weak adjectival inflections indicate one and the same structural constellation and are independent of the semantics (Hypothesis 1a). Finally, I turn to a case where an adjectival inflection seems to be related to “referentiality”.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.  Adjectival Inflections Make Nominal Features Visible
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Adjectival inflections occur on determiners and adjectives but not, for instance, on verbs. It is clear that they are nominal in nature. I argued above that adjectival inflections are semantically vacuous. I demonstrated that adjectival inflections do not indicate, or are a reflex of, (in-)definiteness or (non-)restrictiveness in interpretation. Considering certain other data, there is another possible semantic concept they could be associated with.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Alexiadou \textit{et al.} (2011: 31) point out that inflected \textit{dieses} ‘this’ is an anaphor for noun phrases only, but uninflected \textit{dies} ‘this’ is an anaphor for both noun phrases and clauses. Compare \REF{ex:key:31a} to \REF{ex:key:31b} (data slightly adapted; see also \citealt{HelbigBuscha2001}: 229-30):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%31
    \label{ex:key:31}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Hans hat ein rotes Buch.         \{Dies-es / Dies\} war sehr} \textit{\textsubscript{} }\textit{teuer}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Hans has a    red    book.\textsc{neut}. this-\textsc{st}              was very expensive
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Hans has a red book. It was very expensive.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Daß Maria bereits  angekommen ist, \{*dieses / dies\} weiß} \textit{\textsubscript{} }\textit{ich genau}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    that \textsubscript{} Mary  \textsubscript{} already arrived           is      \textsubscript{} this                know I     well
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I am positive that Mary has already arrived.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given the data above, we might suggest that the two demonstrative forms are semantically different in that inflected \textit{dieses} must have a noun phrase as its antecedent, but uninflected \textit{dies} is less restricted and tolerates both a noun phrase and a clause as its antecedent. As these two elements differ in inflection, we might further suggest that adjectival endings have or are sensitive to the semantics after all. In particular, since inflected \textit{dieses} can only refer back to noun phrases, we could claim that inflectional endings are, in some sense, “referential”. I think this possible interpretation of the facts is not correct. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In Chapter 3, \sectref{sec:key:3}, I discussed adjectives like \textit{lila} ‘purple’ and \textit{rosa} ‘pink’. I pointed out that the inflections on these adjectives are optional when a noun follows \REF{ex:key:32a} but that they are obligatory when such a noun is absent \REF{ex:key:32b}. Above, I argued that the source of split topicalization structures contains a null noun, which is licensed by the predicate split-off. With this in mind, I suggest that \REF{ex:key:32b} also involves a null noun, here licensed by the anaphoric relation with the noun in the previous sentence (\citealt{Fanselow1988}: 101, \citealt{Murphy2018}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%32
    \label{ex:key:32}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein lila(-nes)     Kleid}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a    purple-\textsc{infl} dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a purple dress’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{Da    waren viele  bunte              Kleider. Ich} \textit{kaufte  ein lila*(-nes).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    there were   many multi-colored dresses   I    bought a    purple-\textsc{infl} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘There were many multi-colored dresses. I bought a purple one.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Notably, indeclinable elements never have endings including in anaphoric contexts. This can be illustrated with the numeral for ‘ten’:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%33
    \label{ex:key:33}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{zehn Kleider}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     ten   dresses
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘ten dresses’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Da    waren viele}  \textit{Kleider. Ich kaufte  zehn}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     there were   many dresses  I     bought ten
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘There were many dresses. I bought ten.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The distinction of declinability makes \textit{lila}{}- ‘purple’ in \REF{ex:key:32} and \textit{zehn} ‘ten’ in \REF{ex:key:33} different. The generalization that seems to be emerging is that adjectival inflections are obligatory when the hosting elements are declinable and occur in anaphoric contexts; indeclinable elements remain uninflected in anaphoric contexts. If this holds more generally, then there is an interesting consequence for the two forms of the demonstrative \textit{dies} and \textit{dieses}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

At first glance, this demonstrative appears to be similar to the adjectives \textit{lila} and \textit{rosa} – when overt material follows, the inflection is optional \REF{ex:key:34}. Note though that uninflected \textit{dies} can only occur in nominative/accusative contexts, both as a determiner \REF{ex:key:34a} or as a predeterminer \REF{ex:key:34b}:\footnote{Recall that this restriction makes \textit{dies(es)} ‘this’ different from \textit{all(e)} ‘all’. The inflection of the latter is not subject to a restriction in morphological case (see again Chapter 3, \sectref{sec:key:6}).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%34
    \label{ex:key:34}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{dies(-es)  schön-e    Kleid}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this-\textsc{infl} pretty-\textsc{wk} dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this pretty dress’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{dies(-es)  mein Glück}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this-\textsc{infl} my    happiness.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this happiness of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There are two differences to the adjectives above. First, both the inflected and the uninflected forms of the demonstrative are possible in anaphoric contexts involving noun phrases as in \REF{ex:key:31a}. Note that the noun phrase in \REF{ex:key:31a} is in the neuter gender. Second, the demonstrative is also possible in anaphoric contexts involving clauses, but here only the uninflected form appears as in \REF{ex:key:31b}. If the generalization above is correct, then we cannot say that the demonstrative involves one element with an optional inflection. If this were so, then we would expect only the inflected form \textit{dieses} to occur in anaphoric contexts, contrary to fact. In order to explain the occurrence of both the inflected \textit{and} the uninflected form of the demonstrative in \REF{ex:key:31a}, we have to assume that there are actually two independent elements, a declinable \textit{dieses} and an indeclinable \textit{dies}. In other words, we cannot simply assume that \textit{dies} is based on \textit{dieses} such that the ending has been deleted in PF (for this idea, see \citealt{Gallmann2004}: 154 fn. 3, \citealt{Roehrs2009a}: 159 fn. 33, cf. also G. \citealt{Müller2002a}: 117 fn. 8). Note that the proposal of two separate elements is consistent with the aforementioned fact that unlike inflected \textit{dieses}, uninflected \textit{dies} is restricted to nominative/accusative neuter contexts. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The structure of inflected \textit{dieses} is repeated in \REF{ex:key:35a}; the relevant vocabulary insertion rules are given in (35b-d):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \REF{ex:key:35}  \textit{Inflected} dieses
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  a.             InflP\textsubscript{[+D; +DEF, +DEIX][F, N, O, S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          [F, N, O, S]    DemP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            {\textbar}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        Dem
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              [+D; +DEF, +DEIX]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [+D; +DEF, +DEIX]  →  \textit{dies-}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  [+F, -N, +O, αS]   →  \textit{{}-er}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  etc.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that the stem \textit{dies}{}- spells out [+D; +DEF, +DEIX] by \REF{ex:key:35b} and that the varying inflections realize the CNG feature bundle in Infl (by 35c-d). As for uninflected \textit{dies}, I propose that this element is like the first and second-person pronominals discussed in Chapter 3, \sectref{sec:key:5}. As such, I assume that \textit{dies} involves a reduced demonstrative structure \REF{ex:key:36}. The restriction of \textit{dies} to nominative/accusative neuter contexts can be captured by – what I call here – restriction features. These types of features are on [+DEIX], indicated below by the features following the colon sign after [+DEIX]. Specifically, on the one hand, the features [-F, +N, -O] are only compatible with the structure \REF{ex:key:36} being inserted in nominative/accusative neuter contexts; on the other hand, this type of deictic feature involving a restriction only projects a reduced structure:\footnote{These types of restriction features are discussed in more detail in Chapter 8, Sections 2.2.5 and 2.2.6. Among others, they account for the varying distribution of \textit{ein} in regular singular contexts (e.g., \textit{ein Auto} ‘a car’) vs. in plural contexts (e.g., \textit{m-eine Autos} ‘my cars’).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%36
    \label{ex:key:36}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Uninflected} dies
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                DemP\textsubscript{[+D; +DEF, +DEIX]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          {\textbar}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Dem
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [+D; +DEF, +DEIX: -F, +N, -O]  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that the vocabulary insertion rule for \textit{dies}{}- in \REF{ex:key:35b} can apply to both \REF{ex:key:35a} and \REF{ex:key:36}. If it spells out the features in \REF{ex:key:35a}, then we obtain \textit{dies}{}-, where the CNG bundle is separately realized by the insertion rules in (35c-d); if it applies to \REF{ex:key:36}, then we get an uninflected form of \textit{dies}{}-, with the proviso that the structure in \REF{ex:key:36} can only occur in nominative/accusative contexts. To be clear, the possibility of projecting two different demonstrative structures accounts for the two different forms – the form of \textit{dies}{}- simply depends on which structure, \REF{ex:key:35} or \REF{ex:key:36}, is merged in Spec,ArtP of the syntactic representation. With this in mind, I return to the data in \REF{ex:key:31} from the beginning of this section.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Above, I proposed that anaphoric contexts involve null nouns. I extend this claim now to the two forms of the demonstrative. Specifically, I propose that the structure of the demonstrative for the nominal case in \REF{ex:key:31a} involves a DP that contains either \REF{ex:key:35a} or \REF{ex:key:36} along with a null noun (37a-b). As for the clausal instance in \REF{ex:key:31b}, I assume for concreteness that the two demonstrative structures combine with a propositional element, here illustrated with TP (37c-d):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%37
    \label{ex:key:37}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [\textsubscript{DP} \textit{dieses} [\textsubscript{NP} e\textsubscript{N} ]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [\textsubscript{DP} \textit{dies} [\textsubscript{NP} e\textsubscript{N} ]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  [\textsubscript{DP} \textit{dies} [\textsubscript{TP} e\textsubscript{T} ]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.   *  [\textsubscript{DP} \textit{dieses} [\textsubscript{TP} e\textsubscript{T} ]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with uninflected \textit{dies} in (37b-c), I propose that this element is not subject to any conditions as regards agreement. Given the absence of inflection, \textit{dies} does not have to undergo concord in agreement features. Consequently, it is fine in both nominal and clausal contexts. Note again that the restriction to nominative/accusative neuter environments follows from the restriction features on [+DEIX] in \REF{ex:key:36}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Turning to \textit{dieses} in (37a,d), observe now that -\textit{es} is a neuter inflection. Given the presence of a null noun in \REF{ex:key:37a}, the inflection on \textit{dieses} is licensed by concord in agreement features with this null noun (recall that this noun is part of the anaphor that is related to an antecedent in the neuter gender). Consequently, the demonstrative in \REF{ex:key:37a} can be part of a nominal anaphor. In order to explain the ungrammaticality of \textit{dieses} in the clausal context in \REF{ex:key:37d}, I suggest that the nominal features of the inflection cannot be licensed as there is no noun present. As such, \textit{dieses} is ungrammatical in this context and cannot serve as a propositional anaphor.\footnote{There are dialects that do not make this difference; that is, \textit{dieses} is also possible as a propositional anaphor (see, e.g., \citealt{Duden1995}: 336, \citealt{GriesbachSchulz1965}: 148). I assume that in these dialects, the licensing conditions of the neuter inflection are different; for instance, neuter gender could be licensed as a default option.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  More generally, I propose that the varying inflectional morphology and the different semantic restrictions in \REF{ex:key:31} result from the different demonstrative structures and the element following the demonstrative, either a null nominal (37a-b) or a null clausal element (37c-d). If so, I can maintain the claim that adjectival endings are semantically vacuous (Hypothesis 1a). Indeed, as this section has made clear again, adjectival inflections are licensed in certain contexts only. Furthermore, I propose that adjectival inflections serve to make nominal features such as case, number, and gender visible (Hypothesis 2b). This is particularly clear in contexts where overt nouns are absent. Before I continue the exploration of Hypothesis 2b, I briely need to return to the discussion of weak adjectives.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  It is important to comment on the inflection of the adjective in \REF{ex:key:34a} above, restating that data point below by separating it according to the inflected vs. the uninflected form of the demonstrative. Note again that the two forms of the demonstrative, \textit{dieses} and \textit{dies}, can only take a weak adjective:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%38
    \label{ex:key:38}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{dies-es schön-e    Kleid}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this-\textsc{st} pretty-\textsc{wk} dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this pretty dress’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{dies schön-e    Kleid}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this pretty-\textsc{wk} dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this pretty dress’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If \textit{dieses} and \textit{dies} are indeed two separate elements, then the weak inflection on the following adjective in \REF{ex:key:38b} is not a function of a preceding strong ending but rather of the preceding determiner stem. Note in this regard that both \textit{dieses} and \textit{dies} involve the feature [+DEF], a context where [+D] triggers Impoverishment. Another interesting point about Hypothesis 2b can be made more explicit here.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Recall that Impoverishment deletes [S] from the syntactic representation – the other features remain intact. Furthermore, as mentioned before, the vocabulary insertion rules are underspecified for some of the CNG features. This means that overt inflections only make a subset of the underlying features visible. In other words, Hypothesis 2b does not claim that adjectival inflections make all CNG features visible. Rather, it claims that adjectival inflections make features visible that are left in the syntactic representation after Impoverishment has occurred. In fact, adjectival inflections only make features visible that are specified in their vocabulary insertion rules.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, I documented in Chapter 2 that \textit{ein}{}-words can involve pronominal forms too. Exemplifying with \textit{ein}, this element is different from the cases discussed above in certain ways. Similar to the demonstrative, \textit{ein} can also occur in anaphoric contexts \REF{ex:key:39}. Unlike the demonstrative, \textit{ein} must have an ending in anaphoric contexts involving noun phrases:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%39
    \label{ex:key:39}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Zwei Männer standen auf der Straße. Ein-*(er) von ihnen kam  \textsubscript{} }\textit{zur     Tür}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  two  \textsubscript{} men       stand     in   the \textsubscript{} street   \textsubscript{} one-\textsc{st}     of    them came to.the door
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘Two men were standing in the street. One of them came to the door.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Assuming the presence of a null noun following \textit{einer} ‘one’, I argue that the obligatory inflection on \textit{ein} indicates that there can be only one type of \textit{ein}. As proposed in Chapter 2, \sectref{sec:key:2.2}, this element is a determiner. Second, like the inflection on \textit{dieses} and other elements, the ending on \textit{ein} in \REF{ex:key:39} makes nominal features visible. In the next chapter, I discuss \textit{ein} in more detail. We will see that there are actually two types of \textit{ein}. I hasten to add though that the other type of \textit{ein} is an adjective that follows definite determiners and is crucially not related to the article \textit{ein} in \REF{ex:key:39}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

6.   Conclusion
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this chapter, I turned to four consequences of the analysis laid out in Chapter 2. I discussed two influential proposals of spurious articles – Predicate Inversion and null nouns. I argued that the weak endings on adjectives in these structures indicate that the plural indefinite article in German is not entirely spurious. The two proposals just mentioned involve non-canonical structures. As such, the weak adjectives do not follow from the system developed in Chapter 2. Awaiting an account that can explain the weak adjectives in these non-canonical structures without losing the account of the canonical and other non-canonical instances, I assume that spurious articles actually involve regular structures. While the discussion of these structures is postponed to Chapter 8, I continue assuming that the current account of the strong/weak alternation is on the right track.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Next, I turned to some consequences for the discussion of discontinuous noun phrases. Illustrating some paradoxical properties, I showed that analyses postulating movement that involve one underlying noun phrase cannot account for the strong adjective in the split-off under current assumptions. In contrast, accounts involving the base-generation of two separate underlying nominals and subsequent movement are completely compatible with the analysis of Chapter 2. I also provided evidence that \textit{ein} (or its feature bundles) is unlikely to be inserted late. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Finally, I suggested that non-restrictive adjectives have a similar structural analysis as restrictive ones, the main difference being the presence of a null co-indexed pronoun in the former. In other words, inflectional endings do not signal (non-)restrictiveness of the interpretation of adjectives. In a similar vein, I argued that adjectival inflections are not tied to concepts like “referentiality” but make nominal features like case, number, and gender visible (Hypothesis 2b). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

More generally, I can maintain the claim that adjectival endings are a reflex of the structure (Hypothesis 1b) but not of the semantics (Hypothesis 1a). Furthermore, the discussion of Predicate Inversion has shown in more detail that the degree of the embedding of the adjective is important for the account of the strong/weak alternation (Hypothesis 2a). In the next chapter, I turn to the discussion of \textit{ein} in more detail. We will see that \textit{ein} shares some of the properties of adjectival inflections.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\setcounter{page}{201}\section{\textmd{Chapter 5:} \textmd{\textit{Ein}}\textmd{{}-words and Adjectival} \textmd{\textit{eine}}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.   Introduction
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The previous three chapters detailed the current account of the various properties of adjectival inflections. I now turn to \textit{ein}{}-words, which exhibit related properties. Note though that besides this general relatedness of the two types of elements, adjectival endings will continue to play an important part in the following discussion as they help narrow down the choices when proposing plausible structures. With that in mind, I turn to the main hypotheses about \textit{ein}, formulated in Chapter 1 and repeated here for convenience. Recall that \REF{ex:key:1b} can be fleshed out as in \REF{ex:key:2a}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%1
    \label{ex:key:1}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           \textit{Hypothesis 1}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Adjectival inflections and \textit{ein}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      a.     are expletive elements and
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      b.     indicate abstract structure in the noun phrase.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%2
    \label{ex:key:2}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Hypothesis 3}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{Ein}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   a.  indicates abstract structure in the lower layers of the noun phrase (NP vs. ArtP),       and
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.  it supports overt semantic operators (e.g., NEG \textit{k}{}-) and flags the presence of       covert semantic operators (e.g., REL).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As already stated in Chapter 1, the difference between supporting and flagging the operator by \textit{ein} is that the presence of the operator itself is in some way detectable in the first case but not in the second. The hypotheses in \REF{ex:key:1} and \REF{ex:key:2} are addressed in detail in this and the next two chapters. I start with some general considerations and some basic data involving \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumxxleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumxxlevelii}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Preliminaries and Basic Data
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumxxlevelii}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumxxleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As amply illustrated above, German noun phrases have determiners. For instance, depending on the context, a singular noun may occur with the appropriate form of the indefinite article. The latter is often reduced in speech \REF{ex:key:3a}. This reduction of the stem from \textit{ein-} to \textit{n-} is marked by parentheses or apostrophe below.\footnote{I distinguish different kinds of \textit{ein} below. Parentheses indicate the optionality of the vocalic part of the stem provided that the element is not stressed (which is the case with the indefinite article \textit{ein}, but not with the singularity numeral \textit{EIN} or adjectival \textit{eine}). In some cases, I use an apostrophe to clearly indicate the (reduced) indefinite article. In the latter cases, the unreduced form of the article would lead to a difference in meaning and/or grammaticality.} Alternatively, such a noun can surface with the definite article or its almost homophonous demonstrative counterpart \REF{ex:key:3b} (the ambiguous status of \textit{die} as the article ‘the’ or the demonstrative ‘that’ is indicated as DET(erminer) in the gloss):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%3
    \label{ex:key:3}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{(ei)ne Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     a       girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a girlfriend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{die  Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsc{det} girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the / that girlfriend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As is well known, indefinite and definite determiners cannot co-occur. This applies to both reduced and unreduced indefinite articles as well as to definite articles and demonstratives (for \REF{ex:key:5b} below, see \citealt{Pafel1994}: 251; I discuss the unreduced form of \textit{eine} in contexts like \REF{ex:key:5b} below):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%4
    \label{ex:key:4}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{(ei)ne (ei)ne Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     an      a        girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{(ei)ne die  Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     an     \textsc{det} girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%5
    \label{ex:key:5}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{die  die   Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsc{det} \textsc{det} girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  *  \textit{die ‘ne Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsc{det} a   girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The same distributional restrictions hold if \textit{die} is replaced by the proximal demonstrative \textit{diese} ‘this’.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Casting the empirical net wider, it is not surprising that the negation particle \textit{nicht} ‘not’ can precede determiners \REF{ex:key:6}. However, I point out that possessives like \textit{mein} ‘my’ and the negator \textit{kein} ‘no’ are in complementary distribution with the indefinite article, the definite article, and the related demonstrative; consider \REF{ex:key:7} and \REF{ex:key:8}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%6
    \label{ex:key:6}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{nicht ‘ne Freundin}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  not     a   girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘not a  girlfriend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{nicht die  Freundin}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  not    \textsc{det} girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘not the / that girlfriend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%7
    \label{ex:key:7}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{meine ‘ne Freundin}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  my       a   girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{meine die  Freundin}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  my     \textsc{det} girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%8
    \label{ex:key:8}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{keine ‘ne Freundin}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  no       a   girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  *  \textit{keine die  Freundin}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  no     \textsc{det} girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The same holds if the determiner precedes the possessive or negator in \REF{ex:key:7} and \REF{ex:key:8}. In view of this mutually exclusive distribution, I point out again that the possessive pronominal and the negator are labeled possessive article and negative article, respectively. These names foreshadow the analysis to be developed below.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Interestingly, the distribution is partially different when \textit{ein} is stressed and must occur in its unreduced form (stress is indicated here with capital letters). Similar to \REF{ex:key:6a}, the negation particle \textit{nicht} is compatible with \textit{eine} \REF{ex:key:9a}. Unlike \REF{ex:key:7a}, \textit{ein} can, when stressed and unreduced, co-occur with the possessive article \REF{ex:key:9b}. Importantly, this element is still impossible with the negative article or the indefinite article (9c-d) ((9c) is adopted from \citealt{Fanselow1988}: 111 fn. 29):\footnote{Note already here that the two instances of \textit{ein} in \REF{ex:key:9a} and \REF{ex:key:9b} are not the same: \REF{ex:key:9a} involves the singularity numeral \textit{EIN}, but \REF{ex:key:9b} contains adjectival \textit{eine}. One of the differences is that \REF{ex:key:9b} has an additional interpretation that involves a duality presupposition roughly translated as ‘one of my two girlfriends’. The differences including the various interpretations are discussed below.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%9
    \label{ex:key:9}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{nicht EINE Freundin}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  not    one     girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘not one girlfriend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.   \textit{meine EINE Freundin}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  my      one    girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘my one girlfriend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c. *  \textit{keine EINE Freundin}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  no      one    girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

d. *  \textit{(ei)ne EINE Freundin}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   an     one    girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It seems clear that the difference between the determiner elements in \REF{ex:key:9b} and (9c-d) relates to definiteness: the possessive in \REF{ex:key:9b} is definite, but the negator and the indefinite article in (9c-d) are not. Thus, besides stress and phonetic non-reducibility, definiteness seems to be a relevant factor in the distribution of \textit{ein}. In other words, any account that seeks to be on the right track needs to take into account the different stress and reduction patterns of \textit{ein} and the syntactic-semantic context this article occurs in.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  It is apparent from the previous literature (see \sectref{sec:key:7}) that there is relatively little discussion about the morpho-syntax and semantics of all the different kinds of \textit{ein}.\footnote{To anticipate the discussion in \sectref{sec:key:7}, it will become clear there that when we consider some of the previous accounts, we cannot help but notice that opinions diverge considerably with regard to the nature of this kind of indefinite element. Furthermore, none of these proposals discusses all the different kinds of \textit{ein}.}  As with adjectival inflections, only the canonical cases are usually discussed. In what follows, I seek to provide a more comprehensive overview of this type of element in German (for some brief cross-linguistic discussion, see again Chapter 1, \sectref{sec:key:2.1.3}). Similar to adjectival inflections, I propose that the discussion of the non-canonical cases reveals the true nature of \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{1.2.   Initial Taxonomy of} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I assume the following initial classification. This taxonomy contains three main types of \textit{ein} (\citealt{KarnowskiPafel2004}: 174-75) and some subtypes. Below, I propose in detail that certain elements are composite forms consisting of the article and another component:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%10
    \label{ex:key:10}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a. \textit{ein} as an article:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{}- indefinite article
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{}- vacuous article:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{}- \textit{ein} as part of a composite:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{}- possessive
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{}- negation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{}- \textit{ein} in predicate noun phrases\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{}- \textit{ein} as part of a complex determiner 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b. \textit{ein} as a numeral
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c. \textit{ein} as an adjective
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I refer to the types of \textit{ein} in \REF{ex:key:10a} and \REF{ex:key:10b} collectively as \textit{ein}{}-words; the type in \REF{ex:key:10c} is called adjectival \textit{eine}. In fact, reducing the numeral to the article, I propose below that there are just two types of \textit{ein}: the article and the adjective. Thus, the two corresponding designations, \textit{ein}{}-word and adjectival \textit{eine}, reflect the analysis to be developed below. Before I go into any specifics, consider first some illustrative examples of the above taxonomy. Note already here that while \textit{ein}{}-words have no ending in (11a-f), adjectival \textit{eine} has a weak ending in \REF{ex:key:11g}:\footnote{The German noun \textit{Brot} ‘bread’ is countable. This count reading is translated into English using the classifier \textit{loaf} (e.g., \textit{zwei Brote} ‘(two breads =) two loaves of bread’). Also, I often use the [-figurative] noun \textit{Lehrer} ‘teacher’ in predicative contexts. As a term for a profession, it takes an optional indefinite article in such contexts (Chapter 6). This optionality is crucial for formulating hypotheses about the true nature of \textit{ein}.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%11
    \label{ex:key:11}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  Indefinite Article
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ich habe immer  nur (ei)n frisch-es Brot            \textsubscript{} }\textit{mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I     have always only a     fresh-\textsc{st}  bread.\textsc{neut} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I have always brought only a fresh loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  Possessive
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ich habe immer  nur  mein frisch-es Brot  \textsubscript{} }\textit{mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I     have always only my   fresh-\textsc{st}  bread.\textsc{neut} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I have always brought only my fresh loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  Negation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ich habe kein frisch-es Brot  \textsubscript{} }\textit{mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I     have no   fresh-\textsc{st}  bread.\textsc{neut} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I have brought no fresh loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  Predicative
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Mein Vater  ist (ein) Lehrer}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

my     father is   a      teacher.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘My father is a teacher.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

e.  Complex Determiner
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{Ich habe ein jedes  frisch-es Brot            mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\subsubsection{\textmd{    I    have  an  every fresh-}\textmd{\textsc{st}}\textmd{  bread.}\textmd{\textsc{neut}}\textmd{ brought}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I have brought each fresh loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  f.  Numeral
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ich habe immer  nur  EIN frisch-es Brot  \textsubscript{} }\textit{mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I     have always only one fresh-\textsc{st}  bread.\textsc{neut} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I have always brought only one fresh loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  g.  Adjective
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ich habe nur  das ein-e     frisch-e   Brot             mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I    \textsubscript{} have only the one-\textsc{wk} fresh-\textsc{wk} bread.\textsc{neut} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I have only brought the one fresh loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These different kinds of contexts \textit{ein} appears in will be discussed at length in what follows. The discussion of the current and the next two chapters can be anticipated as follows.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.3.  Outlook
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

One goal of this chapter is to provide a more comprehensive survey of the different types of \textit{ein} with the intention of ultimately reducing them in number. I propose that there are two lexical types of \textit{ein}: the article and the adjective (cf. \citealt{Pafel2005}: 179). The second goal is to determine the properties of the different types of \textit{ein}. I argue that the article is semantically vacuous (Hypothesis 1a) but that adjectival \textit{eine} is not. In other words, the article is, in a number of ways, similar to adjectival inflections discussed in Chapter 2.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In more detail, I propose that the possessive articles, the negative article, and the singularity numeral are composite forms consisting of the indefinite article and another component. Providing some brief remarks on the possessive and negative composites, I dedicate most of the discussion to the article, the numeral, and adjectival \textit{eine}. Some distinctions between the last three elements are argued to follow from their different featural specifications and others from their different positions in the syntactic tree. Specifically, deriving the numeral from the combination of the indefinite article and another component, I account for the identical morphology but the different semantics of the numeral and article. Thus, I propose that the numeral is related to the article in a way that adjectival \textit{eine} is not – the latter is a separate lexical element. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As to the remaining types in \REF{ex:key:11} above, predicative \textit{ein} is illustrated below but discussed in greater detail in Chapter 6. I do not address the complex determiner \textit{ein jeder} ‘(an every =) each’ in much detail here (but see \citealt{Roehrs2012}, \citealt{Zimmermann2011}).\footnote{Note that \textit{ein} here occurs in the context of the semantically plural and definite element \textit{jeder} ‘every’. It is proposed in \citet{Roehrs2012} that \textit{ein} intensifies the distributivity of \textit{jeder}. I tentatively suggest in Chapter 8 that \textit{ein} may flag the presence of a null distributivity operator (cf. de Swart \textit{et al}. 2007: 218). This is consistent with the proposal that \textit{ein} is semantically vacuous, including in the string \textit{ein jeder}.} More generally, the current chapter discusses the morpho-syntax of \textit{ein} and its relation to indefiniteness, and the next two chapters focus on \textit{ein} in relation to semantic notions like emotiveness (Chapter 6) and number (Chapter 7).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The current chapter is organized as follows. In order to motivate the approach that some types of \textit{ein} should be treated in the same way, I first illustrate certain morphological similarities between the different kinds of \textit{ein}. In \sectref{sec:key:3}, some phonetic and semantic differences are pointed out. These differences are summarized in \tabref{tab:key:1} below. A bipartite proposal involving different feature specifications and different syntactic positions is discussed in Sections 4 and 5, respectively. \sectref{sec:key:6} provides some diachronic and cross-linguistic evidence that \textit{ein}{}-words are indeed composite forms in (Modern) German. In \sectref{sec:key:7}, I critique a previous proposal in some detail, and \sectref{sec:key:8} forms the conclusion.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.   Similarities
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In \sectref{sec:key:1.2}, I provided my taxonomy along with an illustration of the basic cases. With these reference points in mind, I discuss the occurrence of the different inflections on \textit{ein} in split topicalizations and with elided nouns. Note that these constructions require special contexts to be felicitous (see again Chapter 4, \sectref{sec:key:3}).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.   Split Topicalization
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Comparing \REF{ex:key:11} to \REF{ex:key:12}, split topicalizations with in-situ adjectives have the same morphology as non-split noun phrases; that is, \textit{ein}{}-words have no endings in (12a-e), but adjectival \textit{eine} has a weak inflection in \REF{ex:key:12f}:\footnote{To investigate the relevant aspects of the morpho-syntax of predicative \textit{ein} in split topicalizations, I chose a non-canonical construction involving the modal particle \textit{vielleicht} ‘really’. This element makes the split of the predicative construction more felicitous. Note also that the translations of the following examples into English are not always straightforward – they often are approximations of the German originals.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%12
    \label{ex:key:12}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  Indefinite Article
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Brot             habe ich immer  nur  (ei)n  frisch-es mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

bread.\textsc{neut} have I     always only  a     \textsubscript{} fresh-\textsc{st}  brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for bread, I have always brought only a fresh loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  Possessive
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Brot             habe ich immer nur   mein  frisch-es mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

bread.\textsc{neut} have I     always only my   \textsubscript{} fresh-\textsc{st}  brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for bread, I have always brought only my fresh loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  Negation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Brot            habe ich kein frisch-es mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

bread.\textsc{neut} have I     no   fresh-\textsc{st}  brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for bread, I have brought no fresh loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  Numeral
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Brot             habe ich immer  nur  EIN frisch-es mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

bread.\textsc{neut} have I     always only one fresh-\textsc{st}  brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for bread, I have always brought only one fresh loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

e.  Predicative
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Brot             ist das vielleicht (ei)n frisch-es!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

bread.\textsc{neut} is  that \textsc{prt}           a     fresh-\textsc{st}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for bread, this is really a fresh loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  f.  Adjective
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Brot             habe ich nur   das ein-e     frisch-e   mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

bread.\textsc{neut} have I     only the  one-\textsc{wk} fresh-\textsc{wk} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for bread, I have only brought the one fresh loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.   Split Topicalization with Fronted Adjective
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If the adjective is part of the split-off, the \textit{ein}{}-word exhibits a strong ending (13a-e). Interestingly, the stranded \textit{ein}{}-words have an optional schwa (see also \citealt{Roehrs2009a}: 156, \citealt{Sternefeld2008}: 152), where the option with schwa seems to be, at least to my ears, of elevated style. With a determiner preceding, there is no change for \textit{eine} in \REF{ex:key:13f}:\footnote{When \textit{ein} is stranded by itself \REF{ex:key:13a}, it is actually stressed. This makes the indefinite article ambiguous with the numeral in \REF{ex:key:13d}. Note that even if another stressed element such as \textit{so} ‘such’ is added, the unreduced form of \textit{ein} is still much better here:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumiileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Brot  \textsubscript{} }\textit{habe ich immer  nur  \textsubscript{} }\textit{so   \{ein(e)s / *?‘ns / *‘nes\} mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumiileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

bread.\textsc{neut} have I    \textsubscript{} always only such a                                   brought‘As for bread, I have always brought only this kind of loaf.’Notice that \textit{ein} in (i) is used pronominally and as such, it has some stress on it. This presumably explains the degradedness of the reduced forms. It is not clear to me though if this instance of \textit{ein} is the unreduced article or the numeral (which consists of the article and an additional component).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%13
    \label{ex:key:13}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  Indefinite Article
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{(Frisch-es) Brot            habe ich immer  nur  ein-(e)s mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 fresh-\textsc{st}    bread.\textsc{neut} have I     always only a-\textsc{st}      brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for (fresh) bread, I have always brought only a loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  Possessive
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{(Frisch-es) Brot             habe ich immer  nur  mein-(e)s mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 fresh-\textsc{st}     bread.\textsc{neut} have I     always only my\textsc{{}-st}     brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for (fresh) bread, I have always brought only my loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  Negation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{(Frisch-es) Brot             habe ich kein-(e)s mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 fresh-\textsc{st}     bread.\textsc{neut} have I     no-\textsc{st}      brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for (fresh) bread, I have brought no loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  Numeral
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{(Frisch-es) Brot             habe ich immer nur  EIN-(E)S mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 fresh-\textsc{st}     bread.\textsc{neut} have I    always only one-\textsc{st}     brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for (fresh) bread, I have always brought only one loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

e.  Predicative
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{indent}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     \textit{(Frisch-es) Brot             ist das vielleicht ein-(e)s!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{indent}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{indent}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     fresh-\textsc{st}     bread.\textsc{neut} is  that \textsc{prt}          one-\textsc{st}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{indent}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for (fresh) bread, this is really one [fresh] loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  f.  Adjective
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{(Frisch-es) Brot             habe ich nur  das ein-e     mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 fresh-\textsc{st}     bread.\textsc{neut} have I    only the  one-\textsc{wk} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for (fresh) bread, I have only brought the one loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Comparing the inflections on the adjectives in \sectref{sec:key:2.1} to the ones on the \textit{ein}{}-words here, we can observe again that they are the same. In keeping with Chapter 4, \sectref{sec:key:5}, I assume that these inflections make nominal features like case, number, and gender visible (Hypothesis 2b). Recall also that the emergence of the strong ending on the \textit{ein}{}-words is due to a certain vocabulary insertion rule (Chapter 2, \sectref{sec:key:2.2}). Parallel facts hold when the noun is elided.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.3.   Adjective Followed by Elided Noun
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

When adjectives are present, noun phrases with elided nouns have the same morphology as those with non-elided nouns (Chapter 2); that is, the \textit{ein}{}-words have no endings in (14a-e), but adjectival \textit{eine} has a weak inflection in \REF{ex:key:14f}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%14
    \label{ex:key:14}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  Indefinite Article
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ich habe immer  nur (ei)n frisch-es mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I     have always only a     fresh-\textsc{st}  brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I have always brought only a fresh loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  Possessive
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ich habe immer  nur  mein frisch-es mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I     have always only my   fresh-\textsc{st}  brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I have always brought only my fresh loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  Negation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ich habe kein frisch-es mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I     have no   \textsubscript{} fresh-\textsc{st}  brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I have brought no fresh loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  Numeral
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ich habe immer  nur  EIN frisch-es mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I     have always only one fresh-\textsc{st}  brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I have always brought only one fresh loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

e.  Predicative
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Das ist vielleicht (ei)n frisch-es!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

that is  \textsc{prt}           a     fresh-\textsc{st}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘This is really a fresh loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  f.  Adjective
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ich habe nur   das ein-e      frisch-e   mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I     have only the  one-\textsc{wk} fresh-\textsc{wk} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I have only brought the one fresh loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, consider topicalization where the split-off contains an adjective and an elided noun.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.4.   Split Topicalization with Fronted Adjective and Elided Noun
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If the noun phrases are split and the adjectives and elided nouns are part of the split-offs, then similar to \sectref{sec:key:2.2}, the \textit{ein}{}-words have strong endings (15a-e). Again, there is no change for \textit{eine} in \REF{ex:key:15f}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%15
    \label{ex:key:15}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  Indefinite Article
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Frisch-es habe ich immer  nur  ein-(e)s mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

fresh-\textsc{st}   have I     always only a-\textsc{st}     brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for a fresh one, I have always brought only a loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  Possessive
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Frisch-es habe ich immer  nur  mein-(e)s mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

fresh-\textsc{st}   have I     always only my-\textsc{st}     brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for a fresh one, I have always brought only my loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  Negation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Frisch-es habe ich kein-(e)s mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

fresh-\textsc{st}   have I     no-\textsc{st}      brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for a fresh one, I have brought no loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  Numeral
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Frisch-es habe ich immer  nur  EIN-(E)S mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

fresh-\textsc{st}   have I     always only one-\textsc{st}     brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for a fresh one, I have always brought only one loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

e.  Predicative
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Frisch-es ist das vielleicht ein-(e)s!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

fresh-\textsc{st}   is  that \textsc{prt}          one-\textsc{st}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for a fresh one, this is really one [fresh] loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  f.  Adjective
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Frisch-es habe ich nur  das ein-e     mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

fresh-\textsc{st}   have I     only the one-\textsc{wk} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘As for a fresh one, I have only brought the one loaf.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To summarize these sections, all \textit{ein}{}-words are marked by the emergence of strong endings when they are “stranded” by themselves in split topicalizations. Adjectival \textit{eine} is different – it remains unchanged. Similar facts of the \textit{ein}{}-words were observed when the latter occur in elliptical contexts; that is, when the adjective and the noun are not present at all (Chapter 2, \sectref{sec:key:2.2.2}). The emergence of the strong inflections on the \textit{ein}{}-words follow from the same vocabulary insertion rule stated in \sectref{sec:key:2.2.2}: it applies to \textit{ein}{}-words, independently of whether \textit{ein} is “stranded” by split topicalization or occurs in elliptical contexts – in each case, there is no overt material following \textit{ein}.\footnote{Recall from Chapter 4, \sectref{sec:key:3} that \textit{ein} in split topicalizations is not actutally stranded by the adjective and/or noun moving out of the source DP. Rather, the adjective and/or noun are part of a separate nominal, the split-off. As such, on my assumptions, both split topicalization and ellipsis involve null nouns in the nominal containing \textit{ein}.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.   Differences
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I focus on three phonetic and semantic differences: encliticization, stressability, and semantic singularity (see also \citealt{KarnowskiPafel2004}: 174-75, \citealt{Pafel2005}: 180). These differences are summarized in tabular form at the end of this section.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.1.   Encliticization
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As seen above, reduced forms of the indefinite article are independently possible. Thus, I take encliticization of \textit{ein} to be instantiated when the indefinite article undergoes further phonetic changes due to its preceding element, its host. For instance, coronal \textit{‘n} as the reduced form of \textit{ein} becomes \textit{’m} when it is encliticized onto a word ending in a labial sound (R. \citealt{Wiese1996a}: 166). For this purpose, I use, for the most part, verbal hosts with their endings apocopated (e.g., \textit{habe} > \textit{hab’} ‘have’). With this in mind, note that cliticization to a preceding word is only possible with an indefinite article \REF{ex:key:16a} and predicative \textit{ein} (16e) (encliticization is indicated in the gloss by a period; I do not use phonetic brackets to indicate the phonetic change):\footnote{In the predicative context, the copular \textit{sein} ‘to be’ was changed to \textit{bleiben} ‘to remain’. This was done to have a verbal stem ending in a non-coronal sound.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%16
    \label{ex:key:16}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  Indefinite Article
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ich hab’m (frisch-es) Brot            mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\subsubsection{\textmd{    I    have.a   fresh-}\textmd{\textsc{st}}\textmd{   bread.}\textmd{\textsc{neut}}\textmd{ brought}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I have brought a (fresh) loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.    Possessive
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{*  Ich hab’ m’m  (frisch-es) Brot             mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\subsubsection{\textmd{    I    have} \textmd{\textsc{poss}}\textmd{.a fresh-}\textmd{\textsc{st}}\textmd{   bread.}\textmd{\textsc{neut}}\textmd{ brought}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I have brought my (fresh) loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.    Negation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{*  Ich hab’ k’ŋ    (frisch-es) Brot            mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\subsubsection{\textmd{    I    have} \textmd{\textsubscript{} }\textmd{\textsc{neg}}\textmd{.a fresh-}\textmd{\textsc{st}}\textmd{   bread.}\textmd{\textsc{neut}}\textmd{ brought}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I have brought no (fresh) loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.    Numeral
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{*  Ich hab’M    (frisch-es) Brot            mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\subsubsection{\textmd{    I    have.one  fresh-}\textmd{\textsc{st}}\textmd{   bread.}\textmd{\textsc{neut}}\textmd{ brought}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I have brought one (fresh) loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  e.  Predicative
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ich bleib’m  (jung-er)   Lehrer}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I     remain.a \textsubscript{} young-\textsc{st} teacher.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I remain a (young) teacher.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

f.  Adjective
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       *  \textit{Ich habe gestern     nur  dem’m-en  (frisch-en) Brot  \textsubscript{} }\textit{zugesprochen}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I    have  yesterday only the.one-\textsc{wk} fresh-\textsc{wk}  bread.\textsc{neut} eaten
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Yesterday, I only ate the one (fresh) loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To be clear, encliticization of \textit{ein} is not possible when \textit{ein} is part of a composite (16b-c) or stressed (16d,f). I will not have much more to say here about the regularities that govern encliticization.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.   Stressability
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With regard to the possibility of bearing stress, some of the judgments from the previous section reverse. The types of \textit{ein} fall into three groups: first, the indefinite article may not be stressed \REF{ex:key:17a}; second, the possessive article, the negative article, and \textit{ein} in predicate noun phrases may be stressed (17b,c,e); and third, the numeral and adjectival \textit{eine} must be stressed (17d,f,f’):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%17
    \label{ex:key:17}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  Indefinite Article
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{*  Ich habe ‘N (frisch-es) Brot            mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     have  a   fresh-\textsc{st}   bread.\textsc{neut} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I have brought a (fresh) loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  Possessive
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ich habe MEIN (frisch-es) Brot            mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     have my     \textsubscript{}  fresh-\textsc{st}   bread.\textsc{neut} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I have brought my (fresh) loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  Negation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Ich habe KEIN \textsubscript{} }\textit{(frisch-es) Brot            mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     have no      \textsubscript{}  fresh-\textsc{st}   bread.\textsc{neut} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I have brought no (fresh) loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  Numeral
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{*  Ich habe ‘n\textsubscript{} }\textit{(frisch-es) Brot            mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     have  one fresh-\textsc{st}   bread.\textsc{neut} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I have brought one (fresh) loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  e.  Predicative
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Mein Vater ist EIN Lehrer}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\subparagraph{\textrm{\textmd{ \textup{my    father is} }}\textrm{\textmd{\textup{\textsubscript{} }}}\textrm{\textmd{\textup{one  teacher.}}}\textrm{\textmd{\textsc{masc}}}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘My father is one teacher.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

f.  Adjective
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       *  \textit{Ich habe nur   das ‘n-e       (frisch-e)  Brot            mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I    have  only the   one-\textsc{wk} fresh-\textsc{wk} bread.\textsc{neut} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I have brought only the one (fresh) loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

f’.  Adjective
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         \textit{Ich habe nur   das EIN-E  (frisch-e)  Brot            mitgebracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I    have  only the  one-\textsc{wk} fresh-\textsc{wk} bread.\textsc{neut} brought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I have brought only the one (fresh) loaf of bread.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Although stressed, I do not provide adjectival \textit{eine} in capital letters below in order to distinguish it better from the singularity numeral. More importantly, note that with the exception of the singularity numeral and adjectival \textit{eine} (which are both independently stressed), it seems clear that stress has a semantic effect. For instance, \REF{ex:key:17e} implies that my father is not just a teacher but one teacher among others (cf. Higginbotham’s 1987: 68 fn. 4 discussion of English).\footnote{In fact, stressed \textit{EIN} in \REF{ex:key:17e} might be a numeral in a predicative context. Note that with the exception of the singularity numeral and adjectival \textit{eine}, I do not discuss the effects of stress on the semantics.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.3.   Semantic Singularity
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I turn to some semantic differences. While the indefinite article usually implies singularity of the entity \REF{ex:key:18a}, the numeral emphasizes singularity as opposed to plurality \REF{ex:key:18b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%18
    \label{ex:key:18}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Ich habe (ei)n Mädchen geküßt}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     have  a      girl.\textsc{neut} kissed
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I have kissed a girl.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ich habe EIN Mädchen geküßt (nicht ZWEI)}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     have one girl.\textsc{neut} kissed   not    two
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I have kissed one girl (not two).’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Below, I argue that singularity in \REF{ex:key:18a} does not come from \textit{ein} itself – the latter is proposed to be a semantically vacuous element. Note again that this is consistent with the indefinite article occuring in plural contexts as illustrated in the previous chapters and discussed in more detail in \sectref{sec:key:4} below. In contrast, the singularity numeral has semantics, and it is proposed below that it consists of (vacuous) \textit{ein} and a contentful element.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  There are other cases where \textit{ein} is not associated with singularity (also \citealt{Schoorlemmer2009}: 197 fn. 17). It is clear that \textit{ein} as part of the possessive articles or the negative article has no relevance with regard to semantic singularity either. In fact, these two \textit{ein}{}-words can take head nouns with plural morphology (19a-b). Note also that a predicate noun phrase does not denote an entity but a property \REF{ex:key:19c}. As such, semantic singularity is not a relevant characteristic here either (see Chapter 7): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%19
    \label{ex:key:19}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Ich fahre m-eine Autos}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I    drive  \textsc{poss}{}-a cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I drive my cars.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ich fahre k-eine Autos}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I    drive  \textsc{neg}{}-a cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I drive no cars.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{BMW  ist (ei)n Auto}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    BMW is   \textsubscript{} a     car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘BMW is a car.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note in passing that the cases above also show that \textit{ein} cannot be related to indefiniteness.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, nominalized infinitives and generic noun phrases make no claim about singularity of the relevant event or entity either (the data are adopted from \citealt{Bisle-Müller1991}: 115, 151):\footnote{While a nominalized infinitive is not compatible with the singularity numeral \REF{ex:key:20b}, it is fine with adjectival \textit{einmalig} ‘one-time’ preceded by the indefinite article:   (i)  \textit{(Ei)n} \textit{einmaliges} \textit{Abweichen       vom        Kurs   \textsubscript{} }\textit{ist verzeihbar}.     a     \textsubscript{} one-time    departing.\textsc{neut} from.the course is  forgivable    ‘Departing from one’s course one time is forgivable.’Presumably, this has to do with the event structure of the nominal. Also, note that \REF{ex:key:20a}, \REF{ex:key:20c}, and (i) allow \textit{ein} to be replaced by a definite article (triggering a weak inflection on the following adjective in (i)).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%20
    \label{ex:key:20}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{(Ei)n Abweichen        vom        Kurs   \textsubscript{} }\textit{ist nicht gut}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      a     departing.\textsc{neut} from.the course is  not    good
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Departing from one’s course is not good.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. \textit{*  EIN Abweichen         vom        Kurs   \textsubscript{} }\textit{ist nicht gut}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one  departing.\textsc{neut} from.the course is  not    good
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Departing from one’s course is not good.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{(Ei)n Wal              ist ein Säugetier.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      a     whale.\textsc{masc} is  \textsubscript{} a    mammal
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘A whale is a mammal.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{*  EIN Wal               ist ein Säugetier.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one  whale.\textsc{masc} is  \textsubscript{} a    mammal
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘A whale is a mammal.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As to adjectival \textit{eine}, note again that like the numeral \textit{EIN} in \REF{ex:key:21a}, this type of \textit{ein} is stressed \REF{ex:key:21b} (recall though that I do not mark it as such). Crucially, unlike the numeral, adjectival \textit{eine} usually presupposes the existence of a second entity and thus implies a certain plurality of the members of the relevant kind. In fact, as observed by M. \citet[43]{Müller1986}, \textit{eine} in \REF{ex:key:21b} has a partitive sense, presupposing a set of typically two entities in the relevant world of discourse (cf. also \citealt{Vater1982}: 71). I indicated this in the English translation:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%21
    \label{ex:key:21}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{EIN Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one  man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one man’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{der eine Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the \textsubscript{} one  man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one of the two men’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that this duality presupposition cannot come from the definite article – the latter typically presupposes uniqueness in singular contexts. Importantly, \textit{eine} must be preceded by a definite element, and it is often contrasted with a second DP containing \textit{andere} ‘other’:\footnote{This construction already existed in OHG (ia). Furthermore, this distribution is also possible in related Yiddish (ib) (from \citealt{Reershemius1997}: 362):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumxvileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a.  \textit{ther eino – ther ander}    (OHG)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumxvileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

the  one      the  other‘one (of the two) – the other’  (Otfrid, \citealt{BrauneReiffenstein2004}: 234)b.  \textit{…tsvey brider …Der eyner hot \textsubscript{} }\textit{zikh  ungerufn Elon un  \textsubscript{} }\textit{der tsveyter Aladan}.    (Yiddish)   \textsubscript{} two   brothers the  \textsubscript{} one    has \textsc{refl} called      Elon and the second  \textsubscript{} Aladan‘…two brothers… One (of the two) was called Elon and the other Aladan.’}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%22
    \label{ex:key:22}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Der eine Mann         kam, der andere nicht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  the  one  man.\textsc{masc} came the other    not
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘One of the (two) men came, the other did not.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, like \textit{ein} in the possessive and negative composites, adjectival \textit{eine} can also be morphologically plural. In this case, adjectival \textit{eine} presupposes two sets of elements. Compare \REF{ex:key:22} to \REF{ex:key:23a}.\text{} In fact, as pointed out by a reviewer, it is possible to combine a set involving one member with a set involving multiple members \REF{ex:key:23b} (for Swedish in this regard, see \citealt{Börjars1998}: 18 fn. 7):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%23
    \label{ex:key:23}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{die      einen, die      anderen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the.\textsc{pl} one     the.\textsc{pl} other
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘these, those’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Der eine Sohn arbeitet und die drei  anderen machen Pause}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the  one  son   works    and the three others    take       break
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘One of the sons is working, and the three others are taking a break.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This means that the duality has to do with two sets that may differ from one another in size. Finally, we see in \sectref{sec:key:4.3.1} below that the duality presupposition associated with adjectival \textit{eine} can be cancelled.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The differences discussed above are summarized in \tabref{tab:key:1} (the properties are coded as follows: OK indicates an optional property; +/- signifies an inherent characteristic; N.A. means that this criterion is not applicable; I comment on the use of the parentheses in \tabref{tab:key:1} in the next paragraph):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Summary of the Differences between the Types of \textit{ein}}
\label{tab:key:1}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\multicolumn{3}{c}{Kinds of \textit{ein}} & Enclitic & Stress & Sem. singularity & Morphological plural\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Article & \multicolumn{2}{c}{Indefinite} & OK & {}- & (+) & (-)\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

& \multicolumn{1}{c}{Vacuous} & Possessive & {}- & OK & N.A. & OK\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

&  & Negative & {}- & OK & N.A. & OK\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

&  & Predicative & OK & OK & N.A. & (-)\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\multicolumn{3}{c}{Numeral} & {}- & + & + & {}-\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\multicolumn{3}{c}{Adjective} & {}- & + & OK (with canceled presupposition) & OK\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These are the most typical properties. Having set out the basic similarities and differences, I turn to accounting for them. In the course of the following discussion, I refine the statements about the indefinite article, especially with regard to semantic and morphological number (for predicative \textit{ein}, see Chapters 6 and 7). Specifically, I propose that the article \textit{ein} is semantically vacuous (Hypothesis 1a), and I illustrate again that it can occur in morphologically plural contexts. The upcoming refinement of the statements about number is indicated in \tabref{tab:key:1} by parentheses (that is, it is argued below that the indefinite article is not specified for number). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.   Step 1 of the Proposal: Morphology and Semantics
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall the generalization from \sectref{sec:key:1} that determiners may, independently of word order, not co-occur. Focusing on \textit{ein}{}-words, the definite article, and the demonstratives, this is illustrated again below (recall that \textit{die}, glossed below as DET, comprises the definite article \textit{die} and its related distal demonstrative \textit{DIE}): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%24
    \label{ex:key:24}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          *  \textit{keine / meine / ‘ne / die  / diese Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  no      / my     /  \textsubscript{} a  / \textsc{det} / this   girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There were basically two potential exceptions to this generalization: (i) definite determiners may occur with stressed \textit{eine} (25a-c), and (ii) \textit{diese} ‘this’ can occur with a possessive article \REF{ex:key:25d}. In \sectref{sec:key:4.3.1}, I discuss the different interpretations of the strings in (25a-b) in detail:\footnote{In poetic or elevated German, a possessive element can also be combined with a definite article (ia). Given the presence of the article and the weak inflection on the possessive element, this lower possessive is presumably an adjective. Note that this syntactic distribution is familiar from older varieties of German (ib) ((ib) is taken from \citealt{Harbert2007}: 155):  (i)  a.  \textit{Du  bist} \textit{d-er    mein-e.}      you are \textsubscript{} the-\textsc{st} my-\textsc{wk}       ‘You are mine.’b.  \textit{in dheru sineru heilegun chiburdi}   (OHG)in the     \textsubscript{} his      \textsubscript{} holy       \textsubscript{} birth‘in his holy birth’Given these points, I assume that (ia) is part of a different grammar.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%25
    \label{ex:key:25}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{meine eine Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my     one  girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one of the two of my girlfriends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘my one girlfriend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{die  eine Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsc{det} one  girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one of the two girlfriends’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘that one girlfriend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{diese eine Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this   one  girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this one girlfriend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{diese meine Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this   my      girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this girlfriend of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Furthermore, I showed in \sectref{sec:key:2} that the indefinite article (including predicative \textit{ein}), the possessive articles, the negative article, and the singularity numeral exhibit the same inflectional behavior. In other words, semantically quite diverse elements behave morphologically the same. In this and the next section, I account for these and some other facts. I provide a brief preview of the account of \textit{ein}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with \REF{ex:key:24}, I follow much discussion in the literature and assume that indefinite and definite articles are in D and that demonstratives are in Spec,DP (e.g., Alexiadou \textit{et al}. 2007: 105-20; \citealt{Bernstein1997}, 2001b; \citealt{Giusti1997}, 2002; \citealt{Leu2007}, 2015; \citealt{Roehrs2010}; van \citealt{Gelderen2007}; see also Chapter 1, \sectref{sec:key:4.1.2}). Furthermore, it is well documented for German that only one such element can be in the DP-level. If so, this restriction accounts for the non-co-occurrence of these three elements in the same DP.\footnote{Usually, the Doubly-filled DP Filter is brought into play here (for some discussion, see \citealt{Abney1987}: 271; \citealt{Giusti1997}: 109, 2002: 70). In \citet{Roehrs2019}, I propose that unlike the North Germanic languages, the West Germanic languages have only one (complex) feature bundle for definiteness explaining why there is only one element showing definiteness in the DP in the latter type of language. Note that this account makes no claim about the co-occurrence of a definite and a semantically vacuous element (see the discussion of \textit{m+ein} ‘my’ below).} Turning to the two remaining elements in \REF{ex:key:24}, that is, to the negative and the possessive articles, I develop a composite analysis of \textit{keine} and \textit{meine} (and other elements) below arguing that these elements consist of vacuous \textit{ein} and an abstract component denoting negation or possession. Among others, this proposal explains, on the one hand, the non-co-occurrence of the latter two elements with articles and demonstratives and, on the other hand, the identical morphology of the various composites and \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  If this is on the right track, then \textit{eine} in (25a-c) cannot be derived from vacuous \textit{ein} and must be a different element. I propose that this type of \textit{ein} is an adjective in a high specifier position.\footnote{That this type of \textit{ein} is indeed special is quite clear in Norwegian. As is well known, this language has a singularity numeral that has the neuter form \textit{ett} ‘one’ (ia). However, in the construction under discussion, the expected form \textit{ette} is impossible, and only a – what looks like – non-neuter form can be used (ib) (Marit Julien, p.c.):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumivleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a.  \textit{ett   stor-t  hus}        (Norwegian)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumivleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      one big-\textsc{st} house.\textsc{neut}      ‘one big house’    b.  \textit{det en-e       stor-e   hus-et}      the one-\textsc{wk} big-\textsc{wk} house.\textsc{neut}{}-\textsc{def}      ‘one of the two big houses’For Icelandic in this regard, see Pfaff (2015: 41 fn. 20).} If so, the distributions in (25a-c) do not present exceptions to the generalization about the non-co-occurrence of determiners. Turning to \REF{ex:key:25d}, this datum has greater potential of being an exception to this generalization. However, as already discussed in Chapter 2, \sectref{sec:key:4}, this type of \textit{diese} is not in Spec,DP. Rather, it is a predeterminer, a semantic intensifier, in LPP (also \sectref{sec:key:5.2.2}). Thus, there is only one element in the DP-level here as well. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These initial remarks can be summarized in the following simplified structure where all elements just discussed are put in their surface positions. Disregarding restrictions on the co-occurrence of these elements, this one structure illustrates the left part of the noun phrase in German. For completeness, I have added the semantic component of the singularity numeral \textit{EIN} ‘one’, illustrated below as \textit{Ø}\textsubscript{[-PL]} (see \sectref{sec:key:5.1}). Note that NEG is the abstract element later to be spelled out as \textit{nicht} ‘not’ or \textit{k(-ein)} ‘no’ (see \sectref{sec:key:4.1.2}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%26
    \label{ex:key:26}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

            LPP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   NEG    LPP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              \textit{diese}     DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      \textit{diese}      D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      \textit{DIE}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      \textit{m}{}-  D    CardP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 \textit{die}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              \textit{(ei)ne}    \textit{Ø}\textsubscript{[-PL]}    Card’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            Card    AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{eine}\textsubscript{ADJ}    Agr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                                                                                               Agr     …
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To anticipate the structural discussion below, I assume that only one element with the categorial feature [+D] can be merged in ArtP. Given that determiners move to the DP-level, this explains why only one determiner can surface in this layer: \textit{diese}, \textit{DIE}, \textit{die}, \textit{(ei)ne}. Note now that NEG, the predeterminer \textit{diese}, possessives, the singularity component \textit{Ø}\textsubscript{[-PL]}, and adjectival \textit{eine} are not merged in ArtP and can, at least in principle, co-occur with the determiners. Specifically, NEG is adjoined to LPP, the predeterminer \textit{diese} is base-generated in Spec,LPP, possessives move from Spec,NP to Spec,DP, and the singularity component is base-generated in Spec,CardP. Focusing on the different types of \textit{ein}, it will become clear in the next section that the negative article, the possessive articles, and the singularity numeral are restricted to the occurrence of \textit{ein} – these elements form composites. Finally, adjectival \textit{eine} is base-generated in the specifier position of a high AgrP and is restricted to definite contexts. These general ideas are fleshed out in what follows.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{4.1.  Composite Elements: Article} ein \textit{as a Supporting and Flagging Element}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I start by providing my basic proposal. This is followed by a detailed discussion of the derivations of the different \textit{ein}{}-words. Finally, I point out some advantages and initial consequences of this proposal.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.1.1. Basic Proposal
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with \textit{ein} itself, I provided a fair amount of empirical evidence above that this element is special. Among others, it can occur not only in singular but also plural contexts. Furthermore, \textit{ein} can surface not only in indefinite but also definite environments. This means that \textit{ein} is not a singular indefinite article. Rather, I propose that it is a semantically vacuous element (Hypothesis 1a). This is in agreement with some of the semantic literature. For instance, Heim \& \citet[62]{Kratzer1998} state that the indefinite article can be defined as follows: [[\textbf{a}]] = λf ${\in}$ D\textsubscript{<e,t>} . f, which maps every function in D\textsubscript{<e,t>} to itself. In other words, \textit{ein} makes no new semantic contribution. Similarly, Coppock \& \citet[400-01]{Beaver2015} assume that the indefinite article is a vacuous identity function on predicates: \textit{a(n)} = λ\textit{P.P} (where like in Heim \& Kratzer the input and the output are the same predicate).\footnote{The indefinite article is often related to its definite counterpart. For instance, \citet[177-81]{Giusti2015} argues that definite articles have nothing to do with definiteness or the iota operator. She proposes that definite (but also indefinite) articles are not determiners but high segments of an N-projection; in other words, they spell out inflectional features of a scattered, reprojected N-head (and in some cases those of an adjective). \citet[208]{Giusti2015} analyzes the definite article in German as \textit{d-er} ‘the’, where \textit{d}{}- is a dummy (see also \citealt{Roehrs2013a}) and -\textit{er} involves features of N. Extending this to the indefinite article, as Guisti briefly mentions, this is compatible with the current claim that \textit{ein} is semantically vacuous (note though that in \citealt{Giusti2015}: 208, the definite article and the indefinite article in German are in different positions – D vs. Spec,DP, respectively). Finally, note that the indefinite article is sometimes also assumed to be semantically vacuous in other languages. For instance, Matushansky \& \citet[245]{Spector2005} state that the indefinite article in post-copular position in French is semantically vacuous and a reflex of a syntactic operation.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Turning to the other \textit{ein}{}-words, it was suggested in Roehrs (2009a: chapter 4) that \textit{ein} is part of the negative article \textit{kein} ‘no’, possessive articles like \textit{mein} ‘my’, and the singularity numeral \textit{EIN} ‘one’ (27a-c). Below, this is instantiated by proposing that \textit{ein} supports certain operators that are detectable, segmentally as in the case of NEG \textit{k}{}- and POSS \textit{m}{}- or suprasegmentally as in the case of \textit{Ø}\textsubscript{[-PL]}, which induces stress.\footnote{I define operator broadly here. They involve scopal elements (negators, quantifiers), indexical elements (possessives), and other semantically active, functional elements (see REL below and certain other elements in subsequent chapters).} We will see in Chapter 6 that \textit{ein} also indicates the presence of the realization operator REL \REF{ex:key:27d}. Unlike (27a-c), here \textit{ein} flags the presence of an operator that has no independent manifestation. I indicate the co-occurrence relation of \textit{ein} and REL by an underscore. Completing the picture, I propose that unlike these four elements, adjectival \textit{eine} is an independent element \REF{ex:key:27e}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%27
    \label{ex:key:27}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  (vacuous) \textit{ein} + NEG →   \textit{kein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.   (vacuous) \textit{ein} + POSS\textsubscript{[1ST}\textsc{\textsubscript{} }\textsubscript{PERS -PL]}   →   \textit{mein} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.   (vacuous) \textit{ein} + \textit{Ø}\textsubscript{[{}-PL]}       →   \textit{EIN} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

d.  (vacuous) \textit{ein} \_ REL      →   \textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  e.  (non-composite) \textit{eine}\textsubscript{ADJ}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The claim that (27a-b) are composite forms is by no means new; for instance, \citet{FanselowCavar2002} and Kobele \& \citet[247]{Zimmermann2012} employ \REF{ex:key:27a}, and \citet[4]{Corver2003} and Corver \& van \citet[114]{Koppen2010} assume \REF{ex:key:27b} in their discussion of Dutch (cf. also \citealt{Julien2016}: 94-95 for certain possessives in Scandinavian). \citet[331]{Murphy2018} and Schoorlemmer (2009: 197 fn. 17) accept both \REF{ex:key:27a} and \REF{ex:key:27b} for German. The analysis in \REF{ex:key:27c} might be relatable to \citet{Ackles1996}, who proposes that English \textit{a(n)} licenses NumP, an element without overt realization, in the context of singular count nouns. \REF{ex:key:27d} is also in good company; for instance, Bennis \textit{et al}. (1998) claim that the operator [+EXCL] needs to be lexicalized by \textit{een} ‘a’ in Dutch (Chapter 4, \sectref{sec:key:2.1}). Finally, adjectival \textit{eine} is proposed by \citet[155]{Gallmann2004}, \citet[160-61]{Lindauer1995}, M. \citet[45]{Müller1986}, and \citet[179]{Pafel2005}. Note that these related works lend some initial credence to the current proposal (more references are provided in the course of the following discussion). Consider the derivation of the composite elements in (27a-c) in the framework of DM.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.1.2. Derivation of Composite Forms
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Beginning with \textit{ein} itself, I proposed in Chapter 2, \sectref{sec:key:2.1.6} that this element consists of the categorial feature [+D] and features for case, number, and gender (I leave the values unspecified below). Unlike the definite article and the demonstratives, \textit{ein} has no features for definiteness or deixis. As such, this is the least specified determiner. This fits well with the proposal above that \textit{ein} is a semantically vacuous element:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%28
    \label{ex:key:28}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Indefinite Article} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Art\textsubscript{[+D][F, N, O, S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  [+D]    [F, N, O, S]    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I turn to the operators in (27a-c). Starting with the possessive articles, recall from Chapter 2, \sectref{sec:key:2.2.3} that I analyze these elements as bound or free morphemes (29a-b).\footnote{A reviewer remarks that it is unusual that determiner stems of the same kind seem to fall into bound and free morphemes (but see also personal pronouns mentioned in Footnote Error: Reference source not found below). Note that bound in this case means a bipartite stem (e.g., \textit{m-ein}) and free signifies a monopartite stem (e.g., \textit{ihr}). While I flesh out this oft-suggested analysis below, there are alternatives. Note in this regard that unlike accusative/dative pronouns, all monopartite possessive articles consist of \textit{(e)r} at the end: \textit{ih-r} ‘her; their’ vs. \textit{ih-n/ih-m} ‘him’; \textit{uns-er} ‘our’ vs. \textit{uns} ‘us’; \textit{eu-er} ‘your’ vs. \textit{eu-ch} ‘you’. A different account than the one proposed below (where I suggest that the pronunciation of \textit{ein} is suppressed in the context of free/monopartite possessives) would be to claim that \textit{ein} is actually spelled out as -\textit{r} in the relevant possessive contexts – another case of contextually conditioned allomorphy. This would mean that all possessive articles involve bound/bipartite forms. I proceed on more traditional assumptions.} I point out now that these forms are more general (also \citealt{Fischer2006}). For instance, the bound morphemes in \REF{ex:key:29a} also occur as part of reflexive pronominal forms \REF{ex:key:29c}. In fact, this decomposition is quite pervasive with personal pronouns \REF{ex:key:29d} (I leave out the genitive forms, which are diachronically related to the possessive forms in \REF{ex:key:29a}):\footnote{Note that third-person \textit{s}{}- is somewhat special: it only involves masculine with possessives \REF{ex:key:29a} but masculine and/or other specifications with reflexives \REF{ex:key:29c} and personal pronouns \REF{ex:key:29d}. Notice also that \textit{S-ie} ‘you(\textsc{formal})’, not provided in \REF{ex:key:29}, is morphologically third but semantically second person (see Chapter 7). Furthermore, recall that the third-person pronoun \textit{sie} ‘she, her; they, them’ was claimed to take an adjectival inflection that is later deleted due to the avoidance of a hiatus (Chapter 3, \sectref{sec:key:5.3.2}). In other words, given current assumptions, this pronoun has the underlying form of \textit{s-ie-e}. Compared to third-person singular \textit{e-r} ‘he’, we find another case of bound/bipartite vs. free/monopartite determiner stems.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%29
    \label{ex:key:29}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{m}{}-,   \textit{d}{}-,               \textit{s}{}-
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my-, your(\textsc{sgl})-, his-
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{ihr},                                  \textit{unser}, \textit{euer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    her/their/your(\textsc{formal}), our,     your(\textsc{pl})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{m-ich},   \textit{d-ich},      \textit{s-ich} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

myself, yourself, himself/herself/themselves
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘myself, yourself, himself/herself/themselves’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{m-ich},    \textit{m-ir};       \textit{d-u},                   \textit{d-ich},               \textit{d-ir};                 \textit{s-ie}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

me.\textsc{acc,} me.\textsc{dat}; you.\textsc{nom}(\textsc{sgl}), you.\textsc{acc}(\textsc{sgl}), you.\textsc{dat}(\textsc{sgl}); she.\textsc{nom}/her.\textsc{acc}/ they.\textsc{nom}/them.\textsc{acc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘me; you; she/her/they/them’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There is another subregularity here in that all elements in \REF{ex:key:29b} involve feminine and plural forms, which tend to pattern together in the nominal paradigms (Chapter 2, \sectref{sec:key:2.1.5}). Taken together, this provides some empirical motivation for separating possessive articles into bound and free morphemes in \REF{ex:key:29a} and \REF{ex:key:29b}, respectively.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In \citet{Roehrs2020a}, I propose that possessives involve a Possessive Phrase (PossP) and that they are base-generated low in the nominal structure. Following standard assumptions, I assume that prenominal possessives move from Spec,NP to Spec,DP (Chapter 1, \sectref{sec:key:4.1.2}). After movement of the article and PossP, the relevant part of the structure, the DP-level, can be illustrated as follows:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%30
    \label{ex:key:30}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

              DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           PossP\textsubscript{k}    D’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           Poss  D           [… t\textsubscript{i} … t\textsubscript{k} …]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            Art\textsubscript{i}    D
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         [+D]         [F, N, O, S]    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

After Linearization of \REF{ex:key:30}, Vocabulary Insertion occurs. There are two types of cases involving possessive articles.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  First, after bound possessive morphemes like \textit{m}{}- in \REF{ex:key:29a} are inserted in the left periphery of the nominal, the string in \REF{ex:key:31} is obtained. In Chapter 2, \sectref{sec:key:2.2.2}, I formulated specific vocabulary insertion rules for \textit{ein}. If an overt element like an adjective and/or noun follows, then \textit{ein} in nominative masculine and nominative/accusative neuter contexts spells out both the categorial feature [+D] and the CNG feature bundle (cf. \REF{ex:key:30} above) yielding uninflected \textit{ein}; if no such element follows, then \textit{ein} spells out [+D], and an inflectional suffix spells out [F, N, O, S]. The latter scenario yields inflected forms of \textit{ein}. Both options are illustrated below by putting the inflection in parentheses:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%31
    \label{ex:key:31}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{m-  ein}    (-\textsc{infl})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I propose that \textit{ein} supports both the bound morpheme \textit{m}{}- and the inflection (unless \textit{ein} spells out both [+D] and the CNG feature bundle). Note now that all relevant elements are adjacent to each other. Given these local, linear relations, I suggest that the operation of support is instantiated by Local Dislocation such that \textit{ein} undergoes this operation onto \textit{m}{}- and that the inflection, if present, does so onto \textit{m-ein} (cf. \citealt{Murphy2018}). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Second, the free possessive morphemes in \REF{ex:key:29b} involve the same derivation as in \REF{ex:key:30} above. After Linearization and Vocabulary Insertion, the following string obtains exemplifying with \textit{ihr} ‘her’:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%32
    \label{ex:key:32}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{ihr  ein}    (-\textsc{infl})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This is not the correct surface string yet. Note that there are two free morphemes in \REF{ex:key:32}: \textit{ihr} and \textit{ein}. I suggest that only one free form can surface in this local domain. Given that the possessive has semantics but \textit{ein} does not, I assume that the latter does not appear:\footnote{In a traditional tree structure, the Doubly-filled DP Filter could be brought into play here. However, the framework adopted here (DM) inserts vocabulary items after Linearization. If the tree structure is no longer accessible after Linearization, then a different way needs to be found to rule out the co-occurrence of the free possessive element and \textit{ein}. Note in this regard that we cannot claim that \textit{ein} is only merged as a Last Resort option (NB: this is consistent with Chapter 4, \sectref{sec:key:3.4}, where I argued that \textit{ein} is unlikely to be inserted late); for instance, we cannot claim that \textit{ein} is inserted unless there is already another element present that can perform \textit{ein}’s two main functions (supporting and flagging). This is so because, if \textit{ein} were not inserted in the context of \REF{ex:key:32}, then the CNG bundle would remain untouched in the three (/four) exceptional cases. As a consequence, the inflection would be spelled out and would appear on (i.e., be supported by) the possessive in every instance yielding ungrammatical forms such as *\textit{ihr-er} in, say, a nominative masculine context. Given that, a more promising way to deal with this issue would be to delete \textit{ein} in the context of [Poss, pron, +F], which covers feminine and plural possessive pronouns. Alternatively, we could follow \citet[86-87]{Matushanksy2006} in suggesting that \textit{ein} and the possessive element undergo Fusion (into one element) in feminine and plural contexts.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%33
    \label{ex:key:33}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{ihr  ein}    (-\textsc{infl})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If an inflection is present (i.e., the CNG bundle of the article structure of \textit{ein} remains untouched by the vocabulary insertion rules of \textit{ein}), it will undergo Local Dislocation onto the possessive element. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As to the two remaining composite elements in (27b-c), I assume that \textit{kein} ‘no’ is similar to \textit{mein} ‘my’, the difference being that NEG is adjoined to the noun phrase in \REF{ex:key:30} and that it can be realized as either \textit{k}{}- or \textit{nicht}. The basic rule is provided below:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%34
    \label{ex:key:34}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          NEG   →   \textit{k}{}-   / \_\_ (unstressed) \textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    →   \textit{nicht}   (elsewhere)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In words, NEG is spelled out as \textit{k}{}- in the context of unstressed \textit{ein} but as \textit{nicht} in the remaining instances (but see also \sectref{sec:key:4.2.1} for some complications arising from the apparent optionality of \textit{kein} vs. \textit{nicht} in certain instances). As with the bound possessive morphemes, \textit{ein} undergoes Local Dislocation onto \textit{k}{}-. Finally, the singularity numeral \textit{Ø}\textsubscript{[-PL]} is also supported by \textit{ein} instantiated by Local Dislocation (for detailed discussion including a tree structure, see \sectref{sec:key:5.1}). To be clear, the elements on the right of the arrows in (27a-c) are taken to be the spell-out forms of the combination of the elements on the left of them. With regard to the workings of \textit{ein}, I continue speaking of support; that is, \textit{ein} supports other elements (instantiated by Local Dislocation).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.1.3. Some Advantages and Initial Consequences
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There are two immediate advantages of this proposal. With these composite elements made up of \textit{ein} and another component, it is easy to see how semantically quite diverse elements can share identical morphology. Furthermore, with the article \textit{ein} present, it follows that a second determiner cannot occur, and the complementary distribution with other determiners follows. Interestingly, noun phrases involving indefinite articles may have different interpretations with regard to specificity. As shown below, these interpretations are often claimed to be tied to the indefinite article being in different positions, for instance, D and Card. It could be suggested that vacuous \textit{ein} also supports D\textsubscript{[+}\textsc{\textsubscript{spec}}\textsubscript{]} and Card\textsubscript{[-}\textsc{\textsubscript{spec}}\textsubscript{]}. However, these are structural elements, and it is not likely that they are supported in the sense above. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In more detail, it is well known that indefinite articles are weak determiners \citep{Milsark1974}. Furthermore, indefinite noun phrases may have different readings (see, among many others, \citealt{FodorSag1982}, \citealt{Harbert2007}: 140, \citealt{Heusinger2011}, \citealt{Zamparelli2005}: 760; also \citealt{Diesing1992} and \citealt{Hallman2004}). For instance, they can have a non-specific \REF{ex:key:35a} or a presuppositional, specific \REF{ex:key:35b} interpretation:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%35
    \label{ex:key:35}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.   \textit{Ich würde nur (ei)n Auto        mit   lila      Punkten kaufen}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I    \textsubscript{} would only a     car.\textsc{neut} with purple dots       buy
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘I would only buy a car with purple dots.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.   \textit{Ich habe gestern   (ei)n Auto        mit   lila      Punkten gekauft}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I    \textsubscript{} have yesterday a     \textsubscript{} car.\textsc{neut}\textsubscript{} with purple dots       \textsubscript{} bought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I bought a car with purple dots yesterday.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Without going into much detail here, these readings are licensed by noun phrase-external factors, for instance, the different moods in the hosting clause (subjunctive vs. indicative). I basically follow \citet[105]{Jackendoff1977} and \citet{Bowers1988} in that weak determiners may be in different positions inside the noun phrase (licensed by these noun phrase-external factors). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To make the discussion concrete, I assume that there are two positions inside the DP relevant for the different readings: D, the head of DP, and Card, the head of CardP, which is located just below the DP (Chapter 1). In particular, on the weak, non-specific reading in \REF{ex:key:35a}, I assume that the indefinite article surfaces in Card, and on the strong, specific reading in \REF{ex:key:35b}, it appears in D (also Alexiadou \textit{et al}. 2007: 225, \citealt{Borer2005}: 144-45, \citealt{Chomsky1995}: 342, \citealt{Zamparelli2000}: 264-65). As proposed in Chapter 1, \textit{ein} originates in ArtP. With \textit{ein} lacking a feature for definiteness, it does not have to undergo movement to D. Thus, I propose that \textit{ein} moves to Card if the nominal has a weak reading and to D if the nominal has a strong reading. As such, I assume that vacuous \textit{ein} in Card\textsubscript{[-}\textsc{\textsubscript{spec}}\textsubscript{]} or in D\textsubscript{[+}\textsc{\textsubscript{spec}}\textsubscript{]} does not involve composite forms – the feature bundles later to be spelled out as \textit{ein} simply move to these positions. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, as regards \REF{ex:key:27d}, I claim in Chapter 6 that vacuous \textit{ein} (or a definite determiner) flags the presence of the realization operator REL. Recall that I suggested above that the negative, possessive, and singularity components of the \textit{ein}{}-words also involve operators. Given that, I reiterate the proposal that there are two ways to make operators visible by \textit{ein}: supporting where the operator itself is in some way detectable, as claimed for (27a-c), and flagging where the operator itself remains invisible as in \REF{ex:key:27d}. If this is tenable, then the term indefinite article seems inappropriate, but I continue with the traditional terminology. As to \REF{ex:key:27e}, adjectival \textit{eine} is not a composite form but a separate lexical item (for more details, see \sectref{sec:key:5.2}).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.2.  Some Evidence for the Composite Analyses
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the previous section, I provided the derivations that bring about the three composite forms. What they all have in common is that the two relevant elements are adjacent to one another. I consider this in more detail now providing some evidence for the composite analyses. I start with the most straightforward case.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.2.1. The Negative Article \textit{kein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is a fairly standard proposal that \textit{kein} ‘no’ consists of NEG + \textit{ein} (e.g., \citealt{Bech1955}/57, \citealt{Jacobs1980}, \citealt{Kratzer1995}: 144-47). I provide two morpho-syntactic arguments for the composite analysis. First, considering the data below, it seems clear that the contraction of NEG and \textit{ein} is obligatory in certain cases \REF{ex:key:36}, optional in others \REF{ex:key:37}, but cannot occur in yet other contexts \REF{ex:key:38}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%36
    \label{ex:key:36}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{Ich habe nicht (ei)n  Buch          gekauft}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     have not     \textsubscript{} a      book.\textsc{neut} bought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I did not buy a book.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ich habe kein Buch           gekauft}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     have no    book.\textsc{neut} bought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I bought no book.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%37
    \label{ex:key:37}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Ich habe nicht (ei)n BUCH        gekauft, sondern (ei)n HEFT}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     have not     \textsubscript{} a     book.\textsc{neut} bought  \textsubscript{} but          a     \textsubscript{} booklet.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I did not buy a book but a booklet.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ich habe kein BUCH        gekauft, sondern (ei)n HEFT}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     have no    book.\textsc{neut} bought  \textsubscript{} but          a     \textsubscript{} booklet.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I did not buy a book but a booklet.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%38
    \label{ex:key:38}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{(Ei)n Buch          habe ich nicht gekauft}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     \textsubscript{} a     \textsubscript{} book.\textsc{neut} have I     not    bought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I bought no book/I did not buy a book.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  *  \textit{(Ei)n Buch          habe ich kein gekauft}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     \textsubscript{} a     \textsubscript{} book.\textsc{neut} have I     no    bought
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I bought no book/I did not buy a book.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Specifically, with ordinary stress, NEG and \textit{(ei)n} must form the negative article \REF{ex:key:36}. This is different with contrastive stress on the noun where NEG and \textit{ein} can optionally be spelled out separately as \textit{nicht ein} or as the composite form \textit{kein} \REF{ex:key:37}. Finally, when NEG and \textit{(ei)n} are not adjacent \REF{ex:key:38}, both elements are spelled out separately, with NEG being realized as \textit{nicht}.\footnote{Note again that \textit{ein} and \textit{kein} can co-occur in split topicalizations:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumvleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{(Ei)n Buch          habe ich kein-s    gekauft}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumvleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     \textsubscript{} a     \textsubscript{} book.\textsc{neut} have I     none-\textsc{st} bought‘As for books, I bought none.’Considering the strong ending on \textit{kein} in (i), I assume that the negative article is followed by a null noun (see Chapter 4, \sectref{sec:key:3.3}). In other words, there are two separately base-generated nominals in (i), each with its own \textit{ein}. As such, the grammaticality of this example is not an argument against \textit{kein} requiring adjacency of NEG and \textit{(ei)n}, which is illustrated in \REF{ex:key:38}.} Now, the fact that NEG can be realized in two different ways depending on the stress pattern and the adjacency with \textit{ein}, supports the claim that \textit{kein} is a composite form.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Consider a second piece of evidence for this composite analysis. The examples in \REF{ex:key:39a} and \REF{ex:key:39b} establish that negation is higher than the type particle \textit{so} ‘such’ and that the latter can intervene between negation and the indefinite article.\footnote{Note that \textit{nicht} ‘not’ can form a constituent with the nominal (with stress on \textit{so} ‘such’):  (i)  \textit{Nicht so    ein Mann kam, sondern ein anderer}.    not    such a    man   came but         a   different    ‘Not such a man came, but a different one.’} However, with an intervening \textit{so}, negation is not adjacent to \textit{ein}, and consequently, these two elements cannot be spelled out as \textit{kein} by the morphology as evidenced by \REF{ex:key:39c} and \REF{ex:key:39d}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%39
    \label{ex:key:39}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{nicht so    (ei)n Idiot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    not    such an    idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘not such an idiot’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. *  \textit{so nicht (ei)n Idiot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    so not     an   idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. ?*  \textit{kein so     Idiot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    no    such idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d. *   \textit{so kein Idiot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    so no    idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This provides a second argument that \textit{kein} ‘no’ is a composite form subject to the adjacency of its two components. For other syntactic and semantic arguments for a composite analysis, see \citet[186-87]{Pafel2005}, von Fintel \& \citet[467-68]{Iatridou2007}, and \citet{Zeijlstra2011} (for English, see \citealt{Klima1964}: 273-76); for the discussion of \textit{kein so’n Idiot} ‘(NEG+a so an =) no such idiot’, see Chapter 8, \sectref{sec:key:2.2.2}.\footnote{There are different proposals to implement the claim that \textit{kein} ‘no’ is a composite form. For instance, like the current analysis, Zeijlstra (2011: 119, 131) assumes that \textit{kein} is a complex element. Unlike the current analysis, he argues that \textit{kein} is a complex head consisting of a top node that dominates a negation operator and an existential operator. These different implementations have other consequences, something I will not discuss here.} If \textit{kein} involves a composite analysis, then it is not implausible to propose this for the other complex \textit{ein}{}-words as well.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.2.2. Possessive Articles such as \textit{mein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There is also evidence that possessive articles are composite forms. While Saxon Genitives can both precede and follow their head nouns (40a-b) (e.g., \citealt{Duden2007}: 366, \citealt{EisenbergSmith2002}: 125, Fuß 2011), possessive articles can only precede their head nouns (40c-d):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%40
    \label{ex:key:40}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Magdalenas  Buch          ist schön}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Magdalena’s book.\textsc{neut} is  beautiful
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Mary’s book is beautiful.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.   \textit{Das Buch          Magdalenas  ist schön}.  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the  book.\textsc{neut} Magdalena’s is  beautiful
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘The book of Mary’s is beautiful.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Mein Buch          ist schön}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my    book.\textsc{neut} is  beautiful
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘My book is beautiful.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  *  \textit{Das Buch          mein(s) ist schön}.  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the  book.\textsc{neut} mine      is  beautiful
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘The book of mine is beautiful.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These facts follow from the assumption that possessive articles consist of a possessive component and \textit{ein}. Specifically, \textit{ein} can, as an article, only precede its head noun explaining the restriction in \REF{ex:key:40d}. There is other evidence for a composite analysis. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is well known that the possessive component of the possessive article agrees in gender with its antecedent but that the remaining part of the possessive article agrees in case, number, and gender with the head noun \REF{ex:key:41a}. Furthermore, besides gender, the possessive component also agrees in person and number with its antecedent, as can easily be seen in \REF{ex:key:41b}. These agreement relations are illustrated with different indices below:\footnote{Georgi \& \citet[2077]{Salzmann2011} observe the data points in (i), where a neuter possessor can occur with both a neuter and a feminine possessive article:  (i)  a.  \textit{Das Mädchen hat seine / ihre Schuhe verloren}.      the  girl.\textsc{neut} has its     / her   shoes   lost      ‘The girl has lost her shoes.’b.  \textit{dem       Mädchen seine / ihre Schuhe}      the.\textsc{dat} girl.\textsc{neut} its     / her  shoes      ‘the girl’s shoes’They argue that the agreement relation between the possessor and the possessive article involves an anaphoric binding relation.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%41
    \label{ex:key:41}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Peter}\textsubscript{i} \textit{hat s}\textsubscript{i}\textit{{}-einer}\textsubscript{k} \textit{Freundin}\textsubscript{k} \textit{geholfen}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter  has \textsc{poss}{}-a   girlfriend.\textsc{fem} helped
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Peter helped his girlfriend.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ich}\textsubscript{i} \textit{habe m}\textsubscript{i}\textit{{}-einen}\textsubscript{k} \textit{Freunden}\textsubscript{k} \textit{geholfen}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     \textsubscript{} have \textsc{poss}{}-a     friends      \textsubscript{} helped
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I helped my friends.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These different agreement relations accessing the same word are straightforwardly explained by the composite analysis.\footnote{Olsen (1989b: 139, 1991b: 53) also assumes decomposition of possessive articles, but it is different from the current analysis. Exemplifying with \textit{meinen} ‘my’, \textit{mein} is in Spec,DP, and -\textit{en} is in D. There are three issues here. First, Olsen also locates the possessive marker -\textit{s} in D. Note that D has the same features when the inflection -\textit{en} or the marker -\textit{s} is located in D (\citealt{Olsen1991b}: 48, 53). This raises the question of what rules out the possessive marker surfacing on the possessive pronoun (cf. *\textit{meins neues Auto} ‘my-\textsc{poss} new-\textsc{st} car’ vs. \textit{Peters neues Auto} ‘Peter’s new-\textsc{st} car’). Second and more importantly, the lack of decomposition of the stem into \textit{m}{}- and \textit{ein} loses the generalization that all \textit{ein}{}-words behave the same morpho-syntactically. Third, we pointed out above that \textit{ein} and adjectival inflections have a status different from the possessive component: the first two elements are semantically vacuous, and they make linguistic items visible: \textit{ein} supports or flags operators and adjectival inflections provide overt exponents for abstract features. As such, \textit{ein} should be grouped with the inflection in D as in the current account (and not with the possessive component in Spec,DP as in Olsen’s work). Recall that here \textit{ein} involves a complex head consisting of [+D] and a CNG feature bundle.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.2.3. The Singularity Numeral \textit{EIN} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I discuss the advantages of a composite analysis of \textit{EIN} ‘one’ in detail in \sectref{sec:key:5}. Note already here that \textit{Ø}\textsubscript{[-PL]} is located in Spec,CardP in the weak reading of this indefinite element and that the supporting element \textit{ein} is in Card (recall that \textit{ein} lacks a definiteness feature and as such, it does not have to move to the DP-level). Something similar holds for the strong reading where \textit{Ø}\textsubscript{[-PL]} has moved to Spec,DP, and \textit{ein} is in D. Crucially, these are all Spec-head constellations, where adjacency holds inherently. Consequently, \textit{ein} can support \textit{Ø}\textsubscript{[-PL]}, and both elements are spelled out by the morphology as the numeral (note also that \textit{ein} supporting \textit{Ø}\textsubscript{[-PL]} licenses this null element at the same time). When \textit{Ø}\textsubscript{[-PL]} is not present, the vacuous article is spelled out as \textit{ein} under Card or D as discussed in \sectref{sec:key:4.1.3}.  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up, I take two points as established: first, the negative article \textit{kein} ‘no’, possessive articles such as \textit{mein} ‘my’, and the singularity numeral \textit{EIN} ‘one’ are all composites; second, these elements involve post-syntactic spell-out forms. As we have seen and will see throughout this chapter, this analysis has a number of advantages allowing for a fairly simple account of the different kinds of \textit{ein}. Before I turn to some syntactic differences, I consider some of the semantic and morphological distinctions of these elements in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.3.  Feature Specifications 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

First, I document that the presupposition induced by adjectival \textit{eine} can be cancelled under certain conditions. This lays the foundation for the second subsection, where I provide the feature specifications of the different types of \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.3.1. Cancelling the Presupposition of Adjectival \textit{eine}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that adjectival \textit{eine} has to occur in definite contexts and that it involves a duality presupposition. This presupposition seems to be strongest with the definite article \REF{ex:key:42a}, it is possible with the possessive article \REF{ex:key:42b}, but it is absent with the demonstrative \textit{diese} ‘this’ \REF{ex:key:42c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%42
    \label{ex:key:42}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{die  ein-e    Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the one-\textsc{wk} girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one of the two girlfriends’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{meine ein-e     Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my     one-\textsc{wk} girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one of the two of my girlfriends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘my one girlfriend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{diese ein-e     Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this   one-\textsc{wk} girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this one girlfriend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall also that the duality presupposition cannot stem from the singular definite article. On the one hand, this determiner carries a uniqueness presupposition; on the other hand, other definite elements like possessive articles can also license adjectival \textit{eine}. This means that the duality cannot come from the definite article itself. Rather, I pointed out above that in conjunction with a definite element, the presence of \textit{eine} brings about partitive semantics in that it presupposes the existence of two sets of elements where the sets may involve one or several members. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Besides the definite and the possessive articles, there are other definite elements that occur with adjectival \textit{eine}. Specifically, the periphrastic possessive in \REF{ex:key:43a} and the Saxon Genitive in \REF{ex:key:43b} can each involve a duality presupposition (for \REF{ex:key:43b}, see Gunkel \textit{et al}. 2017: 1300):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%43
    \label{ex:key:43}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Peter sein ein-er  Sohn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter his   one-\textsc{st} son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one of the two of Peter’s sons’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Peter’s one son’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Peters  ein-er  Sohn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter’s one-\textsc{st} son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one of the two of Peter’s sons’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Peter’s one son’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Before moving on, note already here that the weak inflection on \textit{eine} in \REF{ex:key:42} and the strong inflection on \textit{eine} in \REF{ex:key:43} indicate that \textit{eine} is indeed adjectival (see \sectref{sec:key:5.2} for detailed morpho-syntactic discussion). Returning to the semantics, the question arises why the duality presupposition is absent with demonstratives.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  That the duality presupposition is missing with demonstratives is quite clear in \REF{ex:key:44a}. In this case, there is no presupposition that there is a second boy with the relevant property. As might be expected, if the definite article is stressed and functions as the distal demonstrative, there is no presupposition either \REF{ex:key:44b}.\footnote{Note that \textit{eine} in \REF{ex:key:44b} has fairly strong stress – \textit{eine} is inherently stressed and, additionally, it appears in phrase-final position. As a possible consequence of that, adjacent \textit{die} is not (strongly) stressed.}  In a similar vein, \REF{ex:key:44c} consists of a singular subject and a predicate nominal modified by a relative clause. Indeed, there is no presupposition that there is a second man with the property denoted by the relative clause. Furthermore, Orrin Robinson (p.c.) raises the question as to whether the duality presupposition can disappear when one nominal containing adjectival \textit{eine} is coordinated with another involving a numeral \REF{ex:key:44d}. This is indeed the case ((44b) is taken from M. \citealt{Müller1986}: 45):\footnote{In Footnote Error: Reference source not found, we saw that \textit{ein} with duality presupposition could also follow a definite determiner in OHG. Apparently, this presupposition could also be cancelled with demonstratives (ia) and in contexts involving a unique entity (ib) in this older variety of German:  (i)  a.  \textit{thiz eina jār}    (OHG)      this one  year.\textsc{neut}      ‘this year’        (Tatian, \citealt{BrauneReiffenstein2004}: 234)    b.  \textit{der eino almahtico cot}      the one  almighty   god.\textsc{masc}      ‘the almighty God’        (Wessobrunner Gebet 7, \citealt{Schrodt2004}: 21)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%44
    \label{ex:key:44}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Dieser eine Junge        hat viele  Wunder  vollbracht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this      one  boy.\textsc{masc} has many miracles accomplished
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘This one boy has performed many miracles.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{alle Frauen wollen plötzlich   nur   das (/dies(es)) eine: Zärtlichkeit}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    all   women want    suddently only that / this         one    tenderness.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Suddenly, all women only want one thing: tenderness.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Du  bist der eine Mann,        der mich geliebt hat}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are the  one  man.\textsc{masc} that me    loved   has
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are the one man that has loved me.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{Meine eine Freundin          und seine zwei sind ausgegangen.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my      one girl-friend.\textsc{fem} and his     two  are   gone out
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘My one girl-friend and his two went out.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To be clear, then, although \textit{eine} has adjectival morphology in each case and is presumably the same element, it may lack the presupposition property under certain conditions (also M. \citealt{Müller1986}: 45). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I tentatively suggest for (44a-b) that demonstrative determiners, with their strong deictic force, cancel the presupposition of adjectival \textit{eine}. Furthermore, note that \textit{eine} does not seem to be (strongly) stressed in \REF{ex:key:44a}, where in contrast to \REF{ex:key:44b}, the head noun is present. This presumably is also a reflex of the presence of word stress on the preceding demonstrative. Possessives as in \REF{ex:key:42b} and \REF{ex:key:43} have been related to demonstratives in that possessives are also deictic elements. For instance, \textit{meine} ‘my’ can be compared to \textit{diese} ‘this’ such that both are associated with the speaker (see \citealt{Lyons1999}: 18-19, \citealt{Roehrs2019}: 378). This presumably explains why the duality presupposition can be absent with possessives (it is less clear to me though why this presupposition can sometimes be present with possessives).\footnote{The possible presence of a duality presupposition with possessives might be related to the fact that possessives do not presuppose singleton sets (\citealt{CoppockBeaver2015}).}  As to \REF{ex:key:44c}, I assume that the duality presupposition of adjectival \textit{eine} is also cancelled when the nominal containing \textit{eine} forms the predicate of a singular subject. Finally turning to \REF{ex:key:44d}, I assume that the coordination of the two nominals leads to a list-type effect where the different numbers of the people involved are contrasted. This resultant contrast also allows a singularity reading of adjective \textit{eine}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up, I proposed that \textit{eine} is an adjective that can only occur in definite contexts. I assume that this distributional restriction has to do with \textit{eine}’s own (contribution to the) duality presupposition such that presuppositions in general often arise only in definite contexts. With these remarks in mind, I turn to the feature specifications.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.3.2. Feature Specifications of the Different Types of \textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As documented in Chapter 1, \sectref{sec:key:2.2}, \textit{ein} as an article can occur not only in singular but also in plural contexts \REF{ex:key:45}. Note that in each of the two cases below, there is no duality presupposition. This is expected given that these are indefinite contexts:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleCommentText}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%45
    \label{ex:key:45}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \%  "\emph{Was   für eine Idioten}" \textit{sagte Liam}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleCommentText}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleCommentText}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      what for  a     idiots      said  Liam
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleCommentText}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleCommentText}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘“Such idiots!” said Liam.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleCommentText}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \%  \emph{Das Personal war sehr unfreundlich. Ich hatte noch nie     so eine Ferien.}~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the  staff         was very unfriendly     I     had    still  never so a      holidays  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘The staff was very unfriendly. I have never had such a (bad) vacation.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Again, it is clear that \textit{ein} can appear in plural contexts. Given the proposal that this element is a semantically vacuous element, I assume that \textit{ein} can be plural as regards its morphology.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In contrast, adjectival \textit{eine} has semantics regarding number as just discussed. Thus, I make a distinction between morphological and semantic number (also Chapter 7), morph vs. sem for short. Consequently, I assume two types of plural features [PL] in the feature makeup where \textit{α} may range over a negative or positive value:\footnote{With Chapter 2 in mind, plural actually consists of the features [+F, +N]. The difference between plural and singular is that in the plural, both α and β in [αF, βN] are valued positively but in the singular, α and/or β is valued negatively (the latter was captured by a category variable: [-γ]). For simplicity, I use the feature [±PL] moving forward.}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%46
    \label{ex:key:46}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          Number:  [αPL morph; αPL sem]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Starting with the feature specification of the indefinite article, I proposed that \textit{ein} is a semantically vacuous element. To review briefly, this element can occur in singular and plural contexts as well as in indefinite and definite environments. As such, this element cannot have a specification for semantic number or definiteness.\footnote{A reviewer points out that \textit{ein} occurring in plural contexts is not a strong argument that \textit{ein} is not associated with singularity, given that in some languages the plural seems to operate on the singular. As discussed in S. Grimm (2012: chapter 2), there are two cases as regards nouns: count nouns can be pluralized as in English, and collective nouns can be singularized and then pluralized as in Welsh (note also that plural can be subtractive in Murle, see \citealt{HaspelmathSims2010}: 37, or in Hessian German). Schematically, these two cases could be represented as follows: Stem+PL and Stem+SGL+PL. It could be claimed then that -\textit{e} in plural \textit{ein} spells out the plural part and that \textit{ein} realizes the Stem (and singular).  There are certain points that seem to militate against this. First, the observation above was made on the basis of nouns. While singular \textit{ein} has traditionally been related to the feature [COUNT], it has not been associated with the feature [COLLECTIVE] (definite articles in German seem to be neutral in that they can occur in both of these singular contexts). Second, note that the plural form of the indefinite article is not morphologically more complex than its singular counterpart. In fact, both exhibit the same complexity and can even be identical (e.g., in the nominative feminine and plural: \textit{eine}). Third, if one or both of the two schematic structures above, Stem+PL and Stem+SGL+PL, is or are generally available, then we need to find a way to rule out spell-out forms such as *\textit{de-e} or *\textit{de-r-e} in the plural (nominative masculine) and *\textit{da-e} or *\textit{da-s-e} in the plural (nominative neuter). As far as I can see, these schematic structures do not capture the fact that in German, genders in the singular are not relevant in the plural. Finally, it remains unclear why plural \textit{ein} cannot occur in all plural contexts in Standard German.} Furthermore, given the composite analysis of the possessive and negative articles, which can each occur with a mass noun (e.g., \textit{meine / keine Milch} ‘my / no milk), \textit{ein} does not bring about countability either.\footnote{In Chapter 7, I discuss in more detail that countability in the current analysis is due to a side-effect, which is related to number agreement mitigated by the head of NumP.} Thus, this element has no feature for countability. However, we have seen that \textit{ein} morphologically agrees with its singular or plural head noun. I conclude again that this element must have morpho-syntactic features. Above, I proposed that \textit{ein} involves the feature [+D] and a feature bundle for case, number, and gender: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%47
    \label{ex:key:47}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Indefinite Overt Article}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Art\textsubscript{[+D][F, N, O, S]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  [+D]    [F, N, O, S]  →  \textit{ein-}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The CNG features in \REF{ex:key:47} are valued/checked during the derivation. Abstracting away from case and gender, morphological number has to do with [F, N], here more simply restated as [αPL morph]. Thus, besides [+D], [αPL morph] is part of the feature specification of \textit{ein}, specifically, the feature bundle CNG.\footnote{Note that the usual restriction of \textit{ein} to singular count contexts follows from a – what I call – restriction feature on the categorial feature yielding [+D: -PL] (cf. also uninflected \textit{dies} ‘this’ in Chapter 4, \sectref{sec:key:5}). This morpho-syntactic restriction feature can be deleted by the operators POSS, NEG, and certain others that are licensed in emotive/affective contexts (cf. the exclamative operators [+EXCL] and !!! dicussed in Chapter 4, \sectref{sec:key:2}). This deletion will widen the distribution of \textit{ein} allowing it to appear in certain non-singular contexts (for detailed discussion, see Chapter 8, Sections 2.2.5 and 2.2.6).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Turning to the singularity numeral \textit{EIN}, I proposed above that this element is a composite form consisting of vacuous \textit{ein} and the numeral component \textit{Ø}\textsubscript{[-PL]}. I suggested that \textit{Ø}\textsubscript{[-PL]} is the contentful part, a type of operator, and I assume that it is this component that is responsible for singularity and stress (cf. \citealt{Barbiers2005}: 168). I propose that \textit{Ø}\textsubscript{[-PL]} is inherently specified for [-PL] semantically. Given that \textit{Ø}\textsubscript{[-PL]} and vacuous \textit{ein} form a composite, I take it that morphological number comes from \textit{ein} as specified above. Now, since the semantic number and the morphological number of \textit{EIN} have to match, I assume that there is a condition such that semantically singular \textit{Ø}\textsubscript{[-PL]} is only compatible with morphological singular contexts. The number specification for \textit{Ø}\textsubscript{[-PL]} can be stated as [-PL sem, where αPL morph = αPL sem]. I discuss the syntactic structure of \textit{EIN} in \sectref{sec:key:5.1.2}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Adjectival \textit{eine} is different. Like \textit{ein}, I suggest that \textit{eine}\textsubscript{ADJ} has an unvalued/unchecked feature for morphological number. Unlike \textit{ein}, it is an adjective, and it has a specification for semantic number as regards the cardinality of sets (n) presupposed. This can be stated as follows: [αPL morph; n = 2 sem, where n is the number of presupposed sets of entities]. Morphological number is encoded in the CNG feature bundle, and the duality presupposition is located with the adjectival stem \textit{ein}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%48
    \label{ex:key:48}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Adjectival} eine
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 InflP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleJBExample}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          [F, N, O, S]     \textsubscript{} AP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            {\textbar}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          \textsubscript{} A
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   \textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Abstracting away from the categorial features (and case and gender), I summarize the feature specifications of the three types of \textit{ein}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%49
    \label{ex:key:49}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein}:   [αPL morph]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ø}\textsubscript{[-PL]}:  [-PL sem, where αPL morph = αPL sem]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{eine}\textsubscript{ADJ}: [αPL morph; n = 2 sem unless in the context of a demonstrative, etc.]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To be clear, given the complex internal structure of determiners and adjectives (i.e., stem + inflection), the semantic specifications in \REF{ex:key:49} are assumed to be on the stems of the relevant elements, but the morphological ones are encoded in the inflections as unvalued/unchecked feature bundles. With this in mind, I briefly address the statement in \REF{ex:key:49b} again.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There is an interesting interaction between \REF{ex:key:49a} and \REF{ex:key:49b}. As proposed in more detail below, the numeral \textit{EIN} derives from the combination of \REF{ex:key:49a} and \REF{ex:key:49b}, where the stem of \textit{ein} adds the categorial feature [+D], the inflection on \textit{ein} supplies unvalued/unchecked morphological features for case, number, and gender, and \textit{Ø}\textsubscript{[-PL]} imposes the restriction that only singular number will be compatible with other elements in the larger noun phrase. This means that in order to bring about a good derivation, the number features of all those elements must be valued/checked for singular by the more general operation that brings about concord in agreement features. If those number features are valued/checked for plural, incompability with the number specification of \textit{Ø}\textsubscript{[-PL]} arises.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Finally, I suggested above that the wider linguistic context (e.g., different moods in the hosting clause) licenses the different readings of indefinites with regard to specificity. I proposed that this involves different positions of \textit{ein} inside the DP. If these claims are on the right track, then we can maintain the claim that \textit{ein} is semantically vacuous (Hypothesis 1a).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.   Step 2 of the Proposal: Syntax
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the first subsection, I propose in more syntactic detail that synchronically, the numeral \textit{EIN} is, in certain respects, based on the indefinite article even though diachronically, the article derived from the numeral. With the different specifications from above in mind, the remaining differences between \textit{EIN} and \textit{ein} are argued to follow from the two different positions that \textit{Ø}\textsubscript{[-PL]} and \textit{ein} occupy in the syntactic tree. In the second subsection, adjectival \textit{eine} is shown to be independent of the indefinite article and numeral, and occupies a third position.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.1.   Article vs. Numeral
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this subsection, I first provide two pieces of evidence that the numeral \textit{EIN}, specifically the contentful component of it, is in a different position than the indefinite article. Then I proceed to derive certain aspects of the numeral from the indefinite article.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.1.1. Uniform Position(s) of all Numerals
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall from Chapter 1 that numerals are in a position different from determiners or determiner-like elements. Straightforward evidence for this comes from the following cases: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%50
    \label{ex:key:50}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{die  zwei Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsc{det} two  friends 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the / those two friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Peters  zwei Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter’s two  friends 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Peter’s two friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I assumed that numerals are in Spec,CardP. Following previous work though, I also suggested that if there is no determiner, then numerals may be in Spec,DP yielding a specific interpretation. The picture is slightly more complicated with \textit{ein}{}-words, specifically the numeral \textit{EIN} and the article \textit{ein}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the last section, I discussed the different interpretations of nominals involving \textit{ein} with regard to specificity. In particular, I proposed that when the interpretation is non-specific, \textit{ein} is in Card, but when it is specific, \textit{ein} is in D. In other words, \textit{ein} in \REF{ex:key:51a} may be in two different positions. Furthermore, I proposed that \textit{EIN} consists of the singularity part \textit{Ø}\textsubscript{[-PL]} and vacuous \textit{ein}. As regards the different specificity interpretations, I pointed out that when the interpretation is non-specific, \textit{Ø}\textsubscript{[-PL]} is in Spec,CardP and supporting \textit{ein} is in Card, but when it is specific, \textit{Ø}\textsubscript{[-PL]} is in Spec,DP and \textit{ein} is in D. In other words, just like other numerals, \textit{Ø}\textsubscript{[-PL]} may be located in two phrasal positions in \REF{ex:key:51b}:\footnote{Despite certain differences between the singularity numeral and the other numerals, \citet[171]{Barbiers2005} also assumes the same position for all numerals (see also his footnote Error: Reference source not found). For typological discussion of numerals, see \citet{Corbett2000} and \citet{Hurford2003}.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%51
    \label{ex:key:51}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{(ei)n Freund}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     \textsubscript{} a     friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘a friend’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{EIN Freund}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one  friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one friend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Regarding \textit{ein}{}-words then, the relevant elements are in two positions involving phrases (Spec,CardP and Spec,DP for \textit{Ø}\textsubscript{[-PL]}) and in two positions involving heads (Card and D for \textit{ein}). There are two issues that need to be addressed now. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

First, if the article and the numeral (or a part of it) are in different positions, then we need to explain why the two cannot co-occur (52a-b). This is presumably not due to semantic reasons as other (adjectival) elements emphasizing singularity are possible (52c-d). In fact, under the right (emotive) conditions, \textit{ein} can occur with a non-singularity numeral \REF{ex:key:52e}, repeating an example from Chapter 1, \sectref{sec:key:2.2} here (for the latter distribution in Dutch, see Bennis \textit{et al.} 1998: 112):\footnote{As is expected from the above discussion, adjectival \textit{eine} is also ungrammatical following \textit{ein} (ia) as the former is only licensed in a definite context (ib):  (i)    a.  *  \textit{ein einer Bruder}      an  one   \textsubscript{} brother.\textsc{masc}    b.  \textit{Peters  einer Bruder}      Peter’s one   \textsubscript{} brother.\textsc{masc}      ‘Peter’s one brother’      ‘one of the two of Peter’s brothers’Furthermore, an indication that adjectival \textit{eine} is different from \textit{einziger} ‘only’ can be seen in Negative Polarity contexts. Note first that neither a definite nor an indefinite article can license \textit{je} ‘ever’ (iia). However, adding \textit{einziger}, but not \textit{eine}, after the definite article results in a completely fine example (iib): \begin{listWWviiiNumvleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a.  *  \textit{Du  bist \{der / ein\} Mann,       der mich je     geliebt hat}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumvleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      you are   the  / a     man.\textsc{masc} that me   ever loved   has    b.    \textit{Du  bist der \{einzige / ??eine\} Mann,         der mich je     geliebt hat}.      you are  the   only     /     one    man.\textsc{masc} that me   ever loved   has      ‘You are the only man that has ever loved me.’Finally, another indication that \textit{eine} is different from \textit{einziger} ‘only’ is that both elements can cooccur (iic) ((iic) is from Zifonun \textit{et al.} 1997: 1931; to my ears, this example sounds slightly marked without a pause between \textit{eine} and \textit{einzige}):  (iii)  \textit{der eine einzige Gegensatz}    the one  unique  contrast.\textsc{masc}    ‘the only contrast’}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%52
    \label{ex:key:52}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{(ei)n EIN Freund}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     \textsubscript{} a     one  friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{EIN (ei)n Freund}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one   a     friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.   \textit{(ei)n einziger Freund}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     \textsubscript{} a     sole        friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘a sole friend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.   \textit{(ei)n einzelner  Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     \textsubscript{} an   individual man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘a individual man’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  e.  \%  \textit{ach man wir sind schon so     ne zwei wärmflaschen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    oh  boy   we are   \textsc{prt}     such a   two  warm.bottles
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Oh boy, we are such two hot-water bottles.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The ungrammaticality of (52a-b) follows from the assumptions above. Recall that \textit{ein} originates in Art and undergoes movement to Card or D. Proposing that \textit{Ø}\textsubscript{[-PL]} and \textit{ein} are spelled out together as \textit{EIN} under adjacency explains why an indefinite article cannot co-occur with the singularity numeral but only with other (non-composite) numerals \REF{ex:key:52e}. I return to the discussion of \REF{ex:key:52e} at the end of this section. Adjectives like \textit{einziger} ‘sole’ \REF{ex:key:52c} and \textit{einzelner} ‘individual’ \REF{ex:key:52d} can occur with \textit{ein} as they are elements morpho-syntactically independent of \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Turning to the second issue, I documented above that the indefinite article and the singularity numeral have the same morphology. What is interesting to note is that under certain conditions, the German numerals for ‘two’ and ‘three’ can take endings in the genitive. In this instance, these two numerals and the following adjective have identical inflections \REF{ex:key:53a}. This is in stark contrast to \textit{EIN}, which does not have the same ending as the following adjective \REF{ex:key:53b}: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%53
    \label{ex:key:53}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{das Auto zwei-er       nett-er  Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the car    two-\textsc{st.gen} nice-\textsc{st} friends 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the car of two nice friends’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{das Auto EIN-ES       nett-en   Freundes}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the car    one-\textsc{st.gen} nice-\textsc{wk} friend.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the car of one nice friend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Assuming that all numerals are in the same positions (Spec,CardP or Spec,DP), we need to explain why the numerals for ‘two’ and ‘three’ have a different morphological impact on the following adjective than \textit{EIN}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Historically, the indefinite article derives from the numeral for ‘one’. Importantly, the inflectional properties of the singularity numeral have changed over time. In particular, OHG \textit{éin} behaves like Modern German \textit{zwei} ‘two’ and \textit{drei} ‘three’ but crucially not like Modern German \textit{EIN}: \textit{éin} takes a strong adjective like \textit{zwei} and \textit{drei}, but \textit{EIN} takes a weak one. Compare the dative strings in \REF{ex:key:54a} and \REF{ex:key:54b} (the (a)-example is taken from \citealt{Demske2001}: 76):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%54
    \label{ex:key:54}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{mít   éin-emo rôt-emo tûoche}      (OHG)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  with one-\textsc{st}   red-\textsc{st}   scarf.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with one red scarf’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{mit   EIN-EM rot-en  Tuch}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  with one-\textsc{st}   red-\textsc{wk} scarf.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with one red scarf’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is clear, then, that the singularity numeral in these two varieties of German is, in certain ways, different and should not receive the same account. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that diachronically, the indefinite article derives from the numeral for ‘one’. On a somewhat speculative note, we could suggest that in the development from OHG to Modern German, \textit{éin} split into two parts: (vacuous) \textit{ein} + \textit{Ø}\textsubscript{[-PL]}. Now, once these two separate elements are available, the indefinite article can occur with \textit{Ø}\textsubscript{[-PL]}\textit{\textsubscript{} }(yielding the singularity numeral), but it can also start occurring by itself. In other words, the null part \textit{Ø}\textsubscript{[-PL]} can also be left out resulting in the emergence of the indefinite article. If we make this assumption, then it is not implausible to suggest that synchronically, Modern German \textit{EIN} is based on the indefinite article, at least in certain respects. This, in turn, provides an explanation of why Modern German \textit{EIN} has the morphological properties of a determiner – it consists in part of vacuous \textit{ein}. The latter element may trigger Impoverishment, and this accounts for the weak adjective in \REF{ex:key:53b} and \REF{ex:key:54b}. \textit{EIN} has the semantic properties of a numeral because it involves \textit{Ø}\textsubscript{[-PL]}. In \sectref{sec:key:6}, I return to the diachronic split of \textit{éin} into two components suggesting that the same occurred with possessive and negative articles – the other two complex \textit{ein}{}-words.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

More generally, these morpho-syntactic and semantic differences indicate different positions of the two instances of \textit{ein}. Morpho-syntactically, adjectives following \textit{EIN} behave differently from those following other numerals. Assuming a composite analysis of \textit{EIN}, it is \textit{ein}, but not \textit{Ø}\textsubscript{[-PL]} or other numeral, that trigger Impoverishment. Note in this regard that \textit{ein} undergoes head movement from Art up the tree. Crucially though, it cannot move into Spec,CardP, which is occupied by \textit{Ø}\textsubscript{[-PL]} (see also next section). Semantically, \textit{EIN} involves singularity unlike \textit{ein}. If we assume that numerals are in Spec,CardP and specify the cardinality of a nominal, then that implies that \textit{ein} is in a different position. To be clear, like other articles, \textit{ein} is in a head position; like other numerals, \textit{Ø}\textsubscript{[-PL]} is in a phrasal position.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.1.2. Different Scope of \textit{mehr als}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Above, I proposed that articles are in head positions but that numerals are in specifier positions. The second piece of evidence that the article and the numeral are in different positions derives from scopal facts. To begin, \textit{mehr als} ‘more than’ can take scope over the entire noun phrase where the nuclear stress is on the noun \REF{ex:key:55}. In more detail, \textit{mehr als} in (55a-b) implies that the relevant person is more than just a student (perhaps he is also the speaker’s friend), and \textit{mehr als} in \REF{ex:key:55c} implies that not only exactly the one hundred students came but perhaps other students showed up or even other people that are not students:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%55
    \label{ex:key:55}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Er ist} [ \textit{mehr als \textsubscript{} }\textit{(ei)n Student} ].
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is   \textsubscript{} more than a      student.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘He is more than a student.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Er ist} [ \textit{mehr als  \textsubscript{} }\textit{mein Student} ].
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is   \textsubscript{} more than my    student.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘He is more than my student.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Es     kamen} [ \textit{mehr als   die hundert        Studenten} ].
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    there came    \textsubscript{} more than the one.hundred students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘More than the one hundred students came.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These scopal effects can be derived as follows.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Illustrating with \REF{ex:key:55b}, the possessive element has undergone movement to Spec,DP, and the article has moved to D. Consider the relevant portion of the syntactic tree in \REF{ex:key:56}. Since Spec,DP is occupied, the scopal element must be outside the DP proper. For concreteness, I assume that it is adjoined to the DP:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%56
    \label{ex:key:56}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

                     DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      \textit{mehr als}               DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           PossP    D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             \textit{m}{}-\textsubscript{k}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      D            ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              \textit{ein}\textsubscript{i}    [ t\textsubscript{i} t\textsubscript{k} \textit{Student} ]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To be clear, \textit{mehr als} c-commands the entire DP. It takes scope over the possessive and the head noun allowing the various interpretations.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In contrast, \textit{mehr als} may also take scope over numerals only \REF{ex:key:57}. For the following sentences to be true, it must hold for \REF{ex:key:57a} that at least two students came and for \REF{ex:key:57b} that at least one hundred and one did:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%57
    \label{ex:key:57}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Es     kam} [ \textit{mehr als  EIN} ] \textit{Student}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    there came more than one   student.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘More than one student came.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Es     kamen die} [ \textit{mehr als   HUNDERT} ] \textit{Studenten}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    there came   the   more than one.hundred  students.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘The more than one hundred students came.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Returning briefly to the first set of data from this section, note that \textit{mehr als} in \REF{ex:key:55} occurs in the left periphery of the DP: it immediately precedes \textit{ein}{}-words as well as the definite article. In contrast, \textit{mehr als} in \REF{ex:key:57} immediately precedes the numerals – this is particularly clear in \REF{ex:key:57b}, where \textit{mehr als} follows the definite article. In order to derive the different scopal readings in \REF{ex:key:55} vs. \REF{ex:key:57}, I propose that \textit{mehr als} is in a different position when it precedes the numerals in \REF{ex:key:57}. This, in turn, will provide an argument that the two instances of \textit{ein} (article vs. numeral) are in different positions. Consider this in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To repeat, the scopal element \textit{mehr als} immediately precedes the numerals in \REF{ex:key:57}. Given the readings above, it only affects the interpretation of the numerals. In order to find a plausible structure, I follow Svenonius’ (1993: 445-46) argumentation for adjectives in that the modifier and its modifiee must be embedded inside a specifier position. This prevents the modifier from taking scope over the entire noun phrase. If we assume that the same holds for modifiers and their numeral modifiees, then numerals are also in specifier positions in this case (rather than in a head positions in the extended projection of the noun), and their modifiers (including scopal elements) are part of the same specifiers. Again, I assume that this position is Spec,CardP (or after movement Spec,DP). I continue focusing on the singularity numeral.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With this in mind, recall that the numeral \textit{EIN} consists of vacuous \textit{ein} and contentful \textit{Ø}\textsubscript{[-PL]}. The article \textit{ein} undergoes head movement from Art to Card \REF{ex:key:58}. As to \textit{Ø}\textsubscript{[-PL]}, this component is responsible for singularity and stress (but has no independent segmental phonetic realization). I propose that \textit{Ø}\textsubscript{[-PL]} and the scopal element \textit{mehr als} are both part of a specifier, below illustrated with the specifier of CardP. Embedded in a specifier, \textit{mehr als} cannot take scope over the entire noun phrase:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%58
    \label{ex:key:58}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

            DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

D        CardP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       QP             Card’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textit{mehr als}    Q      Card          ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                \textit{Ø}\textsubscript{[-PL]}\textit{\textsubscript{} } \textit{ein}\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                              Art’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                  Art     NumP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                  \textit{ein}\textsubscript{i}    \textit{Student}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Some further remarks are in order. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note again that the indefinite article has a feature for [+D], but not for definiteness. Consequently, \textit{ein} does not have to move to the DP-level but can surface in Card. Indeed, on the current analysis, \textit{Ø}\textsubscript{[-PL]}\textit{\textsubscript{} }and \textit{ein} are separate elements in different positions: given standard assumptions about movement, \textit{ein} as a head moves to Card, but it cannot move into a specifier position, presumably to combine with \textit{Ø}\textsubscript{[-PL]}. However, there is another way to form a composite of these two elements. After Linearization, Vocabulary Insertion, and Copy Reduction, the relevant portion of \REF{ex:key:58} above can be illustrated as in \REF{ex:key:59}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%59
    \label{ex:key:59}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{Ø}\textsubscript{[-PL]} \textit{ein}    (-\textsc{infl})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Null elements have to be licensed in some way (\citealt{Kester1996a}, 1996b; \citealt{Lobeck1995}; \citealt{Rizzi1986}; but also \citealt{Murphy2018}). This also applies to \textit{Ø}\textsubscript{[-PL]}. I proposed above that \textit{ein} licenses \textit{Ø}\textsubscript{[-PL]}\textit{\textsubscript{} }by supporting it – both elements are adjacent to each other in \REF{ex:key:59}. As with the possessive and negative articles, the operation of support can be instantiated by Local Dislocation (\sectref{sec:key:4.1.2}). This derives, in certain respects, the numeral from the indefinite article or, put differently, the numeral is based in part on the indefinite article.\footnote{Several authors (e.g., \citealt{Bernstein1993}: 128, \citealt{Julien2002}: 274) share the intuition that the indefinite article is merged lower and then raises to the DP-level. In contrast to the text proposal, these authors basically derive the indefinite article from the numeral. As far as I can see, there are a number of issues with this type of account, at least for Modern German (for some issues in English, see \citealt{Perlmutter1970}: 239 fn. 10, 244 fn. 13): for instance, it is unclear to me how to account for the different morphological properties of \textit{ein} and \textit{EIN} vs. non-singularity, non-composite numerals as regards the inflections on the following adjectives (cf. \REF{ex:key:53} above) – if \textit{EIN} were a non-composite numeral, then the adjectives following \textit{EIN} and the (other) non-composite numerals should pattern the same in German, contrary to fact. Furthermore, deriving \textit{ein} from the singularity numeal does not explain why \textit{ein} can co-occur with other numerals under certain conditions (assuming that only one numeral may occupy Spec,CardP).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I summarize the discussion of scope. Unlike in \REF{ex:key:56}, in \REF{ex:key:58} \textit{mehr als} is part of a specifier inside the DP c-commanding the numeral \textit{EIN}, or more precisely, the semantically active part \textit{Ø}\textsubscript{[-PL]}. In contrast, \textit{ein} was argued to be in Card. As such, similar to \sectref{sec:key:5.1.1}, these scopal data along with standard assumptions about movement present clear evidence that \textit{Ø}\textsubscript{[-PL]}\textit{\textsubscript{} }is in a different location from \textit{ein}. Finally, recall that, in order to derive the strong reading of the numeral in \REF{ex:key:58}, I assume that the article and \textit{Ø}\textsubscript{[-PL]}\textit{\textsubscript{} }(or rather QP) move to D and Spec,DP, respectively; morphological spell-out occurs as in \REF{ex:key:59}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To take stock thus far, deriving the Modern German singularity numeral from the combination of \textit{Ø}\textsubscript{[-PL]}\textit{\textsubscript{} }and the vacuous article has a number of advantages. On the one hand, we can put all numerals in the same phrasal position(s), and we can explain the different scopal effects of \textit{mehr als} ‘more than’ on the various \textit{ein}{}-words and their related larger noun phrases. On the other hand, we can account for the facts that \textit{EIN} inflects like the indefinite article, that both of these elements cannot co-occur, and that both elements can take a weak adjective. In other words, the splitting of the numeral into two underlying parts and their subsequent composite spell-out accounts for the hybrid properties of \textit{EIN}\text{} .\footnote{A composite analysis could also explain the alternation between the indefinite article and the numeral for ‘one’ in some other languages closely related to Standard German:(i)  a.  \textit{a(n)}  vs.  \textit{one}    (English)    b.  \textit{a(n)}  vs.  \textit{eyn}    (Yiddish)c. \textit{een}  vs.  \textit{één}    (Dutch)d.  \textit{a}  vs.  \textit{õa}    (Bavarian German) In some of these cases, the spell-out of the relevant component(s) results in quite different surface forms.} To highlight the special status of \textit{EIN} further, consider the interaction between \textit{ein} and other numerals in more detail. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Returning briefly to the co-occurrence of \textit{ein} and non-singularity numerals, consider the examples in \REF{ex:key:60}, where \REF{ex:key:60a} contains plural \textit{ein} and \REF{ex:key:60b} involves the negative article in a plural context:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%60
    \label{ex:key:60}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \%  \textit{und so ne zwei Kämpfer die   alles umpflügen,}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    and so a  two  fighters   who all     under.plough
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘and such two guys, who create such chaos’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Es waren k-eine zehn Leute  da}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    it  \textsubscript{} were   \textsc{neg}{}-a \textsubscript{} ten   people there
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘There were not even ten people.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These distributions follow from the fact that unlike \textit{EIN}, other numerals are not composite forms – they are not morphologically related to \textit{ein}. Consequently, the non-composite numerals and the article \textit{ein} can co-occur. As to the structure, I suggest that the numerals in \REF{ex:key:60} are in Spec,CardP and that \textit{ein} is in a higher head position (D).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.2.   Article / Numeral vs. Adjective 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Above, I derived some of the morpho-syntactic aspects of the numeral from the indefinite article. Among others, this accounted for the fact that the numeral and the indefinite article may not (separately) co-occur although they are in different positions (specifier vs. head). This now makes the prediction that if a form of \textit{ein} were to occur with a determiner, then this \textit{ein} could not be the indefinite article (or the numeral). We will see again that such distributions are indeed possible, but I argue that this instance of \textit{ein} is adjectival. In what follows I provide more evidence for this categorically different \textit{ein} suggesting that it is in a different position. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.2.1. Different Morphology
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Determiners do not have weak endings. There is just one exception. As discussed in Chapter 3, \sectref{sec:key:4}, some determiners may optionally have a weak ending in genitive masculine/neuter contexts. However, as seen there, this does not apply to \textit{ein}{}-words. Now, we saw in \sectref{sec:key:3} above that adjectival \textit{eine} can have a weak ending in singular contexts (e.g., \textit{der ein-e}) or in plural ones (e.g., \textit{die ein-en}). Thus, this element behaves morphologically like a regular adjective (and not like an \textit{ein}{}-word). This provides some first motivation for the current classification of \textit{ein} as regards adjective vs. article/numeral. In addition to the difference in weak inflection, there are other inflectional distinctions. Consider this in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As amply documented in \sectref{sec:key:2}, the indefinite article and the singularity numeral cannot have an ending in nominative masculine and nominative/accusative neuter contexts when an overt noun follows (61a-b). An account of this was provided in the context of adjectival inflections in Chapter 2, \sectref{sec:key:2.2.2}. We have also seen that adjectival \textit{eine} has an inflection when a noun follows. Recall though that is has to occur in a definite context – an indefinite environment yields ungrammaticality \REF{ex:key:61c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%61
    \label{ex:key:61}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.   \textit{(ei)n-(*er) Sohn}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     a-\textsc{st}          son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘a son’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.   \textit{EIN-(*ER) Sohn}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one-\textsc{st}        son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one son’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{*  ein-er  Sohn}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one-\textsc{st} son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is less well known that PP can be added to the left periphery of the noun phrase (\citealt{Bhatt1990}, \citealt{Fortmann1996}, \citealt{Haider1992}, \citealt{Roehrs2020a}). This includes \textit{von}{}-possessives. Interestingly, this context yields the same facts as regards the different kinds of \textit{ein}. Compare the examples in \REF{ex:key:61} to their respective counterparts below: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%62
    \label{ex:key:62}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{von Peter (ei)n Sohn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    of    Peter \textsubscript{}  a     son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘a son of Peter’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{von Peter EIN Sohn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    of    Peter one  son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one son of Peter’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  *  \textit{von Peter ein-er  Sohn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    of    Peter one-\textsc{st} son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The judgments reverse with a Saxon Genitive, a context involving definiteness. Compare (62a-c) to (63a-c). Note also that adjectival \textit{eine} has a strong ending in this context \REF{ex:key:63c}, again exhibiting similarities with a regular adjective \REF{ex:key:63d}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%63
    \label{ex:key:63}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{Peters (ei)n Sohn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter’s \textsubscript{} a     son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{Peters EIN Sohn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter’s one son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Peters  ein-er  Sohn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter’s one-\textsc{st} son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one of the two of Peter’s sons’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Peter’s one son’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{Peters  groß-er Sohn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter’s big-\textsc{st}   son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Peter’s big son’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As is expected, concomitant with the different distributions in (61-62) vs. \REF{ex:key:63}, there is a difference in the semantics: while there is no duality presupposition in the first two sets of data, it is present in \REF{ex:key:63c} of the third paradigm. This is confirmed by the fact that this nominal can straightforwardly be followed by \textit{Peters anderer Sohn} ‘Peter’s other son’. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The different inflections on \textit{ein} and the different semantics just discussed correlate with another morpho-syntactic distinction: the definite article can replace \textit{ein} in (61a-b) and (62a-b) but not \textit{einer} in \REF{ex:key:63c}. Consider (64a-b) vs. \REF{ex:key:64c}. With a definite article present in (64a-b), adjectival \textit{eine} can be added to these strings yielding \REF{ex:key:64d}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%64
    \label{ex:key:64}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{der Sohn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the son’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{von Peter der Sohn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    of   Peter \textsubscript{} the son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Peter’s son’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. *  \textit{Peters  der Sohn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter’s the son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{(von Peter) der eine Sohn}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      of   Peter  \textsubscript{} the one  son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one of the two sons (of Peter’s)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Thus far, we have seen that the indefinite article and the singularity numeral exhibit different inflections than adjectival \textit{eine} – the latter inflects like an adjective (and must appear in definite contexts). Before I continue the discussion of the inflections on the different kinds of \textit{ein}, I provide a interim summary of the data and account for the different distributions of the possessives and their following elements.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To take stock thus far, \textit{von}{}-possessives can occur with the definite article, the indefinite article, the singularity numeral, but not with adjectival \textit{eine} (unless a definite article is also present). In contrast, Saxon Genitives can occur with adjectival \textit{eine}, but not with the definite article, the indefinite article, and the singularity numeral. In \citet{Roehrs2020a}, I propose that \textit{von}{}-possessives are outside the DP proper but that Saxon Genitives are in Spec,DP. Given this analysis, it follows that the former allows different determiners including the singularity numeral but that the latter does not (recall also from Chapter 2, \sectref{sec:key:2.3} that Saxon Genitives involve null articles). The reason why \textit{von}{}-possessives do not tolerate adjectival \textit{eine}, but Saxon Genitives do, is that the former do not involve a definite context but that the latter do.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Returning to the inflections, the morphological parallelism of adjectival \textit{eine} and regular adjectives can be strengthened further. As already seen above, the indefinite article and the singularity numeral have endings different from the following adjective. This is illustrated here again in the nominative and dative neuter: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%65
    \label{ex:key:65}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{(ei)n frisch-es        Brot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     a     fresh-\textsc{nom.st} bread.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘a loaf of fresh bread’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{EIN frisch-es        Brot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one fresh-\textsc{nom.st} bread.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one loaf of fresh bread’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{(ei)n-em   frisch-en Brot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     a-\textsc{dat.st} fresh-\textsc{wk} bread.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘a loaf of fresh bread’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{EIN-EM      frisch-en Brot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one-\textsc{dat.st} fresh-\textsc{wk} bread.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one loaf of fresh bread’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Above, we saw that adjectival \textit{eine} and a regular adjective can each have a strong inflection in isolation. We expect then that when both of these elements co-occur, they both have the same endings. This is indeed borne out: both elements are weak in (66a-b) but strong in \REF{ex:key:66c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%66
    \label{ex:key:66}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{da-s             ein-e     frisch-e   Brot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{nom.st} one-\textsc{wk} fresh-\textsc{wk} bread.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one of the two loaves of fresh bread’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{de-m          ein-en    frisch-en Brot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the-\textsc{dat.st} one-\textsc{wk} fresh-\textsc{wk} bread.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one of the two loaves of fresh bread’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Peters  ein-er  lieb-er  Sohn        und Peters  ander-er lieb-er  Sohn} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Peter’s one-\textsc{st} nice-\textsc{st}\textsubscript{} son.\textsc{masc} and Peter’s other-\textsc{st}  nice-\textsc{st} son.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{verstehen   sich   gut}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

understand \textsc{refl} well 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Peter’s first nice son and Peter’s second nice son get along well.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This juxtaposition of \textit{ein} and regular adjectives presents more evidence that \textit{ein} as an article or numeral in \REF{ex:key:65} is different from \textit{ein} as an adjective in \REF{ex:key:66}. I return to the parallelism of adjectival \textit{eine} and \textit{andere} ‘other’ seen in \REF{ex:key:66c} in \sectref{sec:key:5.2.4} below. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall from the discussion of feature specifications in \sectref{sec:key:4.3.2} that adjectival \textit{eine} involves an adjectival structure (i.e., it has InflP on the top of AP). Now, in order to derive the adjectival endings in a uniform way (compatible with Chapter 2), I propose that the structure of adjectival \textit{eine} is merged in a position similar to that of other adjectives. Presumably, this is the highest Spec,AgrP in the nominal structure (see also \citealt{Gallmann2004}: 155, \citealt{Pafel2005}: 179). The tree representation for adjectival \textit{eine} is provided in the next subsection.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.2.2. Co-occurrence with Possessive \textit{ein}{}-words 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Above, \textit{ein} was discussed in the context of prenominal possessives like \textit{von}{}-phrases and Saxon Genitives. Turning to possessive articles, it is worth pointing out again that unlike the indefinite and negative articles in (67a-b), the possessive article in \REF{ex:key:67c} may occur with adjectival \textit{eine}. This includes cases where the possessive article is part of a periphrastic possessive construction as in \REF{ex:key:67d}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%67
    \label{ex:key:67}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a. *  \textit{(ei)ne eine Freundin}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     an     \textsubscript{} one  girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. *  \textit{keine eine Freundin}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    no     \textsubscript{} one  girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.   \textit{meine eine Freundin}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  my     \textsubscript{} one  girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one of the two of my girlfriends’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘my one girlfriend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{Peter seine eine Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter his     one  girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one of the two of Peter’s girlfriends’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Peter’s girlfriend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The sequence of two instances of \textit{ein} in \REF{ex:key:67a} and \REF{ex:key:67b} cannot simply be ruled out by haplology. Roughly, this describes the reduction of identical sequences of sounds (for the discussion of \REF{ex:key:67a} in this regard, see \citealt{Bhatt1990}: 201; more generally, see Neeleman \& van de \citealt{Koot2006}). If this were the case, we would expect (67c-d) to be ungrammatical as well, similar to (67a-b), but contrary to fact. The difference between (67a-b) and (67c-d) has to do with definiteness. Again, I conclude that adjectival \textit{eine} must occur in definite contexts.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Note though that \textit{ein} preceding the noun in \REF{ex:key:67} is actually morphologically ambiguous in the feminine between the singularity numeral and the adjective (indeed, both of these elements are also stressed). Thus, it could be objected that \textit{eine} here is not the adjective. To confirm the analysis of \textit{ein} as an adjective in \REF{ex:key:67}, I consider each of the two analytical options of the \textit{ein} before the noun (i.e., numeral vs. adjective) more carefully.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I start with the first option, where lower \textit{ein} could be taken to be the numeral \textit{EIN}. Consider cases in the nominative masculine, where the morphology clearly distinguishes between \textit{ein}{}-words and adjectives. As can be seen in \REF{ex:key:68}, these instances with the numeral \textit{EIN} are ungrammatical:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%68
    \label{ex:key:68}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{*  (ei)n EIN Freund} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      a     one  friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{*  kein EIN Freund} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     no    one  friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{*  mein EIN Freund} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     my   one  friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{*  Peter sein EIN Freund} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     Peter \textsubscript{} his  one  friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given the ungrammaticality of (67a-b), the status of (68a-b) may not be unexpected. However, note that unlike the grammatical cases in (67c-d), their counterparts in (68c-d) are ungrammatical here. This means that the lower \textit{ein} in (67c-d) cannot involve the numeral \textit{EIN}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In order to account for \REF{ex:key:68}, observe that there are two copies of \textit{ein} in these instances, one before \textit{EIN} and one that has – apparently – formed a composite with \textit{Ø}\textsubscript{[-PL]} yielding the singularity numeral \textit{EIN} itself. In order to account for the ungrammaticality, I suggest that after Copy Reduction, only the higher copy of \textit{ein} is present. This means that (lower) \textit{Ø}\textsubscript{[-PL]} cannot form a composite with \textit{ein}; that is, the distributions in \REF{ex:key:68} cannot be generated. This explains the ungrammaticality of \REF{ex:key:68}.\footnote{In Chapter 8, \sectref{sec:key:2.2.2}, we will see that there are instances where a second copy of \textit{ein} can occur (ia). Crucially, this second copy must be a reduced form of \textit{ein} as can be verified in (ib):  (i)  a.  \textit{EINE so’ne Tochter}      one    so.a   daughter.\textsc{fem}      ‘one such daughter’    b.  *  \textit{EINE so     eine Tochter}      one    such a      daughter.\textsc{fem}Following \citet{Nunes2001}, I argue in Chapter 8 that encliticized elements can escape Copy Reduction. Note that a reduced copy of \textit{ein} is not possible with \textit{Ø}\textsubscript{[-PL]} as \textit{‘n} cannot encliticize to a null element. This can clearly be seen in the feminine (which involves adjectival inflections, thus distinguishing the two instances of \textit{ein}): *\textit{meine} [Ø\textsubscript{[-PL]}]\textit{’ne Tochter} ‘my one daughter’.}    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This is different for adjectival \textit{eine}, the second option for lower \textit{ein} in \REF{ex:key:67}. While this element cannot occur with an indefinite or a negative article in the nominative masculine either (69a-b), it can appear with the possessives under discussion here (69c-d), much like the Saxon Genitives discussed in \sectref{sec:key:5.2.1}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%69
    \label{ex:key:69}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{*  (ei)n ein-er  Freund} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     a     one-\textsc{st} friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{*  kein ein-er   Freund} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    no    one-\textsc{st} friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{mein ein-er Freund} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my   one-\textsc{st} friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one of the two of my friends’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘my one friend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{Peter sein ein-er Freund} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter \textsubscript{} his  one-\textsc{st} friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one of the two of Peter’s friends’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Peter’s one friend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This essentially yields the distribution in \REF{ex:key:67} above confirming the claim that \REF{ex:key:67} involves adjectival \textit{eine}. Note again that (69a-b) are ungrammatical as adjectival \textit{eine} is not in definite contexts here. No such semantic problems arise with the possessives in (69c-d), which are definite in interpretation. I turn to the syntactic derivation of adjectival \textit{eine}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I have argued that \textit{ein} as part of the possessive article and \textit{eine} as an adjective are of different lexical categories. I can point out then that they do not stand in a relevant morpho-syntactic relation with one another. If so, it is expected that these types of \textit{ein} can co-occur. Recalling that adjectival \textit{eine} involves InfP at the top of its AP, the syntactic distribution can be illustrated as follows:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%70
    \label{ex:key:70}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

                     DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         PossP    D’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{m{}-}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

D           AgrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textit{eine}\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             InflP           Agr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           \textit{eine}\textsubscript{ADJ}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              Agr           ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                \textit{eine}\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                     Art’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                  Art           NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                  \textit{eine}\textsubscript{i}      \textit{Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This derivation also applies to more complex cases.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Unsurprisingly, like possessive articles, \textit{diese} ‘this’ can also co-occur with adjectival \textit{eine} \REF{ex:key:71a}. As discussed in Chapter 2, \textit{diese} and \textit{meine} can co-occur as well \REF{ex:key:71b}. In fact, adjectival \textit{eine} can appear with both of these elements at the same time \REF{ex:key:71c}, and an adjective can also be added \REF{ex:key:71d} (recall that there is no duality presupposition with demonstratives):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%71
    \label{ex:key:71}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{diese eine Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this   \textsubscript{} one  girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this one girlfriend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{diese meine Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this   my      girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this girlfriend of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.   \textit{diese meine eine Freundin}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this   my      one  girlfriend.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this one girlfriend of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{und diese meine eine große Liebe       zu sehen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    and this   my      one  great  love.\textsc{fem} to see
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘and to see this one great love of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(\href{https://www.kirmesforum.de/threads/faszination-break-%09dancer.1520/page-2}{{https://www.kirmesforum.de/threads/faszination-break-  dancer.1520/page-2}})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

On current assumptions, \textit{diese} is in Spec,DP in \REF{ex:key:71a} but in Spec,LPP in (71b-d); \textit{meine} is in Spec,DP in (71b-d). Thus, these distributions are expected to be possible. Conversely, if we were to assume just one type of \textit{ein}; that is, if we were to try and derive both the numeral \textit{EIN} and adjectival \textit{eine} from (vacuous) \textit{ein}, then a number of distributions would be hard to account for. This is what I focus on next.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.2.3. Co-occurrence of \textit{ein} and Determiners Revisited
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this subsection, I make the point that adjectival \textit{eine} is a different element from another perspective. Recall that the indefinite article and the numeral have no ending when they appear in front of a noun in certain morpho-syntactic contexts (72a-b). As stated before, a definite determiner cannot precede either of them (72c-d):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%72
    \label{ex:key:72}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{(ei)n Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     a     \textsubscript{} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a man’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{EIN Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one \textsubscript{} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one man’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   c.  *  \textit{der (ei)n Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the  one  man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  *  \textit{der EIN Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the one  man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that \textit{ein} with a weak inflection is possible here \REF{ex:key:73a}. Finally, recall again that Saxon Genitives can occur with \textit{ein} provided the latter has a strong inflection. Compare \REF{ex:key:73b} to (73c-d):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%73
    \label{ex:key:73}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *\textit{(der) ein-e     Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       the   one-\textsc{wk}\textsubscript{} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one of the two men’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Peters  ein-er Sohn} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter’s one-\textsc{st} son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one of the two of Peter’s sons’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Peter’s one son’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  *  \textit{Peters  (ei)n Sohn} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter’s  a      son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  *  \textit{Peters  EIN Sohn} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter’s one  son.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Consider \REF{ex:key:72} and \REF{ex:key:73} in the context of the current system.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Under current assumptions, the indefinite article in \REF{ex:key:72c} is ruled out because only one article can appear in a noun phrase. If we assume again that \textit{EIN} consists, in part, of (vacuous) \textit{ein}, then we can rule out the option of \REF{ex:key:72d} under the same assumption. In other words, these cases are not ruled out because \textit{ein} does not have a weak adjectival ending. Rather, there are two articles in the DP, \textit{der} and (vacuous) \textit{ein}, but only one of them can originate in ArtP and move to the left periphery. Turning to \REF{ex:key:73}, if we accept the assumption that adjectival \textit{eine} is a non-composite form, then this element is predicted to occur with another article in \REF{ex:key:73a}. Similarly, if we assume that Saxon Genitives as in \REF{ex:key:73b} involve null articles (Chapters 2), then this distribution follows from the same assumptions. This means that (73c-d) are out as there are two articles, vacuous \textit{ein} and a null article. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To be clear, then, assuming two basic types of \textit{ein}, we can explain why sometimes a form of \textit{ein} cannot occur with another article but sometimes it can, provided that form of \textit{ein} appears in a definite context. These distributions are hard to account for if we assume just one type of \textit{ein}. Indeed, if \textit{EIN} and adjectival \textit{eine} were both derived from vacuous \textit{ein}, then (73a-b) should be, under these assumptions, ungrammatical – there would be two articles just like (73c-d). Given the different grammaticalities, this, however, does not seem to be the case. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, if a different analysis than the current one is to be pursued, then assumptions about the base-generated position of \textit{ein} and its different possible landing sites need to be layed out clearly. Additionally, the assumptions about the inflections on \textit{ein} need to be spelled out in detail. As demonstrated in previous chapters, a simple surface-oriented explanation does not suffice. This means that if the inflection on \textit{ein} is invoked as an explanation for some of the patterns above, it has to be part of a (more) comprehensive account of \textit{ein} itself and adjectival inflections in general. In addition to making the different syntactic and morphological assumptions explicit, such an account also needs to capture the different semantics of the various types of \textit{ein}. As of now, I am not aware of the existence of such an alternative account. I return to the parallelism between adjectival \textit{eine} and \textit{andere} ‘other’.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.2.4. More Evidence for Adjectival \textit{eine}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The claim that \textit{ein} can be an adjective is further strengthened if \textit{eine} is treated as categorially parallel to \textit{andere} ‘other’. Consider \REF{ex:key:74a}, where \textit{eine} and \textit{andere} are in different, coordinated DPs showing similar morpho-syntactic behavior. Now, as briefly discussed in Chapter 2, \sectref{sec:key:2.3}, there are certain types of adjectives – often called definite adjectives – that can license singular count nouns (74b-b’). The adjective \textit{andere} is different and requires the presence of a determiner (74c-c’):\footnote{Another indication of categorial parallelism comes from the fact that adjectival \textit{eine} can also be co-ordinated with \textit{andere}, as in the following idiom:(i)  \textit{der} [ \textit{eine oder andere} ] \textit{Mann}  the    one  or     other      man.\textsc{masc}  ‘some men’This argument must be used with caution though as the relevant interpretation of \textit{eine} is different here; that is, the idiom does not denote just two people.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%74
    \label{ex:key:74}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Meine ein-e     Tochter  \textsubscript{} }\textit{kam,   mein-e andere    nicht}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my      one-\textsc{wk} daughter.\textsc{fem} came, my      other-\textsc{wk} not
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘One of my daughters came, the other did not.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.    \textit{Folgendes Beispiel           illustriert das.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

following  example.\textsc{neut} illustrate  this 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘The following example illustrates this.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b’.  \textit{Das folgende  Beispiel            illustriert das.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

the  following example.\textsc{neut} illustrate  this
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘The following example illustrates this.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  \textit{*  Anderes Beispiel           illustriert das.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

other      example.\textsc{neut} illustrate  this 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c’.  \textit{Das andere Beispiel            illustriert das.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

the  other    \textsubscript{} example.\textsc{neut} illustrate  this
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘The other example illustrates this.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The need for the presence of a determiner in (74c-c’) is exactly what we saw with adjectival \textit{eine} above. In other words, both adjectival \textit{eine} and \textit{andere} ‘other’ require a determiner. Thus, both of these elements are not definite adjectives. Interestingly, the adjective \textit{andere} can also have the meaning ‘different’. This meaning is subject to a certain context, and this reveals another parallelism of \textit{eine} and \textit{andere} in the meaning of ‘other’.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

5.2.5. Different Semantics: \textit{EIN} vs. \textit{eine} and \textit{andere} meaning ‘different’ vs. ‘other’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this final subsection, I make more remarks on the different semantics of \textit{EIN} and adjectival \textit{eine} and on the different readings of \textit{andere} meaning ‘different’ or ‘other’. As seen above, adjectival \textit{eine} and \textit{andere} ‘other’ are related in that they often co-occur. To set the stage for the discussion of \textit{andere} in its different meanings, I start with \textit{EIN} and adjectival \textit{eine}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As discussed above, the numeral \textit{EIN} involves singularity, but adjectival \textit{eine} presupposes duality. Besides this semantic difference, numerals can be modified by \textit{mehr als} ‘more than’ (75a,b,d) whereas adjectival \textit{eine} cannot \REF{ex:key:75c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%75
    \label{ex:key:75}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [ \textit{Mehr als  EIN Student} ] \textit{kam   zur     Party}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      more than one student.\textsc{masc} came to.the party
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘More than one student came to the party.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [ \textit{Mehr als   HUNDERT  Studenten} ] \textit{kamen zur     Party}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      more than one.hundred students      came   to.the party
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘More than one hundred students came to the party.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. *  \textit{Der} [ \textit{mehr als   eine} ] \textit{Student          kam   zur     Party}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the    more than one    student.\textsc{masc} came to.the party
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \textit{Die} [ \textit{mehr als  HUNDERT} ] \textit{Studenten kamen zur      Party}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the    more than one.hundred students   came    to.the party 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘The more than one hundred students came to the party.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Again, adjectival \textit{eine} is an element semantically different from numerals including \textit{EIN}. With this in place, I turn to the different meanings of \textit{andere}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As just observed, adjectival \textit{eine} cannot be modified by \textit{mehr als} ‘more than’. Something similar holds for adjectival \textit{eine} when modified by the type/degree particle \textit{so} ‘such’. Compare \REF{ex:key:75a} to \REF{ex:key:76a} and \REF{ex:key:75c} to \REF{ex:key:76b}.\footnote{Note that \REF{ex:key:75a} involves the singularity numeral \textit{EIN} but that \REF{ex:key:76a} contains the article \textit{ein}. This is due to the fact that the scopal element \textit{mehr als} ‘more than’ modifies a numeral in \REF{ex:key:75a} but that the type/degree particle \textit{so} ‘such’ is supported by \textit{ein} in \REF{ex:key:76a} (see also Chapter 8, Sections 2.2.2).} Interestingly, when \textit{eine} in \REF{ex:key:76b} is replaced by \textit{andere}, the example becomes grammatical, but \textit{andere} can only be interpreted as ‘different’ (but not as ‘other’) in this context \REF{ex:key:76c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%76
    \label{ex:key:76}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.   \textit{so (ei)n Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  so  \textsubscript{} a     man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘such a man/a man like that’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  *  \textit{der so eine Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  the so one  man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.  \textit{der so andere   Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  the so different man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘the so different man’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \#‘the so other man’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Thus, the ungrammaticality of \REF{ex:key:76b} and the interpretative restriction in \REF{ex:key:76c} fit well with the discussion above, where I showed that adjectival \textit{eine} often co-occurs with \textit{andere} in the meaning of ‘other’. More generally, this provides additional evidence that this type of \textit{ein}, just like \textit{andere} ‘other’, is a special kind of adjective. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up this section, I discussed more evidence that there are two main types of \textit{ein}: the indefinite article and adjectival \textit{eine}. The singularity numeral \textit{EIN} is based, in part, on the indefinite article. Each of these three elements is in a different position: the indefinite article is in Card or D, the singularity numeral, that is, its semantic component, is in Spec,CardP or Spec,DP, and adjectival \textit{eine} is in a high Spec,AgrP position. The adjectival inflections on these different elements support the classification into the two main types. The different inflections follow from the system developed in Chapter 2, again highlighting the fact that adjectival inflections and \textit{ein} can (and should) be discussed in tandem. Before I review some previous work on \textit{ein}, I briefly return to the discussion of \textit{ein}{}-words as composite forms. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{6.}  \textbf{Diachronic} \textbf{and} \textbf{Cross-linguistic} \textbf{Evidence} \textbf{for} \textbf{\textit{ein}}\textbf{{}-words as Composites}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As mentioned in \sectref{sec:key:4.2.2}, there is an interesting asymmetry in the syntactic distribution of Saxon Genitives and possessive articles. Whereas the former can follow their head nouns, the latter cannot:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%77
    \label{ex:key:77}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{das Buch          Maximilians}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the book.\textsc{neut} Maximilian’s 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Maximilian’s book’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{das Buch          sein} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the book.\textsc{neut} his
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This distribution was used to motivate the composite analysis of possessive articles – since \textit{ein} must precede the head noun, the possessive article must precede it as well. This accounts for the ungrammaticality in \REF{ex:key:77b}. Interestingly, other languages seem to be revealing in this respect documenting that the lower position of the possessive pronominal in \REF{ex:key:77b} is not ungrammatical in principle. This means that \REF{ex:key:77b} requires an explanation in German. I consider this in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Unlike Modern Standard German, OHG allows possessive pronominals to follow their head nouns \REF{ex:key:78a}. Furthermore, note that Norwegian can also have possessive pronominals in postnominal position \REF{ex:key:78b} ((78a) is from \citealt{Demske2001}: 173; \REF{ex:key:78b} is from \citealt{Julien2005a}: 140):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%78
    \label{ex:key:78}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{(ther) fater            min}\textbf{ }    (OHG)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsubscript{} the   father.\textsc{masc} my
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘my father’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{katt-a           mi}      (Norwegian)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    cat.\textsc{fem}{}-\textsc{def} my
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘my cat’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I reiterate the proposal that the properties of the possessive articles in Modern Standard German follow from the decompositional analysis discussed above. Specifically, since \textit{ein} itself cannot follow the head noun \REF{ex:key:79a}, other \textit{ein}{}-words cannot either. This was illustrated for the possessive article in \REF{ex:key:77b} above and is illustrated for the singularity numeral and the negative article in (79b-c) below:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%79
    \label{ex:key:79}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{\{das} / \textit{ein / Ø}\textsubscript{D}\textit{\} Buch          ein} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textsubscript{} the \textsubscript{} / a     \textsubscript{} book.\textsc{neut} a
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{\{das} / \textit{ein / Ø}\textsubscript{D}\textit{\} Buch          EIN} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textsubscript{} the \textsubscript{} / a     \textsubscript{} book.\textsc{neut} one
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  *  \textit{\{das} / \textit{ein / Ø}\textsubscript{D}\textit{\} Buch           kein} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textsubscript{} the \textsubscript{} / a     \textsubscript{} book.\textsc{neut} no
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Now, while (79b-c) are also out for independent reasons (i.e., the high base positions of \textit{Ø}\textsubscript{[-PL]} and NEG), the ungrammaticality of \REF{ex:key:79a} suggests a straightforward account of the absence of postnominal possessive pronominals in Modern Standard German as in \REF{ex:key:77b}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In more detail, as seen in \REF{ex:key:77a} and \REF{ex:key:78} above, possessives can be base-generated in a low position. As such, possessive pronominals in Modern Standard German could also be expected to be grammatical in such a low position. This, however, is not the case. Given the composite analysis, note though that the possessive component of the article (e.g., \textit{s}{}- in \REF{ex:key:77b}) cannot be supported by \textit{ein} in Modern German if the possessive stays in situ – as indicated by \REF{ex:key:79a}, \textit{ein} itself cannot occur in such a low position since \textit{ein}, like all determiners, is base-generated in ArtP, a position higher than the (surface) position of the noun. Assuming that the possessive pronominals in OHG and Norwegian are not composite forms involving an indefinite article, they can surface in postnominal position.\footnote{Interestingly, prenominal possessives can also occur lower in the structure in OHG, namely between the determiner and the head noun (see also Footnote Error: Reference source not found; (i) is taken from \citealt{Demske2001}: 227):(i)  \textit{in dhemu heilegin daniheles chiscribe}  (OHG)in the      \textsubscript{} holy       Daniel’s   scripture.\textsc{neut}‘in Daniel’s holy scriptures’Again, this indicates that possessives have a different analysis in the older varieties of German.} This diachronic and cross-linguistic difference lends more credence to the proposal that possessive articles in (Modern) Standard German are composite forms.\footnote{Note that dialectal forms of the possessive article (e.g., Northern German \textit{min} ‘my’) may also involve a decompositional analysis. Their spell-out forms, however, may involve more than simple Local Dislocation to support the possessive element.} I briefly sketch the diachronic path of the three \textit{ein}{}-words involving composites.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In \sectref{sec:key:5.1.1}, I tentatively suggested that OHG \textit{éin} ‘one’ was split into two components in the development of the language \REF{ex:key:80a} leading to the emergence of the indefinite article. With this in mind, I suggest that the other \textit{ein}{}-words were also split into two parts over time. Specifically, possessive pronominals were split into a possessive component and vacuous \textit{ein} \REF{ex:key:80b}.\footnote{This process is possibly tied to ENHG Diphthongization, which changed [i:] to [aI]. Furthermore, the split of the possessive pronoun could have come about by analogy with the splits of the singularity numeral and/or the negative article.} As for the negative article, judging from the diachronic development of \textit{kein} ‘no’ described in Paul \textit{et al}. (1989: 235), it appears as if the negative article consisted of two components early on \REF{ex:key:80c}. Importantly though, the inner makeup seems to have changed from the combination of a negative element with the singularity numeral to a negative element with the indefinite article:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%80
    \label{ex:key:80}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{éin}  →  \textit{Ø}\textsubscript{[-PL]}+\textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one    \textit{Ø}\textsubscript{[-PL]}+a
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{mīn}  →  \textit{m    \textsubscript{} }+\textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my    \textsc{poss}+a
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{ne(c)h+ein}     →     \textit{nekein} →     \textit{k} +\textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    not     \textsubscript{} +one                \textsc{neg}+a
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up, unlike vacuous \textit{ein}, the other Modern German \textit{ein}{}-words are composite forms. In contrast, the counterparts of the latter elements in OHG (and Norwegian) are not. Note that this difference in decompositionality is also compatible with the change in inflection that adjectives went through in the history of German; for instance, recall from Chapter 2, \sectref{sec:key:1.2} that possessive pronominals in OHG (and Norwegian) take adjectives with weak inflections but that possessive articles in Modern German take adjectives with mixed inflections. Thus, while the old possessive pronominals pattern with the definite article, the Modern German counterparts are related to \textit{ein}. It is clear that German has undergone some changes in this empirical domain. In other words, OHG and Modern German must have different accounts of these phenomena.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the next section, I turn to some previous analyses examining one proposal in more detail. We will see that unlike adjectival endings discussed in Chapter 2, there are much fewer contributions investigating the different kinds of \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

7.  A Brief Critique of a Previous Proposal\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the descriptive literature, the indefinite and the definite article are usually discussed together, with the former taken to be the indefinite counterpart of the latter (for German, see \citealt{Duden1995}: 303-21). Recognizing certain shortcomings of this juxtaposition, a number of other views have emerged. For example, \citet{Perlmutter1970} derives English \textit{a(n)} as an unstressed version of the numeral \textit{one}, \citet[47]{Higginbotham1987} argues that the indefinite article in predicate nominals is an adjective meaning ‘one’, and \citet{Ackles1996} argues that \textit{a(n)} marks the presence of NumP with singular count nouns.\footnote{For the discussion of English \textit{a(n)} in quantifying expressions (e.g., \textit{a lot}, \textit{a few}), see \citet{Klockmann2020}. The author tentatively extends the discussion to ordinary noun phrases like \textit{a book} claiming that \textit{a(n)} is not an indefinite element – it is featureless but involves countability. This is partially different from the current discussion of German. Here, \textit{ein} is also claimed not to be related to indefiniteness, but \textit{ein} is proposed to have a feature (i.e., [+D]), and it is held not to be responsible for countabilily.} Elaborating on work by \citet{Oomen1977}, Vater (1982, 1984, 2002) proposes that there is no indefinite article at all but only a numeral/quantifier (“Quantor”). He mainly discusses German but his empirical coverage is meant to be wider.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  It is interesting to point out that all proposals just mentioned make different claims. No consensus has emerged. Recall now that I showed in Chapter 1, \sectref{sec:key:2.1.3} that German, Nowegian, and Yiddish seem to differ in some of the empirical details. Similar to adjectival inflections in Chapter 2, I have proposed that it is these – on the surface minor – details that show the true nature of the indefinite article in German. In what follows, I review only Vater’s proposal, which explicitly discusses German. As far as I am aware, this proposal seems to have received fairly wide acceptance, at least for German. To give just one example, although providing a critique of some points of Vater (1982, 1984), \citet[100-16]{Bisle-Müller1991} reaches a similar conclusion.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In a series of papers (1982, 1984, 2002), Vater argues against the traditional opposition of the indefinite vis-à-vis the definite article (I refer only to \citealt{Vater1982} as that contains the main relevant insights). He proposes that \textit{ein} is not the indefinite counterpart of definite \textit{der} ‘the’. According to Vater, this element is not indefinite as \textit{ein} can lead not only to a non-specific but also to a specific interpretation of the containing noun phrase (for examples, see \sectref{sec:key:4.1.3} above). In addition, it denotes a specific amount, namely singularity. Furthermore, this element is not an article as \textit{ein} is not necessarily “localizing” in function in the sense of \citet{Hawkins1978}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Rather, \citet{Vater1982} proposes that \textit{ein} is a cardinal numeral, that is, a type of quantifier that denotes a specific number of entities (also \citealt{Oomen1977}). Generalizing his discussion, he claims that determiners or articles only involve definite elements. In contrast, the other determiner elements belong to a different part of speech – numerals/quantifiers. He provides some syntactic and semantic arguments for this claim. After illustrating some of these arguments, I return to them showcasing some shortcomings. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Starting with some syntactic arguments, \citet[71]{Vater1982} points out that like other quantifiers, \textit{ein} can undergo quantifier float (note that the translations of the examples here and below are my own):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%81
    \label{ex:key:81}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Antrag       \textsubscript{} }\textit{habe ich keinen gestellt}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    application.\textsc{masc} have I    \textsubscript{} no        submitted
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for applications, I have submitted none.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Antrag                 \textsubscript{} }\textit{habe ich einen gestellt}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    application.\textsc{masc} have I    \textsubscript{} one    submitted
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for applications, I have submitted one.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Second, like other numerals \REF{ex:key:82a}, \textit{ein} can co-occur with a determiner \REF{ex:key:82b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%82
    \label{ex:key:82}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{die zwei Bücher}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the two  books
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the two books’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{das eine Buch}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the \textsubscript{} one  book.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one of the two books’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Turning to some semantic arguments, \citet[71-72]{Vater1982} argues that like other numerals, \textit{ein} can individuate mass nouns. Glossing over some of the details, the interpretation in \REF{ex:key:83a} can involve certain types of bread or certain amounts of bread. Modulo the singular, the example in \REF{ex:key:83b} has a similar range of readings. Importantly, the definite article by itself does not have this individuating function. Note though that the example in \REF{ex:key:83c} is still ambiguous, here between a mass and a count reading:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%83
    \label{ex:key:83}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{zwei Brote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    two  breads
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘two types of bread’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘two loaves of bread’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{ein  Brot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one bread.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one type of bread’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘one loaf of bread’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{das Brot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     the bread.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the (substance of) bread’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the loaf of bread’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In order to derive the count reading in \REF{ex:key:83c}, Vater follows certain aspects of \citet{Perlmutter1970}. Specifically, Vater (1982: 71-72, 1984: 39) proposes that the mass interpretation in \REF{ex:key:83c} only involves a definite article \REF{ex:key:84a}. In contrast, the definite singular count reading in \REF{ex:key:83c} involves \textit{ein}, which is deleted \REF{ex:key:84b}. Interestingly, this \textit{ein} can also surface. In this scenario, the interpretation is, according to Vater, partitive. This is indicated by the translation in \REF{ex:key:84c}. Below, I return to the word put in square brackets:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%84
    \label{ex:key:84}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{das Brot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the bread.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the (substance of) bread’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{das eine Brot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the one  bread.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the loaf of bread’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{das eine Brot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the \textsubscript{} one  bread.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one of the [two] breads’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

After this brief illustration, I return to the above arguments pointing out some shortcomings.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Note that the first syntactic argument does not involve quantifier float but rather a different type of discontinuous noun phrase. In Chapter 4, \sectref{sec:key:3}, I labeled this construction split topicalization. One argument against Vater’s view is that under certain conditions, this type of split does allow (other) determiners to be stranded: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%85
    \label{ex:key:85}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Hemden habe ich immer  nur  \textsubscript{} }\textit{diese da     getragen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  shirts      have I    \textsubscript{} always only these there worn
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘As for shirts, I have always worn only these there.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given that either \textit{ein} or \textit{diese} ‘these’ can occur in a source DP, we can maintain the claim that \textit{ein} is a determiner.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Turning to the second syntactic argument, it is true that determiners and quantifiers can co-occur. However, there are also cases where two determiner elements can be combined \REF{ex:key:86a}. The same holds for two quantifiers on Vater’s assumptions or, alternatively, two determiner elements on my assumptions \REF{ex:key:86b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%86
    \label{ex:key:86}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{diese meine Freunde}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    these my     friends
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘these friends of mine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{ein jeder von uns}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  an  every of   us
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘each of us’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Again, given the possibility of co-occurrence, I continue claiming that \textit{ein} is a determiner(-like) element when it occurs with another determiner as in \REF{ex:key:86b} (specifically, this type of \textit{ein} is a predeterminer, see also Footnote Error: Reference source not found). As for the semantic arguments, I agree that \textit{ein} seems to be individuating in nature. However, I suggest in Chapter 8, \sectref{sec:key:2.2.3} that \textit{ein} is not responsible for this effect, but rather it flags the presence of an operator. Finally, I discuss the partitive interpretation in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \citet[71]{Vater1982} claims that \textit{ein} preceded by a definite article can be paraphrased as \textit{ein} followed by a genitive noun phrase in the plural; that is, \textit{ein} is taken to be the singularity numeral. Consider (87a-b). \citet[72]{Vater1982} explicitly states that \textit{ein} here presupposes a larger set of elements. However, this claim is not quite accurate. Rather, while the paraphrase does indeed imply a larger set, \textit{ein} preceded by the definite article as in \REF{ex:key:87a} typically presupposes just two elements. Furthermore, as mentioned above, this duality presupposition can be cancelled when a demonstrative replaces the definite article \REF{ex:key:87b}. Importantly, the partitive reading in the paraphrase is not cancelled when the demonstrative replaces the definite article there (note again that the translations are mine):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%87
    \label{ex:key:87}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{das eine Buch}          – \textit{eins der     Bücher} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the \textsubscript{} one \textsubscript{} book.\textsc{neut} – one  of.the books
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one of the two books – one of the books’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{dieses eine Buch          – eins dieser    Bücher} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this     one \textsubscript{} book.\textsc{neut} – one  of.these books
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘this one book – one of these books’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I take this to mean that the paraphrases do not capture the semantics of this \textit{ein} accurately. In other words, \textit{ein} is not the singularity numeral here. Furthermore, it appears that other numerals do not invoke this partitivity either; that is, \textit{die zwei Bücher} ‘the two books’ does not mean ‘two of the books’. I conclude that the \textit{ein} in \REF{ex:key:87} is of a different type. Above, I proposed that this \textit{ein} is adjectival, an element only licensed in definite contexts. Besides these issues, it is also worth pointing out that a number of other, important types of \textit{ein} are not discussed in Vater. This shows the limited empirical coverage of that proposal.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Predicate nominals and cases in the plural are not mentioned in Vater’s discussion at all:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%88
    \label{ex:key:88}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Er ist (ein) Lehrer} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

he is   \textsubscript{} a     teacher.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘He is a teacher.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \%  \textit{So    ein-e Idioten!} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     such a-\textsc{pl}  idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     ‘Such Idiots!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is quite clear that the noun phrases in \REF{ex:key:88a} and \REF{ex:key:88b} have nothing to do with singularity: \REF{ex:key:88a} denotes a property, and \REF{ex:key:88b} involves a plurality. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Furthermore, note also that Vater does not discuss reduced forms. However, as can be seen in \REF{ex:key:89}, the form of \textit{ein}, reduced or unreduced, \textit{can} make an important difference with regard to the grammaticality judgments and with regard to the interpretations (for \REF{ex:key:89c}, see \citealt{Pafel1994}: 251):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%89
    \label{ex:key:89}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Geben Sie \textsubscript{} }\textit{mir (ei)nen Apfel!} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

give    you me   an       apple.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Give me an apple!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{Geben Sie \textsubscript{} }\textit{mir *(EI)NEN Apfel!} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

give    you me     one        apple.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Give me one apple!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Geben Sie \textsubscript{} }\textit{mir *(ei)nen!} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

give    you me     one
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Give me one!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The distinction of reduced and unreduced forms has played an important role in categorizing the different kinds of \textit{ein} in the current account. \REF{ex:key:89a} involves the indefinite article \textit{ein}, and \REF{ex:key:89b} shows the singularity numeral \textit{EIN}. As for \REF{ex:key:89c}, note that \textit{ein} is used pronominally here. I reiterate the idea that the reduced form of \textit{ein} is ungrammatical as pronominal forms of \textit{ein} involve some stress, which prevents the reduced forms from occurring.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, Vater treats the negator \textit{kein} ‘no’ as an unanalyzed form, and possessives such as \textit{mein} ‘my’ are not discussed at all. As documented above, \textit{ein} and the other \textit{ein}{}-words exhibit a number of morpho-syntactic similarities; for instances, as regards their inflections. Since inflections are not addressed by Vater, an important morpho-syntactic generalization over the different \textit{ein}{}-words is being missed here. In my view, adjectival inflections and \textit{ein}{}-words should be discussed in tandem. In this respect, I point out again that adjectival inflections provide important clues about the lexical categories of items in the noun phrase – they only surface on determiners, certain quantifiers, and adjectives – and they provide indications of the various structures of the noun phrase as a whole.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up, I agree with Vater (and others) that \textit{ein} is not an indefinite article (although I keep the name for convenience). I disagree with him (and others) that \textit{ein} is the singularity numeral. Rather, I claim that there are two types of \textit{ein}: one is a semantically vacuous element, and the other is adjectival \textit{eine}, which has semantics.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

8.  Conclusion 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

One goal of this chapter was to provide a more comprehensive survey of the different types of \textit{ein}. Arguing that \textit{ein} cannot involve the singularity numeral only, I examined three types: the article, the numeral, and the adjective. In order to capture the morphological similarities and the non-co-occurrence of the article and the numeral, I proposed that the numeral is a composite form – it involves the null element \textit{Ø}\textsubscript{[-PL]}, and the latter is supported by the article. Something similar holds for the possessive and negative articles. Adjectival \textit{eine} was proposed to be an independent lexical element. In order to account for the differences, I suggested that the three elements have different feature specifications and are in different positions in the syntactic tree. \tabref{tab:key:2} below summarizes these differences:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Summary of the Properties of the Types of \textit{ein}}
\label{tab:key:2}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\multicolumn{2}{c}{Kinds of \textit{ein}} & Morphology & Semantics & Position\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\multicolumn{1}{c}{Article \textit{ein}} & Vacuous (indefinite, possessive, negative, predicative) & αPL morph &  & Card or D (depending on the reading, weak vs. strong)\textsuperscript{a}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

& Numeral \textit{Ø}\textsubscript{[-PL]} & (αPL morph = αPL sem) & {}-PL sem & Spec,CardP or Spec,DP\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\multicolumn{2}{c}{Adjectival \textit{eine}} & αPL morph & n = 2 (unless in certain contexts) & (high) Spec,AgrP\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textsuperscript{a} In Chapter 6, I propose that predicative \textit{ein} can also occur in Art (the head of ArtP).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is clear that the conditions under which adjectival \textit{eine} induces and loses its duality presupposition require more investigation, something I will no pursue here.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The second goal was to identify more general properties of \textit{ein}. Similar to adjectival inflections in Chapter 2, I concluded here that \textit{ein} is also semantically vacuous (Hypothesis 1a). In the next two chapters, I turn to more properties of \textit{ein} by discussing some consequences of the current analysis. In particular, I discuss \textit{ein} in relation to semantic concepts such as emotiveness and number. This includes the discussion of \textit{ein} in predicative contexts. We will find confirmation of the conclusions reached thus far. Furthermore, we will see more evidence that \textit{ein} can also indicate a certain amount of structure in the noun phrase and that it can flag the presence of covert operators (Hypothesis 3).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\setcounter{page}{258}\begin{styleJBHeadingii}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textup{Chapter 6:} Ein \textup{and Emotiveness}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleJBHeadingii}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.   Introduction
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In Chapter 5, I argued that \textit{ein} is semantically vacuous (Hypothesis 1a). I now turn to two consequences. In this and the next chapter, I discuss \textit{ein} with regard to emotiveness and number. These next two chapters are also meant to be broader in their empirical and theoretical range; that is, they discuss \textit{ein} in the context of both the noun phrase (DP) and the clause (TP). Specifically, I provide a more detailed survey of two (partially known) restrictions. Focusing on the contribution of \textit{ein}, I identify three types of readings in this chapter where the notion of emotiveness is of importance in singular, out-of-the-blue contexts. In the next chapter, I isolate restrictions with regard to both morphological and semantic number such that singular contexts are also more restricted than plural environments. Consistent with the previous discussion, I reach the conclusion that \textit{ein} does not make a semantic contribution. To contextualize the following discussion, I begin with some general remarks. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.1.  General Structural Similarities Between the DP and TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It was pointed out in Chapter 1 that it is generally assumed that clauses and noun phrases are parallel in meaning and structure (e.g., \citealt{Abney1987}, \citealt{Chomsky1970}, Iordăchioaia 2020). To repeat the relevant data, consider the following juxtaposition: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%1
    \label{ex:key:1}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  The Romans destroyed the city.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  the Romans’ destruction of the city 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In view of the similar interpretations of the arguments in \REF{ex:key:1}, the structural parallelism in \REF{ex:key:2} has come to be established:\footnote{As mentioned in Chapter 1, this alignment is not uncontroversial. Recall that I take DP to be parallel to TP. In this and the next chapter, I use Type Theory (Chapter 1, \sectref{sec:key:4.2.2}) to highlight certain similarities and differences in the DP and TP. Note in this regard that \citet{HeimKratzer1998} take (root) clauses to be of category S. As is clear from their discussion of relative clauses (their chapter 5), S is under CP. Given the alignment in \REF{ex:key:2}, I take S to be TP here.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%2
    \label{ex:key:2}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  CP  TP  AgrP       VP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  PP  DP  NumP       NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The investigation of this parallel has been very fruitful in its empirical discoveries and theoretical innovations (for a survey, see Alexiadou \textit{et al}. 2007). This and the next chapter intend to add to this body of work by focusing on certain combinations of “pronoun + (copular verb) + noun” in simple DPs and copular TPs. Unlike the cases in \REF{ex:key:1} above, I investigate noun phrases and copular sentences that involve non-theta nouns; for instance, the noun Idiot ‘idiot’ in \REF{ex:key:3} and \REF{ex:key:4} below. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Observe first that the DPs and TPs under discussion here involve a pronoun and a noun (3a-b). The difference is that the TPs also contain an auxiliary:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%3
    \label{ex:key:3}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{wir Idioten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    we idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘us idiots’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Wir sind Idioten}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    we are   idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘We are idiots.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Unlike in the plural above, \textit{ein} is added in TPs involving the singular:\footnote{Recall that I put the nominal complement of singular pronominal determiners in parentheses in the English translations.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%4
    \label{ex:key:4}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{du   Idiot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Du  bist (ei)n Idiot}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are   an   idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are an idiot.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Considering \REF{ex:key:3} and \REF{ex:key:4}, we can observe that these constructions are fairly similar to each other and involve comparatively little complexity. Specifically, taking the auxiliary to be semantically vacuous (\citealt{CoppockBeaver2015}: 429; \citealt{HeimKratzer1998}: 61-62), I assume that this verbal element simply indicates the presence of more structure, for instance, the TP-layer. As we will see below, \textit{ein} is similar to the auxiliary in this regard. Focusing on these types of constructions affords us a fairly simple and direct comparison of the workings of \textit{ein} in the nominal and clausal domains. Note also that some of the cases discussed below involve non-canonical constructions.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Overall, I reach the conclusion that \textit{ein} makes no semantic contribution in these constructions either and that the nominal and clausal combinations of “pronoun + (copular verb) + noun” are quite similar albeit not entirely identical. The few differences between the two domains follow from certain assumptions about the different pragmatics involved \citep{Rauh2004}, lexical differences between certain overt and covert elements (\textit{als} ‘as’ vs. ALS), and the obligatoriness of NumP in the DP but its syntactic optionality in the predicate nominal of the TP. If this discussion is on the right track, then I provide additional evidence that the nominal and clausal domains are essentially parallel. With these general remarks in mind, I begin the investigation. Postponing the discussion of number to the next chapter, I start by examining \textit{ein} in emotive contexts.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.2.    General Interpretatory Differences Between the DP and TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this chapter, I compare the structural and interpretatory differences of constructions where pronouns combine with bare nouns \REF{ex:key:5} or with nouns preceded by overt elements like \textit{als} ‘as’ or \textit{ein} ‘a’ \REF{ex:key:6}. To avoid certain lexical and structural ambiguities of \textit{ein} (Chapter 5), the indefinite article is provided in its reduced and unstressable form \textit{‘n}. Interestingly, nouns such as \textit{Bauer} are ambiguous in that they have a neutral/literal meaning (‘farmer’) and an emotive/figurative one (‘peasant’) (e.g., \citealt{Harbert2007}: 147, de Swart \textit{et al}. 2007, and references below). However, this ambiguity disappears in these different types of DP and TP in interesting ways. Specifically, bare nouns have an emotive/figurative meaning in the DP \REF{ex:key:5a} but a neutral/literal meaning in the TP \REF{ex:key:5b}. In contrast, nouns preceded by \textit{als} have a neutral/literal meaning in the DP \REF{ex:key:6a}, but nouns preceded by \textit{ein} have a (predominantly) emotive/figurative meaning in the TP \REF{ex:key:6b} (\# indicates that the interpretation is not available; \% means that the interpretation is possible but somewhat less prevalent or for some speakers not available at all):\footnote{A brief note on the data in \REF{ex:key:6} is in order here. The string in \REF{ex:key:6a} can felicitously be used in a sentence like \textit{Was würdest du als Bauer dazu sagen?} ‘What would you as a farmer say about this?’. As to \REF{ex:key:6b}, \citet[147-48]{Harbert2007} states that the indefinite article is not possible with role nouns in predicative contexts in German, a statement also often found in textbooks of German. As we see here and below, this statement is too strong. Indeed, the use of \textit{ein} with terms of professions is already attested in ENHG (Ebert \textit{et al}. 1993: 316). Cross-linguistically, predicate nouns in English typically have an indefinite article in cases like \REF{ex:key:5b} yielding \REF{ex:key:6b}, but their Romance counterparts do not. \citegen{Zamparelli2008} explanation that this is due to lack of morphological gender in English is probably not the whole story as Yiddish, which does have morphological gender, patterns with English (see \citealt{Lockwood1995}: 112, also \citealt{Harbert2007}: 148). For the discussion of morphological case on predicate nominals, see \citet{MalingSprouse1995}.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%5
    \label{ex:key:5}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{du   Bauer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you peasant.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (peasant)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \#‘you (farmer)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Du  bist Bauer.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a farmer.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \#‘You are a peasant.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%6
    \label{ex:key:6}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{du   als Bauer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as  farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you as a farmer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \#‘you as a peasant’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Du  bist ‘n Bauer.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are   a peasant/farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a peasant.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \%‘You are a farmer.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Comparing the (a)-examples to the (b)-examples, we find a near complementary distribution of the two readings of the noun. Indeed, comparing the (a)-examples to each other and the (b)-examples to each other, this difference in interpretation correlates with the presence or absence of \textit{als} ‘as’ and \textit{ein} ‘a’. Focusing on the latter, this means that \textit{ein} seems to have, at least in some contexts, semantic import. Given the proposal that \textit{ein} is semantically vacuous, this is unexpected and in need of an explanation. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To anticipate the discussion, I follow \citet{Rauh2004} for \REF{ex:key:5a} and \REF{ex:key:6a} in arguing that the pragmatics involved here need to be taken into consideration to explain this difference. As to \REF{ex:key:5b} and \REF{ex:key:6b}, it could be claimed that \textit{ein} brings about the semantic difference. However, I suggest that the added emotiveness in \REF{ex:key:6b} stems from the interaction between an operator, specifically the realization operator REL (de Swart \textit{et al}. 2007), and nouns lexically specified as [+figurative]. I propose that on the one hand, \textit{ein} indicates the presence of syntactic structure above NP (Hypothesis 3a) and that on the other hand, it flags the presence of the realization operator REL (Hypothesis 3b). As such, similar to adjectival inflections, I maintain that \textit{ein} is a reflex of the structure but not of the semantics. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The chapter is organized as follows. \sectref{sec:key:2} provides the data discussing three different readings in the DP and TP, and the role emotiveness plays in them. In \sectref{sec:key:3}, I first lay out my main assumptions, and then I provide the proposal and detailed derivations of these different readings. \sectref{sec:key:4} summarizes the main findings of this chapter.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.  Data
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This section investigates the parallels between the nominal and clausal domains with regard to certain readings. Of particular interest here are interpretations that seem to relate the presence of \textit{ein} to emotiveness. In order to understand and classify the data better, I start with some preliminary remarks, where I dicuss the different types of nouns involved in pronominal DPs and copular TPs and how their various meanings relate to different readings. In the second section, I turn to the data in more detail, and I provide a summary of the different readings in the nominal and clausal domains. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.  Preliminary Remarks
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I illustrate some of the properties of the different types of nouns. Specifically, I discuss how role and kind nouns fare in pronominal DPs and copular TPs. In the second subsection, I expand on concepts like emotiveness and figurative extension. In the final subsection, I establish three readings that help classify and analyze the data.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.1. Basic Differences Between Role and Kind Nouns in the DP and TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Nouns can be classified along different dimensions: role vs. kind, neutral vs. emotive, and related to neutral/emotive, literal vs. figurative (for the latter, see next subsection). It is important to point out that the restrictions to be discussed reveal themselves with certain types of nouns only. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Starting with role nouns, de Swart \textit{et al}. (2005: 453) provide a convenient summary of the properties of bare predicate nominals such as \textit{Landwirt} ‘farmer’:\footnote{For relevant discussion, see also \citet{Kupferman1991}, Stowell (1989, 1991), and more recently \citet[814-17]{Alexiadou2005}, \citet{Déprez2005}, \citet{MatushanskySpector2005}, Munn \& \citet[843-49]{Schmitt2005}, \citet[779-83]{Winter2005}, and \citet{Zamparelli2008}. The latter paper also contains detailed critical discussion of earlier work on this topic.} (i) they form a restricted class of nouns (typically, names for professions, nationalities, and religions), (ii) they only exhibit a restricted range of interpretations (what the authors call capacities), (iii) they allow capacity qualifiers such as \textit{by profession}, (iv) they exhibit number neutrality, and (v) they combine with certain adjectives, which remain uninflected (at least, in Dutch). In the discussion to follow, I revisit (i), (ii), and (iv). Furthermore, related to (ii), I add a sixth characteristic to this: in their literal meaning, bare role nouns are never emotive in pragmatically neutral contexts.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Kind nouns comprise a more diverse group; for instance, they involve names for humans, animals, and inanimates. For current purposes, animate nouns can be classified as regards neutral (\textit{Mann} ‘man’) and emotive (\textit{Idiot} ‘idiot’). With this in place, I turn to the combinations of “pronoun + (copular verb) + noun” to identify some initial properties and restrictions. I focus for now on pragmatically neutral, out-of-the-blue contexts (but see \sectref{sec:key:3.1.3}).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  It is well known that pronominal DPs in the singular only allow emotive kind nouns like \textit{Idiot} ‘idiot’; (neutral) role nouns like \textit{Landwirt} ‘farmer’ and neutral kind nouns like \textit{Mann} ‘man’ are marked \REF{ex:key:7}: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%7
    \label{ex:key:7}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{du \textsubscript{} }\textit{Idiot} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you idiot.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.  ?? \textit{du \textsubscript{} }\textit{Landwirt} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you farmer.\textsc{masc}    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (farmer)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     c.  ?? \textit{du \textsubscript{} }\textit{Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (man)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In contrast, copular constructions can involve bare predicate nouns only if the relevant nominals consist of role nouns like \textit{Landwirt}. Note again though that the indefinite article is optional here for many speakers \REF{ex:key:8a}. Unlike role nouns, kind nouns like emotive \textit{Idiot} and non-emotive \textit{Mann} require an indefinite article (8b-c):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%8
    \label{ex:key:8}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Du  bist (‘n) Landwirt}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are    a   farmer.\textsc{masc}      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a farmer.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Du  bist *(‘n) Idiot}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are    \textsubscript{} an idiot.\textsc{masc}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are an idiot.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Du  bist *(‘n) Mann}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are    \textsubscript{} a   man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a man.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To be clear, abstracting away from the determiners in \REF{ex:key:7a} and \REF{ex:key:8a}, bare nouns involve emotive kind nouns in the DP but (neutral) role nouns in the TP. Furthermore, role nouns in the DP \REF{ex:key:7b} and emotive kind nouns in the TP \REF{ex:key:8b} pattern like neutral kind nouns such as \textit{Mann} in the (c)-examples. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Examining these cases, it becomes clear that true minimal pairs between singular DPs and related TPs are not possible. In other words, in pragmatically neutral, out-of-the-blue contexts, there do not seem to be lexical items that share properties of being both emotive nouns and role nouns; that is, there are no nouns that allow licit patterns such as \textit{du Noun\textsubscript{1}} ‘you Noun’ as well as \textit{Du bist Noun\textsubscript{1}} ‘You are a Noun’. The generalization to be explained, then, is that emotive nouns cannot involve role nouns and that role nouns (in their literal meaning) are not emotive. Expressed in more structural terms, emotive nouns can occur in DPs but require \textit{ein} in TPs, and role nouns cannot occur in DPs but can be bare in TPs. As will become clear, these restrictions only hold in singular, pragmatically neutral contexts.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Before moving on, note that adding an evaluative adjective creates minimal pairs. However, this covers up the differences in \REF{ex:key:7} and \REF{ex:key:8} above, lexico-semantically (all nouns are equally fine now) and syntactically (all copulative cases must have \textit{ein} now):\text{} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%9
    \label{ex:key:9}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{du   blöder Idiot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you stupid   idiot.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (stupid idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.  \textit{du   blöder Landwirt} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you stupid  farmer.\textsc{masc}     
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (stupid farmer)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{du   blöder Mann} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you stupid  man.\textsc{masc}     
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (stupid man)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%10
    \label{ex:key:10}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Du  bist *(‘n) blöder Landwirt}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are      \textsubscript{} a  \textsubscript{} stupid farmer.\textsc{masc}    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a stupid farmer.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Du  bist *(‘n) blöder Idiot}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are      \textsubscript{} a  \textsubscript{} stupid idiot.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a stupid idiot.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Du  bist *(‘n) blöder Mann}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are      \textsubscript{} a  \textsubscript{} stupid man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a stupid man.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As can be seen in \REF{ex:key:10a}, modified role nouns must also take \textit{ein} in predicative contexts. This is explained in \sectref{sec:key:3.4.1}. Thus, in order to probe into the restrictions, I use unmodified nouns.\footnote{I concentrate on countable nouns (for the discussion of the concept of countability, see \citealt{Allan1980}, also Chapters 7 and 8), and I basically leave aside group nouns and \textit{pluralia tantum} nouns. Also, I abstract away from special cases such as “royal” \textit{ihr} ‘you’ and “nursely” \textit{wir} ‘we’.} In the introduction, I made a distinction of nouns as regards their meanings: neutral/literal vs. emotive/figurative. It is important to be more precise about this. We will see that emotiveness and figurativeness are not the same.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.2. Emotiveness and Figurative Extension
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Role and kind nouns can have two subtypes as regards their meaning. Focusing on the main cases discussed below, role nouns like \textit{Landwirt} have a neutral-only meaning (‘farmer’); others like \textit{Bauer} can take on an additional figurative meaning: besides ‘farmer’, \textit{Bauer} can also mean ‘peasant’ in certain contexts (\sectref{sec:key:2.2}).\footnote{Besides figurative, there are other terms found in the literature: metaphorical, extended, approximative, subjective, descriptive, gradable, and [+scalar]. Also, note that this difference in interpretative possibilities also holds true of regular kind nouns and proper names; for instance, while \textit{Obelisk} ‘obelisk’ and \textit{Joachim} ‘Joachim’ pattern with \textit{Landwirt} ‘farmer’, \textit{Säule} ‘column/pillar’ and \textit{Willi} ‘Willy/idiot’ align with \textit{Bauer} ‘farmer/peasant’.} Briefly, the figurative meaning of a role noun like \textit{Bauer} denotes certain stereotypical properties of a farmer (but there is no implication that this person is a farmer; more on this below). This figurative meaning is emotive. This means that while role nouns are not emotive in their literal meaning, they are in their figurative meaning. Kind nouns like \textit{Schwein} have a neutral-only meaning (‘pig’), but if applied to contexts involving humans, they can also take on a figurative meaning (‘swine/pig-like person’); other kind nouns like \textit{Idiot} have an emotive-only meaning (‘idiot’). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As regards emotiveness then, some nouns become emotive when “figuratively extended” in certain pragmatic contexts (\textit{Schwein}, \textit{Bauer}), but others are inherently emotive (\textit{Idiot}). This means that not all emotive readings are due to figurative extension. That said, it is important to elaborate on the figurative extension of \textit{Schwein} and \textit{Bauer} some more. In their neutral/literal meaning, these nouns denote properties like ‘pig’ or ‘farmer’; in their emotive/figurative meaning, these nouns denote certain features of a stereotypical representative of the relevant kind. In other words, while the individuals under discussion are ascribed certain features of a pig or farmer, there is no implication here that the individuals are actually pigs or farmers. Below, this type of reading is labeled ‘comparative’.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   In sum, while certain role nouns only have a neutral/literal meaning (\textit{Landwirt}), other such nouns may have an additional emotive/figurative one (\textit{Bauer}). Emotive kind nouns have an emotive meaning only (\textit{Idiot}), but neutral kind nouns may have an additional emotive/figurative meaning (\textit{Schwein}). Importantly, while a figurative meaning is emotive (\textit{Bauer}/\textit{Schwein}), an unambiguous emotive noun does not have a figurative meaning (\textit{Idiot}). In the summary \tabref{tab:key:1}, I distinguish the different types of nouns by the terms unambiguous and ambiguous (NB: since the neutral meaning of \textit{Schwein} ‘pig’ cannot apply to a human, I put it in parentheses; the negative value in parentheses indicates that this is not a possible reading in pragmatically neutral contexts):\footnote{As we will see below, role nouns in their literal meaning (e.g., \textit{Landwirt/Bauer} ‘farmer’) can take on an emotive component (‘very good/bad farmer’). The same goes for kind nouns (but this is not relevant for current purposes as humans cannot be pigs even in emotive contexts). Furthermore, emotive nouns like \textit{Idiot} ‘idiot’ cannot have a neutral reading and, as far as I can tell, they cannot have a figurative reading. As to the latter, note that \textit{Du bist ‘n Idiot!} does not seem to mean ‘You are an idiot-like person!’, which states that the person addressed only has certain features of an idiot (whatever those may be).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Different Types of Count Nouns}
\label{tab:key:1}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\multicolumn{2}{c}{Type of noun} & \multicolumn{2}{c}{ Role} & \multicolumn{2}{c}{ Kind}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\multicolumn{2}{c}{(Non-)ambiguity of the noun} & {\itshape Landwirt}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 Unamb. & {\itshape Bauer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 Ambiguous & {\itshape Idiot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 Unamb. & {\itshape Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 Ambiguous\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Meaning & Neutral/literal & ‘farmer’ & ‘farmer’ & {}- & (‘pig’)\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

& Emotive & (-) & (-) & ‘idiot’ & (-)\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

& Figur. (emot.) 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

[Comparative] & {}- & { ‘peasant’}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 [“farmer-like person”] & {}- & { ‘swine’}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 [“pig-like person”]\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that the word pairs \textit{Landwirt/Bauer} and \textit{Idiot/Schwein} in \tabref{tab:key:1} are intriguing in that one element is unambiguous and the other is ambiguous (again, with the qualification that the literal meaning of \textit{Schwein} ‘pig’ will not be relevant below). To account for this difference, some amount of lexical specification must be assumed (if so, we might also expect some dialectal and cross-linguistic variation in this regard, which I think is true; I return to the issue of lexical specification in \sectref{sec:key:3.1.2}). In combination with pronominal determiners, these different types of nouns yield three different readings, as explicated in the next subsection. These readings help organize the data in \sectref{sec:key:2.2} and the subsequent analysis in \sectref{sec:key:3}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.3. Three Different Readings
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Basically following den Dikken’s (2006: chapter 5) discussion of binominal structures of the type \textit{an idiot of a doctor}, I point out that there are three relevant interpretations in the cases under discussion here. Focusing for now on pronominal DPs \REF{ex:key:11}, I label the first case as ordinary reading. The second and the third case are called comparative and capacity readings:\footnote{Den Dikken labels the capacity reading as attributive, a term I would like to avoid.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%11
    \label{ex:key:11}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a. Ordinary reading: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{du   Idiot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. Comparative reading: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{du   Schwein}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you pig.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. Capacity reading: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{du   als Landwirt}     
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as  farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you as a farmer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I paraphrase each of these three readings as follows:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%12
    \label{ex:key:12}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           the unique x (informally addressed) such that
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     a.  x is an idiot
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     b.  x is, in a certain way, similar to a pig
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     c.  x is being singled out in the capacity of a farmer
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As we will see in the data section below, similar readings can also be identified in the TP. Before I turn to the data, some further remarks about the readings are in order here.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I point out that the ordinary reading in the DP is most often negatively emotive \citep{Vater2000}, but a positive connotation is also possible. In this regard, \citet{Rauh2004} provides the following example: \textit{du Glückspilz} ‘(you luck.mushroom =) lucky you’. Assuming that the pronoun and noun stand in a predication relation with one another (see \sectref{sec:key:3.2}), I suggest that this type involves direct predication. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The comparative reading is also emotive but involves nouns with emotive/figurative meanings. Unlike the first case, it involves indirect predication; that is, the predication seems to be mediated by a predicate such as \textit{like} or \textit{similar to}.\footnote{This is what I assume for now. Below, I suggest that the comparative reading follows from the realization operator REL.} Note again that epithetically used nouns like \textit{Schwein} ‘(pig =) idiot’ in \REF{ex:key:11b} characterize the individual in their entirety, but there is no implication that the individual being addressed is actually a pig, as reflected in the translation.\footnote{Recall that I translate the figurative meaning of animal names simply as ‘idiot’ (although such names may imply different attributes).}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Finally, in contrast to the first two cases, the capacity reading is neutral and features nouns with neutral/literal meanings. Like the second reading, it also involves indirect predication, which in this case is mediated by the element \textit{als} ‘as; in the capacity of’. As noted by Rauh (2004: 85-86, 94), these \textit{als-}nominals clearly presuppose the presence of other properties, but only the overt one following \textit{als} is taken to be relevant. In contrast to the comparative reading, which characterizes individuals in their entirety, the capacity reading zooms in on a certain aspect of the individual. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  With this in place, I turn to the data in more detail to see how role nouns and emotive or figurative kind nouns pattern as regards the three readings in various morpho-syntactic contexts. I compare the relevant readings in the DP to those of the TP. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.  Data
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I start with the data involving nouns in the plural. We will see that with the exception of the capacity reading, there are no restrictions in these cases. This is followed by unambiguous nouns in the singular, which reveal more restrictions. After providing an interim summary, I discuss ambiguous nouns in the singular, which exhibit a complementary loss of their potential meanings.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.1. Plural
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I begin with the two unambiguous nouns from \tabref{tab:key:1}. As can be seen in \REF{ex:key:13} and \REF{ex:key:14}, role as well as emotive kind nouns are equally possible in ordinary readings:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%13
    \label{ex:key:13}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{ihr   Landwirte}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you farmers
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you farmers’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ihr  seid Landwirte}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  farmers
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are farmers.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%14
    \label{ex:key:14}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{ihr  \textsubscript{} }\textit{Idioten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you idiots’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ihr  seid Idioten}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  idiots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are idiots.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Continuing with ambiguous kind nouns, something similar holds for the comparative reading with figuratively extended nouns:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%15
    \label{ex:key:15}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{ihr  Schweine} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you pigs
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you idiots’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ihr  seid Schweine.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  pigs 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are idiots.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As for the capacity reading, I note that only certain predicate nouns are possible. In particular, role nouns (and certain kind nouns, see below) are fine \REF{ex:key:16a}. However, as pointed out in \citet[93]{Lawrence1993} and \citet[86]{Rauh2004}, emotive and figuratively extended kind nouns are marked (16b-c). It seems to me that the latter also holds true of kind nouns that have a more general, hypernym-like denotation \REF{ex:key:16d}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%16
    \label{ex:key:16}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{ihr  als Landwirte} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as  farmers    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you as farmers’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. ??  \textit{ihr  als Idioten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as   \textsubscript{} idiots   
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. ??  \textit{ihr  als Schweine} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as  pigs         
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d. ??  \textit{ihr  als Personen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as  persons  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The restriction shown in (16b-d) is probably due to the fact that \textit{als} ‘as’ singles out a person with regard to a specific capacity or skill. Importantly, this quality must be restrictive enough such that only some people typically have it in a given context. In other words, while it is clear that not each and every person is a farmer \REF{ex:key:16a}, every person can presumably be in someone else’s bad books (16b-c). As to \REF{ex:key:16d}, every person is a human making \textit{Personen} ‘persons’ too general in meaning to be restrictive in the relevant way. To be clear, only \textit{Landwirte} ‘farmers’ in \REF{ex:key:16a} singles out the addressed individuals in the relevant way but the other nouns in \REF{ex:key:16} do not. I turn to the capacity reading in the clause after I discuss ambiguous nouns.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Turning to ambiguous role nouns such as \textit{Bauer} ‘farmer/peasant’, they are possible in the neutral/literal and emotive/figurative meanings in simple DPs and copular cases (17a-b), where both an ordinary and a comparative reading are possible. In contrast, the restriction discussed for \REF{ex:key:16} emerges again in \textit{als-}nominals \REF{ex:key:17c}. In other words, the capacity reading in \REF{ex:key:17c} is the sum of \textit{als} ‘as’ and the neutral/literal reading of the noun:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%17
    \label{ex:key:17}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{ihr  \textsubscript{} }\textit{Bauern} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you farmers/peasants
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you farmers/peasants’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ihr  seid Bauern.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  farmers/peasants
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are farmers/peasants.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{ihr  als Bauern} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as  farmers
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you as farmers’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \#‘you as peasants’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Unlike the DP cases as in \REF{ex:key:17c}, clauses do not involve \textit{als} ‘as’ in the capacity reading \REF{ex:key:18a}. However, this reading can, as an approximation, be brought out when the modal particle \textit{vielleicht} ‘really’ is added \REF{ex:key:18b}. Note that this example also gets an intensified comparative reading with \textit{Bauern} meaning ‘peasants’. What is interesting to note is that even an unambiguous role noun like \textit{Landwirt} ‘farmer’ can take on an emotive component in such a context, with a negative reading being somewhat easier to obtain \REF{ex:key:18c}:\footnote{Again, translations are not straightforward. Note that the element \textit{some} in the English translations is intended to convey the emotive flavor. Also, these constructions can take – what \citet[36]{Delsing1993} calls – an implicit argument. To illustrate with German, the dative pronoun \textit{mir} ‘me’ can be added to \REF{ex:key:18b}:  (i)  \textit{Ihr  seid mir vielleicht ‘n paar     Bauern!}    you are  me  \textsc{prt}           a couple farmers/peasants    ‘(To me,) you are some farmers/peasants!’Note that this often called ethical dative seems to bring out more clearly the capacity reading of \textit{Bauern} meaning ‘farmers’ such that the presence of other properties is presupposed, but only the overt predicate is taken to be relevant. I stick to the simpler cases in the main text.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%18
    \label{ex:key:18}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{Ihr  seid als Bauern}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  as  farmers/peasants
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ihr  seid vielleicht ‘n paar     Bauern!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  \textsc{prt}            a couple farmers/peasants
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are some farmers/peasants!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Ihr  seid vielleicht ‘n paar    Landwirte!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  \textsc{prt}            a couple farmers
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are some farmers!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Observe that the emotive reading in \REF{ex:key:18c} is not due to the properties of the noun itself or figurative extension. Rather, the modal particle \textit{vielleicht} plays a role here. Specifically, this particle invokes a [+scalar] capacity reading; that is, the persons being addressed are described as being good or bad in the capacity of farmers.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.2. Singular
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The cases in the singular are more restricted. While the same readings as above are in principle possible, some well-known restrictions with regard to the predicate noun emerge. As with the plural cases above, I start with unambiguous nouns in the ordinary reading. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Recalling that I focus for now on pragmatically neutral contexts, I already noted in the introduction that role nouns are not possible in the DP \REF{ex:key:19a} but that they pattern more freely in the TP. As also mentioned above, many speakers allow these nouns not only to occur without \textit{ein} but also with the indefinite article \REF{ex:key:19b}. Unlike role nouns, emotive kind nouns are possible in the DP \REF{ex:key:20a} but must co-occur with an article in the TP \REF{ex:key:20b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%19
    \label{ex:key:19}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a. ??  \textit{du   Landwirt}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Du  bist (‘n) Landwirt}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are    \textsubscript{} a  farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a farmer.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%20
    \label{ex:key:20}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{du   Idiot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Du  bist *(‘n)  Idiot}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are      \textsubscript{} an idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are an idiot.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In the comparative reading in \REF{ex:key:21}, the distribution is identical to the cases involving emotive kind nouns with ordinary readings (as just seen):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%21
    \label{ex:key:21}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{du   Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you pig.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Du  bist *(‘n) Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are      \textsubscript{} a  pig.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are an idiot.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In other words, kind nouns, be they inherently emotive or figuratively extended, pattern alike. Finally, I turn to the capacity reading.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Similar to the cases in the plural, only certain nouns are possible in \textit{als-}nominals:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%22
    \label{ex:key:22}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{du   als Landwirt} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as  farmer.\textsc{masc}    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you as a farmer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. ??  \textit{du   als Idiot} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as  idiot.\textsc{masc}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. ??  \textit{du   als Schwein} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as   pig.\textsc{neut}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d. ??  \textit{du   als Person}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as  person.\textsc{fem}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I assume that the restriction involved in (22b-d) is the same as that seen in the plural above.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In the clausal counterpart, the indefinite article is obligatory with role nouns, and these cases are emotive \REF{ex:key:23a}. Notice also that when emotive or figuratively extended kind nouns combine with the modal particle, the ordinary and comparative readings get intensified (23b-c):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%23
    \label{ex:key:23}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Du  bist vielleicht *(‘n) Landwirt!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  \textsc{prt}              a  \textsubscript{} farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are some farmer!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.  \textit{Du  bist vielleicht ‘n   Idiot!}      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  \textsc{prt}           an idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are really an idiot!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   c.  \textit{Du  bist vielleicht ‘n Schwein!}      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  \textsc{prt}           a pig.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are really an idiot!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, for completeness’s sake, I discuss neutral kind nouns that involve words for humans.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As already seen in \sectref{sec:key:2.1.1}, kind nouns such as \textit{Mann} ‘man’ are awkward in the DP \REF{ex:key:24a}. Importantly, although non-emotive, they nonetheless require \textit{ein} in the TP \REF{ex:key:24b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%24
    \label{ex:key:24}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a. ??  \textit{du   Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  you man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.   \textit{Du  bist *(‘n) Mann}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  you are      \textsubscript{} a  \textsubscript{} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a man.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These types of nouns do not occur with a comparative reading, but they do have a capacity reading:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%25
    \label{ex:key:25}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{du   als Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as  man.\textsc{masc}    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you as a man’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Du  bist vielleicht ‘n Mann!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  \textsc{prt}           a  man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are some man!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Before I discuss ambiguous role nouns like \textit{Bauer} ‘farmer/peasant’ in the singular, I briefly summarize the discussion thus far.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.3. Interim Summary
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with some remarks on emotiveness, the last two subsections have shown that unambiguous role nouns can take on an emotive component in the clausal capacity reading. The latter involves the modal particle \textit{vielleicht} ‘really’. In other words, emotiveness can be distinguished into an inherent property (\textit{Idiot}), it can be due to figurative extension of a lexical meaning (\textit{Schwein} ‘pig’ > ‘swine’, \textit{Bauer} ‘farmer’ > ‘peasant’), and it can be brought about in certain contexts where in conjunction with a modal particle, a neutral-only role noun may describe an individual performing their profession in an amazing or appalling way (\textit{Landwirt} ‘farmer’ > ‘some farmer’). The latter becomes particularly clear when evaluative adjectives like \textit{toller} ‘great’ or \textit{miserabler} ‘poor’ are added. In other words, the context where a neutral noun appears may change the reading of the noun. With this in place, I summarize the nominal and clausal cases according to morphological number and the three readings.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As can be seen in \tabref{tab:key:2}, DPs and TPs in the plural have basically the same readings – only the capacity reading differs in emotiveness:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Summary of the Readings in the Plural}
\label{tab:key:2}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXX}
 & Ordinary reading & Comparative reading & Capacity reading\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

DP & Emotive/Neutral & Emotive & Neutral [\textit{als}]\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

TP & Emotive/Neutral & Emotive & Emotive [\textit{vielleicht}]\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The singular cases are more restricted, especially in the ordinary reading. In more detail, the ordinary reading in the DP only allows (inherent) emotive kind nouns, but the two other readings in the DP are as in the plural. As for the TP, role nouns take an optional article in the ordinary reading, but kind nouns must have \textit{ein}. In the two other readings, nouns are emotive and require a determiner. Considering all three readings, \textit{ein} appears in both neutral and emotive contexts. This is summarized in \tabref{tab:key:3} (the subscript K stands for kind noun and the subscript R for role noun):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Summary of the Readings in the Singular}
\label{tab:key:3}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXX}
 & Ordinary reading & Comparative reading & Capacity reading\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

DP & Emotive [\textit{du Idiot}\textsubscript{K}] & Emotive [\textit{du Schwein}\textsubscript{K}] & Neutral [\textit{du als Landwirt}\textsubscript{R}\textit{/Mann}\textsubscript{K}]\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

TP & Neutral [(\textit{ein}) \textit{Landwirt}\textsubscript{R}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Emotive [\textit{ein Idiot}\textsubscript{K}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Neutral [\textit{ein Mann}\textsubscript{K}] & Emotive [\textit{ein Schwein}\textsubscript{K}] & Emotive [\textit{vielleicht ein Landwirt}\textsubscript{R}\textit{/Mann}\textsubscript{K}]\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that all comparative readings involve figurative extension and consequently are emotive. I make some more remarks. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with the singular (\tabref{tab:key:3}), I can point out again that the ordinary reading and the capacity reading are complementary as regards emotiveness in the DP. The absence vs. presence of \textit{als} ‘as’ seems to play a role here. Furthermore, emotiveness has less structure in the nominal domain (\textit{als} is not present) but more structure in the clausal domain (\textit{ein} must be present). In fact, the bidirectional entailment “non-emotive $\leftrightarrow $ \textit{als}” holds in the DP, and assuming that pronouns are determiners (Chapter 3, \sectref{sec:key:5}), the unidirectional entailment “emotive → determiner” holds in both domains. Finally, as mentioned above, the unidirectional entailment “no determiner → role noun” holds in the predicate nominal of the TP. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Interestingly, these entailments allow the combination of a role noun and \textit{ein} in the TP. This state-of-affairs is neatly summarized for the clause by de Swart \textit{et al}. (2005: 451), who point out that the reading of a bare (role) noun is basically a subset of the readings of the corresponding “\textit{ein} + noun” combination (also \citealt{Zamparelli2008}: 114). In other words, while a bare (role) noun can only be neutral/literal in meaning, the same noun preceded by \textit{ein} can be both neutral/literal (for many speakers) and emotive/figurative (for all speakers).\footnote{Note that de Swart \textit{et al}. (2007) seem to have retreated from this position. However, I believe their former generalization is correct. In this regard, see, for instance, the discussion of John F. Kennedy’s famous sentence \textit{Ich bin ein Berliner} in \citet{Eichhoff1993}, which for many speakers does not only have the emotive/figurative reading but also the neutral/literal one. I thank Veronika Ehrich for this reference and some discussion of this topic.}  Below, we will see that the occurrence of \textit{ein} is simply a side-effect and does not cause this figurative extension in interpretation.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Returning briefly to the plural (\tabref{tab:key:2}), the facts are somewhat different. First, \textit{als} ‘as’ does not have to be present for a neutral ordinary reading in the DP (e.g., \textit{ihr Linguisten} ‘you linguists’), and second, \textit{ein} is not present in the plural.\footnote{Note that non-canonical predicative contexts may involve plural \textit{ein}. This can be seen in the constructed example below: (i)  (?)  \textit{Ihr  seid vielleicht ein-e Bauern!}    you are   \textsc{prt}            a-\textsc{pl}  peasants/farmers    ‘You are some peasants!’    \%‘You are some farmers!’} With this in place, I finally turn to ambiguous role nouns in singular contexts. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.4.  Ambiguous Role Nouns in the Singular
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With the above discussion in mind, I consider examples involving \textit{Bauer} ‘farmer/peasant’. The expectation is that only some readings are possible in certain constructions. In fact, in the relevant cases, there should be a complementary loss of ambiguity. This is indeed borne out. To compare the distribution of the readings, I organize the data here according to syntactic domain.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Starting with the nominal domain, I showed in section 2.2.2 that unambiguous role nouns (e.g., \textit{Landwirt} ‘farmer’) exhibit degraded grammaticality in the ordinary reading but are fine in the capacity reading. If so, we expect that ambiguous nouns have a complementary loss of meaning. Specifically, we predict that ambiguous nouns only involve an (emotive) comparative reading in the DP and that they have a neutral capacity reading in \textit{als-}nominals. This is exactly what we find in \REF{ex:key:26a} and \REF{ex:key:26b}, respectively:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%26
    \label{ex:key:26}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{du   Bauer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you peasant.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (peasant)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \#‘you (farmer)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{du   als Bauer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as  farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you as a farmer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \#‘you as a peasant’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Turning to the clausal cases, a bare role noun should only have a neutral ordinary reading \REF{ex:key:27a}, a role noun with an indefinite article should be ambiguous between a comparative and a neutral ordinary reading \REF{ex:key:27b}, and a role noun in the context of the modal particle \textit{vielleicht} ‘really’ should be ambiguous between an intensified comparative and an emotive capacity reading \REF{ex:key:27c}. Again, this is exactly what the data bear out:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%27
    \label{ex:key:27}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Du  bist Bauer.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a farmer.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \#‘You are a peasant.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Du  bist ‘n Bauer.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are   a peasant/farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a peasant.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \%‘You are a farmer.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Du  bist vielleicht ‘n Bauer!}    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  \textsc{prt}           a peasant/farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are some peasant/farmer!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The discussion can be summarized in \tabref{tab:key:4}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Summary of the Readings of Ambiguous Role Nouns in the Singular}
\label{tab:key:4}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXX}
 & Ordinary reading & Comparative reading & Capacity reading\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

DP &  & Emotive [\textit{du Bauer}] & Neutral [\textit{du als Bauer}]\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

TP & Neutral [(\textit{ein}) \textit{Bauer}] & Emotive [\textit{ein Bauer}] & Emotive [\textit{vielleicht ein Bauer}]\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\tabref{tab:key:4} is similar to \tabref{tab:key:3}, except for the ordinary reading. Unlike the kind noun \textit{Idiot} ‘idiot’, the role noun \textit{Bauer} ‘farmer/peasant’ cannot have an ordinary reading – the latter is only available in the TP, and it must be neutral there. Furthermore, note that \textit{Bauer} is only ambiguous in the presence of \textit{ein}. However, the article by itself does not have an “emotivizing” function as it does allow a neutral ordinary reading of \textit{Bauer} in the TP. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Above, we saw that role nouns in clauses have optional indefinite articles but that kind nouns, independently of emotiveness, have obligatory indefinite articles. This difference between role and kind nouns is taken up in the next section. We will see that the appearance of \textit{ein} is a mere side-effect of the presence of a certain semantic operator such that \textit{ein} flags the presence of that operator (Hypothesis 3b). Thus, I continue to claim that \textit{ein} is semantically vacuous (Hypothesis 1a). I also provide an explanation of the other highlighted properties of the aforementioned constructions. The proposal is based on two important works, de Swart \textit{et al}. (2007) and \citet{Rauh2004}, that manifest an interesting division of labor, at least in the DP.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.  Proposal
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I account for the similarities and differences between pronominal DPs and their clausal counterparts. First, I lay out my assumptions. This includes the discussion of two previous proposals. Then I turn to the three readings in detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.1.   Combining Two Previous Proposals
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In order to account for the readings in the nominal and clausal domains, I basically follow de Swart \textit{et al}.’s (2007) discussion of the copular cases and extend it to pronominal DPs. Before I get into the specifics, I lay the groundwork.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.1.1. De Swart, \citet{WinterZwarts2007}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As mentioned before, this book is not about the semantics of the DP \textit{per se} (or the semantics of the TP, for that matter). Rather than providing detailed denotations, I use Type Theory to show that the relevant elements are semantically compatible with one another (for general background, see Chapter 1, \sectref{sec:key:4.2.2}; \citealt{HeimKratzer1998}). As discussed in Chapter 3, \sectref{sec:key:5}, I take pronominal elements like \textit{du} ‘you’ to be similar to definite articles like \textit{der} ‘the’; that is, pronouns are determiners.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As discussed in \sectref{sec:key:2.1.1}, common nouns come in two types: kind nouns and (a specific set of) role nouns.\footnote{De Swart \textit{et al.} (2007) call the latter capacity nouns. There are two notions of capacity now: an interpretative one (a certain type of reading) and a lexical one (certain groups of nouns). These notions are not identical. For instance, certain kind nouns may also occur in capacity readings (\sectref{sec:key:2}). In order to avoid confusion, I continue calling the relevant lexical items role nouns.} As is well known, kind nouns can be used to generically refer to a species (but not to individuals of that species) in cases like \REF{ex:key:28a}. \citet{Carlson1980} proposed that kind nouns are like names; that is, they are of type <e>. Assuming that generic determiners are expletives (cf. \citealt{Longobardi1994}: 655-59), kind nouns and verbal predicates (type <e,t>) can combine to yield a truth value (type <t>). Turning to \REF{ex:key:28b}, these types of cases are different. Here, the nominal refers to an individual of that species. De Swart \textit{et al}. (2007) follow \citet{Carlson1980} in arguing that the realization operator REL takes a kind noun (type <e>) and maps it to a set of individuals (type <e,t>). The (substantive) determiner (type <<e,t>, e>) combines with this nominal predicate returning an entity (type <e>). The latter can combine with a verbal predicate to yield a truth value:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%28
    \label{ex:key:28}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  The dinosaur is a big animal / is extinct.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  The dinosaur ate some fruit.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The novel claim of de Swart \textit{et al}. (2007) is that role nouns are also of type <e>. To map them into sets of individuals, they have to combine with the capacity operator CAP, a realization operator restricted to role nouns. To be clear then, both sorts of nouns are of type <e> and have to be mapped to type <e,t> if they are to function as predicate nouns. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  De Swart \textit{et al}. (2007) relate the different realization operators to different syntactic structures. Kind nouns combine with REL, where the latter is assumed to be in NumP \REF{ex:key:29a}. In contrast, de Swart \textit{et al}. (2007) propose that there are actually two options for role nouns. The first option involves a direct path, whereby role nouns combine with CAP. Note that CAP is in NP \REF{ex:key:29b}. The second option constitutes an indirect path, whereby role nouns undergo kind coercion resulting in kind nouns. De Swart \textit{et al.} (2007: 213) state that \textit{kind} coercion involves an operator as well (but I will not go into the specifics here to keep the discussion simple). The latter elements then combine with REL in NumP (29b’): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%29
    \label{ex:key:29}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  kind nouns:  N\textsubscript{<e>} + REL       → NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  role nouns:   N\textsubscript{<e>} + CAP       → NP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b’.  role nouns:  N\textsubscript{<e>} → kind coercion + REL  → NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that REL and CAP are both of type <e<e,t>> – these operators take an entity as their argument and return a predicate.\footnote{De Swart \textit{et al}. (2007: 217) argue that CAP is actually more complicated than <e<e,t>>. However, the latter simplication is sufficient for the current account (keeping in mind that CAP only combines with role nouns).} Furthermore, these operators are mutually exclusive; that is, when one applies, the other cannot. Crucially for the current discussion, REL is part of NumP, which triggers the presence of the indefinite article \textit{ein} ‘a’ (note that this is not further elaborated on in de Swart \textit{et al.}). For the most part, I follow Swart \textit{et al.}’s proposal. However, I refine it in certain ways below. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The two different paths to derive a predicate nominal in (29b-b’) are argued to explain the fact that bare role nouns are only neutral/literal in meaning, but role nouns preceded by \textit{ein} ‘a’ are both neutral/literal and emotive/figurative (Swart \textit{et al.}’s 2005 generalization, see \sectref{sec:key:2.2.3} again).\footnote{A different proposal is made by \citet[125]{Zamparelli2008}, who claims that role nouns are ambiguous between nominals that denote certain classes of human beings and nominals that denote abstract, well-established activities that identify those classes of human beings. Unlike the former, the latter nominals lack an inherent value for abstract gender, which, in turn, is taken to explain the absence of the indefinite article in predicative contexts. To the extent that I understand this correctly, this basically implies the existence of two lexical items for every role noun. In contrast, de Swart \textit{et al.}’s analysis takes one relevant lexical item as its point of departure and proposes two operations for the role noun to be able to function as a predicate. The latter approach appears to be more desirable.} As such, de Swart \textit{et al.} relate overt morphological clues directly to the relevant interpretative differences. Specifically, as predicate nominals, bare (role) nouns imply the presence of an NP, which in turn implies CAP \REF{ex:key:30a}. In contrast, plural suffixes on predicate nouns, exemplified in \REF{ex:key:30b} by -\textit{s}, are taken to be in NumP, and \textit{ein} is assumed to be in D in de Swart \textit{et al}. Recall from above though that I proposed that singular \textit{ein} is in ArtP, and this is what I assume for unmodified predicative nominals \REF{ex:key:30c}. Note that both \REF{ex:key:30b} and \REF{ex:key:30c} involve REL. Thus, the minimal structures of predicate nominals are as follows (also \citealt{Borer2005}: 67 fn. 4, \citealt{MunnSchmitt2005}: 827, cf. \citealt{Déprez2005}: 866):\footnote{De Swart \textit{et al}. (2007) make another important claim about these data. They propose that morphological number is actually not specified on nouns. In particular, bare nouns \REF{ex:key:30a} are number neutral (this is one of the properties mentioned in \sectref{sec:key:2.1.1}, discussed in more detail in Chapter 7), but nouns with plural morphology \REF{ex:key:30b} and nouns preceded by \textit{ein} \REF{ex:key:30c} are specified for plural and singular, respectively (the latter two are called ‘Marked Nominals’ by de Swart \textit{et al}.). Given the presence of NumP in the latter two cases, de Swart \textit{et al}. (2007: 214, 217) take morphological and semantic number to originate in NumP. This is compatible with my assumptions in Chapter 7.}\text{} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%30
    \label{ex:key:30}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  N  → [ NP ]      → CAP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  N-\textit{s}   → [ NumP [ NP ]]    → REL
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{ein} N  → [ ArtP [ NumP [ NP ]]]  → REL
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given (30a-c), it is clear that unmodified predicate nominals do not involve DPs. Rather, such nominals are of a smaller size; that is, in the absence of adjectival modifiers, predicate nominals preceded by \textit{ein} do not involve DP but ArtP (recall in this regard that \textit{ein} has no feature for definiteness and does not have to move to the DP-level).\footnote{In fact, even in the presence of adjectival modifiers, these nominals can be smaller than DP – \textit{ein} could be in Card, which is above AgrP. If this turns out to be tenable, then we could claim that DPs involve arguments, but not predicates (e.g., \citealt{Borer2005}: 65-66, \citealt{DéchaineWiltschko2002}: 419, \citealt{Stowell1989}; cf. also \citealt{Longobardi1994}: 628).} If so, then this allows me to state that \textit{ein} and REL are in a local relation (i.e., in adjacent phrases in underlying structure).\footnote{If REL were to turn out to be in ArtP (at least in the singular cases), both \textit{ein} and REL would be in the same phrase.} To be clear, considering \REF{ex:key:30}, predicate nominals can be of different sizes: NP, NumP, ArtP, and larger structures (if adjectival modifiers are present).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Comparing \REF{ex:key:30a} to \REF{ex:key:30c}, I note already here that \textit{ein} indicates the presence of structure beyond NP (Hypothesis 3a) and that \textit{ein} flags the presence of REL (Hypothesis 3b). The latter claim provides a reason for the appearance of \textit{ein}.\footnote{Notice that this comes close to one of Ackles’ (1996: 15) tentative suggestions that English \textit{a(n)} might be a dummy that makes the presence of NumP in an indefinite singular count noun phrase identifiable. Assuming the structural sequence of Q D Num N, she proposes that \textit{a(n)} is the minimal marker that is inserted in NumP when the latter is the leftmost empty phrase of a noun phrase.} Thus far, the proposal seems straightforward. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

However, it is not entirely clear which part of de Swart \textit{et al.}’s (2007) account derives the emotive/figurative reading. Although intuitively attractive at first glance, I show that the emotive/figurative reading does, most likely, not derive from kind coercion but from the presence of REL. Furthermore, as already briefly pointed out in \sectref{sec:key:2.1.2}, not all nouns undergo figurative extension (e.g., \textit{Landwirt} ‘farmer’). Thus, the figurative extension of the literal meaning (e.g., \textit{Bauer} ‘farmer’ > ‘peasant’) must take a certain lexical specification of the head noun into account. Finally, I extend de Swart \textit{et al.}’s (2007) proposal to pronominal DPs (note in this regard that de Swart \textit{et al}. 2007: 215 assume that ordinary DPs like \textit{the child} also involve REL). Consider these points in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.1.2. Lexical Features on Nouns
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As seen above, certain nouns can only be neutral/literal in meaning (e.g., \textit{Landwirt} ‘farmer’), but others can, under certain conditions, be ambiguous (e.g., \textit{Bauer} ‘farmer/peasant’). I start with the latter. It is clear from de Swart \textit{et al.}’s (2007) proposal that the emotive/figurative reading is not derived from the direct path involving CAP. If this were the case, a bare role noun in the TP could have an emotive/figurative reading, contrary to fact. This leaves the indirect path where kind coercion and REL are at work. With REL present, NumP will be projected, and the indefinite article will be present in ArtP too. Unlike the bare noun counterpart, \textit{ein Bauer} can have both a neutral/literal and a emotive/figurative reading, which is correct. The question then arises whether kind coercion or REL is responsible for the figurative reading. As we see momentarily, the answer is not entirely straightforward and involves several considerations. I wind up suggesting that in combination with a lexical feature, REL brings about the interpretative extension (\textit{pace} de Swart \textit{et al}. 2007).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  First, considering \textit{ein Landwirt} ‘a farmer’, we observe that an article is present with a role noun. This means that ArtP, NumP, and REL are present. With a structure larger than NP, kind coercion must have occurred. However, this nominal has a neutral/literal meaning only. This means that certain nouns do not undergo extension of their neutral/literal meaning. As such, nouns must be lexically specified as to whether or not they can undergo this interpretative extension. In other words, neither kind coercion nor REL automatically invokes the figurative extension.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Second, when applied to a person, names for animals (e.g., \textit{Schwein} ‘pig’) can only have an emotive/figurative meaning. Since these nouns are kind nouns to begin with, they do not undergo kind coercion. As such and making minimal assumptions, REL must be responsible for this extension in meaning (‘pig-like person/swine’).\footnote{Alternatively, it is possible to suggest here that a null predicate, call it LIKE, is responsible for the extension in meaning (cf. den Dikken’s 2006: 178 predicate SIMILAR in the comparative binominals). However, I proceed on minimal assumptions; that is, I assume that REL brings about the figurative extension.} However, REL performing this task cannot be the whole story. Certain other kind nouns (e.g., \textit{Katze} ‘cat’) do not seem to be able to undergo this extension easily or at all. This makes these kind nouns similar to the role noun \textit{Landwirt} (see also the word pairs in Footnote Error: Reference source not found). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I propose that certain vocabulary items (\textit{Bauer}, \textit{Schwein}) involve a lexical feature that allows figurative extension; other nouns (\textit{Landwirt}, \textit{Katze}) do not have this feature and cannot undergo this extension. With this in mind, I make the plausible assumption that \textit{Bauer} and \textit{Schwein} are similar in the relevant way (recall that the literal meaning of \textit{Schwein} ‘pig’ is excluded for pragmatic reasons when the relevant entity is anatomically not a pig). For concreteness, I assume that the relevant lexical items are specified as [+figurative] if they are able to undergo the extension. Thus, while both \textit{Bauer} and \textit{Schwein} are specified with this feature, \textit{Landwirt} and \textit{Katze} are not. Given that \textit{ein Bauer} can, for many speakers, also have a neutral/literal meaning, I assume that figurative extension is not obligatory (unless forced by the pragmatics).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To be clear then, I basically follow de Swart \textit{et al}. (2007) with the refinement that figurative extension is not obligatory, that it is due to REL (and not kind coercion), and that it is constrained by the feature [+figurative] present on certain nouns.\footnote{Notice that kind coercion is still needed to map a role noun to a kind noun, so that the noun can combine with REL.} Note again that kind coercion only applies to role nouns but figurative extension to both role and kind nouns. In other words, figurative extension does not entail kind coercion. However, figurative extension does yield a special case of emotiveness. More generally, if REL is indeed responsible for the (optional) figurative extension, then I can continue to claim that \textit{ein} makes no semantic, but only a morpho-syntactic, contribution. I reiterate the proposal that the main functions of \textit{ein} are indicating the size of the structure (Hypothesis 3a) and flagging the presence of an operator (Hypothesis 3b). In other words, flagging REL is the reason why \textit{ein} occurs in these contexts. Before I extend de Swart \textit{et al.}’s (2007) discussion to pronominal DPs, it is important to point out that the full explanation of the nominal cases involves a(nother) pragmatic aspect as part of the account.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.1.3. \citet{Rauh2004} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Thus far, I have only considered pragmatically neutral, out-of-the-blue examples for the ordinary reading in the DP \REF{ex:key:31a}. However, as \citet{Rauh2004} discusses in detail, these cases are perfectly fine given an appropriate context. Two of her examples may suffice here (31b-c) (stress is indicated here by capital letters):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%31
    \label{ex:key:31}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a. ??  \textit{\{ich} / \textit{du\} Linguist}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     \textsubscript{} I    / you linguist.\textsc{masc}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ihr Literaturwissenschaftler mögt den jetzigen Zustand für angemessen halten,       aber ich LINGUIST halte die Linguistik für weit unterrepräsentiert}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You literature scholars may consider the current status quo as adequate but I       (linguist) consider linguistics as quite underrepresented.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Wenn noch nicht einmal du LINGUIST die neue Rechtschreibung beherrschst,       wer sollte es dann}?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘If not even you (linguist) have mastered the new spelling rules, who else could       do that?’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

While I cannot do full justice to all the facets of her account here (e.g., the different stress patterns involved), Rauh’s basic proposal is based on the different deictic qualities of the personal pronouns involved and two Gricean maxims (\citealt{Grice1975}, 1978). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As a rule, \textit{ich} ‘I’ and \textit{du} ‘you(\textsc{sgl})’ are disambiguously specified in their deixis such that the identity of the person concerned is clear in a given context. Additional restrictive information provided by an NP complement (e.g., \textit{Linguist}) is not needed. In fact, by Grice’s maxim of quantity (informally: “be as informative as required but not more”), it is redundant and thus yields a marked string. Given a different, more involved context as in (31b-c) above, such an NP complement may make a relevant contribution, and as such it is allowed by Grice’s maxim of relation (informally: “be relevant”). Specifically, this contribution involves a contrastive or emotive component; compare \REF{ex:key:31b} to \REF{ex:key:31c}. With this in mind, emotive NP complements such as \textit{Idiot} ‘idiot’ are always relevant and thus always possible. Finally, plural pronouns are less deictically specified and thus allow restrictive material in the complement position. I relate \citegen{Rauh2004} proposal for the DP to de Swart \textit{et al}.’s (2007) account of the TP.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As I discuss in detail in Chapter 7, all DPs including pronominal DPs must involve NumP, and as such REL is always present there. With de Swart \textit{et al}.’s discussion of the clause in mind, this makes both a neutral and an emotive noun, at least in principle, possible inside the complement position of a pronominal determiner. Now, recalling \citegen{Rauh2004} pragmatic account of singular DPs, only an emotive complement remains possible in pragmatically neutral, out-of-the-blue contexts. In contrast, singular DPs in a different, appropriate context or plural DPs in general can have neutral or emotive NP complements. To be clear, then, the explanation of pronominal DPs involves a straightforward extension of Swart \textit{et al.} (2007), once we assume that NumP (and thus REL) is always present there and that the explanation must also involve pragmatic considerations. Conversely, predicate nominals in TPs make an assertion, and as such, they provide informative and relevant information. Consequently, they are in agreement with \citegen{Rauh2004} assumptions, here extended to TPs. Finally, combining these two proposals also explains the fact that in pragmatically neutral, out-of-the-blue contexts, there are no true minimal pairs between pronominal DPs and copular TPs when both involve bare predicate nouns (for more details, see \sectref{sec:key:3.3}). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In the next section, I turn to the specifics of my proposal. I focus on the cases in the singular where I have identified most restrictions and where \textit{ein} is present in emotive contexts. Before doing so, I summarize in \tabref{tab:key:5} the cases in the singular (without \textit{als} ‘as’), discussed in this and the previous section. Note that the first column indicates the inherent (non-)emotiveness of the head noun but that the second column shows the readings that noun may take on in DPs and TPs. These readings are exemplified in the third and fourth columns:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Nouns and their Readings in the DP and TP}
\label{tab:key:5}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Noun & Reading & DP & TP\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Neutral & Literal & {\itshape du LANDWIRT}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ ‘you farmer’}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 (stressed - contrastive) & { \textit{Du bist (‘n) Landwirt}.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 ‘You are a farmer.’\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Emotive & Emotive & {\itshape du Idiot}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 ‘you idiot’ & {\itshape Du bist ‘n Idiot!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 ‘You are an Idiot!’\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Neutral & Emotive/figurative [comparative] & {\itshape du Schwein/Bauer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 ‘you idiot/peasant’ & {\itshape Du bist ‘n Schwein/Bauer!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 ‘You are an idiot/peasant!’\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Neutral & Emotive & {\itshape du LANDWIRT}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ ‘you farmer’}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 (stressed - emotive) & {\itshape Du bist vielleicht ‘n Landwirt!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 ‘You are some farmer!’\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note again that all the emotive readings have a determiner including \textit{ein} in singular predicate nominals. However, it is clear from the optionality of \textit{ein} in clausal cases like \textit{Du bist (‘n) Landwirt} that the presence of \textit{ein} does not necessarily lead to emotiveness. This fact is consistent with the proposal that \textit{ein} is semantically vacuous. This means that in instances where \textit{ein} is present in emotive contexts, \textit{ein} must be due to a different reason. I proposed above that \textit{ein} flags the presence of a null operator. With this in place, I turn to the explanation of the individual readings in the DP and TP starting with the – what I have called – ordinary reading. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.  Ordinary Reading
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I propose that the ordinary reading involves direct predication. What I mean by that is that the pronoun and noun combine with one another without invoking a figurative extension of the literal meaning of the head noun. This is different from the comparative reading discussed in the next section. First, I address the nominal domain and then the clausal one.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.1. Ordinary Reading in the DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As discussed in Chapter 3, I assume that pronominal DPs consist of a head noun and a pronominal determiner. To keep the exposition simple, I abstract away from the lower base position of the determiner and its movement to the DP-level. I assume here and argue below that NumP is sandwiched between the head noun and the pronominal determiner. As such, pronominal DPs exhibit a regular DP structure (see Chapter 1, \sectref{sec:key:4}). Consider the example in \REF{ex:key:32a} and its structural analysis in \REF{ex:key:32b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%32
    \label{ex:key:32}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{du \textsubscript{} }\textit{Idiot} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you idiot.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. \textit{Ordinary Reading in DP}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    DP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          (Funtional Application)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{du}\textsubscript{<<e,t>e>}       NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            (Functional Application)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       Num\textsubscript{REL<e<e,t>>}       NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 \textit{Idiot}\textsubscript{<e>}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I turn to the semantics. With \sectref{sec:key:3.1.1} in mind, I restrict myself to giving the semantic types of the relevant elements and how they are combined. This is shown in \REF{ex:key:32b}. Putting it into words and starting at the bottom of the tree, the kind noun \textit{Idiot} ‘idiot’ combines with the REL operator in NumP to return a predicate nominal. I take \citegen{Postal1966} claim that pronouns are determiners at face value. In other words, I assume that \textit{du} ‘you(\textsc{sgl})’ is a pronominal determiner that is semantically similar to an ordinary definite determiner.\footnote{In Chapter 7, I discuss the ungrammaticality of cases like \textit{*du ‘n Idiot} ‘(lit.) you an idiot’. I assume that pronominal determiners can flag the presence of REL preventing the occurrence of \textit{ein} (also \sectref{sec:key:3.4.1}). This makes these types of determiners similar to ordinary determiners like definite articles (see de Swart \textit{et al}. 2007: 215).} Given that, Functional Application can apply to the predicate nominal and the pronominal determiner resulting in an entity with the following reading: the unique x (informally addressed) such that x is an idiot. The derivation is the same for \textit{Mann} ‘man’ but requires a special pragmatic context \citep{Rauh2004}. Similar assumptions hold for \textit{Landwirt} ‘farmer’, but the latter undergoes kind coercion. Since \textit{Landwirt} is not marked by [+figurative], figurative extension does not occur.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Before I discuss the clausal cases, I need to be more explicit about pronouns that have no noun as their complement. Similar to what we have just seen, I assume that they are not simply “intransitive” but more complex. In particular, consistent with Chapter 3, \sectref{sec:key:5}, I assume that there is actually a head noun. I suggest that this head is an unpronounced kind noun, indicated below as \textit{e\textsubscript{N}} (cf. PERSON in \citealt{Rauh2004}: 89). The latter combines with REL to give a predicate nominal. This complex null element, semantically REL(\textit{e\textsubscript{N}}), can now function as an argument to the pronominal functor resulting in an entity:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%33
    \label{ex:key:33}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Pronouns Without Overt Complement}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    DP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{du}\textsubscript{<<e,t>e>}       NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

       Num\textsubscript{REL<e<e,t>>}       NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   \textit{e\textsubscript{N}}\textsubscript{<e>}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that this is the same abstract structure and derivation as in \REF{ex:key:32b} above.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.2. Ordinary Reading in the TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

We saw in \sectref{sec:key:3.1.1} that predicate nominals can be of different sizes: NP, NumP, ArtP, or larger (if an adjectival modifier is present). As proposed in the previous subsection, pronominal DPs involve regular structures that consist of NPs, NumPs, ArtPs, and other phrases. I propose that predicate nominals in copular TPs are different. Unlike DPs, I claim that predicate nominals in copular clauses do not necessarily involve NumPs, ArtPs, or other phrases – they can just be NPs. Now, similar to the DP, the ordinary reading in the clause does not involve figurative extension either. Adopting fairly traditional tree representations, I assume that the clause involves a Predication Phrase (PrP, \citealt{Bowers1993}) where the head Pr selects NP.\footnote{There are other possibilities; for instance, the auxiliary could select a Small Clause (e.g., \citealt{Hoekstra1984}: 231). Note also that \citet{Bowers1993} employs PrP in the DP. However, I follow here the more familiar structural view of DPs already discussed above. The resulting differences in names for functional phrases should not be taken as an indication that the DP and TP are not parallel.} The underlying structure of \REF{ex:key:34a} is given in \REF{ex:key:34b} (for simplicity, I abstract away here from further movements to the left, see Chapter 7):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%34
    \label{ex:key:34}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Du  bist Bauer}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  farmer.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a farmer.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. \textit{Ordinary Reading in TP with Role Nouns}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    PrP\textsubscript{<t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [\textit{du} REL e\textsubscript{N}]\textsubscript{<e>}  Pr’\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{bist}    NP\textsubscript{CAP<e<e,t>>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{\textsubscript{} }\textit{Bauer}\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As to the semantics, \textit{Bauer} is a role noun, and unlike kind nouns it can combine with CAP. The latter is located in NP and brings about a predicate nominal. Above, we saw that “intransitive” pronouns like \textit{du} ‘you(\textsc{sgl})’ actually involve pronominal DPs; that is, they involve entities enclosed by square brackets in \REF{ex:key:34b}. Recalling that the copula is semantically vacuous, the pronominal DP and the predicate nominal combine by Functional Application to yield a truth value. Note now that \REF{ex:key:34a} cannot have the emotive/figurative reading ‘peasant’. This is so because CAP (but not REL) brought about the predicate nominal involving \textit{Bauer}. Finally, in order to account for the full grammaticality of these types of examples, observe that the predicate noun makes an assertion about the subject, and as such it is informative and relevant from a pragmatic point of view. The same holds if we exchange \textit{Bauer} with \textit{Landwirt} ‘farmer’.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, it is worth mentioning again that the combination of “\textit{ein} + N” can also result in ordinary readings \REF{ex:key:35a}. With \textit{Idiot} ‘idiot’ an emotive kind noun, it combines with REL in NumP bringing about \textit{ein} in ArtP. The remainder of the derivation proceeds as above and is illustrated in \REF{ex:key:35b}: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%35
    \label{ex:key:35}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Du  bist ‘n  Idiot.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are   an idiot.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            ‘You are an idiot.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. \textit{Ordinary Reading in TP with Kind Nouns}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    PrP\textsubscript{<t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [\textit{du} REL e\textsubscript{N}]\textsubscript{<e>}  Pr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{bist}            ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      \textit{‘n}          NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           Num\textsubscript{REL<e<e,t>>}  NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                    \textit{Idiot}\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The same holds if the noun involves \textit{Mann} ‘man’. As to \textit{ein}, note again that this element does not have an emotivizing function (e.g., \textit{ein Mann} ‘a man’); that is, \textit{ein} is an expletive element and does not participate in the semantic derivation. The ambiguous noun \textit{Bauer} ‘farmer/peasant’ preceded by \textit{ein} will be discussed in the next section (comparative readings). This is also where I comment on the optional presence of \textit{ein} in the ordinary reading in the TP (e.g., \textit{Du bist (‘n) Bauer}. ‘You are a farmer.’).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To summarize, the ordinary reading is emotive in the nominal domain but neutral or emotive in the clausal domain. Both cases involve direct predication; that is, there is no figurative extension in the DP (i.e., the head noun is inherently emotive), and the TP involves a role noun in combination with CAP or an emotive or neutral kind noun combining with REL. Next, I discuss cases with figurative extension.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.3.  Comparative Reading
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Before I provide the structures of the relevant DPs and TPs, I start with some general considerations.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.3.1. Preliminaries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As a point of departure, I begin with more complex comparative structures. It is well documented that an attributive adjective and its head noun must agree in phi-features yielding concord. This means that the adjectives in \REF{ex:key:36} below are in the extended projection of their following noun (Chapter 1, \sectref{sec:key:4}). In Chapter 3, \sectref{sec:key:5}, I proposed that pronominal DPs involve canonical structures where the adjective is in the specifier just below the determiner:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%36
    \label{ex:key:36}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{ihr               dumm-en   Schweine}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you\textsc{.pl.nom} stupid-\textsc{wk} pigs
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you stupid idiots’    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{mir               groß-en   Gans}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    me\textsc{.sgl.dat} great-\textsc{wk} goose.\textsc{fem}           
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘me (stupid idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In other words, these comparative constructions have the pronominal determiner, the adjective, and the noun in a regular DP structure, just like with the ordinary reading. I proceed with examples involving unmodified nouns.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.3.2. Comparative Reading in the DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that I proposed that the comparative reading involves a figurative extension of the neutral/literal meaning of the head noun; for example, the neutral/literal meaning of \textit{Bauer} ‘farmer’ is extended to ‘farmer-like person’ or ‘peasant’. I proposed that this is due to REL. This fits well with the assumption that NumP is present in the DP. The example in \REF{ex:key:37a} is derived as in \REF{ex:key:37b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%37
    \label{ex:key:37}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{du   Bauer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you peasant.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              ‘you (peasant)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \#‘you (farmer)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. \textit{Comparative Reading in DP}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    DP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{du}\textsubscript{<<e,t>e>}       NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        Num\textsubscript{REL<e<e,t>>}  NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               \textsubscript{} \textit{Bauer}\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As for the interpretation, the role noun \textit{Bauer} undergoes kind coercion and combines with REL in NumP. As the noun is lexically marked as [+figurative], it can undergo figurative extension. Unlike the neutral/literal reading, this extension yields emotiveness and is felicitous in pragmantically neutral, out-of-the-blue contexts \citep{Rauh2004}. Next, NumP combines with the pronoun resulting in an entity with the following reading: the unique x (informally addressed) such that x is, in some way, similar to a farmer. Similar considerations apply to \textit{Schwein} ‘pig’, with the proviso that the interpretation can only be ‘pig-like person’ or ‘swine’ for pragmatic reasons.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.3.3. Comparative Reading in the TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If the above discussion of the DP is tenable, then the comparative reading in the TP should involve a similar account. The example in \REF{ex:key:38a} is derived as in \REF{ex:key:38b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%38
    \label{ex:key:38}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Du  bist ‘n Bauer.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are   a peasant/farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a peasant.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \%‘You are a farmer.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. \textit{Comparative Reading in TP}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    PrP\textsubscript{<t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [\textit{du} REL e\textsubscript{N}]\textsubscript{<e>}  Pr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{bist}            ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      \textit{‘n}          NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           Num\textsubscript{REL<e<e,t>>}  NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   \textit{Bauer}\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Like above, \textit{Bauer} undergoes kind coercion and then combines with REL in NumP. Structurally, this brings about \textit{ein} ‘a’ in ArtP. Semantically, this complex element, namely REL(\textit{Bauer}), combines with the pronominal DP to return a truth value. Note that \textit{Bauer} has the option of keeping its neutral/literal meaning or undergoing figurative extension. This is so because this noun is specified [+figurative], but the figurative extension by REL is not obligatory. This derives the two readings in \REF{ex:key:38a} above. The same basic derivation as in \REF{ex:key:38b} applies to \textit{Schwein} ‘pig’, with the same proviso mentioned for the DP above.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Returning to an issue left open in the last section, notice that the ordinary reading of the role noun involves the indirect path in \REF{ex:key:38}, and as such it entails more structure and more operations than the direct path (\sectref{sec:key:3.1.1}). This might explain why the combination of \textit{ein} and \textit{Bauer} in its neutral/literal meaning is somewhat less easily available or even absent for some speakers. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Finally, recall also that the modal particle \textit{vielleicht} ‘really’ can intensify the comparative reading resulting in an interpretation close to ‘really like’. Consonant with the above discussion and adding an adjective to the predicate nominal \REF{ex:key:39a}, the noun and its modifier are in their regular position \REF{ex:key:39b}: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%39
    \label{ex:key:39}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Du  bist vielleicht ‘n echter Bauer!}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  you are  \textsc{prt}           a  real     peasant.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘You are really like a peasant!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.       CardP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      \textit{‘n}            AgrP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            (Predicate Modification)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          \textit{echter}\textsubscript{<e,t>}          NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         Num\textsubscript{REL<e<e,t>>}  NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                 \textit{Bauer}\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that the adjectival predicate and the nominal predicate combine by Predicate Modification.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Taking stock, the comparative reading in the nominal and clausal domain involves the operator REL in NumP. In combination with a [+figurative] role noun, this results in an emotive/figurative reading in the DP and in the TP (although a neutral/literal reading is not excluded in the latter domain). Note now that the interaction between de Swart \textit{et al}. (2007) and \citet{Rauh2004} also affords us an explanation of why there are no true minimal pairs of the type \textit{du Noun\textsubscript{1}} ‘you Noun’ and \textit{Du bist Noun\textsubscript{1}} ‘You are a Noun’ in pragmatically neutral contexts. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Specifically, all pronominal DPs involve NumP and thus REL. The latter may bring about the emotive/figurative extension of the meaning of the head noun. Now, Rauh’s pragmatic proposal explains why only inherently emotive kind nouns (\textit{Idiot}), [+figurative] kind nouns (\textit{Schwein}), and [+figurative] role nouns (\textit{Bauer}) are possible in the DP. In contrast, de Swart \textit{et al.} argue that bare predicate nouns in the TP only involve NP and thus CAP. With REL absent here, this only allows the occurrence of role nouns, with the feature [+figurative] (\textit{Bauer}) or without (\textit{Landwirt}). Due to the absence of REL, \textit{Bauer} can only have the neutral/literal meaning like \textit{Landwirt}. To be clear, then, Rauh’s proposal forces the bare noun to be emotive in the DP, and de Swart \textit{et al}.’s analysis explains why the bare noun can only be neutral in the TP. If so, the absence of minimal pairs should not be taken as an argument that DPs and TPs are fundamentally different after all – the difference is explained by the interplay of the two proposals above, which involve pragmatic considerations. I turn to the last reading.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.4.  Capacity Reading
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Before I discuss the detailed analyses of the DP and TP, I need to provide some background information motivating the relevant structures.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.4.1. Preliminaries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

We have seen that DPs involving \textit{als} ‘as’ have a (neutral) capacity reading \REF{ex:key:40a}. The latter can be paraphrased as ‘in the capacity of / with regard to being’. In the TP \REF{ex:key:40b}, the modal particle \textit{vielleicht} ‘really’ invokes an emotive capacity reading similar to ‘bad/good in the capacity of / with regard to being’, and it intensifies the comparative reading (for the structure of the latter, see previous section):\footnote{De Swart \textit{et al}. (2007: 218) assume that the combination of “\textit{als} + NP” is a verb modifier. Note though that this does not work for \textit{als}{}-nominals that are part of pronominal DPs: given the Verb Second Constraint in German, the latter clearly involve constituents: \textit{Du als Arzt solltest das nicht machen} ‘You as a doctor should not do that’.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%40
    \label{ex:key:40}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{du   als Bauer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as  farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you as a farmer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \#‘you as a peasant’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Du  bist vielleicht ‘n Bauer!}    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are  \textsc{prt}           a  farmer/peasant.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are some farmer/peasant!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with the DP, I make the fairly straightforward proposal that \textit{als} ‘as’ brings about the capacity reading. As to the TP, I would like to suggest that in combination with the modal particle, the null equivalent of \textit{als} (i.e., ALS) derives the corresponding clausal reading. Like all null elements, ALS has to be licensed. I assume that the modal particle is responsible for that. Schematically, we arrive at the following, where the relevant noun phrases of the data in (40a-b) are put in brackets in (41a-b):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%41
    \label{ex:key:41}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  [\textit{du als Bauer}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{vielleicht} [\textit{ein} ALS \textit{Bauer}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The bracketed strings are of particular relevance below. To determine the syntactic properties of these two capacity constructions, I consider the latter in combination with third-person pronouns. As is well known, these pronouns are syntactically very restrictive and therefore help us find a plausible structural analysis.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Unlike pronouns of the first and second person, pronouns of the third person cannot directly combine with adjectives and/or overt nouns (see \citealt{Höhn2020} and Chapter 3, \sectref{sec:key:5}). Compare \REF{ex:key:42a} to (42b-d): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%42
    \label{ex:key:42}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{er als Bauer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he as  farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘he as a farmer’ 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. *  \textit{er Gute(r)} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he  good     
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. *  \textit{er Bauer \textsubscript{} }
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he farmer.\textsc{masc}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d. *  \textit{er gute(r) Bauer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he good    farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

However, they can be modified by relative clauses \REF{ex:key:43a} (see \citealt{Rauh2003}, \citealt{Vater1985}). In other words, I take \REF{ex:key:42a} to be on a par with \REF{ex:key:43a}. Assuming the traditional adjunction analysis for relative clauses, I propose that \textit{als Bauer} is also adjoined to the pronoun (cf. \citealt{Rauh2004}: 94), schematically illustrated in \REF{ex:key:43b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%43
    \label{ex:key:43}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{er, der  gerade durch    die Tür  gekommen ist}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he who just      through the door come         is
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘he, who just came through the door’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  Adjunction-type Analysis
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{du} [ \textit{als Bauer} ]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Turning to the clausal counterpart \REF{ex:key:41b}, that is, \textit{ein} ALS \textit{Bauer}, recall from Chapter 2 that \textit{ein} ‘a’ cannot have an ending in certain morpho-syntactic contexts when an adjective and/or a noun follows. This holds independently of the presence or absence of the modal particle. Compare \REF{ex:key:44a} to (44b-d):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%44
    \label{ex:key:44}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Er ist vielleicht ein    (guter) Bauer!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is  \textsc{prt}           a/one good   farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘He is some great farmer!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. *  \textit{Er ist (vielleicht) ein-er     Gute(r)} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is    \textsc{prt}           a/one\textsc{{}-st} good     
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. *  \textit{Er ist (vielleicht) ein-er     Bauer \textsubscript{} }
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is    \textsc{prt}           a/one-\textsc{st} farmer.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d. *  \textit{Er ist (vielleicht) ein-er     gute(r) Bauer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is    \textsc{prt}           a/one-\textsc{st} good    farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Rather, inflected \textit{ein} is only possible here when the modifier is a relative clause (a genitive DP, or a PP) or when no relevant element follows at all \REF{ex:key:45a}. Thus, in order to explain the absence of the inflection on \textit{ein} in \REF{ex:key:41b}, I propose that similar to regular adjectives, ALS \textit{Bauer} is in a specifier. This is shown in simplified form in \REF{ex:key:45b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%45
    \label{ex:key:45}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Er ist ein-er (, der  viel     arbeitet)}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is  one-\textsc{st}   who much works
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘He is one that works a lot.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  Specifier-type Analysis
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{ein} [ ALS \textit{Bauer} ] \textit{e\textsubscript{N}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up thus far, we have seen some evidence that \textit{als Bauer} is most likely in an adjoined position and that ALS \textit{Bauer} is in a specifier position. Importantly, de Swart \textit{et al.} (2007: 200) and others have noted that modifiers such as relative clauses and adjectives make optional determiners obligatory. This is particularly clear with role nouns (46a-b), even if such a noun is not pronounced \REF{ex:key:46c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%46
    \label{ex:key:46}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Er ist *(‘n) Bauer,           der \textsubscript{} }\textit{viel    arbeitet}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is       a  \textsubscript{} farmer.\textsc{masc} that much works
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘He is a farmer that works a lot.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Er ist *(‘n) guter Bauer}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is       a  \textsubscript{} good farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘He is a good farmer.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Peter} \textit{ist ‘n schlechter Bauer,          aber Hans ist *(‘n) guter}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter is    a bad           farmer.\textsc{masc} but   Hans is       a  \textsubscript{} good 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Peter is a bad farmer, but Hans is a good one.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is clear then that modifiers in both adjoined and specifier positions require the presene of \textit{ein}. I propose that \textit{ein} is present because the modifiers in \REF{ex:key:46} combine with their nouns by Predicate Modification. Notice that this operation conjoins elements of type <e,t>.  Now, for the noun to be of this type, it must have combined with REL first. As we know from above, this operator implies the presence of NumP and thus \textit{ein}.\footnote{For some discussion of why CAP cannot be involved here, see Chapter 7.} Now, assuming that nominals involving a noun and ALS (or \textit{als} ‘as’) are also predicates, I point out that REL is also present, and this explains the presence of \textit{ein} in \REF{ex:key:45b}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I briefly return to the analysis of the first case, \textit{du als Bauer} in \REF{ex:key:43b}. Observe that this string involves adjunction just like \REF{ex:key:46a} above. However, unlike \REF{ex:key:46a}, there is no \textit{ein} in \REF{ex:key:43b}. In order to address this difference, recall the discussion from \sectref{sec:key:3.2.1}, where \textit{du} itself was proposed to involve a complex structure. Specifically, \textit{du} is a determiner with NumP and a null noun in its complement structure. I update \REF{ex:key:43b} as in \REF{ex:key:47a}. Notice now that this makes \REF{ex:key:47a} and \REF{ex:key:46a} completely parallel in that the adjoined material (in brackets) is preceded by a structurally complex nominal:\footnote{To the extent that \textit{ein} can be followed by \textit{als Bauer}, it must have an inflection: \textit{ein*(-er) als Bauer}. This fits well with the adjunction-type analysis of \textit{als Bauer} in \REF{ex:key:47a}.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%47
    \label{ex:key:47}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{du} REL e\textsubscript{N} [\textit{als Bauer}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{‘n} REL \textit{Bauer} [\textit{der viel arbeitet}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To explain the absence of \textit{ein} in the first nominal in \REF{ex:key:47a}, I suggest that elements like pronominal determiners can also flag REL. This is compatible with de Swart \textit{et al}.’s assumptions about regular DPs like \textit{the child}. In other words, besides \textit{ein} and the definite article, other determiners can also indicate the presence of REL. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up, \textit{du als Bauer} involves adjunction of \textit{als Bauer}, and \textit{ein} ALS \textit{Bauer} contains a complex specifier occupied by ALS \textit{Bauer}. The determiners \textit{du} and \textit{ein} are present due to REL. The absence of the inflection on \textit{ein} is explained by the presence of an overt element in the specifier just below \textit{ein}. With this much in place, I consider the relevant analyses in more detail. I start with the (neutral) capacity reading in the DP.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.4.2. Capacity Reading in the DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I have proposed that \textit{du} itself involves a complex structure and that \textit{als Bauer} is adjoined to that structure as in \REF{ex:key:47a} above. I assume that this type of adjunction is instantiated by a Modifier Phrase (ModP; see Chapter 2, Sections 3.2 and 3.3). I interpret \textit{als} as the head of ModP, and this head takes \textit{Bauer} as a complement. The head \textit{als} brings about the capacity reading of the DP. I derive \REF{ex:key:48a} as \REF{ex:key:48b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%48
    \label{ex:key:48}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a. \textit{du   als Bauer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you as  farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you as a farmer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \#‘you as a peasant’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. \textit{(Neutral)} \textit{Capacity Reading Licensed DP-internally}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         DP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{du}\textsubscript{<<e,t>e>}            NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              (Predicate Modification)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          NumP\textsubscript{<e,t>}                     ModP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Num\textsubscript{REL<e<e,t>>}     NP       \textit{als}           NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                       \textit{e\textsubscript{N}}\textsubscript{<e>}                    \textit{Bauer}\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Observe that the lower NumP and ModP are both of type <e,t> (I comment on the role of \textit{als} momentarily). I assume that they combine by Predicate Modification. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I point out that \textit{als-}nominals are special in that \textit{als} also combines with bare kind nouns such as \textit{Mann} ‘man’ in appropriate contexts \REF{ex:key:49a}. In fact, the presence of \textit{ein} leads to awkwardness \REF{ex:key:49b}: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%49
    \label{ex:key:49}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Was  würdest du   als Mann        dazu sagen?}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what would    you as  man.\textsc{masc}\textsubscript{} it.to  say
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘What would you as a male say about this?’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  ?  \textit{Was  würdest du   als ‘n Mann         dazu sagen?}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what would    you as   a  man.\textsc{masc}\textsubscript{} it.to  say
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘What would you as a male say about this?’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Since a kind noun is involved here, CAP cannot be at work in the adjoined structure. Furthermore, if REL were at work here, we would expect that the presence of \textit{ein} is fully grammatical, contrary to fact. I assume that \textit{als} is a general(ized) capacity operator that maps role and kind nouns alike into predicate nominals; that is, \textit{als} is of type <e<e,t>>. I discuss this in more detail in Chapter 7. I turn to the data from the clausal domain.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.4.3. Capacity Reading in the TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As illustrated many times, adjectives exhibit strong endings after uninflected \textit{ein}. In Chapter 2, I discussed two options to analyze this: either the adjective is in the regular specifier position, and the strong ending follows from the special morpho-syntactic properties of \textit{ein}, or alternatively, the adjective is in a non-canonical position where its ending cannot undergo Impoverishment in the first place. As discussed in that chapter, one such non-canonical position involves the adjective to be deeply embedded in a complex specifier. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall now that the combination of “\textit{vielleicht} + \textit{ein} N” has two readings. I made use of the first option mentioned above (the adjective is in its regular position) with the intensified comparative reading in \sectref{sec:key:3.3.3}. The second option (the adjective is more deeply embedded) was proposed for ALS-nominals in the preliminary section (see \REF{ex:key:45b} above). I now turn to the second option in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Specifically, I propose that cases with the emotive capacity reading \REF{ex:key:50a} have the adjective and noun embedded in a specifier position as in \REF{ex:key:50b}. As discussed in \sectref{sec:key:3.4.1} above, this complex specifier contains the null element ALS. I propose that similar to the DP, the embedded nominal involves ModP, and I assume that ALS is its head. Again, like the DP, Predicate Modification combines the two nominals, here ModP and Agr’:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%50
    \label{ex:key:50}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Du  bist vielleicht ‘n  erstaunlicher Bauer!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  you are  \textsc{prt}           an amazing         farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘You are really an amazing farmer!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. \textit{Emotive} \textit{Capacity Reading Licensed DP-externally}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             CardP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   \textsubscript{} \textit{‘n}                            AgrP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                (Predicate Modification)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            ModP\textsubscript{<e,t>}                             Agr’\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                              {\textbar}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        ALS              AgrP\textsubscript{<e,t>}                 NumP\textsubscript{REL}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           [\textit{erstaunlicher} REL \textit{Bauer}]       {\textbar}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                            NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                                    \textit{e\textsubscript{N}}\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that ALS is licensed by the modal particle \textit{vielleicht}. Furthermore, \textit{ein} is present due to REL in the matrix nominal, and \textit{ein} is uninflected due to the presence of an overt element in the specifier of the higher AgrP.\footnote{Note that the instances with an adjective involve REL in the embedded nominal as in \REF{ex:key:50b}, but I assume that cases without an adjective also involve REL. In other words, ALS always involves REL and thus NumP as part of its complement. Observe in this regard that these cases involve concord in agreement features between \textit{ein} in the matrix nominal and the (adjective and) noun embedded in Spec,AgrP. I take it that the head Mod (ALS) mediates the agreement between these two nominals (also Chapter 8, \sectref{sec:key:3.1}).} Finally, I assume that the emotive flavor originates with the modal particle.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  For clarity, I review the capacity reading in the nominal and clausal domains adding a few more details in the process. There are two differences between the DP and the TP. First, the DP has a neutral interpretation, but the TP has an emotive one. This difference in emotiveness follows from the need to be restrictive enough in the former case (namely when the speaker singles someone out in a certain capacity, see \sectref{sec:key:2.2.1}) and the presence of the modal particle in the latter case. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Second, the indefinite determiner is absent in the DP (\textit{du als Bauer}) but present in the TP (\textit{ein} ALS \textit{erstaunlicher Bauer} e\textsubscript{N}). I consider two structural domains here, delineated by square brackets in the following examples where the embedded (but not the matrix) nominal is enclosed by the brackets. In the matrix nominal, \textit{ein} is absent in \textit{du} [\textit{als Bauer}] as \textit{du} itself flags REL of the matrix nominal; \textit{ein} is present in \textit{ein} [\textit{erstaunlicher Bauer}] e\textsubscript{N} as it flags REL. In the embedded nominal, \textit{ein} is absent in [\textit{als Bauer}] since \textit{als} is a general(ized) capacity operator that takes role and kind nouns as arguments. As to the absence of \textit{ein} in [\textit{erstaunlicher Bauer}], I assume that this is due to haplology with \textit{ein} in the matrix nominal. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up this section, I derived the three readings established at the beginning of this chapter. In the DP, the ordinary and comparative readings involve canonical, simple DPs, but the capacity reading manifests an \textit{als-}nominal in an adjoined position. As for the TP, the first two readings exhibit regular copular structures, but the capacity reading involves a modal particle licensing an unpronounced ALS. The latter is part of a complex specifier, and this specifier forms part of a non-canonical nominal. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.  Conclusion
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This chapter discussed the first consequence of the proposal laid out Chapter 5 – \textit{ein} in the context of emotive constructions. I started with the observation that the DP and the TP are parallel in structure and interpretation. Making this my general heuristic methodology, I extended the discussion to pronominal DPs and copular TPs containing non-theta nouns. Establishing three basic readings (cf. den \citealt{Dikken2006}), I provided a more detailed investigation of how \textit{ein} combines with certain role and kind nouns and how these combinations fare with regard to emotiveness in the nominal and clausal domains. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In order to explain the commonalities and slight differences, I employed, with a few refinements, de Swart \textit{et al}.’s (2007) account of the clause and extended it to pronominal DPs. In order to explain the whole range of data, the explanation of the DP had to include some pragmatic considerations \citep{Rauh2004}. In fact, the interaction between these two proposals also explained the absence of true minimal pairs of pronominal DPs and copular DPs involving bare predicate nouns. More importantly, I showed that \textit{ein} is not responsible for the emotiveness property. Rather, this property is due to REL in combination with a [+figurative] noun. It was argued that \textit{ein} indicates the presence of structure on top of NP (Hypothesis 3a) and the presence of REL (Hypothesis 3b). In other words, I can maintain the claim that \textit{ein} is semantically vacuous (Hypothesis 1a).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

So far, I only discussed cases where we find morphological agreement between the pronoun and the predicate noun. Next I turn to cases of – what looks like – dis-agreement in number.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\setcounter{page}{292}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Chapter 7: \textit{Ein} and Number 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.  Introduction
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this chapter, I continue the investigation begun in Chapter 5 and turn to the second and final consequence of the proposals about \textit{ein}. I argue that \textit{ein} is not a reflex of number, neither morphologically nor semantically. This is consistent with the claim that \textit{ein} is semantically vacuous (Hypothesis 1a).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.1.  A Brief Review
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall from Chapter 6 that true minimal pairs between the DP and TP are not possible in singular, out-of-the-blue contexts. Specifically, although a bare role noun such as \textit{Bauer} is possible in both the DP and TP, it crucially has different interpretations: it is emotive/figurative in the DP (meaning ‘peasant’) and neutral/literal in the TP (meaning ‘farmer’). In other words, bare nouns are fairly restricted with regard to (non-)emotiveness in the two domains. I proposed that factors involving both pragmatics and semantics explain these restrictions, the former (Grician maxims) being particularly relevant for the DP and the latter (the operator CAP) for the TP. When \textit{ein} appears in front of a role noun, the facts fall differently.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In particular, I noted that \textit{ein} cannot surface in pronominal DPs. Compare \REF{ex:key:1a} to \REF{ex:key:1b}. In order to indicate that \textit{ein} is the indefinite article here, I provide the latter in its reduced, unstressable form \textit{‘n}:\footnote{As might be expected, the singularity numeral \textit{EIN} ‘one’ is also ungrammatical here (ia), but adjectival \textit{eine} is not (ib). Note that the second example involves a contrast cancelling the duality presupposition:  (i)  a.   *  \textit{du   EIN Bauer}      you one peasant.\textsc{masc}     b.  \textit{wir zwei Bauern und du   einer Bauer}      we two  farmers and you one   farmer.\textsc{masc}      ‘us two farmers and you one farmer’In the main text below, \REF{ex:key:1b} is ruled out due to the presence of two determiners in one DP. Given the composite analysis of \textit{EIN} ‘one’ in German (Chapter 5), the same explanation extends to (ia). As adjectival \textit{eine} is a lexical item, morphologically unrelated to \textit{ein}, it can occur after a definite determiner as in (ib).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%1
    \label{ex:key:1}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{du            Bauer} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) peasant.\textsc{masc}          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (peasant)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b. *  \textit{du           ‘n Bauer}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) a peasant.\textsc{masc}         
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

However, \textit{ein} is possible in predicate nominals in the TP. Specifically, if \textit{ein} appears in these copular cases, the role noun may undergo an interpretative extension from its neutral/literal meaning in \REF{ex:key:2a} to an emotive/figurative reading in \REF{ex:key:2b}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%2
    \label{ex:key:2}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Du           bist Bauer.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) are farmer.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a farmer.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Du           bist ‘n Bauer.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) are   a peasant/farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a peasant.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \%‘You are a farmer.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I proposed that this effect is not due to the presence of \textit{ein} itself. Rather, this extension in interpretatory possibilities is a function of the null operator REL, the presence of which is flagged by \textit{ein}. This is consistent with the claim that \textit{ein} is a semantically vacuous element.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.2.  Number in the DP and TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This chapter abstracts away from the different meanings of the noun. Below, I focus on issues related to number (for general typological discussion, see \citealt{Corbett2000} and \citealt{Hurford2003}; for a survey of the formal semantics, see \citealt{Link1998}). To start with some simple examples, consider plural contexts where nouns and pronouns of the same number can combine in both the DP and TP:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%3
    \label{ex:key:3}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ihr         Schweine}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) pigs
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you idiots’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ihr         seid  Ärzte.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are   doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are doctors.’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These distributions are as we would expect. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Turning to singular nouns, it is also expected that such nouns cannot combine with plural pronouns to form pronominal DPs. This holds independently of the presence of \textit{ein}: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%4
    \label{ex:key:4}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a. *  \textit{ihr         Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) pig.\textsc{neut}         
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. *  \textit{ihr        ‘n Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) a pig.\textsc{neut}         
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

However, singular nouns behave differently in the TP. Surprisingly, a singular noun is possible in copular contexts \REF{ex:key:5a}. At face value, this presents a case of dis-agreement between a singular noun and a plural pronoun. Crucially though, when \textit{ein} appears, restrictions in number reveal themselves again \REF{ex:key:5b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%5
    \label{ex:key:5}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Ihr         seid alle Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are  all   doctor.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are all doctors.’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. ?*  \textit{Ihr         seid alle ‘n Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are  all     a doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up thus far, as seen in Chapter 6, bare nouns are restricted to non-emotiveness in singular TPs. As just seen though, they are unrestricted in number in TPs: bare nouns can occur not only in singular but also plural contexts – they are number-neutral. To repeat, bare nouns in TPs are restricted as regards emotiveness but unrestricted as regards number.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is clear that the grammaticality status of \REF{ex:key:5a} vs. \REF{ex:key:5b} correlates with the absence vs. presence of \textit{ein}. It could be claimed then that this difference is due to the morphology and/or semantics of \textit{ein}. However, I proposed in Chapter 5 that \textit{ein} itself is not specified for morphological number; that is, it has the specification [αPL morph]. Furthermore, I argued that \textit{ein} has no semantics. If so, we cannot rule out \REF{ex:key:5b} by resorting to problems in morphological or semantic number induced by the presence of \textit{ein}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To anticipate the following discussion, I reiterate the claim that morphological number is actually not specified on nouns in German. In Chapter 6, I followed de Swart \textit{et al}. (2007) in assuming that number morphology has to do with NumP. Specifically, bare nouns such as the role noun \textit{Arzt} ‘doctor’ are proposed to be unmarked for number and only project NP \REF{ex:key:6a}. In contrast, plural nouns like \textit{Ärzte} ‘doctors’ are argued to involve NumP \REF{ex:key:6b}, and singular nominals such as \textit{(ei)n Arzt} ‘a doctor’ are claimed to project more structure. I proposed in Chapter 6 that \textit{pace} de Swart \textit{et al.}, they may involve ArtP as in \REF{ex:key:6c}. In order to differentiate between bare role nouns \REF{ex:key:6a} and singular nouns \REF{ex:key:6c}, I label the first as non-plural reserving the term singular for the second. In other words, what has traditionally been classified as a “singular” noun may have two structural interpretations depending on the presence of \textit{ein}: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%6
    \label{ex:key:6}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  bare/non-plural \textit{Arzt}:  N  → [ NP ]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  plural \textit{Ärzte}:    N-\textsc{pl}  → [ NumP [ NP ]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  singular \textit{‘n Arzt}:  \textit{ein} N  → [[[ ArtP [ NumP [ NP ]]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To explain the difference in grammaticality in \REF{ex:key:5}, I make use of the structural proposal in \REF{ex:key:6}, where NumP is absent with non-plural nouns \REF{ex:key:6a} but present with plural and singular nouns (6b-c). Specifically, \REF{ex:key:5a} involves a non-plural noun as in \REF{ex:key:6a}, but \REF{ex:key:5b} contains a singular noun as in \REF{ex:key:6c}. I propose that \REF{ex:key:5a} is grammatical as NumP is absent in the predicate nominal. In other words, the predicate noun is number-neutral as no agreement relation between the predicate nominal and the subject pronoun is established.\footnote{That bare nouns as in \REF{ex:key:5a} are neither singular nor plural in interpretation (i.e., they are number-neutral) can also be seen in contexts other than the ones discussed in the main text:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumxvleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a.  flea(*s) infestation
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumxvleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  stone carvingFor the discussion of (i) and some other cases, see \citet[132-35]{Borer2005}.} In contrast, \REF{ex:key:5b} is ungrammatical as NumP is present in the predicate nominal. Here, an agreement relation between the predicate nominal and the subject pronoun is established resulting in a mismatch in number. In other words, it is the absence vs. presence of NumP (but not \textit{ein} itself) that is proposed to explain the difference in grammaticality in \REF{ex:key:5}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As for the DP cases in \REF{ex:key:4}, I propose that NumP is always present; that is, predicate nominals within DPs always involve NumP. Here, NumP is present for cartographic reasons. Unlike in the TP, where the presence of NumP is indicated by overt elements, in the DP higher, obligatory phrases like ArtP entail the presence of lower NumP. If so, the presence of NumP rules out \REF{ex:key:4a} due to a mismatch in number between a plural pronoun and a singular noun. Note now that what may look like a non-plural noun in \REF{ex:key:4a} actually involves a singular noun – NumP entails REL and thus \textit{ein} should be present as in \REF{ex:key:4b} (NB: I usually provide the label for the surface forms given the terminology provided in \REF{ex:key:6}). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I suggest that the reason \textit{ein} can never be present inside pronominal DPs, be they singular as in \REF{ex:key:1b} or plural as in \REF{ex:key:4b}, is that the null operator REL can be flagged not only by expletive \textit{ein} but also by other, substantive elements (e.g., pronominal determiners). Assuming that both types of determiner elements are merged in ArtP, only one of them can be inserted and consequently appear. If these considerations are on the right track, then I can maintain the claim that \textit{ein} does not determine number, neither morphologically nor semantically. As in the last chapter, I suggest that \textit{ein} indicates more structure on top of NP (Hypothesis 3a) and that it flags the presence of a null operator (Hypothesis 3b).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The chapter is organized as follows. First, I present the data illustrating the restrictions on morphological and semantic number in more detail. In \sectref{sec:key:3}, I lay out my proposal to account for these facts. \sectref{sec:key:4} summarizes the main findings.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.  Data
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I start with nominal and clausal contexts involving agreement in number. After that, I consider different instances of dis-agreement. The data are summarized in Tables 1 and 2 below. Given the discussion of the last chapter, I present the data of the DP with figuratively extended nouns and the examples of the TP with role nouns. Recall that I refer to “singular” forms of the noun without \textit{ein} as non-plural and to those with \textit{ein} as singular.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.  Cases of Agreement 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To begin, consider cases involving agreement in number. As already seen in the introduction, a non-plural noun can combine with a singular pronoun \REF{ex:key:7a} but a singular noun cannot \REF{ex:key:7b}. Furthermore, a plural noun can combine with a plural pronoun \REF{ex:key:7c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%7
    \label{ex:key:7}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{du            Schwein} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) pig.\textsc{neut}          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{du           ‘n Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) a pig.\textsc{neut}      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{ihr         Schweine}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) pigs
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you idiots’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As expected, \REF{ex:key:7a} can only be singular in interpretation and \REF{ex:key:7c} only plural. In other words, the interpretations in \REF{ex:key:7a} and \REF{ex:key:7c} are parallel to the morphology on the noun. I discuss the ungrammatical case of \REF{ex:key:7b} in \sectref{sec:key:3.2.5} in detail proposing that \REF{ex:key:7b} involves two determiners in the same DP. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Turning to the TP, we find similar facts with the exception of singular nouns. Specifically, a non-plural as well as a singular noun can combine with a singular pronoun (8a-b). Again, a plural noun can occur with a plural pronoun \REF{ex:key:8c}:\footnote{Recall from Chapter 6 that a predicative [-figurative] role noun in German can, for many speakers, take an indefinite article. In other words, these speakers allow both \REF{ex:key:8a} and \REF{ex:key:8b}. Below, I focus on those speakers.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%8
    \label{ex:key:8}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Du          bist Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) are doctor.\textsc{masc}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a doctor.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.  \textit{Du            bist ‘n Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) are    a doctor.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a doctor.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Ihr         seid Ärzte.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are  doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are doctors.’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Again, the semantics runs parallel to the morphology. Next, I illustrate the various cases involving dis-agreement in number in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.  Cases of Dis-agreenent
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with the DP, a plural noun cannot combine with a singular pronoun \REF{ex:key:9a}. Conversely, a singular or a non-plural noun cannot combine with a plural pronoun either (9b-c):\footnote{In \sectref{sec:key:3.3.2}, I discuss special cases such as \textit{Sie Schwein} ‘you (pig =) idiot’, which are characterized by semantic agreement but morphological dis-agreement (the latter though crucially holds between two different nominals as explicated in detail in that section).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%9
    \label{ex:key:9}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{du            Schweine}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

you(\textsc{sgl}) pigs
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.  *  \textit{ihr      \textsubscript{} }\textit{‘n Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) a  pig.\textsc{neut}      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  *  \textit{ihr      \textsubscript{} } \textit{Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) pig.\textsc{neut}      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

So far, the facts are as we might expect. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Turning to the TP, it may not be surprising either that a plural noun cannot combine with a singular pronoun \REF{ex:key:10a} or a singular noun with a plural pronoun \REF{ex:key:10b}. However, it is unexpected that a non-plural noun can combine with a plural pronoun \REF{ex:key:10c}. Importantly, the interpretation here is plural (for similar facts in Dutch, see de Swart \textit{et al}. 2005: 451):\footnote{There seem to be some interesting cross-linguistic differences with this type of data. For example, the Romance languages are different in that they require agreement between the pronoun and the nominal (see \citealt{Zamparelli2008}: 107 fn. 4; also \citealt{MunnSchmitt2005}: 839).} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%10
    \label{ex:key:10}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{Du           bist Ärzte.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) are doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. ?*  \textit{Ihr         seid alle ‘n Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are   all    a doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Ihr         \textsubscript{} }\textit{seid alle Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are   all doctor.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are all doctors.’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that there is a floating quantifier in the grammatical instance \REF{ex:key:10c} above. This element can also be in a different position \REF{ex:key:11a} or be exchanged by another quanfitier \REF{ex:key:11b}. Interestingly, when such an element is missing, the example becomes a bit marked, and the judgments are somewhat unstable \REF{ex:key:11c} (I indicate this by \textsuperscript{m} here):\footnote{Below I argue that the (overt) quantifiers in (11a-b) are distributivity operators: they “single out” the individuals inside the set presupposed by the plural pronoun, and they distribute the relevant predicate over those individuals. As to \REF{ex:key:11c}, I tentatively suggest below that there is a null distributivity operator present (also de Swart \textit{et al}. 2017: 218). The difference in overtness of the operator may explain the more stable judgments in (11a-b) vs. \REF{ex:key:11c}.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%11
    \label{ex:key:11}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Ihr        alle seid Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) all   are  doctor.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are all doctors.’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ihr         seid jeder Arzt}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are   each  doctor.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are each a doctor.’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textsuperscript{m}  \textit{Ihr         seid Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are   doctor.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are doctors.’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Similar facts hold in capacity constructions involving \textit{als} ‘as’. This is what I turn to next.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{2.3.  More Cases Of Dis-agreement: Instances Involving} als\textit{{}-nominals}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that I refer to nouns preceded by \textit{als} ‘as’ as \textit{als}{}-nominals. As we will see, \textit{als}{}-nominals can occr in the context of pronominal DPs and non-copular TPs. First, I discuss cases of agreement and then instances of dis-agreement. Starting with the DPs, \textit{als}{}-nominals agree with their preceding pronoun, independently of whether the pronoun is in the singular \REF{ex:key:12a} or in the plural \REF{ex:key:12c}, with the qualification that the presence of \textit{ein} leads to slight markedness \REF{ex:key:12b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%12
    \label{ex:key:12}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{du            als Arzt}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) as  doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you as a doctor’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  ?  \textit{du            als ‘n Arzt}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) as    a doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you as a doctor’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{ihr         als Ärzte}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) as  doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you as doctors’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The same grammaticality judgments hold for the corresponding sentences involving a non-copular verb:\footnote{The sentences with non-copular verbs involve \textit{als} ‘as’ and have capacity readings. In contrast, the sentences with copular verbs discussed in Sections 2.1 and 2.2 above do not involve ALS ‘as’ and do not have capacity readings (recall from Chapter 6, \sectref{sec:key:3.4.1} that copular TPs can have a capacity reading – they involve null ALS licensed by the modal particle \textit{vielleicht} ‘really’). The different readings do not play a role here – the focus is on number.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%13
    \label{ex:key:13}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Du           sprichst als Arzt}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) speak     as  doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You speak as a doctor.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  ?  \textit{Du           sprichst als ‘n Arzt}.    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) speak     as   a doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You speak as a doctor.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Ihr         sprecht als Ärzte.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) speak    as  doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You speak as doctors.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Turning to the cases involving dis-agreement, consider nominals where a singular pronoun is combined with a plural noun \REF{ex:key:14a}, and a plural pronoun is followed by a singular noun \REF{ex:key:14b}. While these two combinations lead to ungrammaticality, there is one exception: a plural pronoun can be followed by an \textit{als}{}-nominal with a non-plural noun \REF{ex:key:14c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%14
    \label{ex:key:14}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a. *  \textit{du           \textsubscript{} }\textit{als Ärzte}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) as  doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. ?*  \textit{ihr  \textsubscript{} }\textit{alle als ‘n Arzt}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) all   as   \textsubscript{} a doctor.\textsc{masc}        
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{ihr         alle \textsubscript{} }\textit{als Arzt}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) all    as  doctor.\textsc{masc}        
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you all as doctors’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that the interpretation of the non-plural nominal in \REF{ex:key:14c} is indeed plural. The same holds for the clausal counterparts (the datum in \REF{ex:key:15c} is adapted from de Swart \textit{et al}. 2007: 206):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%15
    \label{ex:key:15}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a. *  \textit{Du           sprichst als Ärzte}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) speak    as   doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. ??  \textit{Ihr        \textsubscript{} }\textit{sprecht alle als ‘n Arzt}.    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) speak   all    as    a doctor.\textsc{masc}      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{Ihr        sprecht alle als Arzt}.    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) speak   all   as   doctor.\textsc{masc}      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You speak all as doctors.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To review, this last section discussed \textit{als}{}-nominals in the context of pronominal DPs and non-copular TPs. These two cases show the same syntactic distributions and corresponding semantic interpretations as the copular TPs illustrated in the two preceding sections – these three cases may involve morphological dis-agreement. This is in stark contrast to simple DPs, that is, DPs without \textit{als}{}-nominals, which have to exhibit agreement. Before moving on, I provide a more detailed summary of the individual cases in tabular form.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.4.  Summary of the Data
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I summarize the syntactic distributions in the following charts. I mark the unexpected instances by (!), the latter all restricted to the occurrence of non-plural nouns. Note that if there is a difference in judgments between nominals with or without \textit{als}, an explicit reference to \textit{als} is provided. \tabref{tab:key:1} shows the facts involving the DP (the shadings in \tabref{tab:key:1} are commented on below):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Summary of the Judgments in the DP}
\label{tab:key:1}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXX}
 & Non-plural N & \textit{(ei)n} N & N-\textsc{pl}\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{du} ‘you(\textsc{sgl})’ & ${\surd}$ & { * [without \textit{als}]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 ? [with \textit{als}] & *\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{ihr} ‘you(\textsc{pl})’ & { * [without \textit{als}]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 ${\surd}$ (!) [with \textit{als}] & { * [without \textit{als}]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 ?* [with \textit{als}] & ${\surd}$\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\tabref{tab:key:2} shows the facts of the TP. Observe again that non-plural nouns are surprising as they are grammatical in basically all clausal contexts:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Summary of the Judgments in the TP}
\label{tab:key:2}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXX}
 & Non-plural N & \textit{(ei)n} N & N-\textsc{pl}\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{du} ‘you(\textsc{sgl})’ & ${\surd}$ & { ${\surd}$ [without \textit{als}]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 ? [with \textit{als}] & *\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{ihr} ‘you(\textsc{pl})’ & ${\surd}$ (!) & { ?* [without \textit{als}]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 ?? [with \textit{als}] & ${\surd}$\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To repeat, the unexpected distributions occur when a plural pronoun is combined with a non-plural noun, both in the DP mediated by \textit{als} ‘as’ and in the TP more generally. Furthermore, the judgments are basically the same with or without \textit{als} (and these cases are treated below as the same). There are two exceptions to this that both hold in the DP (indicated by shading of the relevant cells in \tabref{tab:key:1}): depending on the presence of \textit{als}, the judgments differ markedly if a singular pronoun combines with a singular noun or a plural pronoun occurs with a non-plural noun. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In view of these data, I conclude that number is very restricted in simple DPs such that pronouns and nouns have to match in number. Syntactically, singular pronouns cannot combine with plural nouns, and plural pronouns cannot combine with singular or non-plural nouns.\footnote{The only potential exception that I am aware of comes from discontinuous noun phrases, where a plural noun is compatible with both a plural and a singular determiner element:(i)  \textit{Hemden habe ich \{kein-e   / ?kein-es\}  getragen}.    shirts     have  I      none-\textsc{pl}/  none-\textsc{sgl} worn    ‘As for shirts, I have worn none.’I argued in Chapter 4, \sectref{sec:key:3} that the source and the split-off involve two separately base-generated nominals. Furthermore, \citet{OttNicolae2010} suggest that the plural in the split-off presumably has to do with frame-setting in topicalization, and as such the “dis-agreement” here is a different phenomenon.} Semantically, singular and plural pronouns are interpreted as singular and plural, respectively; singular/non-plural and plural nouns are understood as singular and plural, respectively.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In contrast, copular clauses, non-copular clauses, and pronominal DPs, where the latter two involve \textit{als}, are less restricted, both syntactically and semantically. Syntactically, plural pronouns can, under certain conditions, occur with non-plural nouns. Semantically, non-plural nouns are compatible with a plural interpretation. In the next section, I turn to an explanation of these syntactic and semantic facts.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.  Proposal
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I account for the different morphological and semantic possibilities licensed in the different syntactic domains. Just as in Chapter 6, I follow de Swart \textit{et al.} (2007) and extend their proposal to pronominal DPs explaining the differences between predicate nominals in TPs and those in DPs.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.1.  Basic Assumptions
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To begin, I lay out my assumptions of the simple structures; the instances involving \textit{als}{}-nominals are discussed at the end of this chapter. Starting with the syntax and simplifying somewhat for now, I assume again that all DPs, including pronominal DPs, consist of a head noun projecting an NP, a Number head projecting a NumP, and a (pronominal) determiner surfacing in the DP-level \REF{ex:key:16a}. In keeping with the discussion in Chapter 6, the predicate nominal inside the DP always involves NumP (where REL maps an element of type <e> to type <e,t>). TPs are different: while they all involve a subject DP, a copular verb, and a predicate nominal, the latter may vary in size. As in Chapter 6, I assume that NumP is syntactically optional in predicate nominals following a copular auxiliary. Specifically, a non-plural nominal involves NP \REF{ex:key:16b}, a plural nominal consists of NumP \REF{ex:key:16c}, and a singular nominal projects ArtP \REF{ex:key:16d} (Aux = stands for the auxiliary \textit{sein} ‘to be’):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%16
    \label{ex:key:16}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  DP\textsubscript{<e>}:  [\textsubscript{DP} [\textsubscript{NumP} [\textsubscript{NP} ]]]\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  TP\textsubscript{<t>}:  [\textsubscript{DP} [\textsubscript{NumP} [\textsubscript{NP} ]]]\textsubscript{<e>} Aux [\textsubscript{NP} ]\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  TP\textsubscript{<t>}:  [\textsubscript{DP} [\textsubscript{NumP} [\textsubscript{NP} ]]]\textsubscript{<e>} Aux [\textsubscript{NumP} Num\textsubscript{[+PL]} [\textsubscript{NP} ]]\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  TP\textsubscript{<t>}:  [\textsubscript{DP} [\textsubscript{NumP} [\textsubscript{NP} ]]]\textsubscript{<e>} Aux [\textsubscript{ArtP} \textit{ein} [\textsubscript{NumP} Num\textsubscript{[-PL]} [\textsubscript{NP} ]]]\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These are the basic assumptions about the syntax. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As for the semantics, I again follow de Swart \textit{et al.}’s (2007) discussion of the clause in (16b-d) and assume that the predicate nominal must be of type <e,t> to combine with the subject DP (type <e>). Recalling that the copular auxiliary is semantically vacuous, this means that NP, NumP, and ArtP must all be of type <e,t>. As already discussed above, de Swart \textit{et al.} propose that role nouns combine with CAP, which is in NP, and that kind nouns, inherent or coerced, combine with REL, which is in NumP. The first option holds for \REF{ex:key:16b}; the second option applies to plural \REF{ex:key:16c} and to singular \REF{ex:key:16d}, with singular involving an additional structural layer (ArtP). To be clear, these types of predicate nominals are all syntactically different but semantically the same (i.e., type <e,t>). As such, they can combine with the subject DP to yield a truth value (type <t>). Note again that \textit{ein} indicates the presence of a certain amount of structure on top of NP (Hypothesis 3a) and that it flags the presence of the operator REL (Hypothesis 3b).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As for the DP in \REF{ex:key:16a}, I suggested that NumP (and thus REL) is always present. I argue in more detail below that this is so for cartographic reasons – higher, obligatory phrases entail the presence of lower ones. Semantically, combining the head noun and REL yields an element of type <e,t>. As determiners, including pronominal determiners, are of type <<e,t>e>, predicate nominals and determiners can combine to return an entity (type <e>).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   With these general points in place, I turn to the cases from the data section. First, I discuss DPs and TPs involving agreement and dis-agreement. Then, I turn to some special cases involving the pronoun \textit{Sie} ‘you(\textsc{formal})’, identifying the parts of the structure where morphological and semantic number originate. In the final section, I address the more complex DPs and TPs involving \textit{als}{}-nominals. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{3.2.  Agreement in Constructions Without} als
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

First, I review some general facts about agreement in the TP. This is followed by proposing that NumP plays a crucial role in agreement. Finally, I detail the account of agreement as regards plural nouns, non-plural nouns, and singular nouns.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.1. Agreement and Dis-agreement
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Consider the data involving copular TPs below. Recall from above that non-plural nouns are fine in all contexts \REF{ex:key:17}. In contrast, singular nouns can only combine with singular pronouns \REF{ex:key:18} and plural nouns only with plural pronouns \REF{ex:key:19}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%17
    \label{ex:key:17}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Du           bist} \textit{Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) are  \textsubscript{} doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a doctor.’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ihr        seid alle Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are  all  doctor.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are all doctors.’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%18
    \label{ex:key:18}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Du           bist} \textit{‘n Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) are  \textsubscript{} a doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a doctor.’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  ?*  \textit{Ihr        seid alle ‘n Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are  all    a doctor.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%19
    \label{ex:key:19}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{Du           bist} \textit{Ärzte.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) are  doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{Ihr        seid Ärzte.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are  doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are doctors.’  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To be clear, non-plural nouns can occur in both singular and plural contexts, but singular and plural nouns are in complementary distribution as regards number.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To see whether this is a morphological or semantic restriction, I discuss \textit{jeder} ‘each’ in this regard. This quantificational element is morphologically singular but semantically plural; that is, it takes a non-plural restriction but presupposes a plurality of entities. As might be expected, this element can combine with a non-plural noun via an auxiliary \REF{ex:key:20a}. However, while \textit{jeder} can also occur with a singular noun \REF{ex:key:20b}, it cannot occur with a plural one \REF{ex:key:20c}:\footnote{Other quantifiers also presuppose a plurality of entities, but they are morphologically plural and can combine with plural nouns:  (i)  \textit{Einige (Männer hier) sind} \textit{Ärzte.} some    men       here  are  doctors    ‘Some (men here) are doctors.’}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%20
    \label{ex:key:20}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Jeder (Mann hier) ist Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    each    man   here  is  doctor.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Each (man here) is a doctor.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Jeder (Mann hier) ist ‘n Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    each    man   here  is   a doctor.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Each (man here) is a doctor.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  *  \textit{Jeder (Mann hier) ist Ärzte.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    each    man   here  is  doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Considering that \textit{jeder} is semantically plural, the incompatibility with a plural noun in \REF{ex:key:20c} is somewhat surprising. I make the strongest claim and propose that the number restrictions shown in \REF{ex:key:17} through \REF{ex:key:20} are morphological \textit{and} semantic in nature. The lack of morphological agreement between singular \textit{jeder} and the plural predicate nominal explains the ungrammaticality of \REF{ex:key:20c}. As for the grammatical (20a-b), I propose that \textit{jeder} is semantically compatible with non-plural and singular nouns. Specifically, I suggest that \textit{jeder}, itself a distributivity operator, distributes the predicate over the set presupposed by itself.\footnote{Alternatively, there could be an additional null distributivity operator involved here. Note in this regard that de Swart \textit{et al}. (2007: 218) propose a null distributivity operator for cases like \textit{Jan und Sofie sind Arzt} ‘Jan and Sofie are (doctor =) doctors’ in Dutch (more on null distributivity operators in \sectref{sec:key:3.3.2}).}  In what follows, I focus on morphological number, but I return to semantic number in \sectref{sec:key:3.3}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.2. Agreement and NumP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with the TP, recall that predicate nominals in the clause involve NP, NumP, or ArtP depending on the morphology inside the predicate nominal. Specifically, while all nouns involve at least NP, plural morphology on the noun indicates the presence of NumP, and the presence of \textit{ein} shows ArtP (containing NumP). Furthermore, I propose that when NumP is present, an agreement relation must be established between the predicate nominal and the subject DP. In contrast, a non-plural noun does not project NumP – it is a predicate nominal involving NP. I propose that with NumP absent, such a predicate nominal does not have to undergo an agreement relation, and consequently its distribution is much freer (see also den \citealt{Dikken2006}: 210). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

DPs are different. I propose that NumP must be present for cartographic reasons: higher phrases such as ArtP entail the presence of lower phrases such as NumP. Note in this regard that DPs involve determiners and that these determiners originate in ArtP and move to the DP-level. Now, as is well known, elements in the DPs must exhibit concord in agreement features like case, number, and gender. I follow the literature in making the standard assumption that number features originate in NumP (e.g., de Swart \textit{et al}. 2007, \citealt{Julien2005a}, \citealt{Ritter1991}, \citealt{Roehrs2006b}). I assume that the values on Num are morphological (i.e., [αPL morph]) and that NumP mediates concord within the DP. For concreteness, I assume here that the value of the Num head “percolates” up the nominal tree by some concord mechanism.\footnote{As mentioned in Chapter 1, \sectref{sec:key:4.1.1}, there are different mechanisms that have been claimed to bring about concord. Note also that the obligatory presence of NumP is presumably not due to concord itself. This is so because I suggest below that mass nouns receive singular morphology by default (as [αPL morph] is proposed to be absent on Num in those cases).}  As to the head noun, I proposed in Chapter 1, \sectref{sec:key:4} that it moves to adjoin to Num (also \citealt{Julien2005a}), where the head noun establishes an agreement relation with Num. To repeat, NumP is always present in the DP.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Taking stock thus far, there are two types of agreement: concord in agreement features within the DP, indicated below by subscript alphas \REF{ex:key:21a}, and agreement between the subject and the predicate nominal within the copular TP, marked below by subscript betas \REF{ex:key:21b}. The latter type of agreement only holds if NumP is present and if the morphological number of the predicate nominal agrees with that of the subject. Note that \REF{ex:key:21b} also involves concord in agreement features, just like \REF{ex:key:21a}, if NumP and higher phrases are present within the predicate nominal:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%21
    \label{ex:key:21}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  DP:  [ Det\textsubscript{α} Num\textsubscript{α} N\textsubscript{α} ]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  TP:  Subj\textsubscript{β} Aux [ … (Num\textsubscript{α}) N\textsubscript{α} ]\textsubscript{β}     (where β = α on Num)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is clear that NumP is crucial in establishing agreement relations both within the nominal domain and the clausal domain (involving copulas). For the TP, I focus on the agreement relation between the subject and the predicate nominal.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With these points about NumP in mind, we can observe that there are two notions of dis-agreement. Starting with the DP, all ungrammatical cases in the nominal domain involve (true) morphological dis-agreement where the combination of singular and plural elements cannot establish an agreement relation \REF{ex:key:22}. This failure to establish concord in agreement features is due to the obligatory presence of NumP in the DP, which is either specified as singular or plural (for details, see \sectref{sec:key:3.3}): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%22
    \label{ex:key:22}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{du            Ärzte}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  *  \textit{ihr         Arzt}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This is different for the TP. Given certain conditions, we may find instances of true dis-agreement \REF{ex:key:23a} but also cases of \textit{apparent} dis-agreement \REF{ex:key:23b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%23
    \label{ex:key:23}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{Du          bist Ärzte.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) are doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{Ihr         seid alle Arzt}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are   all  doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are all doctors.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Importantly, true dis-agreement in copular contexts also involves the fact that NumP is present, here in the plural predicate nominal in \REF{ex:key:23a}. This leads to ungrammaticality as an agreement relation between the singular subject pronoun and the plural predicate nominal cannot be established. Cases of apparent dis-agreement are different. In \REF{ex:key:23b}, NumP is absent with non-plural nominals, and no agreement relation has to be established. This allows the combination of a plural subject pronoun and a non-plural noun to surface. Consider the individual derivations involving the different types of nouns.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.3. Plural Nouns
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

DPs involve structures where NumP is always present. Assuming again that determiners orginate in ArtP, the tree diagram of \REF{ex:key:24b} is given in \REF{ex:key:24c} (in the discussion, I abstract away from the traces/copies left by movement):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%24
    \label{ex:key:24}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a. *   \textit{du            Schweine}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) pigs
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.   \textit{ihr        \textsubscript{} }\textit{Schweine}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) pigs
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you idiots’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. \textit{Agreement in the DP}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    DP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           \textit{ihr}\textsubscript{<<e,t>e>k}       ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    t\textsubscript{k}         NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         Num\textsubscript{REL<e<e,t>>}    NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textit{Schweine}\textsubscript{<e>}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As mentioned in the previous subsection, all elements inside DP have to establish an agreement relation mediated by NumP. If so, it is easy to rule out \REF{ex:key:24a}, where such a relation between the pronominal determiner, Num, and the noun cannot be established. In contrast, \REF{ex:key:24b} is fine as the relevant elements can establish an agreement relation in number. I turn to the clausal domain.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As in Chapter 6, I follow \citet{Bowers1993} in assuming that copular structures involve a Predication Phrase (PrP). The latter is embedded under a TP. I propose that the head Pr can take different elements as its complement. With plural morphology present on the nouns in (25a-b), I suggest that Pr takes NumP as its predicative complement. I provide the tree diagram of \REF{ex:key:25b} in \REF{ex:key:25c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%25
    \label{ex:key:25}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a. *  \textit{Du           bist Ärzte.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) are doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.  \textit{Ihr         seid Ärzte.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are  doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are doctors.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. \textit{Agreement in the TP}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    TP\textsubscript{<t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     [\textit{ihr} REL e\textsubscript{N}]\textsubscript{<e>k}   T’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textit{seid}\textsubscript{i}        PrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              t\textsubscript{k}     Pr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             t\textsubscript{i}           NumP\textsubscript{<e,t>}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   \textit{Ärzte}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall from the previous chapter that pronominal determiners (\textit{ihr}) are taken to be of type <<e,t>e> but that pronominal DPs as a whole (e.g., \textit{ihr} REL e\textsubscript{N}) are of type <e>. As regards morphological number, I proposed above that the NumP of the predicate nominal has to establish an agreement relation with the subject DP. As plural \textit{Ärzte} ‘doctors’ can entered into such a relation with plural \textit{ihr} but not singular \textit{du}, the difference in grammaticality in (25a-b) is accounted for. As just seen, we can observe that the cases involving plural nouns are straightforward.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.4. Non-plural Nouns
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Again, all DPs contain NumP. As such, a singular pronoun is grammatical with a non-plural noun \REF{ex:key:26a}, but a plural pronoun is not \REF{ex:key:26b}. I derive \REF{ex:key:26a} as in \REF{ex:key:26c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%26
    \label{ex:key:26}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{du            Schwein} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) pig.\textsc{neut}          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. *  \textit{ihr        \textsubscript{} }\textit{Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) pig.\textsc{neut}      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. \textit{Agreement in the DP}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    DP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           \textit{du}\textsubscript{<<e,t>e>k}       ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    t\textsubscript{k}         NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         Num\textsubscript{REL<e<e,t>>}    NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textit{Schwein}\textsubscript{<e>}       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The ungrammaticality in \REF{ex:key:26b} is due to the failure of establishing the relevant agreement relation between the pronominal determiner, Num, and the noun. Note now that with NumP present, REL is present too, and we would expect \textit{ein} to occur. In the next subsection, I discuss why singular nouns are not possible here.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As regards the TP, I followed de Swart \textit{et al}. (2007) in that NumP is absent if no number-related elements are present in the predicate nominal. This is the case for (27a-b). I propose that the head Pr takes NP as its predicative complement here. Recall that bare role nouns combine with CAP. I derive (27a-b) as \REF{ex:key:27c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \REF{ex:key:27}  a.  \textit{Du           bist Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) are doctor.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a doctor.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.  \textit{Ihr         seid alle Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are  all   doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are doctors.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. \textit{Apparent (Dis-)Agreement in the TP}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    TP\textsubscript{<t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         \textit{du}/\textit{ihr}\textsubscript{<e>k}           T’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{seid}\textsubscript{i}         PrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      t\textsubscript{k}       Pr’\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               t\textsubscript{i}     NP\textsubscript{CAP<e<e,t>>}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           \textit{Arzt}\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With NumP absent, the predicate nominal does not have to enter into an agreement relation. In other words, no agreement relation between the predicate nominal and the subject DP has to be established, and both singular and plural subject pronouns are grammatical here. \REF{ex:key:27b}, then, is a case of apparent dis-agreement as no relevant agreement relation is established in the first place. In order to obtain a plural interpretation of the non-plural noun in \REF{ex:key:27b}, I follow de Swart \textit{et al.} (2007) in that NP may involve a distributivity operator. I take the floating quantifier in \REF{ex:key:27b} to be this element. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  This discussion makes an interesting prediction: if a predicate nominal in a copular TP does not involve NumP but just NP, then the head noun can only combine with CAP to yield an element of type <e,t> (see \REF{ex:key:27c} again). This in turn should allow only a neutral/literal meaning of a role noun that can, at least potentially, undergo figurative extension. In keeping with what we saw in Chapter 6, we find the same interpretatory restriction in the (apparent) dis-agreement cases discussed here:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%28
    \label{ex:key:28}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           \textit{Ihr         seid jeder Bauer.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  you(\textsc{pl}) are   each  farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘You (each) are farmers.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \#‘You (each) are peasants.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the next section, I disucss singular nouns, and I provide an explanation as to why \textit{ein} is not possible in pronominal DPs.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.5. Singular Nouns
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with the DP, I return to the question left unanswered in the last subsection, namely why \textit{ein} cannot occur in pronominal DPs despite the fact that REL is present. Note that this not only applies to pronominal DPs \REF{ex:key:29a} but also to ordinary DPs \REF{ex:key:29b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%29
    \label{ex:key:29}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a. *  \textit{du          ‘n Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) a pig.\textsc{neut}      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. *  \textit{das} \textit{‘n Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the   a pig.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I proposed in the last chapter that REL is not only flagged by \textit{ein} but also by other elements. Specifically, definite determiners, pronominal or ordinary, can also flag the presence of REL. Recall that this is consistent with de Swart \textit{et al}.’s (2007) assumption about regular DPs like \textit{the child}. Now, assuming that determiners and \textit{ein} originate in the same position (i.e., ArtP), only one such element can occur. This explains the ungrammaticality of the cases in \REF{ex:key:29}, where two determiners are present. Finally, given the current analysis, the distributional interaction between \textit{ein} and personal pronouns in pronominal DPs provides evidence that the latter are indeed determiners. I turn to the TP.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In Chapter 6, I discussed in detail the proposal that for nouns to be predicates, kind nouns combine with REL directly, but role nouns do so indirectly (via kind coercion and REL). REL triggers the presence of \textit{ein} in singular contexts, which I explained by proposing that \textit{ein} flags the presence of REL. Consider (30a-b). Given the absence of a feature for definiteness on \textit{ein}, I also proposed that this element may surface in ArtP (and not necessarily in DP). Given that, I propose that besides NP and NumP, the head Pr can also take ArtP as its predicative complement. The tree diagram of \REF{ex:key:30a} is provided in \REF{ex:key:30c}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%30
    \label{ex:key:30}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Du           bist ‘n Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) are   a doctor.\textsc{masc} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a doctor.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b. ?*  \textit{Ihr        seid alle ‘n Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) are  alle  a doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c. \textit{Agreement in the TP}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    TP\textsubscript{<t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{du}\textsubscript{<e>k}             T’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{bist}\textsubscript{i}         PrP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      t\textsubscript{k}       Pr’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               t\textsubscript{i}    ArtP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textit{‘n}         NumP\textsubscript{REL<e<e,t>>}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   \textsubscript{} \textit{Arzt}\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Observe that the subject pronoun \textit{du} ‘you’ and \textit{ein} in \REF{ex:key:30a} are part of different nominals – the pronominal one and the predicate one, where the former is separated from the latter by an auxiliary. As such, \textit{du} and \textit{ein} do not compete for insertion in the same ArtP, and they can co-occur. Now, notice that with NumP present, a singular predicate nominal can establish an agreement relation with a singular, but not a plural, subject pronoun. As such, the latter case presents an instance of true dis-agreement, and the failure to establish an agreement relation explains the ungrammaticality of \REF{ex:key:30b}. I return to the main focus of this book summarizing the discussion so far and drawing some conclusions. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with the DP, I proposed that NumP is always present, and a relevant agreement relation between a determiner, Num, and a noun has to be established. Furthermore, although REL is always present in the DP, \textit{ein} does not surface when another determiner occurs. As suggested above, elements other than \textit{ein} can flag the presence of REL in singular contexts. As for the TP, I proposed that the head Pr can take NP, NumP, or ArtP as its predicative complement. Importantly, when NumP is present in the predicative complement, an agreement relation with the subject DP has to be established. In contrast, when NumP is absent, no agreement relation has to be established, and a number-neutral predicate comes about.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

More generally, these assumptions allow us to account for the diverse agreement phenomena illustrated above while maintaining the claim that \textit{ein} has no specifications for number. In keeping with Chapter 5, I provide more details in the next section that morphological number is not due to \textit{ein} but due to the number specification on NumP. In addition, I relate morphological number to semantic number and the mass/count distinction of nouns.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.3.  Morphological and Semantic Number
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Considerations regarding number and the related mass/count distinction are notoriously complex and difficult. For convenience, I employ my own system developed in \citet{Roehrs2006b}.\footnote{There are many other proposals. To name just a few: \citet{Borer2005}, \citet{Eynde2020}, \citet{Ott2011b}, \citet{Watanabe2006}, and H. \citet{WieseMaling2005} (see also references cited in these works). Also, \citet{Hachem2015} and \citet[181-86]{Rehn2019} argue that a simple dichotomy of count vs. mass nouns is not correct (also S. \citealt{Grimm2012}, \citealt{Zhang2012}). On the basis of diachronic facts, \citet{Hachem2015} proposes that grammatical gender functions as a mass quantifier; that is, gender is a semantically meaningful category that creates different types of mass distinctions (her page 100): neuter involves unbounded mass (e.g., \textit{snow}), masculine forms individuative mass (e.g., \textit{frost}), and feminine is collective mass (e.g., \textit{winter}); plural is a separate category. \citet{Rehn2019} points out that these fine-grained distinctions present a problem for \citet{Borer2005} (for other issues with Borer’s work, see also \citealt{Ott2011b}, \citealt{Zhang2012}). To the extent that these finer distinctions are correct, the current analysis could break NumP into several phrases (as is done in \citealt{Hachem2015} and \citealt{Rehn2019}). Having said that, Hachem states that this proposed correlation between gender of the noun and its interpretation is no longer obvious in Modern German – the correlation has been regrammaticalized (see \citealt{Leiss1997}). Furthermore, these finer distinctions do not seem to have an influence on the distribution of adjectival inflections or \textit{ein} in Standard German. As such, I do not take them into consideration here.} My primary goal here is not to argue that this is the correct way to account for morphological and semantic number. Rather, I intend to show that elements other than \textit{ein} can be held responsible for morphological and semantic number thus defending the claim that \textit{ein} is not a reflex of number. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In the first subsection, I relate morphological and semantic number. With this in place, I turn to cases of dis-agreement in pronominal DPs containing \textit{Sie} ‘you(\textsc{formal})’. In the third subsection, I provide more details of the derivation of these cases as regards number. The discussion of these non-canonical structures reveals a number of interesting points.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.3.1. Relating Morphological and Semantic Number
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I begin by illustrating the relevant issues with an ordinary noun like \textit{Schwein} ‘pig’. As is well known, this type of noun has three readings. It can have a mass \REF{ex:key:31a}, a singular count \REF{ex:key:31b}, or a plural count \REF{ex:key:31c} interpretation:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%31
    \label{ex:key:31}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Schwein   schmeckt gut}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    pig.\textsc{neut} tastes       good
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Pork tastes good.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{du            Schwein} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) pig.\textsc{neut}          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.   \textit{ihr        \textsubscript{} }\textit{Schweine}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) pigs      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you idiots’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In order to account for these readings, I propose that there is an intricate interplay between the number specifications on the noun and the Number head (Num). Specifically, head nouns have a statement for semantic number, and the latter interacts with the morphological number on Num.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In more detail, I assume with \citet[94]{Borer2005} that nouns are unmarked for the mass-count distinction. Unlike Borer, I assume that all regular nouns involve two statements as regards number (to be collapsed into one statement below). The first consists of morphological and semantic number with unspecified values \REF{ex:key:32a}; the second states that both of these values have to coincide \REF{ex:key:32b}; that is, either both values for number are positive or both are negative.\footnote{It is not likely that these two statements are acquired for each and every noun. Rather, they are probably provided to all regular nouns by a default mechanism. Note in this regard that we will see below that there are certain elements where the specifications for morphological and semantic number can diverge. Presumably, these specifications are lexically marked on those elements and have to be acquired individually.} As for NumP, its head has the morphological number feature [αPL morph], which enters the derivation specified as positive or negative. Syntactically, recall from Chapter 1, \sectref{sec:key:4} that the head noun moves to adjoin to Num. With these points in mind, the lower part of the DP structure can be fleshed out as in \REF{ex:key:32c}, where the two statements of the noun in (32a-b) are collapsed into one:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%32
    \label{ex:key:32}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           \textit{Noun} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  a.   [αPL morph; βPL sem] 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.   (where α = β)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.      NumP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               Num’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   Num            NP  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   t\textsubscript{k}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      N\textsubscript{[αPL morph; βPL sem; α = β]k}    Num\textsubscript{[αPL morph]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With the head noun and Num in a local relation, I assume that the morphological value for number on Num and that on the noun establish an agreement relation. After the morphological number of the noun has agreed with Num, the equality statement in \REF{ex:key:32b} brings about the semantic number of the noun. This means that there is an interplay between Num and the head noun as regards number, morphologically and consequently semantically.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Before providing the detailed derivations of (31a-c) above, I state the following vocabulary entries for second-person informal pronouns, the pronominal determiners in (31b-c) above (see also Chapter 3, \sectref{sec:key:5}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%33
    \label{ex:key:33}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [+D; -AUTH, +PART; INFORMAL]      →  \textit{ihr} \textsubscript{} /   \textsubscript{} [+F, +N, -O,\textsubscript{}  {}-S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [+D; -AUTH, +PART; INFORMAL]      →  \textit{du}   /   \textsubscript{} [ -O,  {}-S]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The formal pronoun of address (i.e., \textit{Sie} ‘you(\textsc{formal})’) is discussed in the next subsection.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Returning to the data in \REF{ex:key:31}, recall that \textit{Schwein} can have three manifestations: it can be a mass, a singular count, or a plural count noun. I assume that the morphological feature [αPL morph]\textsubscript{} on NumP is syntactically optional. If it is absent, \textit{Schwein} is interpreted as a mass noun, and it occurs with a null article. The subject DP in \REF{ex:key:31a} can be illustrated in the following simplified structure:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%34
    \label{ex:key:34}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

              DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     \textit{Ø\textsubscript{D}}    NumP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Num     NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          t\textsubscript{k}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Schwein}\textsubscript{k}  Num       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textsubscript{  [αPL morph; βPL sem; α = β]}  \textsubscript{} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that the number values of the noun are not specified. I suggest that this yields a (number-neutral) mass interpretation. The apparent singularity of the mass nominal in \REF{ex:key:31a} is due to singular being a default value here. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If [αPL morph]\textsubscript{} on NumP is present, it can have two values. [-PL] results in a singular count noun, and [+PL] brings about a plural count noun. The singular pronominal DP in \REF{ex:key:31b} is illustrated as follows:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%35
    \label{ex:key:35}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

              DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{du}    NumP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     Num     NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          t\textsubscript{k}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Schwein}\textsubscript{k}  Num       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textsubscript{  [-PL morph; {}-PL sem; α = β]}  \textsubscript{[-PL morph]} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The plural counterpart in \REF{ex:key:31c} is similar, but has positive values for number. This feature specification on Num percolates up to the other terminal nodes of the structure bringing about concord in agreement features. Depending on this specification (singular or plural), the corresponding pronominal determiner in \REF{ex:key:33a} or \REF{ex:key:33b} is inserted. Observe now that in the singular and plural cases, the morphological and semantic number coincide by \REF{ex:key:32b}. Note that this yields countability as a side-effect and makes individuative or mass quantifiers (e.g., \textit{mehrere} ‘several’ vs. \textit{viel} ‘much’) compatible with individuative or mass nominals, respectively.\footnote{Again, there are other proposals. H. Wiese \& \citet[8]{Maling2005} claim that countability stems from the head noun. Second, \citet[122]{Crisma1999} argues that the indefinite article does not mark indefiniteness but brings about countability. Third, developing ideas of \citet{DelfittoSchroten1991}, Panagiotidis (2002: 58, 2003a: 421) suggests that countability originates with Num. The latter is similar to what is claimed in the main text. Also, as far as I can see, the current discussion does not need to make use of the countability feature [±COUNT]. However, the adoption of such a feature makes the formulation of the vocabulary insertion rules of determiners more straightforward (see Chapter 8, \sectref{sec:key:2.2.4}).} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I argued in Chapter 6 that bare role nouns in copular clauses do not involve NumP. Now, if there is no NumP in these instances at all, then the number values on these types of head nouns do not get specified resulting in a number neutral element (cf. also \citealt{MunnSchmitt2005}: 827). This makes bare role nouns similar to the mass nominals discussed in \REF{ex:key:34} above.\footnote{Presumably, the pragmatics will force a bare role noun to have a individuative (rather than mass) interpretation in corpular TPs. Furthermore, one might suggest that nouns on their mass interpretation also lack NumP (cf. \citealt{Borer2005}). Note, however, that these nouns can combine with adjectives. With AgrP – the location of adjectives – high in the structure, NumP is also present. Notice though that the mass interpretation of the modified noun is not necessarily turned into a count interpretation (perhaps due to the mere presence of NumP). Thus, I continue to assume that with the exception of bare role nouns, NumP is present and that the number feature on Num is syntactically optional. Having said that, note that in pronominal DPs like \textit{du Schwein}, the number feature on Num must be present – this nominal does not mean ‘you pork’, but rather ‘you (pig =) idiot’. In addition, we utilized NumP (and its number feature) to account for cases of true dis-agreement in the previous section. It seems clear then that nouns do not get an individuative interpretation in the latter instances by the pragmatics.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

With these general remarks in mind, I continue illustrating the relevant issues with some special cases involving the pronoun \textit{Sie} ‘you(\textsc{formal})’, an element rarely discussed in the literature in this respect. As before, I follow the idea that non-canonical cases may reveal the true nature of the phenomenon.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.3.2. Dis-agreement Revisited
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As detailed in \sectref{sec:key:2}, DPs involving \textit{als}{}-nominals and TPs more generally may exhibit morphological dis-agreement in number. Extending this empirical discussion, it is clear from the verbal agreement in \REF{ex:key:36a} that \textit{Sie} ‘you’ is morphologically plural. As is well known, \textit{Sie} is semantically ambiguous between singular and plural; that is, \textit{Sie} can be used to address one or several individuals. It is interesting to note that when this pronoun occurs with a non-plural noun in a copular sentence, it can have both interpretations \REF{ex:key:36a}. The presence of the floating quantifier disambiguates \textit{Sie} as having plural semantics and makes the latter reading more easily available. The DP is different. Although both \textit{Sie} and a non-plural noun can co-occur, this string only has a singular interpretation \REF{ex:key:36b}. This interpretative difference is indicated in the respective translations:\footnote{Note that the combination of formal \textit{Sie} ‘you’ and (negatively) emotive \textit{Schwein} ‘(swine =) idiot’ leads to a stylistic clash. This makes \REF{ex:key:36b} marked for some speakers. Cases similar to \REF{ex:key:36b} involve the fairly common strings \textit{Sie Armleuchter} ‘you (chandelier =) idiot’ or \textit{Sie Arsch} ‘you ass’ (see also \citealt{Darski1979}, \citealt{Roehrs2006b}).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%36
    \label{ex:key:36}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Sie  sind   (alle) Arzt}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you are.\textsc{pl} all    doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are a doctor.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You are (all) doctors.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Sie  Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you pig.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \#’you idiots’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This difference in the semantics between \REF{ex:key:36a} and \REF{ex:key:36b} resembles the cases from \sectref{sec:key:2}. However, here \textit{both} the TP and the DP involve morphological dis-agreement. This is of special interest for the nominal domain as \REF{ex:key:36b} does not involve an \textit{als}{}-nominal. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In keeping with the discussion above, I suggest below that the semantic difference between \REF{ex:key:36a} and \REF{ex:key:36b} follows from the optional presence of NumP in the clausal domain but its obligatory presence in the nominal domain. Specifically, I proposed above that semantic number is due to an interaction between Num and the head noun. I propose that NumP is absent in \REF{ex:key:36a}. Consequently, the predicate noun is number-neutral. In contrast, NumP is present in \REF{ex:key:36b}, and the unexpected grammaticality is accounted for by a structure involving two separate nominals on a par with cases discussed in Chapter 2, \sectref{sec:key:3.5}. Consider this in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Starting with the copular case in \REF{ex:key:36a}, recall from above that if there is no number morphology on the noun, then there is no NumP in the predicate nominal. Consequently, no agreement relation is established, and there is no true dis-agreement. To derive the difference between the singular and plural interpretations of the predicate nominal, I assume that there is a null counterpart of the distributivity operator \textit{alle} ‘all’ (de \citealt{SwartEtAl2007}: 218). I label this null element as DIST. I suggest that it is optionally present (when \textit{alle} is absent):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%37
    \label{ex:key:37}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Sie  sind} (DIST) \textit{Arzt}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  you are.\textsc{pl}            doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If this operator is absent, we derive the singular interpretation; if the operator is present, we obtain the plural reading. Note in passing that this distributivity operator is incompatible with subjects that have singular semantics (i.e., the operator cannot distribute the relevant predicate over just one entity). As such, unlike \textit{Sie}, \textit{du} ‘you(\textsc{sgl})’ combined with a non-plural noun (e.g., \textit{Du bist Arzt} ‘You are a doctor’) can only be singular in interpretation.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As for \REF{ex:key:36b}, I argued above that DPs always contain NumP. At first glance, this then appears to be a case of true morphological dis-agreement between the plural pronominal determiner and the singular nominal. In \sectref{sec:key:3.2}, similar cases were ruled out by suggesting that the singular nominal cannot establish an agreement relation with a plural pronominal determiner. Now, in order to explain the unexpected grammaticality of this example, I suggested in \citet{Roehrs2006b} that these types of DPs have a structural analysis different from the canonical DPs discussed earlier. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Specifically, it is proposed in that paper that this construction involves two nominals, a matrix nominal containing the pronominal determiner and an embedded nominal containing the dis-agreeing predicate nominal. The latter is proposed to be located in a specifier inside the matrix pronominal DP. For lack of a better term, this position was labeled as specifier of a Disagreement Phrase (DisP) in Chapter 2, \sectref{sec:key:3.5}, where cases like \textit{ihr junges} \textit{Gemüse} ‘you young (vegetable =) folks’ were discussed. Abstracting away from movement, the pronominal DP in \REF{ex:key:36b}, repeated below as \REF{ex:key:38a}, can be illustrated as a first approximation as in \REF{ex:key:38b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%38
    \label{ex:key:38}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{Sie  Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you pig.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Apparent} \textit{Dis-agreement in the DP}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           DP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        \textit{Sie}            DisP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [\textsubscript{NumP} Num\textsubscript{[-PL]} [\textsubscript{NP} \textit{Schwein}]]\textsubscript{<e,t>}  Dis’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

        Dis           NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                  Num\textsubscript{[+PL]}   NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              \textit{e\textsubscript{N}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Having claimed above that NumP has a specification for morphological number, I propose that both the matrix and the embedded nominal have a NumP where the value of the former is plural, but the value of the latter is singular. In order to account for the absence of a featural clash between these two nominals in \REF{ex:key:38b}, I blame the lack of obligatory concord between these two nominals on the absence of a head (Mod) that mediates concord in agreement features between these nominals (for more details and other cases, see Chapter 8, \sectref{sec:key:3.1}). To be clear, then, both nominals in \REF{ex:key:38b} form independent agreement domains, and consequently, there is no true morphological dis-agreement. These are the basic assumptions of pronominal DPs involving \textit{Sie} ‘you’. Next, I flesh out some of the details of the derivation in \REF{ex:key:38b}, specifically those about number.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.3.3. Pronominal DPs Involving \textit{Sie} in More Detail
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As briefly alluded to above, the two number values, morphological and semantic, can also diverge; for instance, pluralia tantum nouns such as \textit{Ferien} ‘vacation/holidays’ are morphologically plural but semantically singular (i.e., they involve one entity or event).\footnote{For Pesetsky \& \citet[263]{Torrego2007}, English pluralia tantum nouns like \textit{scissors} are also taken to have a lexically valued feature for number (see also \citealt{Borer2005}: 105).} In other words, they are lexically specified such that they can only occur in the corresponding context in \REF{ex:key:39a}. Furthermore, I assume that \textit{Sie} ‘you’ has a specification for morphological number but that it does not have a fixed value for semantic number – it can be used to address one or several individuals \REF{ex:key:39b}:\footnote{Note that it might be possible to restate the restriction to a certain morphological number in (39a-b) by employing restriction features (see the discussion of uninflected \textit{dies} ‘this’ in Chapter 4, \sectref{sec:key:5} and Chapter 8, Sections 2.2.5 and 2.2.6). However, I continue with the system laid out in \citet{Roehrs2006b}. Notice also that since \textit{Sie} can be merged in semantically singular or plural contexts, the specification for semantic number involving a variable in \REF{ex:key:39b} could presumably be left out. For reasons of parsimony, I continue with the statement in \REF{ex:key:39b}. Interestingly, the Dutch counterpart \textit{u} ‘you(\textsc{formal})’ is morphologically singular and semantically singular or plural. On current assumptions, its feature specification would be [-PL morph; βPL sem].} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%39
    \label{ex:key:39}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  pluralia tantum noun  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [+PL morph; -PL sem]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Sie}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [+PL morph; βPL sem]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To be clear, compared to regular nouns (\sectref{sec:key:3.3.1}), these elements have different values for morphological and semantic number, and (consequently) they lack the equality statement “α = β”. Furthermore, while both values are fixed with pluralia tantum nouns, the value for semantic number of \textit{Sie} is unspecified. I assume that this value is determined by the head noun in the complement position of \textit{Sie} mediated by NumP as discussed in \sectref{sec:key:3.3.1}. To be clear, these lexically specified elements can only be inserted in contexts that are compatible with the featural specification of their vocabulary entries. Before I return to the intriguing case of \textit{Sie} occurring with a non-plural noun, I discuss the derivation of \textit{Sie} in the environment of a plural noun (where both elements agree in morphological number). This lays the groundwork for the remaining discussion.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Considering \REF{ex:key:40a}, note that all number values of \textit{Sie} in \REF{ex:key:40b} are positive. The morphological number [+PL morph] on \textit{Sie} is compatible with the morphological number on Num. The semantic number [βPL sem] on \textit{Sie} is positive. Specifically, due to the interaction of the head noun and Num, the semantic number on the noun is positive and, consequently, only a semantically plural number on \textit{Sie} is compatible with a semantically plural number on the noun:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%40
    \label{ex:key:40}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Sie         Schweine}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) pigs      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you idiots’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.    DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{Sie}    NumP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textsubscript{  [+PL morph; +PL sem}%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textsubscript{]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Num     NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          t\textsubscript{k}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{Schweine}\textsubscript{k}  Num       
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textsubscript{  [+PL morph; +PL sem; α = β]}  \textsubscript{[+PL morph]} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This derivation involving formal \textit{Sie} ‘you’ is similar to informal \textit{ihr Schweine} ‘you idiots’, briefly discussed in \sectref{sec:key:3.3.1}. With this in place, I provide more details of the derivation in \REF{ex:key:38b}, where \textit{Sie} occurs in the context of a non-plural noun. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

For easier readability, I illustrate the pronominal DP in \REF{ex:key:41a} in two parts: \REF{ex:key:41b} shows the matrix nominal, and \REF{ex:key:42} further below illustrates the embedded part XP located in Spec,DisP in \REF{ex:key:41b}. Starting with the matrix nominal, I assume that the complement of \textit{Sie} in \REF{ex:key:41a} is not \textit{Schwein} itself but involves a null pluralia tantum noun. Since both \textit{Sie} and this type of noun are lexically specified for [+PL morph], both elements are morphologically compatible. However, a pluralia tantum noun is only compatible with a negative value on \textit{Sie}’s [βPL sem] statement. This leads to singular semantics of \textit{Sie}:\footnote{This is different from the copular case \textit{Sie sind Arzt} ‘(you are doctor =) you (all) are doctors’ discussed earlier. Here, \textit{Sie}, the subject DP, has an ordinary null noun as part of its complement structure. This null noun has the familiar specification [αPL morph; βPL sem; α = β]. Mediated by NumP, the subject DP (i.e., \textit{Sie} Num e\textsubscript{N}) gets plural semantics (note also that given the null elements, this makes \textit{Sie} structurally similar to \textit{Sie Schweine} ‘you idiots’). The subject DP is then combined with the non-plural predicate noun \textit{Arzt} ‘doctor’ via the head Pr and the auxiliary (Chapter 6). An assumed null distributivity operator accounts for the compatibility of plural \textit{Sie} and the non-plural noun (cf. \REF{ex:key:37}).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%41
    \label{ex:key:41}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Sie  Schwein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you pig.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (idiot)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.    DP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{Sie}\textsubscript{<<e,t>e>}    DisP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textsubscript{  [+PL morph; -PL sem]} %%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      XP\textsubscript{<e,t>}  Dis’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Dis           NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                Num\textsubscript{}    NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              t\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      \textit{e\textsubscript{N}}\textsubscript{i}          Num
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textsubscript{                   [+PL morph; -PL sem]        [+PL morph]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As for the embedded nominal, this NumP – distinguished by the subscript 2 below – is specified as [-PL morph], and the ordinary noun \textit{Schwein} comes out as a singular count element:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%42
    \label{ex:key:42}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

                NumP\textsubscript{2} (= XP)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               Num’\textsubscript{2}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              Num\textsubscript{2}          NP  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                   t\textsubscript{k}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   \textit{Schwein}\textsubscript{k}           Num\textsubscript{2}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textsubscript{           [-PL morph; {}-PL sem; α = β]        [-PL morph]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To recapitulate, the matrix nominal involves a plural NumP and a (null) pluralia tantum head noun, and the embedded nominal contains a singular NumP and an (overt) ordinary head noun. In other words, although there are two NumPs in \REF{ex:key:41b} and \REF{ex:key:42} with different (morphological) specifications for number, both nominals are semantically singular. I now turn to the question as to how the two nominals in \REF{ex:key:41b} and \REF{ex:key:42} are semantically combined.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I assume that the null noun in the matrix nominal is of the kind type. For this element to function as a predicate, it must combine with REL in NumP. Suppose now that the matrix nominal and the embedded nominal in \REF{ex:key:41b} and \REF{ex:key:42} are combined by Predicate Modification, which conjoins two elements of the same semantic type (i.e., <e,t>). In order to avoid type mismatch, the embedded predicate nominal must be of the same type. This is true. With the kind noun \textit{Schwein} ‘(pig =) idiot’ present there, the latter must also involve REL in NumP (to bring about a predicate), which is in line with the assumptions of Chapter 6. Thus, we have justification for the assumption of two NumPs, one in the matrix nominal and one in the embedded one. Furthermore, assuming that Predicate Modification only combines two nominals of the same general semantics, I can also account for the fact that the semantically singular nominal in Spec,DisP is only compatible with a semantically singular matrix nominal (for more details, see \citealt{Roehrs2006b}: 168). This explains the lack of a plural reading in \textit{Sie Schwein} ‘you (pig =) idiot’.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up, following the literature, I suggested that morphological number originates with NumP. Furthermore, I proposed that semantic number is the result of an intricate interplay between Num and the head noun. In addition, I considered some non-canonical cases in copular TPs and pronominal DPs, where \textit{Sie} occurs in the context of a non-plural noun. I explained the grammaticality of \textit{Sie} in the copular cases in the same fashion as in \sectref{sec:key:3.2}, namely by the syntactic optionality of NumP in the clause: in this particular instance, NumP is absent. As for the pronominal DPs, I accounted for the grammaticality of \textit{Sie} here by proposing a different structure where the pronominal determiner is in the matrix DP, but the non-plural noun forms a complex specifier embedded in the matrix DP. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

More generally, I proposed that NumP is present for cartographic reasons in noun phrases bigger than NP. This means that all noun phrases (except those with mass and bare role nouns) involve morphological number. Semantic number may come about in one of three ways: (i) it is lexically specified on the head noun (pluralia tantum), (ii) it originates with the (regular) kind noun if [αPL morph; βPL sem] is specified via the morphological value of Num and the equality statement “α = β”, or (iii) semantic number of bare role nouns in copular sentences stems from the pragmatics (in the singular cases; see Footnote Error: Reference source not found) or a distributivity operator (in the plural cases; see \REF{ex:key:37} above). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If correct, this discussion leads to some interesting issues.\footnote{I mention just one issue in this context. As pointed out in the main text, the kind noun in the embedded nominal in \REF{ex:key:42} requires the presence of REL to supply a predicate. REL, in turn, should trigger the presence of \textit{ein}. However, this yields an ungrammatical example:  (i)  *   \textit{Sie} [\textsubscript{ArtP} \textit{‘n} \textit{Schwein} ] \textit{e\textsubscript{N}}    you        \textsubscript{} a pig.\textsc{neut}To explain the ungrammaticality, there is more involved here than in cases like *\textit{du ‘n Schwein} ‘you a (pig =) idiot’ (see \sectref{sec:key:3.2.5}) – notice that \textit{Sie} and \textit{ein} in (i) are in different nominals. Without going into too much detail, I suggest that \textit{ein} is deleted in the presence of the adjacent pronominal determiner. Note in this regard that presumably, the latter is retained as it has semantics unlike vacuous \textit{ein}.} I will not pursue these questions here. My main goal in this section was to show that elements other than \textit{ein} can be held responsible for morphological and semantic number. If this is tenable, then I can continue claiming that \textit{ein} is not a reflex of number, be it morphological or semantic – it is a semantically vacuous element (Hypothesis 1a). Next, I consider constructions involving \textit{als}{}-nominals in more detail. While these instances have certain similarities to the dis-agreement cases discussed above, they are different in a number of ways.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{3.4.  Agreement in Constructions Involving als}{}-nominals
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, I discuss \textit{als}{}-constructions, nominal and clausal constructions involving \textit{als} ‘as’ and a following noun, the latter two elements labeled as \textit{als}{}-nominals. I begin by showing how \textit{als}{}-nominals combine with the preceding part of their structures. This includes a brief discussion of the semantic types and \textit{als} as a realization operator. Then I address the agreement facts with regard to number.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.4.1. Structure and Semantics
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In Chapter 6, \sectref{sec:key:3.4.2}, I offered the tentative claim that \textit{als} ‘as’ in the DP is a generalized capacity operator that combines with a noun, be it a role or a kind noun, to return an element of type <e,t>. I suggested there that syntactically, the \textit{als}{}-nominal is right-adjoined to its preceding structure and that semantically, this \textit{als}{}-nominal combines with another element by Predicate Modification (which combines two elements of type <e,t>). As such, I proposed that the \textit{als}{}-nominal is adjoined to an element of type <e,t>. Recalling that \textit{du} ‘you’ itself is argued to have internal structure, NumP is such an element. I also suggested in that section that \textit{als} is the head of ModP and I proposed that this ModP is adjoined to NumP of the pronominal DP. The resultant structure is slightly adapted here, deriving \REF{ex:key:43a} as in \REF{ex:key:43b}: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%43
    \label{ex:key:43}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{du           als Arzt}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) as  doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you as a doctor’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.      DP\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{du}\textsubscript{<<e,t>e>}            NumP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              (Predicate Modification)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          NumP\textsubscript{<e,t>}                     ModP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      Num\textsubscript{REL<e<e,t>>}     NP       \textit{als}           NP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                       \textit{e\textsubscript{N}}\textsubscript{<e>}                     \textit{Arzt}\textsubscript{<e>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As for the non-copular TP, de Swart \textit{et al}. (2007: 218) assume that \textit{als}{}-nominals are verb modifiers. I propose that the \textit{als}{}-nominal in the clausal cases is adjoined to the VP. Note that intransitive verbal predicates are of type <e,t>. Semantically, this is the same type as that of \textit{als}{}-nominals. The simplified structure of \REF{ex:key:44a} is provided in \REF{ex:key:44b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%44
    \label{ex:key:44}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Du          \textsubscript{} }\textit{sprichst als Arzt.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) speak     as  doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘You speak as a doctors.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  TP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{du}\textsubscript{k}                 T’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           \textit{sprichst}\textsubscript{i}          \textit{v}P
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               t\textsubscript{k}      VP\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

                        VP\textsubscript{<e,t>}  [\textsubscript{ModP} \textit{als Arzt}]\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              t\textsubscript{i}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These are the basic assumptions about the syntax and semantics. Before moving on to the agreement facts, I specify the role of \textit{als} in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In Chapter 6, we saw that CAP and REL are both possible with role nouns. Recall that CAP involves direct predication but that REL involves an indirect path to predication. Note now that these realization operators are mutually exclusive – only one is needed to supply a predicate, and the presence of a second would lead to type mismatch. In other words, CAP must be, at least in principle, optional. I now make the assumption that all realization operators are optional in type but not in number; that is, while there is a potential choice between different operators, the presence of one such operator is required to supply a predicate. This might lead us to expect that there are other realization operators. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As tentatively suggested in Chapter 6, \sectref{sec:key:3.4.2}, I take \textit{als} to be such an element. Similar to the two other elements, if \textit{als} is present, both CAP and REL cannot be. I reiterate the proposal that \textit{als} takes all elements of type <e>, that is, role nouns or kind nouns alike, as arguments and returns predicate nominals (type <e,t>). In other words, \textit{als} is a general(ized) capacity operator of type <e,<e,t>>. While of the same semantic type, this makes this element different from CAP and REL. I turn to the agreement facts.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.4.2. Agreement 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with the (non-copular) TPs, I assume that agreement in morphological number between the subject and the \textit{als}{}-nominal is established independently of the presence of the verb. In a way, this makes the adjunction of the \textit{als}{}-nominal in the clause similar to a displaced relative clause – in both instances, the two related elements are separated by a verb.\footnote{Below is an example of a displaced relative clause:  (i)  \textit{Er hat das Haus gekauft, das nicht so teuer         war}.    he has the house bought  that not    so expensive was    ‘He bought the house that was not so expensive.’} Note that this parallel pattern fits well with the discussion in Chapter 6, \sectref{sec:key:3.4.1}, where I argued that \textit{als}{}-nominals in pronominal DPs are similar to relative clauses. Consequently, I treat the \textit{als}{}-nominals in the non-copular TPs and the ones in the pronominal DPs in a related way. Below, I put the verb in parentheses discussing both the TPs and the DPs at the same time.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

We saw in \sectref{sec:key:3.1} that plural morphology on the noun indicates the presence of NumP. Assuming that the number specification of the predicate nominal inside the \textit{als}{}-nominal has to establish an agreement relation with the pronominal element, this rules out \REF{ex:key:45a} but allows \REF{ex:key:45b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%45
    \label{ex:key:45}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a. *  \textit{du          (sprichst) als Ärzte}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) speak      as   doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.   \textit{ihr      \textsubscript{} }\textit{(sprecht) als Ärzte}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) speak     as  doctors
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (speak) as doctors’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In contrast, a non-plural noun in the \textit{als}{}-nominal does not project NumP. It involves NP and does not enter into an agreement relation with the pronominal element. As such, both \REF{ex:key:46a} and \REF{ex:key:46b} are fine:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%46
    \label{ex:key:46}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{du          (sprichst) als Arzt}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{sgl}) speak      as  doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (speak) as a doctor’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{ihr       \textsubscript{} }\textit{(sprecht) als Arzt}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl}) speak     as   doctor.\textsc{masc}      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (speak) as doctors’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I assume that the plural interpretation in \REF{ex:key:46b} follows from the presence of a null distributivity operator just like in the copular cases. So far, the agreement facts are similar to what we saw above. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Turning to singular nouns, I observed in \sectref{sec:key:2.3} that the presence of \textit{ein} leads to awkwardness in a singular context \REF{ex:key:47a}. As also pointed out in that section, a singular \textit{als}{}-nominal is ungrammatical with a plural pronominal \REF{ex:key:47b}, where the presence of a non-copular verb and quantifier yields a somewhat better result than the counterpart without those two elements (this correlation in \REF{ex:key:47b} is indicated by two sets of parentheses):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%47
    \label{ex:key:47}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  ?  \textit{du            (sprichst) als ‘n Arzt}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(SGL) speak      as  \textsubscript{}  a doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you (speak) as a doctor’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.(??)?* \textit{ihr       \textsubscript{} }\textit{(sprecht alle) als ‘n Arzt}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(PL) speak    all    as    a doctor.\textsc{masc}      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The agreement facts in \REF{ex:key:47} are similar to the copular constructions discussed in \sectref{sec:key:3.2} (cf. also Tables 1 and 2). There is only one exception: the presence of \textit{ein} in a copular construction such as \textit{Du bist ‘n Arzt} ‘You are a doctor’ is completely fine for many speakers, but the presence of \textit{ein} in an \textit{als}{}-nominal like \REF{ex:key:47a} leads to a certain degree of awkwardness. Note though that the latter case involves \textit{als} ‘as’.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In order to account for \REF{ex:key:47}, I propose that syntactically, \textit{als} has intermediate characteristics as regards CAP and REL. To see this, I briefly return to \REF{ex:key:45} and \REF{ex:key:46}. I suggest for \REF{ex:key:45} that \textit{als} is in NumP with a plural noun (similar to REL) but I assume for \REF{ex:key:46} that it is in NP with a non-plural noun (similar to CAP) – the first case will force morphological agreement, the second will not.\footnote{This means that Mod takes different complements (NP, NumP) and that \textit{als} might move to Mod.} This rules out the plural noun in \REF{ex:key:45a} but allows the non-plural noun in \REF{ex:key:46b}. Turning to the singular nouns in \REF{ex:key:47}, I propose that \textit{als} does not trigger \textit{ein}. This makes \textit{als} similar to CAP and explains the markedness of \REF{ex:key:47a}. As to the more marked \REF{ex:key:47b}, \textit{als} does not bring about \textit{ein} either, and additionally, no agreement relation between the plural pronominal element and the singular noun in the \textit{als}{}-nominal is established.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up the entire section, the preceding discussion revealed an interesting syntax-semantics correlation: cases of morphological agreement are very restricted in their interpretations, but instances of apparent morphological dis-agreement (copular cases and \textit{als-}nominals) are fairly free. Given the proposed interaction between Num and the head noun, this difference follows from the presence of NumP in the first cases but its absence in the second. Importantly, the role of \textit{ein} is only indirect. Its presence allows us to draw conclusions about the syntactic structure and the type of realization operator present in that structure (i.e., REL). As seen throughout this section, \textit{ein} does not specifify number, neither morphologically nor semantically.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.  Conclusion
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This chapter provided the second and final consequence of the proposals involving \textit{ein}, here focusing on number. Continuing the investigation from the previous chapter, I provided a survey of the data and discussed issues pertaining to morphological and semantic number in the nominal and clausal domains. I showed that the DP is more restricted in this regard than the TP. I proposed that this is accounted for by the obligatory presence of NumP in the DP (a reflex of the cartography of the DP) and its optional syntactic presence in the TP. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Specifically, I proposed that morphological number resides in Num and semantic number is a result of an intricate interaction between Num and the head noun. With current purposes in mind, I can conclude that \textit{ein} itself does not determine number, neither morphologically nor semantically. Rather, we have seen that \textit{ein} indicates the presence of a certain amount of structure on top of NP (Hypothesis 3a) and that it flags the presence of the operator REL (Hypothesis 3b). I also showed that elements other than \textit{ein} can flag the presence of REL precluding \textit{ein} from occurring. Again, these facts find a natural explanation if \textit{ein} is semantically vacuous (Hypothesis 1a); that is, \textit{ein} is an element that can be absent without a loss in meaning. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  More generally, to the extent that the discussion here proves tenable, it provides further support for de Swart \textit{et al}.’s (2007) proposal, and more generally, it strengthens the parallels between the DP and TP. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\setcounter{page}{323}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Chapter 8: Discussion and Concluding Remarks
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.  The Bigger Picture
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Focusing on German, one goal of this book was to provide a more comprehensive discussion of adjectival inflections and \textit{ein} compared to that which exists in the literature. Beside an overview of these important empirical subdomains of the noun phrase, the second goal was to find commonalities and differences between these two types of elements and to discuss certain theoretical issues that arise in these contexts. It is important to point out again that both goals go in partially different directions. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Providing an overview aims to be fairly exhaustive, but painting the big picture is an attempt to see what different domains, empirical or theoretic, have in common. As such, discussing issues to some comprehensive degree in one domain tends to move the focus away from traits shared by the different domains. In this final chapter, I try to make more headway toward the second goal; that is, toward comparing both adjectival inflections and \textit{ein} more directly. In the second part of this chapter, I discuss some further consequences of the current analysis, show avenues for future research, and draw some more general conclusions. Before I compare adjectival inflections and \textit{ein} in more detail, recall the general contents of the previous chapters. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In Chapter 1, I laid out my general assumptions and formulated the main claims of this book. Chapter 2 focused on adjectival inflections and included a detailed investigation of the special role of \textit{ein}{}-words in this regard. In Chapter 3, variation was discussed leading to the postulation of two secondary mechanisms that regulate adjectival endings, and in Chapter 4, some consequences of the analysis for other accounts were addressed. Chapter 5 provided an overview of the different types of \textit{ein}. This was followed by \textit{ein} being discussed with regard to emotiveness and number in Chapters 6 and 7, respectively. The analysis of adjectival inflections developed earlier helped narrow down the choices of plausible analyses in later chapters.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Contrasting these phenomena, we observed that the analysis of adjectival inflections and that of \textit{ein} are closely connected. In fact, I argued that adjectival inflections and \textit{ein} are similar in certain aspects but different in others. I review the main hypotheses discussed throughout this book starting with the similarities followed by the differences. Besides providing a summary and drawing some conclusions, I add a few other details.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{1.1.   Commonalities of Adjectival Inflections and} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I formulated the following two main claims:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%1
    \label{ex:key:1}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           \textit{Hypothesis 1}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Adjectival inflections and \textit{ein}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      a.     are expletive elements and
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      b.     indicate abstract structure in the noun phrase.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Beginning with Hypothesis 1a, we have seen that both adjectival endings and \textit{ein} are not a reflex of (in-)definiteness. While both elements share this characteristic, evidence for Hypothesis 1a has also come from other, different empirical domains. Specifically, adjectival inflections have nothing to do with the restrictiveness of the interpretation of modifiers or “referentiality”, and \textit{ein} is not an exponent of emotiveness and number. These points were summarized in the statement that adjectival inflections and \textit{ein} are semantically vacuous.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As for Hypothesis 1b, both adjectival inflections and \textit{ein} indicate abstract structure. However, they do so in different ways. Compare Hypothesis 2a to Hypothesis 3a:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%2
    \label{ex:key:2}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Hypothesis 2}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Adjectival inflections:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   a.  indicate abstract structure in the higher layers of the noun phrase (DP vs. LPP),       [and] they provide clues about structures involving various degrees of embedding       of adjectives (simple vs. complex DPs).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%3
    \label{ex:key:3}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Hypothesis 3}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{Ein}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   a.  indicates abstract structure in the lower layers of the noun phrase (NP vs. ArtP).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

While both adjectival inflections and \textit{ein} indicate certain sizes of abstract structure, the former also provides clues about different types of embeddings. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In what follows, I briefly review some of the evidence for Hypotheses 1, 2a, and 3a below. Hypotheses 2b (adjectival endings make features visible) and 3b (\textit{ein} supports or flags operators), which deal with differences between adjectival inflections and \textit{ein}, are discussed in \sectref{sec:key:1.2}. I begin with the intersecting semantic concept of (in-)definiteness, where both types of elements not only show a similar lack of semantic import but also directly interact with one another. Following that, I repeat one more argument from the other, different empirical domains showing that both types of elements are semantically vacuous: adjectival inflections do not indicate the restrictiveness of the interpretation of modifiers, and \textit{ein} is not related to number. Finally, I review the main facts that show that both types of elements provide clues about abstract structure (the indication of different types of embeddings by adjectival inflections is left for the section dedicated to the differences – \sectref{sec:key:1.2}).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.1.1. Some Evidence for Hypothesis 1a
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Starting with (in-)definiteness, consider again cases in the nominative, the (a)-examples, and in the dative, the (b)-examples: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%4
    \label{ex:key:4}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein gut-er    Wein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a    good-\textsc{st} wine.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a good wine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mit   ein-em gut-en     Wein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with a-\textsc{st}     good-\textsc{wk} wine.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with a good wine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%5
    \label{ex:key:5}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{sein gut-er    Wein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    his   good-\textsc{st} wine.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘his good wine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mit   sein-em gut-en     Wein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with his-\textsc{st}    good-\textsc{wk} wine.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with his good wine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Traditionally, the article \textit{ein} ‘a’ is taken to be an indefinite element, but the possessive article \textit{sein} ‘his’ is a definite one. With that in mind, compare the (a)-examples to their (b)-counterparts. We observe that indefinite contexts exhibit strong and weak adjectives \REF{ex:key:4} but that definite environments also show strong and weak adjectives \REF{ex:key:5}. Put differently and contrasting the (a)-examples to each other and the (b)-examples to each other, strong adjectives occur in both indefinite and definite contexts, and weak adjectives also surface in both indefinite and definite environments. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Furthermore, what is sometimes left unmentioned in this context is that determiners can have the exact same inflections as adjectives (and that is why I referred to these inflections as adjectival, rather than adjective, endings). Considering the (b)-examples above, we note that both the (indefinite) article and the (definite) possessive article have strong inflections as their own endings. Indeed, I discussed one case in Chapter 3, \sectref{sec:key:4}, where certain (definite) \textit{der}{}-words may optionally have a strong or a weak ending. Considering the distributions of these inflections, we can observe again that these are clearly contradictory patterns – there is no association of adjectival inflections with (in-)definiteness.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Recall though that I proposed in Chapter 5 that possessive articles consist of a possessive component and \textit{ein} (e.g., \textit{s-ein} ‘his’). If we assume that \textit{ein} itself determines the adjectival inflections, then we can observe that \textit{ein} occurs with a strong adjective in the nominative but with a weak adjective in the dative. This would be consistent with the claim that \textit{ein} itself is indefinite, and the inflections on adjectives would vary according to morphological case. However, weak adjectives are also possible in the nominative \REF{ex:key:6a}, and strong adjectives can also appear in the dative \REF{ex:key:6b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%6
    \label{ex:key:6}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{seine gut-en     Weine}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    his    good-\textsc{wk} wines
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘his good wines’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{mit   gut-em   Wein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with good-\textsc{st} wine.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with good wine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Thus, there is no correlation between case and the strong/weak alternation on the adjective either.   These contradictory points disappear once we claim that neither adjectival inflections nor \textit{ein} are related to (in-)definiteness (or case). Rather, both of these elements are semantically vacuous and interact with each other morpho-syntactically. I proposed in Chapter 2 that determiners including \textit{ein} trigger Impoverishment on the adjective yielding the weak endings. There is one exception: instances of \textit{ein} specified as [-N, -O] are special – in this featural context (along with the absence of positive features for definiteness and deixis), [+D] does not trigger Impoverishment on the adjective. It is clear that adjectival inflections and \textit{ein} are directly related albeit not by the semantics.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Turning to the other, differing empirical domains, I point out again that adjectival endings do not indicate the restrictiveness of the interpretation of modifiers. Considering \REF{ex:key:7}, notice that both a restrictive and a non-restrictive adjective can have a weak ending:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%7
    \label{ex:key:7}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{der alt-e     \textsubscript{} }\textit{Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the \textsubscript{} old\textsc{{}-wk} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the man that is old’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{der (übrigens)    \textsubscript{} }\textit{alt-e     Mann}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the  \textsubscript{} incidentally old-\textsc{wk} man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘the man, who is (by the way) old’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recalling that determiners move from ArtP to DP, I proposed in Chapter 4 that the determiner is interpreted above the adjective in \REF{ex:key:7a} but below the adjective in \REF{ex:key:7b}. The latter also involves a coindexed \textit{pro}. The weak endings on the adjectives are the result of Impoverishment applying in a canonical DP structure. This analysis is consistent with the fact that adjectival endings involve no semantics.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As for \textit{ein}, I claimed that this element is not associated with number (and related countability). To illustrate this claim again, notice that the example in \REF{ex:key:8a} denotes a singularity, and the ones in (8b-c), taken from the Appendix, involve pluralities:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%8
    \label{ex:key:8}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Nur}  \textit{EINE Katze    war auf dem Hof!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    only one \textsubscript{} cat.\textsc{fem} was in   the  yard
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Only one cat was in the yard!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \%  \textit{Och~}\emph{so n-e  süssen}\textit{~Katzis... :-)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

oh,  so a-\textsc{pl} cute    kittens
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Oh, such cute kittens!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    c.  \%  \textit{Gerade gesehen... was   für eine Idioten, die   so Aufmerksamkeit wollen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    just       seen          what for  a-\textsc{pl} idiots    who so attention             want
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Just seen… what kind of idiots that want attention like that’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Contrasting these two types of examples, we observe that \textit{ein} can appear in very diverse semantic contexts. Again, given that \textit{ein} appears to be associated with different, contradictory semantics, I proposed that this element is also semantically vacuous. I suggested in Chapter 5 that \textit{EIN} in \REF{ex:key:8a} consists of \textit{ein} and the null operator \textit{Ø}\textsubscript{[-PL]} inducing singularity, that \textit{ein} makes null operators visible (8b-c), and more generally that number is due to an interaction between the head noun and Num (Chapter 7). I return to the discussion of (8b-c) in \sectref{sec:key:2.2.3} below.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given these facts, we expect that adjectival inflections and \textit{ein} can be left out without a change in meaning. I showed that this is indeed the case in certain lexical contexts, with specific sets of adjectives in non-elliptical contexts \REF{ex:key:9a} and with role nouns in predicate nominals \REF{ex:key:9b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%9
    \label{ex:key:9}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Das ist ein lila(nes)      Buch}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this is   a    purple-\textsc{infl} book.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘This is a purple book.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Er ist (ein) Lehrer}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is    a     teacher.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘He is a teacher.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Furthermore, the suggestion that both types of elements are semantically vacuous also allows us to avoid the conclusion that certain semantic features are redundantly present when these elements do co-occur. Again, this holds for both adjectival inflections and \textit{ein} (I return to \REF{ex:key:10b} in \sectref{sec:key:2.2.2}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%10
    \label{ex:key:10}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{gut-e         teur-e                Autos} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    good-\textsc{infl} expensive-\textsc{infl} cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘good expensive cars’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \%  \textit{k-eine so’ne Autos}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsc{neg}{}-a so a   cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘no such cars’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If these elements are indeed semantically vacuous, then the actual semantics involved is brought about in a different way. Discussing the various cases, I made remarks to such effect in the previous chapters.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.1.2. Some Evidence for Hypothesis 1b: Hypothesis 2a vs. 3a
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Hypothesis 1b, the indication of abstract structure, consists of two subclaims: Hypothesis 2a and Hypothesis 3a. I proposed that adjectival inflections and \textit{ein} indicate differences in nominal structures with regard to size. For instance, the strong/weak alternation supports the claim that synactic arguments are not only DPs \REF{ex:key:11a} but also LPPs \REF{ex:key:11b}. Note in this regard that \textit{alle} ‘all’ is followed by an element with a weak inflection in \REF{ex:key:11a} but by an element with a strong ending in \REF{ex:key:11b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%11
    \label{ex:key:11}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [\textsubscript{DP} \textit{all-e  nett-en   Studenten} ]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         all-\textsc{st} nice-\textsc{wk} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [\textsubscript{LPP} \textit{all-e} [\textsubscript{DP} \textit{dies-e    nett-en   Studenten} ]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          all-\textsc{st}       these-\textsc{st} nice-\textsc{wk} students
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘all these nice students’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I argued in Chapter 2 that Impoverishment only occurs in simple, canonical DPs \REF{ex:key:11a}, the domain in which the determiner undergoes movement. The strong ending on \textit{diese} ‘these’ in \REF{ex:key:11b} followed from the assumption that this nominal involves a non-canonical structure. Unlike in \REF{ex:key:11a}, \textit{alle} ‘all’ in \REF{ex:key:11b} is base-generated in LPP. Consequently, Impoverishment does not occur.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Turning to structural differences indicated by \textit{ein}, I sided with de Swart \textit{et al}. (2007) in arguing that predicate nominals without \textit{ein} involve NPs \REF{ex:key:12a} and that those with \textit{ein} are bigger – under my assumptions (at least) ArtPs \REF{ex:key:12b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%12
    \label{ex:key:12}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Er ist} [\textsubscript{NP} \textit{Lehrer} ].
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is        teacher.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘He is a teacher.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Er ist} [\textsubscript{ArtP} \textit{ein Mann} ].
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    he is  \textsubscript{} a    man.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘He is a man.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I followed the above-mentioned authors in that \REF{ex:key:12a} involves a role noun with CAP in NP but that \REF{ex:key:12b} contains a kind noun with REL in NumP, the latter being flagged by \textit{ein} in ArtP. More generally, observe again that adjectival inflections indicate differences in structure in the higher layers of the noun phrase but that \textit{ein} shows distinctions in the lower projections. This is consistent with another fact.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As just seen, role nouns can appear in predicative contexts without an article. Importantly, role nouns require a determiner when they appear in argument position: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%13
    \label{ex:key:13}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{*(Ein) Lehrer          braucht Unterstützung}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     a      teacher.\textsc{masc} needs    support
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘A teacher needs support.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Considering \REF{ex:key:13}, it could be claimed that \textit{ein} brings about the semantic change from a predicate nominal \REF{ex:key:12a} to an argumental expression \REF{ex:key:13} and that \textit{ein} would then be tied to the semantics after all. However, there is an independent explanation for the occurrence of \textit{ein} in \REF{ex:key:13}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet{Longobardi1994} proposed that argumental nominals project DPs (also \citealt{Borer2005}: 65-66, \citealt{Stowell1989}). For role nouns to occur in argumental expressions, more structure beyond NP must be projected. As discussed in Chapter 6, part of this structure is NumP which involves REL. The latter is flagged by \textit{ein}. We can point out that it is not \textit{ein} itself that turns a predicate nominal into an argumental expression – it is more structure. Having said that, \textit{ein} does not indicate the presence of the DP-level either: many speakers allow \textit{ein} to appear with role nouns in predicative contexts, and kind nouns appear with \textit{ein} in such contexts more generally. As discussed above, both of the latter nominals only involve ArtP (not DP) and yet, they contain \textit{ein} (due to REL). This means that \textit{ein} does not necessarily indicate a DP. The reason why argumental noun phrases require a DP-level is that this phrasal layer involves a person feature \citep{Longobardi1994}. In other words, it is the structure involving the person feature (and not \textit{ein}) that brings about an argumental noun phrase.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Finally, I restate the observation that adjectival inflections and \textit{ein} interact morpho-syntactically calling into question certain structural claims of other proposals. In Chapter 4, \sectref{sec:key:2}, I pointed out that \textit{ein} is possible in certain \textit{wh}{}-exclamatives \REF{ex:key:14a} and in constructions involving \textit{so} ‘such’ \REF{ex:key:14b}. Now, while not all speakers of German allow \textit{ein} to occur in these contexts, those who do have a weak ending on the adjective as shown in the following attested examples:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%14
    \label{ex:key:14}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \%  \textit{NFU und NFSU2 (was  für n-e   dumm-en  Abkürzungen)}~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

NFU and NFSU2  what for a-\textsc{pl} stupid-\textsc{wk} abbreviations
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘NFU and NFSU2 (what stupid abbreviations!)’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \%  \emph{So n-e  süß-en}\textit{~    klein-en   Pfoten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

so a-\textsc{pl} cute-\textsc{wk} little-\textsc{wk} paws
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

‘Such cute little paws!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I demonstrated in the preceding chapters that a simple, surface-oriented account of the strong/weak alternation cannot explain all the cases. Rather, determiners including \textit{ein} bring about a weak ending on the adjective in a specific structural constellation – simple DPs. In other words, \textit{ein} interacts with the inflection on the adjective morpho-syntactically. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If this is so, then the current account also raises some questions for other proposals. Specifically, the weak inflections on the adjectives above raise issues for a Predicate Inversion analysis of \REF{ex:key:14a} as discussed in Bennis \textit{et al}. (1998), and they argue against the assumption of a null noun in \REF{ex:key:14b}, either after \textit{ein} or after the adjectives (cf. van \citealt{Riemsdijk2005}). Furthermore, the strong inflections on adjectives in the split-off of discontinuous noun phrases are only consistent with split topicalization being analyzed as involving two separately base-generated nominals (e.g., \citealt{Fanselow1988}). In the latter context, I also argued that \textit{ein} (i.e., its feature bundles) is unlikely to be inserted late in split topicalizations involving indefinite pronouns.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Returning briefly to \REF{ex:key:14}, it is interesting to observe that \textit{ein} does not only determine the inflection on the adjectives but that \textit{ein} also seems to have some semantic effect there. For instance, van \citet{Riemsdijk2005} points out for similar cases in Dutch that these types of constructions can express some relatively excessive property of the entities under discussion. These cases were not addressed in detail in Chapter 4. In \sectref{sec:key:2.2.3} below, I suggest again that \textit{ein} itself does not have an influence on the interpretation in these cases. Rather, an operator is held responsible for this effect, and this operator is flagged by \textit{ein}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

More generally, given these shared properties and the interwoven argumentation, I reiterate my proposal that adjectival inflections and \textit{ein} deserve to be discussed in tandem. Importantly, these elements also differ in certain ways. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{1.2.   Differences between Adjectival Inflections and} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the previous section, I reviewed the claim that adjectival inflections and \textit{ein} indicate abstract structure. I left open there that the former also provide clues about the different types of embeddings, namely clues that indicate simple vs. complex DPs. To illustrate this again, consider \REF{ex:key:15}. The example in \REF{ex:key:15a} involves a simple, canonical DP where the adjective is in Spec,AgrP. In contrast, the example in \REF{ex:key:15b} is a case of morphological dis-agreement. I argued in Chapter 2, \sectref{sec:key:3.5} that it involves a different structure where both the adjective and the noun are in Spec,DisP, and the head noun of the larger structure is a null element. This yields a non-canonical structure (see also the discussion of pronominal DPs involving \textit{Sie} ‘you’ and non-plural nouns like \textit{Schwein} ‘(pig =) idiot’ in Chapter 7, \sectref{sec:key:3.3.2}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%15
    \label{ex:key:15}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ihr} [\textsubscript{AgrP} \textit{blöd-en} [\textsubscript{NP} \textit{Schweine} ]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you        stupid-\textsc{wk}  pigs
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you stupid idiots’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{ihr} [\textsubscript{DisP} [\textsubscript{AgrP} \textit{blöd-e\textsubscript{} }[\textsubscript{NP} \textit{Bande} ]] [\textsubscript{NP} \textit{e\textsubscript{N}} ]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you               stupid-\textsc{st}  gang.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you stupid gang’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Importantly, although the adjectives in both \REF{ex:key:15a} and \REF{ex:key:15b} are in Spec,AgrP, the one in \REF{ex:key:15b} is more deeply embedded – this AgrP is in Spec,DisP. Consequently, Impoverishment cannot occur in the latter case resulting in a strong ending on the adjective. In addition to complex specifiers, it was also shown in Chapter 2 that strong adjectival endings surface in several other non-canonical structures, for instance, in different kinds of adjunction. In each case, Impoverishment does not occur, and I concluded that weak inflections only surface in simple, regular DPs. More generally, the strong/weak alternation on the adjective was employed throughout the book as a means to find plausible structures for related constructions (many containing \textit{ein}) and as briefly mentioned just above, to evaluate other proposals as regards their structural claims.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Both adjectival inflections and \textit{ein} also make contributions in their own right. While both types of elements indicate the presence of certain elements, they differ in that adjectival endings make morphological features visible but that \textit{ein} supports or flags semantic operators. Consider the remaining claims of Hypotheses 2 and 3.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.2.1. Some Evidence for Hypothesis 2b
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I put forth the following hypothesis for adjectival inflections:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%16
    \label{ex:key:16}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Hypothesis 2}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Adjectival inflections:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.  […] make nominal features like case, number, and gender visible.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To briefly illustrate, this claim receives credence from the comparison of a regular noun phrase as in \REF{ex:key:17a} and its discontinuous counterpart as in \REF{ex:key:17b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%17
    \label{ex:key:17}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein lila-(n-es)   Kleid}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a    purple-n-\textsc{st} dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a purple dress’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Kleid          habe ich ein lila-*(n-es)}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    dress.\textsc{neut} have \textsubscript{} I    \textsubscript{} a    purple-n-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for dresses, I have a purple one’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I proposed that the adjectival ending in \REF{ex:key:17b} makes the features of the displaced noun visible; that is, the adjectival inflection is an exponent of case, number, and gender (specifically for \REF{ex:key:17b}: nominative, singular, neuter). In fact, due to concord inside the noun phrase, adjectival inflections make features of the entire DP visible. The same holds for the inflection on \textit{ein}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%18
    \label{ex:key:18}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{ein Kleid}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a    dress.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a dress’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Kleid          habe ich nur  \textsubscript{} }\textit{ein-*(es)}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    dress.\textsc{neut} have \textsubscript{} I    \textsubscript{} only one-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘As for dresses, I have only one.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Comparing \REF{ex:key:17b} and \REF{ex:key:18b}, note again that adjectival inflections and \textit{ein} interact in that \textit{ein} determines the ending on the following adjective \REF{ex:key:17b} but that this very ending may also appear on \textit{ein} itself if the adjective (and noun) is absent \REF{ex:key:18b}. Recall from Chapter 1, \sectref{sec:key:3.2.2} that the additional adjectival ending in \REF{ex:key:18b} is only possible on \textit{ein} under very specific conditions (which makes \textit{ein} different from other determiners). This was made formal in Chapter 2, \sectref{sec:key:2.2} postulating certain vocabulary insertion rules for \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.2.2. Some Evidence for Hypothesis 3b
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The element \textit{ein} also makes certain elements visible. Unlike adjectival inflections, \textit{ein} indicates the presence of semantic operators.\footnote{It may turn out that adjectival inflections are similar to \textit{ein} here as well. Specifically, adjectival inflections might also be able to flag the presence of an operator in certain contexts. In this regard, \citet[183]{Merchant1996} points out that uninflected \textit{all} ‘all’ tends to induce a collective reading (ia) but that its inflected counterpart seems to yield a distributive reading (ib):(i)  a.  \textit{all diese guten Freunde}              all these good  friends      ‘all these good friends’b.  \textit{all-e      \textsubscript{} }\textit{diese guten Freunde}              all-\textsc{infl} these  good  friends      ‘all these good friends’Taking this at face value, the presence or absence of the inflection seems to correlate with different readings. Rather than suggesting that adjectival inflections are associated with the semantics after all (e.g., \citealt{Roehrs2015}: 264), we might tentatively suggest that they make a distributivity operator in (ib) visible.}  I formulated the following general claim:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%19
    \label{ex:key:19}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Hypothesis 3}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{Ein}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   b.  […] supports overt semantic operators (e.g., NEG \textit{k}{}-) and flags the presence of       covert semantic operators (e.g., REL).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I argued above that the possessive articles, the negative article, and the singularity numeral are composite forms. These complex elements involve possessor, negator, or singularity components that, if supported by \textit{ein}, receive the following spell-out forms in a noun phrase: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%20
    \label{ex:key:20}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{m-eine Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsc{poss}{}-a woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘my woman’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{k-eine Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsc{neg}{}-a woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘no woman’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{EINE    Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{Ø}\textsubscript{[-}\textsc{\textsubscript{pl}}\textsubscript{]}+a woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one woman’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note again that supporting involves overt operators. Overtness of the operator is a convenient shorthand for stating that the relevant semantic components have a detectible manifestation: possessors and negation have an overt segmentable element (e.g., \textit{m}{}-, \textit{k}{}-), but the singularity numeral involves a non-segmentable element, namely stress. I suggested above that these three components are operators. This not only allows us to relate the three instances to each other but also to other cases.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In particular, basically following de Swart \textit{et al}. (2007), I proposed that \textit{ein} indicates the presence of the realization operator REL, an element that does not have an independent overt manifestation \REF{ex:key:21}. With \textit{ein} appearing in this context, this is what I referred to as flagging. Notice also that the flagging of an operator and the indication of a certain amount of structure are related here. Assuming with de Swart \textit{et al}. (2007) that REL is in NumP, \textit{ein} indicates the presence of REL and a certain abstract structure in singular contexts (i.e., ArtP):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%21
    \label{ex:key:21}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          [\textsubscript{ArtP} \textit{eine} [\textsubscript{NumP} REL\textsubscript{<e<e,t>>} [\textsubscript{NP} \textit{Frau}\textsubscript{<e>} ]]]\textsubscript{<e,t>}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         \textsubscript{} a                                      \textsubscript{} woman
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘a woman’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, it is worth pointing out that the cases in \REF{ex:key:20} above also involve REL – the head nouns (type <e>) must be mapped to sets of individuals (type <e,t>). In other words, \textit{ein} does not only support an overt operator there, but it also flags a covert one at the same time. In Sections 2.2.2 and 2.2.3 below, I list more cases involving supporting and flagging and discuss some of the related issues in more detail. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

1.3.  Summary of the Main Claims
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this last subsection, I provide a convenient summary of the main insights in bullet-point format:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Adjectival inflections are not a reflex of:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumxileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(in-)definiteness 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(non-)restrictiveness of interpretation of modifiers 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

referentiality
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumxileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The article \textit{ein} is not a reflex of:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumxileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

indefiniteness 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

emotiveness
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

singular number/countability 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumxileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Both adjectival inflections and \textit{ein} are semantically vacuous and can:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumxiileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

appear in contradictory semantic contexts
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

be left out in certain, well-defined contexts without loss of meaning
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

have multiple occurrences without leading to semantic redundancy
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumxiileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Furthermore, both adjectival inflections and \textit{ein} can each be in different positions:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumixleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

strong endings are in AgrP, CardP, DP, and LPP; weak endings are in AgrP, CardP, and DP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{ein} is in Art, Card, and D
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumixleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Abstracting away from adjectival inflections indicating different degrees of embedding, these two types of elements indicate abstract structure:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumviileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

adjectival inflections:  DP vs. LPP 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{ein}:       NP vs. ArtP 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumviileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These two types of items make other elements visible:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumviileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

adjectival inflections:  morphological features
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{ein}:       semantic operators
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumviileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Miscellaneous for adjectival inflections:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumviileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

concord is a necessary (but not sufficient) condition for weak endings 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Impoverishment occurs locally and in a bottom-up fashion
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumviileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Miscellaneous for \textit{ein}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumviileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

it is the least specified determiner\footnote{Besides having no semantics, there are some other advantages of assuming that \textit{ein} has no feature for definiteness. On the one hand, \textit{ein} does not have to move to the DP-level allowing us to claim that predicates can be of a smaller structural size; on the other hand, if \textit{ein} can stay low in the structure, we can propose that \textit{ein} always surfaces on the right side of the operator it supports (e.g., with \textit{EIN} ‘one’, see also \sectref{sec:key:2.2.5}). This, in turn, allows us to use the same mechanism (Local Dislocation) to instantiate this support.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

it is not inserted late\footnote{Recall that with Vocabulary Items inserted late in DM, this statement actually has to do with the feature bundles that make up \textit{ein(-er)} where the stem involves [+D] and its adjectival inflection contains features for case, number, and gender. In other words, both the stem and the inflection are treated in the same way – no features are inserted late, only the related Vocabulary Items.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

it is triggered by REL (but not by CAP or \textit{als} ‘as’)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumviileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Overall, I conclude that although semantically vacuous, these two types of elements provide clues about abstract structure and the presence of covert elements (features and operators) in the German noun phrase. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.   Some Extensions and Further Consequences 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I consider some possible extensions and more consequences. Given the current state of the investigation, I only briefly discuss the relevant issues and tentatively hint at some solutions. A full-blown account of all the relevant aspects must await another occasion. I start with adjectival inflections and then move on to \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.1.  Extensions and Consequences of Adjectival Inflections: German Dialects
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In Chapter 3, \sectref{sec:key:7}, I provided my account of adjectival inflections in one dialectal variety – Mannheim German. I have not investigated and analyzed other regional forms of German in this regard. In what follows, I make some brief remarks about adjectival inflections in one well-studied dialect, Alemannic German, showing how this type of dialect could be accommodated in the current discussion.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet{Rehn2019} provides an interesting overview and in-depth analysis of prenominal adjectives in Alemannic (for different subdialects of Alemannic, see \citealt{Baechler2017}).\footnote{\citet{Baechler2017} provides convenient paradigms of adjectival inflections (her pages 305-13) and definite articles and demonstratives (her pages 339-44) in a number of different Alemannic subdialects (note though that unlike the current analysis, she does not consider definite articles and demonstratives as bipartite forms).} She observes that they can appear without an inflection in both indefinite and definite contexts. \citet[114]{Rehn2019} points out that this cannot be a continuation of ENHG as uninflected adjectives in Alemannic occur in more contexts than in ENHG. Rather than involving a null ending, she argues that these adjectives are truly uninflected. \citet[103-04]{Rehn2019} argues that uninflected adjectives and nouns could be analyzed as compound-like elements in some instances but crucially not in all cases and dialects. As such, the distribution of the inflections on adjectives (and other elements in the noun phrase) is proposed to follow from certain features that Alemannic (and German more generally) must mark overtly. In what follows, I briefly summarize some of the main aspects of Rehn’s work (but I am not able here to do full justice to all the intricacies of her multi-faceted proposal).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\citet{Rehn2019} observes that inflections on adjectives are basically optional. This is shown with examples in the nominative/accusative in (22a-b). There is no difference in the interpretation that corelates with the presence or absence of the inflections, and there are no morpho-syntactic restrictions. There is one exception. In the absence of a determiner, the inflection on the adjective is obligatory illustrating this with an example in the nominative in \REF{ex:key:22c} (examples are from \citealt{Rehn2019}: 133, 121):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%22
    \label{ex:key:22}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{des lang(-e)  Soil}    (Alemannic German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the long-\textsc{wk} rope.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the long rope’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{a guat(-s) Buach}    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a good-\textsc{st} book.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a good book’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{guad*(-er) Wei}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    good-\textsc{st}     wine.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘good wine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In order to explain the distribution of the adjectival inflections in \REF{ex:key:22}, \citet[122-26]{Rehn2019} proposes that German DPs must mark features for number and oblique case overtly. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Considering the distribution of the indefinite and definite articles, Rehn observes that certain features are consistently marked overtly (as different). To exemplify with Standard German, number is marked differently with the indefinite article in the singular (\textit{ein}) and plural (\textit{Ø\textsubscript{D}}). Similarly, the definite article involves \textit{der}, \textit{das}, \textit{die} in the singular and \textit{die} in the plural (NB: feminine singular and plural forms are not entirely straightforward as the nominative/accusative forms are all \textit{die}, and the dative forms are both \textit{der}). Turning to case, Rehn points out that there is syncretism in the nominative and accusative in both the singular and plural. In fact, only the oblique cases are consistently different, for instance, nominative/accusative neuter \textit{das} vs. dative neuter \textit{dem} and genitive neuter \textit{des} (NB: feminine singular has the form \textit{der} in both the dative and genitive). As for gender, this category is not consistently different. Masculine and neuter are the same with \textit{ein} in the nominative, and masculine and neuter are the same more generally with \textit{einem/dem} in the dative and \textit{eines/des} in the genitive. Rehn draws the conclusion that only number and oblique case must be marked overtly in German DPs.\footnote{Given the \textit{nota bene} remarks in the main text, the difference between number and oblique case vs. non-oblique case and gender is not entirely clearcut. I put this potential issue aside here.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \citet[125]{Rehn2019} formulates the following principle:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%23
    \label{ex:key:23}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Feature Specification in German DPs:}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Within a DP in German, number and oblique case must be marked overtly either
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumxiiileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

on an article,
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

through inflection realized in Mod\textsuperscript{0} [here Agr, DR] when no article is realized,
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

or on the noun itself when neither of the above is realized.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumxiiileveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

This principle is meant to hold for both Standard German and the regional dialects alike. Note that the obligatory inflections in Standard German are, in Rehn’s (2019: 135-39) view, due to a standardization process in the 18\textsuperscript{th} and early 19\textsuperscript{th} centuries. For Rehn, inflections on adjectives are generally optional in German, and the obligatory inflections in Standard German are “a mere PF-phenomenon”.\footnote{\citet[133]{Rehn2019} observes that inflections on adjectives are also optional in other dialectal varieties of German. Note though that this does not hold for all dialects: Berlin German has obligatory inflections.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Returning to Alemannic, the principle in \REF{ex:key:23} accounts for the data in \REF{ex:key:22}. In the non-oblique cases provided in \REF{ex:key:22}, only number must be marked overtly. This marking is supplied by the articles in (22a-b), and as a consequence, the inflections on the adjectives are optional. As for the obligatory inflection on the adjective in \REF{ex:key:22c}, there is no article, and the inflection on the adjective itself is the only way to mark number \citep[124]{Rehn2019}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

While the principle in \REF{ex:key:23} may account for the data in \REF{ex:key:22}, it raises questions about DPs involving bare mass nouns. In the non-oblique cases as in \REF{ex:key:24a}, number must be overtly marked; in the oblique cases as in \REF{ex:key:24b}, both number and the oblique case must be overtly marked. As far as I am aware, there is no overt marking in (24a-b), and yet, the DPs are grammatical: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%24
    \label{ex:key:24}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Wei}        (Alemannic German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    wine.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘wine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.  \textit{mit}   \textit{Wei}      
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    with wine.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘with wine’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In my view, the grammaticality of these cases is telling. To explain their status, we have to assume that features for case, number, and gender are only spelled out by adjectival inflections (but not endings on nouns). An alternative to Rehn’s proposal could be to extend the current analysis to Alemannic German.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the current system, null articles and nouns have no feature bundles for case, number, and gender – only overt determiners and adjectives do (I follow \citealt{Rehn2019}: 155 in that case inflections on nouns are under Cl, in the current system Num, signaling case and number at the same time). I assume the following feature realization rule for German DPs, for both the standard and regional varieties:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%25
    \label{ex:key:25}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Feature Specification of German DPs:}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

If CNG bundles are present, at least one of them must be made overt.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that this is a stronger claim than Hypothesis 2b (i.e., adjectival inflections make nominal features like case, number, and gender visible) in that this conditional is meant to help explain the distribution of adjectival inflections. Consider how Standard and Alemannic German can be accounted for by \REF{ex:key:25}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As discussed above, overt determiners have CNG features as part of their structures. Furthermore, (regular) adjectives project InflP as part of their extended projection. As such, the rule in \REF{ex:key:25} is adhered to in all cases involving overt determiners and/or adjectives in the counterparts of \REF{ex:key:22} in Standard German – CNG features are present and made overt (I comment on the obligatory presence of the inflection below). Bare mass nominals as in \REF{ex:key:24} are predicted to be fine too as they do not involve CNG bundles in the first place. As for Alemannic, recall that there is no difference in the interpretation that corelates with the presence or absence of the inflections on adjectives (for such a case in Dutch, see \citealt{Evans2021}). As such, I agree with \citet[194]{Rehn2019} that there is no difference in the syntactic position of inflected vs. uninflected adjectives. One way to account for the obligatory presence of inflections on adjectives in Standard German and the optional absence of inflections on adjectives in Alemannic German is to assume that like in Standard German, InflP is projected with (regular) adjectives in Alemannic German. Unlike in Standard German, the inflections on the adjectives are optionally deleted in Alemannic German unless the deletion violates the rule in \REF{ex:key:25} – the latter scenario involves the presence of only one CNG feature bundle as in \REF{ex:key:22c}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Finally, I make some remarks on the indefinite article. Note that DPs like \textit{ein Buch} or \textit{a Buach} ‘a book’ are grammatical in both Standard and Alemannic German. Recall that I claimed in Chapter 2, \sectref{sec:key:2.2.2} that the indefinite article spells out CNG features. This includes the non-oblique instances where this element has no overt inflections in three feature combinations. Given the vocabulary insertion rules provided in that section, the uninflected indefinite article \textit{ein} spells out both [+D] and [CNG] at the same time. If this is so, then this means that the non-oblique cases are not special. Rather, we can maintain that all features of the CNG bundles must be spelled out. In other words, non-oblique case features are on a par with oblique case features making the account of the inflectional distribution more general. If the sketch above turns out to be a viable alternative account of the Alemannic data, it supports the current analysis of the indefinite article. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up, while I have not examined adjectival inflections in dialectal varieties of German in much detail here, I believe the current system can be extended to those cases. Above, this was exemplified by a brief discussion of Alemannic German. Admittedly, the details of this extension still need to be worked out more carefully. This goes beyond the scope of this book. Besides certain theoretical questions that arise, there are also empirical points that need to be clarified. For instance, it is important to determine the distribution of inflections on multiple adjectives in cases where determiners are absent. Such data will help narrow down the analytical options. Next, I consider \textit{ein} in more detail. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{2.2.  Extensions and Consequences Involving} ein
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this section, I turn to some extensions and consequences of the discussion of \textit{ein}. At the end, I provide the updated vocabulary insertion rules of the main types of articles. I begin by reviewing certain parts of the previous discussion.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.1. Reviewing Cases of Supporting and Flagging
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As argued in Chapter 5, \textit{ein} can support POSS of the possessive articles, NEG of the negative article, and \textit{Ø}\textsubscript{[-PL]} of the singularity numeral: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%26
    \label{ex:key:26}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{m-eine Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsc{poss}{}-a woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘my woman’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{k-eine Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsc{neg}{}-a woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘no woman’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \textit{EINE    Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{Ø}\textsubscript{[-}\textsc{\textsubscript{pl}}\textsubscript{]}+a woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one woman’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In Chapter 6, I followed work by de Swart \textit{et al}. (2007) such that kind nouns like \textit{Frau} ‘woman’ combine with the realization operator REL returning predicate nominals. Note though that combinations of REL(\textit{Frau}) can occur not only as part of predicate nominals \REF{ex:key:27a} but also as part of argumental expressions \REF{ex:key:27b}. In either case, REL is flagged by \textit{ein}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%27
    \label{ex:key:27}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Die Person da     hinten ist eine Frau}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the  person there back    is  a      woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘The person back there is a woman.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Eine Frau            kam   durch    die Tür}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    a      woman.\textsc{fem} came through the door
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘A woman came through the door.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Indeed, as briefly pointed out in \sectref{sec:key:1.2}, one instance of \textit{ein} can make two operators visible at the same time. To see this again, consider the example in \REF{ex:key:28a}. Having added an adjective, it is clear that the kind noun \textit{Frau} ‘woman’ must combine with REL in NumP to yield a predicate. With both elements of type <e,t>, Predicate Modification can combine the predicate nominal and its adjectival modifier. REL triggers the presence of \textit{ein}. Furthermore, the singularity operator must be supported by \textit{ein}. Thus, \REF{ex:key:28a} has the more abstract representation in \REF{ex:key:28b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%28
    \label{ex:key:28}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{EINE blonde Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one \textsubscript{} blonde woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one blonde woman’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ø}\textsubscript{[-}\textsc{\textsubscript{pl}}\textsubscript{]} Adjective REL Noun
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To be clear, \textit{ein} flags the presence of REL and supports the singularity operator at the same time. The same applies to \textit{mein} ‘my’ and \textit{kein} ‘no’ discussed above. This means that cases of supporting also involve flagging. In fact, cases of supporting seem to entail instances of flagging in that all overt operators occur with REL:\footnote{Currently, I am not aware of cases where \textit{ein} only supports an operator but does not flag a covert operator at the same time. In other words, all nouns in strings involving “… \textit{ein} … Noun” seem to involve REL (or another covert operator, see below).}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%29
    \label{ex:key:29}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          Supporting and Flagging:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     a.  (\textit{m-eine})    \textsubscript{}     REL
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     b.  (\textit{k-eine})         \textsubscript{} REL
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     c.  (\textit{Ø}\textsubscript{[-}\textsc{\textsubscript{pl}}\textsubscript{]}\textit{+eine}) REL
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Before moving on, consider some other points about REL.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I proposed in Chapter 7 that other determiners can also flag REL. Besides regular definite determiners like \textit{der} ‘the’ as in de Swart \textit{et al}. (2007), I proposed that pronominal determiners like \textit{du} ‘you’ can do so as well (e.g., \textit{der Wagen} ‘the car’; \textit{du Schwein} ‘you (pig =) idiot’). This means that \textit{ein} surfaces in indefinite contexts but \textit{der} or \textit{du} in definite environments. Given that only one determiner can be merged in ArtP, the distinction followed from the different vocabulary entries where \textit{ein} is not marked for (in-)definiteness but \textit{der} has a definiteness feature and \textit{du} has features for author and participant (entailing definiteness). More generally, it seems clear that different elements can flag the same operator and that the presence of REL is not a sufficient condition for the occurrence of \textit{ein} (we see in \sectref{sec:key:2.2.3} below that the presence of REL is not a necessary condition for the occurrence of \textit{ein} either). With this brief review in place, consider more complex cases.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.2. Nominals with Two Overt Operators
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As just seen again, there are two ways for \textit{ein} to make operators visible: supporting and flagging. Interestingly, the cases discussed above can be more complex – they can involve two overt operators in one nominal, both supported by \textit{ein}. To set the stage, recall that we saw above that \textit{so} ‘such’ can precede \textit{ein} where both unreduced and reduced forms of \textit{ein} are possible (for \REF{ex:key:30b}, cf. \citealt{ElmentalerRosenberg2015}: 389):\footnote{\citet{HoleKlumpp2000} propose that \textit{son}, that is, \textit{so’n} in \REF{ex:key:30}, is an article that simultaneously expresses definite type reference and indefinite token reference. One of their main arguments is that since \textit{son} can appear in plural contexts but the indefinite article \textit{ein} cannot, \textit{son} must be an atomic element. They wind up suggesting that German has three articles: the definite article, the indefinite article, and \textit{son}. Note, however, that German allows the following strings:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{listWWviiiNumxivleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\item \ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a.  \textit{so jemand}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{listWWviiiNumxivleveli}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

so someone.\textsc{masc}‘someone like that’    b.  \textit{so (et)was}      so something.\textsc{neut}      ‘something like that’There are different kinds of \textit{so} \citep[20-21]{Heusinger2011}. Taking into consideration the different semantic contributions of \textit{jemand} ‘someone’ and \textit{(et)was} ‘something’ (and abstracting away from the independent non-reducibility of \textit{jemand} and \textit{(et)was}), the element \textit{so} in (i) appears to be, as far as I can tell, the same as in \REF{ex:key:30} (admittedly, this intuition has to be investigated in more detail). If so, this parallel of (i) and \REF{ex:key:30} makes atomicity of \textit{so’n} less likely. More importantly, \textit{ein} can also surface as an unreduced form after \textit{so} (also in the plural, Chapter 1, \sectref{sec:key:2.2}), and \textit{so’n} can follow another article and/or a numeral in the same noun phrase (as shown in \REF{ex:key:31} below). Thus, the occurrenc of two instances of \textit{ein}, one after \textit{k}{}- and one after \textit{so}, and the low position of \textit{so’n} itself indicate that \textit{son} is not an atomic article but a bipartite form (as indicated in the alternative spelling). Finally, as shown in Chapter 1, \sectref{sec:key:2.2}, plural \textit{ein} can also appear in \textit{was-für} contexts, both in reduced and unreduced form. As far as I know, there are no claims that \textit{(was) für} and \textit{ein} form an atomic article. For cross-linguistic discussion of \textit{so} and the indefinite article, see Wood \& Vikner (2011, 2013).}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%30
    \label{ex:key:30}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{so (ei)ne (nette) Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    so  a         nice   woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘such a nice woman’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{irgend so (ei)ne Frau} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    any     so   a       woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘some woman or other’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The element \textit{so} can roughly be rendered as ‘such’ or ‘of that type’. If we interpret this element as an overt type operator, then a number of other interesting points can be made.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Putting the possessive articles aside for a moment (but see below), we have seen that \textit{ein} can support NEG and \textit{Ø}\textsubscript{[-PL]}. I assume that this also holds for \textit{so} ‘such’. Considering \REF{ex:key:31a} and \REF{ex:key:31b}, note that there are two operators in each case and that \textit{ein} follows both in each case yielding two instances of the indefinite article. Notice that the example in \REF{ex:key:31b} is parallel to an example from the Appendix, \REF{ex:key:31c}, where a numeral other than \textit{EIN} ‘one’ precedes \textit{so} suggesting that \textit{EIN} is the regular singularity numeral in \REF{ex:key:31b}. Indeed, \textit{so’n} can follow another article and a numeral as in the constructed example in \REF{ex:key:31d} (the example in \REF{ex:key:31b} is adapted from \citealt{HoleKlumpp2000}: 238, also \citealt{ElmentalerRosenberg2015}: 383, 389; for Dutch, see \citealt{Barbiers2008}: 6; note that the numeral \textit{zwei} ‘two’ should be stressed in \REF{ex:key:31d}):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%31
    \label{ex:key:31}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{k-eine so’ne Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textsc{neg}{}-a so.a  \textsubscript{} woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘no such woman’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{EINE    so’ne Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{Ø}\textsubscript{[-}\textsc{\textsubscript{pl}}\textsubscript{]}+a so.a   woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one such woman’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  \%  \textit{drei}~  \emph{so ne geilen}~     \textit{Mädels}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    three so.a   awesome girls
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘three such awesome girls’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  \%  \textit{Es gibt   keine  \textsubscript{} }\textit{zwei so’ne Frauen auf der Welt}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    it   gives \textsc{neg}{}-a two  so.a    women in   the world
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘There are not two such women in the world.’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The current account is able to throw some light on these multiple occurrences of \textit{ein}. Before I provide the derivations, I identify some restrictions. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

First, unlike \REF{ex:key:30}, \REF{ex:key:31} cannot involve an unreduced form of \textit{ein} as a second instance. This is particularly clear with the singularity numeral \REF{ex:key:32a}. Furthermore, the first instance of \textit{ein} cannot be a reduced form either \REF{ex:key:32b} ((32a) is based on \citealt{ElmentalerRosenberg2015}: 383):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%32
    \label{ex:key:32}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{EINE so eine Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one    so a     woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{‘ne so eine Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     a   so a      woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Second, \textit{so} ‘such’ cannot be left out:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%33
    \label{ex:key:33}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{keine’ne Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    no.a       \textsubscript{} woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{EINE’ne Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one.a      woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Third, the second instance of \textit{ein} cannot be left out: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%34
    \label{ex:key:34}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{keine so Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    no     so woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{EINE so Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    one    so woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Fourth, \textit{so’n} is not possible in definite contexts such as possessives (35a-b) or definite determiners (35c-d):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%35
    \label{ex:key:35}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  *  \textit{meine so’ne Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    my     so.a   woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  *  \textit{Peters  so’ne Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    Peter’s so.a   woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  *  \textit{die so’ne Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the so.a   woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  d.  *  \textit{diese so’ne Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    this   so.a    woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Summarizing these sets of data, note that these multiple occurrences of \textit{ein} only surface in indefinite contexts where one instance of \textit{ein} occurs before \textit{so} and one after it. Furthermore, when both instances of \textit{ein} are present, \textit{so} cannot be left out; that is, both instances of \textit{ein} cannot be adjacent. When the first instance of \textit{ein} and \textit{so} are present, the second instance of \textit{ein} must be present as well. Finally, the first instance of \textit{ein} involves an unreduced form, but the second instance of \textit{ein} is a reduced form. To repeat, this is different when the first instance of \textit{ein} is missing as in \REF{ex:key:30}. In the latter case, \textit{ein} can appear in both reduced or unreduced forms.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I argued in Chapter 1, \sectref{sec:key:4} that determiners are base-generated in ArtP and move successive cyclically up the tree. I propose that the multiple occurrences of \textit{ein} above are also related by movement such that two copies of \textit{ein} are pronounced. For concreteness, I assume that the operator \textit{so} is in the specifier position of a Type Phrase (TypeP). I repeat \REF{ex:key:31b} below as \REF{ex:key:36a} and derive it as in \REF{ex:key:36b}. I suggest that \textit{Ø}\textsubscript{[-PL]} and \textit{so} each get supported by \textit{ein}. The relevant elements are linearly adjacent:\footnote{For the counterpart in Dutch, \citet[171]{Barbiers2005} suggests that NumP (CardP in the current system) is base-generated above DP. Such a strong (non-standard) claim does not have to be made in the current account.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%36
    \label{ex:key:36}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{EINE    so’ne Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    \textit{Ø}\textsubscript{[-}\textsc{\textsubscript{pl}}\textsubscript{]}+a so.a   woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘one such woman’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.      CardP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{Ø}\textsubscript{[-}\textsc{\textsubscript{pl}}\textsubscript{]}          Card’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             \textit{eine}\textsubscript{i}           TypeP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             \textit{so}   Type’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

            \textit{’ne}\textsubscript{i}    ArtP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

         \textit{eine}\textsubscript{i}        [\textit{Frau}]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Some remarks are in order here. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I argued in Chapter 2, \sectref{sec:key:2.1.6} that \textit{ein} involves a complex head consisting of the categorial feature [+D] and a separate feature bundle for case, number, and gender. Notice that this complex head undergoing movement immediately explains that the second instance of \textit{ein} is inflected and that it is inflected in the same way as the first instance. More tentatively, I assume that the operator \textit{so} is a deficient element (i.e., a bound stem) in that it must be supported by \textit{ein}. This accounts for the fact that \textit{ein} must follow \textit{so} – just like with the other overt operators. There are two kinds of derivation. Starting with \REF{ex:key:30}, where only one overt operator is present, either an unreduced or a reduced form of \textit{ein} follows \textit{so}. I assume that in the reduced instances, \textit{ein} has encliticized to the operator.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Second, unlike \REF{ex:key:30}, (31a-b) involve two instances of \textit{ein}, and the \textit{ein} following \textit{so} is a reduced form. \citet{Nunes2001} argues that Move is not a primitive operation but rather the output of Copy, Merge, Form Chain, and Copy Reduction (Chapter 1). Assuming that Copy Reduction applies to free, unbound copies, unreduced forms of lower copies are deleted. Extending this discussion, if \textit{ein} is an unreduced copy, it gets deleted by Copy Reduction. This explains the ungrammaticality of (32a-b) as the second instance of \textit{ein} should have been deleted. Furthermore, it explains the ungrammaticality of \REF{ex:key:34} as \textit{ein} has been deleted but \textit{so} remains unsupported here. The examples in \REF{ex:key:33} are presumably out as \textit{ein} cannot encliticize to a copy of itself. Note though that \textit{ein} can encliticize to \textit{so}. This reduced form of \textit{ein} is a bound copy and becomes invisible to Copy Reduction (cf. \citealt{Nunes2001}: 311-12). This derivation allows a second \textit{ein} to surface, and this lower copy can support \textit{so}.\footnote{Discussing similar cases, Wood \& Vikner (2011, 2013) also locate lower instances of \textit{ein} in a lower positon of the extended projection of the noun.}   
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Note that I have not discussed cases where both instances of \textit{ein} are reduced. While perhaps not fully grammatical, they seem to be possible:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%37
    \label{ex:key:37}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          ?  \textit{‘ne so’ne Frau}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   a   so.a   woman.\textsc{fem}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  ‘such a woman.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Similar to the discussion above, these cases can be accounted for by movement and enclitization of the second instance of \textit{ein} to \textit{so} ‘such’. Additionally, we can suggest that the first instance of \textit{ein} is reduced to \textit{‘n}, just like other instances of the (reduced) article.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, consider the data in \REF{ex:key:35} again. I assume that \textit{mein} ‘my’ is not possible in the construction in \REF{ex:key:35a} as this type of \textit{so} is incompatible with definiteness (more on this in \sectref{sec:key:2.2.5}). This also rules out the cases of the other definite determiner(-like) elements in (35b-d) in addition to the fact that they do not involve (a higher instance of) \textit{ein} to begin with; that is, the cases in (35b-d) are also ungrammatical as there are two independent determiners in the noun phrase at the same time (e.g., \textit{Ø\textsubscript{D}}/\textit{der}/\textit{dieser} and \textit{ein}).\footnote{Above, we saw that the singularity numeral cannot be followed by \textit{so} ‘such’ and unreduced \textit{ein} \REF{ex:key:32a}. The same holds for possessive articles (ia). The question arises as to why the negative article followed by \textit{so} and unreduced \textit{ein} as in (ib) sounds better than its two counterparts:(i)     a.   *  \textit{meine so eine Frau}      my     \textsubscript{} so a     woman.\textsc{fem}    ‘my such woman’  b.  ?  \textit{keine so eine Frau}      no     \textsubscript{} so a     woman.\textsc{fem}      ‘no such woman’Note that an informal \textit{google} search retrieved hits such as (ib). The fairly good status of this example is surprising as regards the previous discussion. While I do not have a definite answer here, there appears to be a tension between the need to support \textit{so} and the requirement to delete lower unreduced copies of \textit{ein} (as discussed in the main text). This presumably accounts for the markedness of this type of example. While this tension also holds for the singularity numeral and the possessive articles, there are additional factors that make the latter two cases worse: the case with the singularity numeral involves two (almost) homophonous instances of \textit{ein} and the possessive articles induce definite contexts.} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up thus far, \textit{ein} appears to be able to support multiple instances of overt operators, and it can flag a single instance of a covert one:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%38
    \label{ex:key:38}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          Supporting and Flagging:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     a.  (\textit{m-eine})        \textsubscript{} REL
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     b.  (\textit{k-eine})          (\textit{so-ne})  REL
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     c.  (\textit{Ø}\textsubscript{[-PL]}\textit{+eine})  (\textit{so-ne})  REL
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Considering \REF{ex:key:38}, we observe that \textit{ein} can support (at least) four different kinds of overt operators. The question arises if \textit{ein} can also flag different kinds of covert operators. I tentatively suggest in the next section that besides REL, \textit{ein} can also indicate the presence of EXIST, QUAL, QUANT, SORT, and CONT. However, so far, I have not been able to identify a clear case indicating that \textit{ein} can flag multiple instances of covert operators at the same time.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.3. Flagging of Other Covert Operators
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There are some other well-known cases where \textit{ein} seems to make a semantic contribution, at least at first glance. I discuss split-scope phenomena, I return to exclamatives, and I briefly discuss \textit{ein} in mass nominals. For each of these cases, I suggest that it is not \textit{ein} that brings about the different interpretations. Rather, different covert operators are taken to be responsible, and \textit{ein} flags the presence of these null elements. One result of this discussion is that I identify more cases involving flagging. Furthermore, I draw the tentative conclusion that there do not seem to be cases where \textit{ein} flags multiple covert operators at the same time.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I have argued at length that \textit{ein} is not a reflex of indefiniteness. It could be suggested that the absence of definite elements such as \textit{der} ‘the’ or \textit{dieser} ‘this’ leads to indefiniteness, perhaps as a default option. However, this is, most likely, not correct. There is evidence that indefiniteness is not a default option but rather that it involves an actual element. This evidence comes from split-scope phenomena (e.g., \citealt{Bech1955}/57, \citealt{Jacobs1980}, data taken from \citealt{Zeijlstra2011}: 113):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%39
    \label{ex:key:39}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Du  musst keine Krawatte tragen}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  you must  no     tie.\textsc{fem}     wear
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a.   ‘It is not required that you wear a tie.’    (¬ > must > \textstyleunicodei{\textrm{${\exists}$})}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

b.   ‘There is no tie that you are required to wear.’  (¬ > \textstyleunicodei{\textrm{${\exists}$}} > must\textstyleunicodei{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

c.   ‘It is required that you don’t wear a tie.’    (must > ¬ > \textstyleunicodei{\textrm{${\exists}$})}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstyleunicodei{The most} salient reading is paraphrased in \REF{ex:key:39a}, where the negation has the widest scope, and the existential operator has the lowest scope. In other words, the negative article \textit{kein} ‘no’ is split into two parts, and both elements have semantic import. Note first that split scope provides more evidence for the composite analysis of the negative article (for a different view, see \citealt{JägerDoris2012}). Now, given that the different scope options are derived from the various positions of the existential operator in (39a-c), I conclude that this operator involves an actual element. In keeping with the claims made thus far, I suggest that this existential operator is a null element and that this element is responsible for indefiniteness. For convenience, I relabel \textstyleunicodei{\textrm{${\exists}$}} as EXIST(ence). I assume that the presence of this covert operator is flagged by \textit{ein}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Second, as briefly discussed in Chapter 4, \sectref{sec:key:2} special contexts such as exclamatives license the presence of \textit{ein} with plural nouns as in the (constructed) example in \REF{ex:key:40a}. To repeat, van \citet[167]{Riemsdijk2005} points out that these – often called – emotive or affective constructions can express some relatively excessive property of the entities under discussion. Mass nouns can also occur with \textit{ein} \REF{ex:key:40b}. Note that both examples have a clear quality reading expressed in the translation as ‘great’. While the latter reading may be dominant, a quantity interpretation also seems possible rendered in the translation as ‘a lot of’:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%40
    \label{ex:key:40}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a. \%  \textit{Was  für (ei)ne Autos!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what for  a       cars
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Such great cars!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Such a lot of cars!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.   \textit{Was  für (ei)n Bier!}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    what for  a      beer.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Such great beer!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Such a lot of beer!’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The presence of \textit{ein} is unexpected with plural nouns in indefinite contexts and with mass nouns more generally.\footnote{As briefly discussed in Chapter 1, \sectref{sec:key:2.2.1}, southern dialects of German allow indefinite articles to occur in plural contexts as part of argumental expressions (without \textit{so} ‘such’). The same holds true of mass nouns (\citealt{Haider1988}: 52 fn. 14):  (i)  \textit{wos   moch i den mid  an wossa}    (Bavarian German)    what make I \textsc{prt} with a   water    ‘What do I do with water?’Unlike Standard German, the indefinite article here is not restricted to singular (non-emotive) contexts.} I argued in Chapter 4, \sectref{sec:key:2.2} that cases like \REF{ex:key:40a} cannot involve a null noun between the article and the overt noun; that is, \textit{ein} cannot be due to the presence of a null noun. I assume the same for \REF{ex:key:40b}. While \textit{was für} ‘what kind’ is an (interrogative) operator in questions, the examples in \REF{ex:key:40} involve exclamatives. Thus, it seems less likely that \textit{was} \textit{für} is the operator here. I suggest that there are two covert semantic operators at work, QUAL(ity) and QUANT(ity) (cf. Chapter 4, \sectref{sec:key:2}, where Bennis \textit{et al}. 1998 and van \citealt{Riemsdijk2005} also claimed null operators, their [+EXCL] and !!!, respectively, to be present in the related constructions in Dutch).\footnote{Note that besides \textit{was für} ‘what kind’, the cases in \REF{ex:key:40} are also possible with \textit{so} ‘such’ instead. It might be claimed that \textit{so} is the relevant operator here (similar to \sectref{sec:key:2.2.2} above).}  Depending on which operator is present, a different interpretation obtains. The presence of these operators is flagged by \textit{ein}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Third, mass nouns are interesting in other ways. When they occur with an indefinite article, they can also have an effect different from \REF{ex:key:40b} above. Specifically, \REF{ex:key:41a} has a sortal reading, and \REF{ex:key:41b} has a container reading as is clear from the translation involving \textit{loaf}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%41
    \label{ex:key:41}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Pumpernickel ist (ei)n Brot}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    pumpernickel is   \textsubscript{} a     bread.\textsc{neut} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Pumpernickel is a bread (= certain kind of that substance).’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  \textit{Ich möchte      (ei)n Brot}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    I     would.like \textsubscript{} a     bread.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘I would like a loaf of bread (= a certain quantity of that substance).’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Notice that unlike \REF{ex:key:40b}, these instances involve non-exclamative contexts. I suggest that semantic operators also account for these data. To be concrete, I simply label these elements as SORT(er) and CONT(ainer) (cf. ‘univeral sorter’ and ‘universal packer’, respectively; see H. \citealt{WieseMaling2005} for discussion). Again, I take it that \textit{ein} flags the presence of the operators that bring about the different readings.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  To sum up, I made the claim more general that \textit{ein} indicates the presence of covert operators. In particular, I extended this claim to split-scope phenomena (EXIST), to plural indefinite nouns in exclamatives (QUAL, QUANT), and to mass nouns (QUAL, QUANT, SORT, CONT). This means that besides REL, other covert operators appear to be flagged by \textit{ein}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%42
    \label{ex:key:42}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          Flagging:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     a.  \textit{(ei)n}    REL
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     b.  \textit{(ei)n}    EXIST
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     c.  \textit{(ei)n}    QUAL
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     d.  \textit{(ei)n}    QUANT
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   e.  \textit{(ei)n}    SORT
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

   f.  \textit{(ei)n}     CONT
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Thus far, I have not identified any cases where \textit{ein} flags two covert operators at the same time.\footnote{A case with two covert operators might involve the complex determiner \textit{ein jeder} ‘(an every =) each’, where \citet{Roehrs2012} suggests that \textit{ein} intensifies the distributive reading of \textit{jeder}. Accepting the proposal that \textit{ein} itself is not responsible for the semantics, a null distributivity operator (DIST) could be postulated. The example in (ia) could then be schematically analyzed as in (ib):(i)  a.  \textit{ein jeder  nette Student}      an  every nice  student.\textsc{masc}      ‘each nice student’    b.  \textit{ein} DIST \textit{jeder} Adjective REL NounThis presents a case of multiple covert operators in one nominal. However, it is unlikely that \textit{ein} flags both DIST and REL at the same time, as the definite determiner \textit{jeder} is present; that is, \textit{jeder} presumably flags REL on par with \textit{der} ‘the’ or \textit{du} ‘you’ as discussed above. Notice also that DIST is different from the other covert operators above in that it intensifies the reading of a specific determiner (rather than determines the reading of the nominal as a whole). As such, I do not add it to the list in \REF{ex:key:42}.} More generally, if the interpretation of the above empirical observations is tenable, then I can state again that the traditional term indefinite article is a misnomer. The fact though that one covert operator (CAP) is not indicated by \textit{ein}, but the operators of the rather long list in \REF{ex:key:42} are is not very satisfying. Some more remarks are in order.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

That CAP is not flagged by \textit{ein} but that the types of operator in \REF{ex:key:42} are should, ideally, be related to an independent factor. If possible, we might like to claim that the respective operators have something in common. Pending further investigation, I think it is not impossible to make such a claim. In particular, we could suggest that the position the operator appears in is related to it being flagged by \textit{ein}. De Swart \textit{et al}. (2007) proposed that CAP is in NP and that REL is in NumP, the latter being indicated in singular contexts by \textit{ein}. Note that NumP is closer to the base-position of \textit{ein} (ArtP) than NP. Assuming that locality plays a role here, this could explain why REL (but not CAP) is flagged by \textit{ein}. If so, we could suggest further that all covert operators in \REF{ex:key:42} are in NumP. This, in turn, might open another avenue of research.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  It is linguistically neither very interesting nor very elegant to postulate a different operator for every different reading. This is basically what I did at the beginning of this subsection. In my view, it is desirable to pursue the idea that these operators can be related or even collapsed into fewer elements. While I have to leave the ultimate solution of this issue to semanticists, there are a couple of morpho-syntactic observations worth making here.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  REL and EXIST typically occur with count nouns. While REL was originally motivated for predicative contexts, EXIST (or \textstyleunicodei{\textrm{${\exists}$}}) is postulated for argumental expressions. At the very least, these cases seem structurally related in that predicative NumP (REL) is part of argumental DP (EXIST). At best, these two operators could be collapsed into one if the syntax of predicate vs. argumental nominals could explain the remaining properties (e.g., figurative extension, scope, etc.).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  By contrast, QUAL, QUANT, SORT, and CONT typically occur with mass nouns. If these four covert operators are indeed all in NumP as tentatively suggested above, then they could be collapsed into one more general operator that gets specified in a given context. In fact, it might turn out that the pragmatics specifies this general operator as regards the different readings. While I have to leave a more robust discussion of these issues for future research, it does not seem impossible to simplify the account above. Pending a solution of these issues, I continue with the operators as presented in \REF{ex:key:42}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The result of the discussion above is that we can observe that all nominals containing \textit{ein} involve at least one operator. I believe this is an interesting claim. Crucially though, \textit{ein} occurs with a number of different operators. This means that stating the vocabulary insertion rule for \textit{ein} and accounting for the presence or absence of \textit{ein} in the various contexts is not trivial. A number of complications arise. As such, the discussion in the next three subsections is, admittedly, somewhat involved. However, I think that it serves well to highlight the complexities that arise. Indeed, as should be clear, any account espousing the composite analysis of complex \textit{ein}{}-words has to deal with these complications. First, I address \textit{ein} in the context of covert operators, then I add overt operators to the discussion, and finally, I provide my account involving feature deletion, which may widen the distribution of \textit{ein}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.4. Contexts of Vocabulary Insertion for \textit{ein}: Covert Operators
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the previous chapters, I argued that the indefinite article does not have semantic import. Rather, it flags and supports operators. I proposed that \textit{ein} involves the categorial feature [+D] and a separate feature bundle for case, number, and gender. I took [+D] to be the semantically vacuous element that is spelled out as the stem \textit{ein}; the features for case, number, and gender are spelled out as its inflection. If features in addition to [+D], case, number, and gender are present, different elements are spelled out. To give some examples: if [+DEF] is present, the definite article \textit{der} ‘the’ is spelled out; if [+DEF; +DEIX] are present, the demonstrative \textit{dieser} ‘this’ is realized; if features for [AUTH] and [PART] are present, pronominal determiners surface. Note though that the distribution of determiners is more complicated when we consider the indefinite articles \textit{Ø\textsubscript{D}} and \textit{ein} in the context of operators. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

For instance, if \textit{ein} flags REL, it only occurs in singular contexts. However, if \textit{ein} supports an overt operator (in addition to flagging REL), it can occur in mass, singular, and plural contexts. Leaving the discussion of overt operators to the next subsection, note that all covert operators flagged by \textit{ein} involve indefinite contexts (see previous subsection). Before I turn to a more detailed discussion of the vocabulary insertion rules of the articles in indefinite contexts, I review the cases above and add a few minor points to the investigation. This lays the foundation for the account of Vocabulary Insertion of determiners. Given the complex sets of data, the discussion is summarized at various points in Tables 1 through 3 below.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Nouns become instantiated as mass, singular count, or plural count nominals during the derivation. I proposed in Chapter 7 that the interplay between the head noun and Num as regards morphological and consequently semantic number leads to the three manifestations. Importantly, independent of number (and thus manifestation of the noun as mass, singular, or plural), all types of nominals involve an operator. Focusing on kind nouns, recall that they are of type <e>. In order to become predicates (type <e,t>), they combine with REL. This step is clearly needed so that these nominals can combine with adjectival modifiers (type <e,t>) by Predicate Modification. De Swart \textit{et al}. (2007) did not discuss mass nouns in this regard. However, since they can also be modified, I assume that they also involve REL. Furthermore, mass nouns can also occur in the context of existential operators. As such, they involve EXIST – just like count nouns. As is well known, in indefinite, non-exclamative contexts, mass and plural count nominals involve null articles, and singular count nominals contain \textit{ein}. This is summarized in \tabref{tab:key:1} (recall that mass nouns involve Num without a specification for morphological number): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Covert Operators in Indefinite, Non-exlamative Contexts}
\label{tab:key:1}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 Type of nominal & Non-exclamative\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

mass (Num) & { \textit{Ø\textsubscript{D}} (REL/EXIST)}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ singular (Num\textsubscript{[-PL]})} & { \textit{ein} (REL/EXIST)}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ plural (Num\textsubscript{[+PL]})} & { \textit{Ø\textsubscript{D}} (REL/EXIST)}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

More needs to be said about mass nouns.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Mass nouns on their sortal (SORT) and container (CONT) reading as in (41a-b) occur in non-exclamative contexts and most likely involve nominals where Num is specified as [-PL morph]. This is so as these nominals can be pluralized (e.g., \textit{zwei verschiedene Zucker} ‘two different sugars’; \textit{zwei Brote} ‘two loaves of bread’). As such, I put the singular instances of these readings with the singular nominals in \tabref{tab:key:2} below. I assume that the distinction between the ordinary count reading of \textit{ein Schwein} ‘a pig’ and the special sortal reading of \textit{ein Zucker} ‘a (type of) sugar’ follows from the different lexical items involved and their preferred respective interpretation (\citealt{Borer2005}: 102, see also H. \citealt{WieseMaling2005}: 8). In \tabref{tab:key:2}, the singular sortal and container readings are put in a different row from the ordinary readings involving REL/EXIST. To be clear, compared to \tabref{tab:key:1}, \tabref{tab:key:2} involves an additional row in the non-exclamative column.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As to exclamative contexts (see column three of \tabref{tab:key:2}), it is not obvious if the quantity (QUANT) or quality (QUAL) reading of mass nouns involving \textit{ein} as in \REF{ex:key:40b} is singular. The pluralization (i.e., \textit{Was für eine Biere!} ‘Such beers!’) seems a bit marked and has a different, sortal reading. Furthermore, it does not seem plausible to relate the unspecific quantity reading, rendered in \REF{ex:key:40b} above as ‘a lot of’, to semantic singularity and countability. Rather, it seems more straightforward to interpret these cases as actual mass nominals; that is, as nominals that involve Num with no morphological specification at all. If so, these nominals are number neutral and can be interpreted in different ways. These nominals are put in the row of mass nominals in the exclamative column of \tabref{tab:key:2}. Plural count nominals involving \textit{ein} as in \REF{ex:key:40a} are put with the plural instances. Perhaps unsurprisingly, singular cases also occur in exclamative contexts. Given the singular context, they only have a quality reading (e.g., \textit{Was für ein Auto!} ‘Such a great car!’). The remarks above can be summarized as follows:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Covert Operators in Indefinite Contexts}
\label{tab:key:2}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 Type of nominal & Non-exclamative & Exclamative\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

mass (Num) & { \textit{Ø\textsubscript{D}} (REL/EXIST)} & { \textit{ein} (QUANT, QUAL)}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ singular (Num\textsubscript{[-PL]})} & { \textit{ein} (REL/EXIST)} & { \textit{ein} (QUAL)}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

& { \textit{ein} (SORT, CONT)} & \\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

{ plural (Num\textsubscript{[+PL]})} & { \textit{Ø\textsubscript{D}} (REL/EXIST)} & { \textit{ein} (QUANT, QUAL)}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Considering \tabref{tab:key:2}, note again that the articles occur in the context of certain operators. To repeat, articles in non-exclamative contexts are inserted in their usual contexts: the null articles are inserted in mass and plural contexts, and \textit{ein} in singular environments. Exclamative contexts are different. Here, \textit{ein} occurs in all number contexts. Given this complexity, the vocabulary insertion rules for the indefinite articles \textit{Ø\textsubscript{D}} and \textit{ein} are developed in two steps, the first step is discussed in the remaining part of this subsection and the second step in the next subsection.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

All determiners including articles involve the categorial feature [+D]. \textit{Ein} is the least specified determiner – it only has the categorial feature (abstracting away from the inflection). As mentioned above, other determiners including the null articles have more features. In order to restrict the null articles to non-exlamative contexts, I assume for now that these contexts involve an exclamative feature [+EXCL(amative)] (but see the next subsection where this feature is made unnecessary). Postulating that the null articles have the negative counterpart of [+EXCL] as part of their vocabulary insertion rules, this feature will block the occurrence of the null articles in exclamative contexts. Before I provide the preliminary set of vocabulary insertion rules, I need to make some other remarks. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I argued above that countability is a side-effect of Num being specified for singular or plural. While the feature [±COUNT] has not done any work in the current system so far, I make use of it here as it allows me to state the vocabulary insertion rules for determiners more straightforwardly.\footnote{Note that with features having only two values (e.g., [±PL]), a three way distinction such as mass/singular/plural cannot be expressed by just one feature. As will become clear, underspecification does not work in these cases – the feature [-COUNT] has to be present.} In order to restrict the occurrence of the articles to their usual contexts (i.e., the non-exclamative cases in \tabref{tab:key:1}), I assume a special type of feature for all indefinite articles. I label this specification as restriction feature. This restriction feature can be of two types: [±COUNT] and [±PL]. This special feature restricts the occurrence of the articles to contexts as regards morphological number, so that the mass article only occurs in mass contexts, the singular article only in singular contexts, and the plural article only in plural contexts. I assume that this specification is on the categorial feature [+D], and I put it after the colon following the categorial feature (i.e. [+D:\_\_\_]). Note that this feature is not a regular concord feature (the latter being spelled out by the inflection) – it is a compatibility feature (see also the discussion of uninflected \textit{dies} ‘this’ in Chapter 4, \sectref{sec:key:5}). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Again, restricting the discussion to indefinite contexts for now, I assume that the mass article \textit{Ø\textsubscript{D}} involves [+D: -COUNT] and that the plural article \textit{Ø\textsubscript{D}} has [+D: +PL]. The above-mentioned feature [-EXCL] confines the null articles to non-exclamative contexts. The first versions of the vocabulary insertion rules for mass and plural \textit{Ø\textsubscript{D}} are provided in (43a-b). The singular article \textit{ein} involves the feature [+D: -PL], and its preliminary vocabulary insertion rule is given in \REF{ex:key:43c} (NB: this number restriction is neither on Num nor on N and will not lead to semantic singularity of \textit{ein}):\footnote{Recall from Footnote Error: Reference source not found that \textit{ein} in southern German dialects has a wider distribution, something I will not make more formal here. Also, recall from Chapter 2, \sectref{sec:key:2.2.2} that the vocabulary insertion rules for \textit{ein} consisted of three entries. \REF{ex:key:43c} in the main text presents the elsewhere case. The remaining two rules are repeated here for convenience and updated with the restriction feature [-PL]:(i)  a.  [+D: -PL]    →   \textit{ein-} / [-F, -N, -O, +S]    b.  [+D: -PL][-F, -O]    →   \textit{ein} / \_\_ word ]\textsubscript{φ}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%43
    \label{ex:key:43}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Preliminary Vocabulary Insertion Rules for:}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

a.  [+D: -COUNT; -DEF; -EXCL]   →   \textit{Ø\textsubscript{D}}  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [+D: +PL; -DEF; -EXCL]    →   \textit{Ø\textsubscript{D}} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  [+D:  {}-PL]         →   \textit{ein-} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Returning to the co-occurrence of the operators and articles (\tabref{tab:key:2}), the null articles \textit{Ø\textsubscript{D}} and \textit{ein} are inserted if the features of their vocabulary entries in \REF{ex:key:43} are present in the syntactic representation, provided their restriction feature on [+D] is compatible with the concord feature on Num of the noun phrase as a whole. Note in this regard that I assume that [-COUNT] in \REF{ex:key:43a} is only compatible with the absence of [αPL morph] on Num. This accounts for the distribution of the different articles occurring with REL/EXIST in the three number contexts (mass, singular, plural) in non-exclamative environments. SORT, CONT, QUANT, and QUAL are different. They only occur with \textit{ein} (\tabref{tab:key:2}). SORT and CONT occur with \textit{ein} in singular contexts; that is, they occur with \textit{ein} when Num is specified as [-PL morph] – note that only \textit{ein} is compatible with this number specification on Num.\footnote{As mentioned above, SORT and CONT also occur in plural contexts (e.g., \textit{zwei verschiedene Zucker} ‘two different sugars’; \textit{zwei Brote} ‘two loaves of bread’). Here, Num is specified as [+PL morph] leading to the insertion of different determiners, provided they are compatible with this number specification (e.g., plural \textit{Ø\textsubscript{D}} and \textit{diese} ‘these’).} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As to QUANT and QUAL, they occur in mass, singular, and plural contexts in exclamative environments. This means that null articles cannot be inserted here – they have the feature [-EXCL]. Note though that \textit{ein} cannot be inserted in mass and plural contexts either, as \textit{ein} is restricted to singular environments by [-PL] on [+D]. The question arises how \textit{ein} can appear in mass and plural contexts with QUANT and QUAL (but not with REL/EXIST, SORT, or CONT in non-exclamative contexts). In the next subsection, I propose that certain operators can delete the restriction feature on [+D]. This allows \textit{ein} (and \textit{Ø\textsubscript{D}}) to occur in other number contexts.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.5. Contexts of Vocabulary Insertion for \textit{ein}: Covert and Overt Operators
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As discussed above, in non-exclamative contexts, the null articles are restricted to mass and plural contexts, and \textit{ein} is restricted to singular contexts. Note again that these articles can be used in other environments when certain operators are present, operators that occur in exclamative contexts (e.g., QUANT), but also operators that occur more generally (e.g., POSS). This means that certain combinations of operators and articles have to be allowed while others have to be excluded. \tabref{tab:key:3} below repeats the covert operators occurring in exclamative contexts (i.e., QUANT, QUAL), and it also contains the overt operators and their co-occurring articles. Note that POSS (\textit{m}{}-) and POSS (-\textit{s}) stand for possessive articles and Saxon Genitives, respectively:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Certain Operators and Their Co-occurring Articles}
\label{tab:key:3}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXXXXX}
 & QUANT/QUAL & {\itshape so} & { \textit{Ø}\textsubscript{[-PL]}} & { POSS (\textit{m}{}-)} & { POSS (-\textit{s})} & { NEG (\textit{k}{}-)} & { NEG (\textit{nicht})}\\
\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

mass & {\itshape ein} & {\itshape ein} & {\itshape {}-} & {\itshape ein} & \textit{Ø\textsubscript{D}} & {\itshape ein} & {\itshape der}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

singular & {\itshape ein} & {\itshape ein} & {\itshape ein} & {\itshape ein} & \textit{Ø\textsubscript{D}} & {\itshape ein} & {\itshape der}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

plural & {\itshape ein} & {\itshape ein} & {\itshape {}-} & {\itshape ein} & \textit{Ø\textsubscript{D}} & {\itshape ein} & {\itshape der}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These are the main combinations of operators and articles. This means that the following account has to rule out strings with combinations like *\textit{so-Ø\textsubscript{D}} \textit{Milch/Autos} ‘such milk/cars’, *\textit{m-der Freund} ‘my friend’, *\textit{Peters der Freund} ‘Peter’s friend’, *\textit{k-Ø\textsubscript{D}} \textit{Milch/Autos} ‘no milk/cars’, and others.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  I propose that the deletion of features under Art in the syntactic representation may widen and narrow the distribution of determiners.\footnote{This deletion could be instantiated by Impoverishment.} Specifically, deleting the restriction feature on [+D] will allow all relevant articles to occur in all three number contexts (i.e., mass, singular, plural). To narrow the distribution of articles again, another feature under Art in the syntactic representation can also be deleted. For instance, deleting the categorial feature [+D] under certain conditions will prevent \textit{Ø\textsubscript{D}} and \textit{der} from surfacing but not \textit{ein}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The account of REL/EXIST, SORT and CONT is as above – no features are deleted. However, the analysis of QUANT and QUAL is different. Given the suggestion just made, this alternative will account not only for the occurrence of \textit{ein} with these two operators but also for the distribution of determiners with the overt operators more generally. In addition, this deletion of features will make the use of the feature [EXCL] unnecessary. Before I turn to the details, I lay out some other assumptions.  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  All argument DPs have determiners. In what follows, I focus on the three types of articles: the null articles as in \REF{ex:key:44} below, \textit{ein} provided in (45), and the definite article given in \REF{ex:key:46}. As proposed in Chapter 2, \sectref{sec:key:2.1.6}, these three types of articles have slightly different structures and features under Art (see the (a)-examples below). Each article has its own corresponding vocabulary insertion rule (see the (b)-examples below). This is consistent with the general assumptions of DM, where a difference is made between the syntactic representation and subsequent Vocabulary Insertion. To be clear, Art with its varying feature bundles is in the derivation at the beginning, but the corresponding vocabulary insertion rules of the articles supply the spell-out forms later. Recall that all these articles are merged in ArtP and move to DP (triggering Impoverishment as discussed in Chapter 2). Below are the final structures and vocabulary insertion rules of the three types of articles (for the vocabulary insertion rules of adjectival inflections, see Chapter 2, \sectref{sec:key:2.1.5}; the forward slash sign in \REF{ex:key:44a} is explained below):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%44
    \label{ex:key:44}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Indefinite Null Articles}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               Art\textsubscript{[+D: -COUNT/+PL; -DEF]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      [+D: -COUNT/+PL; -DEF]    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [+D; -DEF]      →  \textit{Ø\textsubscript{D}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%45
    \label{ex:key:45}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  \textit{Indefinite Overt Article}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             Art\textsubscript{[+D: -PL][CNG]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [+D: -PL]       [CNG]    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [+D]        →  \textit{ein-}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%46
    \label{ex:key:46}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.   \textit{Definite Article}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

               Art\textsubscript{[+D; +DEF][CNG]}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    [+D; +DEF]   [CNG]    
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [+D; +DEF]      →  \textit{d-}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that the features bundles under Art undergo feature union. I comment on the feature bundles under Art and on the feature bundles in the vocabulary insertion rules.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that the definiteness feature [DEF] is not a concord feature of the noun phrase as a whole. Rather, it is part of specific determiners. The two null indefinite articles involve [-DEF] \REF{ex:key:44}. For convenience, I provided both null articles in \REF{ex:key:44}, but it should be kept in mind that they differ in their restriction feature on [+D] (the two articles are distinguished by the different restriction feature separated by a forward slash sign in \REF{ex:key:44a}). As to the two other articles, \textit{ein} lacks a definiteness feature \REF{ex:key:45}, and the definite article has [+DEF] \REF{ex:key:46}. Furthermore, unlike the two types of indefinite articles (\textit{Ø\textsubscript{D}}, \textit{ein}{}-), there is only one definite article (\textit{d}{}-). The latter element can be inserted in all number contexts. As such, I assume that there is no restriction feature on [+D] with the definite article \REF{ex:key:46}. Importantly, observe that none of the vocabulary insertion rules involve restriction features. Other than that, the features of the vocabulary insertion rules are identical to the features of their corresponding determiner stems under Art. Finally, recall that CNG stands for the inflectional feature bundle involving case, number, and gender. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Crucially, I assume that the determiner structures are all merged freely; that is, either the structure of a null article \REF{ex:key:44a}, the structure of \textit{ein} (45a), or the structure of the definite article \REF{ex:key:46a} is merged (projecting ArtP in the process). As discussed in detail below, this will allow some articles – specifically the null articles and \textit{ein} – to appear in some other contexts.\footnote{For instance, this will allow \textit{ein}, usually restricted to singular contexts, to appear in plural environments (where Num involves a positive feature for [PL]). As made more precise below, this means that the compatibility of determiners and Num in number is checked after the deletion of the restriction feature on [+D]. This will also result in some cases of overgeneration. The latter are accounted for by the deletion of the feature [+D] on the determiner in certain contexts leading to bad derivations.}  Furthermore, I assume that all features of a determiner structure must be spelled out or supported. This means that the categorial feature [+D] and, if present, the definiteness feature must be spelled out by a determiner stem. Furthermore, the CNG feature bundle, if present, must be supported by an (overt) determiner stem. Since every type of Art above involves features that must be spelled out, every nominal that involves ArtP has a determiner. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFootnote}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In addition, I assume that all operators surface in the left periphery of the noun phrase: either they are base-generated there (e.g., NEG), or they move there (e.g., POSS).\footnote{This high position also licenses the occurrence of adjectival inflections and \textit{ein} as expletive elements (see \sectref{sec:key:3.2} below).} As a consequence, all operators precede the determiner. This can schematically be illustrated as follows where OP stands for operator, and α may be XP in case the operator is adjoined (e.g., NEG) or X’ in case the operator is in a specifier position (e.g., POSS): 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%47
    \label{ex:key:47}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

                      XP
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 \textit{OP}               α  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

              Art          …
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Now, in order to widen the distribution of articles in certain contexts, I assume that all operators (except REL/EXIST, SORT and CONT) delete the restriction feature on [+D] under Art before the determiner structure is checked for compatibility with Num of the larger noun phrase and before Vocabulary Insertion occurs. While this does not affect the distribution of the definite article in \REF{ex:key:46}, this allows the indefinite articles in \REF{ex:key:44} and \REF{ex:key:45} to occur in other number contexts with specific operators: the null article(s) with POSS (Saxon Genitives), and \textit{ein} in most of the other cases. With this in place, I continue the discussion of covert QUANT and QUAL and extend it to the overt operators. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

2.2.6. Contexts of Vocabulary Insertion for \textit{ein}: Feature Deletions
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As just proposed, the deletion of the restriction feature on [+D] under Art widens the distribution of the indefinite articles. To constrain this widening in the relevant way, there are several options to narrow the distribution of articles. As far as I can see, the most straightforward option is to assume that the categorial feature [+D] is deleted in certain featural contexts by specific operators.\footnote{Note that if other features than [+D] were deleted, then this would, in certain contexts, allow the least specified element \textit{ein} in \REF{ex:key:45b} to be inserted in the null article structure in \REF{ex:key:44a} or in the definite article structure in \REF{ex:key:46a}.} For instance, QUANT and QUAL trigger the deletion of [+D] in the context of a definiteness feature, where [DEF] includes [-DEF] and [+DEF]. This is shown in \REF{ex:key:48a}, along with the other operators that work the same way. (48b-c) show the remaining cases. In \REF{ex:key:48b}, [+D] is deleted in the context of the [CNG] feature bundle and the operator POSS (-\textit{s}); in \REF{ex:key:48c}, [+D] is deleted in the context of the feature [-DEF] and the operator NEG (deletion of a feature is marked below by strikethrough):
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%48
    \label{ex:key:48}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          \textit{Deletion of [+D] under Art in the Contexts of […]:}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  a.  [+D]   /   [DEF] triggered by QUANT, QUAL, \textit{so}, \textit{Ø}\textsubscript{[-PL]}, POSS (\textit{m}{}-)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [+D]   /   [CNG] triggered by POSS (-\textit{s})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  [+D]   /   [-DEF] triggered by NEG
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

These are the deletions that narrow the distributions of the articles. As explicated in more detail below, \REF{ex:key:48a} only allows \textit{ein} to be inserted in the relevant contexts, \REF{ex:key:48b} only allows \textit{Ø\textsubscript{D}}, and \REF{ex:key:48c} only allows \textit{ein} or \textit{d}{}- to be inserted in the relevant context. The articles that cannot be inserted into their corresponding structure will lead to bad derivations as the undeleted features of their corresponding structure will not be spelled out or supported. Also, note that each of the “surviving” determiners in \REF{ex:key:48} still has the feature [+D] in tact under Art. This means that Impoverishment will not be affected (by this feature deletion on the other determiners). Consider the effects of the deletion of the restriction feature and other features in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As mentioned above, QUANT and QUAL delete the restriction feature on [+D]. This brings about [D: {}-COUNT], [D: {}- PL], or [D: +PL] in the relevant syntactic representations (Art). Consequently, all three article structures in \REF{ex:key:44a}, \REF{ex:key:45a}, and \REF{ex:key:46a} can, at least in principle, occur in the three morphological contexts as regards number (mass, singular, plural). In other words, this deletion widens the distribution of the indefinite articles (recall that the definite aricle has no restriction feature to begin with). In addition, I suggested in \REF{ex:key:48a} above that these two operators delete the categorial feature [+D] under Art in the context of [DEF], which includes [-DEF] and [+DEF]. This allows the structure in \REF{ex:key:45a} to be spelled out, but not the structures in \REF{ex:key:44a} and \REF{ex:key:46a}. This, in turn, narrows the distribution of articles. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Specifically, if Art in the syntactic representation involves [+D: -COUNT; -DEF] or [+D: +PL; -DEF] as in \REF{ex:key:44a} above (modulo the deletion), then \textit{Ø\textsubscript{D}} cannot be inserted by \REF{ex:key:44b} as [+D] was deleted by the relevant operator. Similar assumptions hold for the definite article where Art involves [+D; +DEF] [CNG] as in \REF{ex:key:46a} above, again modulo the deletion. Note that these two cases lead to bad derivations as the remaining definiteness feature under Art cannot be spelled out by a determiner. This is different for \textit{ein}, where Art involves [+D: {}-PL] [CNG] as in \REF{ex:key:45a}. Here, there is no definiteness feature under Art and in the corresponding vocabulary insertion rule. Consequently, \textit{ein} can be inserted. This type of account carries over to the overt operators and their co-occuring determiners.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Unless independently ruled out (e.g., \textit{Ø}\textsubscript{[-PL]}), all operators occur in all three number contexts (Tables 2 and 3). Like QUANT and QUAL, I suggest that \textit{so}, POSS, and NEG also delete the restriction feature on [+D]. Like above, this allows all three article structures to occur in the three number contexts with these operators as well. Furthermore, similar to QUANT and QUAL just discussed, overt operators also delete [+D] under Art in certain contexts narrowing the distribution of the articles, as stated in \REF{ex:key:48} above.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In more detail, \textit{so} ‘such’ also deletes the categorial feature [+D] under Art in the context of [DEF]. As with QUANT and QUAL, if \REF{ex:key:44a} or \REF{ex:key:46a} is merged in ArtP and moves to DP, their corresponding vocabulary items cannot be inserted. As the definiteness feature under Art cannot be spelled out, this leads to bad derivations. By contrast, if \REF{ex:key:45a} is merged in ArtP and moves to DP, \textit{ein} can be inserted by \REF{ex:key:45b}, and all the features under Art are spelled out. This results in a good derivation. Similar considerations hold for \textit{Ø}\textsubscript{[-PL]}, with the proviso that this element itself is restricted to occur in singular contexts (Chapter 5). There are two types of derivations for POSS and NEG each.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Like \textit{so} above, the possessive component (POSS) of the possessive articles deletes [+D] under Art in the context of [DEF]. This allows \REF{ex:key:45a} to be spelled out, but not \REF{ex:key:44a} or \REF{ex:key:46a}. The latter two lead to bad derivations as the definiteness feature under Art cannot be spelled out. In contrast, I suggest that the possessive marker -\textit{s} (POSS) of Saxon Genitives deletes [+D] under Art in the context of the [CNG] feature bundle. This allows \REF{ex:key:44a} to be spelled out (NB: there is no deletion of [+D] here – the null articles have no CNG features; only the restriction feature on [+D] is deleted). In contrast, the articles in \REF{ex:key:45b} and \REF{ex:key:46b} cannot be inserted – [+D] has been deleted under Art. These derivations are bad as the [CNG] feature bundles of \REF{ex:key:45a} and \REF{ex:key:46a} cannot be supported by the Saxon Genitive (e.g., *\textit{Peters-er}). In addition, the definiteness feature in \REF{ex:key:46a} cannot be spelled out.\footnote{Recall from Chapter 1, Footnote Error: Reference source not found that definiteness spread, if instantiated by Spec-head agreement, leads to incompatibility between definite Saxon Genitives and the null articles (which are specified as [-DEF]). There are two possible solutions. First, similar to the head D (which involves [uDEF]; see Chapter 1, \sectref{sec:key:4.1.1}), we could assume that the null articles do not have an inherent feature for definiteness but rather that they get specified in this regard during the derivation. Note though that if the definiteness feature of the null articles gets specified as positive, this presumably turns \textit{Ø\textsubscript{D}} into triggers for Impoverishment, resulting in weak adjectives also in nominative/accusative/dative contexts, contrary to fact (see again Chapter 2, \sectref{sec:key:2.2.2}). Clearly, such a specification process must be prevented. Alternatively, the feature [DEF] on \textit{Ø\textsubscript{D}} could be of a different kind. Similar to some vocabulary insertion rules discussed above, this feature could involve an (unvaluable) variable (i.e., [αDEF]), which is compatible with both a positive or a negative value of definiteness. In other words, \textit{Ø\textsubscript{D}} could occur in either indefinite or definite contexts (note that similar to \textit{ein}, the (in-)definiteness of the larger noun phrase would depend on the presence of possessors and other elements). Since this type of feature does not get specified, Impovishment would not be triggered. I do not attempt here to decide between these two solutions. Note though that either solution involves a definiteness feature on \textit{Ø\textsubscript{D}}, and the structures of the null articles, their vocabulary insertion rules, and the relevant deletion rules above would only have to be changed from [-DEF] to [uDEF] or [αDEF].}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, I assume that NEG deletes [+D] under Art in the context of [-DEF]. This allows both \REF{ex:key:45a} and \REF{ex:key:46a} to be spelled out. Note that \REF{ex:key:46b} is more specific than \REF{ex:key:45b}. This means that \REF{ex:key:46a} is spelled out by \REF{ex:key:46b} and \REF{ex:key:45a} by \REF{ex:key:45b}. The null articles in \REF{ex:key:44a} lead to bad derivations. In the latter cases, the definiteness feature under Art cannot be spelled out by \REF{ex:key:44b}. The vocabulary insertion rules from Chapter 5, \sectref{sec:key:4.1.2}, repeated below for convenience, account for the overt realization of NEG depending on the context: if \REF{ex:key:45a} is merged and spelled out by \REF{ex:key:45b}, then NEG is realized as \textit{k}{}-; if \REF{ex:key:46a} is merged and spelled out by \REF{ex:key:46b}, then NEG is spelled out as \textit{nicht}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%49
    \label{ex:key:49}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          NEG   →   \textit{k}{}-   / \_\_ (unstressed) \textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    →   \textit{nicht}   (elsewhere)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up this section on \textit{ein}, I reviewed the combinations of operators and articles and accounted for them. It is probably fair to state that this discussion provided a somewhat involved solution, and certain issues remain. Having said that, I hope that it makes clear what complexities arise in the discussion. As already mentioned above, any account adopting a composite analysis of complex \textit{ein}{}-words such as \textit{kein} ‘(NEG+a =) no’ has to come to terms with these issues. Some other conclusions can be drawn.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given the discussion above, it appears that determiners are not only inserted to flag and support operators. Rather, Art in the syntactic representation involves features, and these features must be spelled out by a determiner to yield a good derivation. All determiners including \textit{ein} have [+D] as part of their vocabulary entries. Consequently, if Art is present in the noun phrase, [+D] is spelled out yielding the fact that all nominals bigger than NumP have a determiner (in fact, since REL and other operators flagged by determiners reside in NumP, it may turn out that all nominals bigger than NP involve determiners). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As to \textit{ein} itself, this element is usually inserted in singular contexts given the restriction feature [-PL] on [+D]. However, when certain operators are present, the restriction feature is deleted, and \textit{ein} can occur in other number contexts. The side-effect is that \textit{ein} can not only flag and support operators in singular but also in mass and plural environments. This means that \textit{ein} is probably not due to a simple Last-Resort operation – its occurrence (and that of other determiners) follows from the specific features present under Art, depending on the presence of certain operators. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

To sum up the entire \sectref{sec:key:2}, I addressed some further consequences of the current proposal. Specifically, I briefly discussed how the current analysis could be extended to the distribution of adjectival inflections in Alemannic German. Furthermore, I briefly examined more cases of supporting and flagging. Thus far, I have identified cases that only involve flagging, but there do not seem to be instances that only show supporting. Furthermore, I have found instances where overt and covert operators are supported and flagged by \textit{ein} at the same time. This included cases where two overt operators are supported (and a covert operator is flagged) by \textit{ein}. However, two covert operators do not seem to be flagged by \textit{ein} at the same time. Finally, I provided an account of the insertion of \textit{ein} (and other articles) in the contexts of overt and covert operators. The postulation of a restriction feature and the deletion of it and that of [+D] accounted for the varying distribution of \textit{ein} (and other articles) as regards the presence of different operators.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.   More General Considerations
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In this final section, I return to complex nominals in the context of concord in agreement features, and I make suggestions as to what all expletives, clausal and nominal, may have in common.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.1.  Nominal Structure and Concord
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In the previous chapters, a variety of syntactic phrases played a role. These phrases can be assembled into one hierarchical, abstract structure:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%50
    \label{ex:key:50}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          [ LPP [ D [ Card [ Agr [ Art [ Num [ N ]]]]]]]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I note two things about this structure: I assumed that nouns move to Num and that articles merged in Art (or more precisely, merged as Art) also undergo movement to higher positions in certain cases. With minor differences, this is consistent with the proposals by \citet{Julien2005a}, \citet{Roehrs2009a}, and others. Importantly, the structure in \REF{ex:key:50} received further application in this book. I take this to indicate that this structure shows good promise for future investigations. I turn to some issues related to agreement in non-canonical noun phrases.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  As is well known, elements in the noun phrase are subject to concord in agreement features. For example, determiners, adjectives, and nouns in structures like \REF{ex:key:50} agree in case, number, and gender. In the course of this book, I also discussed a number of more complex cases where a second nominal is combined with the matrix DP by way of an embedded complex specifier or right adjunction. In the literature, these non-canonical nominals have not received much attention. I return to some of them briefly here focusing on agreement. We will see that all these constructions agree in semantic number but not necessarily in morphological number. The relevant structures are illustrated here again but in a simplified way.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  In the context of capacity readings, I discussed DPs that involve \textit{als} ‘as’ and its null counterpart ALS. To recapitulate, the \textit{als-}nominal in \REF{ex:key:51a} involves adjunction to the pronoun where \textit{als} is the head of ModP, and the role noun \textit{Arzt} ‘doctor’ is an NP complement of that head. I proposed that the adjoined nominal does not contain NumP, and as such the number mismatch with a plural pronoun is only apparent (the plural interpretation follows from the presence of a distributivity operator as postulated by de Swart \textit{et al}. 2007: 218). In contrast, while the ALS{}-nominal in \REF{ex:key:51b} also contains ModP, this contruction does exhibit concord. I suggested that the ALS{}-nominal must contain NumP and that it is located in the specifier of AgrP:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%51
    \label{ex:key:51}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{ihr} [\textsubscript{ModP} \textit{als}\textsubscript{Mod} \textit{Arzt} ]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you(\textsc{pl})  as       doctor.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you as doctors’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  (\textit{vielleicht}) \textit{ein} [\textsubscript{ModP} ALS\textsubscript{Mod} \textit{Landwirt} ] Agr \textit{e\textsubscript{N}}\textsubscript{} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

     really        a                          farmer.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘(really) some farmer’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Two other types of nominals should be discussed in this context.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Indefinite pronoun constructions also involve complex structures \REF{ex:key:52a}. As in the first two cases, these structures also involve ModP. Like \REF{ex:key:51a}, this ModP was also argued to involve adjunction. However, unlike \REF{ex:key:51a}, this nominal exhibits concord. In view of the presence of an inflected adjective and a null noun, I argued that REL and thus NumP must be in the structure. Finally, I discussed a case of morphological dis-agreement but semantic agreement \REF{ex:key:52b}. Given the figuratively extended meaning of the noun, I concluded that REL and thus NumP must be present. I proposed that the dis-agreeing nominal is embedded in the specifier of DisP:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%52
    \label{ex:key:52}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

           a.  \textit{jemand} [\textsubscript{ModP} \textit{Ø}\textsubscript{Mod} \textit{ander-er\textsubscript{} }\textit{e\textsubscript{N}} ]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    someone.\textsc{masc}      different-\textsc{st}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘someone different’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.     \textit{Sie} [\textsubscript{NumP} \textit{Bauer} ] Dis \textit{e\textsubscript{N}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    you         peasant.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘you peasant’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

I summarize the properties of these different cases in \tabref{tab:key:4}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
%%please move \begin{table} just above \begin{tabular
\begin{table}
\
\caption{Summary of the Constructions and Their Properties}
\label{tab:key:4}
\end{table}\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{tabularx}{\textwidth}{XXXXX}

\lsptoprule
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Construction & Structure & Agreement b/w nominals & NumP & Head\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{als}{}-capacity & adjunction & apparent morphological and semantic dis-agreement & no NumP (but distributivity OP)\textsuperscript{a} & \textit{als}\textsubscript{Mod}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

ALS-capacity & specifier & morphological and semantic agreement & NumP & ALS\textsubscript{Mod}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Indefinite pronoun construction & adjunction & morphological and semantic agreement & NumP & Ø\textsubscript{Mod}\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

DisP & specifier & morphological dis-agreement but semantic agreement & NumP & {}-\\
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\lspbottomrule
\end{tabularx}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textsuperscript{a} The \textit{als}{}-capacity construction involves NumP in the plural cases (e.g., \textit{ihr als Ärzte} ‘you as doctors’).
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

It is interesting to note that all four constructions agree in semantic number, either mediated through NumP or a distributivity operator. As for concord, agreement in phi-features is not an issue if NumP is absent in the embedding (\textit{als}{}-capacity).\footnote{Recall from Chapter 7 that this is similar to clausal structures involving copular verbs and non-plural role nouns (e.g., \textit{Ihr seid alle Arzt.} ‘You are all (doctor =) doctors.’), where NumP is also absent.} Such agreement holds though if NumP is present (ALS-capacity, indefinite pronoun construction). The only true exception is DisP. The latter involves NumP but does not show concord in agreement features. I briefly explore why this might be so.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  It is clear that the position of the embedded nominal \textit{per se} cannot account for these agreement patterns as both adjuncts and specifiers may exhibit concord (indefinite pronoun construction, ALS-capacity) or not (\textit{als}{}-capacity, DisP). Rather, it seems to be a property of the head of the embedded nominal that determines the agreement possibilities. In particular, ALS\textsubscript{Mod} and Ø\textsubscript{Mod} could be taken to mediate concord. I also argued that \textit{als}\textsubscript{Mod} is an operator that may take an NP complement, as in the case in \REF{ex:key:51a}, but it may also embed a NumP complement, as in the case of plural nouns (see superscript a in \tabref{tab:key:4}). With NumP present in the plural, the \textit{als}{}-capacity construction also shows agreement. All these three constructions involve the head Mod.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Unlike the first three cases, the nominal embedded in Spec,DisP does not contain the head Mod. Given this distinction, I would like to suggest that all three Mod heads mediate concord between the two nominals provided NumP is present.\footnote{I briefly discussed in Chapter 2, \sectref{sec:key:3.2} that Mod is instantiated by the prepositional element \textit{de} ‘of’ in certain Romance languages. As suggested in \citet[21-23]{Roehrs2008}, this element mediates concord between different nominals, for instance, in French indefinite pronoun constructions of the type \textit{quelque chose}\textsubscript{MASC} \textit{de grand}\textsubscript{MASC} ‘something big’.} Due to the absence of such a head in the nominal inside Spec,DisP, the latter does not have to undergo concord with the matrix nominal. Currently, it is not clear to me how to implement this idea. As such, I have to leave the details of this suggestion for future research.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

3.2.  Expletive Elements more Generally
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

As mentioned at the beginning, this book is not meant to contribute to the theory of expletive elements \textit{per se}. Rather, it seeks to identify more elements that share some of the properties of generally accepted expletives. \textit{There} in existential constructions and, to a lesser degree, definite articles in the context of proper names seem to be such established elements. This book argued that adjectival inflections and \textit{ein} are also expletive elements. In this final section, I compare these four elements in a bit more detail with the goal of identifying more common properties.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall that \citet{Chomsky1995} pointed out that expletive elements seem to violate the Principle of Full Interpretation. He proposed that the associate noun phrase in existential constructions moves at LF to license \textit{there}. In other words, a substantive element moves to license a vacuous one. \citet{Longobardi1994} suggested something similar for proper names and proprial articles. To motivate the movement of the proper name to the article, I follow Chomsky by claiming that all expletives are deficient elements (note that Chomsky takes \textit{there} to be a LF-affix). 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Making this more general, this means that adjectival inflections and \textit{ein} also need to be licensed by a contentful element. For adjectives, I proposed in Chapter 2, \sectref{sec:key:2.4} that the adjective stem moves to provide a host for the inflection. As to \textit{ein}, I tentatively suggested in \sectref{sec:key:2.2.5} above that operators surface in the left periphery of the noun phrase: either they are base-generated there, or they move there to precede \textit{ein}. I assume that this local relation licenses \textit{ein}. This means that \textit{ein} makes an operator visible but gets licensed by it at the same time. To repeat from Chapter 1, there is a division of labor: syntactically, the expletive indicates the substantive element; semantically, the substantive element identifies the expletive. These relations are shown below before movement of the licenser:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

             Indicate (syntax)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%53
    \label{ex:key:53}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          EXPL          SUBST
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

           Identify (semantics)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

There is a second point worth making.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Recall also that one of the conclusions of this book is that adjectival inflections and \textit{ein} make different abstract structures visible. Again, I suggest that this is a general property of all expletives. In other words, I would like to claim that both \textit{there} and the proprial article also indicate abstract structure. Consider this point in more detail.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Above, I made a distinction between supporting and flagging. Starting with the latter, this mechanism seems to exhibit certain similarities to what is involved with \textit{there} and its associate and with proprial articles and proper names. Specifically, in all these cases, the expletive element is a free, unbound morpheme (at least in the overt component of the derivation). I observe now that the expletives and the contentful elements involve two different positions where the expletives are higher in the structure than the substantive elements:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%54
    \label{ex:key:54}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

                       α
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  EXPL            β
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%[Warning: Draw object ignored]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 SUBST  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{there}    associate
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  prop. art.  proper name
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  \textit{ein}    operator
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The contentful licensers are merged lower in the structure due to semantico-syntactic factors; that is, they are base-generated low for independent reasons. Specifically, associates are at the bottom of existential constructions given local theta-role assignment \REF{ex:key:55a}, nouns including proper names form the bottom of their extended projections \REF{ex:key:55b}, and semantic operators like REL turn nouns into predicates before these nominals can combine with other elements \REF{ex:key:55c}. Notice that the expletives and their licensers are not linearly adjacent in their positions as other elements can intervene: 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%55
    \label{ex:key:55}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [ There ] is [ a man ] in the garden.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.  [ \textit{der} ] \textit{alte} [ \textit{Peter} ]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      the    old    Peter.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘Peter, who is old’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  c.  [ \textit{ein} ] \textit{netter} [ REL ] \textit{Freund}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

      a      nice                 friend.\textsc{masc}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘a nice friend’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Given these data, I reiterate the claim that these expletives and their licensers are related by movement. Recall that expletives are deficient elements and that the substantive elements are in a lower position for independent reasons. Now, as syntactic movement only proceeds upwards, we have an explanation as to why the expletives must be in the higher position (and not the lower one) – they are licensed by the movement of the substantive elements. Being in a high position, expletives indicate abstract structure. This includes \textit{there} and proprial articles.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  The mechanism of supporting is partially different. It, too, makes operators visible but forms overt composites of the two relevant elements under linear adjacency. This involves the complex \textit{ein}{}-words and inflected adjectives. Like flagging, the substantive element is base-generated low in the structure and undergoes movement to the left periphery. This is clearly the case with the possessive component. After movement, it combines with expletive \textit{ein} \REF{ex:key:56a}. Similary, the adjective stem is base-generated below its inflection and moves to combine with it \REF{ex:key:56b}:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea%56
    \label{ex:key:56}
    \gll \\
         \\
    \glt
    \z % you might need an extra \z if this is the last of several subexamples

          a.  [\textsubscript{DP} \textit{m}\textsubscript{i}{}-\textit{ein}  \textit{Auto} t\textsubscript{i} ]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

          \textsc{poss}{}-a car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘my car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  b.    \textit{das} [\textsubscript{InflP} \textit{groß}\textsubscript{i}{}-\textit{e} t\textsubscript{i} ] \textit{Auto}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    the         big-\textsc{infl}    car.\textsc{neut}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

    ‘the big car’
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

In each case, the substantive part originates low in the structure, and the expletive element is located higher in the structure.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  This leaves the three overt operators that are part of the negative article \textit{kein} ‘no’, the singularity numeral \textit{EIN} ‘one’, and \textit{so’n} ‘such a’ to be discussed. Unlike the cases above, these three operators are base-generated higher in the structure than \textit{ein}: NEG is outside the DP proper, \textit{Ø}\textsubscript{[-PL]} is in Spec,CardP, and \textit{so} is in Spec,TypeP. Recall from \sectref{sec:key:2.2.2} though that I have, thus far, not been able to identity cases that only involve supporting. In other words, all cases involve flagging and may, additionally, also show supporting. If this turns out to be correct, then we can claim that flagging is the primary mechanism to indicate operators, and supporting is a secondary, additional mechanism.\footnote{This could mean that flagging is a universal process but supporting is not.} Consequently, I take flagging, as discussed above, to manifest the typical properties of the relationship between expletives and their licensers. To be clear, the cases involving supporting do not seem to be telling as regards the relation between expletives and licensers.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  With this in mind, I summarize the discussion of all expletives. I start by providing a list of properties of \textit{there}, an established expletive discussed in the introduction of this book. Besides the properties mentioned there, other features have been identified. Lasnik \& Uriagereka with Boeckx (2005: 134, 150-51) point out the following traits in their overview discussion of the relevant construction:\footnote{There are other properties of \textit{there} that are not relevant to the discussion of adjectival inflections and \textit{ein} (nor are they relevant to proprial articles); for instance, a third element – the verb – does not agree with \textit{there}, but with the associate.} (i) \textit{there} does not have a meaning and has to be licensed, (ii) \textit{there} and the associate are in two different positions, (iii) \textit{there} is in the highest site and the associate is in the lowest site, (iv) \textit{there} stands in a certain formal relation with the associate such that both sites are related by movement, and (v) the higher position is made visible, either by \textit{there} or the associate indicating the abstract subject position.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  Comparing these properties to those of adjectival inflections and \textit{ein}, we notice that they are basically identical. I would like to point out though that the last point, indicating abstract structure, is often mentioned but does not seem to have attracted much attention (unlike Hypothesis 1b in the current discussion). It is possible that this property can ultimately be related to other phenomena.\footnote{One possible avenue of research might be to relate the indication of abstract structure to language acquisition.} Be that as it may, there are some further characteristics that have been pointed out for established expletives: (vi) unlike the current cases, the associate moves at LF to license the expletive, (vii) unlike the current cases, the expletive can be null in some languages (e.g., Italian), and (viii) unlike the current cases, there is an alternative construction whereby the associate occupies the higher position and the expletive does not appear at all. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Of particular interest here is the last property in (viii). As far as I am aware, adjectival inflections or \textit{ein} are never in complementary distribution with their licensers as regards the higher position. In my view, this is a difference that might indicate that there are two types of expletives after all: \textit{there} and proprial articles vs. adjectival inflections and \textit{ein}. Having said that, it might turn out that independent factors such as the requirement to spell out CNG features or to overtly indicate operators could explain the obligatory presence of the latter two elements. I leave a more detailed exploration of these ideas for future research.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Finally, the main empirical goal of this book was to provide a more comprehensive discussion of adjectival inflections and \textit{ein}. I believe this pursuit has lead to some interesting theoretical proposals. At this point, I cannot claim to have resolved all issues surrounding adjectival inflections and \textit{ein}. However, I hope to have made a good argument for the main hypothesis of this book – these two elements are semantically vacuous in German. Undoubtedly, further investigations and the detailed discussion of other languages will yield new insights into these empirical domains.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\setcounter{page}{361}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Appendix: Examples of Plural \textit{ein}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

The examples in Sections 1-3 are presented first from google, then from Twitter, and finally from Facebook. Within each of these three search domains, the examples are provided in the following order: \textit{was für (ei)ne}, \textit{was für} \textit{(ei)ne} with adjective, \textit{so (ei)ne}, and \textit{so} \textit{(ei)ne} with adjective. Similarly, the examples in \sectref{sec:key:4} are presented according to the alphabetical order of the different corpora. Within the corpora, the examples involving \textit{was für} are given before those involving \textit{so (ei)ne}. Again, instances without adjectives precede those with adjectives.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{1.1.  Googled:} was für (ei)ne \textit{without adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

"\emph{\textbf{\textup{Was} \textbf{für} \textbf{eine} \textbf{Idioten}}}" sagte Liam
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

"\emph{\textbf{\textup{Was} \textbf{für} \textbf{eine} \textbf{Arschlöcher}}}." waren meine letzten worte und somit verschwand ich auch.~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{WAS} \textbf{FÜR} \textbf{NE} \textbf{VÖGEL} ALTER SCHWEDE !!!!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} \textbf{für} \textbf{ne} \textbf{Vögel}...aber Glück für dich eben.~  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%please move the includegraphics inside the {figure} environment
\
%%\includegraphics[width=\textwidth]{figures/roehrs-img001.png}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} \textbf{für} \textbf{ne} \textbf{Vögel} habts denn da aufgetrieben?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} \textbf{für'ne} \textbf{Idioten}...!!!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Oh gott, \textbf{was} \textbf{für} \textbf{ne} \textbf{idioten}, es gibt klar ne menge websiten,
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

So nun rief er beim Freundlichen an,~\emph{\textbf{\textup{was} \textbf{für} \textbf{ne} \textbf{Idioten}}}~sie sind, den Ölkühler zu wechseln und kein Öl nachzufüllen,~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

wenn du da bist dann würde ich denen (besser nach dem kauf der siebchen) sagen \textbf{was} \textbf{für} \textbf{ne} \textbf{idioten} das sind
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Da fragte ich mich auch, \textbf{was} \textbf{für} \textbf{ne} \textbf{Idioten}, vorallem hatte ich immer Luft in der Anlage.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Also beim MM in Lübeck gabs gestern noch genug laptops von Samsung... liegt wohl auch daran das die das ding falsch ausgeschildert haben, denn dort steht 1,8 Ghz auf dem Schild~  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%please move the includegraphics inside the {figure} environment
\
%%\includegraphics[width=\textwidth]{figures/roehrs-img002.png}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 ~vom Despina. Und das Devin verkaufen sie zum gleichen Preis.... \textbf{was} \textbf{für} \textbf{ne} \textbf{idioten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Gute Idee aber du beschimpfst jeden und flames \textbf{was} \textbf{für} \textbf{ne} \textbf{Idioten} sie sind da machts kein Spaß dir zuzuckuken
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

die wollten das wir die Linke oder die NPD wählen,aber sie sagten die Linke währe für sie besser…lol \textbf{was} \textbf{für} \textbf{ne} \textbf{Idioten}…
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} \textbf{für} \textbf{ne} \textbf{Spinner} bei Lyon, 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} \textbf{für} \textbf{ne} \textbf{Spinner} gibts hier eigentlich?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Die zwei hab' ich auch gesehen. \textbf{Was} \textbf{für} \textbf{ne} \textbf{Spinner}. Der Typ der das mamor im Auto hatte
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\section{...ach gott wie lustig, \textbf{was} \textbf{für} \textbf{ne} \textbf{Typen} es hier so gibt .... :-) o.T.}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples


\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Dass muss man sich mal überlegen, \textbf{was} \textbf{für} \textbf{ne} \textbf{Schweine} dass sind.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

hm ...\textbf{was} \textbf{für} \textbf{ne} \textbf{zicken}... !!!~  
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

%%please move the includegraphics inside the {figure} environment
\
%%\includegraphics[width=\textwidth]{figures/roehrs-img003.png}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{1.2.  Googled:} was für (ei)ne \textit{with adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Auf \textbf{was} \textbf{für} \textbf{ne‘} \textbf{coolen} \textbf{Ideen} kommt ihr denn noch! LG
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

NFU und NFSU2 (\textbf{was} \textbf{für} \textbf{ne} \textbf{dummen} \textbf{Abkürzungen})~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Ich empfand dieses ignorante Verhalten als sehr unverschämt und beleidigend auf meine Person bezogen und bin gegangen!!!~\emph{\textbf{\textup{Was} \textbf{für} \textbf{eine} \textbf{dummen}}}\textbf{~Hühner} dort ;).~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\emph{\textbf{\textup{Was} \textbf{für} \textbf{eine} \textbf{hübschen}}}\textbf{~Jungs} kennst du? Namen?~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Woow \textbf{was} \textbf{für} \textbf{eine} \textbf{schönen} \textbf{Beine} hast du wirklich Frau \textsf{SMILEY}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\emph{\textbf{\textup{Was} \textbf{für} \textbf{eine} \textbf{geilen}}}\textbf{~Bilder} wer hat die bloß gemacht
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} \textbf{für} \textbf{ne} \textbf{geilen} \textbf{Threads} ey!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{was} hast du immer~\emph{\textbf{\textup{für} \textbf{ne} \textbf{krassen}}}\textbf{~anfälle} im voice :d
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} mach ich heut~\emph{\textbf{\textup{für} \textbf{ne} \textbf{krassen}}}\textbf{~Fehler} LOL
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{was} meine Unachtsamkeit~\emph{\textbf{\textup{für} \textbf{ne} \textbf{krassen}}}\textbf{~Folgen} hat
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Wow heftig dass ist voll komisch zu sehen \textbf{was~}\emph{\textbf{\textup{für} \textbf{ne} \textbf{krassen}}}\textbf{~Menschen} albanische Wurzeln haben
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} denn~\emph{\textbf{\textup{für} \textbf{ne} \textbf{verdammten}}}\textbf{~Ringe}?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{1.3.  Googled:} so (ei)ne \textit{without adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\emph{\textbf{\textup{So} \textbf{eine} \textbf{Ferien}}}~könnten darauf deuten, dass sie wünscht, sich selbst zu erholen.~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Das Personal war sehr unfreundlich. Ich hatte noch nie~\emph{\textbf{\textup{so} \textbf{eine} \textbf{Ferien}}}.~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Nich schön,~\emph{\textbf{\textup{so} \textbf{ne} \textbf{Ferien}}}.~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

ich hab mal~\emph{\textbf{\textup{so'ne} \textbf{ferien}}}~in den alpen mit ner jugendgruppe gemacht~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{1.4.  Googled:} so (ei)ne \textit{with adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

und denn kommen dir~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~Typen} entgegnen gestolpert
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

ihr macht~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~lieder}~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

~Hahahahah~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~vids} hahahah.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\emph{\textbf{\textup{So} \textbf{ne} \textbf{geilen}}}\textbf{~Leute} echt jeder auf seiner Art so sympathisch!!~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{drei~}\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~Mädels}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Wie kommt man auf~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~Ideen}?~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Hi,wie kriegen die~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~Lichteffekte} hin?~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Dass es hier auf dieser Seite~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~Schlampen} wie mich gibt?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Ich hoffe die TranX macht dann auch~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~Geräusche}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

warum die Phiole Rechts im Bild nicht~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~Reflection} \textbf{Effekte} hat wie die in der Mitte
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Herzlichen Glückwunsch und macht weiter~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~Videos}!~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Neue Sounddesigner und dann~\emph{\textbf{\textup{so'ne} \textbf{geilen}}}\textbf{~Kisten}?~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

ich finde ihr solltet~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~partys} mal am samstag machen~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Und nur~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~Antworten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

textlich hat der~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~dinger} dabei das ich manchmal mit grinsendem gesicht dasitze und denke "geil".
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

wieso gibts~\emph{\textbf{\textup{so'ne} \textbf{geilen}}}\textbf{~typen} nur in japan
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Mushido hat immer~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~Beats} :D
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\emph{\textbf{\textup{So} \textbf{ne} \textbf{geilen}}}\textbf{~Geländewagen} find ich richig geil
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

es gibt ja im BC bei den ganzen objekten~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~schilder} wie "berlin Gasthof"
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

die hat~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~Beine}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

wenn da~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~Sachen} Rauskommen~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

woher kriegsten du~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~witze}?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

jetzt nutze ich~\emph{\textbf{\textup{so} \textbf{ne} \textbf{geilen}}}\textbf{~dinger} für den PC~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Man würd'~\emph{\textbf{\textup{so} \textbf{'ne} \textbf{süßen}}}\textbf{~Sachen} natürlich niemals tun.~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Och~\emph{\textbf{\textup{so} \textbf{ne} \textbf{süssen}}}\textbf{~Katzis}... :-)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\emph{\textbf{\textup{So'ne} \textbf{süßen}}}\textbf{~Dreckspätze} und selbst bei so einem. Mistwetter macht Doris noch ne tolle Geschichte draus.~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Unsere Sabine macht immer~\emph{\textbf{\textup{so} \textbf{ne} \textbf{süßen}}}\textbf{~Geschenke}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Ihr seid doch alles~\emph{\textbf{\textup{so} \textbf{ne} \textbf{süßen}}}\textbf{~Krümels}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

das sind~\emph{\textbf{\textup{so} \textbf{ne} \textbf{süßen}}}\textbf{~hundewelpen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\emph{\textbf{\textup{So} \textbf{ne} \textbf{süßen}}}\textbf{~kleinen} \textbf{Pfoten}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Ja, ja,~\emph{\textbf{\textup{so'ne} \textbf{süßen}}}\textbf{~kleinen} \textbf{Löckenwölfe} sind schon entzückend!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

mit ihm~\emph{\textbf{\textup{so} \textbf{'ne} \textbf{süßen}}}\textbf{~Momente} zu haben
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

als ich schwanger war (was ja auch noch nicht sooo lang her ist) gabs~\emph{\textbf{\textup{so} \textbf{ne} \textbf{süßen}}}\textbf{~teile} leider nicht
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{2.1.  Twitter:} was für (ei)ne \textit{without adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Wasfür} \textbf{eine} \textbf{Idioten} leben hier? Wahnsinn!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Was glaubst Du denn, \textbf{was} da \textbf{für} \textbf{eine} \textbf{Leute} wohnen?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{was} das \textbf{für} \textbf{eine} \textbf{Leute} sind, mit denen sich die Polizei auseinandersetzen muss.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} sind das denn jetzt \textbf{für} \textbf{ne} \textbf{Leute}, die
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} waren denn das \textbf{für} \textbf{ne} \textbf{Leute} eben auf Periscope. Schlimm.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

man man man \textbf{was} \textbf{für} \textbf{ne} \textbf{idioten} es dich gibt
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} \textbf{für} \textbf{ne} \textbf{Idioten} und danke
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} \textbf{für} \textbf{ne} \textbf{idioten} seit ihr eigentlich habt ihr noch alle tassen im schrank
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} \textbf{für} \textbf{eine} \textbf{Idioten} wohnen nur in Sachsen-Anhalt? Ich explodiere.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{was} \textbf{für} \textbf{eine} \textbf{Idioten}! Die Bude aber noch an den größeren Deppen Fratzenbuch für 16Milliarden zu verkaufen,ist Realsatire
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Gerade gesehen... \textbf{was} \textbf{für} \textbf{eine} \textbf{Idioten}, die so Aufmerksamkeit wollen
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} \textbf{für} \textbf{eine} \textbf{Idioten} , wir können in Deutschland gar keine Schraube drehen.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} sind denn das \textbf{für} \textbf{ne} \textbf{vögel} am alex? Oo [Berlin]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

wat für ne spinner
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

was für ne spinner
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Lateline man \textbf{was} \textbf{für} \textbf{eine} \textbf{spinner} von Anrufer
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} \textbf{für} \textbf{eine} \textbf{spinner} er hätte sich lieber ne andere Sportart suchen sollen
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

DPA, \textbf{was} \textbf{für} \textbf{eine} \textbf{Arschlöcher} seid ihr denn?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} sind das \textbf{für} \textbf{ne} \textbf{Arschlöcher}, die IMMER NOCH megalaute Böller zünden?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Wat für ne Kunden
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Die sind ja picky. \textbf{Was} \textbf{für} \textbf{ne} \textbf{Säcke}. :D
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} \textbf{für} \textbf{ne} \textbf{Säcke} sind das denn bitte? WTF? D:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

viel spass mit deiner Familie, yaaa. \textbf{was} \textbf{fuer} \textbf{eine} \textbf{Ferien}! Hier habe ich auch schoene Ferien. :D
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Ferien???????????????????????? \textbf{Was} \textbf{für} \textbf{ne} \textbf{Ferien}????????????????????????
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{was} \textbf{für} \textbf{ne} \textbf{ferien} habt ihr!? :o
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{was} \textbf{für} \textbf{ne} \textbf{ferien} ?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{wat} \textbf{fuer} \textbf{ne} \textbf{spasten}. das ergibt doch keinen sinn xd
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

echt jetzt?? \textbf{Was} \textbf{für} \textbf{ne} \textbf{spasten} sind das bitte -.- arghhh
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{2.2.  Twitter:} was für (ei)ne \textit{with adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Ich wurde auf Twitch gebannt weil ich geschrieben hab: Schöne achseln diggah!? \textbf{was} \textbf{für} \textbf{eine} \textbf{dummen} \textbf{mods}?!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Community schrumpft 60\%...lol...\textbf{was} \textbf{für} \textbf{ne} \textbf{verdammten} \textbf{idioten}...
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Wat für ne furchtbaren Trikots
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} \textbf{für} \textbf{eine} \textbf{bescheuerten} \textbf{Haufen} sitzen in unsere Ministerien. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{2.3.  Twitter:} so (ei)ne \textit{without adjective (but with numeral)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

ach man wir sind schon \textbf{so} \textbf{ne} \textbf{zwei} \textbf{wärmflaschen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

und \textbf{so} \textbf{ne} \textbf{zwei} \textbf{Kämpfer} die alles umpflügen,
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{So} \textbf{'ne} \textbf{zwei} \textbf{Bubis}... Denken auch ich halt mein maul ...
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{2.4.  Twitter:} so (ei)ne \textit{with adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Selten \textbf{so} \textbf{ne} \textbf{geilen} \textbf{Livebands} gehört.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

"wir sind \textbf{so} \textbf{ne} \textbf{geilen} \textbf{Socken}"
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Wir haben hier \textbf{so} \textbf{'ne} \textbf{geilen} \textbf{Lehrer-Praktikanten}.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{So} \textbf{ne} \textbf{geilen} \textbf{Bilder}...
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Ich bins als Dorfkind ja nicht gewohnt \textbf{so} \textbf{ne} \textbf{geilen} \textbf{läden} zu habn
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

aber als ich in meiner emo-phase war hab ich mir immer \textbf{so'ne} \textbf{geilen} \textbf{stories} ausgedacht und muss heute noch dran denken 8)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Ich will \textbf{so} \textbf{ne} \textbf{geilen} \textbf{smileys} BEI DER TASTATUR DABEI
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

und dann noch \textbf{so'ne} \textbf{geilen} \textbf{schuhe}!!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

wie kommt man sonst auf \textbf{so'ne} \textbf{geilen} \textbf{Ideen}?!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Aber wie können die \textbf{so} \textbf{ne} \textbf{geilen} \textbf{Tänzer} rauswerfen???
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

woa cheng \textbf{so} \textbf{ne} \textbf{geilen} \textbf{weißen} \textbf{schuhe} hab ich ja noch nie gesehen xd das strahlt richtig
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Kann ja nicht jeder \textbf{so} \textbf{ne} \textbf{geilen} \textbf{Videos} machen wie du :D
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Aber du hast doch auch ohne den Anzug ein Sixpack und \textbf{so'ne} \textbf{geilen} \textbf{Muskeln}!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

wo bekommt man den \textbf{so} \textbf{ne} \textbf{geilen} \textbf{Lollys}?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Doch, der Tag war okay. Nur hatte ich bisher nicht \textbf{so} \textbf{ne} \textbf{geilen} \textbf{Nachrichten} gehört
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Das kommt jetzt mega weird..aber so'n Stripper als Freund......also weil die machen immer \textbf{so} \textbf{ne} \textbf{geilen} \textbf{Bewegungen},weißt du...
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Du machst immer \textbf{so} \textbf{ne} \textbf{geilen} \textbf{Verlosungen} !
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{so'ne} \textbf{geilen} \textbf{Tage} muss es auch mal geben
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

uf \textbf{so} \textbf{ne} \textbf{geilen} \textbf{ideen} kommst auch nur du!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

ey ich schwör Dir Freitag und Montag werden \textbf{so} \textbf{'ne} \textbf{geilen} \textbf{Tage} !! 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

und immer \textbf{so} \textbf{ne} \textbf{geilen} \textbf{sachen} ;)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

hör auf hier \textbf{so'ne} \textbf{geilen} \textbf{Bands} aufzuzählen,sonst muss ich leider kurz ausflippen,verdammte Axxxt.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Und dann auch immer \textbf{so} \textbf{ne} \textbf{geilen} \textbf{Argumente} bringen, wie "FRÜHER war das auch nicht so"
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Alter \textbf{irgend} \textbf{so} \textbf{ne} \textbf{dummen} \textbf{Hunde} haben direkt neben mir n Böller gezündet
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{so} \textbf{ne} \textbf{dummen} \textbf{aufgaben} kann sich auch nur klaas ausdenken
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

als ob brendon jetzt ernsthaft VON DER QUEERCOMMUNITY \textbf{so'ne} \textbf{dummen} \textbf{kommentare} dafür abbekommt dass er sich als pan geouted hat
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

komm jetzt noch mehr \textbf{so} \textbf{ne} \textbf{dummen} \textbf{machosprüche} von dir?
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

"Selbst schuld, wenn du immer \textbf{so'ne} \textbf{dummen} \textbf{Kommentare} gibst."
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Ich war eben kurz davor \textbf{so} \textbf{'ne} \textbf{dummen} \textbf{Kinder} zu fragen,
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Warum passieren mir immer \textbf{so} \textbf{ne} \textbf{dummen} \textbf{Sachen}?!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Klar, streut noch \textbf{so} \textbf{'ne} \textbf{dummen} \textbf{Gerüchte}. :O
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Da waren heute \textbf{2} \textbf{so'ne} \textbf{dummen} \textbf{Weiber} beim Zumba-Kurs, die schrien ständig wie \textbf{so'ne} \textbf{Fangirls} [NB: this token was not counted at all, DR]. -\_\_\_\_- Das hat genervt!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Gestern so um 23 Uhr waren \textbf{so'ne} \textbf{dummen} \textbf{Jugendlichen} vor meinem Febster und haben laut Musik gespielt + laut geredet.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Sollte ich öfter machen, dann krieg ich auch nicht \textbf{so} \textbf{'ne} \textbf{dummen} \textbf{Nachrichten} von Mitschülern.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

ich hab ja früher \textbf{so} \textbf{ne} \textbf{dummen} \textbf{Digitalzeichnungen} gemacht
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

komm jetzt nicht noch auf \textbf{so'ne} \textbf{dummen} \textbf{Ideen}... ;o)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Boa. \textbf{So} \textbf{ne} \textbf{dummen} \textbf{Eltern} hätte ich auch gern.
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

ich schon \textbf{so} \textbf{ne} \textbf{dummen} \textbf{Vögel} von der CDU können mir gestohlen bleiben
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Ich wer dir \textbf{so} \textbf{ne} \textbf{dummen} \textbf{Sprüche} noch austreiben !
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{so'ne} \textbf{dummen} \textbf{Menschen}!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Wieso gibt es \textbf{so} \textbf{ne} \textbf{dummen,} \textbf{bescheuerten} \textbf{Leute} die sowas machen?? D:
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

hahaha und \textbf{so} \textbf{ne} \textbf{dummen} \textbf{leute} am tisch ehy
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

es gibt halt \textbf{so} \textbf{ne} \textbf{dummen} \textbf{und} \textbf{abgehobenen} \textbf{leute}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

ach \textbf{so} \textbf{'ne} \textbf{dummen} gibt es öfter. xD
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{3.1.  Facebook:} so (ei)ne \textit{with adjective}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{so} \textbf{ne} \textbf{dummen} \textbf{Schweine} !!! wie kann man so bloß mit den Tieren umgehen !!!! HASS !!!!!!!!!
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Hey Schnitzel, hoffe du bist bald wieder wohl auf. \textbf{So} \textbf{ne} \textbf{dummen} \textbf{idioten} ey.....
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Wie kann man nur \textbf{so} \textbf{ne} \textbf{dummen} \textbf{Idioten} wählen.....
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Bitte blockiert endlich \textbf{so} \textbf{ne} \textbf{blöden} \textbf{Tussen}!!! Wir sind hier nicht beim Werbe-Flohmarkt! [Hamburg]
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Ich treffe täglich auf \textbf{so} \textbf{ne} \textbf{ekelhaften} \textbf{Leute} ohne Achtung...
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

4.   Datenbank für Gesprochenes Deutsch
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Australiendeutsch
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{und}~\textstylew{deshalb}~\textstylew{kann}~\textstylew{ich}~\textstylew{Ihnen}~\textstylew{nich}~\textstylew{sagen},~\textstylew{was}~\textstylew{\textbf{was}}\textbf{~}\textstylew{\textbf{für}}\textbf{~}\textstylew{\textbf{eine}}\textbf{~}\textstylew{\textbf{Geschäfte}}~\textstylew{sie}~\textstylew{hier}~\textstylew{gehabt}~\textstylew{haben}. 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(pseudonym: Bastian; born in Timkenberg, Mecklenburg-Vorpommern)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{Was} \textbf{für} \textbf{'ne}~\textbf{Fächer} ?~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(born: no information, but probably an interviewer from Germany)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

sone~dumme Bengels 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

(pseudonym: Patrick; born in Tanunda, Australia; ancestors from Silesia)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Berliner Wendekorpus 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{\textbf{was}}~\textstylew{hast-e}~\textstylew{dir~}\textstylew{\textbf{für-ne}}~\textstylew{\textbf{gedanken}}~\textstylew{gemacht}~\textstylew{wie-s}~\textstylew{weitergeht}? 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (no information about speaker available)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{und}~\textstylenv{*}\textstylew{~\textbf{so-ne}}~\textstylew{\textbf{sachen}}~\textstylew{ja\^{}}~\textstylew{ooch}~\textstylew{n}~\textstylew{bißel}~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (}pseudonym: Dirk; lives in~Friedrichshain, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{und}~\textstylew{allet~\textbf{so-ne}~\textbf{so-ne}}~\textstylew{\textbf{späßchen}} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (}pseudonym: Dirk; lives in~Friedrichshain, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{und}~\textstylew{allet~}\textstylew{\textbf{so-ne}}~\textstylew{\textbf{späße}}~\textstylew{wa}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (}pseudonym: Dirk; lives in~Friedrichshain, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{dit}~\textstylew{sind}~\textstylew{allet~}\textstylew{\textbf{so-ne}}~\textstylew{\textbf{dinger}}~\textstylew{ja} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (}pseudonym: Dirk; lives in~Friedrichshain, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{dit}~\textstylew{sind}~\textstylew{wie}~\textstylew{jesacht~}\textstylew{\textbf{so-ne}}~\textstylew{\textbf{dinge}}~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (}pseudonym: Dirk; lives in~Friedrichshain, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{wenn}~\textstylew{ick}~\textstylew{sehe}~\textstylew{zehn~}\textstylew{\textbf{so-ne}}~\textstylew{\textbf{stullchen}}~\textstylew{daa}~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (}pseudonym: Dirk; lives in~Friedrichshain, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{dit}~\textstylew{sind}~\textstylew{ja}~\textstylew{ooch}~\textstylew{nich~}\textstylew{\textbf{so-ne}}~\textstylew{\textbf{sachen}} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (}pseudonym: Dirk; lives in~Friedrichshain, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{aber}~\textstylew{äh~}\textstylew{\textbf{so-ne}}\textstylew{~}\textstylew{\textbf{so-ne}}~\textstylew{\textbf{dinge}}~\textstylenv{*}~\textstylew{dit}~\textstylew{stimmt} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (}pseudonym: Dirk; lives in~Friedrichshain, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{und}~\textstylew{dazu}~\textstylew{noch~\textbf{so-ne}~\textbf{so-ne}}~\textstylew{\textbf{tochterfirmen}} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudonym: Gina; lives in} Hennigsdorf, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{oder}~\textstylenv{*}~\textstylew{so~}\textstylew{\textbf{so-ne}}~\textstylew{\textbf{antworten}}~\textstylew{kriegt}~\textstylew{man}~\textstylew{da}~\textstylew{halt}~\textstylew{ne} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudonym: Gina; lives in} Hennigsdorf, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{und~}\textstylew{\textbf{so-ne}}~\textstylew{\textbf{dinger}}~\textstylew{ja} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudonym: Gina; lives in} Hennigsdorf, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{da}~\textstylew{jabt-s}~\textstylew{auch}~\textstylew{jetzt}~\textstylenv{((unverständlich))}\textstylew{~}\textstylew{\textbf{so-ne}}~\textstylew{\textbf{shops}}~\textstylew{da} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudonym: Gina; lives in} Hennigsdorf, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{die}~\textstylew{sind}~\textstylew{da}~\textstylew{allet~\textbf{so-ne}}~\textstylew{\textbf{iischen}}~\textstylew{da} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudonym: Gina; lives in} Hennigsdorf, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{ürgendwie}~\textstylenv{*}~\textstylew{manchmal}~\textstylew{hab}~\textstylew{ick}~\textstylew{ooch~\textbf{so-ne}~\textbf{so-ne}}~\textstylew{\textbf{dinger}}~\textstylew{jehört}~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudonym: Gina; lives in} Hennigsdorf, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

rundrum und~\textbf{sone~sachn} die 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: Lore; lives in Treptow, Berlin)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

jetzt in~\textbf{sone~kategorien} einteilen 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (no information about speaker available)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

immer nur~\textbf{sone~fetzen} aufgefangen...dit sind~\textbf{sone~wörter} die 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: Jenny; lives in Mitte, Berlin)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{und}~\textstylew{nich}~\textstylew{\textbf{so-ne}}~\textstylew{\textbf{zehn}}\textbf{~}\textstylew{\textbf{abjepackten}}\textbf{~}\textstylew{\textbf{stullchen}}~\textstylew{fürn}~\textstylew{gleichen}~\textstylew{preis}~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (}pseudonym: Dirk; lives in~Friedrichshain, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{weil}~\textstylew{ooch~}\textstylew{\textbf{so-ne}}~\textstylenv{*}~\textstylew{\textbf{blöden}}\textbf{~}\textstylew{\textbf{zuchverbindungen}}~\textstylew{warn} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudonym: Gina; lives in} Hennigsdorf, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{na}~\textstylew{wie}~\textstylew{sacht}~\textstylew{man}~\textstylenv{*}\textstylew{~}\textstylew{\textbf{so-ne}}~\textstylew{\textbf{ausrangierten}}\textbf{~}\textstylew{\textbf{sachen}} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudonym: Gina; lives in} Hennigsdorf, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{ne\^{}}~\textstylenv{*}~\textstylew{imma}~\textstylew{nur~}\textstylew{\textbf{so-ne}}~\textstylew{\textbf{ollen}}\textbf{~}\textstylew{\textbf{äppel}} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudonym: Gina; lives in} Hennigsdorf, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{weil}~\textstylenv{*}~\textstylew{ick}~\textstylew{hatte~\textbf{so}}\textbf{~}\textstylew{\textbf{ne}}~\textstylew{\textbf{panischen}}\textbf{~}\textstylew{\textbf{ängste}}~\textstylew{in}~\textstylew{mir} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudoym: Maria; lives in} Marzahn, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{dit}~\textstylew{sind}~\textstylew{ja~\textbf{so}}\textbf{~}\textstylew{\textbf{ne}}~\textstylew{\textbf{richtchen}}\textbf{~}\textstylew{\textbf{arroganten}}\textbf{~}\textstylew{\textbf{scheißer}}~\textstylenv{*}~\textstylew{die}~\textstylew{ham} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudonym: Kira; lives in} Hellersdorf, Berlin\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

durch~\textbf{sone~engen} \textbf{gassen} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: Micha; lives in Lichtenberg, Berlin)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

daß auch~\textbf{sone~auswüchse} [NB: this token was not counted at all, DR] von...ich \textbf{sone} \textbf{übergroßen} \textbf{erwartungen} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: Willy; lives in Köpenick, Berlin)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

warn ja~\textbf{sone~weißen} \textbf{schnecken} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: Aldi; lives in Lichtenrade, Berlin)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Biographische und Reiseerzählungen
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

sonst auch~\textbf{sone~bekannten} \textbf{Losungen}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: Gisela; born in Potsdam)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Deutsch heute
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

also~\textbf{sone~Sachen} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: none; born in Berlin)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{also}~\textstylew{da}~\textstylew{kommen}~\textstylew{\textbf{so}}\textbf{~}\textstylew{\textbf{ne}}\textbf{~}\textstylew{\textbf{Nägel}}~\textstylew{nachher}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: none; born in Crivitz, Mecklenburg-Vorpommern)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Deutsch in Namibia
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{\textbf{was}}\textbf{~}\textstylew{\textbf{für}}\textbf{~}\textstylew{\textbf{ne}}\textbf{~}\textstylew{\textbf{kinderspiele}}\textbf{~}\textstylew{ham}~(\textstylew{wir})~\textstylew{gespielt} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{(pseudonym: none; pupil born in} Otjiwarongo, Namibia; parents speak German\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{un}~\textstylew{was}~\textstylew{is}~\textstylew{dem}~\textstylew{seine}~\textstylew{story}\textbf{~}\textstylew{\textbf{was}}\textbf{~}\textstylew{\textbf{für}}\textbf{~}\textstylew{\textbf{ne}}\textbf{~}\textstylew{\textbf{manners}}\textbf{~}\textstylew{hat}~\textstylew{der} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{(pseudonym: none; pupil born in} Gobabis,\textstylew{ Namibia; parents speak German)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{\textbf{was}} \textstylew{\textbf{für}}\textbf{\_}\textstylew{\textbf{ne}}\textbf{~}\textstylew{\textbf{spiele} }
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{(pseudonym: none; pupil born in} Windhoek,\textstylew{ Namibia; mother speaks German)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

sprechn un~\textbf{sone~sachn} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (\textstylew{pseudonym: none; lab assistant; parents speak German})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

nich~\textbf{sone~leute} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: none (but same as just above); lab assistant; parents speak German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

ich will~\textbf{sone~haare} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: none; pupil born in Pietermaritzburg, South Africa; parents speak German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

du un~\textbf{sone~sachn} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: none; adult born in Windhoek, Namibia; parents speak German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

un un~\textbf{sone~sachn} dabei 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: none; self-employed; parents speak German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

will da~\textbf{sone~stückn} rausschneidn 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: none; accountant; parents speak German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Flucht und Emigration nach Großbritannien
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Aber \textbf{all}~\textbf{sone~Probleme} gab es 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (name: Margarete; born in Berlin)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Forschungs- und Lehrkorpus Gesprochenes Deutsch
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{un}\_\textstylew{da}~\textstylew{gibts~}\textstylew{\textbf{so}}\textbf{~}\textstylew{\textbf{ne}}~\textstylew{und~}\textstylew{\textbf{so}}\textbf{~}\textstylew{\textbf{ne}}~\textstylew{\textbf{urteile}} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudonym: Rainer; dialect: Westphalian)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{gummimanschette}~\textstylew{mit~}\textstylew{\textbf{so}}\textbf{~}\textstylew{\textbf{ne}}~\textstylew{\textbf{gummikragen}} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudonym: Albrecht; dialect: Eastphalian)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{außer}\textstyleolmarker{[}\textstylew{dem}~\textstylew{wir}~\textstylew{können}~\textstylew{ja}\textstyleolmarker{]~}\textstylew{\textbf{so}}\textbf{~}\textstylew{\textbf{ne}}~\textstylew{\textbf{romantiker}}~\textstylew{we}~\textstylew{ge}~\textstylew{machen} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{ \textrm{(pseudonym: Anita; dialect: Rhine-Franconian)}}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\end{}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{ja}~\textstylew{die}~\textstylew{ham}~\textstylew{da}~\textstylew{normalerweise}~\textstylew{\textbf{so}}\textbf{\_}\textstylew{\textbf{ne}}~\textstylew{\textbf{mützen}}~\textstylew{auf} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudonym: Christina; dialect: Rhine-Franconian})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{so}~\textstylew{wie~\textbf{so}}\textbf{~}\textstylew{\textbf{ne}}~\textstylew{\textbf{nägel}} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{  (pseudonym: Henrike; dialect:} Swabian\textstylew{)}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textstylew{weißt}~\textstylew{du}~\textstylew{da}~\textstylew{sitz}~\textstylew{er}~\textstylew{sogar}~\textstylew{\textbf{so}}\textbf{~}\textstylew{\textbf{ne}}~\textstylew{\textbf{kleinen}}\textbf{~}\textstylew{\textbf{rahmen}}~\textstylenv{(.)}~
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: Annabelle; dialect: \textstylew{Rhine-Franconian})
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

also~\textbf{sone~flachen} \textbf{wandvorlagen} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: Tobias; dialect: Northern Low German)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

also so~\textbf{sone~freien} \textbf{schulen} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: Birgit; dialect: Mecklenburg-Western Pomeranian)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Gesprochene Wissenschaftssprache Kontrastiv
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

gibt s~\textbf{sone~karten} im 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: Karen; dialect: not available)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Mennonitenplautdietsch in Nord- und Südamerika
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Ik gleich~\textbf{sone~Menschen} nich 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: none; born in Ciudad Cuauhtémoc, Mexico)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

gleich nich~\textbf{sone~Menschen} wat 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: none (but different from just above); born in Ciudad Cuauhtémoc, Mexico)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Russlanddeutsche Dialekte
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\textbf{viele~sone~Tänze} und 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: none: born in Ukraine)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

sone~scheene Jeschichten 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: none (but same as just above); born in Ukraine)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

jesungen~\textbf{scheene~sone}\textbf{~Lieder} 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: none (but same as just above); born in Ukraine)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\itshape
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

Zwirner-Korpus
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

und alle~\textbf{sone~Scherze} gemacht 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: none; born in Schessinghausen, Lower Saxony)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\bfseries
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

alle~sone~ähnlichen Sachen 
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

  (pseudonym: none; born in Rosgaard, Schleswig-Holstein)
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea 
 \gll \\
   \\
 \glt
\z % you might need an extra \z if this is the last of several subexamples
\begin{verbatim}%%move bib entries to  localbibliography.bib
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{\textmd{\textup{Abney1987,
	author = {\textmd{\textup{Abney, Steven},
	note = {Doctoral Dissertation,   MIT.}}},
	title = {The {English} Noun Phrase in its Sentential Aspect},
	year = {1987}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Abraham2013,
	author = {Abraham, Werner},
	journal = {\textit{Linguistische Berichte}, \textit{Sonderheft}},
	pages = {247--271},
	title = {Dialect as a spoken-only medium: {{W}}hat it means – and what it does not   mean},
	volume = {19},
	year = {2013}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Ackles1996,
	author = {Ackles, Nancy},
	journal = {\textit{Working Papers in Linguistics}},
	pages = {1--35},
	title = {The Indefinite Article, Historical Syntax and the Noun Phrase Structure},
	volume = {14},
	year = {1996}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Alexiadou2004,
	address = {Amsterdam},
	author = {Alexiadou, Artemis},
	booktitle = {\textit{Diachronic clues to synchronic grammar},   pp},
	editor = {Eric Fuß and Carola Trips},
	pages = {31--58},
	publisher = {John Benjamins},
	title = {On the development of possessive determiners: {{C}}onsequences for {DP}   structure},
	year = {2004}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Alexiadou2005,
	author = {Alexiadou, Artemis},
	journal = {\textit{Lingua}},
	pages = {787--819},
	title = {Possessors and (in)definiteness},
	volume = {115},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Alexiadou2014,
	address = {Amsterdam},
	author = {Alexiadou, Artemis},
	publisher = {John   Benjamins},
	title = {\textit{Multiple Determiners and the Structure of {DP}s}},
	year = {2014}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{AlexiadouStavrou2007,
	address = {Berlin},
	author = {Alexiadou, Artemis, Liliane Haegeman, and Melita Stavrou},
	publisher = {Mouton de Gruyter},
	title = {\textit{Noun Phrase in the   Generative Perspective}},
	year = {2007}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{AlexiadouAlexiadou2011,
	address = {Amsterdam},
	author = {Alexiadou, Artemis, Gianina Iordăchioaia and Florian Schäfer},
	booktitle = {\textit{The   Noun Phrase in {Romance} and {Germanic}: {{S}}tructure, variation, and change}},
	editor = {Petra Sleeman and Harry Perridon},
	pages = {25--40},
	publisher = {John Benjamins},
	sortname = {Alexiadou, Artemis, Gianina Iordachioaia and Florian Schafer},
	title = {Scaling the variation in   {Romance} and {Germanic} nominalizations},
	year = {2011}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Allan1980,
	address = {\textit{Language} 56 \REF{ex:key:3}},
	author = {Allan, Keith},
	publisher = {541-67},
	title = {Nouns and Countability},
	year = {1980}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Anderson1992,
	address = {Cambridge},
	author = {Anderson, Stephen},
	publisher = {Cambridge University Press},
	title = {\textit{A-morphous Morphology}},
	year = {1992}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{ArregiArregi2012,
	address = {Basque Auxiliaries and the Structure of   Spellout} [Studies in Natural Language and Linguistic Theory 86]. Dordrecht},
	author = {Arregi, Karlos and Andrew Nevins},
	publisher = {Springer},
	title = {\textit{Morphotactics},
	year = {2012}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Baechler, Raffaela. 2017. \textit{Absolute Komplexität in der Nominalflexion: Althochdeutsch,}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Mittelhochdeutsch, Alemannisch und deutsche Standardsprache}. Berlin: Language
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Science Press.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Barbiers2005,
	author = {Barbiers, Sjef},
	journal = {\textit{Journal of Comparative Germanic   Linguistics}},
	pages = {159--183},
	title = {Variation in the morphosyntax of \textit{one}},
	volume = {8},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Barbiers2008,
	address = {United Kingdom},
	author = {Barbiers, Sjef},
	booktitle = {\textit{Microvariation in   Syntactic Doubling}. [Syntax and Semantix 36]},
	editor = {Sjef Barbiers,  Olaf Koeneman, Marika Lekakou and Margreet van der Ham},
	pages = {1--34},
	publisher = {Emerald},
	title = {Microvariation in syntactic doubling – an introduction},
	year = {2008}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Bayer2015,
	address = {Tokyo},
	author = {Bayer, Josef},
	booktitle = {\textit{In Untiring Pursuit of Better Alternatives}},
	editor = {Hiroki Egashira, Hisatsugu Kitahara,  Kazuo Nakazawa, Tadao Nomura, Masayuki Oishi, Akira Saizen, and Motoko Suzuki },
	pages = {2--11},
	publisher = {Kaitakusha},
	title = {A Note on Possessor Agreement},
	year = {2015}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{BayerBayer2001,
	author = {Bayer, Josef, Markus Bader, and Michael Meng},
	journal = {\textit{Lingua}},
	pages = {465--514},
	title = {Morphological underspecification meets   oblique case: {{S}}yntactic and processing effects in {German}},
	volume = {111},
	year = {2001}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Bech1955,
	address = {\textit{Studien über das deutsche verbum infinitum}. København},
	author = {Bech, Gunnar},
	publisher = {Det Kongelige   Danske Akademie av Videnskaberne},
	title = {/57},
	year = {1955}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{BennisBennis1998,
	author = {Bennis, Hans, Norbert Corver and Marcel den Dikken},
	journal = {\textit{The   Journal of Comparative Germanic Linguistics}},
	pages = {85--117},
	title = {Predication in nominal phrases},
	volume = {1},
	year = {1998}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{\textmd{\textup{Bernstein1993,
	author = {\textmd{\textup{Bernstein, Judy B.},
	note = {Doctoral   Dissertation, The City University of New York}}\textmd{.}},
	title = {Topics in the Syntax of Nominal Structure across {Romance}},
	year = {1993}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{\textmd{\textup{Bernstein1997,
	author = {\textmd{\textup{Bernstein, Judy B.},
	journal = {Demonstratives and reinforcers in Romance and Germanic languages.} }\textmd{Lingua}\textmd{ \textup{},
	note = {.}}},
	pages = {87--113},
	sortname = {\textmd{\textup{Bernstein, Judy B.},
	volume = {102},
	year = {1997}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\textmd{\textup{Bernstein, Judy B. 2001a. The DP Hypothesis: Identifying Clausal Properties in the Nominal   Domain. In Mark Baltin \& Chris Collins (eds.)} }\textmd{The Handbook of Contemporary Syntactic   Theory}\textmd{\textup{, pp. 536-561. Malden, MA: Blackwell Publishing Ltd.}}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{\section{\textmd{Bernstein2001b,
	author = {\section{\textmd{Bernstein, Judy B.},
	journal = {Focusing the “right” way in Romance determiner phrases.} \textmd{\textit{Probus}}\textmd{},
	note = {.}}},
	pages = {1--29},
	sortname = {\section{\textmd{Bernstein, Judy B.},
	volume = {13},
	year = {2001}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Bhatt1990,
	address = {[Studien zur   deutschen Grammatik 38]. Tübingen},
	author = {Bhatt, Christa},
	publisher = {Gunter Narr Verlag},
	title = {\textit{Die syntaktische {Struktur} der Nominalphrase im {Deutschen}}},
	year = {1990}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Bierwisch1967,
	address = {In \textit{To Honor Roman Jacobson}: \textit{Essays on the Occasion   of His Seventieth Birthday}. Vol. I, pp. 239-270. Den Haag},
	author = {Bierwisch, Martin},
	publisher = {Mouton},
	title = {Syntactic Features in Morphology: {{G}}eneral Problems of so-called   Pronominal Inflection in {German}},
	year = {1967}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{\section{\textmd{Bisle-Müller1991,
	address = {Semantische und prakmatische   Aspekte ihrer Verwendung}}\textmd{. Tübingen},
	author = {\section{\textmd{Bisle-Müller, Hansjörg},
	note = {}}},
	publisher = {Niemeyer},
	sortname = {\section{\textmd{Bisle-Muller, Hansjorg},
	title = {} \textmd{\textit{Artikelwörter im {Deutschen}},
	year = {1991}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Bittner2006,
	author = {Bittner, Dagmar},
	journal = {\textit{Zeitschrift für   germanistische Linguistik}},
	number = {3},
	pages = {354--373},
	title = {Was motiviert die partielle Unflektiertheit des indefiniten Artikels? {{M}}arkiertheitstheoretische und sprachhistorische Überlegungen},
	volume = {34},
	year = {2006}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Blevins1995,
	author = {Blevins, James B.},
	journal = {\textit{Linguistics and Philosophy}},
	pages = {113--152},
	title = {Syncretism and Paradigmatic Opposition},
	volume = {18},
	year = {1995}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Borer2005,
	address = {Structuring Sense}, Volume I. Oxford},
	author = {Borer, Hagit},
	publisher = {Oxford University   Press},
	title = {\textit{In Name Only},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Borer2013,
	address = {Structuring Sense}, Volume III. Oxford},
	author = {Borer, Hagit},
	publisher = {Oxford University   Press},
	title = {\textit{Taking Form},
	year = {2013}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Börjars1998,
	address = {Publications of the   Philological Society 32. Boston},
	author = {Börjars, Kersti},
	publisher = {Blackwell},
	sortname = {Borjars, Kersti},
	title = {\textit{Feature Distribution in {Swedish} Noun Phrases}},
	year = {1998}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Bošković2004,
	address = {\textit{Natural Language \& Linguistic Theory} 22 \REF{ex:key:4}},
	author = {Bošković, Željko},
	publisher = {681-742},
	sortname = {Boskovic, Zeljko},
	title = {{Be} careful where you float your quantifiers},
	year = {2004}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Bosse2009,
	address = {\textit{Interdisciplinary   Journal for Germanic Linguistics and Semiotic Analysis} 14 \REF{ex:key:2}},
	author = {Bosse, Solveig},
	publisher = {251-287},
	title = {Split {DP}s in ({Northern}) {German} as Derived Structures},
	year = {2009}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Bowers1988,
	author = {Bowers, John},
	journal = {In    \textit{Proceedings of the West Coast Conference on Formal Linguistics}},
	note = {. Stanford   Linguistics Association, Stanford University.},
	pages = {47--62},
	title = {Extended X-Bar Theory, the {ECP}, and the Left Branch Condition},
	volume = {7},
	year = {1988}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Bowers1993,
	address = {\textit{Linguistic Inquiry} 24 \REF{ex:key:4}},
	author = {Bowers, John},
	publisher = {591-656},
	title = {The Syntax of Predication},
	year = {1993}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{BrauneBraune2004,
	address = {(15\textsuperscript{th} ed) Tübingen},
	author = {Braune, Wilhelm and Ingo Reiffenstein},
	publisher = {Max Niemeyer Verlag},
	title = {\textit{Althochdeutsche {Grammatik} I}},
	year = {2004}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Brugè1996,
	author = {Brugè, Laura},
	journal = {\textit{University   of Venice Working Papers in Linguistics}},
	number = {1},
	pages = {1--61},
	sortname = {Bruge, Laura},
	title = {Demonstrative movement in {Spanish}: {{A}} comparative approach},
	volume = {6},
	year = {1996}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\section{\textmd{Brugè, Laura. 2002. The Positions of Demonstratives in the Extended Nominal Projection. In   Guglielmo Cinque (ed.)} \textmd{\textit{Functional Structure in DP and IP: The Cartography of   Syntactic Structures}}\textmd{. vol.1, pp. 15-53. Oxford: Oxford University Press.}}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Carlson1980,
	address = {New York},
	author = {Carlson, Gregory N.},
	publisher = {Garland},
	title = {\textit{Reference to kinds in {English}}},
	year = {1980}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Carnie2021,
	address = {A Generative Introduction}. 4\textsuperscript{th} ed. Cambridge},
	author = {Carnie, Andrew},
	publisher = {Blackwell   Publishers},
	title = {\textit{Syntax},
	year = {2021}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{\textmd{\textup{Chomsky1957,
	address = {The Hague},
	author = {\textmd{\textup{Chomsky, Noam},
	note = {}}},
	publisher = {Mouton},
	sortname = {\textmd{\textup{Chomsky, Noam},
	title = {} }\textmd{Syntactic structures}\textmd{\textup{},
	year = {1957}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Chomsky, Noam. 1970. Remarks on nominalization. In R. Jacobson \& P. Rosenbaum (eds.)   \textit{Readings in English Transformational Grammar}, pp. 184-221. Waltham, MA, Ginn \& Co.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Chomsky1981,
	address = {Dordrecht},
	author = {Chomsky, Noam},
	publisher = {Foris},
	title = {\textit{Lectures on Government and Binding}},
	year = {1981}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Chomsky1991,
	author = {Chomsky, Noam},
	note = {[reprinted in   Noam Chomsky. 1995. \textit{The Minimalist Program}, pp. 129-166.]},
	title = {Some Notes on Economy of Derivation and Representation},
	year = {1991}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\textmd{\textup{Chomsky, Noam. 1995.} }\textmd{Minimalist Program.}\textmd{ \textup{Cambridge: MIT Press.}}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Chomsky2000,
	address = {Cambridge, Mass.},
	author = {Chomsky, Noam},
	booktitle = {\textit{Step by Step: {{{E}}}ssays on Minimalist Syntax in Honor   of Howard Lasnik}},
	editor = {Roger Martin, David  Micheals and Juan Uriagereka},
	pages = {89--155},
	publisher = {MIT Press},
	title = {Minimalist Inquiries: {{T}}he Framework},
	year = {2000}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{ChomskyChomsky1993,
	author = {Chomsky, Noam and Howard Lasnik},
	note = {[reprinted   in Noam Chomsky. 1995. \textit{The Minimalist Program}, pp. 13-127.]},
	title = {The Theory of Principles and Parameters},
	year = {1993}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Cinque, Guglielmo. 1994. On the Evidence for Partial N-Movement in the Romance DP. In   Guglielmo Cinque, Jan Koster, Jean-Yves Pollock, Luigi Rizzi \& Raffaella Zanuttini   (eds.) \textit{Paths Towards Universal Grammar. Studies in Honor of Richard S. Kayne}, pp. 85-  110. Georgetown University Press.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Cinque2010,
	address = {A Comparative Study}. Cambridge, MA},
	author = {Cinque, Guglielmo},
	publisher = {MIT   Press},
	title = {\textit{The Syntax of Adjectives},
	year = {2010}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Cirillo2016,
	author = {Cirillo, Robert},
	journal = {\textit{Journal of   Germanic Linguistics}},
	number = {3},
	pages = {179--218},
	title = {Why all {John}’s friends are {Dutch}, not {German}: {{O}}n the determiner-like   characteristics of the inflection on the universal quantifier in {West} {German}ic},
	volume = {28},
	year = {2016}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{CoppockCoppock2015,
	author = {Coppock, Elizabeth and David Beaver},
	journal = {Linguistics and   Philosophy},
	number = {5},
	pages = {377--435},
	title = {Definiteness and Determinacy},
	volume = {38},
	year = {2015}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Corbett2000,
	address = {Cambridge},
	author = {Corbett, Greville G.},
	publisher = {Cambridge University Press},
	title = {\textit{Number}},
	year = {2000}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Corver1991,
	author = {Corver, Norbert},
	journal = {\textit{Proceedings of NELS}},
	pages = {33--47},
	title = {Evidence for {DegP}},
	volume = {21},
	year = {1991}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{ii}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Corver1997,
	author = {Corver, Norbert},
	journal = {\textit{Natural Language and Linguistic Theory}},
	note = {.\\},
	pages = {289--368},
	title = {The Internal Structure of the {Dutch} Extended Adjectival Projection},
	volume = {15},
	year = {1997}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Corver, Norbert. 2003. A Note on Micro-dimensions of Possession in Dutch and Related   Languages. In Jan Koster and Henk van Riemsdijk (eds.) \textit{Germania et alia. A linguistic   Webschrift for Hans den Besten}. [available at:   http://odur.let.rug.nl/koster/DenBesten/contents.htm]
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{ii}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Corver2006,
	address = {Amsterdam},
	author = {Corver, Norbert},
	booktitle = {\textit{Studies on Agreement}},
	editor = {João Costa \textstyleptbrandiii{\& Maria  Cristina Figueiredo Silva}},
	pages = {47--73},
	publisher = {John   Benjamins},
	title = {Proleptic Agreement as a good design property},
	year = {2006}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{CorverCorver2010,
	author = {Corver, Norbert and Marjo van Koppen},
	journal = {\textit{The Journal of Comparative Germanic Linguistics}},
	number = {2},
	pages = {99--140},
	title = {Ellipsis in {Dutch} possessive noun phrases: {{A}}   micro-comparative approach},
	volume = {13},
	year = {2010}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{CorverCorver2011a,
	address = {Amsterdam},
	author = {Corver, Norbert and Marjo van Koppen},
	booktitle = {\textit{The Noun Phrase in {Romance} and {Germanic}: {{S}}tructure, variation, and   change}},
	editor = {Petra Sleeman and Harry  Perridon},
	pages = {57--87},
	publisher = {John Benjamins},
	title = {Micro-diversity in {Dutch} interrogative {DP}s. {{A}}   case study on the (dis)continuous \textit{wat voor ’n N}{}-construction},
	year = {2011}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{CorverCorver2011b,
	address = {\textit{Natural Language \& Linguistic Theory} 29 \REF{ex:key:2}},
	author = {Corver, Norbert and Marjo van Koppen},
	publisher = {371-421},
	title = {{NP}-ellipsis with adjectival remnants: {{A}} micro-  comparative perspective},
	year = {2011}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Crisma1999,
	author = {Crisma, Paola},
	journal = {\textit{Rivista di   Grammatica Generativa}},
	pages = {105--125},
	title = {Nominals without the article in the {Germanic} languages},
	volume = {24},
	year = {1999}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Curme1910,
	author = {Curme, George O.},
	journal = {\textit{Journal of English and Germanic Philology}},
	pages = {439--482},
	title = {The Origin and Growth of the Adjective Declension in {Germanic}},
	volume = {9},
	year = {1910}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{\textmd{\textup{Darski1979,
	author = {\textmd{\textup{Darski, Józef},
	journal = {Die Adjektivdeklination im Deutschen.} }\textmd{Sprachwissenschaft}\textmd{ \textup{},
	note = {.}}},
	pages = {190--205},
	sortname = {\textmd{\textup{Darski, Jozef},
	volume = {4},
	year = {1979}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{\textmd{\textup{Déchaine\textmd{\textup{Déchaine2002,
	address = {Decomposing Pronouns.} }\textmd{Linguistic Inquiry}\textmd{ \textup{33 \REF{ex:key:3}},
	author = {\textmd{\textup{Déchaine, Rose-Marie and Martina Wiltschko},
	note = {}}},
	publisher = {409-442},
	sortname = {\textmd{\textup{Dechaine, Rose-Marie and Martina Wiltschko},
	year = {2002}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

De Lacy, Paul. 2006. \textit{Markedness. Reduction and preservation in phonology}. Cambridge:   Cambridge University Press
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{DelfittoDelfitto1991,
	author = {Delfitto, Denis and Jan Schroten},
	journal = {\textit{Probus}},
	number = {2},
	pages = {155--85},
	title = {Bare Plurals and the Number Affix in {DP}},
	volume = {3},
	year = {1991}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{DelormeDelorme1972,
	author = {Delorme, Evelyne and Ray C. Dougherty},
	journal = {\textit{Foundations of Language}},
	pages = {2--29},
	title = {Appositive {NP} Constructions: \textit{we, the men; we   men; I, a man}; {ETC}},
	volume = {8},
	year = {1972}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Delsing1993,
	author = {Delsing, Lars-Olof},
	note = {A Comparative Study}. Doctoral Dissertation, University of Lund.},
	title = {\textit{The Internal Structure of Noun Phrases in the {Scandinavian}   Languages},
	year = {1993}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Demske2001,
	address = {Berlin and New York},
	author = {Demske, Ulrike},
	publisher = {Walter de Gruyter},
	title = {\textit{{Merkmale} und Relationen: {{D}}iachrone {Studien} zur Nominalphrase des   {Deutschen}}},
	year = {2001}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Déprez2005,
	author = {Déprez, Viviane},
	journal = {\textit{Lingua}},
	pages = {857--883},
	sortname = {Deprez, Viviane},
	title = {Morphological number, semantic number and bare nouns},
	volume = {115},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Diesing1992,
	address = {Cambridge},
	author = {Diesing, Molly},
	publisher = {MIT Press},
	title = {\textit{Indefinites}},
	year = {1992}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Dikken1995,
	author = {Dikken, Marcel den},
	note = {Paper presented at GLOW 18, Tromsø. Unpublished   manuscript, Vrije Universiteit Amsterdam/HIL.},
	title = {Copulas},
	year = {1995}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Dikken2006,
	address = {The Syntax of Predication, Predicate Inversion,   and Copulas}. Cambridge},
	author = {Dikken, Marcel den},
	publisher = {MIT Press},
	title = {\textit{Relators and Linkers},
	year = {2006}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Duden1989,
	address = {(2nd. ed) Mannheim},
	author = {Duden},
	publisher = {Bibliographisches Institut},
	title = {\textit{Deutsches Universalwörterbuch}},
	year = {1989}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Duden1995,
	address = {Vol 4. (5th ed.) Mannheim},
	author = {Duden},
	publisher = {Dudenverlag},
	title = {\textit{Duden: {{G}}rammatik der deutschen Gegenwartsprache}},
	year = {1995}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Duden2007,
	address = {Vol. 9. (6th ed.) Mannheim},
	author = {Duden},
	publisher = {Dudenverlag},
	title = {\textit{Duden:} \textit{Richtiges und gutes {Deutsch}}},
	year = {2007}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{\textmd{\textup{Duden2016,
	address = {Vol 4. (9th ed.) Mannheim},
	author = {\textmd{\textup{Duden},
	note = {}}},
	publisher = {Dudenverlag},
	sortname = {\textmd{\textup{Duden},
	title = {} }\textmd{Duden: {{G}}rammatik der deutschen Gegenwartsprache}\textmd{\textup{},
	year = {2016}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{\textmd{\textup{Durrell2002,
	address = {4th ed. Chicago},
	author = {\textmd{\textup{Durrell, Martin},
	note = {}}},
	publisher = {McGraw-Hill},
	sortname = {\textmd{\textup{Durrell, Martin},
	title = {} }\textmd{Hammer’s {German} grammar and usage}\textmd{\textup{},
	year = {2002}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Dürscheid2002,
	address = {\textit{Zeitschrift für Sprachwissenschaft} 21 \REF{ex:key:1}},
	author = {Dürscheid, Christa},
	publisher = {57-81},
	sortname = {Durscheid, Christa},
	title = {“Polemik satt und Wahlkampf pur” – Das postnominale Adjektiv im   {Deutschen}},
	year = {2002}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{EbertEbert1993,
	address = {\textit{Frühneuhochdeutsche Grammatik.} Tübingen},
	author = {Ebert, Robert Peter, Oskar Reichmann, Hans-Joachim Solms, and Klaus-Peter Wegera},
	publisher = {Max Niemeyer Verlag},
	year = {1993}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Eichhoff1993,
	address = {\textit{Monatshefte} 85 \REF{ex:key:1}},
	author = {Eichhoff, Jürgen},
	publisher = {71-80},
	sortname = {Eichhoff, Jurgen},
	title = {“Ich bin ein {Berliner}”: {{A}} History and a Linguistic Clarification},
	year = {1993}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Eisenberg1998,
	address = {Band 1: Das Wort}. Stuttgart},
	author = {Eisenberg, Peter},
	publisher = {Metzler},
	title = {\textit{Grundriß der deutschen {Grammatik}},
	year = {1998}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Eisenberg1999,
	address = {\textit{Band 2: Der Satz}. Stuttgart},
	author = {Eisenberg, Peter},
	publisher = {Metzler},
	title = {\textit{Grundriß der deutschen {Grammatik}}},
	year = {1999}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{EisenbergEisenberg2002,
	address = {Frankfurt},
	author = {Eisenberg, Peter and George Smith},
	booktitle = {\textit{{Grammatik} und {Grammatik}vermittlung}},
	editor = { Corinna Peschel},
	pages = {113--126},
	publisher = {Lang},
	title = {Der einfache {Genitiv}. {{{E}}}igennamen als Attribute},
	year = {2002}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{ElmentalerRosenberg2015,
	address = {Band 1:   Regionale Sprachlagen}. Hildesheim},
	author = {Elmentaler, Michael, and Peter Rosenberg},
	publisher = {Olms},
	title = {\textit{Norddeutscher {Sprachatlas} ({NOSA})},
	year = {2015}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{EmbickEmbick2007,
	address = {Oxford},
	author = {Embick, David and Rolf Noyer},
	booktitle = {\textit{{Oxford} Handbook of Linguistic   Interfaces}},
	editor = {Gillian Ramchand and Charles Reiss},
	pages = {289--324},
	publisher = {Oxford University Press},
	title = {Distributed Morphology and the Syntax – Morphology   Interface},
	year = {2007}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Emonds1987,
	author = {Emonds, Joseph E.},
	journal = {\textit{Linguistic Inquiry}},
	pages = {613--632},
	title = {The invisible Category Principle},
	volume = {18},
	year = {1987}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Esau1973,
	author = {Esau, Helmut},
	journal = {\textit{Folia Linguistica}},
	number = {1/2},
	pages = {136--45},
	title = {Form and Function of {German} Adjectival endings},
	volume = {6},
	year = {1973}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Evans2019,
	author = {Evans, Elliott},
	note = {Doctoral dissertation, Indiana University.},
	title = {The Origin, Functions, and Histories of {Germanic} Adjective Endings},
	year = {2019}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Evans2021,
	address = {\textit{Studia Linguistica} 75   \REF{ex:key:3}},
	author = {Evans, Elliott},
	publisher = {511-537},
	title = {Prenominal {French} and Uninflected {Dutch} Adjective},
	year = {2021}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Eynde2020,
	author = {Eynde, Frank van},
	journal = {DP debate. \textit{Glossa: A Journal   of General Linguistics} 5(1):},
	pages = {1--23},
	title = {Agreement, disagreement and the {NP} vs},
	volume = {65},
	year = {2020}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Fanselow1988,
	author = {Fanselow, Gisbert},
	journal = {\textit{Linguistische Berichte}},
	pages = {91--113},
	title = {Aufspaltung von {NP}n und das Problem der “freien” {Wortstellung}},
	volume = {114},
	year = {1988}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleref}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{FanselowĆavar2002,
	address = {In \textit{Theoretical Approaches to   Universals}, ed. A. Alexiadou, 65–107. Amsterdam},
	author = {Fanselow, Gisbert, and Damir Ćavar},
	publisher = {John Benjamins},
	sortname = {Fanselow, Gisbert, and Damir Cavar},
	title = {Distributed deletion},
	year = {2002}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleref}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{\textmd{\textup{Felix1990,
	address = {The Structure of functional categories.} }\textmd{Linguistische Berichte}\textmd{ \textup{125},
	author = {\textmd{\textup{Felix, Sascha W.},
	note = {}}},
	publisher = {46-  71},
	sortname = {\textmd{\textup{Felix, Sascha W.},
	year = {1990}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{FintelFintel2007,
	address = {\textit{Linguistic Inquiry}   38 \REF{ex:key:3}},
	author = {Fintel, Kai von and Sabine Iatridou},
	publisher = {445-483},
	title = {Anatomy of a Modal Construction},
	year = {2007}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Fischer, Silke. 2006. Zur Morphologie der deutschen Personalpronomina - eine   Spaltungsanalyse. In Gereon Müller \& Jochen Trommer (eds.) \textit{Subanalysis of Argument   Encoding in Distributed Morphology}. [Linguistische Arbeitsberichte 84], 77-101.   Universität Leipzig.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Fiva1985,
	address = {\textit{Nordic Journal of Linguistics} 8 \REF{ex:key:1}},
	author = {Fiva, Toril},
	publisher = {25-47},
	title = {{NP}-Internal Chains in {Norwegian}},
	year = {1985}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{FodorFodor1982,
	author = {Fodor, Janet Dean and Ivan A. Sag},
	journal = {\textit{Linguistics   and Philosophy}},
	pages = {335--398},
	title = {Referential and Quantificational Indefinites},
	volume = {5},
	year = {1982}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Fortmann1996,
	address = {Zur funktionalen   Analyse der Nominalphrase im Deutschen}. Linguistische Arbeiten 347. Tübingen},
	author = {Fortmann, Christian},
	publisher = {Niemeyer},
	title = {\textit{Konstituentenbewegung in der {DP}-{Struktur}},
	year = {1996}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Fuhrhop2003,
	author = {Fuhrhop, Nanna},
	journal = {\textit{Linguistische Berichte}},
	pages = {91--108},
	title = {Zur {Grammatik} der Stadtadjektive},
	volume = {193},
	year = {2003}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Fuß2011,
	author = {Fuß, Eric},
	journal = {\textit{Linguistische Berichte}},
	pages = {19--42},
	sortname = {Fuss, Eric},
	title = {{Eigennamen} und adnominaler {Genitiv} im {Deutschen}},
	volume = {225},
	year = {2011}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Gallmann1990,
	address = {Tübingen},
	author = {Gallmann, Peter},
	publisher = {Niemeyer},
	title = {\textit{Kategoriell komplexe Wortformen}},
	year = {1990}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Gallmann1996,
	address = {\textit{Linguistische Berichte} 164},
	author = {Gallmann, Peter},
	publisher = {283-  314},
	title = {Die Steuerung der Flexion in der {DP}},
	year = {1996}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Gallmann1998,
	address = {Amsterdam},
	author = {Gallmann, Peter},
	booktitle = {\textit{Possessors, Predicates and Movement in the   Determiner Phrase}},
	editor = { Artemis Alexiadou and Chris Wilder},
	pages = {141--175},
	publisher = {John Benjamins Publishing Company},
	title = {Case Underspecification in Morphology, Syntax and the Lexicon},
	year = {1998}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Gallmann, Peter. 2004. Feature Sharing in DPs. In Gereon Müller, Lutz Gunkel, and Gisela   Zifonun (eds.) \textit{Explorations in Nominal Inflection}. Berlin: Mouton de Gruyter. pp. 121-  160.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Gallmann2018,
	address = {Amsterdam},
	author = {Gallmann, Peter},
	booktitle = {\textit{{Germanic} Genitives}},
	editor = {Tanja Ackermann, Horst J.  Simon and Christian Zimmer},
	pages = {149--188},
	publisher = {John   Benjamins},
	title = {The genitive rule and its background},
	year = {2018}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{GallmannLindauer1994,
	author = {Gallmann, Peter, and Thomas Lindauer},
	journal = {\textit{Beiträge zur Geschichte der Deutschen Sprache und Literatur} (PBB)},
	number = {1},
	pages = {1--27},
	title = {Funktionale Kategorien in Nominalphrasen},
	volume = {116},
	year = {1994}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Gelderen2007,
	author = {Gelderen, Elly van},
	journal = {\textit{Journal of Germanic Linguistics}},
	number = {4},
	pages = {275--308},
	title = {The Definiteness Cycle in {Germanic}},
	volume = {19},
	year = {2007}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{GeorgiGeorgi2011,
	author = {Georgi, Doreen, and Salzmann, Martin},
	journal = {\textit{Lingua}},
	number = {14},
	pages = {2069--2088},
	title = {{DP}-internal double agreement is not double Agree: {{C}}onsequences of Agree-based case assignment within {DP}},
	volume = {121},
	year = {2011}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Giusti1997,
	address = {London and New York},
	author = {Giusti, Giuliana},
	booktitle = {\textit{The New Comparative Syntax}},
	editor = {Liliane Haegeman},
	pages = {95--123},
	publisher = {Longman},
	title = {The categorial status of determiners},
	year = {1997}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\section{\textmd{Giusti, Giuliana. 2002. The Functional Structure of Noun Phrases. A Bare Phrase Structure   Approach. In Guglielmo Cinque (ed.)} \textmd{\textit{Functional Structure in DP and IP: The   Cartography of Syntactic Structures}}\textmd{. vol.1, pp. 54-90. Oxford: Oxford University Press.}}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Giusti, Giuliana. 2015. \textit{Nominal Syntax at the Interfaces: A Comparative Analysis of}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\textit{Languages with Articles}. Newcastle: Cambridge Scholars Publishing.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{GiustiIovino2016,
	author = {Giusti, Giuliana, and Rossella Iovino},
	journal = {\textit{Studia   Linguistica}},
	note = {~},
	number = {3},
	pages = {221--249},
	title = {~{Latin} as a Split-{DP} Language},
	volume = {70},
	year = {2016}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Glaser1993,
	address = {Opladen},
	author = {Glaser, Elvria},
	booktitle = {  \textit{{Dialektsyntax}}},
	editor = {Werner Abraham und Josef Bayer},
	pages = {99--116},
	publisher = {Westdeutscher Verlag},
	title = {Syntaktische Strategien zum Ausdruck von Indefinitheit und Partitivität im   {Deutschen} ({Standardsprache} und {Dialekt})},
	year = {1993}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Greenberg1978,
	address = {Stanford},
	author = {Greenberg, Joseph},
	booktitle = {\textit{Universals of human language}},
	editor = {Joseph Greenberg  },
	pages = {47--82},
	publisher = {Stanford University      Press},
	title = {How does a language acquire gender markers},
	volume = {3},
	year = {1978}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Grice1975,
	address = {New York},
	author = {Grice, H. Paul},
	booktitle = {\textit{Syntax and   Semantics. {{V}}ol. 3: {{S}}peech Acts}},
	editor = {Peter Cole and Jerry Morgan},
	pages = {43--58},
	publisher = {Academic Press},
	title = {Logic and conversation},
	year = {1975}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Grice1978,
	address = {New York},
	author = {Grice, H. Paul},
	booktitle = {\textit{Syntax and   Semantics. {{V}}ol. 9: {{P}}ragmatics}},
	editor = {Peter Cole},
	pages = {113--28},
	publisher = {Academic Press},
	title = {Further notes on logic and conversation},
	year = {1978}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleJBReferenceList}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{GriesbachGriesbach1965,
	address = {(3\textsuperscript{rd} ed.) München},
	author = {Griesbach, Heinz and Dora Schulz},
	publisher = {Max Hueber Verlag},
	title = {\textit{{Grammatik} der deutschen {Sprache}}},
	year = {1965}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleJBReferenceList}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Grimm1870,
	author = {Grimm, Jacob},
	note = {Vol 1. (2\textsuperscript{nd} ed). Berlin.},
	title = {\textit{{Deutsche} {Grammatik}}},
	year = {1870}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Grimm2012,
	author = {Grimm, Scott},
	note = {} Doctoral Dissertation,\textbf{ }Stanford University.},
	title = {\textit{Number and Individuation},
	year = {2012}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleJBReferenceList}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Grimshaw1991,
	author = {Grimshaw, Jane},
	note = {Ms., Brandeis University.},
	title = {Extended Projections},
	year = {1991}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleJBReferenceList}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{GrohmannGrohmann2003,
	author = {Grohmann, Kleanthes K. and Liliane Haegeman},
	journal = {\textit{Proceedings of the   19th Scandinavian Conference on Linguistics. Nordlyd}},
	number = {1},
	pages = {46--62},
	title = {\href{http://www. {{P}}unksinscience. {{O}}rg/kleanthes/papers/scl19_gh. {{P}}df}{{Resuming Reflexives}}},
	volume = {31},
	year = {2003}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{GunkelEtAl2017,
	address = {Das Nominal (Volume 2). [Schriften des Instituts für   Deutsche Sprache 14]. Berlin, Boston},
	author = {Gunkel, Lutz, Adriano Murelli, Susan Schlotthauer, Bernd Wiese, and Gisela Zifonun with  collaboration by Christine Günther and Ursula Hoberg},
	publisher = {De Gruyter},
	sortname = {Gunkel, Lutz, Adriano Murelli, Susan Schlotthauer, Bernd Wiese, and Gisela Zifonun with  collaboration by Christine Gunther and Ursula Hoberg},
	title = {{Grammatik} des {Deutschen}   im europäischen {Vergleich}},
	year = {2017}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Hachem2015,
	address = {Utrecht},
	author = {Hachem, Mirjam},
	publisher = {LOT},
	title = {\textit{Multifunctionality: {{T}}he Internal and External Syntax of D-and W-Items   in {German} and {Dutch}}},
	year = {2015}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{HaegemanHaegeman2010,
	address = {\textit{Linguistic Inquiry} 41 \REF{ex:key:2}},
	author = {Haegeman, Liliane and Terje Lohndal},
	publisher = {181-211},
	title = {Negative Concord and (Multiple) Agree: {{A}} Case   Study of {West} {Flemish}},
	year = {2010}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Haider1988,
	address = {\textit{Zeitschrift für Sprachwissenschaft} 7 \REF{ex:key:1}},
	author = {Haider, Hubert},
	publisher = {32-59},
	title = {Die Structure der deutschen {NP}},
	year = {1988}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Haider1992,
	address = {Berlin},
	author = {Haider, Hubert},
	booktitle = {\textit{{Deutsche} Syntax. {{A}}nsichten und Aussichten. {{I}}nstitut für deutsche {Sprache} \citealt{Jahrbuch1991}}},
	editor = {Ludger Hoffmann},
	pages = {304--333},
	publisher = {Walter de Gruyter},
	title = {Die {Struktur} der Nominalphrase. {{L}}exikalische und funktionale {Struktur}en},
	year = {1992}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Haider1993,
	address = {Tübingen},
	author = {Haider, Hubert},
	publisher = {Narr},
	title = {\textit{{Deutsche} Syntax – Generativ}},
	year = {1993}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Halle1997,
	author = {Halle, Morris},
	journal = {\textit{MIT Working   Papers in Linguistics}},
	pages = {425--449},
	title = {Distributed Morphology: {{I}}mpoverishment and Fission},
	volume = {30},
	year = {1997}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{HalleHalle1993,
	address = {Cambridge},
	author = {Halle, Morris and Alec Marantz},
	booktitle = {\textit{The View from Building 20. {{{E}}}ssays in Honor of   Sylvain Bromberger}},
	editor = { Kenneth Hale and Samuel K. Keyser},
	pages = {111--176},
	publisher = {MIT Press},
	title = {Distributed Morphology and the Pieces of Inflection},
	year = {1993}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{HalleMarantz1994,
	author = {Halle, Morris, and Alec Marantz},
	journal = {\textit{MIT   Working Papers in Linguistics}},
	pages = {275--288},
	title = {Some key features of Distributed Morphology},
	volume = {21},
	year = {1994}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Hallman2004,
	author = {Hallman, Peter},
	journal = {\textit{Language}},
	number = {4},
	pages = {707--47},
	title = {{NP}-interpretation and the structure of predicates},
	volume = {80},
	year = {2004}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Harbert2007,
	address = {Cambridge},
	author = {Harbert, Wayne},
	publisher = {Cambridge University Press},
	title = {\textit{The {Germanic} Languages}},
	year = {2007}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{HarleyNoyer1999,
	author = {Harley, Heidi and Rolf Noyer},
	journal = {\textit{Glot International}},
	note = {.   \url{https://babel.ucsc.edu/~hank/mrg.readings/harley & noyer.pdf}},
	number = {4},
	pages = {3--9},
	title = {Distributed Morphology},
	volume = {4},
	year = {1999}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Harris1994,
	address = {In~\href{http://broca.mit.edu/mitwpl.web/WPLs.html}{{\textit{MITWPL}}}\textit{~21: Papers on phonology and morphology}, ed. Andrew Carnie and Heidi   Harley, 321-353.~Cambridge},
	author = {Harris, James},
	publisher = {MITWPL},
	title = {The syntax-phonology mapping in {Catalan} and {Spanish} clitics},
	year = {1994}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Haspelmath2019,
	author = {Haspelmath, Martin},
	journal = {\textit{Te Reo}},
	number = {1},
	pages = {93–115},
	title = {Indexing and flagging, and head and dependent marking},
	volume = {62},
	year = {2019}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{HaspelmathHaspelmath2010,
	address = {(2\textsuperscript{nd} ed.) London},
	author = {Haspelmath, Martin and Andrea D. Sims},
	publisher = {Hodder Education},
	title = {\textit{Understanding Morphology}},
	year = {2010}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Hawkins1978,
	address = {London},
	author = {Hawkins, John A.},
	publisher = {Croom Helm},
	title = {\textit{Definiteness and indefiniteness: {{A}} study in reference and   grammaticality prediction}},
	year = {1978}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Hawkins1994,
	address = {Cambridge},
	author = {Hawkins, John A.},
	publisher = {Cambridge University Press},
	title = {\textit{A performance theory of order and constituency}},
	year = {1994}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Hawkins2004,
	address = {Oxford},
	author = {Hawkins, John A.},
	publisher = {Oxford University   Press},
	title = {\textit{Efficiency and Complexity in Grammars}},
	year = {2004}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Hazout2004,
	author = {Hazout, Ilan},
	journal = {\textit{Linguistic Inquiry}},
	number = {3},
	pages = {393--430},
	title = {The Syntax of Existential Constructions},
	volume = {35},
	year = {2004}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{HeckHeck2008,
	address = {Somerville, MA},
	author = {Heck, Fabian, Gereon Müller and Jochen Trommer},
	booktitle = {  \textit{Proceedings of {WCCFL} 26}},
	editor = {Charles B. Chang and Hannah J. Haynie},
	pages = {226--233},
	publisher = {Cascadilla Proceedings   Project},
	sortname = {Heck, Fabian, Gereon Muller and Jochen Trommer},
	title = {A phase-based approach to   {Scandinavian} definiteness marking},
	year = {2008}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{HeimHeim1998,
	address = {\textit{Semantics in Generative Grammar.} Cambridge, Mass.},
	author = {Heim, Irene and Angelika Kratzer},
	publisher = {Blackwell},
	year = {1998}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{HelbigHelbig2001,
	address = {Ein Handbuch für den   Ausländerunterricht}. Berlin und München},
	author = {Helbig, Gerhard and Joachim Buscha},
	publisher = {Langenscheidt},
	title = {\textit{{Deutsche} {Grammatik}},
	year = {2001}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Heusinger, Klaus von. 2011. Specificity, Referentiality and Discourse Prominence: German   Indefinite Demonstratives. In Ingo Reich (ed.) \textit{Proceedings of Sinn \& Bedeutung} 15, pp.   9-30.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{HeycockZamparelli2005,
	author = {Heycock, Caroline and Roberto Zamparelli},
	journal = {\textit{Natural Language Semantics}},
	pages = {201–270},
	title = {Friends and colleagues: {{P}}lurality,   coordination, and the structure of {DP}},
	volume = {13},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Higginbotham1987,
	address = {Cambridge},
	author = {Higginbotham, James},
	booktitle = {\textit{The Representation of (In)definiteness}},
	editor = {Eric J. Reuland and Alice G. B. ter  Meulen},
	pages = {43--70},
	publisher = {MIT Press},
	title = {Indefiniteness and Predication},
	year = {1987}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Hoekstra1984,
	address = {Dordrecht},
	author = {Hoekstra, Teun},
	publisher = {Foris},
	title = {\textit{Transitivity: {{G}}rammatical Relations in Government-Binding Theory}},
	year = {1984}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Höhn2020,
	author = {Höhn, Georg F. K.},
	journal = {\textit{Glossa: a   journal of general linguistics} 5(1):},
	pages = {1--43},
	sortname = {Hohn, Georg F. K.},
	title = {The third person gap in adnominal pronoun constructions},
	volume = {69},
	year = {2020}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{HoleHole2000,
	author = {Hole, Daniel and Gerson Klumpp},
	journal = {\textit{Linguistische Berichte}},
	pages = {231--244},
	title = {Definite type and indefinite token: {{T}}he article \textit{son} in   colloquial {German}},
	volume = {182},
	year = {2000}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Hoof2006,
	address = {Oxford},
	author = {Hoof, Hanneke van},
	booktitle = {  \textit{The {Blackwell} companion to syntax}, Vol. {{I}}V},
	editor = {Martin Everaert and Henk van Riemsdijk},
	pages = {410--465},
	publisher = {Blackwell},
	title = {Split Topicalization},
	year = {2006}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Hurford2003,
	address = {Berlin},
	author = {Hurford, James R.},
	booktitle = {\textit{Noun   Phrase Structure in the Languages of {Europe}} [Empirical Approaches to Language   Typology, {EUROTYP} 20-7]},
	editor = {Frans Plank},
	pages = {561--620},
	publisher = {Mouton de Gruyter},
	title = {The interaction between numerals and nouns},
	year = {2003}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{IhsaneIhsane2001,
	author = {Ihsane, Tabea and Genoveva Puskás},
	journal = {\textit{Generative Grammar in   Geneva}},
	pages = {39--54},
	sortname = {Ihsane, Tabea and Genoveva Puskas},
	title = {Specific is not definite},
	volume = {2},
	year = {2001}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Iordăchioaia2020,
	author = {Iordăchioaia, Gianina},
	journal = {\textit{Glossa: A Journal of General   Linguistics} 5(1):},
	pages = {1--25},
	sortname = {Iordachioaia, Gianina},
	title = {D and {{N}} are different nominalizers},
	volume = {53},
	year = {2020}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Jackendoff1977,
	address = {Cambridge},
	author = {Jackendoff, Ray S.},
	publisher = {MIT Press},
	title = {\textit{X’ Syntax: {{A}} Study of Phrase Structure}},
	year = {1977}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Jacobs.Joachim1980,
	author = {Jacobs. Joachim},
	journal = {\textit{Theoretical Linguistics}},
	pages = {121--136},
	title = {Lexical decomposition in Montague Grammar},
	volume = {7},
	year = {1980}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Jacobs2005,
	address = {A linguistic introduction}. Cambridge},
	author = {Jacobs, Neil G.},
	publisher = {Cambridge University   Press},
	title = {\textit{{Yiddish}},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{JägerJäger2012,
	address = {Amsterdam},
	author = {Jäger, Agnes, and Penka, Doris},
	booktitle = {\textit{Comparative {Germanic} Syntax: {{T}}he   State of the Art} [{Linguistik} Aktuell/Linguistics Today 191]},
	editor = {Peter Ackema, Rhona Alcorn, Caroline Heycock, Dany Jaspers, Jeroen van  Craenenbroeck, and Guido Vanden Wyngaerd},
	pages = {199--222},
	publisher = {John Benjamins},
	sortname = {Jager, Agnes, and Penka, Doris},
	title = {Development of sentential negation in the history of   {German}},
	year = {2012}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{JandaJanda1992,
	author = {Janda, Richard D. and Brian D. Joseph},
	journal = {In \textit{Proceedings from the 28th Regional Meeting of the   Chicago Linguistic Society}. Vol},
	pages = {251--266},
	title = {Pseudo-Agglutinativity in Modern {Greek} Verb-  Inflection and “Elsewhere”},
	volume = {1},
	year = {1992}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Julien2002,
	author = {Julien, Marit},
	journal = {\textit{Studia Linguistica}},
	pages = {264--315},
	title = {Determiners and Word Order in {Scandinavian} {DP}s},
	volume = {56},
	year = {2002}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Julien2005a,
	address = {Amsterdam},
	author = {Julien, Marit},
	publisher = {John   Benjamins},
	title = {\textit{Nominal Phrases from a {Scandinavian} Perspective}},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Julien2005b,
	address = {Amsterdam},
	author = {Julien, Marit},
	booktitle = {\textit{The Function of Function Words and Functional Categories}},
	editor = {Marcel den Dikken and Christina Tortora},
	pages = {217--49},
	publisher = {John Benjamins Publishing Company},
	title = {Possessor licensing, definiteness and case in {Scandinavian}},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Julien2016,
	author = {Julien, Marit},
	journal = {\textit{The Journal of   Comparative Germanic Linguistics}},
	pages = {75--109},
	title = {Possessive predicational vocatives in {Scandinavian}},
	volume = {19},
	year = {2016}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{KarnowskiPafel2004,
	address = {Berlin},
	author = {Karnowski, Paweł and Jürgen Pafel},
	booktitle = {\textit{Explorations in nominal   inflection}},
	editor = { Gereon Müller, Lutz Gunkel, and Gisela Zifonun},
	pages = {161--188},
	publisher = {Mouton de Gruyter},
	sortname = {Karnowski, Pawel and Jurgen Pafel},
	title = {A topological schema for noun phrases in {German}},
	year = {2004}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{KatzirSiloni2014,
	address = {Amsterdam},
	author = {Katzir, Roni, and Tal Siloni},
	booktitle = {\textit{Advances in the Syntax of {DP}s: {{S}}tructure, agreement, and case}. [{Linguistik} Aktuell/Linguistics Today 217]},
	editor = {Anna  Bondaruk, Gréte Dalmi, and Alexander Grosu},
	pages = {267--293},
	publisher = {John Benjamins},
	title = {Agreement and definiteness in {Germanic} {DP}s},
	year = {2014}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Keller2004,
	address = {Paper presented at \textit{Kongress des Bundesverbandes der Dolmetscher und Übersetzer e.V. 2000} [Conference of the Federal Union of Interpreters and Translators]. (available at},
	author = {Keller, Rudi},
	note = {phil-fak.uni-duesseldorf.de/uploads/media/Sprachwandel.pdf)},
	publisher = {https://www},
	title = {{Sprachwandel}: {{F}}aszination {Sprache} – Herausforderung Übersetzung},
	year = {2004}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Kester1996a,
	author = {Kester, Ellen-Petra},
	journal = {\textit{Journal of Linguistics}},
	pages = {57--78},
	title = {Adjectival inflection and the licensing of empty categories in {DP}},
	volume = {32},
	year = {1996}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Kester1996b,
	author = {Kester, Ellen-Petra},
	note = {Doctoral Dissertation, Utrecht University.},
	title = {The Nature of Adjectival Inflection},
	year = {1996}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Klein2007,
	address = {Amsterdam},
	author = {Klein, Thomas},
	booktitle = {  \textit{{Beiträge} zur Morphologie: {{G}}ermanisch, Baltisch, Ostseefinnisch}},
	editor = {Hans Fix},
	pages = {193--225},
	publisher = {John Benjamins Publishing Company},
	title = {Von der semantischen zur morphologischen Steuerung},
	year = {2007}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Klima1964,
	address = {Englewood   Cliffs, NJ},
	author = {Klima, Edward S.},
	booktitle = {\textit{The   structure of language: {{R}}eadings in the philosophy of language}},
	editor = {Jerry Fodor and Jerrold Katz},
	pages = {246--323},
	publisher = {Prentice Hall},
	title = {Negation in {English}},
	year = {1964}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Klockmann2020,
	author = {Klockmann, Heidi},
	journal = {\textit{Glossa: A Journal of General Linguistics} 5 \REF{ex:key:1}:},
	pages = {1--31},
	title = {The article \textit{a(n)} in {English} quantifying expressions: {{A}} default marker   of cardinality},
	volume = {85},
	year = {2020}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Kobele, Gregory M. \& Malte Zimmermann. 2012. Quantification in German. In E.L. Keenan \& D. Paperno (eds.) \textit{Handbook of Quantifiers in Natural Languages} [Studies in Linguistics and Philosophy 90], pp. 227-283. Springer.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Kratzer1995,
	address = {Chicago},
	author = {Kratzer, Angelika},
	booktitle = {\textit{The Generic {Book}}},
	editor = {Gregory N. Carlson and Francis Jeffry Pelletier},
	pages = {125--175},
	publisher = {The University of Chicago Press},
	title = {Stage-level and Individual-level Predicates},
	year = {1995}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Krause1999,
	author = {Krause, Cornelia},
	journal = {\textit{MIT Working Papers in Linguistics}},
	pages = {191--217},
	title = {Two Notes on Prenominal Possessors in {German}},
	volume = {33},
	year = {1999}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Krifka, Manfred. 2009. Case syncretism in German feminines: Typological, functional and   structural aspects. \textstyledisplayname{Patrick O. Steinkrüger}\textstylecontributor{~}\textstylemetadataandcontributorsfont{and~}\textstyledisplayname{Manfred \textit{Krifka}} (eds.) \textit{On Inflection}, pp.   141-171. Berlin: de Gruyter Mouton.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Krischke2012,
	author = {Krischke, Wolfgang},
	journal = {\textit{Linguistik online}},
	pages = {55–84},
	title = {Des Menschens {Genitiv}e: {{N}}ormabweichende {Genitiv}-        Varianten bei schwachen Maskulina},
	volume = {53},
	year = {2012}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Kupferman1991,
	author = {Kupferman, Lucien},
	journal = {\textit{Langages}},
	pages = {52--75},
	title = {Structure événementielle de l’alternance un/Ø devant les noms   humains attributs},
	volume = {101},
	year = {1991}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Lasnik2000,
	address = {Contemporary Lectures on Classic Transformational Theory.} Cambridge, Mass.},
	author = {Lasnik, Howard},
	publisher = {MIT Press},
	title = {\textit{Syntactic Structures} revisited\textit{},
	year = {2000}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{LasnikLasnik2005,
	address = {Foundations and Prospects}. Malden, Mass.},
	author = {Lasnik, Howard and Juan Uriagereka with Cedric Boeckx},
	publisher = {Blackwell},
	title = {\textit{A course in minimalist syntax},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Lawrenz1993,
	address = {Begriffsbestimmung und syntaktischer Status. Tübingen},
	author = {Lawrenz, Birgit},
	publisher = {Gunter Narr Verlag},
	title = {Apposition},
	year = {1993}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Leiss1994,
	author = {Leiss, Elisabeth},
	journal = {Kristische Anmerkungen zur Sexualisierung von    Grammatik. \textit{Linguistische Berichte}},
	pages = {281--300},
	title = {Genus und Sexus},
	volume = {152},
	year = {1994}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Leiss1997,
	address = {Heidelberg},
	author = {Leiss, Elisabeth},
	booktitle = {   \textit{Grammatica ianua artium. {{F}}estschrift für Rolf Bergmann}},
	editor = {Elvira Glaser and Michael Schäfer},
	pages = {33--48},
	publisher = {Winter},
	title = {Genus im {Althochdeutschen}},
	year = {1997}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{LekakouSzendrői2007,
	author = {Lekakou, Marika, and Kriszta Szendrői},
	note = {Eliding the Noun in Close Apposition, or},
	year = {2007}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Greek polydefinites Revisited. \textit{University College London Working Papers in Linguistics} 19: 129-154.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Lekakou, Marika, and Kriszta Szendrői. 2012. Polydefinites in Greek: Ellipsis, close
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

apposition and expletive determiners. \textit{Journal of Linguistics} 48(1): 107-149.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Leu2005,
	address = {\textit{UPenn Working Papers in Linguistics} 11   \REF{ex:key:1}},
	author = {Leu, Thomas},
	publisher = {143-55},
	title = {Something invisible in {English}},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Leu2007,
	address = {\textit{U. Penn Working Papers in Linguistics} 13 \REF{ex:key:1}},
	author = {Leu, Thomas},
	publisher = {141-154},
	title = {These {HERE} demonstratives},
	year = {2007}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{\textmd{\textup{Leu2008a,
	author = {\textmd{\textup{Leu, Thomas},
	note = {Doctoral Dissertation, New York   University.}}},
	title = {The Internal Syntax of Determiners},
	year = {2008}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Leu2008b,
	author = {Leu, Thomas},
	journal = {Syntax},
	number = {1},
	pages = {1--25},
	title = {\textit{What for} Internally},
	volume = {11},
	year = {2008}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Leu2015,
	address = {New York},
	author = {Leu, Thomas},
	publisher = {Oxford University Press},
	title = {\textit{The Architecture of Determiners}},
	year = {2015}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Lieber1988,
	author = {Lieber, Rochelle},
	journal = {Papers from the Parasession on Agreement in Grammatical Theory. \textit{Chicago Linguistic   Society}},
	pages = {202--22},
	title = {Phrasal Compounds in {English} and the Morphology-Syntax Interface},
	volume = {24},
	year = {1988}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Lindauer1995,
	address = {Eine morphologische Untersuchung zum deutschen DP/NP-System}. Tübingen},
	author = {Lindauer, Thomas},
	publisher = {Max Niemeyer Verlag},
	title = {\textit{Genitivattribute},
	year = {1995}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Link, Godehard. 1998. Ten years of research on plurals – where do we stand? In Fritz Hamm \& Erhard Hinrichs (eds.) \textit{Plurality and quantification.} [Studies in Linguistics and   Philosophy 69], pp. 19-54. Dordrecht: Kluwer.\\
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Lobeck1995,
	address = {Oxford},
	author = {Lobeck, Anne},
	publisher = {Oxford   University Press},
	title = {Ellipsis: {{F}}unctional heads, licensing, and identification},
	year = {1995}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Löbel1989,
	address = {Amsterdam},
	author = {Löbel, Elisabeth},
	booktitle = {\textit{Syntactic Phrase Structure Phenomena in Noun Phrases and Sentences},   pp},
	editor = {Christa Bhatt, Elisabeth Löbel and Claudia  Schmidt},
	pages = {133--58},
	publisher = {John Benjamins Publishing Company},
	sortname = {Lobel, Elisabeth},
	title = {Q as a Functional Category},
	year = {1989}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Löbel1990a,
	author = {Löbel, Elisabeth},
	journal = {\textit{Linguistische    Berichte}},
	pages = {232--264},
	sortname = {Lobel, Elisabeth},
	title = {D and {{Q}} als funtionale Kategorien in der Nominalphrase},
	volume = {127},
	year = {1990}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Löbel1990b,
	author = {Löbel, Elisabeth},
	journal = {\textit{Zeitschrift für Sprachwissenschaft}},
	pages = {135--169},
	sortname = {Lobel, Elisabeth},
	title = {Typologische {Aspekte} funktionaler Kategorien in der Nominalphrase},
	volume = {9},
	year = {1990}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Löbel1991,
	address = {Tübingen},
	author = {Löbel, Elisabeth},
	booktitle = {\textit{{Strukturen} und {Merkmale} syntaktischer Kategorien}},
	editor = {Gisbert Fanselow and Sascha  Felix},
	pages = {1--32},
	publisher = {Gunter Narr Verlag},
	sortname = {Lobel, Elisabeth},
	title = {Apposition und das Problem der Kasuszuweisung und   Adjazenzbedingung in der Nominalphrase des {Deutschen}},
	year = {1991}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Lockwood1968,
	address = {\textit{Historical German Syntax.} Oxford},
	author = {Lockwood, W. B.},
	publisher = {Clarendon},
	year = {1968}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Lockwood1995,
	address = {Hamburg},
	author = {Lockwood, W. B.},
	publisher = {Helmut Buske   Verlag},
	title = {\textit{Lehrbuch der modernen jiddischen {Sprache}}},
	year = {1995}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Lohrmann2010,
	author = {Lohrmann, Susanne},
	note = {Ph.D.   dissertation, University of Stuttgart.},
	title = {The Structure of the {DP} and its Reflex in {Scandinavian}},
	year = {2010}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Lohrmann2011,
	address = {Amsterdam},
	author = {Lohrmann, Susanne},
	booktitle = {\textit{The Noun Phrase in {Romance} and {Germanic}: {{S}}tructure, variation, and   change}},
	editor = {Petra Sleeman and Harry  Perridon},
	pages = {111--125},
	publisher = {John Benjamins},
	title = {A unified structure for {Scandinavian} {DP}s},
	year = {2011}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Longobardi1994,
	address = {\textit{Linguistic Inquiry} 25 \REF{ex:key:4}},
	author = {Longobardi, Giuseppe},
	publisher = {609-665},
	title = {Reference and Proper Names: {{A}} Theory of N-Movement in Syntax   and Logical Form},
	year = {1994}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\textmd{\textup{Longobardi, Giuseppe. 2001. The Structure of DPs: Some Principles, Parameters, and   Problems. In Mark Baltin \& Chris Collins (eds.)} }\textmd{The Handbook of Contemporary   Syntactic Theory}\textmd{\textup{, pp. 562-603. Malden, MA: Blackwell Publishing Ltd.}}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Lowe2016,
	author = {Lowe, John J.},
	journal = {\textit{Natural Language \& Linguistic   Theory}},
	pages = {157--195},
	title = {{English} possessive ’\textit{s}: {{C}}litic \textit{and} affix},
	volume = {34},
	year = {2016}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Lühr1991,
	author = {Lühr, Rosemarie},
	journal = {Zur Flexion   von \textit{der}, \textit{die}, \textit{das} als Demonstrativpronomen, Relativpronomen und Artikel. \textit{Beiträge zur   Geschichte der deutschen Sprache und Literatur}},
	number = {2},
	pages = {195--211},
	sortname = {Luhr, Rosemarie},
	title = {Die deutsche Determinansphrase aus historischer {Sicht}},
	volume = {113},
	year = {1991}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Lyons1999,
	address = {Cambridge},
	author = {Lyons, Christopher},
	publisher = {Cambridge University Press},
	title = {\textit{Definiteness}},
	year = {1999}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{MalingMaling1995,
	address = {Dordrecht},
	author = {Maling, Joan and Rex A. Sprouse},
	booktitle = {\textit{Studies in   Comparative {Germanic} Syntax}},
	editor = {Hubert Haider, Susan Olsen and Sten Vikner},
	pages = {167--186},
	publisher = {Kluwer},
	title = {Structural Case, specifier-head relations, and the case of   predicate {NP}s},
	year = {1995}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Marantz, Alec. 1997. No Escape from Syntax: Don’t Try Morphological Analysis in the Privacy   of Your Own Lexicon. In A. Dimitriadis et al (eds.) Proceedings of the 21\textsuperscript{st} Penn   Linguistics Colloquium. \textit{UPenn Working Papers in Linguistics} 4(2), pp. 201-225.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Matushansky, Ora. 2002. A Beauty of a Construction. In Line Mikkelsen \& Christopher Potts   (eds.) \textit{Proceedings of the 21\textsuperscript{st}} \textit{West Coast Conference on Formal Linguistics}, pp. 264-  277. Somerville, MA: Cascadilla.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Matushanksy2006,
	address = {\textit{Linguistic Inquiry} 37(1)},
	author = {Matushanksy, Ora},
	publisher = {69-  109},
	title = {Head Movement in Linguistic Theory},
	year = {2006}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Matushansky2008,
	author = {Matushansky, Ora},
	journal = {\textit{Linguistics and   Philosophy}},
	pages = {573–627},
	title = {On the linguistic complexity of proper names},
	volume = {21},
	year = {2008}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{MatushanskySpector2005,
	address = {Nijmegen},
	author = {Matushansky, Ora, and Benjamin Spector},
	booktitle = {\textit{Proceedings of Sinn \& {Bedeutung} 9}},
	editor = {Emar Maier,  Corien Bary and Janneke Huitink},
	pages = {241--255},
	publisher = {NCS},
	title = {Tinker, tailor, soldier, spy},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Merchant1996,
	author = {Merchant, Jason},
	journal = {\textit{NELS}},
	pages = {179--193},
	title = {Object scrambling and quantifier float in {German}},
	volume = {26},
	year = {1996}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{MilnerMilner1972,
	author = {Milner, Jean-Claude, and Judith Milner},
	note = {DRLAV 2. Université de Paris VIII.},
	title = {\textit{{La} morphologie du groupe nominal en allemand}},
	year = {1972}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Milsark1974,
	author = {Milsark, Gary},
	note = {Doctoral Dissertation, MIT.},
	title = {Existential Sentences in {English}},
	year = {1974}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Müller, Gereon. 2002a. Remarks on nominal inflection in German. In Ingrid Kaufmann \& Barbara Stiebels (eds.) \textit{More than Words. A Festschrift for Dieter Wunderlich}. Berlin:   Akademie Verlag. pp. 113-145.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Müller2002b,
	author = {Müller, Gereon},
	journal = {\textit{Deutsche   Sprache}},
	number = {4},
	pages = {328--365},
	sortname = {Muller, Gereon},
	title = {Zwei {Theorien} der pronominalen Flexion im {Deutschen}},
	volume = {30},
	year = {2002}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\section{\textmd{Müller, Michael. 1986. Zur Verbindbarkeit von Determinantien und Quantoren. In Heinz Vater   (ed.)} \textmd{\textit{Zur Syntax der Determinantien}}\textmd{, pp. 33-55. Tübingen: Gunter Narr Verlag.} }
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{MunnMunn2005,
	author = {Munn, Alan and Cristina Schmitt},
	journal = {\textit{Lingua}},
	pages = {821--855},
	title = {Number and indefinites},
	volume = {115},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Muñoz2019,
	author = {Muñoz, Patrick},
	journal = {\textit{Semantics and   Pragmatics}},
	number = {6},
	pages = {1–32},
	sortname = {Munoz, Patrick},
	title = {The proprial article and the semantics of names},
	volume = {12},
	year = {2019}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Murphy2018,
	author = {Murphy, Andrew},
	journal = {\textit{The Journal of   Comparative Germanic Linguistics}},
	number = {3},
	pages = {327--379},
	title = {Pronominal inflection and {NP} ellipsis in {German}},
	volume = {21},
	year = {2018}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{NeelemanKoot2006,
	address = {Oxford},
	author = {Neeleman, Ad and Hans van de Koot},
	booktitle = {\textit{The {Blackwell} Companion to Syntax}, Vol {IV}},
	editor = {Martin Everaert and Henk  van Riemsdijk},
	pages = {685--710},
	publisher = {Blackwell Publishers},
	title = {Syntactic Haplology},
	year = {2006}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{NeelemanNeeleman2004,
	author = {Neeleman, Ad, Hans van de Koot and Jenny Doetjes},
	journal = {\textit{The Linguistic     Review}},
	pages = {1--66},
	title = {Degree expressions},
	volume = {21},
	year = {2004}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Nevins2007,
	author = {Nevins, Andrew},
	journal = {\textit{Natural Language \& Linguistic Theory}},
	number = {2},
	pages = {273–313},
	title = {The representation of third person and its consequences for person-case   effects},
	volume = {25},
	year = {2007}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Norris2014,
	author = {Norris, Mark},
	note = {Doctoral Dissertation, University of   California, Santa Cruz.},
	title = {A Theory of Nominal Concord},
	year = {2014}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Nübling2011,
	address = {Berlin/New York},
	author = {Nübling, Damaris},
	booktitle = {\textit{{{Grammatik}} –   Lehren, Lernen, Verstehen: {{Z}}ugänge zur {{Grammatik}} des Gegenwartsdeutschen}},
	editor = {Köpcke, Klaus Michael, and Arne Ziegler},
	pages = {175--195},
	publisher = {de Gruyter},
	sortname = {Nubling, Damaris},
	title = {Unter großem persönlich\textit{em} oder persönlich\textit{en} Einsatz? {{D}}er sprachliche   Zweifelsfall adjektivischer Parallel- vs. {{W}}echselflexion als {Beispiel} für aktuellen   grammatischen {Wandel}},
	year = {2011}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{NüblingHeuser2015,
	address = {Eine Einführung in die   Onomastik}. (2nd ed.) Tübingen},
	author = {Nübling, Damaris, Fabian Fahlbusch, and Rita Heuser},
	publisher = {Narr},
	sortname = {Nubling, Damaris, Fabian Fahlbusch, and Rita Heuser},
	title = {\textit{{Namen}},
	year = {2015}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Nunes2001,
	address = {\textit{Linguistic Inquiry} 32 \REF{ex:key:2}},
	author = {Nunes, Jairo},
	publisher = {303-344},
	title = {Sideward Movement},
	year = {2001}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Nykiel2015,
	author = {Nykiel, Jerzy},
	journal = {\textit{Journal of Germanic Linguistics}},
	number = {2},
	pages = {105--144},
	title = {The reduced definite article \textit{th’} in late {Middle English} and beyond: {{A}}n   insight from the definiteness cycle},
	volume = {27},
	year = {2015}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{\textmd{\textup{Olsen1987,
	author = {\textmd{\textup{Olsen, Susan},
	journal = {Zum “substantivierten” Adjektiv im Deutschen: Deutsch als eine pro-Drop-Sprache.} }\textmd{Studium Linguistik}\textmd{ \textup{},
	note = {.}}},
	pages = {1--35},
	sortname = {\textmd{\textup{Olsen, Susan},
	volume = {21},
	year = {1987}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\textmd{\textup{Olsen, Susan. 1989a. AGR(eement) in the German Noun Phrase. In Christa Bhatt, Elisabeth Löbel \& Claudia Schmidt (eds.)} }\textmd{Syntactic Phrase Structure Phenomena in Noun Phrases and Sentences}\textmd{\textup{, pp. 39-49. Amsterdam: Benjamins.}}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{\textmd{\textup{Olsen1989b,
	author = {\textmd{\textup{Olsen, Susan},
	journal = {Das Possessivum: Pronomen, Determinans oder Adjektiv?} }\textmd{Linguistische Berichte}\textmd{ \textup{},
	note = {.}}},
	pages = {133--153},
	sortname = {\textmd{\textup{Olsen, Susan},
	volume = {120},
	year = {1989}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\textmd{\textup{Olsen, Susan. 1991a. AGR(eement) und Flexion in der deutschen Nominalphrase. In Gisbert Fanselow \& Sascha Felix (eds.)} }\textmd{Strukturen und Merkmale syntaktischer Kategorien}\textmd{\textup{, pp. 51-70. Tübingen: Gunter Narr Verlag.}}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\textmd{\textup{Olsen, Susan. 1991b. Die deutsche Nominalphrase as “Determinansphrase”. In Susan Olsen \& Gisbert Fanselow (eds.)} }\textmd{DET, COMP und INFL. Zur Syntax funktionaler Kategorien und grammatischer Funktionen}\textmd{\textup{, pp. 35-56. Tübingen: Niemeyer.}}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Olsvanger1947,
	address = {New York},
	booktitle = {\textit{Röyte pomerantsen} (Red oranges)},
	editor = {Olsvanger, Immanuel},
	publisher = {Schocken   Books},
	title = {\textit{Röyte pomerantsen} (Red oranges)},
	year = {1947}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Oomen1977,
	address = {Tübingen},
	author = {Oomen, Ingelore},
	publisher = {Niemeyer},
	title = {\textit{Determination bei generischen, definiten und indefiniten   Beschreibungen im {Deutschen}}},
	year = {1977}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Ott2011a,
	author = {Ott, Dennis},
	note = {The Syntax of Split Topics. Doctoral Dissertation, Harvard   University.},
	title = {Local Instability},
	year = {2011}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Ott2011b,
	address = {\textit{The Journal of Comparative Germanic   Linguistics} 14 \REF{ex:key:1}},
	author = {Ott, Dennis},
	publisher = {1-46},
	title = {Diminutive-formation in {German}},
	year = {2011}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{OttOtt2010,
	address = {Ms, Havard University. [available at},
	author = {Ott, Dennis and Andreea Nicolae},
	note = {auf.net/lingBuzz/001071}]},
	publisher = {\url{http://ling},
	title = {The Syntax and Semantics of Genus-species Splits in   {German}},
	year = {2010}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Pafel1994,
	author = {Pafel, Jürgen},
	journal = {\textit{Zeitschrift für   Sprachwissenschaft}},
	number = {2},
	pages = {236--275},
	sortname = {Pafel, Jurgen},
	title = {Zur syntaktischen {Struktur} nominaler Quantoren},
	volume = {13},
	year = {1994}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Pafel1995,
	address = {Amsterdam},
	author = {Pafel, Jürgen},
	booktitle = {  \textit{On Extraction and Extraposition in {German}}},
	editor = {Uli Lutz and Jürgen Pafel},
	pages = {145--177},
	publisher = {John Benjamins},
	sortname = {Pafel, Jurgen},
	title = {Kinds of Extraction from Noun Phrases},
	year = {1995}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Pafel2005,
	address = {[Linguistik Aktuell/Linguistic Today-Series   84]. Amsterdam},
	author = {Pafel, Jürgen},
	publisher = {John Benjamins Publishing Company},
	sortname = {Pafel, Jurgen},
	title = {\textit{Quantifier Scope in {German}}},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Panagiotidis2000,
	author = {Panagiotidis, Phoevos},
	journal = {\textit{Lingua}},
	pages = {717--742},
	title = {Demonstrative determiners and operators: {{T}}he case of {Greek}},
	volume = {110},
	year = {2000}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Panagiotidis2002,
	address = {‘Pronominality’ and   licensing in syntax}. Amsterdam},
	author = {Panagiotidis, Phoevos},
	publisher = {John Benjamins},
	title = {\textit{Pronouns, Clitics and Empty Nouns},
	year = {2002}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Panagiotidis2003,
	address = {\textit{Natural Language \& Linguistic Theory} 21(2)},
	author = {Panagiotidis, Phoevos},
	publisher = {381-  432},
	title = {Empty Nouns},
	year = {2003}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Partee1973,
	address = {New York},
	author = {Partee, Barbara H.},
	booktitle = {\textit{Montague Grammar}},
	editor = {Barbara  H. Partee},
	pages = {51--76},
	publisher = {Academic Press},
	title = {Some transformational extensions of Montague Grammar},
	year = {1973}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{PaulGrosse1989,
	address = {(23rd   ed.). Tübingen},
	author = {Paul, Hermann, Peter Wiehl, and Siegfried Grosse},
	publisher = {Max Niemeyer Verlag},
	title = {\textit{{Mittelhochdeutsche} {Grammatik}}},
	year = {1989}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Perlmutter1970,
	address = {The Hague},
	author = {Perlmutter, David M.},
	booktitle = {\textit{Progress in Linguistics. {{A}} Collection of Papers}},
	editor = {Manfred Bierwisch and Karl Erich Heidolph},
	pages = {233--248},
	publisher = {Mouton},
	title = {On the article in {English}},
	year = {1970}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{\textmd{\textup{Pesetsky1978,
	author = {\textmd{\textup{Pesetsky, David},
	journal = {Category Switching and So-called Pronouns.} }\textmd{Chicago Linguistic Society} \textmd{\textup{},
	note = {.}}},
	pages = {350--361},
	sortname = {\textmd{\textup{Pesetsky, David},
	volume = {14},
	year = {1978}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Pesetsky, David \& Esther Torrego. 2007. The syntax of valuation and the interpretability of   features. In Simin Karimi, Vida Samiian \& Wendy K. Wilkins (eds.) \textit{Phrasal and clausal   architecture: Syntactic derivation and interpretation. In honor of Joseph E. Emonds}, 262-  294. Amsterdam: John Benjamins Publishing Company.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Peter2013,
	author = {Peter, Klaus},
	journal = {\textit{Jahrbuch für Germanistische Sprachgeschichte}},
	number = {1},
	pages = {186--204},
	title = {Steuerungsfaktoren für Parallel- und Wechselflexion in Adjektivreihungen},
	volume = {4},
	year = {2013}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Petrova, Svetlana. 2024. On the distribution of the strong and weak adjectival inflection in Old   High German: A corpus investigation. In Kristin Bech \& Alexander Pfaff (Eds.), \textit{Noun   phrases in} \textit{early Germanic languages}, pp. 181 - 218. Berlin: Language Science Press.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Pétursson1992,
	address = {Mit Übungen und Lösungen}.   Hamburg},
	author = {Pétursson, Magnús},
	publisher = {Helmut Buske Verlag},
	sortname = {Petursson, Magnus},
	title = {\textit{Lehrbuch der Isländischen {Sprache}},
	year = {1992}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{\textmd{\textup{Pfaff2015,
	author = {\textmd{\textup{Pfaff, Alexander},
	note = {A Tale of Outsiders and Inside Jobs. Doctoral dissertation, University of   Tromsø.}}},
	title = {Adjectival and Genitival Modification in Definite Noun Phrases in    {Icelandic}},
	year = {2015}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\textmd{\textup{Pfaff, Alexander. 2017.~Adjectival inflection as diagnostic for structural~position: inside and   outside the Icelandic definiteness~domain.~}}\textmd{Journal of Comparative German Linguistics}\textmd{ \textup{20: 283–322.}}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Pfaff2020,
	author = {Pfaff, Alexander},
	journal = {How to become an adjective when you're not strong (enough)? \textit{Nordlyd}},
	number = {1},
	pages = {19--34},
	volume = {44},
	year = {2020}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Pike1963,
	author = {Pike, Kenneth L.},
	journal = {\textit{Anthropological Linguistics}},
	pages = {1--23},
	title = {Theoretical Implications of Matrix Permutation in Fore (New {Guinea})},
	volume = {5},
	year = {1963}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Pike1965,
	author = {Pike, Kenneth L.},
	journal = {\textit{Zeitschrift für Mundartforschung}},
	pages = {193--221},
	title = {Non-Linear Order and Anti-Redundancy in {German} Morphological   Matrices},
	volume = {31},
	year = {1965}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Plank2003,
	address = {Berlin},
	author = {Plank, Frans},
	booktitle = {\textit{Noun Phrase Structure in the   Languages of {Europe}} [Empirical Approaches to Language Typology, {EUROTYP} 20-7],   pp},
	editor = {Frans Plank},
	pages = {337--395},
	publisher = {Mouton de Gruyter},
	title = {Double Articulation},
	year = {2003}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\textmd{\textup{Postal, Paul M. 1966. On the so-called pronouns in English, in F. Dinneen (ed.)} }\textmd{Nineteenth   Monograph on Language and Linguistics}\textmd{\textup{, Georgetown University Press, Washington,   D.C., pp. 201-24.}}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Presslich2000,
	address = {Frankfurt a. Main},
	author = {Presslich, Marion},
	publisher = {Lang},
	title = {\textit{Partitivität und Indefinitheit: {{D}}ie {Entstehung} und {Entwicklung} des       indefiniten Artikels in den germanischen und romanischen {Sprachen} am {Beispiel} des       {Deutschen}, Niederländischen, Französischen und Italienischen}},
	year = {2000}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{\textmd{\textup{Prokosch1939,
	address = {Baltimore},
	author = {\textmd{\textup{Prokosch, Eduard},
	note = {} }},
	publisher = {The Linguistic Society   of America},
	sortname = {\textmd{\textup{Prokosch, Eduard},
	title = {} }\textmd{A Comparative {Germanic} Grammar}\textmd{\textup{},
	year = {1939}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Pysz2006,
	author = {Pysz, Agnieszka},
	journal = {\textit{Skase Journal of Theoretical Linguistics}},
	number = {3},
	pages = {59--85},
	title = {The structural location of adnominal adjectives: {{P}}rospects for Old   {English}},
	volume = {3},
	year = {2006}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{RankinRankin2016,
	address = {Wiederholen und Anwenden}. (6th ed.) Boston, MA},
	author = {Rankin, Jamie and Larry D. Wells},
	publisher = {Cengage Learning},
	title = {\textit{{Handbuch} zur deutschen {Grammatik}},
	year = {2016}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Rauh2003,
	author = {Rauh, Gisa},
	journal = {\textit{Linguistische Berichte}},
	pages = {389--424},
	title = {Warum wir Linguisten “euch Linguisten”, aber nicht “sie Linguisten”   akzeptieren können},
	volume = {196},
	year = {2003}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Rauh2004,
	author = {Rauh, Gisa},
	journal = {\textit{Linguistische Berichte}},
	pages = {77--105},
	title = {Warum “Linguist” in “ich/du Linguist” kein Schimpfwort sein muß},
	volume = {197},
	year = {2004}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{RauthSpeyer2021,
	address = {\textit{Glossa: a journal of general linguistics} 6(1},
	author = {Rauth, Philipp, and Augustin Speyer},
	note = {1-24.},
	publisher = {4)},
	title = {Adverbial reinforcers of demonstratives in dialectal   {German}},
	year = {2021}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Reershemius1997,
	address = {Grammatische und   diskursanalytische Untersuchungen}. Tübingen},
	author = {Reershemius, Gertrud},
	publisher = {Niemeyer},
	title = {\textit{Biographisches Erzählen auf {Jiddisch}},
	year = {1997}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Rehn2019,
	author = {Rehn, Alexandra},
	note = {} Doctoral dissertation,   University of Konstanz.},
	title = {\textit{Adjectives and the Syntax of {German} (ic) {DP}s},
	year = {2019}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Riemsdijk1989,
	address = {Dordrecht},
	author = {Riemsdijk, Henk van},
	booktitle = {\textit{Dialect Variation and the Theory of Grammar}},
	editor = {Paola Benincà},
	pages = {105--136},
	publisher = {Foris Publications},
	title = {Movement and Regeneration},
	year = {1989}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Riemsdijk1998a,
	author = {Riemsdijk, Henk van},
	journal = {\textit{Natural Language and Linguistic Theory}},
	pages = {633--678},
	title = {Head Movement and Adjacency},
	volume = {16},
	year = {1998}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Riemsdijk1998b,
	author = {Riemsdijk, Henk van},
	journal = {\textit{The Journal of Comparative Germanic Linguists}},
	pages = {1--48},
	title = {Categorial Feature Magnetism: {{T}}he endocentricity and distribution   of projections},
	volume = {2},
	year = {1998}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Riemsdijk2005,
	address = {Oslo},
	author = {Riemsdijk, Henk van},
	booktitle = {\textit{Grammar \& Beyond: {{{E}}}ssays in honor of Lars   Hellan}},
	editor = {Mila  Vulchanova and Tor A. Åfarli},
	pages = {163--178},
	publisher = {Novus Press},
	title = {Silent nouns and the spurious indefinite article in {Dutch}},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Ritter1991,
	address = {San Diego},
	author = {Ritter, Elizabeth},
	booktitle = {\textit{Perspectives on Phrase Structure: {{H}}eads and   Licensing}. [Syntax and Semantics 25]},
	editor = {Susan D. Rothstein},
	pages = {37--62},
	publisher = {Academic Press},
	title = {Two functional categories in noun phrases: {{{E}}}vidence from Modern   {Hebrew}},
	year = {1991}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{\textmd{\textup{Rizzi1986,
	address = {}\textmd{Linguistic Inquiry}\textmd{ \textup{17(3)},
	author = {\textmd{\textup{Rizzi, Luigi},
	note = {}}},
	publisher = {501-  557},
	sortname = {\textmd{\textup{Rizzi, Luigi},
	title = {Null Objects in {Italian} and the Theory of} }\textmd{pro}\textmd{},
	year = {1986}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Roehrs2002,
	author = {Roehrs, Dorian},
	journal = {\textit{IULC Working Papers Online}},
	pages = {1--46},
	title = {The Boundary between Morphology and Syntax: {{D}}eterminers \textit{{Move}} Into   the Determiner Phrase},
	volume = {2},
	year = {2002}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Roehrs2005,
	address = {Amsterdam},
	author = {Roehrs, Dorian},
	booktitle = {\textit{The Function of Function Words and Functional Categories}},
	editor = {Marcel den Dikken and Christina  Tortora},
	pages = {251--285},
	publisher = {John Benjamins},
	title = {Pronouns are Determiners After All},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Roehrs2006a,
	address = {The Morpho-Syntax of the Germanic Noun Phrase},
	author = {Roehrs, Dorian},
	note = {Doctoral Dissertation, Indiana University.},
	publisher = {Determiners MOVE   into the Determiner Phrase},
	year = {2006}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Roehrs2006b,
	address = {Amsterdam},
	author = {Roehrs, Dorian},
	booktitle = {\textit{Comparative Studies in {{German}ic} Syntax: {{F}}rom {Afrikaans} to Zurich {German}}},
	editor = { Jutta M. Hartmann and László Molnárfi},
	pages = {143--180},
	publisher = {John Benjamins},
	title = {Pronominal Noun Phrases, Number Specifications, and Null Nouns},
	year = {2006}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Roehrs2008,
	address = {\textit{The Journal of   Comparative Germanic Linguistics} 11 \REF{ex:key:1}},
	author = {Roehrs, Dorian},
	publisher = {1-42},
	title = {Something Inner- and Cross-linguistically Different},
	year = {2008}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Roehrs2009a,
	address = {Amsterdam},
	author = {Roehrs, Dorian},
	publisher = {John Benjamins},
	title = {\textit{Demonstratives and definite articles as nominal auxiliaries}},
	year = {2009}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Roehrs, Dorian. 2009b. Inflectional Parallelism with German Adjectives. \textit{Interdisciplinary   Journal for Germanic Linguistics and Semiotic Analysis} 14 \REF{ex:key:2}: 289-326
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Roehrs2010,
	address = {\textit{The Journal of Comparative   Germanic Linguistics} 13 \REF{ex:key:3}},
	author = {Roehrs, Dorian},
	publisher = {225-268},
	title = {Demonstrative-reinforcer Constructions},
	year = {2010}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Roehrs2011,
	address = {\textit{Groninger Arbeiten zur germanistischen Linguistik} 53 \REF{ex:key},
	author = {Roehrs, Dorian},
	note = {Jan-Wouter Zwart, 79-113.},
	publisher = {1},   edited by C},
	title = {Split {NP}s},
	year = {2011}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Roehrs2012,
	address = {\textit{Interdisciplinary Journal for Germanic Linguistics and Semiotic Analysis} 17 \REF{ex:key:1}},
	author = {Roehrs, Dorian},
	publisher = {1-56},
	title = {Complex Determiners: {{A}} Case Study of {German} \textit{ein jeder}},
	year = {2012}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Roehrs2013a,
	address = {\textit{Journal of Germanic Linguistics} 25 \REF{ex:key:4}},
	author = {Roehrs, Dorian},
	publisher = {295-411},
	title = {The Inner Makeup of Definite Determiners: {{T}}he Case of {Germanic}},
	year = {2013}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Roehrs2013b,
	author = {Roehrs, Dorian},
	note = {\textit{Working Papers in Scandinavian   Linguistics} 91, edited by Christer Platzack, 37-112.},
	title = {Possessives as Extended Projections},
	year = {2013}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Roehrs2015,
	address = {\textit{The Journal of Comparative Germanic Linguistics} 18   \REF{ex:key:3}},
	author = {Roehrs, Dorian},
	publisher = {213-271},
	title = {Inflections on Pre-nominal Adjectives in {Germanic}: {{M}}ain Types,   Subtypes, and Subset Relations},
	year = {2015}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Roehrs2019,
	address = {\textit{Journal of Germanic Linguistics} 31 \REF{ex:key:4}},
	author = {Roehrs, Dorian},
	publisher = {363-408},
	title = {Three Typological Differences between the {North} and the {West} {Germanic}   {DP}},
	year = {2019}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Roehrs2020a,
	address = {\textit{Studia Linguistica} 78   \REF{ex:key:1}},
	author = {Roehrs, Dorian},
	publisher = {98-138},
	title = {The Left Periphery of the {German} Noun Phrase},
	year = {2020}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Roehrs2020b,
	address = {Cambridge},
	author = {Roehrs, Dorian},
	booktitle = {\textit{The} \textit{{Cambridge} Handbook of {Germanic} Linguistics}},
	editor = {Michael T.  Putnam and B. Richard Page},
	pages = {537--563},
	publisher = {Cambridge University Press},
	title = {The Structure of Noun ({NP}) and Determiner Phrases ({DP})},
	year = {2020}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Roehrs2021,
	address = {Unpublished book manuscript, version 2 (available on Lingbuzz},
	author = {Roehrs, Dorian},
	note = {auf.net/lingbuzz/002457)},
	publisher = {https://ling},
	title = {Semantically Vacuous Elements in {German}: {{A}}djectival Inflections and the   Article \textit{ein}},
	year = {2021}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Roehrs2022,
	address = {\textit{mayner a khaver}.   \textit{Linguistics: An Interdisciplinary Journal of the Language Sciences} 60 \REF{ex:key},
	author = {Roehrs, Dorian},
	publisher = {1}},
	title = {Prenominal Possessives in {Yiddish}: \textit{mayn khaver} vs},
	year = {2022}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{RoehrsRoehrs2014,
	address = {Amsterdam},
	author = {Roehrs, Dorian and Marit Julien},
	booktitle = {  \textit{Adjectives in {Germanic} and {Romance}}. [{Linguistik} Aktuell/Linguistic Today-Series 212],   pp},
	editor = {Petra Sleeman, Freek Van de Velde, and Harry Perridon},
	pages = {245--261},
	publisher = {John Benjamins Publishing Company},
	title = {Adjectives in {German} and {Norwegian}: {{D}}ifferences in weak   and strong inflections},
	year = {2014}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Rubin1996,
	author = {Rubin, Edward J.},
	note = {\textit{Proceedings of the   15\textsuperscript{th}} \textit{WCCFL}, pp. 429-439.},
	title = {The Transparent Syntax and Semantics of Modifiers},
	year = {1996}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Russ1989,
	address = {A linguistic survey}. Stanford,   CA},
	booktitle = {\textit{The Dialects of Modern {German}},
	editor = {Russ, Charles V. J.},
	publisher = {Stanford University Press},
	title = {\textit{The Dialects of Modern {German}},
	year = {1989}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Sahel2021,
	author = {Sahel, Said},
	journal = {Wechselflexion im DAT SG MASC/NEUTR – Ein   Erklärungsansatz zur Persistenz der Variation. \textit{Zeitschrift für germanistische Linguistik}},
	number = {1},
	pages = {3--32},
	title = {Parallel- vs},
	volume = {49},
	year = {2021}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Salmons2021,
	address = {Edinburgh},
	author = {Salmons, Joseph},
	publisher = {Edinburgh University Press},
	title = {\textit{Sound Change}},
	year = {2021}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Salzmann2020,
	author = {Salzmann, Martin},
	journal = {DP debate. Why previous arguments are inconclusive and   what a good argument could look like. Evidence from agreement with hybrid nouns.   \textit{Glossa: a journal of general linguistics} 5(1)},
	pages = {1--46},
	title = {The {NP} vs},
	volume = {83},
	year = {2020}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Salzmann, Martin. 2022. The NP vs. DP-debate and notions of headedness. In Ulrike Freywald,   Horst J. Simon, and Stefan Müller (eds.) \textit{Headedness and/or grammatical anarchy?} 55-  71. Berlin: Language   Science Press.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{SappSapp2016,
	address = {\textit{Journal of   Germanic Linguistics} 28 \REF{ex:key:2}},
	author = {Sapp, Christopher and Dorian Roehrs},
	publisher = {89-166},
	title = {Head-to-Modifier Reanalysis: {{T}}he Rise of the   Adjectival Quantifier \textit{viel} and the Loss of Genitive Case Assignment},
	year = {2016}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Sauerland1996,
	author = {Sauerland, Uli},
	note = {Ms., MIT.},
	title = {The Late Insertion of {Germanic} Inflection},
	year = {1996}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Schirmunski2010,
	address = {Vergleichende Laut- und Formenlehre   der deutschen Mundarten} (original version from 1962 edited by Larissa Naiditsch).   Frankfurt am Main},
	author = {Schirmunski, Viktor. M.},
	publisher = {Peter Lang},
	title = {\textit{{Deutsche} {Mundartkunde}},
	year = {2010}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Schlenker1999,
	author = {Schlenker, Philippe},
	journal = {\textit{Recherches Linguistiques de Vincennes}},
	note = {.   [http://rlv.revues.org/document1216.html]},
	pages = {115--132},
	title = {{La} flexion de l’adjectif en allemand: {{L}}a morphologie de haut en bas},
	volume = {28},
	year = {1999}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Schoorlemmer2009,
	address = {The Morphosyntax of DP.   Doctoral Dissertation, University of Leiden, the Netherlands [published by Utrecht},
	author = {Schoorlemmer, Erik},
	publisher = {LOT]},
	title = {Agreement, Dominance and Doubling},
	year = {2009}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleNewTimesRoman}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Schoorlemmer2012,
	author = {Schoorlemmer, Erik},
	journal = {\textit{The Journal of Comparative Germanic Linguistics}},
	number = {2},
	pages = {107--156},
	title = {Definiteness marking in {Germanic}: {{M}}orphological variations on the   same syntactic theme},
	volume = {15},
	year = {2012}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleNewTimesRoman}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Schrodt2004,
	address = {Tübingen},
	author = {Schrodt, Richard},
	publisher = {Max Niemeyer Verlag},
	title = {\textit{Althochdeutsche {Grammatik} {II}}},
	year = {2004}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\textmd{\textup{Schwartz, Bonnie D. \& Sten Vikner. 1996. The verb always leaves IP in V2 clauses. In Adriana   Belletti \& Luigi Rizzi (eds.)} }\textmd{Parameters and Functional Heads: Essays in Comparative   Syntax}\textmd{\textup{, pp. 11-62. New York: Oxford University Press.}}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Sigurðsson1989,
	author = {Sigurðsson, Halldór Ármann},
	note = {In a Comparative GB   Approach. Doctoral Dissertation, University of Lund.},
	title = {Verbal Syntax and Case in {Icelandic}},
	year = {1989}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Sigurðsson2006,
	author = {Sigurðsson, Halldór Ármann},
	journal = {\textit{Arkiv för nordisk   filologi}},
	pages = {193--236},
	sortname = {Sigurdsson, Halldor Armann},
	title = {The {Icelandic} Noun Phrase: {{C}}entral traits},
	volume = {121},
	year = {2006}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{SigurðssonWood2020,
	author = {Sigurðsson, Halldór Ármann, and Jim Wood},
	journal = {\textit{Glossa: A Journal of General Linguistics} 5(1):},
	note = {).},
	pages = {1--16},
	sortname = {Sigurdsson, Halldor Armann, and Jim Wood},
	title = {“{We} Olaf”: {{P}}ro[(x-){XP}] constructions in   {Icelandic} and beyond},
	volume = {16},
	year = {2020}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Starke2009,
	author = {Starke, Michal},
	journal = {A short primer to a new approach to language. \textit{Nordlyd}},
	number = {1},
	pages = {1–6},
	title = {Nanosyntax},
	volume = {36},
	year = {2009}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Steinmetz2001,
	address = {Frankfurt am Main},
	author = {Steinmetz, Donald},
	booktitle = {\textit{New   Insights in {Germanic} {II}}},
	editor = {Irmengard Rauch and Gerald F. Carr},
	pages = {201--224},
	publisher = {Verlag Peter Lang},
	title = {The Great Gender Shift and the attrition of neuter nouns in {West}   {{German}ic}: {{T}}he example of {German}},
	year = {2001}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Sternefeld2004,
	address = {Berlin},
	author = {Sternefeld, Wolfgang},
	booktitle = {\textit{Explorations in Nominal Inflection}},
	editor = {Gereon  Müller, Lutz Gunkel, and Gisela Zifonun},
	pages = {269--299},
	publisher = {Mouton de Gruyter},
	title = {Feature Checking, Case, and Agreement in {German} {DP}s},
	year = {2004}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Sternefeld2008,
	address = {Eine morphologisch motivierte generative Beschreibung   des Deutschen}. Vol I. Tübingen},
	author = {Sternefeld, Wolfgang},
	publisher = {Stauffenberg},
	title = {\textit{Syntax},
	year = {2008}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Stowell1989,
	address = {Chicago},
	author = {Stowell, Tim},
	booktitle = {\textit{Alternative Conceptions of Phrase Structure}},
	editor = {Mark R. Baltin and Anthony S. Kroch},
	pages = {232--262},
	publisher = {The University of Chicago Press},
	title = {Subjects, Specifiers, and X-Bar Theory},
	year = {1989}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Stowell, Tim. 1991. Determiners in NP and DP. In Katherine Leffel \& Denis Bouchard (eds.) \textit{Views on Phrase Structure}, pp. 37-56, Kluwer, Netherlands.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Studler2011,
	address = {\textit{Artikelparadigmen},
	author = {Studler, Rebekka},
	note = {} Doctoral Dissertation,\textbf{   }University of Zurich.},
	publisher = {Form, Funktion und syntaktisch-semantische   Analyse von definiten Determinierern im Schweizerdeutschen},
	year = {2011}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Stump2001,
	address = {A Theory of Paradigm Structure}. Cambridge},
	author = {Stump, Gregory T.},
	publisher = {Cambridge University Press},
	title = {\textit{Inflectional Morphology},
	year = {2001}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Svenonius, Peter. 1993. The Structural Location of the Attributive Adjective. In Erin Duncan,   Donka Farkas \& Philip Spaelti (eds.) \textit{The Proceedings of the Twelfth WCCFL}, pp. 439-  454. Stanford Linguistics Association.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{SwartZwarts2005,
	address = {Nijmegen},
	author = {Swart, Henriëtte de, Yoad Winter, and Joost Zwarts},
	booktitle = {\textit{Proceedings of Sinn \& {Bedeutung} 9},   pp},
	editor = { Emar Maier, Corien Bary and Janneke Huitink},
	pages = {446--460},
	publisher = {NCS},
	sortname = {Swart, Henriette de, Yoad Winter, and Joost Zwarts},
	title = {Bare Predicate Nominals in {Dutch}},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{SwartZwarts2007,
	address = {\textit{Natural Language \& Linguistic Theory} 25 \REF{ex:key:1}},
	author = {Swart, Henriëtte de, Yoad Winter, and Joost Zwarts},
	publisher = {195-222},
	sortname = {Swart, Henriette de, Yoad Winter, and Joost Zwarts},
	title = {Bare Nominals and Reference to   Capacities},
	year = {2007}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Szabolsci, Anna. 1994. The Noun Phrase. In Ferenc Kiefer \& Katalin É. Kiss (eds.) \textit{Syntax and   Semantics 27. The Syntactic Structure of Hungarian}, pp. 179-274, San Diego: Academic   Press.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Tappe1989,
	address = {Amsterdam},
	author = {Tappe, Hans-Thilo},
	booktitle = {\textit{Syntactic Phrase Structure Phenomena in Noun Phrases   and Sentences}},
	editor = {Christa Bhatt, Elisabeth  Löbel and Claudia Schmidt},
	pages = {159--179},
	publisher = {John Benjamins Publishing Company},
	title = {A Note on Split Topicalization in {German}},
	year = {1989}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Taraldsen1990,
	address = {Dordrecht},
	author = {Taraldsen, Knut Tarald},
	booktitle = {\textit{Grammar in Progress. {{G}}low Essays for Henk van Riemsdijk}},
	editor = {Joan Mascaró  and Marina Nespor},
	pages = {419--431},
	publisher = {Foris Publications},
	title = {D-projections and N-projections in {Norwegian}},
	year = {1990}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Thráinsson1994,
	address = {New York},
	author = {Thráinsson, Höskuldur},
	booktitle = {  \textit{The {Germanic} Languages}},
	editor = {Ekkehard König and Johan van der Auwera},
	pages = {142--89},
	publisher = {Routledge},
	sortname = {Thrainsson, Hoskuldur},
	title = {{Icelandic}},
	year = {1994}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Thráinsson2007,
	address = {Cambridge},
	author = {Thráinsson, Höskuldur},
	publisher = {Cambridge University Press},
	sortname = {Thrainsson, Hoskuldur},
	title = {\textit{The Syntax of {Icelandic}}},
	year = {2007}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@misc{Trommer2005,
	author = {Trommer, Jochen},
	note = {Paper presented at Linguistischer Arbeitskreis, \citealt{Köln2005}.},
	title = {Die formale Repräsentation von {Markiertheit} in der Flexion des   {Deutschen}},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Vater1982,
	address = {Tübingen},
	author = {Vater, Heinz},
	booktitle = {\textit{{Sprachtheorie} und Angewandte {Linguistik}: {{F}}estschrift für Alfred Wollmann zum 60. {{G}}eburtstag}},
	editor = {Werner Welte},
	pages = {67--74},
	publisher = {Narr},
	title = {Der „unbestimmte“ Artikel als Quantor},
	year = {1982}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Vater1984,
	author = {Vater, Heinz},
	journal = {\textit{Zeitschrift für Sprachwissenschaft}},
	number = {1},
	pages = {19--42},
	title = {Determinantien und Quantoren im {Deutschen}},
	volume = {3},
	year = {1984}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Vater1985,
	address = {Osnabrück},
	author = {Vater, Heinz},
	booktitle = {\textit{{Deutsche} {Grammatik} {II}}. \textit{Osnabrücker {Beiträge} zur {Sprachtheorie}} 30},
	editor = {Angelika Redder},
	pages = {107--126},
	publisher = {Universität Osnabrück},
	title = {Determinantien und Pronomina},
	year = {1985}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Vater1991,
	address = {Tübingen},
	author = {Vater, Heinz},
	booktitle = {\textit{{DET}, {COMP} und {INFL}. {{Z}}ur Syntax funktionaler Kategorien und grammatischer Funktionen}},
	editor = {Susan Olsen and Gisbert Fanselow},
	pages = {15--34},
	publisher = {Niemeyer},
	title = {Determinatien in der {DP}},
	year = {1991}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Vater2000,
	address = {Tübingen},
	author = {Vater, Heinz},
	booktitle = {\textit{{Deutsche} {Grammatik} in Theorie   und Praxis}},
	editor = {Rolf Thieroff,  Matthias Tamrat, Nanna Fuhrhop and Oliver Teuber},
	pages = {185--99},
	publisher = {Max Niemeyer Verlag},
	title = {“Pronominantien” – oder: {{P}}ronomina sind Determinantien},
	year = {2000}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Vater, Heinz. 2002. The word class ‘Article.’ In D. Alan Cruse, Franz Hundsnurscher, Michael   Job \& Peter Rolf Lutzeier (eds.) \textit{Lexikologie: Ein internationales Handbuch zur Natur   und Struktur von Wörtern und Wortschätzen}, (Vol. 1), pp. 621-28. Berlin: de Gruyter.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Velde2011,
	address = {Amsterdam},
	author = {Velde, Freek van de},
	booktitle = {\textit{The Noun Phrase in {Romance} and {Germanic}: {{S}}tructure, variation,   and change}},
	editor = {Petra Sleeman and Harry Perridon},
	pages = {241--256},
	publisher = {John Benjamins},
	title = {Anaphoric adjectives becoming determiners},
	year = {2011}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{VergnaudVergnaud1992,
	author = {Vergnaud, Jean-Roger and Maria Luisa Zubizaretta},
	journal = {\textit{Linguistic Inquiry}},
	number = {4},
	pages = {595--652},
	title = {The Definite Determiner and the Inalienable Constructions in {French} and {English}},
	volume = {23},
	year = {1992}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Vogel2006,
	author = {Vogel, Petra M.},
	journal = {\textit{Zeitschrift für Dialektologie und Linguistik}},
	number = {2},
	pages = {176--193},
	title = {"Ich hab da nen kleines Problem!" Zur neuen Kurzform des indefiniten   Artikels im {Deutschen}},
	volume = {73},
	year = {2006}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Watanabe2006,
	author = {Watanabe, Akira},
	journal = {\textit{Natural Language and Linguistic Theory}},
	number = {1},
	pages = {241--306},
	title = {Functional Projections of Nominals in {Japanese}: {{S}}yntax of Classifiers},
	volume = {24},
	year = {2006}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Wegener1995,
	address = {Tübingen},
	author = {Wegener, Heide},
	publisher = {Niemeyer},
	title = {\textit{Die Nominalflexion des {Deutschen} – verstanden als Lerngegenstand}},
	year = {1995}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Weinreich.Uriel1999,
	address = {(6\textsuperscript{th} ed.) New York City},
	author = {Weinreich. Uriel},
	publisher = {YIVO Institute for Jewish Research},
	title = {\textit{College {Yiddish}}},
	year = {1999}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Wesener, Thomas. 1999. Production strategies in German spontaneous speech: definite and   indefinite articles. In John J. Ohala, Yoko Hasegawa, Manjari Ohala, Daniel Granville,   and Ashlee C. Bailey (eds.) \textit{14\textsuperscript{th}}\textit{~International Congress of Phonetic Sciences}, 687-690.   San Francisco, CA. (http://www.internationalphoneticassociation.org/icphs/icphs1999)
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Wiese1996,
	address = {Amsterdam/Philadelphia},
	author = {Wiese, Bernd},
	booktitle = {\textit{Theoretical Linguistics and Grammatical Description. {{P}}apers in   Honour of {Hans}-Heinrich Lieb. {{O}}n the occasion of his 60th birthday}. [Current Issues in   Linguistic Theory 138]},
	editor = { Sackmann, Robin},
	pages = {323--344},
	publisher = {Benjamins},
	title = {Iconicity and Syncretism. {{O}}n Pronominal Inflection in Modern {German}},
	year = {1996}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Wiese1999,
	address = {Unterspezifizierte Paradigmen},
	author = {Wiese, Bernd},
	note = {\textit{Linguistik Online 4, 3/99}.},
	publisher = {Form und Funktion in der pronominalen   Deklination},
	year = {1999}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Wiese, Bernd. 2009. Variation in der Flexionsmorphologie: Starke und schwache   Adjektivflexion nach Pronominaladjektiven. In Konopka, Marek \& Bruno Strecker   (eds.)~\href{https://pub.ids-mannheim.de/laufend/jahrbuch/jb2008.html}{{\textit{Deutsche Grammatik – Regeln, Normen, Sprachgebrauch}}}. [\href{https://pub.ids-mannheim.de/laufend/jahrbuch/}{{Jahrbuch des Instituts   für deutsche Sprache}}~2008], 166-194. Berlin/New York: de Gruyter.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{WieseWiese2005,
	author = {Wiese, Heike and Joan Maling},
	journal = {\textit{Journal of Germanic Linguistics}},
	number = {1},
	pages = {1--38},
	title = {Beers, kaffi, and schnaps: {{D}}ifferent grammatical options for   Restaurant Talk coercions in three {Germanic} languages},
	volume = {17},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Wiese1988,
	author = {Wiese, Richard},
	journal = {\textit{Wiener   Linguistische Gazette}, Beiheft [Supplement]},
	pages = {32--4},
	title = {The Proper Treatment of Inflection in the {German} Article System},
	volume = {7},
	year = {1988}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Wiese1996a,
	address = {Oxford},
	author = {Wiese, Richard},
	publisher = {Oxford University Press},
	title = {\textit{The Phonology of {German}}},
	year = {1996}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleFooter}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Wiese1996b,
	author = {Wiese, Richard},
	journal = {\textit{Linguistic Inquiry}},
	number = {1},
	pages = {183--93},
	title = {Phrasal Compounds and the Theory of Word Syntax},
	volume = {27},
	year = {1996}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{indent}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Williams1982,
	author = {Williams, Edwin S.},
	journal = {\textit{Linguistic   Inquiry}},
	number = {1},
	pages = {160--3},
	title = {Another Argument that Passive is Transformational},
	volume = {13},
	year = {1982}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{indent}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{\textmd{\textup{Wiltschko1998,
	author = {\textmd{\textup{Wiltschko, Martina},
	journal = {On the Syntax and Semantics of (Relative) Pronouns and   Determiners.} }\textmd{Journal of Comparative Germanic Linguistics}\textmd{ \textup{},
	note = {.}}},
	pages = {143--181},
	sortname = {\textmd{\textup{Wiltschko, Martina},
	volume = {2},
	year = {1998}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{styleSubtitle}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Winter2005,
	author = {Winter, Yoad},
	journal = {\textit{Lingua}},
	pages = {767--786},
	title = {On some problems of (in)definiteness within flexible semantics},
	volume = {115},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Wood2007,
	address = {Amsterdam},
	author = {Wood, Johanna L.},
	booktitle = {\textit{Nominal determination. {{T}}ypology, context constraints, and   historical emergence}},
	editor = {Elisabeth Stark, Elisabeth Leiss,  and Werner Abraham},
	pages = {339--361},
	publisher = {John Benjamins},
	title = {Demonstratives and possessives},
	year = {2007}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Wood, Johanna L. \& Sten Vikner. 2011. Noun phrase structure and movement. A cross-linguistic   comparison of \textit{such}/\textit{sådan}/\textit{solch} and \textit{so}/\textit{så}/\textit{so}. In Petra Sleeman \& Harry Perridon (eds.)   \textit{The Noun Phrase in Romance and Germanic: Structure, variation, and change}, pp. 89-  109. Amsterdam: John Benjamins.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Wood, Johanna \& Sten Vikner. 2013. What’s to the left of the indefinite article? – Et sådan et   spørgsmål er svært at svare på. In \textit{Gode ord er bedre end guld}. Festskrift til Henrik   Jørgensen, Simon Borchmann, Inger Schoonderbeek Hansen, Tina Thode Hougaard, Ole   Togeby \& Peter Widell (eds.), 515-540. Aarhus: Aarhus University.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

Wunderlich, Dieter. 1997. Der unterspezifizierte Artikel. In C. Dürscheid, K. H. Ramers \& M.    Schwarz (eds.) \textit{Sprache im Fokus}, 47-55. Niemeyer, Tübingen,.
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\begin{indent}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Zamparelli2000,
	address = {New York},
	author = {Zamparelli, Roberto},
	publisher = {Garland Publishing},
	title = {\textit{Layers in the Determiner Phrase}},
	year = {2000}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples
\end{indent}
\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@article{Zamparelli2005,
	author = {Zamparelli, Roberto},
	journal = {\textit{Lingua}},
	pages = {759--766},
	title = {Introduction: {{S}}ome questions about (in)definiteness},
	volume = {115},
	year = {2005}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Zamparelli2008,
	address = {Amsterdam},
	author = {Zamparelli, Roberto},
	booktitle = {\textit{Essays on Nominal Determination. {{F}}rom morphology to   discourse management}},
	editor = {Henrik Høeg  Müller and Alex Klinge},
	pages = {101--130},
	publisher = {John Benjamins},
	title = {Bare predicate nominals in {Romance} languages},
	year = {2008}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Zeijlstra2011,
	address = {\textit{The Journal   of Comparative Germanic Linguistics} 14 \REF{ex:key:2}},
	author = {Zeijlstra, Hedde},
	publisher = {111-138},
	title = {On the syntactically complex status of negative indefinites},
	year = {2011}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@incollection{Zhang2012,
	address = {Oxford},
	author = {Zhang, Niina Ning},
	booktitle = {\textit{Count and mass across languages}},
	editor = {Diane  Massam},
	pages = {220--237},
	publisher = {Oxford   University Press},
	title = {Countability and numeral classifiers in {Mandarin} {Chinese}},
	year = {2012}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{ZifonunZifonun1997,
	address = {Berlin},
	author = {Zifonun, Gisela, Ludger Hoffmann and Bruno Strecker},
	publisher = {Mouton de Gruyer},
	title = {\textit{{Grammatik} der deutschen {Sprache}}},
	year = {1997}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Zimmermann2011,
	address = {\textit{The Journal of Comparative Germanic   Linguistics} 14 \REF{ex:key:3}},
	author = {Zimmermann, Malte},
	publisher = {203-40},
	title = {On the functional architecture of {DP} and the feature content of   pronominal quantifiers in Low {German}},
	year = {2011}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

@book{Zwart1997,
	address = {A Minimalist Approach to the   Syntax of Dutch}. Dortrecht},
	author = {Zwart, C. Jan-Wouter},
	publisher = {Kluwer Academic Publishers},
	title = {\textit{{Morphosyntax} of Verb Movement},
	year = {1997}
}

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\ea
\gll \\
\\
\glt
\z % you might need an extra \z if this is the last of several subexamples

\end{verbatim}
\sloppy\printbibliography[heading=subbibliography,notkeyword=this]
\end{document}